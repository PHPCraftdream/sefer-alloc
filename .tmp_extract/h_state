//! The [`AllocCore`] NUMA-cluster state accessors — the
//! [`AllocCore::NUMA_NODE_REFRESH_PERIOD`] periodic-refresh constant,
//! [`AllocCore::current_node_cached`],
//! [`AllocCore::invalidate_numa_node_cache`] and their `dbg_*` seams — plus
//! the table-query fns ([`AllocCore::segment_bases`],
//! [`AllocCore::contains_base`], [`AllocCore::small_cur`],
//! [`AllocCore::classify`]). Mechanically split out of the former flat
//! `alloc_core.rs` body (the face group's 1000-line cap); the bodies are
//! byte-identical to their pre-split forms.

#[cfg(feature = "numa-aware")]
use crate::alloc_core::numa;
use crate::alloc_core::size_classes::{AllocKind, SizeClasses};

use super::AllocCore;

