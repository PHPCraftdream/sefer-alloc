# ADR: `tagged-index-stack` doc consolidation and review-history extraction (P3-5)

- Status: **ACCEPTED.** Doc-consolidation decision for the `tagged-index-stack` publish-readiness campaign; authorizes de-duplicating rustdoc/README/CHANGELOG/test-doc narratives into one canonical location per narrative, with this ADR as the durable record of everything the trims remove.
- Source: `docs/reviews/2026-09-01-124216-tagged-index-stack-review-Sol-codex-run-4.md`, finding **P3-5** ("объём объяснений маскирует отсутствие структурной гарантии" — the volume of explanation was the round-10..15 remedy for a defect that needed a structural one; now that the structural fix has landed, the accumulated explanation volume must be consolidated, not scattered).
- Author: part of the `tis-sol4` Wave-3 doc-diet task (worktree/branch `tis-sol4-w3-docdiet`), 2026-09-01.
- Companion decision record: `docs/adr/2026-09-01-tagged-index-stack-storage-binding-closure.md` (Wave-2) — the `unsafe trait StackStorage` / Group-A sealing decision this ADR's relocated-fact ledger leans on. That ADR is not modified here.

## Context: why consolidation, and why now

The crate went through ~13 review rounds in four days. Each round's remediation edited rustdoc, README, CHANGELOG, and test docs in parallel, so the same narrative (shared-storage hazards, panic causes, MSRV, backoff tradeoffs) accreted on every surface, each copy stamped with its originating round. Duplication regrew twice even after consolidation attempts:

- Round 13's single-sourcing of the shared-storage hazard statement into the `StackStorage` trait doc: commit `92d0182` ("one canonical shared-storage hazard statement in StackStorage's trait doc").
- Round 15 / Wave-1's README-to-canonical-rule-3 sync: commit `ee47233` ("sync README to canonical rule 3, two-shape guard, bounded loom scope").

Both syncs went stale again. **OBSERVED drift in the current tree** (all verified 2026-09-01):

- `crates/tagged-index-stack/README.md:60` still says "link cells must not be shared between stacks meant to be independent" — the pre-round-15 wording of rule 3. The canonical restatement (`src/imp.rs`, `StackStorage` rule 3, ~lines 673-686) is a REACHABILITY invariant in which cell sharing per se is harmless for disjoint index populations.
- `crates/tagged-index-stack/README.md:64` says anyone who can read a live `ArrayIndexStack` "can take its `.head()`" — false since the Group-A sealing commit `a205d75`, which removed `ArrayIndexStack` from the public `StackStorage` trait.
- `crates/tagged-index-stack/README.md:68` cites the runtime test `array_index_stack_head_still_double_issue` — the test deleted/converted by the same Group-A change (`a205d75`); per the Wave-2 ADR it is now a compile-fail oracle, not a compiling demonstration.
- `crates/tagged-index-stack/README.md:325-328` and `crates/tagged-index-stack/CHANGELOG.md:374-377` state MSRV 1.81; `crates/tagged-index-stack/Cargo.toml:28` declares `rust-version = "1.79"` (Sol-codex run-4 P2-3, fixed by commit `0a37f33` in the manifest but not propagated to README/CHANGELOG).

Root cause: narrative placed on more than one surface with no single-owner rule. Consolidation is therefore not a one-time cleanup but a policy: exactly one canonical location per narrative, everything else a pointer, and this ADR as the safety net holding any fact the trims remove.

## Review-era history table

Round-10..15 `tagged-index-stack` review docs are NOT present under `docs/reviews/` (verified by glob); rounds 10-15 below are reconstructed from commit subjects and the crate's own CHANGELOG/test-doc round references, and are marked accordingly. Rounds 3-9 and the Sol-codex runs are cited from committed review documents.

| Round / review | Evidence source | Headline findings | Remediation (verified commits) |
|---|---|---|---|
| Round 3 (`@oh`) | `docs/reviews/2026-08-30-2243-tagged-index-stack-review-round3-oh.md` | P1-1 loom as dev-dependency exposure; P2-1/P2-2 loom model oracle gaps; P3-6 `lib.rs` 73% comments; P3-7 README claim accuracy | (pre-history of this crate's extraction; not re-derived) |
| Sol-codex run 1 | `docs/reviews/2026-08-30-181147-tagged-index-stack-review-Sol-codex.md` | P1-1 safe-API `Links`-swapping double-issue; P1-2 "defeats ABA" overclaim; P2-2 silent `pack` truncation; P3-5 manual compile-fail gap | run-2 doc records these as processed |
| Round 4 (`@oh`) | `docs/reviews/2026-08-31-025356-tagged-index-stack-review-round4-oh.md` | P2-1 loom activation docs; P2-2 `scripts/loom.mjs` provenance; P3-5 rule-4 silent miss | (interim polish commits) |
| Round 5 (`@oh`) | `docs/reviews/2026-08-31-053435-tagged-index-stack-review-round5-oh.md` | P2-1 `PUSH_RETRY_COUNT` activation; P3-7 ~29% of CHANGELOG is review-process archaeology | (interim polish commits) |
| Round 6 (`@oh`) | `docs/reviews/2026-08-31-072911-tagged-index-stack-review-round6-oh.md` | P2-1 no CAS-retry backoff, measured 7.9×-9.2× contention collapse; P3-5 comment density | backoff work lands round 7-8 |
| Round 7 (`@oh`) | `docs/reviews/2026-08-31-100751-tagged-index-stack-review-round7-oh.md` | P2-1 `BACKOFF_SPIN_CAP=6` published fairness ratio; P2-2 zero committed multi-threaded tests; P4-7 wrong `BACKOFF_SPIN_CAP` doc citation | `11b6833` (retry activation oracles), `154d9a9`, `10811fb` |
| Round 8 (`@oh`) | `docs/reviews/2026-08-31-125420-tagged-index-stack-review-round8-oh.md` | P2-1 cap-sweep §3.2 drops fairer caps 0/4; P2-2 per-call latency framing; P3-5 loom-shim fidelity overclaim | `32bde89` (pop guard `track_caller`/receipt), `a1f9dc5`, `fed6f3f`, `5712436` |
| Round 9 (`@oh`) | `docs/reviews/2026-08-31-130017-tagged-index-stack-review-round9-oh.md` | P2-1 summary CSV structural issues; P2-2 §3.4 tail-mass cherry-pick; P2-3 starvation-free warning placement | `9b4bc4d`, `c6859b9`, `45ca04d`, `0ffc9df` |
| Round 10 (`@oh`) — RECONSTRUCTED from commit subjects + CHANGELOG | git log (`9a8a68c`, `10bb387`, `081080b`, `338ef34`, `3cd5894`, merges `8a3c596`/`42e2480`/`0fbe6d7`) | P2-1 packaged-suite skips; P2-2/P2-3/P3-1/P3-2 overclaimed double-issue docs + pre-publish `try_pack` deleted; P3-3/P3-4 stale citations; P3-6/P3-7 perf-doc restoration + CHANGELOG collapse | `9a8a68c`, `10bb387`, `081080b`, `2bb71d5`, `5c0fbcc`, `338ef34` |
| Round 11 (`@oh`) — RECONSTRUCTED | git log (`5f854af`, `b5c111b`, `7c4c205`, merges `932bb79`/`4f4a540`) | P2-1/P2-2 published-package skip guards (compile-fail needs packaged context, hardened against broken checkout); P3-2; P3-3/P3-4 binding overclaims; P4 bundle | `5f854af`, `b5c111b`, `7c4c205`, `ca40f4d`, `2778ddf` |
| Round 12 (`@oh`) — RECONSTRUCTED | git log (`ad28248`, merge `de80ca1`) | P2-1/P3-3/P3-4 shared-head overclaims and `empty()` attribution | `ad28248`, `37015d2`, `a9b5420` |
| Round 13 (`@oh`) — RECONSTRUCTED | git log (`dee2e48`, `92d0182`, `665a6a0`, merge `9224e79`); CHANGELOG:322-327, :374 | P2-1/P2-2 self-loop detector added to `pop_index` — **first runtime behavior change of the campaign** (`dee2e48`); P3-3 MSRV re-derivation 1.88→1.80→1.81 (`01b1a46`, `665a6a0`); hazard narrative single-sourced into the trait doc (`92d0182`) | `dee2e48`, `92d0182`, `665a6a0`, `154d9a9`, `9883e9f` |
| Round 14 (`@oh`) — RECONSTRUCTED | git log (`58cfdf1`, `baf91c3`); CHANGELOG:327 ("round-14 correction") | two-cause disjunction of the self-loop panic (double-push vs. foreign-writer answer — the original "proves a foreign writer" wording was the error); falsifiable grep recipe fixed; stale trait-doc example replaced; link-cell contract synced | `58cfdf1`, `baf91c3` |
| Round 15 (`@oh`) — RECONSTRUCTED | git log (`2135d21`, `90a01ed`, `ee47233`) | P3-3/P3-4 hazard inventory corrected + extended to **4 binding-level shapes**; rule 3 restated as reachability; two-cause disjunction single-sourced; **3 new pinning tests**; P2-1 backoff tradeoff framed honestly at all 3 doc sites (`90a01ed`); README canonical sync (`ee47233`) | `2135d21`, `90a01ed`, `ee47233`, `7c7ffe5` |
| Sol-codex run 3 | `docs/reviews/2026-08-31-162115-tagged-index-stack-review-Sol-codex-run-3.md` (reviewed `45ca04d`) | P1-1 safe-API backing-swap double-issue; P1-2 ABA honesty; P2-1 public `pack` silent truncation; P2-4 advertised fail-fast cfg diagnostics; P3-6 `INDEX_BITS` bound | P1-1 → `f368448` (storage-trait redesign, one implementor owns the head↔links binding); P1-2 → `ab6a2ed`; P2-1 → `02df31d`; P2-4 → `db8bb77`; P3-6 → `e252b19` |
| Sol-codex run 4 | `docs/reviews/2026-09-01-124216-tagged-index-stack-review-Sol-codex-run-4.md` (reviewed `90a01ed`) | P1-1 Group A seal (`a205d75`) + Group B `unsafe trait` (`0486e90`) — see the Wave-2 ADR; P1-2 compile-fail oracle proves what it claims (`0472f29`); P2-3 MSRV corrected to declared-measured floor 1.79 (`0a37f33`); P3-1/P3-2 weak-memory gate — measurement NULL/pending, no ordering change (`d718e8b`); P2-1/P2-2 README contract contradictions; P3-5 **this task**; P3-3/P3-4 timing probes (`17cbde5`) | `a205d75`, `0486e90`, `0472f29`, `0a37f33`, `d718e8b`, `17cbde5` |

Round-by-round notes (beyond the table), for the rounds whose committed review docs exist:

- Round 3 (`docs/reviews/2026-08-30-2243-tagged-index-stack-review-round3-oh.md`, revision `4836a01`): P1-1 — `loom` shipped as a non-dev dependency exposure; P2-1/P2-2 — loom model oracle gaps (models don't assert their target invariant); P3-1 measured ABA wrap-time derivation; P3-2/P3-3/P3-4 pop/push ordering and assert-precision; P3-6 `src/lib.rs` 73% comments; P3-7 README "three hard-won" count wrong; P3-9 published loom-suite description mismatch.
- Sol-codex run 1 (`docs/reviews/2026-08-30-181147-tagged-index-stack-review-Sol-codex.md`, revision `57f8da4`, Russian-language static audit, NO-GO): P1-1 — safe API allows different `Links` instances between calls, deterministically destroying the stack; P1-2 — "defeats ABA" overstated for the supported 32-bit tag; P2-2 — public sharp-edged `pack` with silent truncation; P2-4 — no crate-scoped clippy gate; P3-5 — known compile-fail gap left manual. Run 2 (`docs/reviews/2026-08-31-090022-tagged-index-stack-review-Sol-codex.md`, revision `e70b2d9`, NO-GO) records the P1-1 blocker as still open post-remediation — the direct ancestor of run 3's P1-1 and run 4's P1-1.
- Round 4 (`...round4-oh.md`, revision `8027d9a`): P2-1 loom activation docs; P2-2 `scripts/loom.mjs` provenance; P3-2 the "50.75 ns" perf-number provenance; P3-5 rule 4 silently missing a shape; P3-8 `docs/correctness-open-items` cross-refs. Round 5 (`...round5-oh.md`, revision `d944b89`): P2-1 `PUSH_RETRY_COUNT` activation; P3-1 `ArrayLinks` contract; P3-7 ~29% of the published CHANGELOG is review-process archaeology — the campaign's first consolidation finding. Round 6 (`...round6-oh.md`, revision `b89796a`): P2-1 neither CAS-retry arm backs off (measured 7.9×–9.2× contention collapse) — the origin of the whole backoff saga; P3-1 `TAIL` rustdoc; P3-5 comment density; P4-8 CHANGELOG still `0.1.0 - Unreleased`. Round 7 (`...round7-oh.md`, revision `e92643f`): P2-1 `BACKOFF_SPIN_CAP=6` ships a published fairness ratio; P2-2 zero committed multi-threaded tests; P3-5 the crate's headline claim; P4-7 `BACKOFF_SPIN_CAP` doc cites the wrong source. Round 8 (`...round8-oh.md`, revision `10811fb`): P2-1 the cap-sweep report's §3.2 drops the two caps its own CSV shows are fairer; P2-2 per-CALL latency framing; P3-5 the loom shim's fidelity overclaim. Round 9 (`...round9-oh.md`, revision `a1f9dc5`): P2-1 the machine-readable artifact closing round-8 P3-3 is structurally flawed; P2-2 §3.4 cherry-picks tail-mass cells; P2-3 the "lock-free but not starvation-free" warning placement.

For rounds 10-15 the reconstruction evidence in the table is commit subjects (which name round number and finding IDs inline) plus the crate's own CHANGELOG and test-doc round references (`CHANGELOG.md:322`, `:327`, `:338`, `:374`; `tests/custom_storage_impl.rs:20-53`, `:191`, `:244`, `:273`). Their full review documents are NOT in `docs/reviews/` and their exact finding texts are therefore UNVERIFIED beyond those commit subjects and in-crate references.

## Decision record — the doc-consolidation principles

**1. Rustdoc carries the exact current contract only.** Long-lived source text (`src/**/*.rs`, README, test module docs) contains: safety/preconditions, the ordering proof, panic conditions, short examples. Nothing else. **No internal review-round identifiers ("round-13 @oh review P2-1", "Sol-codex run-4") in any long-lived source text.** Dated CHANGELOG entries under `## 0.1.0 - Unreleased` are the sanctioned home for round references, as history.

**2. Canonical-location map (single source of truth per narrative; every other mention is a short pointer).** Reviewer checklist — grep each location, expect the full statement there and nowhere else:

- a. Shared-storage hazard inventory (4 binding-level shapes) + detection coverage → `crates/tagged-index-stack/src/imp.rs`, `StackStorage` trait doc, section "The shared-storage hazard class".
- b. `# Safety` clauses (the implementor obligations — seven as of the storage-binding ADR's `push_index` addendum, which added clauses 6/7; five when this row was first written) → same trait doc's `# Safety` section.
- c. Link ordering contract/proof → same trait doc's "Ordering contract" section; implementation-side ordering comments in `push_index_impl`/`pop_index_impl` cover the crate's own atomics and cross-reference it.
- d. No-double-push caller contract → `StackOps::push_index`'s `# Safety` section (clause 2 — this superseded the "# Caller contract" heading name once `push_index` itself joined the unsafe boundary; see the storage-binding ADR's `push_index` addendum).
- e. Self-loop two-cause disjunction → `StackOps::pop_index`'s "# Panics".
- f. loom per-model breakdown → `tests/loom_aba.rs` module doc.
- g. Per-test status list (which test pins which shape/clause; guard fires vs. silent) → `tests/custom_storage_impl.rs` module doc.
- h. Decision history → this ADR, plus `docs/adr/2026-09-01-tagged-index-stack-storage-binding-closure.md` for the storage-binding decision.

**3. De-rounding policy for tests.** Review-round identifiers and finding IDs are removed from test module/function docs; the FACT each comment carried (what the test pins, why the guard exists) is restated without the identifier. Test function names are NOT renamed (they are load-bearing: cited in README, CHANGELOG, and the per-test status list). No assertion changes.

**4. CHANGELOG carve-out.** Dated `0.1.0-unreleased` entries may keep round references as history. Entries describing CURRENT behavior must be corrected when false. Three known corrections, all OBSERVED in the current tree: (i) `CHANGELOG.md:108-116`'s "still compile and still double-issue" — superseded by the unsafe-trait conversion (`0486e90`); (ii) the rule-3 "link cells must not be shared" wording — superseded by the reachability restatement (`2135d21`); (iii) the MSRV-1.81 sections (`CHANGELOG.md:374`, `README.md:325-328`) — superseded by the declared 1.79 (`Cargo.toml:28`, `0a37f33`).

## Relocated / obsolete fact ledger (the safety net)

Every fact below is being REMOVED from long-lived source text by the companion trims; this section is where each survives.

- **`# Stability` reversal narrative** (forbid→deny history; the E0053/`forbid` compile errors under the old policy; the owner's reversal): survives in the Wave-2 ADR §"The real cost of the `unsafe trait` option" and §"Final decision", plus `src/lib.rs`'s "Where unsafe lives" section (which keeps the current-state why). The **rejected `unsafe fn head()` alternative's full three-point design rationale** — restated here in full because rustdoc keeps only a one-line version (substance copied from the current trait doc, `crates/tagged-index-stack/src/imp.rs` ~944-957): (1) every known hazard is IMPLEMENTOR-side, never caller-side, so `unsafe fn` forces acknowledgment at the wrong site — implementors would signature-match `unsafe fn head()` with no contract acknowledgment at the `impl` site, the actual thing that needs forcing; (2) it falsely implies that *calling* `head()` is the dangerous act, when the danger is building on the returned reference; (3) it would reopen `unsafe` blocks inside the `StackOps` plumbing for no gain. Whole-trait `unsafe` with safe methods leaves exactly ONE `unsafe` token in the crate and ZERO `unsafe` blocks, and every `push_index`/`pop_index` caller (including sefer-alloc's registry) is untouched. (The compile errors `unsafe fn head()` once produced under `forbid` no longer apply but are recorded in the Wave-2 ADR.)
- **Rule-1 completeness audit method** — the literal grep recipes, with their "as of this writing" counts (source: `src/imp.rs` ~646-671; counts re-verified 2026-09-01 against the current file). Dated audit snapshot **2026-09-01**: `grep -n "^impl" crates/tagged-index-stack/src/imp.rs` → **10 impl blocks**, none adding a PUBLIC `&StackHead` accessor beyond `head` itself; `grep -n "derive(" crates/tagged-index-stack/src/imp.rs | grep -v "///"` → **3 hits** (lines 288, 1496, 1643), **all `Debug`**; `grep -nE -- "-> &.*StackHead" crates/tagged-index-stack/src/imp.rs | grep -v "///"` → **4 hits** (lines 980, 1185, 1195, 1614): `head`'s declaration plus three in the `pub(crate)` `SealedStorage` plumbing (sealed trait accessor, blanket bridge impl, `ArrayIndexStack`'s direct impl) — all three crate-internal only. Two routes to `&StackHead` from safe code exist (own a `StackHead` value; call `head()`), route 2 is closed under delegation (hence a value-level, not type-level, obligation), and no third route exists as of this writing. Known falsification limits of the recipes themselves: the `.*` pattern misses a rustfmt-wrapped return type; the fallback is reading the impl list.
- **README's 128-bit-CAS rejection rationale** (`crates/tagged-index-stack/README.md:201-213`, "Why the default is not a wider packed word") — restated here; README keeps a short pointer: a 128-bit CAS (e.g. `portable-atomic`, nightly intrinsic) was considered and explicitly rejected for the default packed word because (1) `loom` has no `AtomicU128`, so it would drop loom's coverage of the real type; (2) it adds an unsafe third-party dependency to a crate whose own code holds exactly one audited `unsafe` token; (3) `cmpxchg16b` is not in the x86-64 baseline, so `pop`'s read-only head observation would likely become an RMW on some targets. A genuine future need for >65535 indices in one pool should be a separate, explicitly opt-in feature-gated type — not a change to this default.
- **README's bench-receipt provenance transcription** (rustc 1.97.0, MINGW64 uname / `logical_cpus: 16` with no CPU model, the i7-11800H identification from the paired-A/B JSON, the 53.89 ns/parm sample spread — `crates/tagged-index-stack/README.md:163-167`): the underlying facts live in the cited committed artifact `docs/perf/_raw_tis_backoff_cap_sweep_run1.log` and `docs/perf/paired_ab_runs/2026-08-04T16-42-31-023Z.json` themselves. The ADR records only that the transcription was removed as duplication of the cited artifact; consult those files for the values.
- **Ordering-contract defence-in-depth decision + measurement state** (sources: `docs/perf/TIS_LINK_ORDERING_WEAK_CAS_GATE.md` §0 and the `d718e8b` commit message, both read 2026-09-01): link-cell Acquire/Release is RETAINED even though the head-publication proof would permit Relaxed on some accesses. P3-1 (link ordering → Relaxed): x86-64 codegen identical across variants; the aarch64 static delta is real (link `ldar`/`stlr` present in base, removable under Relaxed; base pop is 13→12 instructions, −7.7%, under `links_relaxed`); but wall-clock on real weak-memory silicon is UNMEASURED (OPEN_ITEMS item 62; a workflow_dispatch-only arm64 CI job `tis-weak-memory-wallclock-gate` was authored, never executed). P3-2 (`compare_exchange` → `compare_exchange_weak`): measured NULL — codegen-identical on aarch64 under both outlined-atomics and `+lse` lowerings on rustc 1.97.0/LLVM 22; the driver asserts the identity so a toolchain change reopens the question loudly. **No ordering change lands.** Zero `src/` changes in `d718e8b` (bench/docs only).
- **Round-relative phrasings removed from test docs** (e.g. `tests/custom_storage_impl.rs:33` "which had NO automated coverage before round 15"; the module doc's round-11/12/13/15 attributions at lines 20-53, 191, 244, 273): the provenance each phrase carried now lives in this ADR's history table above.

## Consequences

- **Exactly-one-full-exposition verification.** A reviewer greps the signature phrases — "shared-storage hazard class" (expect the full 4-shape inventory only in the `StackStorage` trait doc), the five-clause `# Safety` list, the two-cause self-loop disjunction, the no-double-push caller contract — and must find each full statement in exactly ONE place, with short pointers elsewhere. More than one full statement, or zero, is a violation of this ADR.
- **Drift regrowth is the known failure mode** — it happened after `92d0182` and again after `ee47233`. Any future edit that adds narrative to a second surface (duplicating a hazard explanation into README or a test doc, restating the Safety clauses, re-stamping round IDs into rustdoc) violates this ADR even if the added text is individually accurate. The fix is always: extend the canonical location, leave a pointer.
- This ADR is append-only for relocated facts: if a companion trim removes a fact not yet in the ledger above, the ledger must be extended in the same change, not the fact deleted.

## Addendum (2026-09-01, later the same day): the two "rule N" lists are consolidated into one canonical `# Safety` list (@fh review §4.1, the review's own most-important finding)

**What it closes.** This ADR's consolidation pass (P3-5) removed round-identifiers but left the reviewer-defense register untouched, and it never addressed a structural duplication in the one contract an `unsafe impl` author is required to read: `StackStorage`'s trait doc carried TWO independently numbered 5-item lists, both cited as "rule N" elsewhere in the crate, with DIFFERENT content under the same digits. The normative `# Safety` list (one live binding per head / one backing, consistently / disjoint reachable-index populations / valid answers from dedicated cells / same logical head every call) and a second numbered list under "The binding: what is structural, what is not" (one backing + ONE live implementor value / stable index↔cell mapping / coherence + a binding-level clause / `load_next` returns TAIL-or-valid / backing lifetime). Cross-references across src/, tests/, and README ("rule 3's binding-level clause", "rule 4's release-active guard", "rule 1's completeness note") mostly meant the SECOND list's numbers, and the tests had to translate between them (`tests/custom_storage_impl.rs` literally said "clause 2 — the coherence obligation the explanatory rules list as rule 3"). For an `unsafe trait`, the `# Safety` list is the text an implementor is obligated to read and satisfy; a second, differently-numbered list beneath it undermined exactly the clarity the `unsafe trait` conversion was meant to buy.

**The fix.** One numbered list survives: the `# Safety` section stays numbered (the normative contract). The second list became UNNUMBERED prose keyed to the `# Safety` clause numbers by name/description, not by a competing number; the synthesis mapping (the digits did not all coincide) is: old-list item 1 → clause 1's value-level discharge; item 5 → clause 1's whole-life half; item 2 + item 3's coherence half → clause 2's two halves; item 3's binding-level half → clause 3; item 4 → clause 4. Every cross-reference crate-wide now says "clause N" (or "`# Safety` clause N") pointing at the RIGHT surviving clause — in nearly every case the same digit it formerly cited, but now by virtue of the mapping rather than by accident. The tests' translation apparatus is gone. The hazard-inventory's "shape 1..4" numbering is a separate, consistently-referenced space and was deliberately untouched.

**Same pass (the register items this ADR's P3-5 explicitly did not finish).** Reviewer-defense register trims and code-restating-comment removal in `src/imp.rs` (`imp.rs` 1936 → 1887 lines, code lines unchanged; ALL-CAPS emphasis 105 → 46 in src/); register unified on plain/direct/technical in `src/lib.rs` + `README.md`; the measured figures (tag-width budget, lock-freedom/starvation trade) canonicalized in `src/lib.rs`'s crate doc with README collapsed to qualitative claims + pointers; review-round identifiers swept from the remaining package-shipped surfaces the original scope check did not cover (benches, examples, `Cargo.toml` — already clean from a prior task — and the compile-fail fixtures' `Cargo.toml`s + `hook_token_unconstructible/src/main.rs`); and the unreleased-0.1.0 CHANGELOG collapsed to final-state bullets (406 → 250 lines: editing-process narration removed, every fact preserved; the found-and-fixed history remains in this ADR and the storage-binding ADR).

**Canonical-location-map extension.** The map in this ADR's Decision record gains one row: measured tag-width-budget and lock-freedom/starvation FIGURES → `src/lib.rs`'s crate doc; `README.md` carries the qualitative claims and pointers (one README-local exception kept: the ~16 MiB RAD-1 bootstrap-first-touch provenance note, which exists nowhere else). No fact left the crate: every number cut from README was first moved into lib.rs; no contract sentence removed from `src/imp.rs` lacked an equivalent surviving statement.

**Landing commits** (branch `tis-fh-groupb`): item 1 (the consolidation itself) `8a79bce`; items 2/4/5 (register/stock-phrase/restating-comment trims) `ebe85ca`; items 3/8 (register + numeric dedup) `a3d5d32`; items 6/7 (identifier sweep + CHANGELOG collapse) `34cdb56`.

## Addendum (2026-09-02): `StackStorage` trait doc split into normative core + design/audit-guide addendum (P3-4, task #1842)

**What it closes.** Review run 8 (`docs/reviews/2026-09-02-180547-tagged-index-stack-review-Sol-codex-run-8.md`, finding P3-4) found the `StackStorage` trait rustdoc still ~466 physical lines despite this ADR's earlier P3-5 compression pass, and flagged the size itself as a maintenance hazard: the same run's P3-2 finding (a stale "clause 1 covers shape 4 only in spirit" hedge, fixed earlier in this task alongside this addendum) is direct evidence a doc this large can silently drift out of internal agreement between one edit and the next. This addendum executes the review's own recommended split: keep in the trait doc ONLY a short normative `# Safety`, the exact ordering contract, and a COMPACT hazard/detection-boundary table; move completeness-census proofs, dated grep-recipe steps, and historical/counterexample walkthroughs to this design/audit-guide addendum — extending this EXISTING ADR rather than creating a new document, per this crate's established doc-consolidation convention.

**Before/after.** `crates/tagged-index-stack/src/imp.rs`'s `StackStorage` trait doc comment block: **466 lines → 333 lines** (the contiguous `///` block immediately above `pub unsafe trait StackStorage<const INDEX_BITS: u32>`), a 133-line / ~28.5% reduction. No `# Safety` clause, `# Panics`/`# Errors` behavior, or ordering-contract requirement was shortened in a way that changes what it requires — every clause's actual obligation is byte-for-byte the same 7 numbered items it was before this pass. Two section headings were renamed for clarity (not referenced from outside `imp.rs` — verified by grep before renaming): "The binding: what is structural, what is not" → "The binding: structural vs. value-level obligations"; "The shared-storage hazard class: inventory and detection coverage" → "The shared-storage hazard class: detection boundary" (now containing a 4-row Markdown table — one row per shape, `Shape | Hazard | Runtime detection` — replacing the four full prose paragraphs).

**Relocated-fact ledger.** Every fact removed from the trait doc's prose, and where it survives:

- **Clause 4's out-of-range/EMPTY-sentinel corruption mechanism** (the `pack_truncating` masking example, `0x1_0000` at `INDEX_BITS = 16` packing to index `0`, the EMPTY-sentinel chain-leak) — MOVED, not archived: it now lives directly in [`StackOps::pop_index`]'s `# Panics` section (`crates/tagged-index-stack/src/imp.rs`, immediately after the self-loop panic condition), which already needed it — the pre-existing text there said "see that section for the two corruption modes," pointing at the very trait-doc paragraph this pass trimmed; leaving the pointer without its target would have been a real loss, not a compression, so the content moved to the pointer's actual destination instead of this ADR. This is a stronger canonical home than before: `pop_index`'s own `# Panics` is the ADR's existing canonical location (map item "e") for the self-loop two-cause disjunction this same paragraph sits beside.
- **The "unsafe fn boundary gates the crate's hooks, NOT the implementor's own storage" caveat** (an implementor exposing its own head through its own inherent API can still have the shape rebuilt against it) — RESTORED IN PLACE, not moved: an earlier draft of this split dropped it by accident during compression; caught during self-review (see "Mandatory methodology" step 2 in the task that produced this addendum) and restored as a sentence appended to "The three hooks are unsafe fn" section, where it fits thematically better than its original location.
- **Clause 1's "two implementor values, no clause's wording catches it" elaboration** (the full worked reasoning for why two individually-correct implementor values sharing one head is invisible to any per-impl audit) — the trait doc keeps the CONCLUSION ("no per-implementor audit sees the combination... discharge both by construction"); the full elaboration, restated here because the trait doc keeps only the compressed form:

  > A `StackStorage` implementor IS the backing: the `head()` it returns and the cells its `load_next`/`store_next` touch are bound through one `impl` block on one object. Two SEPARATE, individually coherent implementor values can each return a reference to the same `StackHead` while reading and writing DIFFERENT link storage — that still compiles (behind an `unsafe impl` asserting the contract it violates), and it reproduces this crate's original release-blocking double-issue hazard. No clause's wording catches it: every clause is stated per implementor, and each of the two values satisfies the clauses on its own terms. The hazard is two implementor values of possibly the same impl sharing one head — not multiple impl blocks existing — and the values are individually correct, so no per-impl audit can see the combination.

- **Clause 1's completeness-census proof** (the two routes to `&StackHead`, the dated grep-recipe snapshot, its falsification limits) — NOT newly moved by this pass: it already had its canonical home in this same ADR's "Relocated / obsolete fact ledger" section above (the "Rule-1 completeness audit method" entry, dated 2026-09-01), predating this task. The trait doc's paragraph was independently duplicating that existing ADR content; this pass just removed the duplicate and pointed at the pre-existing entry, closing a drift risk rather than creating a new relocation.
- **Shape 2's "why the inventory counts BINDINGS, not VALUES" reasoning specific to shape 2** (coherence allows only one `impl StackStorage<B>` per type, so a single value cannot carry two live same-width bindings, unlike shapes 3/4) — restated here in full, because the trait doc's surviving parenthetical states the GENERAL counting-unit rule but not this shape-specific corollary:

  > In practice the two bindings [of shape 2] are two implementor values — coherence allows only one `impl StackStorage<B>` per type, so a single value cannot carry two live same-width bindings — but the inventory's unit is the BINDING, not the value, because shapes 3 and 4 are reachable within a single value (shape 3) or across time with never more than one live value (shape 4).

- **Shapes 2, 3, and 4's full worked examples** (the `{0, 2, 4, 6}`/`{1, 3, 5, 7}` disjoint-population illustration for shape 3's general case; the width-16/width-12 dual-binding arithmetic — push `1, 2` at width 16, push `3` then re-push `1` at width 12, chains corrupt to `2 -> 1 -> 3 -> TAIL`/`1 -> 3 -> TAIL`, drains `2, 1, 3`/`1, 3`; the `let grown = Pool { head: old.head, links: ArrayLinks::new() }` shape-4 rebinding snippet and its first-pop-leaks/second-pop-panics effect) — NOT newly archived here: verified 2026-09-02 that EVERY one of these worked examples already has a more detailed, executable home in `tests/custom_storage_impl.rs`'s pinning tests and their doc comments — `one_value_two_bindings_shared_backing_still_double_issue`'s doc comment (lines ~547-565) carries the exact link-value arithmetic (`links[1] = TAIL`, `links[2] = 1`, the clobbering mechanism) in MORE detail than the trait doc's prose ever did; `head_moved_into_fresh_links_leaks_and_then_panics`'s body (lines ~521-544) IS the `let grown = Pool { ... }` snippet, as real compiled code with inline comments narrating the same first-pop/second-pop effect. The trait doc's compact table row is therefore a genuine compression, not a relocation-with-loss: the full detail was already living in the pinning tests before this pass, duplicated (not sourced) by the trait doc's prose.
- **The full per-shape/per-test "Detection coverage" paragraph** (which test pins which shape, guard-fires vs. silent, per shape) — NOT newly archived: `tests/custom_storage_impl.rs`'s own module doc (lines ~48-74) already carries the complete per-test status list — this is the ADR's PRE-EXISTING canonical-location-map item "g" ("Per-test status list... → `tests/custom_storage_impl.rs` module doc"), unchanged by this pass. The trait doc's paragraph was, again, an independent duplicate of an already-canonical location, not the location itself.

**Canonical-location-map update.** Item "a" in this ADR's Decision record now reads: Shared-storage hazard inventory (4 binding-level shapes) + detection coverage → a COMPACT summary (a 4-row table) in `crates/tagged-index-stack/src/imp.rs`, `StackStorage` trait doc, section "The shared-storage hazard class: detection boundary"; the full shape-by-shape worked examples live in the PINNING TESTS themselves (`tests/custom_storage_impl.rs`, per-test doc comments — see the relocated-fact ledger above), and the per-clause structural-vs-value-level elaboration this addendum could not compress into the trait doc without loss lives in this addendum.

**Verification performed before landing.** `grep -rn "The binding: what is structural\|shared-storage hazard class\|hazard-class inventory\|inventory shape" crates/ README.md CHANGELOG.md` confirmed the two renamed section headings are cross-referenced ONLY from within `imp.rs` itself (three self-references, all updated in the same change); every one of the seven pinning-test names named in the trimmed "Detection coverage" paragraph (`internally_disagreeing_storage_still_double_issue`, `two_implementor_values_sharing_one_head_still_double_issue`, `hand_crafted_acyclic_forgery_still_double_issues`, `two_stacks_sharing_link_storage_still_double_issue`, `one_value_two_bindings_shared_backing_still_double_issue`, `head_moved_into_fresh_links_leaks_and_then_panics`, `double_push_of_current_head_panics_on_first_pop`) was independently grepped and confirmed still named in `tests/custom_storage_impl.rs`'s module doc, `push_index`'s own doc, or `stack_unit.rs` before treating that paragraph's removal as loss-free.

**Landing commit:** part of the `tis-sol8-groupd` branch's P3-4 commit (this task, #1842) — see that commit's message for the exact SHA.
