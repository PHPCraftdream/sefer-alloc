# Correctness / CI-debt open items -- [T] Tracked tier -- crates.io publish-readiness: metadata, naming, dependencies, NO-GO audits

**Part of the split index.** This file holds the full text of every **[T]**
(tracked, not yet actioned) card whose subject matches this file's own
criterion (below). Start at `docs/CORRECTNESS_OPEN_ITEMS.md` for the
purpose/scope/convention header and the round-start reading order, and for
the complete item-number to file lookup table; come here for these specific
card bodies. See `docs/correctness-open-items/ACTIVE.md` for the **[A]**
tier, `docs/correctness-open-items/RESOLVED.md` for the closure trail, and
the sibling `[T]`-tier files (`TRACKED_hook_safety.md`, `TRACKED_verification_coverage.md`, `TRACKED_platform_contracts.md`, `TRACKED_ci_gate_coverage.md`, `TRACKED_test_flakiness.md`, `TRACKED_correctness_residuals.md`, `TRACKED_process_record.md`, `TRACKED_misc.md`) for the rest of
the tier.

**Criterion for this file:** A card belongs here if it is about a decision or blocker that gates a crate's crates.io publication -- naming/description/license/dependency one-way-door decisions, semver-coupling decisions, or a NO-GO verdict (and its blocking findings) from an independent pre-publication audit.

**Card count:** 27.

**Why split by theme, not by item-number range (task #1222, 2026-08-20):**
task #1221 (same day) split the former single `TRACKED.md` into four
number-range files, balanced by line count. The owner rejected that split
and asked for a thematic split instead -- grouping cards by what they are
actually ABOUT, derived from reading all 70 cards rather than assumed.
Every citation of this index that points at ONE SPECIFIC ITEM carries
that item's number, in the form `` `docs/CORRECTNESS_OPEN_ITEMS.md`
item N `` -- task #1227 repaired the seven in `aligned-vmem` that did
not, and two outside it were still open as of that task (both are
recorded in the thin index's Structure section). Citations that point
at the FILE as a whole, at a named SECTION, or at a CLASS of items
rather than one item carry no item number and never needed one (task
#1227's finding; until #1236 these headers overclaimed it as a
universal, asserting that no citation ever pointed at anything but
an item number). Only the numbered citations depend on where item
numbers live, and `docs/CORRECTNESS_OPEN_ITEMS.md` (the thin index)
carries the complete, mechanically generated item-N -> file lookup
table covering EVERY `[T]`-tier number (including the `59a`/`59b`
sub-items) that keeps them resolving -- that table, not this file's
name, is what makes the thematic split safe: the lookup is two-hop
(index table, then this file), but mechanical and always correct. No citing-file
count is typed in this header on purpose: the "42+" typed here at
the split was already 43 (census against the split commit) -- #1230
removed it from one of these nine headers, #1236 from the other
eight; compare against this command's output, never a hardcoded
count:

```text
git grep -l "docs/CORRECTNESS_OPEN_ITEMS\.md" -- ':!docs/' | wc -l
```

(Split 2026-08-20, task #1222, superseding task #1221's number-range
split the same day.)

---
24. **[T, filed 2026-08-06, task #627/S4, `docs/reviews/2026-08-06-sprint-closing-readonly-review.md` finding S4] `README.md:515` claims all 11 workspace members are "a real crates.io crate someone can `cargo add` on its own" — at least 3 are not published.**

    - **Status:** OPEN — not fixed. Documentation-only issue; no code
      change needed, only a README correction or a publish action.
    - **Current-number-or-verdict:** confirmed via crates.io API
      (`docs/plans/2026-08-05-release-execution-map.md`'s own
      [П]-verified table, independently re-confirmed by
      `docs/reviews/2026-08-05-fh-release-readiness-verification-review.md`):
      `racy-ptr-cell`, `size-classes`, and `tagged-index-stack` are NOT
      published on crates.io. As of commit `2a75d91` (task #648/P14),
      `.github/workflows/release.yml` gained tag patterns and
      `workflow_dispatch` dropdown options for all three, so the file now
      lists 8 crates: `aligned-vmem`, `sefer-region`, `malloc-bench-rs`,
      `numa-shim`, `racy-ptr-cell`, `size-classes`, `tagged-index-stack`,
      `sefer-alloc` — release-workflow plumbing exists for every crate this
      item originally flagged as missing it. This does NOT close the item:
      none of the three has actually been published to crates.io yet (the
      headline claim below is unchanged), and 3 more workspace members
      (`globalalloc-model`, `proc-memstat`, `proc-probe` — never checked
      against crates.io) still have no release-workflow entry at all.
      `README.md`'s crate-count table now lists 10, not 11, workspace
      members (the eleventh, `ring-mpsc`, was removed from the workspace
      entirely — user-requested, 2026-08-06, task #655: it had zero
      production consumers and its one filed use case, the in-tree
      `RemoteFreeRing`/`HeapOverflow` swap, was independently found NO-GO —
      see `docs/crate_extraction/CRATE_P4_FOLLOWUP_NOGO.md`. This shrinks,
      but does not close, this item's scope: one fewer unpublished crate to
      track, from 4 down to 3). `README.md`'s badge table now shows badges
      only for the remaining 10.
    - **Why filed instead of fixed here:** the fix depends on the same
      publish-DAG decision already deferred this sprint by explicit user
      instruction (tasks K3/K4/K9/L2/L3/L5, "path dependencies stay local
      for now, publish before release") — publishing the missing crates
      resolves it one way, rewriting the README claim resolves it the
      other way, and which one happens is a release-planning decision, not
      a code fix to make preemptively.
    - **Why this needed its own item:** the finding was already recorded in
      `docs/plans/2026-08-05-release-execution-map.md` (§"Не мои решения")
      and `docs/checkpoints/2026-08-06-0015.md`, but NEITHER file is
      consulted by CLAUDE.md's own "Round start: check BOTH open-items
      indexes" convention — only `docs/CORRECTNESS_OPEN_ITEMS.md` and
      `docs/perf/OPEN_ITEMS.md` are. Without this entry the finding would
      have been invisible to a future round despite being fully
      documented elsewhere — exactly the failure mode that convention
      exists to prevent (R18-8/R22-3 precedent, cited in this file's own
      "Round start" rule).
    - **Next trigger:** resolve as part of the deferred publish-DAG pass
      (K3/#598) — either publish the missing crates before 0.3.0 ships, or
      rewrite `README.md:515` to something like "ten
      independently-publishable building blocks; N of them are published
      on crates.io today" and remove badges for unpublished ones.
    - **Evidence:** `README.md:515`, `:545-555`;
      `docs/plans/2026-08-05-release-execution-map.md` §"Ход B" table and
      §"Не мои решения" item 4;
      `docs/reviews/2026-08-06-sprint-closing-readonly-review.md` finding S4.
    - **[Added 2026-08-20, task #1207] A SECOND, HARDER blocker for the
      same three crates (plus `numa-shim`), found while investigating the
      release guard: none of the four has a `CHANGELOG.md` AT ALL.**
      Measured directly — `test -f crates/<c>/CHANGELOG.md` returns MISSING
      for `racy-ptr-cell`, `size-classes`, `tagged-index-stack` and
      `numa-shim`; only `aligned-vmem` has one. This is not cosmetic:
      `.github/workflows/release.yml:268-272` resolves the CHANGELOG path
      from `cargo metadata` by package name and then fails CLOSED
      (`::error::CHANGELOG not found at $CHANGELOG` / `exit 1`) — a
      fail-closed branch task #1115 added deliberately. So a publish
      attempt for any of the four stops at that step, before `cargo
      publish` runs. The guard is behaving correctly; the files simply do
      not exist. Owner: **task #1220** (write real per-crate changelogs
      reconstructed from `git log -- crates/<c>/`, not stubs).
    - **[Added 2026-08-20, task #1207] Release-guard status, recorded
      because the task that raised it was based on MY OWN too-narrow
      verification and resolved NULL.** The claim under investigation was
      that `## 0.2.0 - Unreleased` would ship in the tarball with no gate
      catching it. The supporting evidence was `grep -rn "Unreleased"
      scripts/*.mjs .github/workflows/ci.yml` → zero hits, which is TRUE
      but was generalised into "no guard anywhere looks at that word".
      The guard exists: `.github/workflows/release.yml:301-309`, and its
      own comment names `## 0.2.0 - Unreleased` (aligned-vmem) as one of
      the two heading shapes it is written to catch, case-insensitively,
      after an anchored per-version section match that requires exactly one
      section. Counterfactual run against the guard body extracted
      verbatim: with today's header it exits 1 ("Stamp the real release
      date"); with a dated header it exits 0; a CHANGELOG carrying a
      legitimate `## Unreleased` section for FUTURE work alongside a dated
      released section still passes, so it is not a false-positive
      generator. **Recorded decision:** the version header is dated
      immediately before `cargo publish`, not now — dating it today would
      fabricate a release date, which is the exact defect task #1099/I2
      (`5dc4385`) already had to remove once. That decision is held by the
      guard, not by intention. **Honest limitation:** the guard lives only
      in `release.yml` under `dry_run != 'true'`, so neither `npm run
      check` nor `ci.yml` ever exercises it — a contributor sees it fire
      for the first time during a real release. That is not the task-#1131
      "guard nothing runs" class (it does run, at exactly the moment the
      decision above refers to), but it is stated here rather than left to
      read as full CI coverage.

27. **[T, filed 2026-08-06, task #653/P19, `docs/reviews/2026-08-06-publish-readiness-sweep-closing-review.md` finding P3-4 item 3] tagged-index-stack's `compile_error!` guard for unsupported `target_has_atomic` widths doesn't suppress the cascading `E0432` unresolved-import error on the same build — a deliberate, unrecorded tradeoff.**

    - **Status:** OPEN — low-priority polish, deliberately deferred, not an
      oversight. Purely cosmetic (the build already fails either way on an
      unsupported target; the only difference is whether the FIRST error a
      user sees is the clear named-reason `compile_error!` or that error
      followed by a cascade of confusing `E0432`s).
    - **Current-number-or-verdict:** commit
      `300b41f97a0e7c85310e5ed53dcbf289414e779f`'s own body: adding the
      `#[cfg(not(target_has_atomic = "64"))] compile_error!` guard (F2) does
      fire first and gives a clear, named-reason error on an unsupported
      target — a real (if small) behavior improvement. But it does not
      suppress the subsequent cascading `E0432` unresolved-import error
      that still follows on the same build, because `compile_error!` does
      not halt the rest of module compilation. Fully suppressing the
      cascade would require `#[cfg(target_has_atomic = "64")]`-gating every
      downstream item in the file — "judged too intrusive for the benefit
      on an already-broken build," per the commit body.
    - **Why filed instead of fixed here:** it is a conscious, defensible,
      already-reasoned-through tradeoff, not a bug — filing it only so the
      decision is recorded somewhere indexed instead of living solely in
      one commit message.
    - **Next trigger:** none required; revisit only if a future contributor
      finds the cascading `E0432` output genuinely confusing enough in
      practice to justify the `cfg`-gating cost across the file. Low
      priority, no forcing deadline.
    - **Evidence:** commit `300b41f97a0e7c85310e5ed53dcbf289414e779f`'s
      full commit body; `crates/tagged-index-stack/src/lib.rs` (the
      `compile_error!` guard);
      `docs/reviews/2026-08-06-publish-readiness-sweep-closing-review.md`
      finding P3-4 item 3.

28. **[T, filed 2026-08-06, task #653/P19, `docs/reviews/2026-08-06-publish-readiness-sweep-closing-review.md` finding P3-4 item 4] Two one-way-door publish decisions for `racy-ptr-cell` — its name and its 383-character `description` — were surfaced in commit `9ecada3`'s body but never recorded anywhere indexed, and become permanent the moment the crate first publishes.**

    - **Status:** OPEN — needs a maintainer decision before `racy-ptr-cell`'s
      first publish to crates.io. No code change; a naming/metadata call.
    - **Current-number-or-verdict:** commit
      `9ecada3d25bcbdf33e9b184c4233685e5b6a243f`'s body, §"Not addressed
      here": (a) the crate name `racy-ptr-cell` reads to a newcomer as "has
      data races" — the OPPOSITE of the guarantee the crate actually
      provides (a lock-free, race-safe exactly-once cell) — and was
      confirmed free on crates.io as of the original review date (subject
      to re-confirmation closer to actual publish time, since crates.io
      names can be claimed by others in the interim); (b) `Cargo.toml`'s
      `description` field is 383 characters, long for a crates.io listing
      (crates.io does not hard-limit description length, but long
      descriptions truncate awkwardly in search-result UI). Neither is
      recorded anywhere indexed prior to this filing, nor in K3/#598's own
      task description.
    - **Why filed instead of fixed here:** both are one-way-door naming/
      metadata decisions requiring maintainer judgment (a rename affects
      every existing reference across the workspace and any external
      consumer once published; a description rewrite is a content call) —
      not something to resolve unilaterally in a bookkeeping-only task.
    - **Next trigger:** resolve as part of the deferred publish-DAG pass
      (K3/#598), before `racy-ptr-cell`'s first `cargo publish` — decide
      whether to rename the crate (and if so, to what) and whether to
      shorten `description`, then re-verify crates.io name availability
      immediately before the actual publish action (names can be claimed
      by others between now and then).
    - **Evidence:** commit `9ecada3d25bcbdf33e9b184c4233685e5b6a243f`'s full
      commit body, §"Not addressed here"; `crates/racy-ptr-cell/Cargo.toml`
      (`name`, `description` fields);
      `docs/reviews/2026-08-06-publish-readiness-sweep-closing-review.md`
      finding P3-4 item 4.

29. **[T, filed 2026-08-06, task #654/P20, `docs/reviews/2026-08-06-publish-readiness-sweep-closing-review.md` finding P4-12] `#![deny(missing_docs)]` on 4 about-to-be-published crates is a one-way-door tradeoff versus the more common `warn` + CI `-D warnings` convention — no conscious publish-time decision recorded.**

    - **Status:** OPEN — low severity, pre-publish-decision item. No code
      defect: all four crates compile clean today at 100% doc coverage.
    - **Current-number-or-verdict:** `#![deny(missing_docs)]` was added to
      `racy-ptr-cell` (commit `9ecada3`, task #642) and to `sefer-region`,
      `size-classes`, `tagged-index-stack` (commit `7c8621f`, task #651).
      All four verified at 100% doc coverage as of the commits above, and
      all four compile clean today. The tradeoff: `deny` (vs. the more
      common ecosystem convention of a lib-level `warn` plus CI-level `-D
      warnings`) means that once a crate is PUBLISHED, a future rustc
      release that widens what counts as `missing_docs` would turn a
      downstream consumer's `cargo build` of that already-published,
      pinned version red — with no recourse for the consumer, since they
      cannot edit a crate they don't own. `warn` would not have this
      failure mode (a widened lint would show as a new warning on
      recompilation of the crate's own source, not retroactively break an
      already-published, unmodified version's build for downstream
      consumers). None of these four crates has been published yet, so
      the tradeoff is still avoidable.
    - **Why filed instead of fixed here:** this is a deliberate policy
      choice between two defensible conventions (`deny` vs. `warn` + CI
      gate), not a bug — it needs a conscious maintainer decision before
      first/next publish, not a unilateral edit in a bookkeeping-only task.
    - **Next trigger:** before any of `racy-ptr-cell` / `sefer-region` /
      `size-classes` / `tagged-index-stack`'s first (or next) `cargo
      publish`, decide whether to keep `#![deny(missing_docs)]` as-is
      (accepting the one-way-door risk) or downgrade to `#![warn(missing_docs)]`
      plus an equivalent CI-level `-D warnings` gate (matching the more
      common ecosystem convention, avoiding the retroactive-break failure
      mode). Natural to fold into the deferred publish-DAG pass (K3/#598).
    - **Evidence:** commit `9ecada3d25bcbdf33e9b184c4233685e5b6a243f`
      (`racy-ptr-cell`); commit `7c8621f` (`sefer-region`, `size-classes`,
      `tagged-index-stack`); `crates/racy-ptr-cell/src/lib.rs`,
      `crates/sefer-region/src/lib.rs` (the `sefer-region` package),
      `crates/size-classes/src/lib.rs`,
      `crates/tagged-index-stack/src/lib.rs` (each crate's
      `#![deny(missing_docs)]` attribute);
      `docs/reviews/2026-08-06-publish-readiness-sweep-closing-review.md`
      finding P4-12.

46. **CLOSED** by task #1053 (option (a): coupling accepted and documented,
    plus a `pub use aligned_vmem::Reservation;` re-export). See "Recently
    resolved" in RESOLVED.md for the full closure narrative.

85. **[T, filed 2026-08-11, task #821, renumbered from 46 by task #1143 (2026-08-19) — a duplicate item number, not a new item] `crates/sefer-region` depends on `captrack`
    v0.1.1 (exact-pinned) as a dev-dependency for a single ignored probe test.**
    **Renumbering note (task #1143):** this card was originally filed as item 46 by commit `7ee57a9` (2026-08-11), which appended it at the file's then-current end without checking that 46 was already taken — the numa-shim `reserve_on_node` item (filed 2026-08-09, task #778, two days earlier) had legitimately owned number 46 since before this card existed (confirmed via `git show 7ee57a9~1:docs/CORRECTNESS_OPEN_ITEMS.md`, which shows item 46 = numa-shim at that parent commit). The duplicate persisted through every later edit, including the R34-24 archive split, because the pairing test (`tests/no_stale_doc_references.rs::correctness_index_recently_resolved_pointers_carry_verdicts`) only checks the "Recently resolved" section's pointers, not active-tier item numbers, so nothing caught it. Renumbered to 85 (the next number above the highest then in use, 84) rather than shifting every intervening item, to avoid the exact cascading-renumber risk task #1109's index split already demonstrated (9 pointers truncated, 19 of 32 verdicts lost) when a purely mechanical edit touched more of the file than intended. No other document in this repo cites this card as "item 46" — every other reference to "item 46" in `docs/`, including all checkpoints, means the DIFFERENT numa-shim item in this same file, or `docs/perf/OPEN_ITEMS.md`'s own unrelated former item 46 (the Unix exact-reserve hit-rate item, since closed and moved to that file's "Recently resolved"); this card's own content is referenced elsewhere only by task number (#821) or commit (`7ee57a9`), never by item number, so the renumbering breaks no cross-reference (verified by `git grep -n "item 46"` across `docs/` — see below).
    `captrack` is a heavy dependency (proc macro, `ctor` constructor with a
    background thread, supply-chain side effects). The workspace-root
    `.cargo/config.toml` suppresses autodump via `CAPTRACK_AUTODUMP=0`, but that
    config does NOT travel with the published crate tarball — external consumers
    who build `sefer-region`'s tests will see the ctor run at process startup
    without that suppression. The dependency is intentional (the probe provides
    capacity telemetry for `Region<T>`'s `slotmap::SlotMap` backing that cannot
    be obtained any other way — see `tests/captrack_probe.rs`'s module doc), and
    a std-only alternative was drafted and explicitly declined in this task
    (the user decided to keep captrack rather than rewrite the probe). The
    mitigation applied is exact version pinning (`=0.1.1`), which eliminates the
    risk that a future minor/patch bump of captrack silently changes its
    side-effect profile without an explicit review.

    **Standalone-build verification (empirical, not reasoned):** A fresh copy
    of `crates/sefer-region/` was extracted to `C:\temp\sefer-region-standalone-test`,
    outside this workspace (no parent `[workspace]` Cargo.toml, no
    `.cargo/config.toml` with `CAPTRACK_AUTODUMP=0`). Commands run:
    - `cargo build --tests` — clean compile, no visible side effect
    - `cargo test --no-default-features --features std` — all tests passed,
      `captrack_probe` correctly ignored
    - No stray JSON files, no persistent background processes observed (the
      ctor's behavior was not directly instrumented, only its visible side
      effects; absence of obvious artifacts is NOT proof of absence, but it is
      the empirical baseline recorded here)

    **Status:** OPEN — the dependency and probe remain in place per explicit
    user decision; only exact-pinning mitigation was applied (not removal,
    not std-only replacement).

    **Next trigger:** revisit if captrack's own side-effect profile changes
    (e.g., future releases introduce additional ctor activity), or if a future
    round decides to swap it for the std-only alternative that was drafted and
    declined in task #821.

90. **[T, second instance of item 89's class — a NO-GO review landed with zero index cards] Commit `967b821` committed `docs/reviews/2026-08-19-aligned-vmem-publication-readiness-audit-r2.md` (independent read-only re-audit of `aligned-vmem`, verdict **NO-GO for publishing 0.2.0** — 0 Critical, 0 High, 4 Medium, 3 Low, 3 perf candidates, 1 coverage gap) and, per its own body, "filed as #1171 (decision), with #1172/#1173/#1174 sequenced behind it" — but filed no card in this index, `docs/perf/OPEN_ITEMS.md`, or either archive. Filed here (task #1175) exactly one round after item 89 named the same "filed as a follow-up" pattern in a different script, confirming it is a recurring class, not a one-off.** (Filed 2026-08-19, task #1175.)

    - **Status (refreshed 2026-08-23, task #1251 — the seventh independent audit's G2 finding):** All four owners are LANDED, AND the single OPEN QUESTION below is now ANSWERED — see the appended answer directly under it (task #1190, decided 2026-08-20: **NO**). Nothing this card itself still tracks is pending. **The line this replaces** (refreshed 2026-08-20, task #1208) said *"This card closes when #1190 is answered"* — true when written, but #1190's answer landed inside this SAME card, three lines below, that same day, and the Status line above it was never refreshed to say so; the seventh independent audit caught the three-day-old gap (`docs/reviews/2026-08-23-133616-aligned-vmem-readonly-review-GLM.md`, finding G2). **Not moved to "Recently resolved"**: the crate's 0.2.0 NO-GO still stands, but solely on item 97/98's F1 (the deliberately-deferred `CHANGELOG.md` `## 0.2.0 - Unreleased` header) — nothing item 90 itself owns remains open.
      **The line this replaces said "None of #1172/#1180/#1173/#1174 has landed yet", and stayed true-then/false-later for a full wave** — it was accurate when written (2026-08-19, task #1171) and was invalidated by the four landings without anyone updating it. That is the exact staleness class R34-24 created current-state cards to prevent, and the reason task #1208 exists as a separate, deliberately-LAST task in its wave: refreshing a Status line mid-wave only re-stales it (the task-#1193 lesson).
    - **Current-verdict:** `git grep -n "2026-08-19-aligned-vmem-publication-readiness-audit-r2"` returned zero hits before this edit (re-verified 2026-08-19, task #1175) — the review file existed, was fully committed (203 lines), and was referenced nowhere. `grep -n "from_raw_parts" docs/CORRECTNESS_OPEN_ITEMS.md` before this edit matched only item 53 (CLOSED, a different `granted_huge` finding from the 2026-08-14 pre-release review, not this one). Report is read-only by its own stated scope ("Режим: только чтение... без запуска тестов, сборки, cargo check, Clippy, Miri, benchmark и `cargo publish --dry-run`") — its findings below are UNVERIFIED BY EXECUTION, only by source reading; treat accordingly.
    - **The four Medium findings (report §M1–M4, `docs/reviews/2026-08-19-aligned-vmem-publication-readiness-audit-r2.md:39-117`), each blocking 0.2.0 publication because a post-publication fix would need a semver-incompatible change:**
      - **M1** — `Reservation::from_raw_parts`'s `granted_huge: bool` cannot describe an arbitrary adopted HugeTLB mapping (no encoded huge-page size or `munmap` granularity requirement); `Drop` silently leaks the whole mapping if `reservation_len` is not huge-page-multiple.
      - **M2** — an already-adopted huge mapping's decommit behavior depends on the CONSUMER's Cargo feature set: `is_huge()` is available without `huge-pages`, but the eligible-forward `MADV_DONTNEED` path is feature-gated, so the same live mapping is served or silently skipped depending on the caller's crate composition.
      - **M3** — `from_raw_parts`'s `# Safety` section mixes true memory-safety preconditions with functional/behavioral ones; the crate's own integration tests deliberately violate some of the mixed-in conditions while explaining why doing so is not UB — a self-contradictory contract.
      - **M4** — `try_decommit() -> Result<(), VmemError>` reports range validity, not decommit outcome; `Ok(())` cannot distinguish a Rust-level skip, kernel-rejected `madvise`/`VirtualFree`, accepted advisory, or actual RSS reclaim, despite rustdoc for `can_decommit_reclaim_and_zero()` suggesting the return value can be used to judge exactly that.
    - **Three Low findings (report §L1–L3, lines 119-146):** **L1** — poison-state `decommit()` is documented as a no-op but `debug_assert!(false, ...)` makes it a debug-build panic, undocumented in `# Panics` and unpinned by any test oracle. **L2** — `VmemError`'s single no-code sentinel is overloaded across at least six distinct causes (invalid args / OS refusal-no-code / zero grant / page-size query failure / fault-injection / mock backend), and `os_refusal_unknown_code`'s "FOUR sources" doc claim does not enumerate the real call sites. **L3** — **already covered by item 88 above (MSRV does not compile `aligned-vmem/huge-pages` — no `-p aligned-vmem` row in the `msrv` CI job); not a duplicate card, only cross-referenced here as the same gap re-surfacing from a second source.**
    - **Coverage gap (report, "Coverage gap" section, lines 170-174):** the real-HugeTLB CI job proves `MAP_HUGETLB` grant + eligible `madvise` dispatch, but reads no memory post-decommit and measures no RSS/pool reclaim — it distinguishes "kernel accepted the advice" from nothing, not from "memory was actually returned."
    - **This header's "3 perf candidates" (report §P1-P3), owners and content:** filed in `docs/perf/OPEN_ITEMS.md` item 57 (task #1182) — the perf-scoped index this material's own Scope belongs to, which the commit landing this card did not touch.
    - **Owning follow-up tasks (post-decision split):** **#1171** — DONE by this card: records the owner's M1/M3/M4 decision below and owns the OPEN QUESTION at the end of that block. **#1172** — phase 1: M1-hybrid + M3 split + M2-as-consequence, plus the stale `decommit_lazy` huge-advice fix (`crates/aligned-vmem/src/reservation.rs:637-639,891-895`) and the `lib.rs:62-63` precision edit (its point (4)). **#1180** — phase 2: `DecommitOutcome`, absorbing the third audit's §P2 dispatch-unification finding (one private helper, one `page_size` snapshot). **#1173** — L1+L2 (L3 redirects to item 88). **#1174** — strengthen the real-HugeTLB coverage gap (write→decommit→recommit/read-zero + RSS/pool observation).
    - **Owner decision on M1/M3/M4 (task #1171, decided 2026-08-19) — all three recommendations adopted in full, unchanged.** Three sources converged: the PUB-R2 report (the findings), the `@fh` consultation it prompted (the recommendations, previously recorded here as pending), and a third independently commissioned audit (`docs/reviews/2026-08-19-2148-aligned-vmem-publication-audit-Сол-кодекс.md`, revision `000c076`, verdict NO-GO, same four Medium findings) that reaches the same hybrid, the same safety/contract split, and the same `DecommitOutcome`, and restates both premise refutations below. Provenance caveat, stated so this card does not overclaim independence: that audit reported at 21:48 +02:00, AFTER the `@fh` recommendations were committed (`062a318`, 20:22 +02:00); its own §"Итог" cites "item 90" of THIS file by name and its boundary section lists the correctness open items among its inputs — so it demonstrably READ the `@fh` block it agrees with. It is therefore a CONCURRING review by an independently commissioned reader, NOT a blind re-derivation, and its agreement is weaker evidence than three genuinely independent derivations would be. What is genuinely its own contribution: the two ADDRESS-alignment conjuncts (`reservation`, `base`) it adds to the 2-MiB assert list the decision below adopts, on top of `@fh`'s original three fields (`len`, `reservation_len`, the offset `base − reservation`) — a genuine strengthening (see item 91's corrected note below for why), even though the resulting list is five NAMED quantities but only FOUR independent checks, not five independent ones (task #1196/OX6-L1 correction) — the 1 GiB-granularity argument behind the OPEN QUESTION below, the vendor-doc confirmations for M4 (Linux `madvise(2)`, Microsoft `VirtualFree`), and its §P1-P3 perf findings. Its file was UNTRACKED when this decision was recorded; task #1186 commits it together with its own index card — item 91 below, which carries the same provenance caveat and files the audit's genuinely-new findings. **Edit-history note (task #1205):** the "genuinely its own contribution" clause immediately above was REWRITTEN IN PLACE by task #1196, not appended to. `9e1a734`'s commit body says item 90's decision text was "left VERBATIM … the correction is an appended note", which is true of the `- **M1 — HYBRID**` sub-bullet below (untouched, with a note added under it) but NOT of this paragraph, which `git diff 9e1a734^ 9e1a734` shows as a `-`/`+` replacement. Recorded here because the non-retroactive convention this file follows is worth more than a tidy claim about having followed it; the in-place edit itself is left standing, since reverting it would restore a figure task #1196 correctly showed to be misleading.
      - **M1 — HYBRID** (none of the report's own options): keep `granted_huge: bool`, but narrow what `true` MEANS to "the mapping is in the crate's own 2 MiB format": (a) `from_raw_parts` asserts, on Linux/Android, that `len`, `reservation_len`, `reservation`, `base`, and the offset `base − reservation` are ALL 2 MiB multiples when `granted_huge == true`; (b) accepting `granted_huge: true` is gated behind the `huge-pages` feature — this CLOSES M2 as a consequence (with no feature there are no huge-flagged adopted mappings at all, so there is no divergent behavior to have); (c) rewrite the `reservation_len` rounding bullet (`crates/aligned-vmem/src/reservation.rs:1071-1078`) to state the HugeTLB exception explicitly. REJECTED: banning huge in `from_raw_parts` outright — breaks the documented `into_full_parts` → `from_raw_parts` round-trip, the only host-independent CI coverage of the huge branch. REJECTED, conditionally on the open question below: typed huge-granularity metadata — over-engineering while the crate implements exactly one size, `LINUX_HUGE_PAGE_SIZE = 2 MiB`.
        **Appended note (task #1196/OX6-L1, decision text above left verbatim per this file's non-retroactive convention):** the five names in (a) are not five independent checks — `reservation` and `base` both being 2-MiB multiples already implies their difference `base − reservation` is too, so the offset conjunct can never be the one that fails; the assert as SHIPPED (`crates/aligned-vmem/src/reservation.rs`) has FOUR independent checks, not five, and the code and its comments now say so. The decision itself is unaffected — the shipped assert still checks exactly the five names listed here; only the "five independent requirements" framing was corrected, not the check.
      - **M3 — SPLIT.** `# Safety` keeps ONLY memory-safety preconditions; the functional requirements (accuracy of `granted_huge`, Windows commit-state) move to a separate "Correctness contract" section stating the precise consequence of violating each. REJECTED: declaring the tested violations UB and rewriting the tests — that would assert something demonstrably false; `tests/granted_huge_reader_enumeration.rs` pins exactly the non-UB behavior.
      - **M4 — `Result<DecommitOutcome, VmemError>`** with `Skipped` / `Advised` / `Refused(VmemError)` behind ONE private helper. `Advised` means "the kernel accepted the advice", explicitly NOT "RSS was reclaimed" — that gap is owned by the coverage-gap item (#1174), not by M4. REJECTED: option (b) — renaming/documenting the function as a range-contract check only.
      - **Adopted blast radius (for #1172/#1180):** the M1-hybrid assert breaks `tests/reservation_decommit_contract.rs:405` (`method_try_decommit_reports_malformed_range_on_huge_flagged_reservation`, currently ungated — needs `#[cfg(feature = "huge-pages")]`; its sibling at `tests/decommit_capability.rs:490-492` is already gated). `numa-shim` is unaffected (always passes `granted_huge: false`). `DecommitOutcome` lands in phase 2 (#1180) across the backend files plus one new `src/decommit_outcome.rs` (one-file-one-export rule).
      - **Two PUB-R2 premises stand REFUTED (established by `@fh`; the third audit concurs, but had read the `@fh` block — see the provenance caveat above — so this is one derivation plus one agreement, not two independent ones. They generate no remediation work):** (1) "`Drop` silently loses the `munmap` error" is FALSE — under `bench-internals` it IS counted, in `UNIX_MUNMAP_FAILURES` (`crates/aligned-vmem/src/os/unix.rs:1103-1109`, task P2-6). Caveat both sources add: in the ordinary public API the result is still unobservable — a separate question, not an accounting defect. (2) The crate-level "every entry point has two forms" text is correctly scoped by `crates/aligned-vmem/src/lib.rs:55` ("every **reservation/commit** entry point"); the REAL inaccuracy is a DIFFERENT sentence, at `lib.rs:62-63` ("The infallible forms forward to the `try_*` forms"), which is wrong for the decommit family — that fix belongs to task #1172, point (4).
      - **Version: publish as 0.2.0, NOT 0.3.0** (0.2.0 has never reached crates.io; skipping to 0.3.0 would orphan existing CHANGELOG references for no semantic reason).
      - **OPEN QUESTION (owner: task #1190 — re-pointed by #1190 itself from #1171, which is DONE per the "Owning follow-up tasks" line above and so must not stand as a live owner per R34-24; raised by the third audit after the decision was taken; ANSWERED 2026-08-20, appended directly below):** does the crate intend to support ADOPTING HugeTLB mappings whose granularity is not 2 MiB (e.g. 1 GiB)? Today's default is NO — the crate nowhere promises such support — and under that default the hybrid stands as decided above. If the answer ever becomes YES, typed huge-granularity metadata must be introduced BEFORE the first publication (afterward it is a semver break), and task #1172's scope grows accordingly.
        **Appended answer (task #1190, owner decision 2026-08-20): NO.** `aligned-vmem` does not support adopting a HugeTLB mapping whose granularity is not 2 MiB; the 2-MiB assert is the contract, not a temporary narrowing. Decided pre-publication, so the `granted_huge: bool` contract is final as shipped — a future "yes", if it ever comes, would be ADDITIVE (a new constructor carrying typed huge-granularity metadata, not a relaxation of the existing assert presented as a bugfix), because `granted_huge: true` already means "this crate's own 2 MiB format" (#1172's narrowing) and stays truthful for that case. Recorded by #1190 in `crates/aligned-vmem/src/reservation.rs` (`from_raw_parts`'s `granted_huge`-accuracy and 2-MiB-multiple contract bullets) and `crates/aligned-vmem/CHANGELOG.md`.
    - **Next trigger:** landing commits for #1172 → #1180 → #1173/#1174 — move this Status line to reflect each as it lands (with commit SHAs) until all four are closed; and the owner's answer to the OPEN QUESTION in the decision block (if YES, typed metadata moves INTO #1172 and must land before the first publish — record the answer here when given).
    - **Evidence:** `docs/reviews/2026-08-19-aligned-vmem-publication-readiness-audit-r2.md` (full report, 203 lines, commit `967b821`); `docs/reviews/2026-08-19-2148-aligned-vmem-publication-audit-Сол-кодекс.md` (third audit, revision `000c076`, reported 2026-08-19 21:48 +02:00 — committed by task #1186 alongside its index card, item 91); commit `062a318` (task #1175, 2026-08-19 20:22 +02:00 — landed the `@fh` recommendations this decision adopts; their full rationale, including the `libc_madvise`/task-#1164 availability argument for M4, is preserved verbatim there); `crates/aligned-vmem/src/os/unix.rs:53-59,1103-1109` (huge-munmap-granularity mechanism + the existing `UNIX_MUNMAP_FAILURES` counter under `bench-internals`); `crates/aligned-vmem/src/lib.rs:55,62-63` (the correctly-scoped "reservation/commit" entry-point claim, and the actually-wrong infallible-forwards sentence); `crates/aligned-vmem/src/reservation.rs:637-639,891-895,1071-1078,1217-1229` (the stale `decommit_lazy` advice, the unqualified rounding bullet, and the `PAGE`-only assert); `tests/reservation_decommit_contract.rs:405` and `tests/decommit_capability.rs:490-492` (the test that will need gating under the M1-hybrid, and its already-gated sibling); `tests/granted_huge_reader_enumeration.rs` (pins the non-UB behavior M3's split preserves); item 53 above (the distinct, already-closed prior `granted_huge` finding, confirmed not a duplicate); item 88 above (owns L3/MSRV — not duplicated here).

91. **[T, third independent audit of `aligned-vmem` — its report indexed in the SAME change that commits it, per the item-89/90 class lesson] `docs/reviews/2026-08-19-2148-aligned-vmem-publication-audit-Сол-кодекс.md` (author Сол-кодекс; reported 2026-08-19 21:48:52 +02:00; revision `000c076`; verdict **NO-GO for publishing `aligned-vmem 0.2.0`** — 0 Critical, 0 High, 4 Medium (the blockers), 4 Low, 3 Performance, 2 Coverage) is a STATIC read-only audit: no build, tests, or CI gates were executed, so its findings are UNVERIFIED BY EXECUTION — source reading only. Its four Medium blockers are the SAME M1-M4 item 90 already owns (the report says so itself, its line 19), so this card files NO duplicate blockers — it records the re-confirmation, indexes the report, and assigns owners to the genuinely new findings.** (Filed 2026-08-19, task #1186.)

    - **Status (refreshed 2026-08-23, task #1251 — G2):** Its stated closure condition — "item 90's four owners plus #1187-#1190 have all landed or been dispositioned" — is now fully met: item 90's four (#1172/#1180/#1173/#1174), **#1187**, **#1188**, **#1189**, AND **#1190** (decided 2026-08-20: **NO** — see item 90's OPEN QUESTION block) are all landed/dispositioned. **The line this replaces** (task #1208) said "Closes with #1190" as though still pending; #1190 was answered the same day that line was last refreshed and the text was never updated to reflect it — the same gap the seventh independent audit's G2 finding caught on item 90. **Not moved to "Recently resolved"**, for the same reason as item 90: the crate's NO-GO persists solely via item 97/98's F1, outside this card's own scope.
    - **Current-verdict:** NO-GO stands at `000c076` — all four Medium blockers re-derived there and still open. Mode is static: findings are NOT execution-confirmed; the report's §"Граница исследования" (its lines 216-222) enumerates exactly what was read (crate sources, Unix/Windows/miri/mock backends, public rustdoc, manifest, README/changelog, test oracles, CI, and THIS file's open items — its line 220).
    - **Independence caveat (mirrors item 90's; times and ancestry re-verified this task):** the audit was NOT blind. `git show -s --format='%ci' 062a318` → 2026-08-19 20:22:47 +0200 (the commit that landed the `@fh` recommendations into item 90); the report's header (its line 5) reads 2026-08-19 21:48:52 +02:00 at revision `000c076` (its line 7), and `git merge-base --is-ancestor 062a318 000c076` succeeds — the audited tree CONTAINS the `@fh` block. The report cites "item 90" of THIS file by name (its line 19) and lists the correctness open items among its inputs (its line 220): it demonstrably READ what it agrees with. It is a CONCURRING review by an independently commissioned reader, NOT a blind re-derivation, and its agreement is correspondingly weaker evidence — the same conclusion, in the same words, as item 90's caveat above; the two statements must not drift apart.
    - **M1-M4 — CONFIRMATION on a new revision, not new findings:** owners unchanged from item 90 — **#1172** (phase 1: M1-hybrid + M3 split + M2-as-consequence), **#1180** (phase 2: `DecommitOutcome`), **#1173** (L1+L2), **#1174** (C1 coverage). ONE blocker set, confirmed twice — item 90's decision block owns what was decided.
    - **Genuinely the audit's own (the value beyond concurrence) — corrected by task #1196/OX6-L1, see item 90's appended note above:** (a) the two ADDRESS-alignment conjuncts this audit added to `from_raw_parts`'s 2-MiB-multiple assert list — `reservation` and `base` — on top of `@fh`'s original three-field list (`len`, `reservation_len`, and the offset `base − reservation`; report line 73). This addition is genuine and strictly STRENGTHENS the check: under `@fh`'s three fields alone, a reservation at address 1 MiB with base at 3 MiB PASSES (offset = 2 MiB, a clean multiple) while BOTH addresses are individually 2-MiB-misaligned — and it is the ADDRESS alignment that `munmap(2)` on a `MAP_HUGETLB` mapping actually requires, not just the length. But the same two additions make `@fh`'s third field (the offset) REDUNDANT, not additive on top of it: `reservation` and `base` both being 2-MiB multiples already implies their difference is too, so the assert's five NAMED quantities resolve to FOUR independent checks, not five. (Task #1196 corrects the "five-field"/"all five" framing used here and in item 90's decision block above to this four-independent/five-named form; the fifth conjunct itself was not removed from the code — it stays for its panic-message diagnostics and because it would become load-bearing again if either address conjunct were ever weakened.) (b) the 1-GiB HugeTLB argument — an adopted 1-GiB mapping cannot be honestly served by a 2-MiB eligibility check, because a 2-MiB-multiple range can still be invalid for the mapping's real granularity (report line 65); this is what raised item 90's OPEN QUESTION, and the follow-up carrying it to the owner is **task #1190**; (c) vendor confirmations for M4 that Linux `madvise(2)` and Windows `VirtualFree` both return an observable success/failure result (report line 120, man7/Microsoft Learn links inline there); (d) §P1-P3 + §C1-C2 (owned below).
    - **New findings, each with an owner (nothing below duplicates item 90 or its tasks):**
      - **L4 (report lines 149-159), five stale doc/comment sites → task #1187** owns the two not already covered: `crates/aligned-vmem/src/api/decommit.rs:250-251` (published rustdoc of a public `unsafe fn` still calls the huge-page operation "incompatible outright", contradicting what task #1140 established — the Linux >= 5.18 eligible path) and the `Drop` SAFETY comment at `crates/aligned-vmem/src/reservation.rs:1262` (asserts the reservation "was returned by `reserve_aligned`", ignoring the public `from_raw_parts` adoption path). The two of the five ALREADY owned are `crates/aligned-vmem/src/lib.rs:62-63` and `crates/aligned-vmem/src/reservation.rs:637-639` — both inside #1172's scope, cross-referenced only. The fifth (moving long historical task-number narratives out of runtime modules) is a PROPOSAL to the owner — a convention change to decide, not filed as work.
      - **P1 (report lines 163-167) → task #1188:** with `align > 2 MiB` the generic path maps `size + align` and keeps the whole mapping — up to 2× the scarce, pre-allocated HugeTLB pool (the exact huge fast path exists only at `align == LINUX_HUGE_PAGE_SIZE`, 2 MiB); post-hoc huge-page trimming cannot remove the admission cost. Measure pool occupancy, fallback rate, and extra-syscall cost on the real-HugeTLB runner before optimizing.
      - **P2 — ABSORBED into #1180** (one private dispatch, one `page_size` snapshot); no separate owner, deliberately not re-filed.
      - **P3 + C2 (report lines 173-177 and 187-191 — NOT one contiguous span: §C1 sits between them at 181-185 and is owned by #1174) → task #1189:** speculative platform paths need measurement before any removal (Linux larger-huge retry before ordinary fallback; Windows large-page failed-request cost profile; generic 64-bit Unix over-reserve), and HugeTLB release has no direct oracle — `UNIX_MUNMAP_FAILURES` is never checked around `Drop` in the real-HugeTLB tests, so a release call that disappeared entirely would not redden the job.
      - **C1 — already #1174. L1, L2 — already #1173. L3 (MSRV) — already item 88.** Cross-references only; no duplicate cards filed.
    - **Next trigger:** landing commits for #1187/#1188/#1189, and the owner's answer to #1190 (record the answer in item 90's OPEN QUESTION block — that is where the decision lives, not here); once item 90's four owners and #1187-#1190 are all dispositioned, move this card to "Recently resolved" per the R34-24 convention.
    - **Evidence:** the report `docs/reviews/2026-08-19-2148-aligned-vmem-publication-audit-Сол-кодекс.md` (222 lines — `wc -l`, re-measured this task; the "223" figure that circulated in this wave's task briefs counted the trailing newline as a line and is wrong; untracked in the main checkout until the same operator commit that carries this card — the Cyrillic filename breaks no guard: no script in `scripts/` enumerates `docs/reviews/`, `verify-gate-report.mjs` scans `scripts/` + `docs/perf/` only, and the vmem guards walk only `.rs` files under `crates/`); timestamps and ancestry re-verified this task (`git show -s --format='%ci' 062a318` → 20:22:47 +0200; `git show -s --format='%ci' 000c0767416b96df11aa7bbb8b80efb4c09cb754` → 20:58:40 +0200; `git merge-base --is-ancestor` → exit 0); report lines 5, 7, 15, 19, 65, 73, 120, 220 re-read before citing; `crates/aligned-vmem/src/api/decommit.rs:250-251` and `crates/aligned-vmem/src/reservation.rs:1262` confirmed by `grep -n` this task ("incompatible outright" → line 251; "was returned by `reserve_aligned`" → line 1262); commit `a839d63` (task #1140, the Linux >= 5.18 huge-decommit eligibility work the `decommit.rs` sentence contradicts); item 90 above (the one confirmed blocker set; its independence caveat is this card's mirror); item 88 above (owns L3).

93. **[T, FOURTH independent audit of `aligned-vmem` — report committed and indexed in the SAME change, per the item-89/90 class lesson] `docs/reviews/2026-08-20-073908-aligned-vmem-publication-audit-Сол-кодекс.md` (author Сол-кодекс; reported 2026-08-20T07:39:08+02:00; revision `dc2ecdd`; verdict **NO-GO for publishing `aligned-vmem 0.2.0`** — 0 Critical, 1 High, 3 Medium, 3 Low, 3 Performance, 3 Coverage) is a STATIC read-only audit: no tests, build, `cargo check`, Clippy, Miri, benchmarks or `cargo publish --dry-run` were run, so its findings are UNVERIFIED BY EXECUTION unless this card says otherwise per finding.** (Filed 2026-08-20, task #1209.)

    - **Status (refreshed 2026-08-23, task #1251 — G2; supersedes the 2026-08-20/task #1208 refresh below):** Both items the 2026-08-20 refresh left open are now dispositioned: **#1190** answered 2026-08-20 (**NO** — see item 90's OPEN QUESTION block) and **#1216** (the fifth independent audit, @oh) ran and closed 2026-08-20, its findings filed as item 94/#1225-#1230. Everything else in the 2026-08-20 text below is unchanged and still accurate: **#1210/#1211/#1212/#1213/#1214** landed; **#1207** resolved **NULL**; **#1219**/**#1220** were spun off as follow-ups.
      *(2026-08-20, task #1208, the refresh this supersedes on the #1190/#1216 point only):* OPEN. Landed since filing: **#1210** (H1 — the UB-producing test deleted outright, `1f930e2`), **#1211** (M1 — docs half `05557a6`, `src/` half `1522d25`), **#1212** and **#1213** (`1522d25`), **#1214** (C2/C3/P1-P3 into the indexes, `dd1061e`). **#1207** resolved **NULL** (`7db9f08`): the `Unreleased`-in-tarball hazard it described is already covered by `.github/workflows/release.yml:301-309`; the task's premise came from a grep too narrow for the conclusion drawn from it. ~~Still open: **#1190** (M3, the owner decision) and **#1216** (step 5, the fifth independent audit).~~ Two follow-ups were spun off rather than absorbed silently: **#1219** (restore `Refused` coverage — #1210's honest cost) and **#1220** (four crates have no `CHANGELOG.md`, found while resolving #1207).
    - **Current-number-or-verdict:** NO-GO stands at `dc2ecdd`, and this refresh does not lift it — lifting it is a separate decision, and now rests solely on item 97/98's F1 (the deliberately-deferred `CHANGELOG.md` `## 0.2.0 - Unreleased` header), not on anything this card tracks. The original blocking set is H1 + M1 + M2 + M3, all now DONE: **#1210** (H1), **#1211** (M1), **#1212** (M2), **#1190** (M3, decided 2026-08-20: NO). The audit's own recommended release gate's step 5 — "run a NEW independent audit after the fixes" — is task **#1216**, which ran and closed 2026-08-20 (item 94).
    - **Next trigger:** none remaining for this card's own blocking set — all five steps of the audit's own recommended release gate are done. The crate's NO-GO now depends only on item 97/98's F1 (the owner's release decision), tracked there, not here. **Not moved to "Recently resolved"**, for the same reason as items 90/91.
    - **Evidence:** the report itself (182 lines, `wc -l`, committed by this task); the H1 code site re-read by me directly (`crates/aligned-vmem/src/os/windows.rs:366`, `let addr = unsafe { base.add(start) };` against `far_start = 64 * 1024 * 1024` in `crates/aligned-vmem/tests/decommit_outcome.rs`); the M1 site re-read directly (`crates/aligned-vmem/README.md:56` still documents `try_decommit ... -> Result<(), VmemError>`); the M2 semver premise re-checked directly (`crates/aligned-vmem/src/decommit_outcome.rs:25` is `#[non_exhaustive]`); the C3 duplicate check (item **59b** already exists at this file's "Windows half of item 59" card — #1214 updates it rather than filing a new one).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only with no sub-agents and executed nothing. Its own §"Граница исследования" (line 180) records that HEAD advanced from `22a91cc` to `dc2ecdd` during the work and asserts the extra commit "only changes comments/guards around the test and does not remove H1". **Verified by me, not accepted:** `git show --stat dc2ecdd` touches `tests/decommit_outcome.rs`, `docs/CORRECTNESS_OPEN_ITEMS.md` and `tests/no_stale_doc_references.rs` (task #1206) and does not alter the `far_start` arithmetic — so H1 does stand at the audited revision.
    - **What this audit says the PREVIOUS wave actually closed (its lines 153-166), and why that matters:** it explicitly states the four earlier Medium findings must NOT be carried over mechanically — three are closed on the merits, and the huge-granularity question has narrowed to an explicit pre-release design decision. It credits eight closures by name: the `from_raw_parts` safety/correctness split; `granted_huge=true` no longer silently changing behavior with `huge-pages` off; the Linux/Android 2-MiB shape asserts; `try_decommit` returning `DecommitOutcome` with Unix/Windows wrappers preserving syscall refusal; page-size poison no longer a debug-only panic; the MSRV gate covering `aligned-vmem --all-features` plus `--no-run` test compile; real-HugeTLB CI gaining write → decommit → read-zero plus a release-attempt oracle; and release paths gaining attempt/failure counters. The NO-GO therefore NARROWED between the third and fourth audits rather than reproducing.
    - **Owner map (no duplicate cards filed — cross-references only):** H1 → **#1210** (also closes the audit's C1). M1 → **#1211**. M2 → **#1212**. M3 → **#1190** (pre-existing owner decision; the audit adds one requirement — remove the "not yet answered by the crate owner" wording from the published rustdoc, since an unresolved question on an unsafe boundary is itself a release defect). L1+L2+L3 → **#1213**. C2 + C3 + P1/P3 → **#1214**, which UPDATES item 59b for C3 rather than filing it new. C1 → already item 92 + #1210. P2 → already measured by task #1188 (perf item 57). The `## 0.2.0 - Unreleased` header the audit does not name is separately owned by **#1207**.
    - **Boundary of the audit's own reading (its line 178):** `Cargo.toml`, public exports and rustdoc, reservation/lazy ownership, raw-parts round trips, the decommit/recommit/commit/release API, the Unix/Windows/miri/mock backends, the error model, arithmetic/align checks, the feature/cfg matrix, README, CHANGELOG, test oracles, package/CI/MSRV/docs.rs gates, and the delta from the previous audit. Nothing outside that set was examined.

97. **[T, SIXTH independent audit of `aligned-vmem` — report committed and indexed in the SAME change, per the item-89/90 class lesson that #1186, #1209, and #1233 each had to relearn] `docs/reviews/2026-08-23-131201-aligned-vmem-publication-audit-Сол-кодекс.md` (author Сол-кодекс; reported 2026-08-23 13:12:01 Europe/Berlin; revision `db50f1a`; verdict **NO-GO for publishing `aligned-vmem 0.2.0`** — 1 P0, 3 P2, 1 P3) is a STATIC read-only audit: no tests, build, `cargo check`, Clippy, Miri, benchmarks, docs, or `cargo publish` were run, so its findings are UNVERIFIED BY EXECUTION.** (Filed 2026-08-23, task #1248.)

    - **Status:** OPEN, with the P0 deliberately deferred. F2/F3/F4 landed (task #1249, commit `0e65bfe`, doc/comment-only — verified: `cargo check`/`clippy -D warnings` under default, `fault-injection lazy-commit huge-pages bench-internals`, `--all-features`, and `--cfg aligned_vmem_mock`; `cargo doc --all-features -D warnings`; full `cargo test` under the fault-injection feature set, 60/60 passing). F5 needed no fix (see below). F1 (P0, `crates/aligned-vmem/CHANGELOG.md:7`'s `## 0.2.0 - Unreleased` header) is explicitly NOT actioned per the owner's direct instruction ("P0 пока не обращаем внимания") — this is the crate's genuinely-not-yet-published state, matching item 93's card's own record that task #1207 already resolved the "stale-Unreleased-in-tarball" HAZARD as NULL (the release workflow's own changelog gate at `.github/workflows/release.yml:301-309` already blocks a publish while this line stands): F1 is not a defect to fix, it is the crate correctly refusing to consider itself released, and lifting it is the owner's publish decision, not a doc edit.
    - **Current-number-or-verdict:** NO-GO stands at `db50f1a` (the audited revision) — F2-F4 were documentation-precision fixes, not blockers this audit itself ranked P0/blocking, and are now closed by `0e65bfe`; only F1 (deliberately deferred, an owner publish decision) remains open under this item.
    - **Next trigger:** an owner decision to actually cut a 0.2.0 release (F1), which is a publish decision outside this index's scope, not a follow-up task.
    - **F5, verified this task to need NO code/doc change:** the report's F5 ("the real OS refusal converting to `DecommitOutcome::Refused` remains statically unproven") is, by the report's own text, already an honest, already-recorded observation, not a newly-found gap — its own sentence says so: *"Текущий тестовый комментарий это честно отмечает"* (the existing test comment already notes this honestly). Verified independently (not just trusting the report's parenthetical): the SAME residual is already explicit in three places — this file's own item 92 card (task #1219 status update: *"no test observes a real kernel refusing a real range, and none can... the honest claim is 'the Refused arm is reachable...', not 'the OS refused'"*), `crates/aligned-vmem/src/decommit_outcome.rs`'s `Refused` variant doc ("One test-only second source of this payload... no syscall ran, so there is no `last_os_error` to capture"), and `crates/aligned-vmem/src/api/decommit.rs`'s `dispatch_try_decommit` doc ("No real OS refusal is involved on that injected path — no syscall ran"). No fourth site is needed.
    - **Evidence:** the report itself (74 lines, `wc -l`, committed by this task); F2's site re-read directly (`crates/aligned-vmem/src/reservation.rs:307-310`, the `decommit_reclaims_and_zeroes` scope bullet, against its own sibling `can_decommit_reclaim_and_zero`'s accurate "conservatively" bullet at lines 402-407 and the free `decommit` function's "Huge-page granularity" section in `crates/aligned-vmem/src/api/decommit.rs:107-135`); F3's three sites re-read directly (`Cargo.toml:76-113`, `README.md:84-87`, `CHANGELOG.md:118`, all still commit-path-only despite task #1219's decommit-side hook) plus `decommit_outcome.rs:117`'s "test-only" wording (inaccurate for a public opt-in Cargo feature any downstream consumer can arm in production); F4's census re-read against `crates/aligned-vmem/CHANGELOG.md:290-307` (the "10 real sites / 7 production / 3 test-only" count predates task #1219's fourth test-only site, `VmemError::os_refusal_unknown_code()` at `src/api/decommit.rs:409` inside `dispatch_try_decommit`'s `#[cfg(feature = "fault-injection")]` branch).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents; it explicitly states it ran no tests/build/clippy/miri/benchmarks/docs/`cargo publish` — every finding is a source read, not an execution result. Its own §"Безопасность и корректность" states no new obvious UB was found in the unsafe/FFI/RAII/arithmetic paths it reviewed, and separately records (not as a finding, as context) four unmeasured perf opportunities it declines to call proven speedups absent benchmarks — consistent with this index's own perf/correctness scope split.

94. **[T, FIFTH independent audit of `aligned-vmem` — report committed and indexed in the SAME change, per the item-89/90 class lesson that #1186 and #1209 each had to relearn] `docs/reviews/2026-08-20-fifth-aligned-vmem-publication-audit.md` (agent `@oh`; revision `1b72e73`; verdict **NO-GO for publishing `aligned-vmem 0.2.0`** — 1 High, 3 Medium, 6 Low, 2 Info) is, unlike the third and fourth audits, an EXECUTING audit: it ran `cargo test`, `clippy`, `cargo doc` under two feature sets, `cargo package --list` and all six guard scripts, and it states per finding whether the evidence is executed or read.** (Filed 2026-08-20, task #1233.)

    - **Status (refreshed 2026-08-23, task #1251 — G2; supersedes the "not yet carried into..." clause below):** **F12/#1190's answer HAS landed** in all three named places — `src/reservation.rs:1312` ("**Owner decision (2026-08-20, task #1190): NO.**"), `CHANGELOG.md:350`, and item 90's own card (its OPEN QUESTION block) — verified directly by `grep -n "1190" crates/aligned-vmem/src/reservation.rs crates/aligned-vmem/CHANGELOG.md`, and `grep -rn "not yet answered"` across `src/`, `README.md`, `CHANGELOG.md` returns zero hits. Everything else below (2026-08-20, at filing) is unchanged and still accurate: **#1225/#1226/#1227/#1228/#1229/#1230** landed; F9 was already owned by #1218.
      *(2026-08-20, at filing, the clause this supersedes on the F12/#1190 point only):* OPEN, but the blocking half is already closed. Landed since the audit reported: **#1225** (F1, `368d391`), **#1226** (F2, `3b5bea5`), **#1227** (F3, `2f9d7b9`), **#1228** (F4+F5, `3210f61`), **#1229** (F6+F7+F8, `dcbe0fc`), **#1230** (F10+F11, `56377ee`). **F9 was already owned** by task #1218 before this audit reported it, and that task's description now carries the audit's second, previously-unknown half (the `hasNonCommentChange` regex is blind to git's quoted `diff --git "a/…"` header, which MASKS the first bug by downgrading ERROR to WARNING — so fixing only one of the two turns a false warning into a false error). ~~**F12 is #1190**, the owner decision, ANSWERED 2026-08-20 ("NO" — see that task) but not yet carried into the rustdoc, the card, or the CHANGELOG.~~
    - **Current-number-or-verdict:** both of the NO-GO's original grounds are CLOSED: ground 2 (F1, M2) by #1225; ground 1 (F12/M3) by #1190's answer landing in all three named places (see Status above). This refresh does not itself lift the crate's NO-GO — that is a separate owner decision, and it now rests solely on item 97/98's F1 (the deliberately-deferred `CHANGELOG.md` `## 0.2.0 - Unreleased` header), not on anything this card tracks.
    - **Next trigger (refreshed 2026-08-23, task #1251 — G2):** none remaining for this card's own blocking set. **The line this replaces** said "#1190's answer landing in `src/reservation.rs`, item 90's card, and `CHANGELOG.md`" as a still-pending trigger; that landing completed 2026-08-20, and this line (like item 93's matching Next-trigger) was never refreshed to say so until the seventh independent audit's G2 finding caught it. **Not moved to "Recently resolved"**, for the same reason as items 90/91/93.
    - **Evidence:** the report itself (committed by this task); F1 re-verified by me directly before acting on it — `git log --oneline -1 -S "OS/kernel accepted it" -- crates/aligned-vmem/README.md` resolves to `05557a6` (task #1211, this same wave), and `git show 1522d25 -- crates/aligned-vmem/src/decommit_outcome.rs | grep "^@@"` prints exactly one hunk, `@@ -48,21 +48,58 @@`, proving lines 10-24 were never touched by the task chartered to fix them; the census figures behind F11 re-run at three revisions (43 at HEAD, 44 at `4f4d9f4`, 43 at `1b72e73`), confirming "42" was wrong when written.
    - **What this audit found that the previous four did not, stated because it is the reason to keep running them:** every one of F1-F8 is a defect INSIDE the fixes of the audit before it, and F1 is the sharpest instance the campaign has produced — task #1211 ADDED the false sentence to `README.md` at 09:05 and task #1212 DELETED the identical sentence from `src/` as incorrect at 09:24, each treating the other's file as out of scope. Two tasks of one wave, nineteen minutes apart, on the crates.io front page.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only with no sub-agents, no git writes and no file edits, but DID execute code — this is the first of the five audits to do so, and its "Verified clean" section (~20 surfaces, including the lookup table re-derived 70/70 and card-body preservation re-derived across all three splits) is therefore evidence, not reasoning. Its own brief is reproduced in task #1216's description; the working copy `.audit5-brief.md` is scratch and deliberately not committed. **Filename is ASCII-only on purpose:** the third and fourth reports carry Cyrillic names, and those names are exactly what triggers the `verify-commit-prefixes.mjs` pair of bugs this audit reports as F9.
    - **[Added 2026-08-20, task #1234 — a dispositioned DECISION, not a fifth-audit finding; the audit did not report this] `1522d25`'s commit body overclaims one of its two "#1174 sites", and the decision is to leave `decommit_outcome.rs` WITHOUT a #1174 reference — deliberately.** The body (task #1212) says: *"Two sites still called task #1174 an OPEN gap — `decommit_outcome.rs`'s `Advised` doc and `reservation.rs`'s `try_decommit` payload list. Both now state what #1174 actually proved (zero-fill on readback …) and what it did NOT (physical reclaim …)."* Both halves verified this task, per half: for `reservation.rs` the claim is TRUE — a line naming #1174 was replaced by positive statements ("Task #1174 (closed) added zero-fill-on-readback … What #1174 did NOT"); for `decommit_outcome.rs` it is FALSE — the "open per task #1174" line was DELETED and nothing about what #1174 proved was added; the file's `1174` count goes 1 → 0 across that commit. The body cannot be edited, so the falsity is permanent no matter what any later commit does to the tree. **The shipped contract is unharmed** — re-read in full this task: `decommit_outcome.rs`'s type-level doc still opens "**None of the three variants is a claim about physical memory having actually been reclaimed**", and `Advised`'s own doc still carries "**Never a claim that physical pages were actually returned to the OS**, even on the native backend". **Decision — option (b) of the two posed: do NOT add a #1174 reference to `decommit_outcome.rs`.** Grounds: (1) that file documents the per-backend SEMANTICS of each variant (what "accepted" means on native/mock/miri), not CI coverage — task #1174 is a coverage fact about one CI test, and the surfaces that legitimately carry it are the ones whose subject IS that coverage chain; (2) the fact already lives in FIVE files (counted this task: `src/api/decommit.rs`, `src/api/reserve_aligned_huge.rs`, `src/reservation.rs`, plus `README.md` and `CHANGELOG.md` — the three-file count in the task brief undercounts), and this repository has a named drift class for one fact in N copies (#1161, six reproductions); (3) adding the reference now could not make the commit body true anyway — the body claims what `1522d25` ITSELF did, and that diff is immutable. **Next trigger:** none required; re-open only if a future reader mistakes the absent #1174 reference in `decommit_outcome.rs` for an oversight — this entry is the record that it is a decision. **Evidence (all run this task):** `for s in 1522d25^ 1522d25; do git show $s:crates/aligned-vmem/src/decommit_outcome.rs | grep -c 1174; done` → `1`, `0`; `git show 1522d25 -- crates/aligned-vmem/src/decommit_outcome.rs | grep -E "^[-+].*(1174|zero-fill|read.back|HugePages_Free)"` → exactly one deleted line, zero added; `git show 1522d25 -- crates/aligned-vmem/src/reservation.rs | grep -E "^[-+].*1174"` → one deletion, two additions; full re-read of current `decommit_outcome.rs` (both no-reclaim-claim sentences present); `grep -rc 1174` across the five files named above. **Why an append to item 94, not a new item 95:** the existing-owner check found item 93 records only "`M2 → #1212` DONE" and item 94's F1/F2 dissect OTHER `1522d25` body claims (the "OS/kernel accepted" sentence and the "certified clean" sub-claim), so no card owns this one — but a new item number would require the `docs/CORRECTNESS_OPEN_ITEMS.md` lookup-table row and card-count edits outside task #1234's file scope, leaving the index's "complete item-N → file lookup" claim false (the exact F3 class). Appending to the card whose Evidence block already carries the same `git show 1522d25 -- crates/aligned-vmem/src/decommit_outcome.rs` command follows the dated `[Added …]` precedent inside item 24.

98. **[T, SEVENTH independent audit of `aligned-vmem` — report committed and indexed in the SAME change, per the item-89/90 class lesson that #1186, #1209, #1233, and #1248 each had to relearn] `docs/reviews/2026-08-23-133616-aligned-vmem-readonly-review-GLM.md` (author GLM; reported 2026-08-23 13:36 Europe/Berlin; revision `54f69e9`; verdict **technically GO for publishing `aligned-vmem 0.2.0`** — 1 P0 (action, not a defect; duplicates item 97's F1), 1 P2 (bookkeeping), 1 P3 (docs), plus INFO-level perf/residual context) is a STATIC read-only audit: no tests, build, `cargo check`, Clippy, Miri, benchmarks, docs, or `cargo publish` were run, so its findings are UNVERIFIED BY EXECUTION.** (Filed 2026-08-23, task #1250.)

    - **Status:** OPEN, with the P0 deliberately deferred, matching item 97's posture. G2 and G3 landed (task #1251+#1252, commit `7fb8348`, doc/comment-only — verified: `cargo check`/`clippy -D warnings` under default, full feature set, `--all-features`, and `--cfg aligned_vmem_mock`; `vmem-doc-drift-guard.mjs` clean; full `cargo test` under fault-injection, 111/111 passing). G1 (P0, same `CHANGELOG.md:7` `## 0.2.0 - Unreleased` header item 97/F1 already owns) is explicitly NOT actioned. G4 (perf, INFO) and G5 (INFO, already-documented residuals) needed no action — verified this task, see below.
    - **Current-number-or-verdict:** the FIRST audit of this campaign to return a technically-clean verdict on the code itself — its own text: *"Ни одного нового дефекта кода, UB, утечки или семантического расхождения в прочитанных путях я не нашёл."* — after re-reading (not re-trusting) the key closures of all six prior audits directly against source: errno-capture discipline, strict provenance, checked arithmetic, poison fail-closed, the `from_raw_parts` 2-MiB asserts, the Windows `SystemInfo` layout, and the Linux >= 5.18 huge-decommit contract. The crate remains formally NO-GO for exactly the one reason item 97 already names: `CHANGELOG.md:7`'s `## 0.2.0 - Unreleased` header, deliberately deferred by the owner.
    - **G1 — P0 (action, not a defect): the release act itself.** Duplicate of item 97's F1 (same site, same deferral) — cross-referenced only, not re-owned here.
    - **G2 — P2 (bookkeeping), CONFIRMED by direct re-read, now CLOSED (commit `7fb8348`): items 90/91/93/94's own Status/Next-trigger text still named `#1190`/`#1216` as open, though both were answered/complete.** Item 90's Status said *"This card closes when #1190 is answered"* — but the answer ("NO") was recorded inside that SAME card's own OPEN QUESTION block (`**Appended answer (task #1190, owner decision 2026-08-20): NO.**`), a self-contradiction the card carried since 2026-08-20. Item 91's Status said *"Closes with #1190"* — same gap. Item 93's Status said *"Still open: **#1190** ... and **#1216**"* — task #1216 (the fifth independent audit) was also complete. Item 94's Next trigger said *"#1190's answer landing in `src/reservation.rs`, item 90's card, and `CHANGELOG.md`"* — already landed; the same gap was found, while fixing this, on item 94's own Status/Current-number-or-verdict text too (not explicitly named by this report, caught by direct re-verification). This was the exact R34-24 staleness class item 90's own text warns against ("stayed true-then/false-later for a full wave"). All four cards refreshed in place, old text preserved alongside per item 90's own task-#1208 precedent; none moved to "Recently resolved" — see each card's own refreshed Status for why.
    - **G3 — P3 (docs), CONFIRMED by direct re-read, now CLOSED (commit `7fb8348`): `crates/aligned-vmem/src/mock.rs:31,69` pointed at `src/lib.rs`, stale since the task #1055 monolith split.** Line 31: *"see the `unsafe impl Send for Reservation` and its `SAFETY` comment in `src/lib.rs`"* — the impl lives at `crates/aligned-vmem/src/reservation.rs:1572`. Line 69: *"see those functions' implementations in `src/lib.rs`"* (about `recommit`/`commit_range`'s empty-range no-op not being logged) — the free-function implementations that early-return before `mock::record` now live at `crates/aligned-vmem/src/api/recommit.rs` and `crates/aligned-vmem/src/api/commit_range.rs`. Zero consumer-facing effect (`mock.rs` compiles only under `--cfg aligned_vmem_mock`), but the same doc-drift class `vmem-doc-drift-guard.mjs` otherwise catches.
    - **G4 — INFO (perf): all four candidates already tracked, unmeasured, none a blocker — re-verified against the cited items this task.** 64-bit Unix over-reserve (`docs/perf/OPEN_ITEMS.md` items 48/53), the doomed double `MAP_HUGETLB` attempt on the huge exact-path miss (item 52, rejected NULL four times over), the Windows speculative large-page window (items 55/57), and `fault-injection`'s zero-cost-when-disabled mutex/RMW in the production graph (task #1021/R4-8) — no new perf item filed; nothing here is a proven speedup or a publish blocker.
    - **G5 — INFO: already-honestly-documented residual limitations, re-verified this task, no action needed.** Darwin/BSD eager decommit advisory-only, `decommit_reclaims_and_zeroes()` correctly returns `false` there (item 48). The real-OS-refusal-vs-fault-injection-simulation gap is already covered by item 97's F5 (three existing sites: item 92's card, `DecommitOutcome::Refused`'s doc, `dispatch_try_decommit`'s doc). Physical huge-page pool release has no direct oracle (`HugePages_Free` is observation-only, reasonably so — a kernel-global counter). The reasoned-from-spec-only platform targets (BSD×4, Android, tvOS/watchOS, AArch64 Linux) remain reasoned-from-spec. `try_decommit_lazy`'s absence remains a documented possible future addition, not a gap.
    - **Next trigger:** the same owner release decision item 97 already names for G1 — the crate's NO-GO now rests solely there, nothing else this card or items 90/91/93/94 track remains open.
    - **Evidence:** the report itself (74 lines, `wc -l`, committed by this task); revision claim verified directly (`git rev-parse HEAD` = `54f69e9`, matching); `crates/aligned-vmem/CHANGELOG.md` line count verified directly (`wc -l` = 814, matching the report's own claim); G2's four Status/Next-trigger lines re-read directly at `docs/correctness-open-items/TRACKED_publish_readiness.md` (items 90, 91, 93, 94); G3's two sites re-read directly at `crates/aligned-vmem/src/mock.rs:31,69` against the two `grep` commands quoted above.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents; it explicitly states it ran no tests/build/clippy/miri/benchmarks/docs/`cargo publish` — every finding is a source read, not an execution result. It is the first audit of this campaign to report a fully clean code-level result; the crate's continued NO-GO rests entirely on the owner's deliberately-deferred release act (G1/item 97's F1), not on any remaining code defect this or any prior audit found unaddressed.

99. **[T, EIGHTH independent audit of `aligned-vmem` — report committed and indexed in the SAME change, per the item-89/90 class lesson that #1186, #1209, #1233, #1248, and #1250 each had to relearn] `docs/reviews/2026-08-23-152224-aligned-vmem-readonly-review-oh.md` (author `@oh`, Opus effort=high; reported 2026-08-23 15:22:24 Europe/Berlin; revision `743b27a`; verdict **code-level GO** — formally still NO-GO for the one reason items 97/98 already own — 5 findings F1-F5 (P2×3, P3×2), plus 4 unfiled perf candidates P1-P4) is a STATIC read-only audit: no sub-agents, no tests/build/clippy/miri/benchmarks/`cargo publish` executed, so its findings are UNVERIFIED BY EXECUTION.** (Filed 2026-08-23, task #1253.)

    - **Status:** OPEN, with the P0 deliberately deferred, matching items 97/98's posture. F1-F5 landed (task #1254-#1258, commit `5106142`, doc/comment/additive-trait-only, zero behavior change — verified: `cargo check`/`clippy -D warnings` under default, full feature set, `--all-features`, and `--cfg aligned_vmem_mock`; `cargo doc -D warnings` under `--all-features` and the docs.rs feature set; `vmem-doc-drift-guard.mjs` clean; full `cargo test` under fault-injection and under `--all-features`, 111/111 passing both). F3's option (b) (tighten the eager validator to match the lazy path) stays an explicit, undecided 0.3.0-or-never owner decision — NOT implemented, recorded only. P1-P4 filed to `docs/perf/OPEN_ITEMS.md` item 58 (task #1259, same commit), unmeasured, no code change.
    - **Current-number-or-verdict:** the SECOND consecutive audit to return a clean code-level result (the seventh, GLM's, was the first). All five findings independently re-verified by direct source read this task, not merely trusted from the report: `grep -rn VirtualProtect crates/aligned-vmem/` returns exactly one hit, `README.md:360` (F1); the three F2 sites confirmed bare (`src/reservation.rs:1317`, `README.md:350`, and — verified inside the literal `compile_error!("..." )` string, not merely nearby — `src/os/unix.rs:747`); `validate_size_align` (`src/api/internal.rs:11`) confirmed to check `size.is_multiple_of(PAGE)` (compile-time) while `validate_initial_commit` (`:75`, lazy path only) checks the runtime `page_size()`, and only `api/reserve.rs`/`api/reserve_aligned_huge.rs` call the former without the latter (F3); `unsafe impl Send` confirmed present only for `Reservation` (`src/reservation.rs:1572`), absent for `ReservationParts`/`ReservationFullParts` despite both holding `*mut u8` fields (F4); the double `page_size()` read confirmed in both `LazyReservation::ensure_committed`/`shrink_committed` (`src/lazy_reservation.rs:153,175`) against the free `try_commit_range`/`decommit`'s own internal `page_size_or_poison()` reads (`src/api/commit_range.rs:77`, `src/api/decommit.rs:216`) (F5).
    - **Next trigger:** the same owner release decision items 97/98 already name for the formal NO-GO; F1/F2/F4/F5 close when their owning tasks land; F3's option (b) stays a recorded, undecided 0.3.0 candidate — not a follow-up task — until the owner chooses.
    - **Evidence:** the report itself (366 lines, `wc -l`, committed by this task); revision claim verified directly (`git rev-parse HEAD` = `743b27a` at review time, matching); all five findings' source-level claims re-verified directly this task per the citations in "Current-number-or-verdict" above, not accepted from the report's own text.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no file edits, no git writes; it explicitly states nothing was executed. Its §3 ("Surfaces I re-read and found clean") re-verifies ten categories of prior audits' closures directly against `743b27a` rather than trusting earlier reports — errno/`GetLastError` capture timing, strict provenance, checked arithmetic, poison fail-closed, the `from_raw_parts` asserts (including the task-#1196 four-vs-five-independent-checks correction), Windows `SystemInfo` layout, `Drop`/release-exactly-once, `mock` reentrancy/TLS-teardown, `fault_injection` atomicity, `bench_internals` counter completeness, MSRV, and packaging — none of which is repeated here since the audit's own §3 already states each with its file+line citation.

100. **[T, NINTH independent audit, and the first of this campaign to target `numa-shim` rather than `aligned-vmem`] `docs/reviews/2026-08-23-164206-numa-shim-publication-audit-Sol-codex.md` (author Sol-codex; reported 2026-08-23 16:42:06 Europe/Berlin; revision `87424a45a158c4ace1e4ec097d2c7a319a907eb5`; verdict **NO-GO for publishing the current revision** — F1 P0, F2/F3/F4/F8 P1, F5/F6 P2, F7 P2/P3 — plus 5 unmeasured perf observations) is a STATIC read-only audit: no tests, build, `cargo check`, Clippy, Miri, benchmarks, docs, or `cargo publish` were run, so its findings are UNVERIFIED BY EXECUTION. No new UB found in the unsafe/FFI/ownership paths it reviewed.** (Filed 2026-08-23, task #1261.)

    - **Status:** OPEN — refreshed 2026-08-23 by task #1278 (N9's owning task from the tenth review: this card had read "none of F1-F8 or the perf observations actioned" ever since filing, stale once the wave landed; refreshed per the item-97/98/99 pattern, commits `54f69e9`/`4850234`/`2ebc3c5`), and refreshed AGAIN by task #1286 (E6's owning task from the eleventh review, item 102: #1278's refresh landed mid-wave, while #1275/#1277/#1279 were still in flight, and all three landed within minutes of it — the second consecutive occurrence of the exact staleness class N9 itself flagged; this refresh deliberately runs LAST, after the entire #1274-#1285 wave, per item 102's own structural recommendation that a card refresh belongs in a wave's final commit). Current disposition: **F1 OPEN** (deliberate — the owner version decision, task #1262 not started by design, constrained by item 101's N1 to `0.2.0`, not `0.1.1`, and now also carrying E10's three `README.md` "0.1" pin sites and E1's run-PHASE-1-LAST ordering rule, both already folded into #1262's own task description); **F2 EXECUTED** (#1288 — option (a) landed: the `mock` feature removed, replaced by `--cfg numa_shim_mock`; see RESOLVED.md item 42); **F3 FIXED across all four surfaces** (#1265 reconciled 2 of 4; the 3 residuals of item 101's N7 fixed by #1277, landed `813d9e7`, all three verified closed against source by item 102; the fourth surface, the README, was item 102's E4 — closed by #1284, `4bda76e`); **F4 PARTIAL** (#1266 landed — additive `NodeResolution` API, detection-side only; does not close the binding-side hazard, item 101's N8/§4, which stays open BY DESIGN with its residual now recorded where N8 asked); **F5 DECIDED and EXECUTED** (task #1289 — see this card's F5 addendum below for the full record, including how the actual decision differs from the writeup's own recommended option); **F6 CLOSED** (#1268); **F7 fixed-then-regressed-then-FIXED** (#1269 addressed all three sub-points but introduced item 101's N4 reservation leak and N5 empty-`if`; re-fixed by #1275, landed `41af0fd`, N4/N5 verified closed by item 102, which walked all five `reserve_aligned_numa` exits); **F8 Phase-1 re-run landed, then STALE again** (the original PASS was measured at superseded base `356fb44` per item 101's N12; #1279 re-ran Phase 1 at `c427dd6`, 31/0 passed/failed, commit `663394a` — but item 102's N12/E1 proved that base already superseded too, so the FINAL re-run is owner-gated behind the F1 version bump per E1's run-LAST ordering rule, folded into #1262; Phases 2/4 still outstanding — infrastructure this environment cannot provide); the **5 perf observations FILED** to `docs/perf/OPEN_ITEMS.md` (item 59, #1271); **#1263** (the related stale-unpublished-premise correction, not one of this report's F-numbers) landed as `50b166c`. The tenth independent review (item 101 below) re-verified F1-F8 against source directly; the eleventh — item 102 below, `docs/reviews/2026-08-23-192230-numa-shim-publication-readiness-review-oh.md`, CONDITIONAL GO at `1043b0e` — re-verified tasks #1274-#1279's fixes against source and is NOW the authoritative current re-verification. What remains open on this item is fully owner-gated: F1/#1262 (now including E10's pins, E1's ordering rule, and the thirteenth review's U1/U5/U6 requirements folded into #1262's own description) and F8's final Phase-1 re-run plus phases 2/4. F5 and C1 are both CLOSED as of 2026-08-24: F5 by task #1289 (see the addendum below); C1 by task #1291's push, verified green at CI run `32696488144` on SHA `1afed26`, and reconfirmed again after task #1300's wave at CI run `32702983897` on SHA `a848408`. Filing history: task #1261 was index-only (item-89/90 class convention; owning tasks #1262 (F1), #1264 (F2), #1265 (F3), #1266 (F4), #1267 (F5), #1268 (F6), #1269 (F7), #1270 (F8), #1271 (perf), #1272 (second audit — run, = item 101)).
    - **Current-number-or-verdict:** NO-GO at the audited revision `87424a4` when filed; re-verified still-NO-GO at `472fc98` by the tenth review (item 101), which re-read F1-F8 against the SOURCE directly rather than trusting the fixing tasks' own claims and found the landing state of F3/F4/F7/F8 materially different from those framings; superseded at `1043b0e` by the eleventh review (item 102) — CONDITIONAL GO, no P0/P1/UB/soundness/correctness defect, its three conditions being C1 (push + CI-green on the landing SHA — owner-gated, no TaskList task owns it), C2 (the Phase-1 re-run going stale again, = E1's run-LAST ordering rule, folded into #1262), and C3 (the missing `vmem-integration` Linux CI row, = E2, closed by #1282's `c7c018b` — added to `ci.yml`, though like every CI row this campaign added it has not executed anywhere pending C1). Findings, one line each, current disposition in the header:
      - **F1 (P0) — OPEN, deliberate: the owner version decision (task #1262, not started by design), now constrained by item 101's N1 — the three unrecorded breaking changes to published 0.1.0's `mock` surface force the next version to be `0.2.0`, not `0.1.1`:** `crates/numa-shim/Cargo.toml:3` still `version = "0.1.0"`; `crates/numa-shim/CHANGELOG.md:7` is `## Unreleased` describing 0.1.0 as already published; root `Cargo.toml:914` still pins `numa-shim = "0.1"`; `.github/workflows/release.yml:289-307` requires exactly one dated, non-`Unreleased` version section, so a publish attempt is blocked outright — the same release-workflow gate item 90/93's `## 0.2.0 - Unreleased` finding already relies on for `aligned-vmem`, here blocking a version BUMP rather than a first release.
      - **F2 (P1) — writeup landed, owner decision EXECUTED (task #1264, commit `5ffdc72`; item 101 independently sanity-checked item 42's writeup — central claims all check out, its "next release is already breaking" ground now UNDERSTATED by N1):** `mock = []` (`crates/numa-shim/Cargo.toml:60`) is a non-additive, backend-REPLACING Cargo feature (Cargo unifies features across the whole dependency graph, so any one downstream target enabling `numa-shim/mock` silently swaps real NUMA syscalls for a recording backend for every consumer of that graph) — **re-raises tracked item 42 above** (same hazard, filed 2026-08-09 from a different review); this report adds no new mechanism, only reconfirms it from a fresh read and recommends the same remediation direction item 42 already names (a build-time cfg or a separate test-support crate, following `aligned-vmem`'s own precedent for an analogous seam). **EXECUTED 2026-08-23, task #1288**: option (a) — the `mock` feature is removed, replaced by the build-time `--cfg numa_shim_mock` flag; see this index's RESOLVED.md entry 42 and ARCHIVE.md's full closure narrative.
      - **F3 (P1) — FIXED across all four surfaces (task #1265, commit `ec7d162`, reconciled 2 of 4 places; the 3 residuals of item 101's N7 — the crate-level rustdoc itself still never says `bind_range` is `unsafe`, the crates.io `description` still claims `forbid(unsafe_code)`-friendliness unqualified, and the corrected sentence's "all other unsafe is confined to platform modules" half is itself false — fixed by task #1277, landed `813d9e7`, all three verified closed against source by item 102; the fourth surface, the README, was item 102's E4, closed by #1284 `4bda76e`):** `crates/numa-shim/src/lib.rs:46` ("The public API is safe") contradicted the crate's own `pub unsafe fn bind_range` (`:353`); separately, `README.md:101-104` required the range be a "valid OS reservation" while the real contract at `src/lib.rs:336-351` permitted any live mapped range including heap allocations — two independent doc/contract mismatches needing reconciliation.
      - **F4 (P1) — PARTIAL (task #1266, commits `3a83ee8`+`e4107cf`: the additive `current_node_resolution()`/`NodeResolution` API landed, so callers CAN now distinguish fallback from resolution — but the underlying silent drop is unchanged, and the binding-side hazard is untouched: `bind_range` still silently no-ops on `node >= 64` with no counter or diagnostic, and `FellBackToZero` conflates three materially different causes — item 101's N8 and §4(a)-(d)):** the Linux backend's single `u64` nodemask silently dropped `node >= 64` (`src/lib.rs:868`); topology cache scanned only nodes 0..63, `:733-740`; an unreadable/oversized cpumap or a real CPU on node ≥ 64 fell back to `Some(0)` (`:241-246`, `:741-744`) — documented, but `current_node().unwrap_or(0)` could not distinguish a genuine single-node system from a silently-dropped high node ID.
      - **F5 (P2) — DECIDED and EXECUTED (task #1289, 2026-08-24; scope-expanded to BOTH doc-hidden modules by the original writeup, task #1267 — see this card's F5 addendum below for the full option analysis and the owner's actual choice, which differs from the writeup's recommended option (c); item 101 CORROBORATED the original writeup, more robustly than it argues):** `#[doc(hidden)] pub mod cpumap` (`src/lib.rs:449`) still exposes `format_sysfs_path`/`parse_contains_cpu`/`parse_hex_u32` etc. as real, callable semver surface — `doc(hidden)` hides docs, not the API; removing it later (post-publish) would be a breaking change, so the semver policy needs deciding before the next publish, not after.
      - **F6 (P2) — CLOSED (task #1268, commit `6075c59`: the README example was made self-contained; item 101 records the closure as unverified-by-compilation — nothing compiles the README's rust code fences):** `README.md:57-68`'s `vmem-integration` example imported `aligned_vmem::PAGE`, but `aligned-vmem` was only a transitive optional dependency of `numa-shim` — enabling the feature did not make the `aligned_vmem` name resolvable in a downstream crate's own code, so the example as shown did not compile for a copy-pasting reader.
      - **F7 (P2/P3) — all three sub-points ADDRESSED (task #1269, commits `5b842c7`+`b801b06`) but the fix itself REGRESSED (item 101's N4/N5: the added checked-add error path returns `None` after a successful `VirtualAllocExNuma(MEM_RESERVE)` WITHOUT releasing — a reservation leak the sibling error branch carefully avoids; and the cleanup path's "at least counted, not silently ignored" comment sits over an EMPTY `if` body) — re-fixed by task #1275, landed `41af0fd`, with N4/N5 verified CLOSED by item 102, which walked all five `reserve_aligned_numa` exits to confirm the ownership chain complete rather than just the one site:** Windows path hardening candidates, none a confirmed live bug: `src/lib.rs:1112-1114` built alignment via unchecked `raw_u + align - 1`; `:1140-1145` ignored the `VirtualFree` result on a `MEM_COMMIT` failure cleanup path (silent-leak risk on an already-rare error branch); the successful `VirtualAllocExNuma(MEM_COMMIT)` result was never compared against the expected `base` before `from_raw_parts` consumed it (`:1162-1171`).
      - **F8 (P1) — Phase-1 re-run landed, then STALE again; Phases 2/4 outstanding (task #1270, commits `bf7e1cb`+`9275125`, ran the phases reachable from this host; item 101's N12: the Phase-1 PASS was measured at superseded base `356fb44` — `#1266`'s new API and `#1269`'s Windows change landed inside the gated tree after it, so it covers neither current source nor `node_resolution.rs` — the Phase-1 re-run by task #1279 landed, commit `663394a`, measured at `c427dd6` with 31/0 passed/failed, but item 102's N12/E1 proved that base already superseded too, so the FINAL re-run is owner-gated behind the F1 version bump per E1's run-LAST ordering rule, folded into #1262):** `docs/NUMA_RELEASE_GATE.md`'s required Phase 1-4 runtime gate (mock dispatch, real Linux kernel, Windows virtual NUMA, real multi-socket topology) was not run for this revision — by this audit's own explicit read-only scope, not a newly-discovered process gap; a single-NUMA CI runner cannot substitute for the multi-socket check, and no mbind behavioral oracle exists to confirm the raw syscall was actually accepted and marshalled correctly by the kernel, as opposed to merely not panicking.
      - **Perf observations (5, all explicitly unmeasured — FILED to `docs/perf/OPEN_ITEMS.md` item 59 by task #1271, commit `4efcc76`; still not owned by this card):** (1) `cpu_to_numa_node` is O(nodes × mask bytes) per call even after the topology cache, not O(1) — a `getcpu(2)`-based or reverse-index design is the suggested alternative; (2) cold start does up to 64 sysfs open/read/close triples and a ~64 KiB `Topology` stack frame; (3) `CURRENT_NODE_SLOT: RefCell<u32>` could be a `Cell<u32>` — **re-raises tracked item 45** (`docs/correctness-open-items/TRACKED_misc.md:52`, same defensive-coding/panicking-`borrow_mut()` observation, filed 2026-08-09 from a different review; this report adds no new mechanism); (4) the Windows two-call reserve path already fixed its main commit-charge defect, further syscall-count reduction needs measurement; (5) `mock`'s TLS/recording machinery still costs something even when the feature is compiled in but not exercised as the active backend.
    - **Why filed instead of fixed here:** task #1261 is index-only per the item-89/90 class convention — the report and its card land together, before any finding is acted on; F1-F8 and the perf observations are separately owned by tasks #1262-#1271 (already on the TaskList).
    - **Next trigger:** item 102 below (`docs/reviews/2026-08-23-192230-numa-shim-publication-readiness-review-oh.md`, the eleventh independent review, filed by task #1281, CONDITIONAL GO) is the authoritative current re-verification — read its §0 disposition table and its card before acting on any limb of this item; item 101 (the tenth) remains the intermediate re-verification. The #1274-#1279 wave item 101 filed has landed and was re-verified against source by item 102; item 102's own E-wave follow-ups have since landed too (#1281's `687f877` for E9's line counts, #1282 E2/E8, #1283 E3, #1284 E4/E5, #1285 E7, and #1286 E6 = this card's second refresh, the wave's last step). What remains open is fully owner-gated, in item 102's §7 order: the F1 version decision (#1262 — `0.2.0` not `0.1.1` per N1, the changelog's missing `### Removed`, E10's three `README.md` "0.1" pins, and E1's run-LAST ordering rule for F8 Phase 1, all folded into #1262's own task description); the F5 (#1267 — this card's addendum below) owner decision; F8's final Phase-1 re-run after the version bump, plus phases 2/4 (infrastructure this environment cannot provide); and C1 (push + CI-green confirmation on the landing SHA — explicitly NOT owned by any TaskList task; requires the human owner's explicit authorization to `git push`, which this session has not received for numa-shim). The original trigger's second-independent-audit limb RAN as task #1272 (= item 101 itself); the eleventh audit that card anticipated RAN as task #1280 (= item 102); this item's trigger is NOT fully discharged while F1/F5 and F8's outstanding phases remain open.
    - **Evidence:** the report itself (`docs/reviews/2026-08-23-164206-numa-shim-publication-audit-Sol-codex.md`, committed by this task); `crates/numa-shim/Cargo.toml:3,60`; `crates/numa-shim/CHANGELOG.md:7`; root `Cargo.toml:914`; `.github/workflows/release.yml:289-307`; `crates/numa-shim/src/lib.rs:46,241-246,336-353,449,733-744,868,1112-1145,1162-1171`; `crates/numa-shim/README.md:57-68,101-104`; `docs/NUMA_RELEASE_GATE.md`; item 42 above (the `mock` hazard's original filing); `docs/correctness-open-items/TRACKED_misc.md:52` (item 45, the `RefCell`-vs-`Cell` observation's original filing).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents; it explicitly states no tests/build/`cargo check`/Clippy/Miri/benchmarks/docs/`cargo publish` were run — every finding is a source read, not an execution result. Its own text states no new UB was found in the unsafe/FFI/ownership paths reviewed (Linux raw FFI bounds/`mbind` arguments/`maxnode`/close paths; Windows FFI layout/reserve-commit-release ownership/`from_raw_parts`; Linux cpumap parsing bounds; topology cache allocation-free init and `OnceLock` lifetime; Cargo metadata/MSRV/license/docs.rs wiring; CI/release doc platform coverage) — a static confirmation, not an executed one.
    - **[Added 2026-08-23, task #1267 — F5 scope-expanded: the semver policy for BOTH doc-hidden test-only modules (`pub mod cpumap` AND `pub mod linux`), RECOMMENDATION RECORDED, owner decision pending. Docs-only task — no code changed.]** A later zero-trust review of the original F5 finding expanded its scope: the crate now has TWO `#[doc(hidden)] pub mod` declarations, not one — `pub mod cpumap` (`crates/numa-shim/src/lib.rs:550-551` as of this task; `:449` at the audited revision) exporting `format_sysfs_path`/`parse_contains_cpu`/`trim_end`/`nth_token`/`parse_hex_u32`, and `pub mod linux { pub fn dbg_node_resolution_for_cpu }` (`src/lib.rs:691-693`), added by task #1266 AFTER this audit reported, in exactly the same shape (cfg-gated `#[cfg(all(target_os = "linux", not(miri), not(feature = "mock")))]`). `#[doc(hidden)]` hides items from rendered rustdoc; it does not make them private — every function in both modules is callable by any downstream crate today, so whatever the next publish ships becomes semver surface. Both modules exist to give `tests/` integration tests a target-independent oracle (integration-test binaries link the crate as an external crate and see only `pub` items — the compiler-enforced reason this forwarder pattern exists at all), the pattern CLAUDE.md sanctions as exception category 1 under "One file — one export". The audit's narrower point stands: that sanction was written with this repo's never-published root crate in mind, where "not stable public API" is a workable in-repo convention; for a crate already on crates.io it is invisible to an external consumer and unenforced by the compiler. One policy must cover BOTH modules — they are the same shape of problem, and recommending different dispositions for them would be arbitrary.
      **PREMISE CORRECTION (observed, task #1267) — neither module is in published 0.1.0, so the breaking-change door is still OPEN until the next publish.** The audit's own F5 text ("для уже опубликованного `0.1.0` удаление доступного модуля было бы breaking change") and the task-#1267 brief's "removing either module later would be a breaking change to already-published API" both overstate the current constraint. Measured directly: `git show 845560f:crates/numa/src/lib.rs | grep -c "pub mod cpumap"` returns 0, and that pre-publish tree contains zero `doc(hidden)` occurrences at all — `845560f` (2026-06-28) is the tree's last commit before the 2026-06-29 publish, per this crate's own CHANGELOG. `pub mod cpumap` was introduced 2026-08-09 (`c5e013b`, task #721), six weeks AFTER the publish; `pub mod linux` by `3a83ee8` (task #1266). Consequence: options (a) and (b) below are still NON-BREAKING today; the audit's real deadline — decide before the next major/minor publish — is unchanged and now concrete: the already-queued next release (carrying #1266's `NodeResolution` additive API and the `maxnode` fix) is the publish that would freeze these modules as public API, so the decision must land in that release's scope, not after it.
      **The three options, with the tradeoffs that actually bind (same direction required for both modules):**
      - **(a) Move to a separate test-support crate.** Cleanest in principle, but structurally ASYMMETRIC: `cpumap`'s five functions are pure and self-contained, yet the crate's own production topology cache CALLS them at runtime (`crate::cpumap::format_sysfs_path` at `src/lib.rs:867`, `crate::cpumap::parse_contains_cpu` at `:888`), so the parser cannot live in a dev-only crate — (a) for `cpumap` really means extracting it into a new PUBLISHED utility crate that numa-shim depends on normally (new crate, new release pipeline, new docs surface — disproportionate for five small functions). For `linux::dbg_node_resolution_for_cpu` it is not available at all without further restructuring: the function forwards to `pub(crate)` platform internals (`platform::cpu_to_numa_node_checked`, the crate's own topology cache, `src/lib.rs:710-719`) that no external crate can reach. A UNIFORM (a) therefore implies restructuring the platform internals too — a far larger change than the problem warrants.
      - **(b) Keep an internal non-pub test seam plus integration-test restructuring.** Compiler-blocked in its naive form: integration tests in `tests/` see only `pub` items — there is no `pub(crate)`-visibility path from `tests/`, which is exactly why the forwarder pattern exists. Realizing (b) means moving the oracles inline into `src/` behind `#[cfg(test)]` unit tests, which contradicts this repo's standing "tests live in `tests/`, not inline" convention — itself an owner-level convention change stacked on top of the decision this addendum exists to request. For completeness: feature-gating the modules is NOT a fourth escape hatch — a published Cargo feature is itself published surface (item 42's corrected card and the `mock` feature's own corrected `Cargo.toml` comment record exactly this lesson).
      - **(c) Deliberately declare both modules semver-stable and commit to maintaining them.** Zero restructuring, zero convention change, and it matches how the modules are ALREADY maintained: task #727 hardened `format_sysfs_path`'s buffer sizing to the full `u32` signature (`src/lib.rs:562-573`) precisely because it is reachable `pub` — the functions are already held to a public-quality bar with real behavioral oracles (`tests/cpumap_parser.rs`, `tests/node_resolution_linux.rs`). Cost: a permanent maintenance obligation (any signature change becomes a major bump), a `dbg_`-prefixed, mock-conditional item frozen into the stable surface, and — unless accompanied by un-hiding — the incoherent contract "guaranteed forever but deliberately undocumented".
      **Recommendation (task #1267; NOT a decision — do not implement without owner sign-off):** option **(c)** for BOTH modules at the next release, refined per module: promote `pub mod cpumap` OUT of `#[doc(hidden)]` as real, documented, semver-stable API (small, pure, well-tested, genuinely reusable; stable-but-hidden is an incoherent published contract), and commit `linux::dbg_node_resolution_for_cpu` as a semver-stable hidden seam — frozen as-is unless the owner prefers a pre-publish rename to drop the test-flavored `dbg_` prefix, which is free until that publish and costs a major bump after. Grounds: (a) cannot be applied uniformly without restructuring platform internals; (b) cannot be applied at all without first changing the repo's test-placement convention; (c) is the only zero-restructuring option and ratifies the existing maintenance posture rather than changing it. The implementation work this implies (un-hide + rustdoc for `cpumap`, the CHANGELOG's own "test infrastructure (`#[doc(hidden)]`), not public API" line for task #721, any rename) is deliberately NOT done by this docs-only task.
      - **Status:** F5 = DECIDED and EXECUTED (task #1267's writeup recorded a recommendation; task #1289, 2026-08-24, executed the owner's actual decision — a different option than the writeup's recommended (c), see the addendum below — applying to BOTH the `cpumap` module and task #1266's `linux` module; docs-only, no code/visibility change).
      - **Next trigger:** the owner's pick of (a)/(b)/(c) — must land in the scope of the NEXT version publish (the queued release carrying #1266's `NodeResolution`), because that publish is what freezes both modules as crates.io semver surface.
      - **Evidence:** `crates/numa-shim/src/lib.rs:533-549` (cpumap module's own comment block naming the sanctioned pattern), `:550-551` (`#[doc(hidden)] pub mod cpumap`), `:562-573` (task #727's public-quality hardening), `:688-693` (the `linux` module's comment + cfg + `#[doc(hidden)] pub mod linux`), `:710-719` (`dbg_node_resolution_for_cpu` forwarding to `pub(crate)` `platform::cpu_to_numa_node_checked`), `:867,888` (production call sites into `cpumap::`); `git show 845560f:crates/numa/src/lib.rs` (zero `doc(hidden)`, zero `pub mod cpumap` — the published-0.1.0 premise check, OBSERVED); `git log -S "pub mod cpumap"` → `c5e013b` (2026-08-09, task #721); `git log -S "dbg_node_resolution_for_cpu"` → `3a83ee8` (task #1266); audit report `docs/reviews/2026-08-23-164206-numa-shim-publication-audit-Sol-codex.md` line 62 (the three options in its own words); CLAUDE.md "One file — one export" exception category 1; item 42 above (feature-gating is not a semver escape).
    - **[Added 2026-08-23, task #1290 — F8 phases 2/4: owner risk acceptance RECORDED — a decision, not a closure.]** The owner has explicitly decided (2026-08-23, task #1290) that the queued 0.2.0 release publishes with F8's Phases 2 and 4 outstanding — the infrastructure (Linux kernel for Phase 2, multi-socket metal cloud instance for Phase 4) this environment cannot provide — knowingly overriding, for exactly those two phases and exactly that release, the standing caution both gate-run reports close with ("do NOT cut a 0.x.y release touching `crates/numa-shim/**` on the strength of this run alone"). NOT a judgment that the phases are unnecessary, NOT an amendment of `docs/NUMA_RELEASE_GATE.md` for future releases, and NOT a closure of F8: the E1 final Phase-1 re-run (last, after the version bump, folded into #1262) and Phase 3's in-guest remainder are untouched by it. Full record: `docs/NUMA_GATE_2026-08-23_0.2.0_phase24_waiver.md` (carries the release-SHA placeholder task #1262 must fill). Consumer-facing caveat added to `crates/numa-shim/CHANGELOG.md`'s Unreleased section in the same commit.
    - **[Added 2026-08-24, task #1296 — F5 status update: DECIDED and EXECUTED; also folding T11/T16 into #1262's site list, per the twelfth review's findings T5/T11/T16 (item 103, below). This is this card's fourth consecutive stale-index-card occurrence (N9 → E6 → T5/T6 — the review's own words).]** This card's Status line and F5 finding line above, and the F5 addendum's own Status line, all still read "writeup landed, decision pending"/"awaiting owner decision" — false since task #1289 executed a decision on 2026-08-23. Not a rewrite of the historical addendum above (kept verbatim per the history-is-not-rewritten convention): the addendum's own recommendation was option **(c)** (promote `cpumap` to real documented API, freeze `linux` hidden-but-stable); the OWNER'S ACTUAL DECISION, reached via a later `@fh` consultation the addendum predates, took a fourth path instead — keep BOTH modules structurally as-is (still `#[doc(hidden)]`, still `pub`) and declare both semver-exempt, the `serde::__private` convention. Shipped in `crates/numa-shim/CHANGELOG.md:44-55` ("**DECIDED** (task #1289, owner-confirmed)"), `src/lib.rs:601-607` (`cpumap`) and `:751-757` (`linux`), `README.md:142-146`. **F5 is CLOSED as a decision; no further owner input needed on it.** Separately: `#1262`'s site list above (this card's F1 line, `:398`) and its Evidence line (`:409`) cite the root `Cargo.toml` pin at line 914 — that citation was accurate when item 100 was filed and is left as historical record, but the pin has since moved: task #1288 grew a comment block above it by ~19 lines, so the CURRENT pin is at **`Cargo.toml:933`** (`numa-shim = { path = "crates/numa-shim", version = "0.1", features = ["vmem-integration"], optional = true }`) — task #1262 itself has this corrected line number in its own TaskList description. Also folded into #1262's TaskList description: the `docs/NUMA_GATE_2026-08-23_0.2.0_phase24_waiver.md:19` release-SHA placeholder, `[TO BE FILLED: task #1262's landing SHA]`, which #1262 must fill in on landing — it was not in any site list here or in #1262's original description before this task added it.

101. **[T, TENTH independent audit, the second to target `numa-shim` — run as task #1272, the "second independent audit" limb of item 100's own Next trigger, with F1/F2/F5's full closure still open by design (the brief pre-authorised exactly two known exceptions: F1's version bump and F8's phases 2/4)] `docs/reviews/2026-08-23-183220-numa-shim-publication-readiness-review-oh.md` (author `@oh`, Opus effort=high; reported 2026-08-23 18:32:20 Europe/Berlin; revision `472fc981746891c3c131b6619c3fe5856b8040ae`; verdict **NO-GO for the next crates.io publish of `numa-shim`** — headline **N1 (P0): three unrecorded semver-BREAKING changes to 0.1.0's already-published `mock` surface**, supported by N2/N3 P1, N4/N12 P2, N5-N10 P3, N11 INFO) is a STATIC read-only audit: no sub-agents, no tests, build, `cargo check`, Clippy, Miri, benchmarks, `cargo doc`, or `cargo publish` were run, so its findings are UNVERIFIED BY EXECUTION. Unlike the ninth audit's follow-up tasks, it re-verifies F1-F8 against the SOURCE directly rather than against the fixing tasks' own claims, independently sanity-checks both owner-decision writeups (item 100's F5 addendum CORROBORATED, more robustly than it argues; item 42's F2 writeup's central claims all check out, with its "next release is already breaking" ground now UNDERSTATED by N1 — no corrections offered to either), and reviews task #1266's new `NodeResolution` API (soundness clean, additivity TRUE, four design criticisms). No new UB and no new soundness hole found in the Linux FFI, the Windows FFI, the cpumap parser, the topology cache, or the new API.** (Filed 2026-08-23, task #1273.)

    - **Status:** OPEN — refreshed 2026-08-23 by task #1286 (E6's owning task from the eleventh review, item 102: this card had read "none of N1-N12 actioned by this filing task" ever since filing, stale once the #1274-#1279 wave landed — the second consecutive occurrence of the exact staleness class this report's own N9 flagged for item 100; refreshed per the item-97/98/99/100 pattern, commits `54f69e9`/`4850234`/`2ebc3c5`/`b503140`, and run as the LAST step of the wave, after #1282-#1285 landed, per item 102's structural recommendation that card refreshes belong in a wave's final commit). Current disposition of N1-N12 (item 102's §0 table re-verified all six of tasks #1274-#1279's fixes against source; the post-item-102 E-wave commits are verified by this refresh against their own diffs): **N1 CLOSED** (#1274, `d68f6fc`; premise independently corroborated against the crates.io registry API, item 102 §6); **N2 CLOSED** (#1276 added the plain-default Linux row; item 102 rated the finding PARTIAL→E2 because the real-backend `vmem-integration` row was still missing — E2 closed by #1282, `c7c018b`, which added `cargo test -p numa-shim --features vmem-integration` to the ubuntu job, closing item 102's condition C3 with it — that row DID execute, confirmed by task #1291's push: CI run `32696488144`, job `97339434380`'s raw log shows `reserve_on_node_returns_valid_span ... ok`); **N3 CLOSED** (#1276+#1277); **N4 CLOSED** (#1275, `41af0fd`; item 102 walked all five `reserve_aligned_numa` exits); **N5 CLOSED** (#1275); **N6 CLOSED** (#1277, `813d9e7`; its doc residual E3 closed by #1283, `9074dfe`); **N7 CLOSED** (#1277 fixed all three F3 residuals; the fourth surface, the README, was E4 — closed by #1284, `4bda76e`); **N8 CLOSED as a record** (#1274; the binding-side `node >= 64` hazard itself stays open BY DESIGN, exactly as N8 asked); **N9 CLOSED, re-opened as E6, closed again by this very task** (#1278's refresh of item 100 landed mid-wave and itself went stale; #1286 re-refreshes item 100's card in the same commit as this one); **N10 CLOSED** (#1274); **N11 INFO, never task-owned** (untouched by the wave → item 102's E9; E9's stale-line-counts sub-point was dropped by #1281's own follow-up commit `687f877`, root `README.md`; E9's remaining residuals — the `Resolved(u32)` `#[non_exhaustive]` decision, a `bench-internals`-style gate for `dbg_node_resolution_for_cpu`, and the parallel-implementation point — stay unowned); **N12 CLOSED-THEN-STALE → E1, STILL OPEN** (#1279 re-ran Phase 1 at `c427dd6`, 31/0, commit `663394a`, but item 102 proved that base already superseded; per E1's ordering rule the FINAL re-run happens LAST, after the F1 version bump — owner-gated behind #1262, where the rule is now folded). What remains open is fully owner-gated and stays recorded where it already lives (item 100's card and each writeup's own item/task): the F1 version decision (#1262 — `0.2.0` forced by N1, plus the changelog's `### Removed`, E10's three `README.md` "0.1" pins, E1's run-PHASE-1-LAST ordering rule, and the thirteenth review's U1/U5/U6 requirements, all folded into #1262's own task description) and F8 phases 2/4 (waived for 0.2.0 by task #1290's dated risk-acceptance record — see item 100's card, below the F5 addendum). CLOSED since this Status line's prior refresh: the F2 `mock`-feature decision (item 42 / #1264's writeup — EXECUTED by task #1288); the F5 doc-hidden-semver decision (#1267's writeup — EXECUTED by task #1289, see item 100's F5 addendum); and C1 (push + CI-green confirmation — DISCHARGED by task #1291's push, CI run `32696488144` on SHA `1afed26`, reconfirmed again at CI run `32702983897` on SHA `a848408`). Filing history: task #1273 was index-only (item-89/90 class convention; owning tasks #1274-#1280 — unlike the filing, this refresh can confirm that mapping, via item 102's source-level re-verification).
    - **Current-number-or-verdict:** NO-GO at `472fc98` when filed — per the report's own §0, "for a reason that is NOT yet tracked anywhere" before this card: N1. Superseded: item 102 re-verified at `1043b0e` and returned CONDITIONAL GO (no P0/P1/UB/soundness/correctness defect; of its three conditions, C3's missing CI row was closed by #1282, C2 is E1's ordering rule now folded into #1262, and C1 push+CI-green remains owner-gated). The F1-F8 and N1-N12 lines below are the tenth report's findings AS FILED — a historical snapshot at `472fc98`; item 102's §0 table is the authoritative re-verification, and this card's Status above gives the current per-finding disposition. F1-F8 disposition (the report's §0 table, one line each — finding, owning task, state at `472fc98`, the report's verdict):
      - **F1 (P0) version/changelog/root-pin — #1262 — not started (deliberate) — OPEN, and now UNDERSTATED (see N1/N10).**
      - **F2 (P1) `mock` non-additive feature — #1264 — writeup landed, decision pending — OPEN by design; writeup sound.**
      - **F3 (P1) safety-doc divergence — #1265 — fixed in 2 of 4 places — PARTIAL (see N7).**
      - **F4 (P1) node ≥ 64 silent `Some(0)` — #1266 — additive API landed — PARTIAL (see N8 / the report's §4).**
      - **F5 (P2) `#[doc(hidden)] pub mod` semver — #1267 — writeup landed, decision pending — OPEN by design; writeup sound, premise verified.**
      - **F6 (P2) README `vmem-integration` example — #1268 — fixed — CLOSED (unverified by compilation: nothing compiles the README rust code fences — `src/lib.rs` does not `include_str!` the README and no test extracts them).**
      - **F7 (P2/P3) Windows hardening — #1269 — 3 of 3 sub-points addressed — REGRESSED (see N4, N5).**
      - **F8 (P1) NUMA release gate — #1270 — Phase 1 PASS, 3 phases partial/outstanding — OPEN, and Phase 1 is now STALE (see N12).**
      New findings N1-N12, one line each (severity + the report's own file:line citations; historical as filed at `472fc98` — current disposition in this card's Status above and in item 102's §0 table):
      - **N1 (P0):** three unrecorded semver-breaking changes to published 0.1.0's `mock` surface, all made under `53b3ca2`'s explicitly-stated premise "before this crate's first crates.io publish (task #657)" that was FALSE at the time (0.1.0 published 2026-06-29, per task #1263's crates.io query — corroborated independently by the report, not re-queryable read-only): enum-level `#[non_exhaustive]` on `MockCall` (`dbfeca3`, 2026-07-19), `CALLS`/`CURRENT_NODE_SLOT` `pub` → `pub(crate)` (`53b3ca2`, 2026-08-09), and `#[non_exhaustive]` on `BindRange`/`ReserveOnNode` (`53b3ca2`) — current shape `crates/numa-shim/src/lib.rs:123-179` vs the published surface at `git show 845560f:crates/numa/src/lib.rs`; `50b166c` (task #1263) corrected the false premise for only decision (5) of five (the `mock` feature) and touched exactly two files — NOT `CHANGELOG.md`, whose `## Unreleased` has no `### Removed` and no breaking-change callout at all; consequences: the next version CANNOT be `0.1.1` (SemVer 0.x rules force `0.2.0`), removing one of open task #1262's options, and a 0.1.0 → 0.2.0 upgrader's changelog currently tells them about the `aligned-vmem` return-type move and nothing else; the breaking change is self-proven in-repo by `53b3ca2`'s own commit body (its integration tests failed to compile after the change — the same enforcement downstream consumers hit); also two unrecorded NON-breaking metadata drifts worth one changelog line each (`categories` dropped `no-std::no-alloc` — correctly; `homepage` re-pointed from `crates/numa` to `crates/numa-shim`).
      - **N2 (P1):** `numa-shim`'s real Linux backend has NO per-PR CI coverage — the only ubuntu job (`numa-shim-mock`, `.github/workflows/ci.yml:2611`) runs only `mock`-feature test steps plus `--all-features` clippy, so `smoke.rs`'s `sched_getcpu` + sysfs + real `mbind(2)` tests never run on Linux per-PR (the weekly/dispatch-only `numa-real-kernel` job `:2712` reaches that code only through the ROOT crate's tests, with no `-p numa-shim` step) — AND task #1266's `pub mod linux` + `tests/node_resolution_linux.rs` (both gated `all(target_os = "linux", not(miri), not(feature = "mock"))`) are compiled by NO CI job at all on any platform, so `FellBackToZero` has zero automated coverage anywhere; `tests/node_resolution_linux.rs:4-6`'s own header FALSELY claims "exercised by CI's plain `cargo test -p numa-shim` on ubuntu-latest" — a false claim in a test file's own documentation, the exact class F3 was raised about; fix is one line (`cargo test -p numa-shim` in the ubuntu job).
      - **N3 (P1):** zero rustdoc CI coverage — `crates/numa-shim/Cargo.toml:27-28` declares a docs.rs feature list (`["vmem-integration"]`) narrower than `--all-features`, and `ci.yml` contains NEITHER of the two doc-lint rows CLAUDE.md's rule (task #1142) requires for that shape (every `cargo doc` invocation enumerated: `:65`, `:329`, `:354`, `:2871` — the last with `-D warnings` deliberately removed and no `-p`, so no numa-shim rustdoc warning can even be emitted); and the unconditional crate-level rustdoc's intra-doc links to the feature-gated `reserve_on_node` (`src/lib.rs:32,40,400`) very likely emit `broken_intra_doc_links` warnings in the DEFAULT feature set `cargo add numa-shim` produces — UNVERIFIED-BY-EXECUTION (the report did not run `cargo doc`; the docs.rs-set render was hand-traced and looks clean).
      - **N4 (P2):** task #1269's own F7 "hardening" introduced a reservation LEAK on the error path it added — `crates/numa-shim/src/lib.rs:1282-1302`: `raw_u.checked_add(align - 1)?` returns `None` AFTER a successful `VirtualAllocExNuma(MEM_RESERVE)` and WITHOUT `VirtualFree(raw, 0, MEM_RELEASE)`, while the sibling commit-failure branch `:1328-1346` added by the SAME task carefully releases `raw` before returning `None`; practically unreachable (needs a Win32 reservation within `align` bytes of `usize::MAX`) — the finding is that a hardening change converted a theoretical wrap into a theoretical leak by adding an early-return without extending the function's existing ownership discipline; fix is two lines (release in a `let ... else` before the `return`).
      - **N5 (P3):** the same fix's error-path comment claims the double-failure is "at least counted, not silently ignored" while the `if` body is EMPTY (`src/lib.rs:1332-1344` — comments only, no counter, no flag, no diagnostic), so F7's actual complaint (no way to report the possible leak) is commented about, not addressed; possibly `clippy::needless_if`-flaggable on the `numa-shim-windows` clippy rows with `-D warnings` — UNVERIFIED-BY-EXECUTION; either restore an honest `let _ = …` or add the counter the comment asserts.
      - **N6 (P3):** `current_node_resolution()` reads the mock's scripted state but is NOT recorded (`src/lib.rs:311-322`), contradicting the `mock` module's own "Records **every** invocation" doc (`:70-72`), and the recorded justification ("Recording would … break existing tests' expectations") is factually wrong — every existing test opens with `fresh_drain()`, the two equality-oracle assertions never call the new function, and a `MockCall::CurrentNodeResolution(..)` variant would be additive (the enum is `#[non_exhaustive]`); either record it or amend the module doc to say which functions are recorded.
      - **N7 (P3):** three F3 residuals — the fix landed as `//` line comments (`src/lib.rs:46-48`), invisible in rendered rustdoc, while the actual crate-level rustdoc (lines 1-43) still never says `bind_range` is `unsafe` (F3 was explicitly about the crate-level rustdoc read); `Cargo.toml:7`'s crates.io `description` still ends "forbid(unsafe_code)-friendly for consumers" unqualified — false for one of the crate's three public functions; and the corrected sentence's own second half ("all other unsafe is confined to platform modules") is false — `unsafe fn bind_range_impl_linux` (`:1025`), `unsafe fn libc_mbind` (`:1100`), and the `extern "C" { fn syscall }` block (`:1090`) all live at the crate root.
      - **N8 (P3):** F4's binding-side ask is untouched — `bind_range`'s node ≥ 64 skip (`src/lib.rs:1030`, `if node == NO_NODE || node >= 64 { return; }`) still silently no-ops with no counter, return value, or diagnostic, and `FellBackToZero` conflates three materially different causes (sysfs unreadable; node ≥ 64 — F4's actual hazard; kernel has no NUMA sysfs at all, where node 0 is genuinely CORRECT and not a fallback in any meaningful sense); neither `crates/numa-shim/CHANGELOG.md:63-72` nor item 100 records the residual while F4 reads as "actioned by #1266".
      - **N9 (P3):** item 100's Status card above is STALE — it still says "none of F1-F8 … actioned by this filing task" and "NO-GO stands at `87424a4`" with all eight findings in the present tense (`TRACKED_publish_readiness.md:396-408` per the report), after #1263/#1265/#1266/#1268/#1269/#1270 and both writeup tasks landed; the F5 addendum sits BELOW the card, so the first visible block CLAUDE.md's R34-24 rule makes load-bearing is wrong; this campaign already has the refresh pattern (`54f69e9` item 97, `4850234` item 98, `2ebc3c5` item 99) — item 100 is the one that never got its refresh commit.
      - **N10 (P3):** `crates/numa-shim/CHANGELOG.md` has `### Owner decisions pending` TWICE inside one `## Unreleased` (lines 15 and 83) — easy to lose in exactly the release-time consolidation operation that is pending; merge into one section with two bullets, combined with N1's missing `### Removed`.
      - **N11 (INFO):** smaller items, listed for completeness — `dbg_node_resolution_for_cpu` is reachable in a default-feature Linux build (CLAUDE.md R25-1 rule (2) says such hooks MUST default to `bench-internals` gating; this crate has no such feature) — safe and harmless, a convention point reinforcing the F5 addendum; Windows `current_node_impl`/`current_node_resolution_impl` are copy-paste parallels (`src/lib.rs:1134,1166`); #1266's main commit `3a83ee8` has an empty body (reasoning recoverable only from the code); root `README.md:579` still says "~300 lines" vs `src/lib.rs`'s 1550; `src/lib.rs:1351` is a 151-character line in a `max_width=100` repo (rustfmt most likely leaves the unsplittable macro verbatim — UNVERIFIED-BY-EXECUTION); publish precondition, not a defect: confirm `aligned-vmem 0.2` actually shipped to the registry (its CHANGELOG carries a dated `## 0.2.0 - 2026-08-23` header; the report could not re-query crates.io read-only) before running `cargo publish` for `numa-shim`.
      - **N12 (P2):** F8's one PASSING phase (Phase 1) was measured at base commit `356fb44` (`docs/NUMA_GATE_RUN_2026-08-23_task1270.md:44`), BEFORE `git diff --stat 356fb44 HEAD -- crates/numa-shim/` = +256 lines to `src/lib.rs` plus `Cargo.toml`/`README.md`/`CHANGELOG.md` changes and two new test files — including #1266's new public API and #1269's Windows reserve-path change, both inside the very tree the gate exists to protect; the gate report's own numbers corroborate (28 tests across 4 binaries then vs 5 test binaries at `472fc98`); stale even setting phases 2/4 aside, and re-running costs one command.
    - **Task #1266's new API, reviewed in the report's §4 (one paragraph, because F4's disposition rests on it):** soundness CLEAN (`current_node_resolution()` is a safe `#[must_use]` fn with no pointer arguments that shares the already-audited `sched_getcpu` wrapper and `cpu_to_numa_node_checked` path; the `dbg_` forwarder takes a `u32`, so R25-1's raw-pointer `unsafe fn` rule does not apply) and additivity TRUE (`current_node`/`bind_range`/`reserve_on_node` byte-for-byte unchanged); four criticisms: (a) `FellBackToZero` treats all three of its causes identically, moving F4's conflation one level down rather than removing it — a fourth variant (or a "was topology readable at all" accessor) is cheap and non-breaking (the enum is `#[non_exhaustive]`) but the decision belongs in the release that freezes the enum; (b) it addresses node DETECTION, not the SKIPPED BINDING — `bind_range(ptr, len, 100)` still returns `()` and silently no-ops; (c) `FellBackToZero` is unscriptable under `mock` (the mock arm maps `NO_NODE` → `Unavailable`, everything else → `Resolved(n)`) and its only real test runs in no CI job (N2) — the same defect shape task #722 fixed for `current_node`, reintroduced one function over; (d) the published three-row mapping table between the two functions is an UNENFORCED claim — on Windows/macOS/miri/fallback they are parallel implementations, so a future fix to one can miss the other and nothing would notice; defining `current_node_impl` in terms of `current_node_resolution_impl` on every platform would make the table true by construction; smaller notes: `Resolved(u32)` carries no `#[non_exhaustive]` with no justification recorded either way (unlike the sibling `MockCall` decision the crate documents at length), and no doc warns that `current_node()` and `current_node_resolution()` called back-to-back can disagree if the thread migrates between CPUs.
    - **Why filed instead of fixed here:** task #1273 is index-only per the item-89/90 class convention — the report and its card land together, before any finding is acted on; N1-N12 are separately owned by tasks #1274-#1280 (already on the TaskList), and the report's own §7 gives the recommended order: N1 first (it is F1's real content and must precede everything else), then N4, N5, N2, N3, the F2/F5 owner decisions, N6/N7/N8/N10, N9, N12+F8, then the `aligned-vmem 0.2` registry re-check and publish.
    - **Next trigger:** the limbs this block originally named have all run or landed: N1-N12's owning tasks #1274-#1279 landed (re-verified against source by item 102); item 102's own E-wave landed after it (#1281's `687f877` for E9's line counts, #1282 E2/E8, #1283 E3, #1284 E4/E5, #1285 E7, #1286 E6 = this refresh); and the eleventh independent audit this card once anticipated RAN as task #1280, filed by #1281 as item 102 — CONDITIONAL GO. What remains is the owner-gated set, in item 102's §7 order: (1) C1 — push + confirm CI green on the landing SHA read from the remote (validates #1276's and #1282's never-executed CI rows and every local-only gates-green claim in one step; explicitly NOT owned by any TaskList task, and requiring the human owner's explicit authorization to `git push`, which this session has not received for numa-shim); (2) the F2 (item 42 / #1264) and F5 (#1267's writeup, the addendum on item 100's card above) owner decisions; (3) the F1 version decision (#1262 — `0.2.0`, its full site list now including E10's three `README.md` "0.1" pins and the dated CHANGELOG header with `### Removed`); (4) F8 Phase 1 re-run LAST, after the version bump (E1's ordering rule, folded into #1262; expect 33 tests under `RUSTFLAGS="--cfg numa_shim_mock" cargo test -p numa-shim`, not 31); (5) F8 phases 2/4's owner call (infrastructure this environment cannot provide); (6) publish. E9's remaining INFO residuals stay unowned until someone files them. This card fulfilled item 100's "second independent audit (task #1272)" limb; item 100's trigger is NOT fully discharged while F1/F2/F5 (and F8's outstanding phases) remain open there — and neither is this card's, by the same set.
    - **Evidence:** the report itself (`docs/reviews/2026-08-23-183220-numa-shim-publication-readiness-review-oh.md`, 835 lines, `wc -l`, committed by this task); the revision claim verified directly (`git rev-parse HEAD` = `472fc981746891c3c131b6619c3fe5856b8040ae`, exact match — the report reviewed current HEAD); N1's load-bearing claims spot-checked directly by this filing task, not merely transcribed: `#[non_exhaustive]` at `crates/numa-shim/src/lib.rs:123,144,154`, `pub(crate) static CALLS`/`CURRENT_NODE_SLOT` at `:176,178`, `Cargo.toml:3` = `version = "0.1.0"`, `CHANGELOG.md:7` = `## Unreleased`, `53b3ca2`'s body opening with the false "before this crate's first crates.io publish (task #657)" premise, and `50b166c`'s file list (`crates/numa-shim/Cargo.toml` + `docs/correctness-open-items/ACTIVE.md` only — not `CHANGELOG.md`). The remaining findings' citations (N2-N12) are transcribed from the report, NOT independently re-verified against source by this index-only task — re-verification is each owning task's first step — and the report's own UNVERIFIED-BY-EXECUTION markers (N3's doc-link warnings, N5's clippy exposure, N11's rustfmt read) are preserved as such above.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no file edited, no git write command run; it explicitly states NOTHING was executed — no tests/build/`cargo check`/clippy/Miri/benchmarks/`cargo doc`/`cargo package` — every finding is a source or git-history read, and the report itself marks UNVERIFIED-BY-EXECUTION wherever a finding would ordinarily be settled by running something. Its §6 records what was checked and found clean (Linux raw FFI including the `maxnode = 65` decrement quirk; the cpumap parser's fail-closed bounds; the allocation-free topology cache; Windows FFI layout and reserve/commit/release ownership apart from N4's early return; the `from_raw_parts` argument match against `aligned-vmem` 0.2's signature; the cfg matrix's mutual exclusivity; `mock` recording reentrancy under a global-allocator scenario; #1266's additivity; the raw-log citation discipline behind the F8 gate report; perf item 59's filing quality) — stated so the report does not read as if only defects were looked for.

102. **[T, ELEVENTH independent audit, the third to target `numa-shim`, run as task #1280 after tasks #1274-#1279 landed against item 101's N1-N12] `docs/reviews/2026-08-23-192230-numa-shim-publication-readiness-review-oh.md` (author `@oh`, Opus effort=high; reported 2026-08-23 19:22:30 Europe/Berlin; revision `1043b0e37e403d807b8852b1aa58615edd148bcc`; verdict **CONDITIONAL GO** — no P0, no P1, no UB, no soundness hole, no correctness defect; 9 of N1-N12 CLOSED, N2/N12 PARTIAL, N11 untouched; 11 new findings E1-E11, all P2/P3/INFO) is a STATIC read-only audit plus three read-only crates.io API queries (an explicitly-stated exception to "static", quoted verbatim in its §6) that independently corroborate the 0.1.0-publish-date premise the ninth/tenth reviews took on trust, and confirm `aligned-vmem 0.2.0` / `bench-scale-tool 0.1.0` are both live and unyanked on the registry. Nothing was built or tested. It re-verifies each of tasks #1274-#1279's claimed fixes against current SOURCE (not their commit messages), deliberately hunts for the fix-introduces-a-regression pattern this campaign hit at N4/N5, and finds it did NOT recur at the code level — only one new doc inconsistency (E3).** (Filed 2026-08-23, task #1281.)

    - **Status:** OPEN — E1-E11 not yet actioned by this filing task (index-only, item-89/90 class convention). The verdict's own three conditions are NOT code defects: **C1** (nothing in this 36-commit-deep campaign has been pushed; no CI run exists for any of it — `origin/main` is still `87424a4`, task #1260's aligned-vmem release stamp), **C2** (F8 Phase 1's re-run, task #1279, is already stale again — two more `src/lib.rs`-touching commits landed after it), **C3** (one missing CI row, `--features vmem-integration` on the ubuntu job — the crate's headline Linux `mbind(2)` capability runs on no CI job anywhere). C1 requires a `git push`, which per this repository's own CLAUDE.md the assistant does not do without the human owner's explicit request in this specific session — that authorization was not given for this campaign (contrast the immediately-preceding `aligned-vmem` campaign, where "делай и публикуй крейт" explicitly covered push+publish). C1 is therefore recorded here as an owner-gated step, not assigned to a TaskList task.
    - **Current-number-or-verdict:** CONDITIONAL GO at `1043b0e`. Disposition of item 101's N1-N12 (the report's own table, §0):
      - **N1 (P0) unrecorded breaking changes — #1274 — CLOSED**, premise independently corroborated against crates.io's own API (§6): `numa-shim` 0.1.0 published `2026-06-29T17:36:48.594937Z` with `mock` in its published feature map, so all three recorded breaks are real breaks against real published API.
      - **N2 (P1) no Linux backend CI — #1276 — PARTIAL → E2** (the real, non-mock `reserve_on_node`/`mbind(2)` path still runs on no CI job anywhere; only the plain-default row was added, not the `vmem-integration` row).
      - **N3 (P1) no rustdoc CI / broken links — #1276+#1277 — CLOSED**, unverified by execution (folded into C1).
      - **N4 (P2) reservation leak — #1275 — CLOSED**, and the report walked all five exits from `reserve_aligned_numa` to confirm the ownership chain is now complete, not just the one site.
      - **N5 (P3) empty `if` / false comment — #1275 — CLOSED.**
      - **N6 (P3) resolution not recorded under mock — #1277 — CLOSED**, but see **E3** (a new doc inconsistency about which side of the mapping each mock variant records).
      - **N7 (P3) three F3 residuals — #1277 — CLOSED** for all three; a fourth surface (the README) remains → **E4**.
      - **N8 (P3) F4 residual unrecorded — #1274 — CLOSED as a record** (the binding-side hazard itself stays open by design, as N8 asked).
      - **N9 (P3) item 100 card stale — #1278 — CLOSED then immediately RE-OPENED → E6** (the refresh commit landed mid-wave and now describes three since-landed tasks as "in parallel"/"in flight").
      - **N10 (P3) duplicate CHANGELOG heading — #1274 — CLOSED.**
      - **N11 (INFO) smaller items — unowned — OPEN → E9** (never assigned to a task; untouched by the wave).
      - **N12 (P2) stale Phase 1 PASS — #1279 — CLOSED-THEN-STALE → E1** (mechanically provable from the report's own test-count math: its own report cites 31 tests including `node_resolution 3/3`; `HEAD` has 5 tests in that file after #1277, so the same command now yields 33).
      New findings E1-E11, one line each (severity + citation):
      - **E1 (P2):** F8's Phase 1 re-run (task #1279) is stale again — `41af0fd` (#1275) and `813d9e7` (#1277) both touched `src/lib.rs` after its `c427dd6` measurement. Not a defect, a scheduling problem: recommends an ordering rule — Phase 1 runs LAST, on the final pre-tag revision, after the F1 version-bump commit, never before it.
      - **E2 (P2):** the real Linux `reserve_on_node` path (`src/lib.rs:804-820`, `aligned_vmem::reserve_aligned` + `bind_range_impl_linux` — a real `mbind(2)` against a real fresh `mmap`) is compiled (by the `--all-features` clippy row) but executed by no CI job on any platform; Windows and macOS both have the `--features vmem-integration` row that Linux is missing. Fix: one CI line.
      - **E3 (P3):** `current_node()` records the mock log's RAW pre-map slot value; `current_node_resolution()` (added by #1277) records the MAPPED post-map outcome — individually fine, but #1277's new comment falsely claims the resolution recording "mirrors `CurrentNode`'s … convention", which is the opposite of true, and `MockCall::CurrentNode`'s own doc ("the inner value is what was returned") is separately false on the `NO_NODE` path, which the crate's own test (`mock_dispatch.rs:56-60`) asserts and documents. Doc-only fix: name which side of the mapping each variant records; delete the false "mirroring" claim.
      - **E4 (P3):** none of #1274-#1279 touched `README.md` (verified via `git show --stat` on all six) — the crates.io landing page's `## Public API` block and platform matrix never mention `NodeResolution`/`current_node_resolution()`, permanent API added by #1266 with a CHANGELOG `### Added` entry and the entire subject of F4's remediation.
      - **E5 (P3):** `README.md:59` has a dangling Markdown reference link — `[`reserve_on_node`]` with zero link-reference definitions anywhere in the file (verified: zero lines matching `^\[.*\]:`) — renders literally with brackets on crates.io/GitHub.
      - **E6 (P3):** item 101's own Status card (this card, above — `:424`/`:449`) is stale in exactly the way N9 flagged for item 100: still says "none of N1-N12 actioned" and "Next trigger: N1-N12 landing (tasks #1274-#1280)" after ten of twelve findings landed; separately, #1278's refresh of item 100 is ALSO already stale (describes #1275/#1277/#1279 as "in parallel"/"in flight" — they landed within minutes). Second consecutive occurrence of this exact staleness class; the report recommends a structural fix — the card refresh belongs in the LAST commit of a wave, not a commit written mid-wave.
      - **E7 (P3):** two changes to the published `mock` surface from this same wave are themselves missing from the CHANGELOG — the new `MockCall::CurrentNodeResolution` variant, and the behavior change that `current_node_resolution()` now writes to the mock log at all. Additive/non-breaking, but N1's whole lesson was "unrecorded changes to the published mock surface."
      - **E8 (P3):** the `msrv` (1.88) CI job has explicit `-p sefer-region`/`-p aligned-vmem` rows but no `-p numa-shim` row, so the crate's `cfg(not(feature = "mock"))` arms and all of `pub mod linux` are never compiled at the pinned MSRV — the same class item 88/task #1173 already closed for `aligned-vmem`. Risk assessed low (no post-1.88 API in those arms) — P3, not a blocker.
      - **E9 (INFO):** N11's residuals, never owned by any task, untouched by the wave — root `README.md:580`'s "~300 lines" claim for numa-shim (`src/lib.rs` is 1,597) and "~400 lines" for aligned-vmem, both stale; `NodeResolution::Resolved(u32)` still carries no `#[non_exhaustive]` decision either way; `linux::dbg_node_resolution_for_cpu` still has no `bench-internals`-style gate (convention point, not an R25-1 safety violation since it takes a `u32` not a pointer); the Windows/macOS/miri/fallback parallel-implementation point (tenth review §4(d)) is unactioned.
      - **E10 (INFO):** F1/#1262's tracked checklist (item 100's card) still omits `README.md:33,36,64`'s three `"0.1"` pins — raised only in the tenth review's prose, never folded into the tracked checklist; whoever executes #1262 from the card alone will miss them.
      - **E11 (INFO, the reason the GO is conditional):** `git rev-parse origin/main` = `87424a4`; `HEAD` = `1043b0e`; 36 commits between, including all of #1273-#1279. Every "gates green" claim in those six fix commits is a LOCAL claim from one Windows dev host; the three CI steps task #1276 added have never executed — two `RUSTDOCFLAGS="-D warnings" cargo doc` rows exist specifically to catch defects no local Windows run reproduces, and the plain-Linux test row compiles `pub mod linux` + `tests/node_resolution_linux.rs` for the first time anywhere. `release.yml` structurally blocks `cargo publish` on red/absent CI, so this is not a silent hazard, but per CLAUDE.md an agent's local "tests pass" claim is not a receipt, and this campaign has already found one claimed-good fix (task #1269's F7) that regressed on exactly this kind of unverified trust.
    - **Task #1266's API and the six fixes' regression risk, reviewed in the report's §3 (because the brief specifically asked this review to hunt for a repeat of the N4/N5 pattern):** no functional regression found. Walked all five `reserve_aligned_numa` exits (§2, confirming N4's fix is complete, not just the one site); swept every `MockCall` consumer repository-wide for an exhaustive match the new `#[non_exhaustive]`-enum variant could break (none exists, so it is additive for internal AND external consumers); confirmed the new recording has no behavioral side effect on any existing test's log-count assertions (the root crate never calls `current_node_resolution()`; `node_resolution.rs`'s three pre-existing tests never drain after it); confirmed `record()`'s reentrancy-safety and `CALLS_CAP` bound are unchanged, so the extra call site cannot reintroduce task #726's unbounded-growth hazard; read both of #1276's new CI rows' cfg-compatibility by hand since they compile two never-before-compiled units, and found no cfg mismatch (UNVERIFIED-BY-EXECUTION, exactly what C1 exists to settle). Net: one new DOCUMENTATION inconsistency (E3), zero new functional defects — the fix-breaks-something pattern did not recur at the code level this round.
    - **Why filed instead of fixed here:** task #1281 is index-only per the item-89/90 class convention — the report and its card land together, before any finding is acted on. E1-E10 are fixable without a push and are owned by follow-up tasks already on the TaskList; C1/E11 requires a `git push` the assistant will not perform without the human owner's explicit request in this session; F1/F2/F5/F8-phases-2-4 remain the same owner-gated decisions items 100/101 already record.
    - **Next trigger:** E1-E10 landing (small doc/CI fixes, no push required); then the human owner's decisions, in the order the report's §7 recommends: (1) push + confirm CI green on the landing SHA read from the remote (C1/E11) — validates #1276's three untested CI steps and all six fixes' local-only claims in one step; (2) the F2 (item 42) and F5 (item 101's addendum) owner decisions — both writeups ready, both independently sanity-checked twice now; (3) the F1 version decision (`0.2.0`, not `0.1.1` — forced by N1) and its full site list (crate manifest, root `Cargo.toml:914` pin, three README `0.1` pins per E10, dated CHANGELOG header, tag); (4) F8 Phase 1 re-run LAST, after the version bump (E1's ordering rule — expect 33 tests under `RUSTFLAGS="--cfg numa_shim_mock" cargo test -p numa-shim`, not 31); (5) F8 phases 2/4's owner call (ship with them outstanding + a release-note record, or obtain them first — infrastructure this environment cannot provide either way); (6) publish. The `aligned-vmem 0.2.0` and `bench-scale-tool 0.1.0` registry preconditions are already independently verified satisfied (§6) and need no re-check.
    - **Evidence:** the report itself (`docs/reviews/2026-08-23-192230-numa-shim-publication-readiness-review-oh.md`, 636 lines, `wc -l`, committed by this task); revision claim verified (`git rev-parse HEAD` = `1043b0e37e403d807b8852b1aa58615edd148bcc`, exact match); the three crates.io API query results, quoted verbatim in the report's §6 (numa-shim 0.1.0 publish timestamp + feature map; aligned-vmem 0.2.0 not-yanked; bench-scale-tool 0.1.0 not-yanked); `git show --stat` on all six of `d68f6fc`/`41af0fd`/`719192c`/`813d9e7`/`b503140`/`663394a` establishing exactly which files each touched (cited to prove E4's "none touched README.md" and E6's "none touched item 101's card"); the mechanical staleness proof for E1/N12 (31 reported vs. 33 achievable, from `node_resolution.rs`'s test count before/after `813d9e7`).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no file edited, no git write command run; nothing was built or tested. Its one stated exception — three read-only crates.io registry API queries — are network reads, not builds, explicitly called out and quoted verbatim rather than silently treated as equivalent to a local build. Its §6 records what was checked and found clean beyond the publish-precondition queries: Windows reserve/commit/release ownership across all five function exits; the `MockCall` consumer sweep; `current_node`/`bind_range`/`reserve_on_node`'s continued byte-for-byte additivity; `record()`'s reentrancy and cap; the new Linux test's host-independent design (passes whether or not real sysfs NUMA data exists); the Linux FFI/cpumap parser/topology cache properties the tenth review already cleared, re-confirmed rather than assumed; the cfg matrix's mutual exclusivity; the CI doc-lint rows' correct application of CLAUDE.md's rule (derived feature list, non-empty guard, two non-subsuming rows); item 101's own presence in the correctness index's lookup table.
    - **[Added 2026-08-23, task #1290 — this card's Next-trigger step (5), the F8 phases-2/4 owner call: DECIDED — ship with them outstanding plus a release-note record.]** The owner chose the first of the two options that step names: 0.2.0 publishes with Phases 2/4 outstanding, recorded by a dated waiver (`docs/NUMA_GATE_2026-08-23_0.2.0_phase24_waiver.md`) plus a consumer-facing caveat in `crates/numa-shim/CHANGELOG.md`'s Unreleased section (both in task #1290's commit). Reason: infrastructure this environment cannot provide (no Linux kernel for Phase 2; no multi-socket metal instance/credentials for Phase 4), recorded as observed fact by both gate runs — explicitly NOT a judgment that the phases are unnecessary and NOT an amendment of the gate policy for future releases. The waiver carries the release/tag SHA as `[TO BE FILLED: task #1262's landing SHA]`; #1262 had not landed when the decision was recorded, so whoever executes #1262 must fill it in. Unaffected and still owed in this card's §7 order: C1 (push + CI-green on the landing SHA) and step (4)'s final Phase-1 re-run — this decision touches neither Phase 1 nor Phase 3's in-guest remainder.
    - **[Added 2026-08-24, task #1296 — C1 status update: DISCHARGED, per the twelfth review's finding T6 (item 103, below).]** This card's Status line (above) and its Next-trigger line both still say C1 is unreached ("nothing has been pushed... this session has not received [authorization]") and that #1282's CI row "has not executed anywhere pending C1" — both now false. Task #1291 pushed the CI-red fix and the human owner re-authorized the push (this session, after this card's last refresh). Current fact, independently verified twice (once by item 103's own review, once again by this filing task): `git rev-parse HEAD` == `git rev-parse origin/main` == `1afed26e7776126b09c91a098a049e5d19f53b08`; CI run `32696488144` on that exact SHA is job-level 38 `success` / 3 `skipped` (schedule-only jobs, correctly) / 0 `failure`. #1282's `vmem-integration` Linux row demonstrably executed — job `97339434380`'s raw log shows `reserve_on_node_returns_valid_span ... ok` and `reserve_on_node_large_align_round_trip ... ok`, the crate's headline capability running on its headline platform for the first time in this repository's history (item 103's §6). **C1 is CLOSED.**

103. **[T, TWELFTH independent audit] `docs/reviews/2026-08-24-090217-numa-shim-publication-readiness-review-oh.md` (author @oh, effort=high; reported 2026-08-24 09:02:17 Europe/Berlin; revision `1afed26e7776126b09c91a098a049e5d19f53b08` — `main` and `origin/main` identical for the first time in this campaign; verdict **CONDITIONAL GO on five conditions, none a code change**) is READ-ONLY apart from two load-bearing exceptions: it queried the GitHub Actions API (`gh run view`, including raw job logs for all four `numa-shim-*` jobs) to settle, with log receipts rather than YAML inference, whether the post-#1288 mock rows actually ran mock tests and whether the new Linux `vmem-integration` row actually executed the real `reserve_on_node`/`mbind(2)` path. Both: yes. No P0, no P1, no UB, no soundness hole, no correctness defect, and — for the first time in this campaign — no functional regression introduced by the fixing wave it re-verified.** (Filed 2026-08-24, task #1292.)

    - **Status:** OPEN — refreshed 2026-08-24 by task #1301 (part of the thirteenth review's own recommended §7 order, item 104 below, itself the review that caught this card going stale in the wave that acted on it). Eleven of this card's sixteen findings are CLOSED: T1/#1293, T2/T3/T15/#1294, T4/#1295, T11/T16/#1296 (in-place status corrections landed by this very refresh), T7/#1297 (mitigated, later extended by item 104's own registry-build check), T13/#1298. Two (T5/T6) were PARTIAL after #1296's addendum-only remedy — item 104 (the thirteenth review) found the addendum recorded the truth as new information but did not correct the actual stale Status lines the findings named; this task (#1301) closes that gap by editing items 100's (`:396`,`:402`,`:418`) and 102's (`:426`) Status lines IN PLACE, matching the precedent tasks #1278/#1286 already set on these same cards, while leaving #1296's `:422`/`:489` addenda in place (their substance — which decision the owner actually made — is genuinely new information, not a stale current-state claim). T8/T9/T12 (#1299, 2026-08-25) are now CLOSED — the deferral condition (wait for #1262/publish) was explicitly waived by the owner; T14 is ACKNOWLEDGED with no code fix, per its own text. All sixteen of this card's findings are now closed or acknowledged.
    - **Current-number-or-verdict:** CONDITIONAL GO at `1afed26`, superseded by item 104's CONDITIONAL GO at `a848408` (three new conditions, none a code change: U1/U5 fold into #1262's own final-step commit, U6 asks #1262 to record its Phase-1 re-run the way both prior runs were recorded, U2/U3/U4 are the in-place refresh this very task performs). This card's own CONDITIONAL GO verdict below is a historical snapshot at `1afed26`; item 104's card is the current authoritative re-verification. Item 102's three conditions are settled: **C1 CLOSED** (`git rev-parse HEAD` == `git rev-parse origin/main` == `1afed26e7776126b09c91a098a049e5d19f53b08`; CI run `32696488144` job-level: 38 `success`, 3 `skipped` — the schedule-only jobs, correctly — 0 `failure`, 0 `cancelled`); **C2 unchanged by design**, folded into #1262's ordering rule, now carrying one newly-broken detail (T4); **C3 CLOSED with a log receipt** (`ci.yml:2689`'s Linux `vmem-integration` row ran, job `97339434380`'s log shows `test reserve_on_node_returns_valid_span ... ok` and `test reserve_on_node_large_align_round_trip ... ok` — the crate's headline capability executed on its headline platform for the first time in this repository's history). Task #1288 (the wave's largest change, 24 files) is verified, by filtering its `src/lib.rs` diff to non-comment lines, to be exactly 21 attribute-pair renames (`feature = "mock"` → `numa_shim_mock`) with zero semantic edits — a purely mechanical rewrite. Tasks #1289/#1290 are docs-only by `git show --stat`. Task #1291 is verified complete: only two `windows-latest` jobs exist in `ci.yml`, both audited step-by-step, no other latent occurrence of the bash-`VAR=val`-under-pwsh bug found.
      - **T1 (P3) — one published-rustdoc site #1288 missed:** `crates/numa-shim/src/lib.rs:319` still says `NodeResolution::Unavailable` is reached "Under the `mock` **feature**" — a Cargo feature this release removes — while its sibling variant 32 lines earlier (`:287`) was correctly converted to "under the `numa_shim_mock` cfg". `NodeResolution` is unconditionally public, so this renders on the 0.2.0 docs.rs page and contradicts the CHANGELOG's own `### Removed` entry. The fourth instance of the doc/contract-mismatch class (F3 → N7 → E3 → T1), each time one doc site over from the last fix. **Personally re-verified against current source** (not just the report's citation) before filing this card.
      - **T2 (P3) — `docs/PHASE_NUMA_DESIGN.md:435-436` tells a reader to reproduce a mock test with an invocation and a feature-enablement claim that are both now false** (`numa-aware-mock` no longer enables `numa-shim/mock`; the real gate is a four-conjunct cfg including `numa_shim_mock`) — the specific silent-green-on-empty-binary hazard #1288's own CI sentinels exist to prevent, in the crate's own behavior-design doc.
      - **T3 (P3) — `docs/NUMA_TESTING_OPTIONS.md`'s Phase-1 section still prescribes `mock = []`** and the shipped `crates/numa-shim/Cargo.toml:41` — a file that ships to crates.io — still points a reader at it.
      - **T4 (P2) — the final Phase-1 gate re-run's recorded invocation is now dead.** Both historical gate-run reports and both index cards (`:451`, `:485` above) prescribe `cargo test -p numa-shim --features mock`, which post-#1288 errors with "none of the selected packages contains these features: mock". Fail-loud, not vacuous — a scheduling/record defect, not a correctness hazard — but it blocks #1262's own last step as currently written. Correct invocation, confirmed against run `32696488144`'s log rather than predicted: `RUSTFLAGS="--cfg numa_shim_mock" cargo test -p numa-shim`, expect **33** tests (17 cpumap_parser + 7 mock_dispatch + 5 node_resolution + 4 smoke) — the cards' predicted count was already correct, only the command was wrong. The two historical gate-run reports stay as-is per this repo's history-is-not-rewritten convention; only the index cards need correcting.
      - **T5 (P3) — item 100's F5 disposition is stale**: three sites in this file still present F5 as "awaiting owner decision" (`:396`, `:402`, `:418`) while the decision is DONE and shipped (`CHANGELOG.md:44-55` "DECIDED (task #1289, owner-confirmed)"; `src/lib.rs:601-607`,`:751-757`; `README.md:142-146`).
      - **T6 (P3) — item 102's card still says C1 is unreached and nothing has been pushed** (`:426`, `:451` above) — now false: `origin/main` == `HEAD` == `1afed26`, CI green, #1282's CI row demonstrably executed (C3 above). **T5+T6 are the fourth consecutive occurrence of this staleness class** (N9 → E6 → now, twice) — E6's remedy (refresh in the wave's last commit) was followed by #1286 and still did not hold, because three more commits (#1289/#1290/#1291) landed after that refresh. The report suggests, without filing as a requirement, a mechanical guard: fail a test when a card asserts a task is "pending"/"not started" while that task's commit exists in `git log`.
      - **T7 (P2) — `numa-shim` is the only publish-candidate crate in this workspace with no package-gates CI job** (unlike `sefer-region-gates`/`aligned-vmem-gates`, neither `cargo publish --dry-run` nor `cargo semver-checks check-release` has ever run for this crate). Minimum mitigation before the tag: one manual `cargo publish --dry-run -p numa-shim` (or the release workflow's own dry-run checkbox). Adding the job is the better long-term fix, non-blocking today since 0.1→0.2 is Cargo's legal 0.x major-bump path for all four recorded breaks.
      - **T8/T9 (P3, informational, verified NOT live defects)** — the three converted mock rows on Windows/macOS/macOS-miri carry no per-file sentinel grep (unlike the Linux row), and the root mock row's single sentinel proves only 1 of 6 re-gated files ran; the report independently confirmed from raw CI logs that all four `numa-shim-*` jobs' mock rows DID run real mock tests (7+5 each) in run `32696488144` — not vacuous today, but the class of typo that would go undetected is real, especially at `numa-shim-macos-miri:2827`, the only miri×mock cell in the workflow. **EXECUTED by task #1299 (2026-08-25, `f822975`, merged `cdf845c`):** the deferral condition (wait for #1262/publish, "so they do not churn the release commit") was explicitly waived by the owner — solve now, leave the version bump untouched. T8: `numa-shim-windows` (both mock rows), `numa-shim-macos` (plain mock row), and `numa-shim-macos-miri` now all `tee`+`grep -F` the same two sentinels the Linux row uses (`current_node_records_scripted_value`, `resolution_matches_current_node_resolved_zero`; plus `reserve_preferred_on_node_records_args` on the Windows `vmem-integration` row) — a future cfg typo on any of the three now fails loud instead of silently exiting 0. T9: the root-crate mock row gained 5 more `grep -F` lines (one per remaining re-gated file: `numa_periodic_refresh.rs`, `segment_directory_numa.rs`, `segment_directory_numa_bucket_reuse.rs`, `segment_directory_numa_high_node_ids.rs`, `segment_directory_clear_bit_no_register.rs`), all 6 files now individually proven by name; the row's stale "files" (plural) comment claim is corrected to match. Personally zero-trust reviewed: full diff read (CI YAML + one CHANGELOG entry only, no `src/`/`tests/`/`Cargo.toml` version touched); all 8 sentinel test names independently re-grepped as present unrenamed; the actual `numa-shim` mock+vmem-integration suite independently re-run (both T8 Windows/macOS sentinels present byte-for-byte in real output, not just predicted); the full workspace-root `RUSTFLAGS="--cfg numa_shim_mock" cargo test --all-features --no-fail-fast` (the real T9 row's exact command) independently re-run in full — 251 `test result: ok` blocks, 0 FAILED, all 6 T9 sentinel lines present byte-for-byte. One process correction made before merging: the sub-agent's own commit used `fix(perf):` as its prefix, which fails `scripts/verify-commit-prefixes.mjs`'s own R30-12 taxonomy check (both changed paths — `.github/workflows/ci.yml`, `crates/numa-shim/CHANGELOG.md` — are measurement-only, so `fix(perf)` claims a shipping-code fix with none present); amended to `fix(numa-shim):` before merging, matching the established precedent for this exact class of change (task #1338's `fix(numa-shim): ... add green-and-dead sentinels`, task #1291's `fix(ci): ...`). `numa-shim-macos-miri`'s row could not be re-verified under real miri from this Windows host (same limitation the executing task itself recorded) — indirect evidence only (YAML validity, sentinel names existing unrenamed, the non-miri form of the same command producing the same two sentinel lines).
      - **T10 (INFO, no action requested)** — item 42's `RESOLVED.md` pointer headline, by the `no_stale_doc_references.rs` guard's own byte-identity-to-archive requirement, necessarily reads as the item's ORIGINAL pre-numa-shim-closure text; the actual numa-shim closure is only in the archive entry's trailing paragraph. Not a defect in `8f9bdf9`'s fix — a structural tension between two of this repo's own conventions (R34-24's "must not look stale" vs. history-is-not-rewritten) worth knowing about for any future item closed in halves.
      - **T11 (INFO) — the F8-waiver's `[TO BE FILLED: task #1262's landing SHA]` placeholder is in no card's #1262 site list** (`:396`, `:408`, `:450`, `:484` above enumerate manifest/root-pin/README-pins/CHANGELOG/tag/E1-ordering but not the waiver). Fold it in now, while visible — an unfilled placeholder in a shipped risk-acceptance record would weaken the record's whole purpose (binding acceptance to an exact published tree).
      - **T12 (INFO)** — no MSRV (1.88) CI row compiles the mock arm (`--all-features` can no longer reach it post-#1288); real risk near-zero (the mock module uses only `thread_local!`/`RefCell`/`Vec`/`const`), one optional row closes it. **EXECUTED by task #1299 (2026-08-25, `f822975`, merged `cdf845c`):** added `cargo test -p numa-shim --no-run` under `RUSTFLAGS="--cfg numa_shim_mock"` directly after the `msrv` job's two existing numa-shim rows, matching that job's own established build-only (`--no-run`) pattern. Personally re-verified: the new row builds clean (exit 0) — the pinned 1.88 toolchain was not confirmed available locally, so this was verified on stable as a proxy (same limitation the executing task itself recorded), consistent with this finding's own "real risk near-zero" assessment (no post-1.88 API in the mock module).
      - **T13 (INFO) — two of E9's three residuals remain open and both freeze at publish**: `NodeResolution::Resolved(u32)` carries no `#[non_exhaustive]` decision (both `MockCall` single-field variants document theirs; this one doesn't); the Windows/macOS/miri/fallback `current_node_impl`/`current_node_resolution_impl` pairs remain copy-paste parallels, making the published three-row mapping table an unenforced claim. (The third residual — `dbg_node_resolution_for_cpu`'s R25-1 gating — is now CLOSED, by task #1287, discovered as a side effect of the CI-red fix: **neither the tenth nor the eleventh review realized an automated tripwire, `tests/dbg_hook_safety_tripwire.rs`, already enforces the rule both had rated "a convention point, not a defect"** — a method lesson for this review campaign itself, not just a numa-shim finding.)
      - **T14 (INFO)** — `npm run check` (`scripts/check-all.mjs`/`check-matrix.mjs`) contains zero numa-shim rows; every numa-shim gate this campaign added is CI-only. Not a live risk (CI is green on the landing SHA) — recorded so no future local-only "tests pass" claim is trusted for this crate, mirroring `RESOLVED.md` item 65's identical note for `aligned-vmem`. **Disposition (2026-08-25, task #1299): ACKNOWLEDGED, no code fix — as the finding's own text says, "No fix required unless a numa-shim-gates job (T7's long-term recommendation) is later added, in which case this same gap would apply to it too."** T7 (above) remains its own open, non-blocking recommendation; T14 stays informational until T7 is acted on, at which point the same gap would need addressing for that new job too.
      - **T15 (INFO)** — the one surviving `feature = "mock"` string in the whole tracked tree, `ci.yml:2649`, is inside a past-tense historical comment; grep-verified as the last one.
      - **T16 (INFO)** — every #1262 site list in this file cites the root pin at `Cargo.toml:914`; #1288 grew a comment block above it by ~19 lines, and the pin is now at `Cargo.toml:933`. Purely mechanical, but it is the one site whose omission breaks the build outright (`version = "0.1"` stops matching `0.2.0`).
    - **Regression hunt (the report's own §3, since this review's brief specifically asked it to hunt for a repeat of the F7/N4-N5 and N2/E3 fix-breaks-something pattern):** none found. #1288's cfg conversion cannot have a functional effect (21 attribute renames, no predicate shape changed, no conjunct added/dropped/re-associated — every dispatch function, the `bind_range` short-circuit, the Windows reserve/commit/release chain, the cpumap parser, and the `OnceLock` topology cache are byte-identical to `1043b0e`). Dropping the bench's `required-features` is the change's one behavioral consequence and it is a coverage GAIN (the bench target now builds under rows that previously skipped it, and it compiled clean on all three OSes). `tests/alloc_core_reentrancy.rs`'s skip-polarity inversion is also a coverage gain (it now runs under plain `--all-features`, where it previously skipped). #1289/#1290 are docs-only by `--stat`. #1291 strictly increases executed test count (the two `env:`-carrying rows previously failed the step outright).
    - **Why filed instead of fixed here:** task #1292 is index-only per the item-89/90 class convention — the report and this card land together, before any finding is acted on. T1-T4, T5/T6, T7, T11, T13, T16 are the items the report's own §7 recommends landing before #1262; T8/T9/T12/T14/T15/T10 are explicitly non-blocking, best done in a follow-up so they do not churn the release commit.
    - **Next trigger:** all nine of this card's original §7 steps (1)-(9) have run or landed via tasks #1293-#1298 and this refresh (#1301); item 104 below (`docs/reviews/2026-08-24-102434-numa-shim-publication-readiness-review-oh.md`, the thirteenth independent review, filed by task #1300, CONDITIONAL GO) is the authoritative current re-verification — read its §0 disposition table and its own card before acting on any remaining limb. What remains, per item 104's §7 order: (1) U8/U9/U12's three optional one-line prose fixes (this same refresh, if convenient); (2) **#1262** itself, whose own description now folds in U1/U5/U6 (update the CHANGELOG gate-caveat and waiver Phase-1 text in the same commit as the final gate re-run; record that re-run as a dated report + raw log, matching the pattern both prior Phase-1 runs used); (3) F8 Phase 1 re-run LAST — now execution-confirmed by item 104 to yield exactly 33 on both Windows and Linux CI, not merely predicted; (4) one more `cargo publish --dry-run -p numa-shim` on the post-bump tree (U7); (5) tag, confirm CI green on the landing SHA read from the remote, then publish. T8/T9/T12/T14 (#1299) remain an explicitly non-blocking follow-up set — item 104 looked for new evidence to promote them and found none.
    - **Evidence:** the report itself (`docs/reviews/2026-08-24-090217-numa-shim-publication-readiness-review-oh.md`, committed by this task); revision claim verified (`git rev-parse HEAD` = `git rev-parse origin/main` = `1afed26e7776126b09c91a098a049e5d19f53b08`, exact match, independently re-confirmed before filing this card); CI run `32696488144` job-level JSON (38 success / 3 skipped / 0 failure / 0 cancelled); job `97339434380`'s raw log (`reserve_on_node_returns_valid_span ... ok`, `reserve_on_node_large_align_round_trip ... ok`); `crates/numa-shim/src/lib.rs:287,319` (T1, personally re-read); `docs/correctness-open-items/TRACKED_publish_readiness.md:450,484` (T4, personally grepped for the stale `--features mock` invocation before filing); `git show 48ab4e5 -- crates/numa-shim/src/lib.rs` filtered to non-comment lines (the 21-attribute-pair proof).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no file edited, no git write command run, no `cargo` build/test/clippy/fmt invoked on the review host. Its two stated exceptions — GitHub Actions API queries and raw CI log reads — are network reads of already-completed CI output, not new builds, explicitly called out and used specifically to convert two claims the eleventh review had to leave as UNVERIFIED-BY-EXECUTION (mock-row activation, real-Linux-path execution) into log-verified facts. This filing task personally re-verified two of the report's central claims (T1's doc site, T4's stale invocation) against current source before trusting and filing them, per this campaign's zero-trust convention — not all sixteen findings were independently re-derived, consistent with how items 100-102 were each filed by their own committing task.

104. **[T, THIRTEENTH independent audit] `docs/reviews/2026-08-24-102434-numa-shim-publication-readiness-review-oh.md` (author @oh, effort=high; reported 2026-08-24 10:24:34 Europe/Berlin; revision `a84840824f6e3b5a0a802fe27358489d3d7a68e3` — `main`/`origin/main` identical; verdict **CONDITIONAL GO on three conditions, none a code change, and for the first time in this campaign none of them a finding about the crate's own source**) is the first review in this campaign to RUN `cargo` (isolated `CARGO_TARGET_DIR` outside the repo, `--locked`; `git status --short` byte-identical before/after) rather than stay purely static, converting two of the twelfth review's predictions into execution receipts: T4's corrected Phase-1 invocation really does yield 33/0, and T2's corrected doc invocation really does run 2 tests rather than compile to an empty binary. It also independently re-ran `cargo publish --dry-run -p numa-shim` and went further than task #1297 had: built and tested the packaged tarball's `--features vmem-integration` arm OUTSIDE the workspace against REGISTRY `aligned-vmem 0.2.0`, exercising `Reservation::from_raw_parts` and every symbol numa-shim imports from its optional dependency against the published copy — clean. `git diff 1afed26 a848408 -- crates/numa-shim/src/lib.rs` filtered to non-comment lines is empty: the entire #1293-#1298 wave is 10 doc-comment lines and zero semantic change. No P0, no P1, no UB, no soundness hole, no correctness defect, no functional regression, no out-of-scope edit.** (Filed 2026-08-24, task #1300.)

    - **Status:** OPEN — filed fresh, no refresh cycle yet.
    - **Current-number-or-verdict:** CONDITIONAL GO at `a848408`, superseding item 103's CONDITIONAL GO at `1afed26`. Of item 103's five conditions: **T1, T4, T11, T16 fully CLOSED** (T1 and T4 execution-verified, not just source-read); **T7 MITIGATED and extended** beyond its original ask (§2/§6 of the report — the registry-dependency build gap T7 itself named is now closed too, tracked as this card's U7); **T5/T6 PARTIAL** — task #1296's remedy (append a new dated addendum recording the true state) added a durable correction but did NOT edit the stale Status lines those findings actually named, so a reader hitting item 100's Status line (`:396`, the R34-24 "first visible block") still sees "F5 writeup landed, decision pending" and the C1-unreached claim, uncorrected in place; the C1 half is additionally uncorrected ANYWHERE on item 100 (#1296's C1 addendum landed only on item 102). T13 CLOSED, with its "precedent application, not a new decision" framing independently re-derived and confirmed sound (the cited precedent is real, already extended once before task #1298, and the outcome is the conservative no-op). T2/T3/T15/T11/T16 all CLOSED with source and/or execution verification.
      - **U1+U5 (P2) — task #1262's own mandated last step (the F8 Phase-1 final re-run) will FALSIFY text that ships to crates.io, and #1262's site list does not cover the fix:** `crates/numa-shim/CHANGELOG.md:21-23`'s consumer-facing gate-verification caveat says Phase 1 "passed (31/0 at `c427dd6`... **the final pre-tag re-run is still owed**)" — both clauses become false the moment #1262 runs the re-run (33/0 at the release SHA, obligation discharged), yet the published CHANGELOG would ship describing an obligation already met as still outstanding. Same 31/0 figure and "still owed" sentence duplicated in the waiver (`docs/NUMA_GATE_2026-08-23_0.2.0_phase24_waiver.md:38-44`), whose SHA placeholder #1262 already must fill — the Phase-1 line beside it does not. **Personally re-verified against current source before filing this card.**
      - **U6 (P3) — the final Phase-1 re-run has no recording plan**, unlike both prior Phase-1 runs (each produced a dated gate-run report + a `tee`d raw log). #1262's description currently says only "run it, expect 33" — no output artifact, leaving F8's one passing phase closing on an unrecorded run and violating CLAUDE.md's raw-log-owed rule for the one gate phase 0.2.0 actually passes.
      - **U2+U3+U4 (P3) — the fifth consecutive occurrence of this campaign's stale-index-card class (N9 → E6 → T5 → T6 → U2/U3), and the append-only remedy from item 103/T5-T6 is now shown NOT to be the right tool for correcting a stale Status line** (as opposed to recording new information, which IS what appending is for — the review draws this distinction explicitly and it holds against this card's own established precedent: tasks #1278 and #1286 both corrected these same Status lines by EDITING THEM IN PLACE, not by appending). Item 100's Status line (`:396`) still asserts BOTH F5-pending and C1-unreached; item 103's OWN Status line (`:493`, this very item) already reads "OPEN — filed fresh, no refresh cycle yet" despite this filing task itself landing the wave that closes 11 of its 16 findings — the exact class recurring on the card that reported the class one wave earlier. `docs/NUMA_RELEASE_GATE.md:220` separately (pre-existing from #1290, not from the #1293-1298 wave) states "0.2.0 published 2026-08-23" as fact — false, #1262 has not started.
      - **T13/U8 caveat (INFO) — the `Resolved(u32)` non_exhaustive rationale added by #1298 cites, on the unconditionally-public `NodeResolution` docs.rs page, two types (`MockCall::CurrentNode`/`CurrentNodeResolution`) that live inside `#[cfg(numa_shim_mock)] mod mock`, which never renders on docs.rs** — the conclusion is unaffected (verified independently sound by this review) but a docs.rs reader cannot look up the cited precedent.
      - **U9 (INFO)** — task #1294's `PHASE_NUMA_DESIGN.md` rewrite left one dangling orphaned clause from the phrase it replaced; content correct, sentence no longer parses.
      - **U7 (INFO)** — task #1297's dry-run measured version 0.1.0 (pre-#1262), not the tree that will actually publish; non-blocking since `release.yml`'s own publish step runs with verify ON and re-checks the real tree, but a second manual dry-run after #1262 lands is recommended (seconds, moves any surprise off the irreversible path).
      - **U10/U11/U12 (INFO, trivia)** — three off-by-one line-number self-citations inside item 103 (from #1296's two-line insertion shifting content below it), a pre-existing hand-maintained card-count line in `docs/CORRECTNESS_OPEN_ITEMS.md` that is accidentally correct today rather than maintained, and a pre-existing stale line-number citation in `docs/FEATURE_PROMOTION_STATUS.md:75` (defines at `:740`, not `:616` — substance correct, only the line moved).
    - **Regression hunt (the report's own §3):** none found — the crate's source is byte-identical to `1afed26` (which was already byte-identical to `1043b0e`); the two new doc paragraphs (T2's design-doc rewrite, T13's rationale) both pass `cargo doc -D warnings` under both the exact docs.rs feature set AND `--all-features`; test counts (33 under the mock cfg, 2 for the root invalidation test, the packaged-tarball suite) are unchanged from what T4/T2 predicted. Second consecutive wave with no fix-breaks-something recurrence.
    - **Why filed instead of fixed here:** task #1300 is index-only per the item-89/90 class convention — the report and this card land together, before any finding is acted on. Per the report's own §7 recommended order: condition 3 (the in-place Status-line refresh, U2/U3/U4, plus the optional U9/U8/U12 one-liners) should land FIRST as one commit since it is independent and cheap; conditions 1+2 (U1/U5/U6) fold into task #1262 itself, in the SAME commit that runs the final Phase-1 gate re-run — updated in #1262's own TaskList description by this filing task.
    - **Next trigger:** the report's own §7 order: (1) one card-refresh commit — edit items 100 (`:396`,`:402`,`:418`) and 102 (`:426`) Status lines IN PLACE (not a fourth addendum) to the true current state, refresh item 103's own Status line and Next-trigger, fix item 103's three off-by-one self-citations, correct `NUMA_RELEASE_GATE.md:220`; optionally fold in U8/U9/U12's one-line fixes in the same commit; (2) **#1262** itself, site list now complete (crate manifest, root `Cargo.toml:933`, three README `0.1` pins, dated CHANGELOG header, waiver's `:19` SHA placeholder); (3) F8 Phase 1 re-run LAST, `RUSTFLAGS="--cfg numa_shim_mock" cargo test -p numa-shim`, expect 33 (now execution-confirmed on both Windows and Linux CI, not predicted); (4) in the SAME commit as the re-run — record it as a dated gate-run report + raw log (U6), then update `CHANGELOG.md:21-23` and the waiver's `:38-44` to cite the real re-run figure/SHA and drop "still owed" (U1/U5); (5) one more `cargo publish --dry-run -p numa-shim` on the post-bump tree (U7); (6) tag, confirm CI green on the landing SHA read from the remote, then publish. #1299 (T8/T9/T12/T14) stays explicitly non-blocking — the review looked for new evidence to promote it and found none.
    - **Evidence:** the report itself (`docs/reviews/2026-08-24-102434-numa-shim-publication-readiness-review-oh.md`, 695 lines, committed by this task); revision claim verified (`git rev-parse HEAD` = `git rev-parse origin/main` = `a84840824f6e3b5a0a802fe27358489d3d7a68e3`, exact match, independently re-confirmed before filing); `crates/numa-shim/CHANGELOG.md:21-23` (U1, personally re-read before filing this card — confirmed the exact "31/0... still owed" text); `docs/correctness-open-items/TRACKED_publish_readiness.md:396` (U2, personally re-grepped for both the "F5 writeup landed, decision pending" and the C1-unreached strings before filing — both confirmed still present); CI run `32702983897` job-level JSON (38 success / 3 skipped / 0 failure / 0 cancelled).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only WITH RESPECT TO THE REPOSITORY (no file edited, no git write command run, no sub-agents) but is the first in this campaign explicitly authorized to and actually running `cargo` — every invocation used an isolated `CARGO_TARGET_DIR` outside the repo plus `--locked`, and `git status --short` was byte-identical before and after the full run (verified by the report itself, not merely claimed). This filing task personally re-verified two of the report's central claims (U1's CHANGELOG text, U2's Status-line text) against current source before trusting and filing them, per this campaign's zero-trust convention.

105. **[T, FOURTEENTH independent audit] `docs/reviews/2026-08-24-113155-numa-shim-publication-audit-Sol-codex.md` (author Sol-codex; reported 2026-08-24 11:31:55 Europe/Berlin; revision `58d94da80bf829f0675290546021f5916402fb8c`; verdict **NO-GO — one new P1 functional blocker, one already-tracked P1 residual re-confirmed, two release-process blockers already in flight, and several P2 hardening items**) is a STATIC read-only audit (no sub-agents, no tests/build/`cargo check`/Clippy/Miri/`cargo publish` run; GitHub Actions checked only via a read-only metadata query, which found no CI run for the exact HEAD reviewed — accurate, since that push had not happened yet when this audit ran). **This is the first review in the entire 14-audit campaign to find a genuinely new, previously-unidentified P1 functional defect in already-reviewed code** — confirmed independently by this filing task (see below), not merely transcribed.** (Filed 2026-08-24, task #1302.)

    - **Status:** OPEN — refreshed 2026-08-24 by task #1306's own filing addendum below. The headline P1 (`bind_range` broken by design) is CLOSED, by removal (see the `[Added 2026-08-24, task #1306]` addendum below) — implemented, zero-trust reviewed, and independently re-executed by the closing task, not merely re-read. Remaining on this item: the P2 Windows `debug_assert_eq!` hardening (CLOSED separately by task #1304, landed before this item was even filed — task #1302's filing missed that #1304 already addressed part of what this review flagged; recorded here for completeness) and the P3 mock-doc wording fix (CLOSED by task #1305, same situation). The re-confirmed-but-already-tracked node-`>=`-64 residual is now moot — task #1306 converted it from a silent no-op into a typed `InvalidNode` error, closing it as a side effect of the `bind_range` removal rather than as a standalone fix.
    - **Current-number-or-verdict:** NO-GO at `58d94da`, the first NO-GO verdict since item 102 (the eleventh review, `1043b0e`) — items 103/104 (twelfth/thirteenth reviews) had both progressed to CONDITIONAL GO. This audit reopens the readiness question on a genuine functional-correctness ground, not a process/doc-bookkeeping one.
      - **P1 (NEW, CONFIRMED) — Linux `bind_range` silently no-ops for the exact heap-allocation example the crate's own README teaches:** `src/lib.rs:512-529` (`bind_range`) calls `platform::bind_range_impl` → `bind_range_impl_linux` (`:1099-1131`), which passes `base` directly to `mbind(2)` via `libc_mbind(...)` (`:1120-1128`) and **discards the syscall's return value entirely** — no assignment, no `?`, no check, by original design (task #722 era; the doc comment at `:461-463` already says "silently ignores OS errors... the allocation is always valid regardless of whether binding succeeded"). **This filing task independently verified, via the authoritative `man7.org/linux/man-pages/man2/mbind.2.html` ERRORS section (fetched live, not assumed), that `mbind(2)` returns `EINVAL` when `addr` is not a multiple of the system page size — the kernel does NOT round the address down; it rejects the call outright.** `README.md:48-53`'s headline "Bind a live allocation to a NUMA node" example passes `vec![0u8; 4096].as_mut_ptr()` — a plain heap allocation from the global allocator, whose alignment is that allocator's normal small-object alignment (typically 8-16 bytes on the glibc/system allocator for a 4 KiB request, well below `mmap_threshold`), essentially never page-aligned in practice. **Consequence: the crate's headline documented example, copy-pasted verbatim, silently fails to bind on Linux in the overwhelming majority of runs** — `EINVAL` is returned by the kernel, discarded by the crate, and the caller observes ordinary success with no NUMA policy actually applied. `tests/smoke.rs:25-51`'s `bind_range_on_owned_memory_does_not_panic` only asserts the call doesn't panic and the buffer stays readable — it does not (and structurally cannot, without a kernel-side oracle) assert that binding actually took effect, so this gap has zero test coverage in either direction.
      - **P1 (RE-CONFIRMED, ALREADY TRACKED, not new) — node IDs `>= 64` are silently dropped:** `bind_range_impl_linux:1104-1105` (`if node == NO_NODE || node >= 64 { return; }`) and the topology cache's `0..64` scan range. This is the SAME binding-side hazard items 100/101's F4/N8 already filed and left OPEN BY DESIGN (item 101's card, N8: "the binding-side hazard itself stays open BY DESIGN, exactly as N8 asked" — a deliberate deferral, not an oversight). Honestly documented at `src/lib.rs:465-470` and `README.md:158-161`. No new task filed for this half; it re-confirms the existing F4/N8 residual is still live and still correctly tracked as owner-deferred.
      - **Release-process blockers (BOTH already in flight, not new):** version `0.1.0`/`## Unreleased` — matches F1/#1262, already the next TaskList step per items 103/104's own recommended order; no CI result for the exact HEAD reviewed (`58d94da`) — this audit ran BEFORE task #1302's own push, so by construction no CI existed yet for that SHA; the push happened in this same session, immediately after (see the Evidence line below for the resulting CI run id).
      - **P1/P2 — NUMA gate residual risk restated:** Phases 2/4 waived (matches #1290's already-recorded decision, correctly characterized here as "принятое владельцем исключение, не техническое закрытие риска" / an accepted owner exception, not a technical risk closure — no disagreement with #1290's framing, a restatement of it). Phase 1's final re-run is the same already-tracked U1/U5/U6 requirement folded into #1262.
      - **P2 (NEW, CONFIRMED) — Windows `VirtualAllocExNuma(MEM_COMMIT)` base-pointer match is checked only by `debug_assert_eq!`, compiled out in release builds:** `src/lib.rs:1434` — **personally re-read**: `debug_assert_eq!(committed.cast::<u8>(), base, "VirtualAllocExNuma(MEM_COMMIT) must return the requested base for an already-reserved region")`, followed immediately by `Reservation::from_raw_parts(base, ...)`'s SAFETY comment explicitly relying on "Win32 contract: `committed == base`... verified by debug_assert above." In a release build this assertion is a no-op — if the Win32 API assumption is ever violated, `from_raw_parts` proceeds with a mismatched `base`, constructing a `Reservation` whose bookkeeping does not match what was actually committed. Not a confirmed live bug (the Win32 contract is well-established and no violation is known), but a load-bearing FFI assumption checked only in debug builds is exactly the hardening class CLAUDE.md's verification-first principle targets — release builds should fail closed (release the reservation, return `None`) rather than trust an unchecked assumption.
      - **P2 (already decided, restated as an alternate opinion, not adopted) — `NodeResolution::Resolved(u32)`'s lack of field-level `#[non_exhaustive]`:** this audit recommends the OPPOSITE of task #1298's just-landed decision ("до первой публикации API ещё можно сделать вариант non-exhaustive или заменить его на отдельную struct" / before the first publication the API can still be made non-exhaustive or replaced with a struct). Item 104 (the thirteenth review) independently re-derived and confirmed #1298's reasoning sound just one review cycle ago: the precedent (task #778/F13 for the architecturally-identical `MockCall::CurrentNode(u32)`) is real, already extended once, and the outcome is the conservative no-op that preserves `assert_eq!`-based downstream construction. **Not re-opened by this filing** — a fresh independent opinion against an already-considered-and-affirmed precedent is recorded here for the trail, not treated as new information requiring another owner round; if the owner wants to revisit it, that is their call, not something this review forces.
      - **P2 (already tracked, not new) — cold-path stack/syscall cost:** `Topology`'s `[u8; 1024] × 64` layout and up to 64 `open`/`read`/`close` triples on first `current_node()`. Matches the 5 unmeasured perf observations item 100 already filed to `docs/perf/OPEN_ITEMS.md` item 59 (task #1271) — same mechanism, no new information.
      - **P2 (already tracked, not new) — `#[doc(hidden)] pub` is not real privacy:** matches F5's already-decided (task #1289) semver-exempt-documentation policy; this audit correctly characterizes it as "допустимая зафиксированная политика, не фактическая граница API" (an accepted recorded policy, not an actual API boundary) — agreement with, not a challenge to, the already-landed decision.
      - **P3 (NEW, small, confirmed) — `mock::set_current_node`'s doc says the scripted value is returned by "the next call," but `CURRENT_NODE_SLOT` is not reset:** `src/lib.rs:227-230` — the scripted value is actually returned by ALL subsequent calls until the next `set_current_node`, not just the immediately-following one. One-word-class doc fix: "next call" → "subsequent calls until changed".
    - **Why filed instead of fixed here:** task #1302 is index-only per the item-89/90 class convention — the report and this card land together, before any finding is acted on. The one NEW, CONFIRMED P1 (`bind_range` page-alignment) requires an owner decision among three contract options the report itself lays out (require page-aligned `base` on Linux + fix the README example; accept a page envelope with an explicit caller contract; or surface the syscall's error/status) — matching this campaign's established pattern for API-contract-affecting decisions (F2/F5), this is NOT unilaterally implemented by the filing task. The P2 Windows debug_assert hardening and the P3 mock-doc wording are small, non-contract-changing fixes suitable for direct implementation without an owner round.
    - **Next trigger:** (1) owner decision on `bind_range`'s Linux contract (three options above) — the report's own recommended "naiboleye bezopasnyy malyy variant" (safest small option) is requiring page-aligned `base` and replacing the README's plain-`Vec` example with a page-aligned reservation, but this is presented as a recommendation, not a unilateral pick; (2) implement whichever option the owner selects, with a real regression test that would have caught this (not just "doesn't panic" — an oracle that can distinguish "bound" from "silently not bound," which likely requires either a scripted/mocked assertion of the syscall's arguments, or accepting this is fundamentally unverifiable without root/CAP_SYS_NICE and a real multi-node kernel, in which case the test should at minimum assert page-alignment of the address passed to the real mbind call site as a proxy); (3) the P2 Windows debug_assert hardening (small, no owner decision needed — release-fail-closed is the obviously correct direction); (4) the P3 mock-doc wording fix; (5) then resume the already-in-flight #1262 sequence (items 103/104's own recommended order), now informed by whatever `bind_range` fix landed.
    - **Evidence:** the report itself (`docs/reviews/2026-08-24-113155-numa-shim-publication-audit-Sol-codex.md`, committed by this task); `mbind(2)`'s ERRORS section fetched live from `https://man7.org/linux/man-pages/man2/mbind.2.html` by this filing task (not assumed from the report's citation alone) — confirms "EINVAL... addr is not a multiple of the system page size," with no rounding-down behavior; `crates/numa-shim/README.md:48-53` (the plain-`Vec` example, personally re-read); `crates/numa-shim/src/lib.rs:461-463,1120-1131` (the silently-discarded `mbind` return, personally re-read); `:1434` (the Windows `debug_assert_eq!`, personally re-read); `:227-230` (the mock doc wording, personally re-read); `docs/reviews/2026-08-24-102434-numa-shim-publication-readiness-review-oh.md` (item 104, T13's independent re-confirmation, cited for why the non_exhaustive counter-opinion is not re-opened here).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no file edited, no git write command run, no `cargo` build/test run — a purely static, source-and-man-page read. This filing task went further than a transcription: it independently fetched and verified the `mbind(2)` alignment/EINVAL claim from the authoritative man page (the single fact the entire P1 finding rests on) rather than trusting the report's citation, and personally re-read every file:line citation for the two NEW findings (the `bind_range` gap and the Windows `debug_assert_eq!`) before filing them here.
    - **[Added 2026-08-24, task #1306 — the P1 `bind_range` finding: CLOSED, by removal.]** A follow-up analysis (the orchestrator, prompted by this item's own finding) surfaced a fact neither `@fh` nor `@ox`'s earlier consultations had caught: `mbind(2)`'s default flags (`0`, no `MPOL_MF_MOVE`) affect only FUTURE page faults, not pages already touched — so `vec![0u8; 4096]`'s pages, already faulted in by the `vec!` macro's zero-initialization before `bind_range` is ever called, could never have been retroactively placed even by a "successful" page-envelope fix. This made the three originally-framed options ((a) require alignment, (b) page envelope, (c) surface status) all inadequate on their own — confirmed by a fifth-option recommendation from an independent Sol-codex consultation, `docs/NUMA_BIND_RANGE_CONTRACT_RECOMMENDATION_2026-08-24-121245-Sol-codex.md`: remove the byte-range API entirely, replace it with a reservation-time-only API where "successfully bound" is a true statement by construction. Owner confirmed this direction explicitly. Implemented by task #1306, `2ffc9fc`, zero-trust reviewed personally (core `mbind`/`VirtualAllocExNuma` logic, the root crate's `bind_segment` removal and `reserve_aligned_on_node` rewiring, the unsafe-seam-inventory updates verified against the self-verifying `grep -rnE '^\s*#!?\[allow\(unsafe_code\)\]'` command, and the rewritten `tests/smoke.rs`/`tests/mock_dispatch.rs`/`tests/numa_seam.rs`), then independently re-executed (not just re-read) by this filing task: `cargo fmt --check`, `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings`, `cargo test -p numa-shim --features vmem-integration` (5/5, real backend), `RUSTFLAGS="--cfg numa_shim_mock" cargo test -p numa-shim --features vmem-integration` (17+7+5+5, all passing), and the root crate's `cargo test --test numa_seam --features "numa-aware internals"` (4/4) — all GREEN, independently confirmed rather than trusted from the implementing task's own report. `bind_range` is gone; `reserve_on_node` is renamed `reserve_preferred_on_node` and returns `Result<Reservation, ReserveNumaError>` instead of `Option<Reservation>`; `NodeId` replaces raw `u32` node parameters in the reservation API; the Linux `node >= 64` and unknown-`SYS_MBIND`-architecture cases are now typed errors instead of silent no-ops; the root crate's test-only `bind_segment` unsafe seam is removed, and `reserve_aligned_on_node` (the real production caller) now composes its own best-effort fallback visibly at the call site instead of relying on a hidden fallback inside the shim. All breaking changes recorded in `crates/numa-shim/CHANGELOG.md` under `## Unreleased` (undated — task #1262's job). **This finding is CLOSED. No further action needed on it** — the crate's binding-side headline API is now honest by construction rather than merely better-documented.

106. **[T, FIFTEENTH independent audit] `docs/reviews/2026-08-24-170047-numa-shim-publication-audit-Sol-codex.md`** (author Sol-codex; reported 2026-08-24 17:00:47 Europe/Berlin; revision `8394108bba5c32e6aac8a1cd925814dea40c1bec`; verdict **NO-GO — twelve findings: one P1 semantic blocker, one release-process blocker, and ten P2/P3 hardening/documentation/performance items**) is a STATIC read-only audit (no sub-agents; no tests, build, `cargo check`, Clippy, Miri, benchmarks or `cargo publish --dry-run` run — the user explicitly forbade running them; CI/crates.io checked via read-only external queries only; the only working-tree write was the report itself). **This is the first review to audit the POST-#1306 architecture** — the redesigned `reserve_preferred_on_node` reservation path, not the removed `bind_range`. (Filed 2026-08-24, task #1308.)
    - **Status:** OPEN (release-blocked on F2 only, owner-scheduled) — 11 of 12 findings EXECUTED. All correctness/evidence/documentation findings (F1, F3, F4, F5, F6, F7, F8, F9, F10, F11, F12) closed; F2 (0.2.0 version bump) remains, by explicit owner instruction, deliberately unactioned pending a separate owner signal — not a discovered gap. F5 and F10 are EXECUTED by task #1310 (2026-08-24): the per-node raw-text cpumap cache was replaced by an allocation-free reverse index built once at init (`crate::cpumap::ReverseIndex`), F5's false per-node buffer rationale was rewritten with the correct global-CPU-ID-space derivation (`MAX_INDEXED_CPUS = 8192` from kernel `NR_CPUS` ceilings; init scratch buffer 4096), and both are covered by the new cross-platform `crates/numa-shim/tests/cpumap_reverse_index.rs` (6 tests, incl. the exact multi-node sparse-global-ID F5 scenario and the beyond-capacity degradation parity). **F1 — the review's only P1 — is EXECUTED by task #1308 (2026-08-24):** owner chose fail-closed remapping (via `AskUserQuestion`) over the other two options; `cpu_to_numa_node`'s `.unwrap_or(0)` fallback became `.unwrap_or(NO_NODE)` (the entire behavioral fix, one line — `current_node()`'s existing sentinel-to-`Option` wrapper handles the rest unchanged), `NodeResolution::FellBackToZero` renamed to `TopologyUnavailable` (the old name described a fallback that no longer exists), and every doc comment describing `current_node()`/`current_node_resolution()`/`NodeResolution` rewritten to the new honest semantics. Root crate's `src/alloc_core/numa.rs` needed ZERO changes (its existing `NO_NODE`-as-no-preference composed-fallback pattern from task #1306 already does the right thing automatically) — verified via `tests/numa_seam.rs` (4/4, unmodified, still green). **F4 is EXECUTED by task #1309 (2026-08-24):** `NodeId::new(u32) -> Self` (unchecked) became `NodeId::new(u32) -> Option<Self>`, returning `None` for exactly `NO_NODE` and `Some(_)` for every other `u32` — deliberately not a general node-existence validator, only closing the sentinel-representability gap the finding named; platform validity still lives at `reserve_preferred_on_node`. Every call site across the workspace updated (`src/alloc_core/numa.rs`, README, CHANGELOG, tests, bench); new `tests/node_id.rs` is the regression oracle. Incidentally, while independently verifying #1309's root-crate impact, discovered a genuinely pre-existing, unrelated red: `cargo clippy --features "numa-aware internals" -- -D warnings` fails on `main` with 6 lint errors no CI row exercises — filed as item 107 in `TRACKED_ci_gate_coverage.md`, not blocking this item. **F3+F6+F8 are EXECUTED by task #1311 (2026-08-24):** F8 — the false "8x repeat OOMs quickly" leak claim in `tests/smoke.rs` replaced with a real release oracle (Windows `VirtualQuery`, Linux `/proc/self/maps` scan, four probes spanning the whole reservation); the fragile `current_node().unwrap_or(0)` pattern replaced with loud self-skip when detection returns `None` across all three positive-NUMA-policy tests; `current_node_returns_valid_or_none`'s `node < 64` bound made platform-conditional (Linux-only, `<= u16::MAX` on Windows) instead of universal. F8's 4th sub-point (Windows `MemoryBasicInformation` `_WIN64`-only layout) deliberately left untouched, per F11/task #1313's still-open scope. F6 — the mock gained `mock::set_policy_failure`/`clear_policy_failure` (one-shot, node-scoped) plus new `MockCall::InstallPolicy`/`PolicyFailureRelease` variants, making the two-stage reserve-then-policy contract observable (reservation succeeded + policy failed; errno preserved; released exactly once) — a genuinely additive, breaking-on-the-success-path change (a successful call now records 2 entries, not 1); doc-honesty note added that the mock approximates Linux specifically, not Windows/macOS. F3 — new `crates/numa-shim/tests/policy_oracle_linux.rs`: a Linux-only oracle querying `get_mempolicy(2)`/`MPOL_F_ADDR` on a live reservation to prove the kernel's OWN record shows `MPOL_PREFERRED` for the requested node (not just trusting `reserve_preferred_on_node`'s `Ok` return), with a negative control proving the oracle isn't vacuous; cross-compile-checked only (dev host is Windows) on both `x86_64-unknown-linux-gnu` and `aarch64-unknown-linux-gnu`, first real execution pending a Linux CI run. **F7+F12 are EXECUTED by task #1312 (2026-08-24):** F7a — the self-contradiction in `reserve_preferred_on_node`'s doc comment (reservation time is "the only point where 'successfully bound' is a true statement" sitting four lines above "success does not guarantee physical placement") reconciled into one honest paragraph distinguishing "policy installed" from "placement guaranteed"; "binding"/"bound" language replaced with "preference"/"policy installation" language throughout the doc comments describing the actual contract, kept only for the historical `bind_range` API name and unavoidable subject-matter phrasing. F7b — the README's "no bind-existing-allocation API" paragraph no longer implies this is an OS-level impossibility ("would be a silent no-op lie"); corrected to state Linux `mbind(2)` CAN migrate resident pages via `MPOL_MF_MOVE`/`MPOL_MF_MOVE_ALL`, and that not implementing that is a deliberate crate-scope decision (a different, riskier, partially-failing contract), not a kernel limitation. F12 — the README's claim that a direct `aligned-vmem` dependency is needed to name `Reservation` corrected: `numa_shim::Reservation` is directly nameable via the crate's own `pub use aligned_vmem::Reservation` re-export (`lib.rs:209`); the direct dependency is genuinely needed only for `aligned-vmem`'s own items. The README's public-API summary block was also missing that re-export entry entirely — added. **F9+F11 are EXECUTED by task #1313 (2026-08-24), the final task of this wave:** F11 — owner decision (asked via `AskUserQuestion`, answered): numa-shim officially supports only 64-bit Windows; documentation-only across README/`lib.rs`'s platform matrix/`Cargo.toml`/the `smoke.rs` `PartitionId` comment, no code gate, no behavior change. F9 (safe half only) — `reserve_aligned_numa`'s aligned-base derivation now uses strict-provenance `.addr()`/`.with_addr()` instead of the `raw as usize -> base_u as *mut u8` integer round-trip, mirroring sibling `aligned-vmem`'s own equivalent fix; byte-identical addresses verified directly on this Windows host (not cross-compile-only) via the existing `VirtualQuery`-based over-reservation test. F9's other half — a one-call `MEM_RESERVE | MEM_COMMIT` fast path — deliberately NOT implemented (needs real measurement first per this repo's evidence discipline); filed as an explicitly unmeasured candidate, `docs/perf/OPEN_ITEMS.md` item 60. **All 12 findings from this review are now closed except F2** (the 0.2.0 version bump), which remains deliberately owner-scheduled, not a gap — see this card's own dedicated note above.
    - **Current verdict:** NO-GO on revision `8394108`. The report explicitly states the new reservation path is "close to publishable and noticeably more reliable than the previous version," and that **no confirmed UB, UAF, double-free or mistaken ownership transfer was found in the new Linux/Windows FFI branches** — the NO-GO rests on a semantic defect (F1), release metadata (F2), and missing verification evidence (F3), not on memory-safety.
    - **The twelve findings, with this filing task's own verification verdict per finding.** Six were personally re-verified against the source before filing (not transcribed): **F1 (P1, CONFIRMED)** — `current_node()` maps `NodeResolution::FellBackToZero` to `Some(0)`, so an unreadable/truncated/`>= 64`-node Linux topology becomes an authoritative-looking node 0; the new `reserve_preferred_on_node` then successfully installs a preference for the WRONG node and returns `Ok`, i.e. the fail-closed policy API works correctly but on a wrong input. Verified at `crates/numa-shim/src/lib.rs:431-441` (the documented `FellBackToZero -> Some(0)` mapping table) and `:485-500` (the crate's own doc already admitting `Some(0)` "does not guarantee the calling thread is actually on node 0"); the in-tree consumer path `src/alloc_core/numa.rs:34-35,76-86` does exactly this. The report's recommended fix is fail-closed remapping (`FellBackToZero -> None`) plus preferring `getcpu(2)` over the sysfs cache. **F4 (P2, CONFIRMED)** — `NodeId::new` is an unchecked `const fn` accepting any `u32` (`lib.rs:86-119`) while its own doc at `:98-100` states `NO_NODE` "must NOT be wrapped"; the type does not make the forbidden state unrepresentable. **F5 (P2, CONFIRMED)** — `NODE_CPUMAP_BUF_LEN = 1024`'s justification comment (`lib.rs:953-971`) reasons in "CPUs on a SINGLE node," but a Linux node cpumap is a GLOBAL cpumask whose bit index is the global logical CPU ID, so the buffer bounds global CPU-ID space, not per-node CPU count; the comment's "far below" claim is arithmetically wrong. Practical exposure is low today but the stated rationale is false, and an overflow degrades into F1's silent node-0 fallback. **F7 (P2 docs, CONFIRMED)** — `lib.rs:545-551` calls reservation time "the only point where successfully bound is a true statement" and `:553-554` immediately says `MPOL_PREFERRED` is a soft preference that "does not guarantee physical placement"; the two sit four lines apart. **F8 (P2/P3, CONFIRMED for its first sub-point)** — `tests/smoke.rs:144-153`'s comment claims the 8-iteration repeat loop "OOMs quickly" if `Drop` frees only `span` instead of `reservation_len`; 8 × 8 MiB is ~64 MiB of virtual address space with almost no physical commit, which a 64-bit process will not OOM on — the stated leak oracle does not hold. **F12 (P3 docs, CONFIRMED)** — `README.md:65-70` says a direct `aligned-vmem` dependency is needed to name the return type, but `lib.rs:173-181` deliberately `pub use aligned_vmem::Reservation` for exactly that purpose; the direct dependency is genuinely needed for `page_size`/`PAGE`/the explicit fallback, not for the type name. The remaining findings were NOT independently re-verified by this filing task and are filed as the report's claims: **F2** (release metadata: version still `0.1.0`, `## Unreleased` CHANGELOG, README/root pins at `0.1`, waiver SHA unfilled), **F3** (no independent oracle that a successful `mbind` actually installed the VMA policy — the smoke test treats `Ok` as its own proof; plus incomplete real-NUMA gate phases), **F6** (the mock cannot script policy-failure-after-successful-reservation, and diverges from the real backends on Windows `node >= 64` and macOS), **F9** (Windows always does reserve+commit as two calls with an over-reserve, where a single `MEM_RESERVE | MEM_COMMIT` fast path is available for `align <= allocation granularity`, as the sibling `aligned-vmem` already implements), **F10** (Linux hot lookup remains O(nodes × cpumap bytes) over a ~64 KiB cache), **F11** (32-bit Windows support is undefined: the platform matrix says "Windows," the test FFI layout is hardcoded 64-bit, no i686 CI row).
    - **F2 deliberately NOT filed as a task — explicit owner instruction.** The owner instructed, in the same message that delivered this review, to ignore the version-bump item ("игнорируем бап версии... я сам скажу когда это сделать") and deleted the pre-existing task #1262 that tracked it. F2 remains a genuine, correctly-identified release blocker and is recorded here in the index so it is not lost, but it is owner-scheduled, not agent-scheduled: no task exists for it and none should be created without an explicit instruction.
    - **Next trigger:** (1) F1 — DONE via task #1308 (fail-closed remapping, owner-chosen via `AskUserQuestion`); (2) F4 — DONE via task #1309 (`NodeId::new` valid-by-construction w.r.t. `NO_NODE`); (3) F5/F10 together — DONE via task #1310 (the reverse-index option was implemented; `getcpu(2)` remains an unmeasured future candidate tracked in `docs/perf/OPEN_ITEMS.md` item 59); (4) F3/F6/F8 — DONE via task #1311 (real release oracle, self-skip discipline, platform-conditional bound; scripted two-stage mock; Linux-only `get_mempolicy` policy oracle — F3's first real execution still pending a Linux CI run); (5) F7/F12 — DONE via task #1312 (soft-preference wording reconciled; Reservation re-export claim corrected); (6) F9/F11 — DONE via task #1313 (strict-provenance refactor, 64-bit-only Windows policy decision; the one-call fast path deferred as an unmeasured `docs/perf/OPEN_ITEMS.md` candidate, item 60); (7) F2 — owner-scheduled only, the sole remaining item.
    - **Evidence:** the report itself (`docs/reviews/2026-08-24-170047-numa-shim-publication-audit-Sol-codex.md`, committed by this task); the six file:line citations personally re-read and confirmed by this filing task, listed per-finding above; CI run `32741635275` on the reviewed revision `8394108` (the report notes it was still `in_progress` with 0 failed jobs at snapshot time — this filing task tracks its final outcome separately under task #1307, which is what produced `8394108` in the first place).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no file edited apart from its own report, and — by explicit user constraint — with NO test, build, lint or dry-run execution whatsoever; every conclusion is static source reading plus external read-only queries against crates.io and GitHub Actions. The report itself states this limitation and marks its runtime/placement conclusions as unproven. This filing task independently re-read and confirmed six of the twelve findings against the actual source (F1, F4, F5, F7, F8, F12) before filing them; the other six (F2, F3, F6, F9, F10, F11) are filed as the report's claims, not as independently confirmed defects, and are labelled as such above.

108. **[T, SIXTEENTH independent audit] `docs/reviews/2026-08-24-204022-numa-shim-publication-audit-Sol-codex.md`** (author Sol-codex; reported 2026-08-24 20:40:22 Europe/Berlin; revision `9137c514775ca539a99e454945cf6a6103cc7ecb` — the head of the fifteenth-review remediation wave, previous base `8394108`; verdict **NO-GO until the P1/P2 findings below are fixed — one P1, four P2, two P3 perf non-blockers**) is a STATIC read-only audit (no sub-agents; no tests, build, clippy, miri or benchmarks run; the only working-tree write was the report itself). This is the first review of the post-item-106 state; it explicitly confirms all seven of that wave's fixes as landed and correct (fail-closed `current_node()`, reverse index, `NodeId` sentinel exclusion, mbind errno discipline, Windows strict provenance, soft-preference wording, `#[non_exhaustive]`/re-export docs) and finds **no new confirmed UB, double-free or ownership defect in the Linux/Windows unsafe paths**. (Filed 2026-08-24, task #1317.)
    - **Status:** OPEN — release-blocked on F2 only (owner-scheduled) — ALL 5 of 5 P1/P2 findings from this review EXECUTED (P1 task #1318, P2-EINTR task #1319, P2-provenance+P2-mock-hygiene task #1320, P2-32bit task #1321); the 2 P3s are perf-only and already tracked (`docs/perf/OPEN_ITEMS.md` items 59/60), no action pending measurement. F2-class release metadata (0.2.0 bump) is NOT re-raised by this review as a finding, and remains owner-scheduled per item 106's dedicated note. **P1 EXECUTED by task #1318 (2026-08-24, `546e8d8`):** the `Err(ReserveNumaError::Os(e))` arm in `tests/policy_oracle_linux.rs` now classifies `e.raw_os_error()` — `EINVAL`/`EFAULT`/`ENOSYS`/`None` PANIC (implementation-bug wording, errno number+name in the message); every other errno keeps the F8.2 container-case skip, now printing the errno number. Negative control needed no code change (already fails loud via `.expect`), documented with an explanatory comment. Test-only, no production code touched, no version bump. Personally zero-trust verified: full diff read (2 files, +87/-9, exactly in scope), independently re-ran `cargo fmt --check`, `cargo check -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --tests`, and `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings` myself before merging — all green. **P2-EINTR EXECUTED by task #1319 (2026-08-24, `652d505`):** `read_cpumap_into`'s `open`/`read` loops now retry a captured `EINTR` (errno 4) a bounded number of times (`EINTR_RETRY_LIMIT = 16`, consecutive-only — resets on forward progress) via a small pure `should_retry_eintr` helper, instead of treating it as a permanent failure; every other errno, and retry-limit exhaustion, fail closed exactly as before (no fd leak on any exit path). Production-code change (`src/lib.rs`, not test-only) — read with extra care, every existing `SAFETY` comment's validity preserved, function signature and `Option<usize>` contract for non-EINTR cases unchanged. Personally zero-trust verified: full diff read, independently re-ran `cargo fmt --check`, cross-compile check on both `x86_64-unknown-linux-gnu`/`aarch64-unknown-linux-gnu`, `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings`, and the full mock+`vmem-integration` test suite (46/46 passing) myself before merging — all green. **Merge also picked up an unrelated same-day fix, task #1323 (`f325bb1`, pushed and CI-confirmed): CI run `32763139107` on `9137c51` (the sixteenth review's own reviewed revision) was genuinely red — `take_policy_failure_for` (from task #1311/F6) is `pub(crate)` and its only caller is `#[cfg(feature = "vmem-integration")]`-gated, but CI's mock-clippy job deliberately builds `RUSTFLAGS="--cfg numa_shim_mock" cargo clippy --all-targets` WITHOUT that feature, making the function genuinely dead code under that exact combination — a gap neither zero-trust review nor `npm run check` covers (a per-crate CI job, not part of the root-crate matrix). Fixed with `#[cfg_attr(not(feature = "vmem-integration"), allow(dead_code))]`, the same precedent already used for `mod platform`. Not a sixteenth-review finding — noted here only because the merge that landed P2-EINTR also carried it forward on `main`.** **P2-provenance + P2-mock-hygiene EXECUTED by task #1320 (2026-08-24, `6f242f1`):** `tests/policy_oracle_linux.rs`'s `get_mempolicy_addr` now takes `*mut core::ffi::c_void` directly instead of a `usize` round-trip; both call sites (the positive oracle and the negative control) pass `r.as_ptr().add(page).cast::<core::ffi::c_void>()` — no numeric intermediary, matching task #1313's Windows-production-path provenance fix. `tests/mock_dispatch.rs`'s `policy_failure_script_for_other_node_does_not_fire` now calls `mock::clear_policy_failure()` at test end (the sibling `policy_failure_script_is_one_shot` already did — checked, no other leak found). Both fixes test-only, no production code touched, no version bump. Personally zero-trust verified: full diff read (3 files, +17/-8), independently re-ran `cargo fmt --check`, cross-compile check for the provenance fix on `x86_64-unknown-linux-gnu`, `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings`, and directly EXECUTED `tests/mock_dispatch.rs` on this Windows host (11/11 passing, including the touched test) myself before merging. **P2-32bit EXECUTED by task #1321 (2026-08-24, `25e25e7`):** `src/lib.rs` now emits `compile_error!` under `cfg(all(windows, target_pointer_width = "32"))`, right after the crate's inner attributes, naming the already-decided task #1313/F11 policy; the three documentation sites (`Cargo.toml`'s header comment, `src/lib.rs`'s crate-doc platform matrix, `README.md`'s platform note) updated from "documentation-only, no code gate" to "compile-time enforced", kept in sync per `Cargo.toml`'s own instruction. Does NOT revisit the 64-bit-only policy itself (already owner-decided in task #1313) — purely makes it technically unbypassable. `tests/smoke.rs` deliberately left untouched (neither `_WIN64` comment used the stale "no gate" framing; a genuinely pre-existing, unrelated stale parenthetical at `:287` predates task #1313's decision and was correctly out of this task's narrow scope). No version bump. Personally zero-trust verified: full diff read (4 files, +34/-9), independently re-ran `cargo fmt --check` and personally reproduced all three points of the review's own three-way oracle myself (not just trusting the agent's report) — `cargo check --target i686-pc-windows-msvc` FAILS with the exact policy message, `cargo check --target i686-unknown-linux-gnu` stays green (Windows-specific, not pointer-width-alone), `cargo check` on the host (`x86_64-pc-windows-msvc`) stays green — before merging.

    **All twelve findings from the sixteenth review are now closed except the two already-tracked, unmeasured P3 perf candidates** (Windows one-call fast path, `docs/perf/OPEN_ITEMS.md` item 60; reverse-index cold start / `getcpu(2)` comparison, item 59) and F2-class release metadata, which this review does not itself re-raise and which remains owner-scheduled per item 106's dedicated note. The wave that closed this review's five P1/P2 findings ran as four sequential `/crush` + worktree tasks (#1318-#1321), each personally zero-trust reviewed (full diff read, independent re-run of the exact failing/passing verification commands, never trusting the agent's own report) before merging, plus one incidental same-day CI-red fix (task #1323, unrelated to this review's own findings, described above) discovered and fixed mid-wave when `origin/main`'s real CI (not just local checks) was confirmed red.
    - **The findings, with this filing task's own verification verdict per finding — ALL FIVE P1/P2 personally re-verified against source before filing (not transcribed):**
      - **P1 (CONFIRMED) — the Linux policy oracle skips on ANY `ReserveNumaError::Os`:** `crates/numa-shim/tests/policy_oracle_linux.rs:190-202` — personally re-read: the `Err(ReserveNumaError::Os(e))` arm unconditionally `eprintln!`-skips and returns, with a comment naming only the legitimate cgroup-restricted case. But `Os` also carries implementation errors — `EINVAL` (bad addr/len/maxnode marshalling), `EFAULT`, `ENOSYS` (wrong syscall number on a new arch) — so a regression that makes `mbind` ALWAYS fail leaves CI green via skip. The oracle built by task #1311 to prove the flagship API works can therefore vacuously pass exactly when the API is broken. Fix: split expected environment errno (skip, allowlisted) from implementation errno (hard fail — at minimum `EINVAL`/`EFAULT`/`ENOSYS`), print the errno in the skip diagnostic.
      - **P2 (CONFIRMED) — `EINTR` during first topology load permanently disables NUMA detection:** `crates/numa-shim/src/lib.rs:1452-1493` (`read_cpumap_into`) — personally re-read: any `read()` returning `-1` closes the fd and returns `None` with NO errno check; the caller then commits the incomplete `ReverseIndex` into a `OnceLock` for the process's whole lifetime. One signal delivered during the first `current_node()` call turns a healthy topology into a permanent `None` (fail-closed, so not unsafe — but a real availability defect). Fix: capture errno before `close()`, retry `read` (and `open`) on `EINTR` with a bounded retry count; all other errno keep today's fail-closed behavior.
      - **P2 (CONFIRMED) — the policy oracle itself contains an integer pointer round-trip:** `tests/policy_oracle_linux.rs:215` (`unsafe { r.as_ptr().add(page) } as usize`) and `:148` (`addr as *mut core::ffi::c_void` inside `get_mempolicy_addr`) — personally re-read. The same strict-provenance discipline task #1313 just applied to the Windows production path is violated by the test written in the same wave. Test-only, no production exposure, but the inconsistency is real. Fix: pass the raw pointer through unchanged (`*mut c_void` parameter), no usize intermediary.
      - **P2 (CONFIRMED) — mock test leaves a scripted policy failure armed in thread-local state:** `crates/numa-shim/tests/mock_dispatch.rs:280-330` (`policy_failure_script_for_other_node_does_not_fire`) — personally re-read: the test arms node 5's failure slot, exercises node 3, and ends (`drop(r2)`) WITHOUT `clear_policy_failure()`. `POLICY_FAILURE_SLOT` is thread-local, and the test harness reuses worker threads, so a later test on the same thread that touches node 5 inherits the armed failure — currently masked only by neighboring tests defensively clearing state at START, an implicit ordering dependency. Fix: explicit cleanup at test end (guard or trailing `clear_policy_failure()`), removing the ordering dependence.
      - **P2 (CONFIRMED) — 64-bit-Windows-only remains documentation-only with no compile gate:** `crates/numa-shim/Cargo.toml:15-24` — personally re-read: the comment itself says "Documentation-only policy — no `cfg(target_pointer_width)` gate is enforced"; `grep compile_error\|target_pointer_width src/lib.rs` confirms only doc-comment mentions, no gate. A 32-bit Windows user gets a compiling artifact whose test FFI layout (`MEMORY_BASIC_INFORMATION` in `tests/smoke.rs`) hardcodes `_WIN64` — false compatibility confidence. The review recommends the enforceable variant of the SAME policy the owner already chose in task #1313/F11: a `compile_error!` for `all(windows, target_pointer_width = "32")`. This ENFORCES, not reverses, the owner's 64-bit-only decision.
      - **P3 (already tracked, not new) — Windows one-call `MEM_RESERVE | MEM_COMMIT` fast path:** unmeasured; `docs/perf/OPEN_ITEMS.md` item 60, filed by task #1313. The review agrees it is not a correctness blocker and must be A/B-measured first.
      - **P3 (already tracked, not new) — reverse-index cold start (up to 64 `open`/`read`/`close` on first call) and `getcpu(2)` comparison unmeasured:** matches `docs/perf/OPEN_ITEMS.md` item 59's existing unmeasured candidates; the review explicitly endorses keeping the current reverse-index solution until numbers exist.
    - **Next trigger:** (1) P1 — DONE via task #1318 (errno-classified skip/fail split, `546e8d8`); (2) P2-EINTR — DONE via task #1319 (bounded retry, `652d505`); (3) P2-provenance + P2-mock-hygiene — DONE via task #1320 (`6f242f1`); (4) P2-32bit — DONE via task #1321 (`compile_error!` gate, `25e25e7`); (5) then the SEVENTEENTH independent readiness review on the resulting head (task #1322, ready to launch). P3s: no action until measured, per items 59/60.
    - **Evidence:** the report itself (committed by this task); the five file:line citations personally re-read and confirmed by this filing task, listed per-finding above; `crates/numa-shim/tests/policy_oracle_linux.rs:190-202,215,148`, `crates/numa-shim/src/lib.rs:1452-1493`, `crates/numa-shim/tests/mock_dispatch.rs:280-330`, `crates/numa-shim/Cargo.toml:15-24`, all re-read at revision `9137c51` (the reviewed revision, which is also the current `origin/main` head — no drift between review and verification).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no build/test/lint execution; every conclusion is static source reading. This filing task independently re-read and confirmed ALL FIVE P1/P2 findings against the actual source at the same revision before filing; the two P3s are filed as already-tracked perf candidates, not re-derived.

109. **[T, SEVENTEENTH independent audit] `docs/reviews/2026-08-24-223343-numa-shim-publication-audit-oh.md`** (author `@oh`, Opus effort=high; reported 2026-08-24 22:33:43 Europe/Berlin; revision `b275a225ffe31567261ccf70df0384e711f801a9` — the head of the sixteenth-review remediation wave, previous base `9137c51`; verdict **NO-GO — narrowly: no P1, no memory-safety/provenance/ownership/FFI defect found; block is three P2s all in the verification/gate layer, plus two P3s**) is a STATIC read-only audit (no sub-agents; nothing built or executed — no `cargo build`/`check`/`test`/`clippy`/`rustdoc`/miri/benchmark/`cargo publish --dry-run`; the only working-tree write was the report itself). This is the first review of the post-item-108 state; it independently re-verifies ALL FIVE of that wave's P1/P2 fixes as present, correct, and introducing no new defect, plus spot-checks the Linux `mbind` seam, the Windows `VirtualAllocExNuma` seam, the cpumap parser/reverse index, the public API surface, and MSRV — all clean. (Filed 2026-08-24, task #1322 — a numbering-citation correction: the filing commit's own message says "task #1324" for this work, written before that number was actually assigned; TaskCreate subsequently assigned #1324 to the P2-1 remediation task below, so #1324 is P2-1, not the filing task. The filing/commit work is properly attributed to task #1322, which covered "launch the seventeenth review" end-to-end. Self-caught and corrected here, same convention as the item-107 renumbering incident.)
    - **Status:** OPEN — release-blocked on F2 only (owner-scheduled) — ALL 3 of 3 P2 findings executed (P2-1 task #1324, P2-2 task #1325, P2-3 task #1326); the 2 P3s (P3-1+P3-2, task #1327) remain, small and mechanical, not release-blocking per the review's own framing. F2 (0.1.0 still on crates.io despite 4 breaking changes since) re-confirmed unchanged, NOT re-raised as a fresh finding, remains owner-scheduled per item 106's dedicated note. The review explicitly states: "If the owner prefers, all three [P2s] are cheap and mechanical; alternatively any of them can be explicitly risk-accepted with a recorded note, in which case the code-level verdict is GO." **P2-1 EXECUTED by task #1324 (2026-08-24, `60fee21`):** all four `current_node() == None` skip sites (`policy_oracle_linux.rs` x2, `smoke.rs` x2) now check a new `NUMA_SHIM_REQUIRE_ORACLE` env var — when set, PANIC instead of skip (naming the detection chain and stating a None means regression, not missing hardware); unset (every environment except one), byte-for-byte the pre-fix tolerant skip. The env var is set on exactly ONE CI step (`.github/workflows/ci.yml`, the `numa-shim-mock` job's real-Linux `cargo test -p numa-shim --features vmem-integration` row — verified via `grep -n NUMA_SHIM_REQUIRE_ORACLE ci.yml`, exactly one `env:` block). Test-only + one CI workflow env block; no production code touched, no version bump. Verification honesty (per the implementing task's own note, personally confirmed): the panic-when-set path is not exercisable on this Windows dev host (`current_node()` resolves `Some(_)` here, never `None`) — cross-compile-verified only, first real execution on the Linux CI row; the complementary directions (env unset -> unchanged skip; env set -> still green on a host with working detection) WERE run locally. Personally zero-trust verified beyond the implementing task's own claims: full diff read (4 files, +109), independently re-ran `cargo fmt --check`, cross-compile check on `x86_64-unknown-linux-gnu --tests`, `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings`, the full mock+`vmem-integration` suite (46/46, env unset), and validated the modified `ci.yml`'s YAML syntax directly (`python -c "import yaml; yaml.safe_load(...)"`) before merging — all green.

      **A numbering-citation note (self-caught, same class as this card's own header-note above): the task numbers this card's per-finding bullets and its original Next-trigger line reference were written BEFORE `TaskCreate` actually ran and guessed sequential numbers starting at #1325 — the real assignments are P2-1=#1324, P2-2=#1325, P2-3=#1326, P3-1+P3-2=#1327, nineteenth-review=#1328 (all one lower than originally guessed). The Next-trigger line below is corrected; this Status paragraph now cites the real numbers throughout.**
    - **The findings, with this filing task's own verification verdict per finding:**
      - **P2-1 (CONFIRMED by code reading) — the `current_node() == None` skip arm in the Linux policy oracle (and 3 sibling positive tests) is untouched by task #1318's fix, and a regression anywhere in the crate's OWN detection chain (`sched_getcpu` → `read_cpumap_into` → `ReverseIndex`) silently disables it, CI green throughout:** `crates/numa-shim/tests/policy_oracle_linux.rs:206-213,325-331`, `crates/numa-shim/tests/smoke.rs:157-165,240-248` — personally re-read: all four sites share the identical `let Some(node) = current_node() else { eprintln!(...); return; }` pattern; none distinguishes "this host's topology genuinely can't be determined" from "a regression broke detection." Same defect shape as the sixteenth review's own P1, reached through the detection half instead of the policy half. Fix proposed: an env flag (e.g. `NUMA_SHIM_REQUIRE_ORACLE=1`) set on the one CI row that runs the real Linux backend, under which this skip arm panics instead of skipping — mirrors the existing `SEFER_NUMA_TEST=1` pattern already used by the `numa-real-kernel` job.
      - **P2-2 (a static prediction per the review's own text, NOT independently reproduced by either the review or this filing task — no macOS host available; Windows host's `cfg!(windows)` branch happens to take the "supported" path regardless, so it cannot expose this) — `tests/smoke.rs` carries no backend `#![cfg]` gate and its three real-backend-shaped platform branches would fail under `--cfg numa_shim_mock` + `vmem-integration` on macOS/miri, contradicting the README's own advertised "any target including macOS and miri" claim:** `crates/numa-shim/tests/smoke.rs:143-155,226-238,413-419`; `crates/numa-shim/src/lib.rs:808-877` (mock arm, no platform check at all); `README.md:150-156`; `src/lib.rs:239-243`. No CI row runs this exact combination today (`numa-shim-macos`/`numa-shim-macos-miri` both omit `vmem-integration`), so the review's own confidence level is "cheap to confirm by running the one command it names" — this filing task did NOT have a macOS host to do so, and records this as unverified-but-plausible from code reading, not confirmed.
      - **P2-3 (CONFIRMED — personally reproduced by this filing task, NOT just trusted from the review):** ran `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --no-deps` (no features — the default-feature configuration `cargo add numa-shim` produces) and got exactly the three `unresolved link to 'reserve_preferred_on_node'` errors the review predicted, at `src/lib.rs:51`, `:133`, `:575` — all three write `[\`reserve_preferred_on_node\`]` from an ungated doc comment while the item itself is `#[cfg(feature = "vmem-integration")]`-gated. CI's two rustdoc rows both resolve to `vmem-integration` on, so this default-feature configuration has zero doc-lint coverage — the same gate-blindness class CLAUDE.md's own doc-lint rule (task #1142) describes, and a recurrence of the exact class task #1277 already fixed once. Fix: de-link or feature-gate the three sites, add a default-feature `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --no-deps` CI row.
      - **P3-1 (already reasoned sound, not independently re-derived) — the EINTR fix (task #1319) hardcodes `EINTR = 4` where `std::io::ErrorKind::Interrupted` already exists and is exact, and the predicate `should_retry_eintr` sits inside `#[cfg(target_os = "linux")] mod platform` as a private `fn`, unreachable from any test on any host:** `src/lib.rs:1466,1490-1492`. The review's own precedent argument (the crate's own `cpumap` module doc explicitly rejects "Linux-only ⇒ untestable" as an accident of code organization, not a genuine requirement, `src/lib.rs:885-901`) is sound and matches this repo's own verification-first principle. Not release-blocking; a wrong constant would silently reintroduce task #1319's exact bug with zero test/CI signal.
      - **P3-2 (already reasoned sound) — the sysfs cpumap `open` omits `O_CLOEXEC`, so a concurrent `fork`+`exec` during the topology initializer's one-time 64-node scan leaks the fd into the child:** `src/lib.rs:1526` (`libc_open(path, 0)`). Pre-existing, not introduced by this wave; fix is one flag bit (`0o2000000`), no behavior change otherwise.
    - **P2-2 EXECUTED by task #1325 (2026-08-24, `9705640`):** all three platform branches in `tests/smoke.rs` (`reserve_preferred_on_node_returns_valid_span`, `_large_align_round_trip`, `_rejects_zero_size_with_invalid_arguments`) changed from `cfg!(all(any(target_os="linux", windows), not(miri)))` to `cfg!(any(numa_shim_mock, all(any(target_os="linux", windows), not(miri))))` — under the mock, ALL targets now take the supported-path expectation, matching what the mock actually returns (no platform check). Chose this (item 109's smaller-diff mechanism) over item 110/F2's alternative (gate real-platform tests `not(numa_shim_mock)`, move mock expectations entirely into `mock_dispatch.rs`) — reasoned in the commit message: the alternative would remove the only coverage where the mock's reserve path drives a real `aligned_vmem` reservation on a third platform. New `numa-shim-macos` CI step runs the previously-untested `--cfg numa_shim_mock` + `--features vmem-integration` combination with `grep` sentinels naming the three tests. Test-only + one CI step; no production code touched (task #1311/F6's mock doc-honesty decision untouched), no version bump. Verification honesty: the macOS-specific defect/fix is NOT executable on the Windows dev host (Windows' `cfg!(windows)` took the supported branch both before and after the fix) — the new CI row is the first real proof; the miri twin of this cell remains uncovered by any row (recorded, not fixed here). Personally zero-trust verified: full diff read (3 files, +64), independently re-ran `cargo fmt --check`, validated the modified `ci.yml`'s YAML syntax (`python yaml.safe_load`), `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings`, and the full mock+`vmem-integration` suite (46/46) myself before merging — all green.
    - **P2-3 EXECUTED by task #1326 (2026-08-24, `41ad687`):** the three ungated `[`reserve_preferred_on_node`] intra-doc links (crate-level platform matrix, `NodeId::new`, `NodeResolution::TopologyUnavailable`) de-linked to plain backticks (no link, no breakage under default features). New third rustdoc CI row: `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --no-deps` (true default features, no `--features` flag), next to the existing `--all-features` and docs.rs-derived-set rows — mirrors this crate's own default-feature clippy row's rationale (task #778/F9). Explicitly out of scope, per the review's own framing: the `mock` module's own unrelated intra-doc links remain unchecked by any CI row (mock is a build-time cfg, never reaches docs.rs) — noted, not expanded into. Doc-comment-only + one CI row; no version bump. Personally zero-trust verified — and unlike P2-1/P2-2, this one is FULLY local-verifiable with no honesty caveats: independently re-ran `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --no-deps` (0 errors, was 3), `--all-features --no-deps` (stays green), `--no-deps --features vmem-integration` (the exact docs.rs set, stays green), `cargo fmt --check`, `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings`, and validated the modified `ci.yml`'s YAML syntax myself before merging — all green.

    **All five P1/P2 findings from the seventeenth review are now closed.** Only the two small P3s (task #1327) and F2 (owner-scheduled) remain on this review's own card.
    - **P3-1+P3-2 EXECUTED by task #1327 (2026-08-24, `0a8c849`):** `should_retry_eintr` now checks `err.kind() == std::io::ErrorKind::Interrupted` instead of a hand-decoded raw errno — byte-for-byte identical retry set on the real Linux call path (std's own `decode_error_kind` maps `EINTR` to `Interrupted` on every Unix), portable by construction, no new dependency. Relocated (with `EINTR_RETRY_LIMIT`) out of the Linux-only `mod platform` into a new target-independent `#[doc(hidden)]` SemVer-exempt `numa_shim::eintr` module (task #1289 pattern, mirrors `cpumap`), exercised by a new `tests/eintr_retry.rs` (3 tests, including a counterfactual proving the test isn't tautological — asserts the old raw-errno check would have FAILED where the new check passes) — the FIRST test coverage this predicate has ever had on ANY host. README's doc-hidden-module enumeration updated 2→3. The cpumap `open(2)` call now passes `O_CLOEXEC` (`0o2000000`, cited against Linux `asm-generic/fcntl.h`, identical x86_64/aarch64) — a concurrent `fork`+`exec` during the topology initializer's one-time 64-node scan can no longer leak the fd into the child. **Retry behavior unchanged** (same bound, same reset-on-progress semantics); flag-bit-only change for `O_CLOEXEC`; no version bump. Personally zero-trust verified: full diff read (4 files, +148/-40), independently RAN the new test file myself (3/3 passing — the one fully local-verifiable piece of this task), `cargo fmt --check`, cross-compile check on both `x86_64-unknown-linux-gnu`/`aarch64-unknown-linux-gnu`, `cargo clippy -p numa-shim --all-features --all-targets -- -D warnings`, and the full mock+`vmem-integration` suite (49/49, includes the 3 new tests) myself before merging — all green.

    **All five findings from the seventeenth independent review (item 109) are now closed** except F2 (owner-scheduled, not re-raised as a fresh finding by this review). This review's own wave ran as five sequential `/crush` + worktree tasks (#1324-#1327, one of which — #1325 — also closed the eighteenth review's independently-found F2), each personally zero-trust reviewed before merging.
    - **Next trigger:** (1) P2-1 — DONE via task #1324 (env-flag-gated fatal skip, `60fee21`); (2) P2-2 — DONE via task #1325 (backend-aware `smoke.rs` + macOS mock+`vmem-integration` CI row, `9705640`); (3) P2-3 — DONE via task #1326 (de-linked doc sites + default-feature rustdoc CI row, `41ad687`); (4) P3-1+P3-2 — DONE via task #1327 (`ErrorKind::Interrupted` + testable relocation + `O_CLOEXEC`, `0a8c849`); (5) then, per item 110's own next-trigger chain (F1.1+F1.2+F6 task #1329 → F3 task #1331 → F5 task #1332 → F9+F10-docs task #1333 → F11 task #1334), the NINETEENTH independent readiness review covering BOTH waves (task #1328). Per the seventeenth review's own §3 point 6: the owner's F2 release-metadata decision, the final pre-tag Phase-1 gate re-run on the frozen SHA, `cargo publish --dry-run`, and a confirmed-green CI on that exact SHA, all before any tag — none of which is a task filed by this campaign without a fresh, explicit owner trigger.
    - **Evidence:** the report itself (committed by this task); the P2-3 finding personally reproduced via `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --no-deps`, output matching the review's prediction exactly; the P2-1 finding personally re-read at the cited lines, confirming all four skip sites share the identical undifferentiated pattern; P2-2 and the two P3s recorded as code-reading-sound but not independently executed (P2-2 needs a macOS host this session does not have; the two P3s are reasoning about test-reachability and fd flags, not claims requiring a specific host to reproduce).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, nothing built or executed — matching the fifteenth/sixteenth reviews' own methodology. This filing task went further than transcription for the two claims cheaply checkable on this Windows host: P2-3 was independently reproduced with the exact command the review named (not merely trusted), and P2-1 was independently re-read against the cited source. P2-2 was NOT independently reproduced (no macOS/miri host available this session) and is recorded as the review's own claim, explicitly labelled as such, consistent with this repo's evidence-integrity convention of not overstating verification that did not happen.

110. **[T, EIGHTEENTH independent audit] `docs/reviews/2026-08-24-224323-numa-shim-publication-audit-Sol-codex.md`** (author Sol-codex; reported 2026-08-24 22:43:23 Europe/Berlin; snapshot `555deba751be367ad64c5325b484bf3eababfb1b` — reviewed range `9137c51..555deba`, production/test code of the wave ends at `b275a22`; verdict **NO-GO — one P1 (the policy oracle's errno classification is BOTH over-strict and over-lenient, plus the detection-skip hole), four further P2, six P3/quality items**) is a STATIC read-only audit (no sub-agents; nothing built or executed; the only working-tree write was the report itself). Ran CONCURRENTLY with the seventeenth review (item 109, `@oh`, same evening) — the two independently converge on several findings (detection-skip hole, mock+`vmem-integration` matrix gap, default-rustdoc gap, `ErrorKind::Interrupted`, `O_CLOEXEC`), and this review additionally REFINES the sixteenth-review P1 fix (task #1318) with kernel-contract detail the seventeenth review did not reach. It explicitly confirms tasks #1319/#1320/#1321 (EINTR retry, provenance+mock cleanup, 32-bit gate) as correctly closed, and finds no new production safety defect in the reverse index, Linux `mbind` cleanup, Windows reserve/commit ownership, or strict provenance. (Filed 2026-08-24, task #1330 — the filing/commit task.)
    - **Status:** OPEN — F1.3, F2, F8, F4+F7, and F1.1+F1.2+F6 EXECUTED (5 of 6 tasks landed); F3 (#1331), F5 (#1332), F9+F10-docs (#1333), F11 (#1334) remain. F2-class release metadata remains owner-scheduled per item 106's dedicated note. **F1.1+F1.2+F6 EXECUTED by task #1329 (2026-08-25, `c3f03c2`):** a `get_mempolicy(2)` `MPOL_F_MEMS_ALLOWED` preflight now runs BEFORE the positive oracle's reserve call — verified against the man page's calling convention (mode and addr both NULL under this flag, cannot combine with `MPOL_F_ADDR`); if the resolved node is not in the thread's allowed-memory mask, skips loudly BEFORE ever reaching the reserve call, structurally removing the false-panic-on-legitimate-environment class (F1.1) instead of trying to infer it from errno after the fact. The `Err(Os(e))` arm's classification inverted from "panic on 3 named errno, skip on everything else" to an explicit ALLOWLIST (`ERRNO_ENOMEM`, `ERRNO_EPERM` only, each with a documented reason — ENOMEM is the one errno `mbind(2)`'s ERRORS section documents for the crate's exact `flags=0` call form; EPERM isn't documented for that form but is the observed seccomp/container-sandbox denial, the F8.2 case) — everything else, including EINVAL/EFAULT/ENOSYS and any unclassified errno, now PANICS (F1.2). Nodemask sizing enlarged from a bare `u64` to `[u64; 16]` (1024 nodes) with matching `maxnode = 1024`, fixing the >64-node-host false-oracle-failure (F6) — verified against the kernel's own `maxnode < nr_node_ids` pre-copy-out rejection and `CONFIG_NODES_SHIFT`'s Kconfig range on both x86_64 and aarch64. Test-only, no production code touched, no version bump. This REFINES (not reverses) task #1318's classification — #1318's core contribution (unconditional skip → SOME classification) stands; this task corrects which errno land in which bucket and adds the missing preflight. Personally zero-trust verified: full diff read (2 files, +238/-66), independently re-ran `cargo fmt --check`, cross-compile clippy on both `x86_64-unknown-linux-gnu`/`aarch64-unknown-linux-gnu --all-targets -D warnings`, host `cargo clippy --all-features --all-targets -D warnings`, and — to rule out stale-cache false confidence given this session's own earlier build-cache-contamination finding (item 107) — force-touched the changed file and re-ran clippy once to confirm a genuine recompile (`Checking numa-shim` printed, not a cache hit) before merging — all green. Real execution remains cross-compile-only pending Linux CI, the same limitation as every prior Linux-only oracle task in this campaign.
    - **The findings, with this filing task's own verification verdict per finding:**
      - **F1 (P1) — the policy oracle's task-#1318 errno classification is wrong in BOTH directions, and the detection-skip hole remains:** (1) **CONFIRMED against `mbind(2)`'s documented contract** — the kernel returns `EINVAL` not only for marshalling bugs but for a well-formed call whose nodemask contains no node that is online, allowed by the calling thread's cpuset, or memory-bearing; `current_node()` proves only that the CPU maps to the node, NOT that the node is in `cpuset_mems_allowed` or has memory — so on a CPU-only/memoryless node or a split cpuset, #1318's unconditional `EINVAL` panic is a FALSE RED on a legitimate environment. `ENOSYS` can likewise come from a seccomp/sandbox policy, not only a wrong syscall number. (2) The inverse arm is too wide: any OTHER `Some(errno)` (including `EBADF`/`ERANGE`/anything unexpected) silently skips green — an unknown implementation regression still hides. The review's fix: a PREFLIGHT probe via `get_mempolicy(MPOL_F_MEMS_ALLOWED)` to confirm the target node is allowed and memory-bearing before the positive call, then an errno ALLOWLIST (expected-environment errno explicitly enumerated and skipped; everything else — including unknown errno — fails), not "everything except three panics"/"everything else skips". (3) The `current_node() == None` green-skip hole — IDENTICAL to item 109's P2-1, already being fixed by task #1324 (in flight), which this review independently re-derives with the same env-flag strict-mode proposal.
      - **F2 (P2) — smoke.rs target-OS expectations vs mock backend:** converges with item 109's P2-2 (task #1325); this review's proposed mechanism differs (compile-gate the real-platform smoke tests `not(numa_shim_mock)` and keep mock-contract testing in `mock_dispatch.rs`, vs item 109's backend-aware `cfg!` branches) — the implementing task should weigh both.
      - **F3 (P2, CONFIRMED by this filing task against `src/lib.rs:1275-1283`)** — `current_node_impl` calls `sched_getcpu()` FIRST, then `cpu_to_numa_node`, whose first call runs the up-to-64-file topology scan inside `OnceLock::get_or_init` — so the first-ever call's CPU snapshot is taken BEFORE a long initialization window during which the thread can migrate, and the stale CPU is then looked up in the fresh topology. Same in `current_node_resolution_impl` (`:1285-1296`). Fix: touch `topology()` first, then take the CPU snapshot and do a direct lookup.
      - **F4 (P2) — `O_CLOEXEC`:** identical to item 109's P3-2 (task #1327); this review rates it P2 (allocator-adjacent library, real fd-inheritance defect) vs item 109's P3 — either way already tasked.
      - **F5 (P2, CONFIRMED by this filing task against `tests/node_resolution_linux.rs:41-45`)** — `current_node_agrees_with_resolution_mapping` calls `current_node()` and `current_node_resolution()` as two INDEPENDENT snapshots, each doing its own `sched_getcpu()`; on a real multi-node machine a scheduler migration between the two calls makes correct `Some(0)`/`Resolved(1)` a false failure. Single-node CI hides the race; the planned real-multi-NUMA gate would surface it. Fix: single-snapshot oracle via a test-only helper (the doc-hidden `linux::dbg_*` pattern already exists in this file), or pinned affinity.
      - **F6 (P3) — `maxnode=65` limits the oracle to hosts with `nr_node_ids <= 65`:** modern kernels reject `get_mempolicy` with `EINVAL` when `maxnode < nr_node_ids` BEFORE any copy-out, so on a >64-node host the oracle's own probe fails regardless of the target node being in 0..63. Not independently verified (kernel-source claim; plausible and consistent with the man page's maxnode wording). Fix: a larger fixed nodemask array with matching maxnode; check the one low bit needed.
      - **F7 (P3) — `ErrorKind::Interrupted` + testable predicate:** identical to item 109's P3-1 (task #1327).
      - **F8 (P3) — default-feature rustdoc gap:** identical to item 109's P2-3 (task #1326; already personally reproduced there).
      - **F9 (P3) — stale smoke.rs comment says 32-bit Windows policy "undecided":** matches the pre-existing-staleness observation task #1321's own zero-trust review already recorded in item 108's card (deliberately left untouched then as out-of-scope; now flagged by an independent review, so a task exists to fix it — folded into #1333).
      - **F10 (P3) — perf docs overclaim ("no syscalls" for warm `current_node()`, which still does `sched_getcpu` every call) and both mock-backend benches measure recording overhead, not real cold/warm cost; README's "zero C library dependencies" vs actual system-libc FFI wording** — doc/bench-honesty fixes, folded into #1333 (docs) with the bench-measurement half left to the perf backlog per items 59/60's measure-first discipline.
      - **F11 (P3) — `parse_each_set_cpu` is O(words²) on the cold path** (per-word `nth_token` rescans from the start; `index_node` does the pass twice; topology init repeats × up to 64 nodes). Algorithmic-cost class, same family as the F10/#1310 reverse-index fix. Fix: linear right-to-left iteration (`rsplit`) with an incrementing word index, keeping the two-pass fail-closed structure; confirm with a real-parser benchmark, not a mock bench (task #1334).
    - **F3 EXECUTED by task #1331 (2026-08-25, `5f14b3d`):** Linux `current_node_impl`/`current_node_resolution_impl` now call `topology()` BEFORE `sched_getcpu()`, reusing the returned reference for a direct `.lookup()` instead of going through `cpu_to_numa_node`/`cpu_to_numa_node_checked` (left untouched — still used by the doc-hidden test-only `dbg_*` forwarders). On the first-ever call, the CPU snapshot is now taken AFTER the up-to-64-file sysfs topology scan completes, so the scan can no longer widen the migration window between snapshot and lookup; warm calls are unaffected (same single `OnceLock` check either way). Minimal reorder only — the review's optional extra-strictness suggestion (re-snapshot and retry once if the CPU changed) deliberately NOT implemented (the review itself marks it optional). Production code, Linux-only; no version bump. Personally zero-trust verified: full diff read (2 files, +24/-2), independently re-ran `cargo fmt --check`, force-touched `src/lib.rs` and re-ran clippy on both `x86_64-unknown-linux-gnu`/`aarch64-unknown-linux-gnu` (confirmed genuine recompiles, not cache hits), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite (49/49) myself before merging — all green. The migration-window narrowing itself is not deterministically testable (no portable way to force a mid-scan scheduler migration) — verified by code reading plus existing tests staying green.
    - **F5 EXECUTED by task #1332 (2026-08-25, `55d7c93`):** `tests/node_resolution_linux.rs`'s `current_node_agrees_with_resolution_mapping` no longer calls `current_node()`/`current_node_resolution()` as two independent (and thus independently-migratable) snapshots — it now takes ONE raw `sched_getcpu()` sample (self-contained `extern "C"` declaration inside the test file, matching `policy_oracle_linux.rs`'s own established raw-syscall pattern) and feeds that SAME value into the two pre-existing doc-hidden `linux::dbg_current_node_for_cpu`/`dbg_node_resolution_for_cpu` forwarders — neither of which re-samples the CPU, so no scheduler migration can occur between them. `src/lib.rs` completely untouched (no new production surface, so no risk of the dbg-hook-allowlist-omission defect class that hit this campaign twice before, tasks #1287/#1315). Test name kept unchanged; confirmed no CI sentinel greps for it by name. Test-only, no version bump. Personally zero-trust verified: full diff read (2 files, +40/-4), independently re-ran `cargo fmt --check`, force-touched the test file and re-ran cross-compile check+clippy on both `x86_64-unknown-linux-gnu`/`aarch64-unknown-linux-gnu` (confirmed genuine recompiles), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite (49/49 — this file compiles out entirely under the mock cfg) myself before merging — all green.
    - **F9+F10-docs EXECUTED by task #1333 (2026-08-25, `e25ec74`):** F9 — `tests/smoke.rs`'s stale "32-bit Windows policy undecided" comment corrected to state the policy is DECIDED (task #1313/F11) and compile-enforced (task #1321), with the actual point (deliberate struct duplication) preserved. F10-docs — two "subsequent calls are pure in-memory"/"no syscalls at all" overclaims in `current_node`/`current_node_resolution`'s doc comments corrected: the topology lookup genuinely IS cached/in-memory after warm-up (kept, true), but `sched_getcpu()` remains a per-call platform call the old wording omitted. F10-bench — `numa_bench.rs`'s module doc gained an explicit disclaimer that `first_call`/`warm_call` measure mock dispatch/recording overhead only, never real sysfs cold-path or production warm-lookup cost; bench names kept unchanged for historical continuity, no new real-backend bench added (measure-first perf work stays in `docs/perf/OPEN_ITEMS.md` item 59's territory). "Zero C library dependencies" corrected to "zero third-party C/C++ dependencies" across `src/lib.rs`, `README.md`, and `Cargo.toml`'s `description` field, kept in sync per this crate's own convention. Docs/comments/metadata only, zero behavior change, no version bump (Cargo.toml's `version` field untouched, only `description`). Personally zero-trust verified: full diff read (6 files, +50/-15), independently re-ran `cargo fmt --check`, `cargo clippy -p numa-shim --all-features --all-targets -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --all-features --no-deps`, and the full mock+`vmem-integration` suite (49/49) myself before merging — all green.

    - **F11 EXECUTED by task #1334 (2026-08-25, `feafbc7`):** `cpumap::parse_each_set_cpu`'s `word_count`-then-repeated-`nth_token` loop (each `nth_token` call rescanning `data` from byte 0, O(words²) overall) replaced with a single `data.rsplit(|&b| b == b',').enumerate()` pass — `rsplit` yields comma-delimited words rightmost-first, exactly the `w=0..N` (lowest-to-highest CPU index) order the old backward-indexed loop produced, so every byte of `data` is now visited O(1) times. `ReverseIndex::index_node`'s two-pass validate-then-commit fail-closed structure (task #1310) deliberately untouched — both passes individually become O(words) automatically. `nth_token` correctly RETAINED unmodified (not dead code — still exercised as its own parsing-primitive oracle by `tests/cpumap_parser.rs`). No wall-clock speedup measured or claimed — `fix(perf)` commit prefix per this repo's own taxonomy, matching task #1310's own precedent for an unmeasured complexity-only win; the O(w²)→O(w) reduction is provable by construction (single pass), not benchmarked. Byte-for-byte behavior preservation verified by explicit edge-case analysis in the CHANGELOG entry (empty input, trailing/leading/double comma, word-order boundary) against the OLD `nth_token`-based semantics. No version bump. Personally zero-trust verified: full diff read (2 files, +14/-8), independently re-ran the full `tests/cpumap_parser.rs` (17/17, all malformed-input edge cases) and `tests/cpumap_reverse_index.rs` (6/6) test files, `cargo fmt --check`, `cargo clippy -p numa-shim --all-features --all-targets -D warnings`, cross-compile check on `x86_64-unknown-linux-gnu`, and the full mock+`vmem-integration` suite (49/49) myself before merging — all green.

    **All twelve findings from the eighteenth review (item 110) are now closed** except F2-class release metadata, which this review does not itself re-raise and which remains owner-scheduled per item 106's dedicated note. Combined with item 109's own full closure (task #1327), EVERY finding from BOTH the seventeenth and eighteenth independent reviews is now remediated — the wave is ready for the nineteenth independent readiness review (task #1328).
    - **Next trigger:** (1) #1324 — DONE (F1.3); (2) #1325 — DONE (F2); (3) #1326 — DONE (F8); (4) #1327 — DONE (F4+F7); (5) #1329 — DONE (F1.1+F1.2+F6 oracle rebuild, `c3f03c2`); (6) #1331 — DONE (F3, topology-before-snapshot reorder, `5f14b3d`); (7) #1332 — DONE (F5, single-snapshot oracle, `55d7c93`); (8) #1333 — DONE (F9+F10-docs, `e25ec74`); (9) #1334 — DONE (F11, parser linearization, `feafbc7`); (10) the next independent readiness review (#1328, re-scoped to cover BOTH item-109 and item-110 waves, ready to launch). F2-release-metadata: owner-scheduled only.
    - **Evidence:** the report itself (committed by this task); F3 and F5 personally re-verified against the cited source lines by this filing task (listed above); F1.1's `EINVAL`-as-environment claim verified against `mbind(2)`'s documented ERRORS semantics (the review cites the man7 URL; the kernel behavior matches the man page's "no node is online/allowed/memory-bearing" wording); F6 recorded as plausible-not-verified (kernel-source claim); the convergence with item 109's findings cross-checked against that card directly.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, nothing built or executed. This filing task independently re-verified F3 and F5 against source and F1.1 against the documented kernel contract; F6 is filed as the review's claim, labelled as such. The review ran concurrently with (and independently of) the seventeenth review — their convergence on five findings is itself corroborating evidence for those findings.

111. **[T, NINETEENTH independent audit] `docs/reviews/2026-08-25-022026-numa-shim-publication-audit-oh.md`** (author `@oh`, Opus effort=high; reported 2026-08-25 02:20:26 Europe/Berlin; revision `623062d059b391a7becb5ed509df1c8e73741eaa` — the head of both the seventeenth-review wave (item 109, tasks #1324-#1327) and the eighteenth-review wave (item 110, tasks #1329/#1331-#1334), plus the `#1335` process fix; verdict **NO-GO — narrowly, same class as the seventeenth review: no P1, no memory-safety/provenance/ownership/FFI defect; block is three P2s all in the verification/gate layer, plus five P3s**) is a STATIC read-only audit (no sub-agents; nothing built or executed — no `cargo build`/`check`/`test`/`clippy`/`rustdoc`/miri/benchmark/`cargo publish --dry-run`; the only working-tree write was the report itself). This is the first review of the combined post-item-109-and-110 state; it independently re-verifies ALL NINE of those two waves' fixes as present and correct, and re-derives the eighteenth review's own kernel-contract claims from `mm/mempolicy.c` a second time (MAXNODE=1024 vs `copy_nodes_to_user`'s `ALIGN(1023,64)/8=128` bytes; the `MPOL_F_MEMS_ALLOWED` NULL-mode/NULL-addr calling convention; the `nodemask == single_node_mask(node)` equality assert's soundness) — all confirmed independently a second time. (Filed 2026-08-25, task #1328 — the same task that launched the review.)
    - **Status:** OPEN — release-blocked on F2 only (owner-scheduled) — ALL 3 of 3 P2s EXECUTED. Five P3s remain (tasks #1339/#1340). **P2-3 EXECUTED by task #1338 (2026-08-25, `7badee9`):** the `NUMA_SHIM_REQUIRE_ORACLE=1` real-Linux CI row (`.github/workflows/ci.yml`) now `tee`s its output and hard-`grep -F`s two sentinel test names — `reserve_preferred_on_node_installs_mpol_preferred_on_the_usable_span` (the positive oracle) and `plain_unbound_reservation_is_not_reported_as_preferred_for_our_node` (the negative control) — matching the sibling mock rows' existing style. Both names personally byte-verified against `crates/numa-shim/tests/policy_oracle_linux.rs`'s actual `fn` definitions before merging (`grep -n "^fn reserve_preferred_on_node_installs...\|^fn plain_unbound..."`, both matched exactly). The `NUMA_SHIM_REQUIRE_ORACLE: "1"` env value and the test invocation itself are byte-for-byte unchanged — only a tee+grep wrapper added, closing the never-compiled hole task #1324's env-flag fix couldn't reach. CI-only change (`.github/workflows/ci.yml` + a `CHANGELOG.md` entry); no production/test/version change. Personally zero-trust verified: full diff read, independently validated the resulting YAML with `python -c "import yaml; yaml.safe_load(open(..., encoding='utf-8'))"` (the first attempt without explicit UTF-8 encoding hit a `cp1252` codec error from Python's platform-default encoding on this Windows host — a verification-tooling artifact, not a YAML defect; re-run with explicit `encoding='utf-8'` passed clean) — this row cannot be executed on this Windows dev host (Linux CI runner only), so YAML validity + exact sentinel-name matching against source is the verification ceiling, honestly stated.

    **All eight findings from the nineteenth review are now closed** (three P2s: task #1336/#1337/#1338; five P3s: task #1339 for P3-1+P3-5, task #1340 for P3-2+P3-3+P3-4). F2 (owner-scheduled) is not re-raised by this review; the wave is ready for the TWENTY-FIRST independent readiness review, combined with item 112's own wave (tasks #1341/#1342). **P3-2+P3-3+P3-4 EXECUTED by task #1340 (2026-08-25, `d54288a`):** (P3-2, test-comment-only) `tests/smoke.rs`'s release-oracle race-window comments (Linux `/proc/self/maps` + Windows `VirtualQuery` twins) corrected — the old "unlikely because the four probes are megabytes apart" reasoning assumed uniformly-random `mmap` placement; Linux's default top-down layout actually places a new mapping at the TOP of the highest sufficient gap, which is exactly the just-freed region right after `drop(r)`, so a concurrent mapping (e.g. libtest's own per-test thread stacks) would land covering the HIGHEST probe preferentially, not "somewhere in slack"; the real (and still adequate) protection is the sub-millisecond window plus this binary's lack of concurrent mmap activity in it — no failure was ever observed, this is a documentation-honesty fix. (P3-3, doc-comment-only in `src/lib.rs`, no logic change) the `static TOPOLOGY` `OnceLock` initializer's peak stack usage is now documented next to task #777's existing allocation-free (heap) claim: 8192-byte `ReverseIndex` + 4096-byte cpumap scratch buffer + 64-byte path buffer = 12,352 bytes (~12 KiB) named-locals floor, personally verified against the actual `MAX_INDEXED_CPUS`/`CPUMAP_READ_BUF_LEN` constants (8192/4096, confirmed via grep before merging) — plus a possible second ~8 KiB transient copy if the by-move return into `OnceLock` isn't NRVO-elided, ~20 KiB worst case; fine on a default 2 MiB stack, a needed fact for a small custom-stack-size thread since an initializer stack overflow aborts rather than returning `Result`. (P3-4) new `tests/readme_examples.rs` (`#![cfg(feature = "vmem-integration")]`-gated) transcribes and ACTUALLY RUNS the README's `vmem-integration` example — the crate's most-copied artifact, already broken once before (F6/task #1268) with no guard added until now; transcription itself caught two latent warning-level defects in the README's own snippet (an unused `ReserveNumaError` import, and the first `let r = ...` binding shadowed unread — verified via a standalone `rustc -D warnings` probe), both fixed in the README in the same task via explicit `drop(r);` statements and dropping the unused import. The README's `## Public API` block (bodiless `pub fn ...;` forward declarations, not valid Rust) retagged from a ` ```rust ` fence to ` ```text `, matching this repo's own no-doctest illustrative-signature convention. No production code change (the example uses only existing public API); `src/lib.rs` change is doc-comment-only; no version bump. Personally zero-trust verified: full diff read (5 files, +174/-8; independently confirmed the two struct-size constants cited in the new stack-depth doc comment against their actual `const` definitions before trusting the arithmetic), independently re-ran `cargo fmt --check -p numa-shim`, force-touched `tests/readme_examples.rs` and re-ran `cargo check`/`cargo clippy -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --all-targets -D warnings` (confirmed genuine recompile), host `cargo clippy --all-features --all-targets -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --all-features --no-deps`, and the full mock+`vmem-integration` suite INCLUDING the new `readme_vmem_integration_example_compiles_and_runs` test specifically (1/1 passing, confirming the example is genuinely compiled and executed, not just parsed) myself before merging — all green. **P3-1+P3-5 EXECUTED by task #1339 (2026-08-25, `2ce1b18`):** the `O_CLOEXEC` constant in `src/lib.rs`'s Linux `mod platform` (gated only `all(target_os = "linux", not(miri))`, no arch clause) is now cfg-split — the verified `0o2000000` on every real rustc Linux target except `any(target_arch = "sparc", target_arch = "sparc64")` (confirmed via `rustc --print target-list` that alpha/parisc, where the same bit value means `O_DIRECT`, are not real rustc targets, and hexagon's sole Linux target uses the asm-generic value and is unaffected), where it now compiles to `0` — the cpumap fd opens without close-on-exec there, a knowingly-inert hardening honest in code rather than passing an unrelated bit to `open(2)`. Majority-arch behavior byte-identical. Separately, the CHANGELOG's duplicate `### Changed` heading under `## Unreleased` (the exact task #1274/N10 class) was merged into one, and the stale `FellBackToZero`/`bind_range` prose in `### Added`/`### Fixed` reconciled with later same-cycle changes (task #1308's rename, task #1306's removal) via added clarifying notes rather than deletion, preserving historical accuracy for content that predates this Unreleased cycle's later revisions. `src/lib.rs` production-code change (small, cfg-only) + `CHANGELOG.md` restructuring; no version bump. Personally zero-trust verified: full diff read (2 files, +135/-96 net, all `src/lib.rs` hunks confirmed cfg-only with no logic change, CHANGELOG structure confirmed single `### Changed` heading post-fix via `grep -n "^### "`), independently re-ran `cargo fmt --check -p numa-shim`, force-touched `src/lib.rs` and re-ran `cargo check`/`cargo clippy -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --all-targets -D warnings` (confirmed genuine recompile), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite (all passing) myself before merging — all green. Verification honesty: no sparc toolchain available locally, so the new `0` arm is verified structurally (cfg keys matched against real target names) and by the unchanged x86_64 cross-check, not by an actual sparc build. **P2-2 EXECUTED by task #1337 (2026-08-25, `83ca20a`):** `tests/smoke.rs`'s four affected tests (`reserve_preferred_on_node_returns_valid_span`, `_large_align_round_trip`, `_rejects_zero_size_with_invalid_arguments`, `_rejects_node_beyond_nodemask_range`) gained a three-way platform classification matching production dispatch exactly — mock (any OS/arch, no arch check) keeps the supported path; real backend on Linux x86_64/aarch64 or Windows (no arch restriction there) keeps today's expectations (`Ok`/`InvalidArguments`/`InvalidNode`); real backend on Linux NOT x86_64/aarch64 (the new arm) asserts `Err(ReserveNumaError::UnsupportedArchitecture)`, matching production's own other-arch return and the crate-doc platform matrix's documented row; macOS/miri/other keep the unchanged `UnsupportedPlatform` catch-all. The arch clause is byte-identical to the one `tests/policy_oracle_linux.rs`'s own `#![cfg(...)]` gate already carries. Test-only change (`tests/smoke.rs` + a `CHANGELOG.md` entry), no production code, no version bump. Personally zero-trust verified: full diff read (2 files, +99/-7, all four sites checked against the intended three-way logic including the node-beyond-nodemask test's already-Linux-gated function scope collapsing to a two-way mock-vs-arch check within the body), independently re-ran `cargo fmt --check -p numa-shim`, force-touched the changed file and re-ran `cargo check`/`cargo clippy -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --all-targets -D warnings` (confirmed genuine recompile), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite including `tests/smoke.rs` specifically (5/5 passing, the mock path IS directly exercised by this file unlike `policy_oracle_linux.rs`) myself before merging — all green. Verification honesty: the new arch-mismatch branch's reachability rests on cross-compile CHECK plus reading against production's own cfg arms — it cannot be EXECUTED on this Windows/x86_64 host (no non-x86_64/aarch64 Rust target installed, and no way to run such a binary regardless), same limitation class as every prior Linux-only oracle task in this campaign. Five P3s non-blocking. F2-class release metadata (0.2.0 bump) is NOT re-raised by this review as a finding, remains owner-scheduled per item 106's dedicated note. **P2-1 EXECUTED by task #1336 (2026-08-25, `3c8f516`):** the `MPOL_F_MEMS_ALLOWED` preflight's bare `.expect` in `tests/policy_oracle_linux.rs` replaced with an errno match mirroring the reserve arm's own allowlist philosophy — `ERRNO_EPERM`/`ERRNO_ENOSYS` (a seccomp policy can deny with either; Docker's default profile gates `get_mempolicy`/`mbind`/`set_mempolicy` as one `CAP_SYS_NICE` group) now skip loudly via `eprintln!` before the reserve call, describing the container/sandbox scenario; every other errno, and an `Os` error with no raw errno, still PANICs fail-closed exactly as before. Under `NUMA_SHIM_REQUIRE_ORACLE=1` (the repo's own real-Linux CI row, not a container) the skip escalates to a PANIC instead, so the CI row's strictness is unweakened. The reserve arm's own classification (ENOMEM/EPERM skip; EINVAL/EFAULT/ENOSYS/unknown panic) is byte-for-byte unchanged; the negative control's pre-existing task-#1318-reasoned `.expect` is untouched (review scopes P2-1 to the preflight only). Test-only change (`tests/policy_oracle_linux.rs` + a `CHANGELOG.md` entry), no production code, no version bump. Personally zero-trust verified: full diff read (2 files, +81/-9), independently re-ran `cargo fmt --check -p numa-shim`, force-touched the changed file and re-ran `cargo check`/`cargo clippy -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --all-targets -D warnings` (confirmed genuine recompile — "Checking numa-shim" printed, not a cache hit — per this session's own item-107 cache-contamination lesson), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite (all passing, `policy_oracle_linux.rs` correctly compiles to 0 tests under the mock cfg) myself before merging — all green. Verification honesty: the actual EPERM/ENOSYS code paths are not executable on this Windows dev host (Linux-only file) — cross-compile check + clippy is the verification ceiling, same limitation as every prior Linux-only oracle task in this campaign; first real execution pending the Linux CI row.
    - **The findings, with this filing task's own verification verdict per finding — ALL THREE P2s personally re-verified against source before filing (not transcribed):**
      - **P2-1 (CONFIRMED) — the new `MPOL_F_MEMS_ALLOWED` preflight (task #1329) hard-panics in exactly the seccomp/container environment the file's own `ERRNO_EPERM` allowlist entry exists to tolerate:** `crates/numa-shim/tests/policy_oracle_linux.rs:388-395` (the bare `.expect` on `get_mems_allowed()`) against `:193-199` (`ERRNO_EPERM`'s doc, naming Docker's default seccomp profile denying `mbind` with EPERM as "the F8.2 container case this skip arm has always existed for") and `:432-441` (the `Some(ERRNO_EPERM) => skip` arm, unreachable in that same environment because Docker's seccomp profile denies `get_mempolicy`/`mbind`/`set_mempolicy` as one group). Personally re-read all three cited spans: confirmed the preflight has no errno classification at all — any error from `get_mems_allowed()`, including EPERM/ENOSYS, panics via `.expect` before the reserve call's own (correct) allowlist is ever reached. Net effect: `cargo test -p numa-shim --features vmem-integration` goes hard red on containerized Linux. Fix: classify the preflight's errno with the same allowlist the reserve arm already has (EPERM/ENOSYS ⇒ skip, else panic).
      - **P2-2 (CONFIRMED) — `tests/smoke.rs`'s platform predicate is arch-blind: four tests hard-fail on every Linux target that is not x86_64/aarch64:** `crates/numa-shim/tests/smoke.rs:170-185,272-287,483-490,499-518` vs `src/lib.rs:1432-1436` (production dispatch, arch-gated) and `src/lib.rs:56`/`README.md:241-243` (the platform matrix documenting "Linux other arch (non-miri)" as a supported `UnsupportedArchitecture`-returning row). Personally re-read: the predicate is `cfg!(any(numa_shim_mock, all(any(target_os = "linux", windows), not(miri))))` — target-OS and backend only, never `target_arch`, while `tests/policy_oracle_linux.rs:45-51` (the sibling file) already carries the missing `any(target_arch = "x86_64", target_arch = "aarch64")` clause, and `src/lib.rs:1401,1432` confirm the production dispatch itself is arch-gated. On a real target such as `armv7-unknown-linux-gnueabihf`, four tests would panic/assert-fail expecting `Ok`/`InvalidArguments`/`InvalidNode` and instead getting `Err(UnsupportedArchitecture)`. This is the third dimension of a predicate defect already corrected twice (target-OS, then backend via task #1325's P2-2) — recurs because the predicate still classifies fewer axes than production dispatch does. Fix: three-way classification matching production (mock ⇒ success; real Linux/Windows x86_64/aarch64 ⇒ success; real Linux other-arch ⇒ `UnsupportedArchitecture`; else ⇒ `UnsupportedPlatform`).
      - **P2-3 (CONFIRMED) — the one CI row that executes the flagship policy oracle (`tests/policy_oracle_linux.rs`) carries no green-and-dead sentinel:** `.github/workflows/ci.yml:2706-2708` (the `NUMA_SHIM_REQUIRE_ORACLE=1` row). Personally re-read: `policy_oracle_linux.rs` is `#![cfg(...)]`-gated on five simultaneous conditions; if any one silently stops holding (feature rename, stray cfg leak, arch-clause edit, `#![cfg]` typo), the file compiles to zero tests and the row still exits 0 — CI reports green with the crate's headline `mbind`-installs-`MPOL_PREFERRED` capability entirely unverified. Every *less* important numa-shim CI row already `grep -F`s specific `... ok` sentinels (`:2664-2666`, `:2674-2675`, macOS row `:2855-2859`); this one, the most important, has none. `NUMA_SHIM_REQUIRE_ORACLE=1` (task #1324) closes the silent-*skip* hole but is powerless against the never-*compiled* hole. Fix: `tee` the test output and `grep -F` the two positive-oracle test names.
      - **P3-1 (already reasoned sound, not independently re-derived beyond the review's own UAPI citation) — `O_CLOEXEC = 0o2000000` (task #1327) is applied unconditionally on every Linux arch, but the value is sparc/sparc64 (`0x400000`, this bit is undefined there) and alpha/parisc (`010000000`, this bit means `O_DIRECT` there) specific, not universal:** `src/lib.rs:1569,1608,1336`. The doc comment honestly scopes the verification to x86_64/aarch64; the code's `cfg` gate does not match that scope.
      - **P3-2 (already reasoned sound) — the `/proc/self/maps` release-oracle comment's "unlikely to race" reasoning is inverted for Linux's actual top-down `mmap` placement policy** (a freed region is exactly where the NEXT `mmap` lands preferentially, not "somewhere in megabytes of slack"): `crates/numa-shim/tests/smoke.rs:419-423,427-454`. Not observed to fail; the finding is that the documented mitigation is not the actual mitigation.
      - **P3-3 (already reasoned sound) — the topology `OnceLock` initializer's peak stack frame (~12-20 KiB, an `8192`-byte `ReverseIndex` local plus a possible NRVO-missed move) is undocumented on the one path the crate advertises as global-allocator-reentrant-safe** (task #777's allocation-free rewrite analyzed heap use, not stack use): `src/lib.rs:1487-1523`.
      - **P3-4 (already reasoned sound) — the README's code examples are compiled by nothing** (no `include_str!`, no `tests/readme_*.rs`, no doc-drift guard script unlike sibling `aligned-vmem`), and the crate has already shipped one broken example once (F6/task #1268): `README.md:55-63,97-147,170-226`. The `## Public API` block is also ` ```rust `-fenced with bodiless signatures — not valid Rust, and inconsistent with this crate's own doc-comment convention of ` ```text ` for the same shape.
      - **P3-5 (already reasoned sound) — the still-open `## Unreleased` CHANGELOG section has a duplicate `### Changed` heading (the same class task #1274/N10 already fixed once) and describes API that no longer exists in the same unreleased cycle** (`FellBackToZero`, renamed to `TopologyUnavailable`; `bind_range`'s `node >= 64` no-op behavior, `bind_range` itself removed by task #1306): `crates/numa-shim/CHANGELOG.md:57,169,128-137,139-144`. Also notes the Phase-1 gate record (`:22-24`) is stale on its own test count and the final pre-tag re-run remains self-declared owed.
    - **Next trigger:** (1) P2-1 — task #1336 (preflight errno allowlist); (2) P2-2 — task #1337 (arch-aware smoke.rs predicate); (3) P2-3 — task #1338 (CI sentinel greps); (4) P3-1+P3-5 — task #1339 (O_CLOEXEC arch scope + CHANGELOG cleanup); (5) P3-2+P3-3+P3-4 — task #1340 (release-oracle comment + stack-depth doc + README example guard) — all five filed 2026-08-25, execute via `/crush` + worktree with personal zero-trust review each (closing the full finding set, not only the blocking P2 subset, per this campaign's established practice), then launch the TWENTIETH independent readiness review once this wave lands and CI is confirmed green.
    - **Evidence:** the report itself (committed by this task); all three P2s personally re-read against the cited source lines by this filing task before writing this card (`crates/numa-shim/tests/policy_oracle_linux.rs:180-205,375-445`; `crates/numa-shim/tests/smoke.rs:160-190`, cross-referenced against `src/lib.rs:1330-1440` for the production dispatch's own arch gate; `.github/workflows/ci.yml:2695-2715` for the ungated CI row) — all three confirmed exactly as the review describes, not merely transcribed.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, nothing built or executed. This filing task independently re-read and confirmed all three P2 findings against the actual source at the same revision before filing; the five P3s are filed as the review's own reasoning, labelled as such (not independently re-derived beyond the O_CLOEXEC UAPI-value cross-check, which matches the review's own citation).

112. **[T, TWENTIETH independent audit] `docs/reviews/2026-08-25-021741-numa-shim-publication-audit-run-17-Sol-codex.md`** (author Sol-codex, self-titled "прогон 17" — its own run counter, NOT this campaign's review ordinal; chronologically the TWENTIETH independent review; reported 2026-08-25 02:17:41 Europe/Berlin; revision `623062d059b391a7becb5ed509df1c8e73741eaa` — the SAME head the nineteenth review (item 111) audited, the two ran CONCURRENTLY and independently; verdict **NO-GO until one P2 documentation defect is fixed — no new production Critical/P0/P1, no UB, no FFI-ABI violation, no leak/ownership defect; three P3s**) is a STATIC read-only audit (no sub-agents; nothing built or executed; the only working-tree write was the report itself, in Russian). It independently confirms all nine fixes of the seventeenth+eighteenth-review waves (#1324/#1325/#1326/#1327/#1329/#1331/#1332/#1333/#1334) as statically correct, re-deriving the `#1329` kernel contracts (`[u64;16]`/`maxnode=1024` vs `copy_nodes_to_user`, `MPOL_F_MEMS_ALLOWED` argument validity, `maxnode=65` vs `get_nodes`'s internal decrement) a THIRD independent time. (Filed 2026-08-25, task #1328's session — the review report appeared in the working tree independently; this filing verifies and files it.)
    - **Status:** OPEN — release-blocked on none: all four findings EXECUTED (the single P2 by task #1341; F2+F3+F4 by task #1342). **Follow-up note (2026-08-25): the twenty-first review (item 114) independently re-found this card's own F1 remediation (task #1341) as incomplete — the fix removed the TYPE errors from the two rustdoc examples but left a BEHAVIORAL panic (on `UnsupportedArchitecture`, a real Linux non-x86_64/aarch64 outcome) in place; see item 114's P2-2. Closed by task #1346, which also fixed a stale non-compiling CHANGELOG snippet (item 114's P3-3) and added a missing CI sentinel for `tests/readme_examples.rs` (item 114's P3-4) — see item 114's card for the full technical account; this card's own F1 disposition below is UNCHANGED (it correctly closed the type-mismatch defect it was filed for) but is superseded in scope by task #1346's later, more complete fix to the same examples.** **F2+F3+F4 EXECUTED by task #1342 (2026-08-25, `651fafe`):** `cpumap::nth_token` (zero production callers since task #1334's `rsplit` linearization) DELETED along with its two dedicated oracle tests (`tests/cpumap_parser.rs` drops 17→15 tests) — both real interpreters (`parse_contains_cpu`, `parse_hex_u32`, etc.) keep their own thorough oracles; the `#[doc(hidden)]` SemVer-exempt module made deletion free, and task #1334's own CHANGELOG entry (which recorded `nth_token`'s retention rationale) was amended in place to record the later disposition change within the same unreleased cycle. `mock::CURRENT_NODE_SLOT` narrowed from `RefCell<u32>` to `Cell<u32>` with `.get()`/`.set()` replacing `.borrow()`/a panicking `.borrow_mut()` — structurally removes the reentrant-borrow hazard, closing pre-existing correctness-backlog item 45 (`docs/correctness-open-items/TRACKED_misc.md`, filed 2026-08-09/task #778, moved to `RESOLVED.md` with a full closure narrative, pointer left in place per this repo's established item-45-sibling convention); sibling `CALLS`/`POLICY_FAILURE_SLOT` deliberately kept `RefCell` (non-`Copy` payloads genuinely need it). `Cargo.toml`'s cfg-propagation comment reworded from the incorrect "cfg flags do not unify across the build's dependency graph" to the accurate distinction (absence of transitive Cargo-feature unification, not per-crate cfg locality — a global `RUSTFLAGS` still affects the whole target graph). Production-code changes (`mock::CURRENT_NODE_SLOT`, `cpumap::nth_token` removal) + test deletions + one Cargo.toml comment + two correctness-open-items files; no version bump. Personally zero-trust verified: full diff read (6 files, +21/-73 net; confirmed `RefCell` import retained in `src/lib.rs` since `CALLS`/`POLICY_FAILURE_SLOT` still need it, confirmed the item-45 closure followed the exact sibling-item-49 convention already established in the same file), independently re-ran `cargo fmt --check -p numa-shim`, force-touched `src/lib.rs` and re-ran `cargo check`/`cargo clippy -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --all-targets -D warnings` (confirmed genuine recompile), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite (all passing, confirmed `tests/cpumap_parser.rs`'s count dropped by exactly 2) myself before merging — all green.

    **All four findings from the twentieth review are now closed.** Combined with item 111's own full closure (task #1340), EVERY finding from BOTH the nineteenth and twentieth independent reviews is now remediated — the wave is ready for a combined TWENTY-FIRST independent readiness review. **F1 EXECUTED by task #1341 (2026-08-25, `d9fc5af`):** all three affected `src/lib.rs` rustdoc sites fixed. The crate-level `//! ## Usage` snippet's unused `NO_NODE` import dropped and its misleading `None`-arm wording ("single-node host") corrected to "detection failed or unsupported platform" (a resolved single-node host returns `Some(0)`, not `None`, since task #1308). `NodeId::new`'s "Ergonomic path from detection" match unified both arms to `Reservation` via `.expect(...)` on each — `reserve_preferred_on_node(...).expect("NUMA-preferred reservation failed")` and `aligned_vmem::reserve_aligned(...).expect("OOM")` — matching the exact resolution the README's `vmem-integration` example (oracled by task #1340) already established, rather than inventing a new house style. `reserve_preferred_on_node`'s "Best-effort fallback" snippet fixed to `.ok().or_else(|| aligned_vmem::reserve_aligned(size, align))`, exactly the minimal correct form the review itself suggested, with a new doc-comment sentence explaining why `.ok()` discarding the specific error IS the best-effort contract. `tests/readme_examples.rs` (created by task #1340) extended with three new tests — `crate_usage_snippet_compiles_and_runs`, `node_id_new_ergonomic_path_snippet_compiles_and_runs`, `best_effort_fallback_snippet_compiles_and_runs` — each carrying its corrected snippet as real compiled-and-run code, not just a compile check. All three doc-comment sites are documentation-only changes (no signature/behavior change to `NodeId::new` or `reserve_preferred_on_node`); the test file extension is test-only; no version bump. Personally zero-trust verified: full diff read (3 files, +132/-5; confirmed both `Reservation`-typed match arms in the `NodeId::new` fix genuinely unify to the same type by reading the corrected code against `reserve_preferred_on_node`'s and `reserve_aligned`'s actual signatures), independently re-ran `cargo fmt --check -p numa-shim`, force-touched `src/lib.rs` and re-ran `cargo check`/`cargo clippy -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --all-targets -D warnings` (confirmed genuine recompile), host `cargo clippy --all-features --all-targets -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --all-features --no-deps`, and specifically the extended `tests/readme_examples.rs` under mock+vmem-integration (4/4 tests passing, including all three new ones) myself before merging — all green. Convergence note: this review and the nineteenth review (item 111) audited the SAME revision concurrently — this one found a P2 (F1, the non-compiling rustdoc examples) that item 111's review missed (its P3-4 covered README examples and the ` ```text ` signature block, but NOT these two rustdoc ` ```text ` examples' type errors); conversely item 111 found three P2s (preflight EPERM panic, arch-blind smoke predicate, missing CI sentinel) that this review did not flag (it assessed #1329's preflight and #1325's predicate as correct — both assessments are accurate for the dimensions it checked, the item-111 findings are on OTHER dimensions: container errno handling and the arch axis). The two reviews' disjoint finding sets on identical source are themselves evidence that neither is redundant.
    - **The findings, with this filing task's own verification verdict per finding — ALL FOUR personally re-verified against source before filing (not transcribed):**
      - **F1 (P2, CONFIRMED — the blocking finding) — two public rustdoc examples do not compile, hidden from CI by their ` ```text ` fences:** `crates/numa-shim/src/lib.rs:145-156` (`NodeId::new`'s "Ergonomic path from detection") — the `match` mixes arm types: `Some(n) => reserve_preferred_on_node(...)` yields `Result<Reservation, ReserveNumaError>` while `None => aligned_vmem::reserve_aligned(...)` yields `Option<Reservation>` (personally confirmed: `crates/aligned-vmem/src/api/reserve.rs:63` declares `pub fn reserve_aligned(size: usize, align: usize) -> Option<Reservation>`); and `src/lib.rs:774-777` (`reserve_preferred_on_node`'s "Best-effort fallback") — `Result::or_else`'s closure must return `Result` but returns `reserve_aligned(...)`'s `Option`. Both fences are ` ```text ` (per this repo's no-doctest rule), so rustdoc never type-checks them and the new default-feature rustdoc CI row (task #1326) stays green — it checks links, not `text`-fence contents. The README's own equivalent example uses a correct explicit `match`, so the two sources of truth diverge exactly on the user-facing fallback instruction. Bonus sub-finding (also confirmed): the crate-level usage snippet `src/lib.rs:20-26` imports `NO_NODE` without using it, and its `None => "NUMA unavailable or single-node host"` wording is misleading (a correctly-resolved single-node Linux/Windows host returns `Some(0)`, not `None`). Fix per the review: unify each example's arm types (either `.ok().or_else(...)` for the Option-shaped form, or keep the explicit `match` like the README), correct the usage snippet, and add a compile-checked oracle so a type error in a canonical snippet can't hide behind ` ```text ` again — given this repo's no-doctest rule, a compile-only integration test carrying the canonical snippets (the same mechanism task #1340's README guard will use) fits the existing process better than converting to ` ```no_run ` doctests.
      - **F2 (P3, CONFIRMED) — `nth_token` is shipping-public dead production code after #1334:** `src/lib.rs:1068` — `parse_each_set_cpu` no longer calls it; its only consumers are `tests/cpumap_parser.rs`'s two oracle tests (personally confirmed via grep: zero `src/` callers). Not a correctness bug (module is `#[doc(hidden)]` semver-exempt), but pre-publication is the cheap time to either move the primitive tests inside the crate boundary or delete `nth_token` and test only the real interpreters.
      - **F3 (P3, CONFIRMED, pre-existing — NOT a new item) — `CURRENT_NODE_SLOT` uses `RefCell<u32>` with panicking borrow where `Cell<u32>` expresses the whole-value read/write contract exactly:** `src/lib.rs:396,423-429`. The review itself notes this is already recorded in the correctness backlog (item 45) — no duplicate card or task filed; folded into this wave's P3 bundle for closure since the change is trivial and the review re-raised it.
      - **F4 (P3, CONFIRMED) — a `Cargo.toml` maintainer comment mis-states cfg propagation:** `Cargo.toml:65-68` says cfg flags "do not unify across the build's dependency graph", but a global `RUSTFLAGS="--cfg numa_shim_mock"` DOES affect the whole target graph (personally re-read the comment block) — the real distinction vs a Cargo feature is the absence of transitive feature-unification (a dependency can't turn it on), not per-dependency locality. Fix: reword per the review's suggested phrasing. Maintainer-facing comment only, no runtime effect.
    - **Next trigger:** (1) F1 — task #1341 (fix both examples + usage snippet + compile oracle; coordinate with task #1340's README-example guard, same mechanism); (2) F2+F3+F4 — task #1342 (P3 bundle: nth_token disposition, `Cell<u32>` swap closing backlog item 45's numa-shim half, Cargo.toml comment reword); then the next independent readiness review covers this wave together with item 111's (#1336-#1340) — one combined TWENTY-FIRST review after both waves land and CI is confirmed green, not two separate reviews.
    - **Evidence:** the report itself (committed by this filing); all four findings personally re-verified: F1 against `src/lib.rs:145-156,774-777,20-26` AND `crates/aligned-vmem/src/api/reserve.rs:63` (the return-type ground truth); F2 via grep over `src/` and `tests/cpumap_parser.rs`; F3 against `src/lib.rs:396`; F4 against `Cargo.toml:60-70` — all at revision `623062d`, the reviewed revision.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, nothing built or executed, concurrently with and independently of the nineteenth review. This filing task independently re-verified ALL FOUR findings against source (including the cross-crate return-type check F1 hinges on) before filing — none is transcribed on trust.

113. **[T, TWENTY-SECOND independent audit] `docs/reviews/2026-08-25-091156-numa-shim-publication-audit-run-18-Sol-codex.md`** (author Sol-codex, self-titled "прогон 18" — its own run counter, NOT this campaign's review ordinal; chronologically the TWENTY-SECOND independent review; reported 2026-08-25 09:11:56 Europe/Berlin; revision `5ff29cbeaecfa86fbbfc736bfad34016fb0d9c10` — the head of both the nineteenth-review wave (item 111, tasks #1336-#1340) and the twentieth-review wave (item 112, tasks #1341-#1342); verdict **NO-GO — two P2, no P0/P1, no new UB/ownership/cleanup defect; one P3 test-smell, plus non-blocking perf observations**) is a STATIC read-only audit (no sub-agents; no tests/build/clippy/miri/benchmarks/docs-build/publish commands run; only working-tree write was the report itself, in Russian). Appeared in the working tree independently (same unprompted-arrival pattern as items 110/112) while the twenty-first review (task #1344, `@oh`) was already running concurrently on the same revision. It explicitly confirms the seventeenth review's run's blocking F1 (rustdoc example type mismatches) as closed, and confirms the #1336-#1342 wave's production-path changes (immediate errno capture, reservation release on mbind failure, full-span policy, Windows strict provenance, fail-closed topology, arch-aware test expectations) introduce no regression. (Filed 2026-08-25, task #1345 — the filing/commit task.)
    - **Status:** OPEN — release-blocked on none: both P2/F1 EXECUTED. One P3 (test/CI smell, drift-guard limitation) plus perf observations, non-blocking. **F2 EXECUTED by task #1346 (2026-08-25, `96d6884`; converges with item 114's independently-filed P2-2, same task) — see item 114's P2-2 disposition for the full technical account.** **F1 EXECUTED by task #1345 (2026-08-25, `fcad8b3`; converges with item 114's independently-filed P3-1, same task):** sparc/sparc64's `O_CLOEXEC` cfg arm changed from `0` (disabled) to `0x400000` (sparc's real UAPI value, cross-checked independently against general Linux sparc fcntl bit-layout knowledge, not merely re-trusting the prior citation). Both doc comments and the `open(2)` call-site comment reworded to state the hardening is now applied, not disabled, while keeping task #1339's original disable-when-unverified reasoning as historical record. Majority-arch behavior (x86_64/aarch64 and every other non-sparc real target) untouched. Production code change (`src/lib.rs`, Linux-only, sparc/sparc64-only cfg arm) + CHANGELOG entry; no version bump. Personally zero-trust verified: full diff read (2 files, +27/-15, all three doc-comment sites confirmed mutually consistent), independently re-ran `cargo fmt --check -p numa-shim`, and — unlike every prior sparc-touching task in this campaign — a REAL `sparc64-unknown-linux-gnu` cross-compile is possible on this host now (`rustup target list --installed` confirms the target is present): ran `cargo check -p numa-shim --target sparc64-unknown-linux-gnu --lib --features vmem-integration` myself, confirmed it compiles (one pre-existing unrelated dead-code warning on `MPOL_PREFERRED`, not introduced by this change, not under `-D warnings` for this cross-compile step), force-touched and re-ran `cargo check`/`cargo clippy` on `x86_64-unknown-linux-gnu` (confirmed genuine recompile, majority-arch behavior unaffected), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite (all passing) myself before merging — all green. This is the first sparc-touching task in this campaign with a genuine cross-compile check rather than structural-reading-only verification.
    - **The findings, with this filing task's own verification verdict per finding — BOTH P2s personally re-verified against source before filing (not transcribed):**
      - **F1 (P2, CONFIRMED) — `O_CLOEXEC` is DISABLED (not corrected) on sparc/sparc64, when the correct UAPI value is already known and documented right next to the disabling code:** `crates/numa-shim/src/lib.rs:1565-1598`. Personally re-read: task #1339 (nineteenth review P3-1) split the constant so sparc/sparc64 compile to `const O_CLOEXEC: core::ffi::c_int = 0;` (no close-on-exec at all) rather than sparc's actual UAPI value — and the doc comment directly above states that value explicitly: "sparc's UAPI defines `O_CLOEXEC` as `0x400000`". Task #1339's own reasoning (recorded in this same card, item 111, P3-1) was to avoid "passing an unrelated bit to `open(2)`" — a sound principle when the correct value is UNKNOWN, but the correct value IS known and already written in the comment, so disabling the hardening entirely leaves a real (if narrow) fork+exec fd-leak race on a real rustc target (`sparc64-unknown-linux-gnu`) that a one-line fix removes. Fix: `#[cfg(any(target_arch = "sparc", target_arch = "sparc64"))] const O_CLOEXEC: core::ffi::c_int = 0x400000;` instead of `0`.
      - **F2 (P2, CONFIRMED) — the crate's own public examples (`README.md` and the `NodeId::new` rustdoc snippet) can PANIC on a real Linux architecture where detection succeeds but the reservation backend does not:** `crates/numa-shim/README.md:97-123`, `crates/numa-shim/src/lib.rs:140-157` (the "Ergonomic path from detection" section fixed by task #1341), and their compiled oracle copies in `crates/numa-shim/tests/readme_examples.rs:64-76,190-201`. Personally re-read all four sites: on a real Linux target outside x86_64/aarch64 (e.g. `riscv64gc-unknown-linux-gnu`, `s390x-unknown-linux-gnu`, `powerpc64le-unknown-linux-gnu` — all real, actively-used rustc targets), `current_node()`'s detection chain (`sched_getcpu` + sysfs cpumap) is architecture-independent and can genuinely resolve `Some(node)`, while `reserve_preferred_on_node` returns `Err(ReserveNumaError::UnsupportedArchitecture)` because the `mbind`-based reservation path is x86_64/aarch64-only. Both the README's first example and `NodeId::new`'s "Ergonomic path" rustdoc snippet call `.expect("NUMA-preferred reservation failed")` on that `Result` — a downstream user on one of those architectures who copies the example verbatim gets a panic, not the demonstrated behavior. `tests/readme_examples.rs`'s own module doc (added by task #1341) ALREADY documents this exact panic path as "a known property rather than a surprise" — but a documented panic risk in a copy-pasted public example is still a real risk, not a resolved one. The README's SECOND example (the "more detailed error handling" form) and `src/lib.rs`'s "## Best-effort fallback" section (task #1341's Site 3 fix) both already use the safe `.ok().or_else(|| aligned_vmem::reserve_aligned(...))` composition — the fix is to bring the "simple"/ergonomic forms in line with that same established, already-verified-correct idiom (or explicitly branch on `UnsupportedArchitecture` specifically), then update the compiled oracle to match.
      - **F3 (P3, CONFIRMED, non-blocking) — `tests/readme_examples.rs` is a manual duplicate of the README/rustdoc text, not a true drift guard:** the file's own comment (personally re-read) already states plainly that editing the README without updating the copy silently stops the check while CI stays green. Real limitation, not a regression from any task in this wave; a stronger fix (extracting the fenced block at build/test time, or making the `.rs` file the single source the README embeds) is future work, not a blocker.
      - **Non-blocking perf observations (not filed as tasks, matching this review's own framing as "measure first, do not optimize speculatively"):** `ReverseIndex::index_node`'s two-pass validate-then-commit scheme doubles cold-path work per node (already the deliberate task #1310 design, unchanged); `current_node()`'s per-call `sched_getcpu()` is necessary for migration correctness, not a bug; the `OnceLock` initializer's ~12-20 KiB stack budget is now honestly documented (task #1340); no new O(n²) cold-path code found after the `nth_token` deletion (task #1342).
    - **Next trigger:** (1) F1 — task #1345 (sparc/sparc64 `O_CLOEXEC` corrected to `0x400000`); (2) F2 — task #1346 (README's first example + `NodeId::new` rustdoc snippet made panic-free on `UnsupportedArchitecture`, matching the already-established `.ok().or_else(...)` idiom, `tests/readme_examples.rs` updated to match); F3 and the perf observations are non-blocking, left for a future task if the owner chooses. Then a combined review after this wave lands, alongside whatever the concurrently-running twenty-first review (task #1344) finds.
    - **Evidence:** the report itself (committed by this task); F1 personally re-verified against `src/lib.rs:1565-1598` (confirming the doc comment already states sparc's correct value while the code compiles to `0`); F2 personally re-verified against `README.md:97-123`, `src/lib.rs:140-157`, and `tests/readme_examples.rs:64-76,190-201` (confirming the `.expect` panic path and the module doc's own "known property" acknowledgment) — all at revision `5ff29cb`, the reviewed revision, which is also this filing task's base.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, no build/test/lint execution — matching the prior Sol-codex reviews' own methodology. This filing task independently re-verified BOTH P2 findings against the actual source at the same revision before filing. This review ran CONCURRENTLY with (and independently of) the twenty-first review (task #1344, `@oh`) — their eventual convergence or divergence is itself further evidence, per this campaign's established pattern with items 111/112.

114. **[T, TWENTY-FIRST independent audit, task #1344] `docs/reviews/2026-08-25-093114-numa-shim-publication-audit-oh.md`** (author `@oh`, Opus effort=high; reported 2026-08-25, revision `5ff29cbeaecfa86fbbfc736bfad34016fb0d9c10` — the SAME head item 113's review audited, the two ran CONCURRENTLY; verdict **NO-GO — for the fourth consecutive review, nothing that ships is wrong: no P1, no memory-safety/provenance/ownership/FFI defect; two P2s, both incomplete halves of the two P2 remediations this very wave landed; four P3s, one of them (P3-2) a NEW defect introduced by this wave's own docs-honesty fix**) is a STATIC read-only audit (no sub-agents; nothing built or executed). It re-verifies all twelve fixes from tasks #1336-#1342 individually against source (not the index cards), confirms `nth_token`'s deletion leaves zero dangling references, confirms `CURRENT_NODE_SLOT`'s `Cell<u32>` swap is sound and complete. Two of its findings (P2-2, P3-1) independently converge with item 113's F2/F1 — confirmation, not transcription; four (P2-1, P3-2, P3-3, P3-4) are NOT in item 113. (Filed 2026-08-25, task #1344's session.)
    - **Status:** CLOSED (2026-08-25, in-place refresh by task #1351 — item 115's P2-1 caught this line still reading "OPEN — not yet remediated" after every finding had landed; sixth occurrence of the stale-index-card class). All six findings remediated: P2-1 → task #1347 (`68b04cd`); P2-2 → task #1346 (`96d6884`, converging with item 113's F2); P3-1 → task #1345 (`fcad8b3`, converging with item 113's F1); P3-2 → task #1348 (`44264ac`); P3-3/P3-4 → folded into task #1346.
    - **The findings, with this filing task's own verification verdict per finding — ALL personally re-verified against source before filing (not transcribed):**
      - **P2-1 (CONFIRMED — the review's own top-priority finding) — `tests/policy_oracle_linux.rs`'s negative control still hard-panics in exactly the seccomp container task #1336 was filed to fix, behind a comment that is now factually wrong:** `:639-642` (the bare `.expect` on `get_mempolicy_addr`), against `:626-631` (the comment claiming this test "never touches the NUMA policy machinery"). Personally re-read: `get_mempolicy(2)` (called via `get_mempolicy_addr`) IS in Docker's default seccomp profile's denied group (verified: `get_mempolicy`/`mbind`/`set_mempolicy` are one `CAP_SYS_NICE`-gated entry, default action `SCMP_ACT_ERRNO`/EPERM) — task #1336's own reasoning, written 200+ lines above in the SAME file, directly contradicts this test's comment. Task #1336 fixed the preflight's errno classification but never carried the same fix to this sibling call site. Concrete failure: on containerized Linux, `current_node()` → `Some(0)`, the plain reservation succeeds, `get_mempolicy_addr` → `EPERM`, `.expect` panics — task #1336's own GO condition ("no false red on a sandbox that denies the NUMA policy syscalls") is not met. **EXECUTED by task #1347 (2026-08-25, `68b04cd`):** the negative control's `get_mempolicy_addr` call site now classifies its errno with the SAME allowlist/panic/skip/`NUMA_SHIM_REQUIRE_ORACLE`-escalation structure the preflight already uses (reusing the existing `ERRNO_EPERM`/`ERRNO_ENOSYS` constants, no redefinition) — EPERM/ENOSYS skip loudly (fatal under `NUMA_SHIM_REQUIRE_ORACLE=1`), everything else panics fail-closed. The now-corrected comment states the test DOES call `get_mempolicy(2)` and is classified the same way the preflight is; the positive oracle's OWN separate `get_mempolicy_addr` call (line ~565, which only runs after a successful `mbind`, structurally excluding the same-group denial scenario) was deliberately left untouched, confirmed correct scoping by personal review of that call's surrounding control flow before merging. Kept inline (matching the file's existing style — the preflight and reserve-arm classifications are also inline, no shared helper) rather than extracting a new abstraction. Test-only change (`tests/policy_oracle_linux.rs` + a `CHANGELOG.md` entry); no production code, no version bump. Personally zero-trust verified: full diff read (2 files, +78/-8, confirmed the match arm's `Err(e)` type and `.raw_os_error()` call are consistent with `get_mempolicy_addr`'s actual `std::io::Result` signature), independently re-ran `cargo fmt --check -p numa-shim`, force-touched the changed file and re-ran `cargo check`/`cargo clippy -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --all-targets -D warnings` (confirmed genuine recompile — the touch forced a real rebuild, not a cache hit), host `cargo clippy --all-features --all-targets -D warnings`, and the full mock+`vmem-integration` suite (all passing, this file correctly compiles to 0 tests under mock) myself before merging — all green. Verification honesty: the actual EPERM/ENOSYS paths cannot be executed on this Windows dev host (Linux-only file, no Docker container reproduction attempted) — cross-compile check + clippy is the verification ceiling, same limitation as every prior Linux-only oracle task in this campaign.
      - **P2-2 (CONFIRMED, converges with item 113's F2) — `tests/readme_examples.rs` (created by this wave) reintroduces the exact arch-blind hard-fail task #1337 removed from `smoke.rs` days earlier, inherited from the README's own headline example:** `readme_examples.rs:115,198`; `README.md:97-123`; `src/lib.rs:140-157`. Independently re-derived the same mechanism item 113's F2 describes (real Linux non-x86_64/aarch64 arch: detection resolves `Some(n)`, reservation returns `UnsupportedArchitecture`, `.expect` panics) — personally confirmed `mod platform`'s detection gate (`all(target_os="linux", not(miri))`, no arch clause) is genuinely arch-independent while the reservation dispatch is arch-gated. **EXECUTED by task #1346 (2026-08-25, `96d6884`):** both sites now compose `reserve_preferred_on_node(...).ok().or_else(|| aligned_vmem::reserve_aligned(size, align)).expect("OOM")`, matching the crate's own already-established best-effort idiom (`reserve_preferred_on_node`'s "## Best-effort fallback" doc section, the README's second example — both fixed by task #1341). Both `tests/readme_examples.rs` tests updated to match exactly; the file's "known property rather than a surprise" panic caveat corrected since no panic path (other than genuine OOM) remains in either test. Personally zero-trust verified — see this card's own "Follow-up note" on item 112's card for the cross-reference, and item 113's F2 disposition below for the parallel closure narrative on that card.
      - **P3-1 (CONFIRMED, converges with item 113's F1) — sparc/sparc64 `O_CLOEXEC` is disabled while the correct `0x400000` value sits in the comment directly above the disabling code:** `src/lib.rs:1589-1598`. Same finding as item 113's F1, independently re-derived and cross-checked against `arch/sparc/include/uapi/asm/fcntl.h`. **EXECUTED by task #1345 (2026-08-25, `fcad8b3`)** — see item 113's own F1 disposition for the full technical account; this card's own P3-1 closes on the same commit.
      - **P3-2 (CONFIRMED, NOT in item 113 — a NEW defect introduced by task #1340's own fix, not pre-existing) — the corrected race-window comments in `tests/smoke.rs` contradict themselves and each other:** `:514-533` (Linux) and `:476-488` (Windows). Personally re-read: the Linux comment's own consecutive sentences assert BOTH that concurrent mappings (libtest thread stacks) exist in the binary AND that the binary lacks concurrent mmap activity; "if this file gains more mmap-performing tests" is phrased as though the count were zero when `smoke.rs` already contains a second mmap-performing test. The Windows twin states the "lacks concurrent activity" claim with no libtest-thread caveat at all, so the twins now also disagree with each other. Documentation-accuracy only — no test assertion changed, no failure observed, the underlying placement-mechanism analysis (top-down `mmap`) remains correct. **EXECUTED by task #1348 (2026-08-25, `44264ac`):** both twins rewritten to state the honest, mutually consistent claim — concurrent mappers (libtest thread stacks plus the sibling `reserve_preferred_on_node_returns_valid_span` test) genuinely exist in this binary today, so the oracle's real protection is the sub-millisecond window between `drop(r)` and the probes/read, NOT an absence of concurrent activity; a collision requires an unlucky scheduling interleave, not structurally excluded, and the risk is present now rather than contingent on the file gaining more mmap-performing tests later. The Linux twin's correct top-down-placement mechanism analysis and both twins' closing "suspect a placement collision, not a failed release" guidance left untouched — pure comment edit, no assertion/probe logic touched. Test-only change (`tests/smoke.rs` + a `CHANGELOG.md` entry); no production code, no version bump. Personally zero-trust verified: full diff read (2 files, +30/-12, re-read both twins side by side to confirm they are now mutually consistent and internally non-contradictory — the one thing that had to be true for this finding to actually close), independently re-ran `cargo fmt --check -p numa-shim`, host `cargo clippy --all-features --all-targets -D warnings`, force-touched the changed file and re-ran `cargo check` cross-compile for `x86_64-unknown-linux-gnu` (confirmed genuine recompile) plus the two affected tests specifically under mock (both passing, unchanged behavior) myself before merging — all green.

    **All findings from the twenty-first independent review (item 114) are now closed** (P2-1 task #1347; P2-2 converged with item 113's F2, task #1346; P3-1 converged with item 113's F1, task #1345; P3-2 task #1348; P3-3/P3-4 folded into task #1346). Combined with item 113's own full closure, EVERY finding from the nineteenth, twentieth, twenty-first, and twenty-second independent reviews is now remediated — the wave is ready for a combined twenty-third independent readiness review.
      - **P3-3 (CONFIRMED, NOT in item 113) — `CHANGELOG.md` still publishes the exact non-compiling snippet task #1341 was filed to remove, in a document that ships in the `.crate` tarball:** `:112,120` (the historical entry describing task #1306's original redesign, NOT task #1341's own entry ~27 lines below which already quotes the correct form). Personally re-read: `.or_else(|_| aligned_vmem::reserve_aligned(...))` — `Result::or_else`'s closure must return `Result`, `reserve_aligned` returns `Option` (re-confirmed against `crates/aligned-vmem/src/api/reserve.rs:63`). **EXECUTED by task #1346 (2026-08-25, `96d6884`):** both occurrences corrected to `.ok().or_else(|| aligned_vmem::reserve_aligned(size, align))`, matching the later same-file entry describing task #1341's own fix — only the broken snippet and its immediate prose pointer touched, surrounding historical narrative unchanged.
      - **P3-4 (CONFIRMED, NOT in item 113) — `tests/readme_examples.rs` has no green-and-dead CI sentinel, unlike its three sibling cfg-gated test files:** verified via grep across `.github/`, `scripts/`, `package.json` — no row greps any of its four test names. The exact task-#1101/#1070-"Breakage B" vacuous-green hazard task #1338 just finished closing for `policy_oracle_linux.rs`, recreated for the file that guards this wave's own two most recent findings (item 111's P3-4, item 112's F1). **EXECUTED by task #1346 (2026-08-25, `96d6884`):** `ci.yml`'s `NUMA_SHIM_REQUIRE_ORACLE=1` row (the same block task #1338 added sentinels to) gains one additional `grep -F "test readme_vmem_integration_example_compiles_and_runs ... ok"` line, matching the sibling greps' exact style. Personally verified: YAML syntax validated (`python -c "import yaml; yaml.safe_load(open(..., encoding='utf-8'))"`), and the sentinel's test name confirmed byte-exact against the actual `fn` definition before merging.

    **All six of this card's findings are closed** (P2-2/P3-3/P3-4 via task #1346; P3-1 via task #1345; P2-1 via task #1347; P3-2 via task #1348 — this line previously said P2-1/P3-2 "remain", corrected in place by task #1351 per item 115's P2-1).
    - **Next trigger:** none open on this card — the combined post-wave review already ran (item 115, the twenty-third review, CONDITIONAL GO, the campaign's first non-NO-GO) and re-verified all four of this card's fix commits individually. Any further trigger lives on item 115's own card.
    - **Evidence:** the report itself (committed by this filing); P2-1 personally re-verified against `policy_oracle_linux.rs:626-642,198-209,399-412` (confirming the comment's factual claim is contradicted by the code two lines below it) and against Docker's seccomp-profile grouping claim (re-derived independently, not merely trusted); P2-2/P3-1 cross-checked against item 113's independently-filed F2/F1 (convergent, not duplicated — both filed as one task each, not two); P3-2 against `smoke.rs:476-533` (confirming the internal and cross-twin contradiction); P3-3 against `CHANGELOG.md:112,120` plus `crates/aligned-vmem/src/api/reserve.rs:63` (the return-type ground truth); P3-4 via grep across CI/scripts/package.json — all at revision `5ff29cb`, the reviewed revision.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, nothing built or executed. This filing task independently re-verified every finding against source before filing — none transcribed on trust, including the two that converge with item 113 (confirmed independently, not copied from that card).

115. **[T, TWENTY-THIRD independent audit] `docs/reviews/2026-08-25-115731-numa-shim-publication-audit-run-19-Sol-codex.md`** (author Sol-codex, self-titled "прогон 19"; reported 2026-08-25 11:57:31 Europe/Berlin; revision `4a03fd37c921ee2fed5537f2caf36f4eec78d372` — the post-#1299 head, reviewing range `19ba84d..HEAD` which covers the entire #1345-#1348 wave plus #1299; verdict **CONDITIONAL GO — the FIRST non-NO-GO verdict of this whole Sol-codex/oh review campaign: "по исходному runtime-коду — GO", no new P0/P1, no ownership/provenance/leak/FFI defect, and all five reviewed fixes (#1345-#1348, #1299) individually re-verified as correct**) is a STATIC read-only audit (no sub-agents; nothing built or executed — its own header says so). Same unprompted arrival pattern as items 110/112/113: the report appeared already committed to `main` (`1df1b2c`, same git author identity, the concurrent-process pattern already investigated and confirmed benign at item 113's filing). Its two remaining conditions are both release-gate/documentation questions, NOT runtime defects. (Filed 2026-08-25, this session.)
    - **Status:** CLOSED (2026-08-25, same session as filing) — both P2 conditions remediated: P2-1 by task #1351 (`82bb2ab`, item 114's three contradicting lines synchronized in place), P2-2 by task #1352 (`bda0c87`, the #1099/I4 caveat rewritten to record the run-32828560947/job-97741909197 verification receipt). P3 remains an acknowledged post-release residual by design. What remains campaign-wide: push the accumulated unpushed wave with explicit owner authorization, confirm CI green on the landing SHA (which also gives #1299's own Windows sentinel rows their first execution), then the next independent review — potentially the release-gate closure.
    - **The findings, with this filing task's own verification verdict per finding:**
      - **P2-1 (CONFIRMED) — item 114's card contradicts itself:** `TRACKED_publish_readiness.md`'s item 114 says `Status: OPEN — not yet remediated` and (near the end) "P2-1 (task #1347) and P3-2 (task #1348) remain", while its own mid-card paragraph correctly states "All findings from the twenty-first independent review (item 114) are now closed". Personally re-read all three sites — the contradiction is real: the Status line and the closing "remain" line predate tasks #1347/#1348 landing and were never refreshed in place, while the all-closed paragraph WAS added. The sixth occurrence of this campaign's stale-index-card class (N9 → E6 → T5/T6 → U2/U3/U4 → item 103's own Status line → now). Fix: one in-place doc edit synchronizing the card's Status/"remain"/Next-trigger lines with reality.
      - **P2-2 (RESOLVED-BY-EVIDENCE at filing time) — the new Windows CI sentinel rows (task #1299's T8) hand a mixed-separator `$RUNNER_TEMP` path to Git Bash, a pattern `ci.yml`'s own task-#1099/I4 caveat (near the `test-windows` job) records as "UNVERIFIED on a real windows runner":** the review is right that the caveat exists and that the #1299 rows share the construction — but the caveat is now STALE, and this filing task closed the question with a log receipt rather than a code change: in CI run `32828560947` (the green run on `774716e`, completed earlier this same day), job `test windows (production)` (`97741909197`, conclusion `success`) executed exactly the flagged pattern — `cargo test ... 2>&1 | tee "$RUNNER_TEMP/forced-page-lazy.log"` under `shell: C:\Program Files\Git\bin\bash.EXE` followed by `grep -F "test forced_page::lazy_initial_commit_call_sites_survive_a_forced_64k_page ... ok" "$RUNNER_TEMP/..."` — and the raw job log shows both the tee'd cargo output AND the grep's own stdout echoing the found sentinel line, proving tee wrote and grep read the mixed-separator path successfully on a real windows-latest runner. The #1299 rows themselves have not executed yet (unpushed at filing time) but are the same construction on the same runner/shell; their own first green execution arrives with the next push, and the failure mode if anything differs is loud (red step naming the path), exactly as the caveat's own analysis states. Fix: update the #1099/I4 caveat comment to record the verification (run/job id) instead of "UNVERIFIED".
      - **P3 (ACKNOWLEDGED, non-blocking, deferred post-release per the review's own framing) — `tests/readme_examples.rs` is a manual transcription of the README's fenced examples, and its own module doc says editing the README without editing the copy "silently stops guarding it":** true by construction and already self-documented in the file; the sentinel added by #1346 proves the COPY runs, not that the copy MATCHES the README. The review's own "Что можно улучшить после релиза" section places the real fix (single source of truth — extract the fenced block at test time, or generate the README fragment from the tested code) after release. Recorded here as an acknowledged residual, not filed as a pre-release task.
    - **Next trigger:** (1) the item-114 card synchronization (P2-1) and the #1099/I4 caveat update (P2-2's log-receipt recording) — both doc/comment-only, one small commit each or combined; (2) push the accumulated unpushed wave (#1299's merge + this filing) with explicit owner authorization, confirm CI green on the landing SHA — which also gives the #1299 Windows sentinel rows their own first execution proof; (3) the next independent readiness review after that, which — given this review's own "по runtime-коду GO" — may be the release-gate closure review.
    - **Evidence:** the report itself (`1df1b2c`, committed by the concurrent process; this filing adds the card only); P2-1 personally re-verified against item 114's own card text (all three contradicting sites re-read); P2-2 verified positively via `gh run view 32828560947 --json jobs` (job-level `success` for `test windows (production)`) and `gh run view --job 97741909197 --log` (the tee command line, the bash.EXE shell line, and the grep's found-line output, all quoted from the raw log); P3 against `tests/readme_examples.rs`'s own module doc.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, nothing built or executed. This filing task personally re-verified both P2 findings before filing — P2-1 against the card text itself, P2-2 against live CI evidence the static review explicitly declined to gather ("Исполнение CI в этом исследовании намеренно не выполнялось") — the one finding-disposition difference between the report and this card (RESOLVED-BY-EVIDENCE vs the report's open question) comes from that additional evidence, not from disagreement with the report's reasoning.

116. **[T, TWENTY-FOURTH independent audit] `docs/reviews/2026-08-25-122931-numa-shim-publication-audit-run-20-fm.md`** (author fm, Fable 5 effort=medium, self-titled "прогон 20"; reported 2026-08-25 12:29:31 +02:00; revision `04713d97c65ac9f1a2e4bbf9418238281744fcb8` — the post-item-115 head, reviewing diff range `4a03fd3..04713d9` plus a full fresh whole-crate read; verdict **CONDITIONAL GO — the SECOND consecutive non-NO-GO, converging with item 115's run 19: no P0/P1/P2 anywhere, runtime code GO**) is a STATIC read-only audit (explicit brief: no sub-agents, no cargo invocation of any kind — no test/build/check/clippy/miri/bench/publish; source reading plus read-only `git log`/`git diff`, one CI receipt quoted from the repo's own committed record rather than re-fetched). Explicitly launched by the owner (not self-arriving, unlike items 110/112/113/115) with a dual brief: verify the diff since item 115 is genuinely doc/CI-only, AND do a fresh whole-crate review independent of prior findings' memory. (Filed 2026-08-25, this session.)
    - **Status:** CLOSED at filing — the one new finding (P3-1) is a doc-precision nit with no behavior change; the restated finding (P3-2) is already tracked on item 103.
    - **The findings, personally re-verified against source before filing:**
      - **P3-1 (NEW, doc-precision only, fail-closed direction, CONFIRMED) — `read_cpumap_into`'s doc understates its own rejection boundary by one byte:** `crates/numa-shim/src/lib.rs:1623` says `None` is returned "if the file is wider than `out`" — personally re-read the implementation (`:1642-1682`): the `total >= out.len()` guard at `:1674` fires and closes the fd/returns `None` BEFORE the EOF-proving zero-byte read can ever be issued, so a file EXACTLY `out.len()` (4096) bytes long is indistinguishable from a wider one and is rejected too, not just a strictly-wider file — the doc's "wider than" undersells its own behavior by one boundary case. Unreachable in practice (real cpumap text tops out around ~2304 bytes for `MAX_INDEXED_CPUS`; a 4096-byte file already implies a CPU-ID space beyond any supported kernel's `NR_CPUS`) and fail-closed (the wrong direction for a safety concern). **EXECUTED by task #1353 (2026-08-25, `e47f605`):** `src/lib.rs:1623`'s doc now reads "as wide as or wider than `out`" with a short parenthetical explaining the exactly-`out.len()` case, matching actual behavior. Doc-comment only, no behavior change. Personally zero-trust verified: `cargo fmt --check -p numa-shim`; force-touched the file and re-ran `cargo check -p numa-shim --features vmem-integration --target x86_64-unknown-linux-gnu --tests` (genuine recompile, clean); `RUSTDOCFLAGS="-D warnings" cargo doc -p numa-shim --all-features --no-deps` (clean).
      - **P3-2 (RESTATED, not new — already tracked)** — the CHANGELOG's Phase-1 caveat still cites the historical "31/0 at `c427dd6`" count against the current 33-test suite; already folded into item 103's U1/U5/U6, which fold the caveat/waiver text update into task #1262's own final-step commit alongside the pre-tag Phase-1 re-run. No new action from this card.
      - **Diff-range claim (CONFIRMED mechanically by this filing task, not just trusted from the report):** `git diff 4a03fd3..04713d9 -- .github/workflows/ci.yml | grep -E '^[+-]' | grep -v '^[+-][+-][+-]' | grep -vE '^[+-]\s*#'` returns ZERO lines — every changed line in that range's one non-doc file is a `#` comment, confirming tasks #1351/#1352 (and the surrounding docs-only commits) touched nothing but comments/documentation, exactly as both this report and their own commit messages claim.
    - **Next trigger:** none code-side. Release-process only, unchanged from items 103/106/115: (1) push the accumulated unpushed wave with explicit owner authorization and confirm CI green on the landing SHA (first execution of task #1299's new sentinel/MSRV rows); (2) the owner-scheduled release scope (version bump, CHANGELOG consolidation, #1262's final Phase-1 re-run, post-bump dry-run); (3) the release-gate closure review.
    - **Evidence:** the report itself (`05a1915`, committed by the reviewing agent); P3-1 personally re-verified against `src/lib.rs:1623,1642-1682` (both the doc line and the full function body re-read); the diff-comment-only claim independently re-derived by this filing task with its own grep command (not re-run of the report's own claim), output pasted above.
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent (fm, Fable 5 medium), no sub-agents, no cargo command of any kind, no file edited besides its own report. This filing task personally re-verified the diff-comment-only claim with an independently-constructed command (not merely re-trusting the report's own grep) and the P3-1 doc/behavior mismatch against current source before filing.

117. **[T, TWENTY-FIFTH independent audit] `docs/reviews/2026-08-25-130850-numa-shim-publication-audit-run-20-Sol-codex.md`** (author Sol-codex, self-titled "прогон 20" — a separate run counter from fm's own "run 20", item 116, coincidental same number, different author/methodology; reported 2026-08-25 13:08:50 Europe/Berlin; revision `ba98bc375b914e5fbe0ad53022f228c5e3702970` — the post-item-116/task-#1353 head; verdict **GO on the source code and publication surface — the strongest verdict of the campaign to date: not "CONDITIONAL GO", a plain GO, with the explicit finding "no P0/P1/P2, no UB, no provenance/ABI/FFI violation, no double-free, no ownership leak, no errno/GetLastError timing bug, no new platform-dispatch regression"**) is a STATIC read-only audit from scratch (no sub-agents; explicitly forbidden by the requester from running anything executable — tests/build/`cargo check`/Clippy/Miri/benchmarks/rustdoc/publish). Same unprompted arrival pattern as items 110/112/113/115: appeared already committed directly to `main` (`a8f4aaa`, same git author identity, the concurrent-process pattern already investigated and confirmed benign at item 113's filing). Re-read the ENTIRE crate from scratch (all 2346 lines of `src/lib.rs` including every `cfg` branch, all ten test files, the benchmark, every numa-shim CI cell, `Cargo.toml`/README/CHANGELOG, and the `aligned_vmem::Reservation::from_raw_parts` safety contract the Windows backend hands ownership through) against `rust-intel`'s full category list, not just the diff since the last review. Explicitly caveats its own verdict: "a static GO should not be read as a claim that I executed the release gate — nothing executable ran in this study by the requester's direct prohibition." (Filed 2026-08-25, this session.)
    - **Status:** OPEN — five P3s, all non-blocking per the review's own explicit framing ("не задерживать публикацию... не correctness blockers"); none filed as tasks yet, recorded here for future action per owner priority.
    - **The findings, with this filing task's own verification verdict per finding — spot-checked, not all five independently re-derived (consistent with this campaign's established filing convention):**
      - **P3-1 (CONFIRMED, personally re-verified) — the benchmark's `current_node/first_call` vs `/warm_call` split is order/filter-dependent because the mock records every call into a capped thread-local log, and `bench-iters.txt` has drifted from the current workload set:** `benches/numa_bench.rs:18-26,61-78`, `src/lib.rs:300,401,424-425,525-534`, `bench-iters.txt:7-11`. Personally re-read: `bench-iters.txt` lines 7-9 still list three `bind_range/*` entries (`no_node_noop`, `page_to_node_7`, `zero_len_noop`) — the API task #1306 removed entirely — while `reserve_preferred_on_node/invalid_node_error` (the bench that actually exists at `numa_bench.rs:90`) has NO corresponding line in `bench-iters.txt` at all (confirmed via grep, zero matches). The inline comment at `numa_bench.rs:63-64` — "the same cost the real backend pays after its cache is populated" — is also confirmed present verbatim, which the review correctly flags as misleading (mock path = TLS access + recording; real warm path = platform CPU sample + `OnceLock` access + reverse-index lookup, a different cost shape entirely). Not a correctness defect — a benchmark-hygiene gap that would mislead a future cold/warm comparison. **EXECUTED by task #1354 (2026-08-25, `1df20ba`, merged `a9c7d8d`):** all three workloads now use `Harness::bench_batched` with `mock::drain()` as UNTIMED per-iteration setup (the harness's own documented "setup must be fresh every iteration" shape; a registration-time drain would not normalize, since all closures run later inside one sequential `h.run()`, and draining inside a plain `bench()` closure would time the drain itself) — every timed iteration now starts from the same known state (an empty log), independent of workload order/filter/scale. The misleading `:63-64` comment is corrected with an accurate cost-shape comparison; the module doc gains a normalization paragraph naming the accepted tradeoff (one `Instant::now()` pair per iteration now included in ns/op). `bench-iters.txt`'s three stale `bind_range/*` lines are deleted and `reserve_preferred_on_node/invalid_node_error` gets a fresh calibrated entry (both `current_node` entries also recalibrated as a direct, flagged consequence of the shape change; diff-verified that no other bin's entry drifted). **Empirically demonstrated, not just argued:** before the fix, `warm_call` measured 14.49 ns/op run after `first_call` (log already at cap) vs 23.23 ns/op run alone (log starts empty) — a ~60% order-dependent gap; after the fix, the same comparison reads 69.04 vs 68.80 ns/op (0.3%), with repeated sampling confirming the residual is non-monotone-w.r.t.-order noise, not a shape defect. Personally zero-trust verified: full diff read; force-recompiled and confirmed a genuine `--cfg numa_shim_mock` rustc invocation (not a stale-cache pass); `cargo fmt --check -p numa-shim` and `cargo clippy` (both cfgs) clean; the full mock+`vmem-integration` test suite independently re-run, 51/51 across 12 binaries; the benchmark itself independently re-run at reduced scale, completed without panicking, numbers consistent with the source task's own report.
      - **P3-2 (restated, already tracked — not new)** — `tests/readme_examples.rs`'s manual-transcription drift-guard limitation; same residual item 115's own P3 already recorded and deferred post-release.
      - **P3-3 (restated, already tracked — not new)** — `tests/smoke.rs`'s release-oracle same-process concurrent-mapper race window; the comments are now honest about the risk (per task #1348's fix, item 114's P3-2) but the flake potential itself is structural and unclosed; recommends process/binary isolation if it ever actually flakes, explicitly non-blocking today (no observed flake).
      - **P3-4 (CONFIRMED, personally re-verified) — `CHANGELOG.md`'s `### Owner decisions pending` section heading contradicts its own two entries' content:** `CHANGELOG.md:36-55`. Personally re-read both bullets: the `mock`-feature entry says "DECISION MADE 2026-08-23 (task #1288)"; the `#[doc(hidden)]` semver-policy entry says "**DECIDED** (task #1289, owner-confirmed)" — both entries are resolved, yet the section header still says "pending". A crates.io-shipping document giving a false impression of open decisions. One-line fix: rename to "Resolved owner decisions" or fold into `### Changed`.
      - **P3-5 (CONFIRMED, personally re-verified) — two known cfg cells have no dedicated CI signal:** `.github/workflows/ci.yml:2910-2911,2950-2962`, `CHANGELOG.md:172`. The macOS-miri job covers real/default, real/`vmem-integration`, and mock/default, but not mock+`vmem-integration` under miri (the file's own comment already documents this gap, task #1325's CHANGELOG entry). Separately, no rustdoc row builds with `--cfg numa_shim_mock`, so the public mock seam's intra-doc links have no dedicated gate (distinct from the already-covered `--all-features`/docs.rs-derived/default rustdoc rows, none of which set the cfg). Both test-only surfaces, neither reachable from a docs.rs/default consumer build; hardening, not a release blocker. **EXECUTED by task #1356 (2026-08-25, `5ec5806`, merged `a3fbcd2`):** added the missing miri mock+`vmem-integration` row (same three sentinels as its non-miri twin) and a mock rustdoc row. **A real green-and-dead trap was found and avoided in the process:** `cargo doc` does NOT forward `RUSTFLAGS` to the rustdoc invocation it spawns — a naive `RUSTFLAGS=... cargo doc` row would exit 0 with zero warnings and generate NO `mock/` docs at all; the cfg must ride in `RUSTDOCFLAGS` instead. Delegated to `/crush`, which lost its network connection mid-verification (TLS handshake timeout) after making the edits but before committing; this task personally completed the CHANGELOG entry, independently re-verified every claim (YAML validity, sentinel names, the non-miri proxy run, and a counterfactual `RUSTFLAGS`-only re-run that reproduced the exact green-and-dead failure being guarded against — confirming the fix is load-bearing, not cosmetic), then committed and merged.
    - **Next trigger:** none of the five P3s are release-blocking per this review's own explicit recommendation; the review names the benchmark-hygiene item (P3-1) as "the most useful near-term fix after publication" (normalize recording state, delete stale `bench-iters.txt` IDs, recalibrate) — a good post-release housekeeping candidate, not filed as a task yet. Campaign-wide, unchanged from items 115/116: (1) push the accumulated unpushed wave with explicit owner authorization and confirm CI green on the landing SHA; (2) the owner-scheduled release scope; (3) the release-gate closure review — this review is explicit that its own static GO is NOT a substitute for that step.
    - **Evidence:** the report itself (`a8f4aaa`, committed by the reviewing process); P3-1 personally re-verified against `bench-iters.txt:7-11` (grep for `bind_range`/`invalid_node_error`, exact match to the review's claim) and `benches/numa_bench.rs:63-64` (comment text quoted verbatim, confirmed present); P3-4 personally re-verified against `CHANGELOG.md:36-55` (both bullet texts re-read in full).
    - **Independence and mode, stated so this card does not overclaim.** The audit ran read-only, one agent, no sub-agents, nothing executable run (explicitly by the requester's own prohibition, stated in the report itself) — even rustdoc, which several campaign reviews DO run read-only, was not run here. This filing task personally re-verified two of the five findings (P3-1, P3-4) against current source before filing; P3-2/P3-3 are restatements of already-tracked residuals (items 115/114) and were not re-derived independently; P3-5 was spot-checked by re-reading the cited `ci.yml` line ranges' own comments, which already document the gap the review names.
