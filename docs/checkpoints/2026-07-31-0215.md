# Checkpoint — 2026-07-31 02:15 [round-31-complete-with-review]

## Session summary

This checkpoint supersedes `docs/checkpoints/2026-07-31-0100.md` (Round
31's 7 core tasks) by recording the completion of the wrap-up ritual: an
independent `@oh` full-round review, and the fix of everything that review
found actionable.

**`@oh` review** (`docs/reviews/2026-07-31-r31-full-review.md`, commit
`eb6e3dc`): 0 P0, 3 P1, 12 P2. All build/lint/test commands green on
`HEAD` at review time; `production`'s composition confirmed byte-identical
before/after; six independently-recomputed headline numbers all reproduced
exactly from raw data. I personally re-verified the two most consequential
P1 claims before acting (`git diff 14a9ef3..HEAD -- CLAUDE.md` = 0 lines
confirming P1-3; `grep -c "throw|assert" scripts/r31_0_summary.mjs` = 0
confirming P1-1) rather than trusting the review's narration.

**P1 fixes, both personally zero-trust reviewed**:
- **P1-1** (`eb6e3dc`, committed by me directly): `scripts/r31_0_summary.mjs`
  claimed to derive R31-0's P0 headline Δ table mechanically but actually
  computed no arithmetic and never read the run2 logs. Extended it to
  genuinely compute every cell's delta and hard-`throw` on mismatch. I
  personally proved non-vacuity myself (not just trusted the fix): corrupted
  one raw-log value, confirmed the script threw `HEADLINE MISMATCH ... do
  not publish until reconciled`, reverted, confirmed it passes again. The
  underlying CSV data was byte-identical before/after (only the pending SHA
  placeholder changed) — no re-measurement occurred, only re-derivation.
- **P1-2** (`2d9eef2`, via `@sh`): R31-2's report falsely claimed a
  runtime config-read-back that the harness never performed. Fixed via
  option (b) — honest append-only correction — after the agent correctly
  reasoned that option (a) (add the read-back) would require switching the
  workload off the real `#[global_allocator]` onto `claim_with_config`,
  which would itself become the wrong-layer mistake the round's P1-3 rule
  now exists to prevent. Verdict unchanged.
- **P1-3** (`2d9eef2`, via `@sh`) — the most important one: codified the
  round's own centerpiece defect class (a judge can satisfy every existing
  rule, including a path-activation oracle, and still measure the wrong
  allocator layer) as a new CLAUDE.md rule, explicitly framed as the third
  instance of a meta-pattern (R25-5 wrong config → R26-4; R29-16 wrong code
  path → R30-8; R30-3 wrong layer → this rule). Also filed as
  `docs/perf/OPEN_ITEMS.md` item 32 so a fresh Round 32 session inherits the
  memory without needing to read this review doc first.
- **P2-6** (folded into `2d9eef2`): `ReservedSmallSegment` now carries
  `#[must_use]` on the struct itself, not just its accessor.
- **Remaining 11 P2s**: filed UNVERIFIED-BY-ME into `docs/perf/OPEN_ITEMS.md`
  item 33 and `docs/CORRECTNESS_OPEN_ITEMS.md` item 9, matching the exact
  precedent Round 30's own review response established (items 31/8).

**One agent connection dropped mid-response** during the first review-fix
attempt (API error, not a graceful stop). I found real, uncommitted P1-1
progress in the working tree afterward, personally verified it was sound
(diffed the CSV to confirm no data changed, only the pending-SHA
placeholder; ran the script; proved non-vacuity myself), and committed it
directly rather than discarding it or blindly trusting it.

**Final state**: all tests/clippy/fmt green on `HEAD` (`2d9eef2`),
confirmed by me directly (not just from an agent's claim) as the very last
action before this checkpoint.

## Active goal

The user's `/babygoal` instruction for this window is now fully satisfied:
tasks implemented via `@sh` with commits between them; `/checkpoint` done
(this file, plus the prior `2026-07-31-0100.md`); CHANGELOG updated
incrementally by every task and by both review-response commits; markdown
committed (including both review docs); `@oh` review launched and acted on.

## TaskList

Unchanged from `docs/checkpoints/2026-07-31-0100.md`: Round 31's core 7
tasks completed (#471, #466, #465, #464, #476, #467, #473); 7 tasks
explicitly deferred to Round 32 (#468, #469, #470, #472, #474, #475, #477);
#449 (crush-fallback) dormant.

## Decisions

- **Did not blindly trust the review's own claims either** — independently
  re-verified its two most load-bearing assertions (P1-1, P1-3) against
  the repo directly before dispatching any fix work, matching this
  session's standing zero-trust discipline applied symmetrically to both
  the round's own agents AND the review that audited them.
- **Recovered cleanly from a sub-agent's mid-response connection drop** by
  inspecting the actual working-tree state rather than assuming either
  "nothing happened" or "trust whatever's there" — verified the partial
  work was sound before committing it myself.
- **Fixed all 3 P1s and 1 P2 (P2-6) rather than deferring any of them** —
  unlike the Round 32 backlog deferrals, none of these needed a product
  decision, all were cheap, and P1-3 specifically closes a real
  memory-loss risk for the very next round.

## Open questions

Unchanged from the prior checkpoint: two production-composition decisions
(`virgin-zero-skip`, `large-cache-extended`) await explicit user sign-off;
whether Round 32's deferred backlog should run under the same discipline.

## Repo state

```
?? .claude/
```

(clean otherwise — `HEAD` at `2d9eef2`, all Round 31 work + both reviews +
both review-response fixes committed)

```
2d9eef2 docs(perf): Round 31 review response, P1-2/P1-3 + file 12 P2s -- allocator-layer rule codified
eb6e3dc docs(perf): Round 31 review response, P1-1 -- scripts/r31_0_summary.mjs now actually derives and asserts the headline deltas
aaed7c4 docs: Round 31 core-complete checkpoint
d93c091 docs: two independent readonly reviews that seeded Round 31 (@fm acceleration review, post-response P0 review)
865534d refactor(profile): split Profile into independent SmallPoolPolicy/LargeCachePolicy axes (R31-9, task #473)
```
