# Checkpoint — 2026-08-09 06:00 [crate-by-crate-rust-intel-remediation]

## Session summary

This session executes a `/babygoal`-driven crate-by-crate sweep of the 5+1
workspace sub-crates' `/rust-intel` audit findings from 2026-08-07, per the
user's standing instruction: process ONE crate at a time, never move to the
next until ALL of the current crate's tasks are closed; after each crate,
`/checkpoint` + update CHANGELOG.md + commit markdown + `@oh` closing
review, then repeat. The TaskList enforces this via a strict `blockedBy`
chain: sefer-region → tagged-index-stack → racy-ptr-cell → a small
cross-cutting workspace task (#711, hoisting `loom = "0.7"` into
`[workspace.dependencies]`) → aligned-vmem → numa-shim → size-classes.
Since the last context compaction, this session has been driven almost
entirely by recurring `# babysit tick` cron invocations: each tick checks
for a fresh git-log signal on the perpetually-`in_progress` task #656
(sefer-region's crates.io republish decision, gated on the user and never
advanceable by an agent), and when no fresh signal exists, picks up the
next real ready pending task instead — this is how tagged-index-stack's
review-follow-ups (#771-772) and all of racy-ptr-cell's fix tasks got done,
tick by tick, without any new user instruction.

**sefer-region and tagged-index-stack are both fully closed**, including
follow-up tasks each crate's own round-closing `@oh` review surfaced.
tagged-index-stack's review (`docs/reviews/2026-08-09-tagged-index-stack-round-closing-review.md`)
found a genuine HIGH-severity defect (F1) in a task the round had already
marked complete — the untagged ABA loom counterfactual's oracle was itself
schedule-dependent and never actually reached the corruption it claimed to
prove — fixed in task #771, with 10 more lower-severity findings closed in
#772.

**racy-ptr-cell is now fully closed too** — all 6 of its own fix tasks
(#700, #706-710) landed, each individually zero-trust verified with a real
counterfactual (temporarily reverting the fix, confirming the associated
test fails for the right reason, then restoring it) before commit. Notable:
#700 (HIGH) fixed a vacuous happens-before oracle where two loom tests read
`init_marker` only AFTER `join()`, which itself establishes happens-before
regardless of the cell's internal ordering; #706 added an RAII
`RollbackGuard` so a panicking `init` closure can't wedge the
`INITIALIZING` sentinel forever (a silent whole-process livelock); #710
made a maintainer-style API-posture decision (promoted two `dbg_*` hooks
from `#[doc(hidden)]` to documented stable public API, since one hook's own
doc explicitly invites downstream consumer use — a real self-contradiction
`#[doc(hidden)]` couldn't resolve) plus closed 9 missing `// SAFETY:` tags.

**racy-ptr-cell's post-work chain is 2/4 done**: #740 (checkpoint,
completed — an earlier checkpoint this same session,
`docs/checkpoints/2026-08-09-0530.md`) and #741 (CHANGELOG.md update,
completed, commit `11ca6ee`) are done. **#742 (commit all markdown docs
from this round) is next** — likely a no-op or near-no-op, since this
round's markdown (README.md, the CHANGELOG entry, the checkpoint, the
review-doc resolution note) were already committed inline with each task's
own commit rather than batched separately; #742 exists to catch anything
left uncommitted. Then #743 (`@oh` final review of the whole racy-ptr-cell
round) — #743's completion unblocks #711 (the workspace `loom` dependency
hoist), which unblocks aligned-vmem's fix-task group, the next crate in
the sweep.

This `/checkpoint` invocation interrupted nothing in-flight — the previous
tick had just cleanly completed task #741 and reported "started #741
(completed)" before this command arrived.

## Active goal

No formal `/goal` Stop hook is active. Crate-by-crate sequencing is
enforced structurally via the TaskList's `blockedBy` graph, and steady
forward progress is driven by the recurring `# babysit tick` cron — both
of these are the standing mechanisms this session runs on, not something
this checkpoint needs to re-establish.

## TaskList

### in_progress
- #656 sefer-region — verify/prepare for crates.io republish (perpetually
  blocked on a maintainer decision the user must make; not part of the
  active crate sweep — each babysit tick correctly finds no progress
  signal here and moves to real pending work instead)

### pending (next up, in strict blockedBy order)
- #742 Post-work (racy-ptr-cell): commit all markdown docs from this round
  — READY (blockedBy resolved)
- #743 Post-work (racy-ptr-cell): run @oh final review of all round work
  [blocked by #742] — unblocks #711
- #711 Workspace: hoist `loom = "0.7"` into `[workspace.dependencies]`
  [blocked by #743] — unblocks aligned-vmem group
- #699,712-719 aligned-vmem fixes (1 HIGH: fault-injection tests run in
  zero CI configs) — blocked by #711
- #744-747 Post-work (aligned-vmem) — #747 unblocks numa-shim group
- #697,720-727 numa-shim fixes (0 HIGH, real bugs incl. mbind maxnode
  off-by-one) — blocked by #747
- #748-751 Post-work (numa-shim) — #751 unblocks size-classes group (LAST)
- #701,728-731 size-classes fixes (0 HIGH, cleanest crate) — blocked by #751
- #752-755 Post-work (size-classes) — #755 is the final task of the
  entire crate-by-crate sweep
- #657-661,756-768 publish-readiness / bench-scale-tool / captrack tasks
  — independently gated behind each crate's final `@oh`-review, not part
  of the active sequence itself
- #673 sefer-region: [UNVERIFIED — no defect found] deliberately deferred
  future-decision-gate, not part of the active sweep

### recently completed (this session)
- #741 Post-work (racy-ptr-cell): CHANGELOG.md update — commit `11ca6ee`
- #740 Post-work (racy-ptr-cell): /checkpoint — commit `968690e`
- #710,709,708,707,706,700 racy-ptr-cell's own fix tasks — commits
  `2af6da3`,`2e5fb72`,`b65c51a`,`9b98c7a`,`048c657`,`0db373d`
- #772,771 tagged-index-stack round-closing-review follow-ups
- #705,704,703,702,698 tagged-index-stack own fix tasks

## Decisions

- **User: "act independently, don't ask me — you can ask @oh"** — standing
  instruction for the rest of this session; superseded the earlier
  `AskUserQuestion` pattern for maintainer-style tradeoffs.
- **#706's fix**: RAII guard (`RollbackGuard`) — idiomatic panic-safety,
  fires correctly on the unwind path only (defused on both non-unwinding
  exits).
- **#709's fix**: `expose_provenance`/`with_exposed_provenance_mut` rather
  than storing pointers directly — the "preferred" route from the task's
  own description doesn't work here, since `Mutex<Vec<*mut Payload>>`
  would itself be `!Sync` (raw pointers are `!Send`).
- **#710's API-posture decision**: promote `dbg_is_ready`/
  `dbg_rollback_reenterable` to documented, non-hidden stable public API
  rather than feature-gating them — `#[doc(hidden)] pub` is
  self-contradictory when a hook's own doc explicitly invites external
  consumer use; feature-gating was rejected as disproportionate since both
  hooks are already exercised unconditionally by the crate's own test
  suite.

## Open questions

None new since the last checkpoint. Carried over, still unresolved: the
sefer-region 0.1.1 version-bump decision (#656's gate — genuinely requires
the user, not an agent decision, per the project's own version-bump rule),
the unexplained `TypeId` mismatch from task #692's panic-payload downcast
investigation (practically worked around, not root-caused), and
`docs/reviews/2026-08-05-release-readiness-gap-audit.md`'s NO-GO verdict
for a 0.3.0 release (summarized to the user earlier in the session, no
task filed yet).

## Repo state

```
(clean — nothing uncommitted)
```

```
11ca6ee docs: update CHANGELOG.md with the racy-ptr-cell rust-intel round (tasks #700,706-710)
968690e docs: commit checkpoint after racy-ptr-cell round fully closed (task #740)
2af6da3 docs(racy-ptr-cell): decide the dbg_* hooks' API posture, close 9 missing SAFETY tags, fix a stale identifier (task #710)
2e5fb72 test(racy-ptr-cell): fix a provenance-losing int->ptr round-trip in a loom test's reclaim path (task #709, MEDIUM)
b65c51a test(racy-ptr-cell): close two zero-coverage soundness-load-bearing contracts (task #708)
```
