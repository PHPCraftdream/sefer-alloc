# Checkpoint — 2026-07-21 21:xx [r12-planned]

## Session summary
This session resumed from checkpoint `2026-07-21-r11-complete.md` (Round 11, #244–#251, all
landed as commits `33581bd`..`8e592f4`, CHANGELOG updated, TaskList empty). Three pieces of work
happened after resume, in order:

1. **Docs commit + `npm run check`.** Committed the two Round 11 checkpoint files (`dc0a673`).
   Ran `npm run check`; it failed on `dealloc_sublinear::own_thread_free_is_subquadratic`
   (measured ratio 3.15 vs the 3.0 threshold) — investigated and confirmed this was machine-noise
   flakiness, not a real O(1)→O(N) regression (4 independent isolated reruns — 1 release, 3 debug
   — all passed; absolute times were nowhere near the documented O(N²) regime). Re-ran `npm run
   check`: fully green (fmt, clippy×3, full `production` test suite, iai).

2. **Fixed 5 real compiler warnings surfaced by that green run** (user asked "исправь все
   предупреждения"). Root-caused all five (no `#[allow(...)]` band-aids):
   - `heap_core_dealloc_batch.rs`: `dealloc_batch_small` and its exclusive imports (`Layout`,
     `os`, `SegmentHeader`, `SegmentKind`, `SegmentMeta`, `SizeClasses`, `MIN_BLOCK`,
     `TCACHE_CAP`) were gated one feature short of their sole caller `dealloc_batch`'s
     `batch-api` gate — under `production` (no `batch-api`) they compiled with no caller
     reachable. Added `batch-api` to every one of those cfg gates.
   - `segment_directory.rs`: `get_bit`'s only caller (`dbg_directory_get_bit_for_node`) is
     `numa-aware`-only; `get_bit` itself carried no cfg. Gated it identically to its caller.
   - `alloc_core_small.rs`: R11-6's NUMA-bucket-order `buckets`/`n_buckets` locals were `mut`
     with a dummy init meant for the `numa-aware` branch; under `not(numa-aware)` (production
     default) they were never mutated (unused-mut / unused-assignment). Split into two
     `#[cfg]`-gated `let` bindings, each bound directly to its final state.
   Verified: full `npm run check` ALL GREEN with zero warnings from own code (only a third-party
   `proc-macro-error2` future-incompat note remains — investigated separately, see Decisions).
   Also spot-checked `--features batch-api,production[,hardened]` and
   `--features production,numa-aware-mock` didn't regress. Committed as `090e4ff`.

3. **Round 12 planning.** User (`/fm`) supplied two independent external reviews of the Round 11
   wave (one focused on correctness/soundness gaps R11 left behind, one focused on new
   radical-speedup candidates). Read both in full, personally verified the load-bearing factual
   claims against the actual source before trusting them (confirmed: the `&SegmentDirectory` /
   `&mut SegmentDirectory` aliasing pattern in `alloc_core_small.rs` around the directory scan
   loop; `MAX_NODES = 8` in `segment_directory.rs` vs. the NUMA shim's `0u32..64` node scan in
   `crates/numa/src/lib.rs`; `EMPTIED_BASES_CAP = 64` in `heap_core_xthread.rs`; the exact-span
   Large rounding at `alloc_core_large.rs:94-95`). Synthesized both reviews into a single
   prioritized Round 12 queue and filed 13 tasks (#252–#264) with full technical briefs (facts
   verified, fix direction, test/judge criteria) so each is resumable without re-reading this
   conversation. Established two `blockedBy` dependency edges: #253 (NUMA node ID fix) after
   #252 (aliasing fix — same scan code), and #255 + #264 (realloc reserved-capacity, page-run
   layer) after #254 (exact-span Large — its verdict reshapes both downstream tasks).

No `/crush` or `@sh` agent work has started yet on Round 12 — this checkpoint is taken right
after planning, before execution begins. The last thing said to the user was an offer to start
executing #252 first via `/crush`; no reply had arrived when `/checkpoint` was invoked.

## Active goal
None currently armed (no `/goal` Stop hook in force this session). The standing methodology from
CLAUDE.md — implement via `/crush` (fallback `@sh` on peak-hours/quota exhaustion per the user's
established rule), commit + test between tasks, zero-trust review before every commit — applies
by default to executing the queue below, once the user confirms starting.

## TaskList
### pending
- #252 R12-1 (P0): устранить reference-aliasing directory-сайдкара в скан-цикле
- #253 R12-2 (P0): корректно представить NUMA node ID 8–63 в directory (blockedBy: #252)
- #254 R12-3 (P0-perf): exact-span Large A/B — убрать округление до 4 MiB
- #255 R12-4 (P0-perf): reserved-capacity Large realloc — Windows-gated прототип (blockedBy: #254)
- #256 R12-5 (P1): дешёвый периодический refresh кэша current_node()
- #257 R12-6 (P1): закрыть overflow-finalization сверх 64 emptied bases
- #258 R12-7 (P1): class-aware dirty routing — wall-clock gate, затем per-class bitmap
- #259 R12-8 (P1): объединить AllocBitmap+MagazineBitmap в 2-bit BlockStateMap
- #260 R12-9 (P1): primordial-only lazy commit — расщепить политику, включить в production
- #261 R12-10 (P1): virgin-zero skip для Small alloc_zeroed — stage-2 реализация
- #262 R12-11 (P2): убрать PageMap из production carve-пути (или за diag-фичу)
- #263 R12-12 (P2): честно назвать batch API экспериментальным (semver-поверхность)
- #264 R12-13 (P2, после R12-3): page-run layer stage-2 — только если exact-span не закрыл нишу (blockedBy: #254)

### recently completed
(none this session — Round 11's #244–#251 all completed in the prior session, see
`2026-07-21-r11-complete.md`)

## Decisions
- Prioritized both P0 correctness fixes (#252 aliasing, #253 NUMA node coverage) strictly ahead
  of any Round 12 perf work — chosen over doing the two "P0-perf" radical-speedup candidates
  first, because both are soundness-adjacent (one is formal UB per the documented safety
  contract, the other is a silent locality-defeating bug on the exact large-node-count machines
  the R11-6 optimization targets).
- Ordered exact-span Large (#254) before reserved-capacity realloc (#255) and before a page-run
  layer stage-2 (#264, made conditional on #254's verdict) — both reviews converge that exact-span
  Large is the cheapest new experiment and may substantially shrink or eliminate the page-run
  layer's justification, so building the expensive page-run machinery first risked wasted work.
- Investigated (read-only, no dependency bump performed) whether upgrading `iai-callgrind`
  0.14→0.16 would clear the `proc-macro-error2` future-incompat warning left over from the
  `090e4ff` cleanup. Queried the crates.io sparse index directly: `iai-callgrind-macros` has
  required `proc-macro-error2 ^2.0.1` unchanged across every version from 0.4.1 through the
  current latest 0.6.1, and `proc-macro-error2` itself has published nothing past 2.0.1 (no
  upstream fix exists yet). Concluded the bump would not help; user agreed to leave it as-is
  ("ладно, оставим") — no Cargo.toml change made, no task filed for it (nothing actionable until
  upstream ships a fix).
- Did NOT file a task for the "moonshot" unbounded cross-thread remote-free queue from review 2
  (the 0.3–2s paused-owner stall fix) — both reviews independently agree it should come after
  exact-span Large and class-aware dirty routing, and its concurrency/ABA surface is large enough
  that it deserves its own scoping pass once #254/#258 land, not a premature task brief now.
- Filed #263 (batch API experimental-naming) as P2 even though both reviews raised the semver
  concern (this is now the second review round to flag it, after R10/R11) — judged it low-risk/
  low-effort relative to the P0/P1 correctness and perf items, so it doesn't compete for the front
  of the queue, but flagged in the task description that it's a repeat finding worth not deferring
  indefinitely.

## Open questions
- Whether to start executing #252 now via `/crush` — asked the user in the last message before
  this checkpoint; no answer received yet within this window.
- #260 (primordial-only lazy commit) task description explicitly flags that actually flipping the
  new split feature into the `production` bundle (Cargo.toml:238) should be confirmed with the
  user as a separate question when that task nears completion, since it changes the production
  feature composition (not a version bump, but still a user-facing default-behavior change worth
  a check-in per the spirit of the project's "explicit request" conventions).
- #263 similarly flags that if the delegate finds the two naming options (rename the feature vs.
  keep `batch-api` but nest it under `experimental`) roughly equivalent, it should ask the user
  to pick rather than deciding unilaterally.

## Repo state
```
?? _gad_suite.log
?? docs/perf/_raw_baseline_off.log
?? docs/perf/_raw_baseline_off_reduced.log
?? docs/perf/_raw_criterion_medium.log
?? docs/perf/_raw_criterion_production.log
?? docs/perf/_raw_firstalloc_medium.log
?? docs/perf/_raw_firstalloc_production.log
?? docs/perf/_raw_iai_medium.log
?? docs/perf/_raw_iai_production.log
?? docs/perf/_raw_medium_on.log
?? docs/perf/_raw_medium_on_reduced.log
?? docs/perf/_raw_r10_6_dir_off_nonnuma.log
?? docs/perf/_raw_r10_6_dir_on_nonnuma.log
?? docs/perf/_raw_r10_6_numa_sweep.log
?? docs/perf/_raw_r10_7_d_vs_f.log
?? docs/perf/_raw_r10_7_tcache_arm.log
?? docs/perf/_raw_r10_7_tcache_isolated.log
?? docs/perf/_raw_r10_7_warm_arm.log
?? docs/perf/_raw_r11_6_after_numa.log
?? docs/perf/_raw_r11_6_baseline_numa.log
?? docs/perf/_raw_r9_9_followup.log
?? docs/perf/_raw_r9_9_followup_run2.log
?? docs/perf/_raw_r9_9_followup_run3.log
```
(All untracked files are generated measurement/CI scratch artifacts from this and prior sessions'
bench/test runs — not real pending changes. The repo tree is otherwise clean and fully committed
through `090e4ff`.)

```
090e4ff fix(warnings): close the under-gated cfg cracks R11-4/R11-6 left in the production build
dc0a673 docs(checkpoints): land Round 11 session checkpoints (in-progress + complete)
8e592f4 docs(changelog): document Round 11 -- batch/NUMA fixes, two closed perf cliffs, three design-only stages
229e25f docs(perf): virgin-zero skip for Small alloc_zeroed -- independent re-verification, CONDITIONAL GO, design-only (R11-8)
c22807d docs(perf): page-run layer design for the 1.25-2 MiB density gap -- CONDITIONAL GO, design-only (R11-7)
```
