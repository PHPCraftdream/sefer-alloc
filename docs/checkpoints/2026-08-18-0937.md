# Checkpoint — 2026-08-18 09:37 [audit-vmem: review-driven round, #1078 in flight]

## Session summary

Продолжение кампании `[audit-vmem]` перед первым релизом `aligned-vmem` 0.2.0. Сессия шла под директивой `/babygoal`: решать задачи через `/crush` в изолированных `git worktree`, коммитить между задачами, по завершении — ревью через агента `@oh` с заведением новых задач по находкам. Директива продублирована `/goal`-хуком, который блокирует остановку до выполнения условия.

Волна прошла плохо и поучительно. Распил `lib.rs` (4656 строк → ~30 файлов, коммит `a4b8e50`), сделанный до этой сессии, был отревьюен, объявлен проверенным «по трём платформенным осям × 5 комбинаций фич» и запушен — с **двумя сломами компиляции**: `bench-internals` без `huge-pages` на 64 битах и полностью несобирающееся `--cfg aligned_vmem_mock` плечо (8 ошибок). `main` покраснел. Дальше каждая починка обнажала следующий слой: `7326b39` (слома) → `613861a` (противоречие `debug_assert` против теста, замаскированное несобираемостью) → `9d1bfc7` (macOS ARM64: `AllocCore::new()` возвращал `None`, ~33 теста, потому что `initial_commit` считался от компильной 4 KiB `PAGE`, а API валидирует по рантаймному `page_size()`) → `5eacf24` (ещё 4 сайта того же класса в `smoke.rs`). Четыре круга.

Параллельно я запустил независимый аудит крейта агентом `@fxx` **без контекста кампании** (не сообщал ни номер волны, ни число прошлых ревью, ни что уже найдено) — 11 находок, все пять значимых закрыты задачами #1066–#1069. Затем — финальное состязательное ревью всей волны (22 коммита, `04d2532..8785e10`) агентом `@oh`, которому я, наоборот, честно изложил историю провала, чтобы он искал **пятый слой**. Он его нашёл: `alloc_core.rs:2416` округлял по компильной `os::PAGE` и кормил этим `commit_range` с рантаймной валидацией — фича `large-reserved-capacity` мертва на 16/64 KiB хостах. Плюс девять других находок; заведены задачи #1077–#1082.

`#1077` закрыт коммитом `a828ebc`. Контрфакт я воспроизвёл лично, и он доказал главное: под откаченным багом **существующий** тест на «тот же указатель» прошёл, упал только новый — то есть прежнее покрытие этот класс не видело.

**Сейчас в полёте:** `#1078` — `vmem-doc-drift-guard` сканирует только `lib.rs`/`Cargo.toml`/`README.md`, тогда как его предмет уехал в `os/unix.rs` (18 вхождений триггеров) и `api/reserve_aligned_huge.rs` (5). Я сам прогонял эту гвардию дважды за сессию и принимал зелёный как доказательство — а под этим зелёным в ту же волну проехало настоящее нарушение её класса. Сессия `wt-1078` в worktree `sefer-alloc-wt-1078`.

Незапушен один коммит (`a828ebc`). CI на `ae948e4` был полностью зелёным, включая macOS — это подтвердило фикс `9d1bfc7` на реальном 16 KiB раннере.

## Active goal

`/goal`-хук активен, условие дословно:

> [audit-vmem] - решли все задачи и поломки с помощью /crush & worktrees, между задачами делай коммиты. Когда все эти задачи будут выполнены - сделай ревью этих задач с помощью агента @oh и заведи новые задачи по ревью с тем же префиксом (если ревью что-то найдет)

babysit-крон активен: `dcc21d11`, `7,22,37,52 * * * *`, session-only.

## TaskList

### in_progress
- #1078 [OH] F2 — `vmem-doc-drift-guard` обезоружен распилом (crush-сессия `wt-1078` жива)
- #657–#661 — пять задач публикации крейтов (не трогались этой сессией)

### pending
- #1079 [OH] F3+F4 — `Reservation::decommit`: safe `pub fn` паникует в debug при rustdoc «no-op»; release-половина контракта не прогоняется нигде
- #1080 [OH] F5+F7 — page-гвардия имеет нулевое покрытие `src/` по существу; тест #1074 пиннит арифметику хелпера, а не сайты вызова
- #1081 [OH] F6+F10 — два bench-internals хука шлют компильный `small_meta_end()` в реальный decommit/recommit; после #1072 это паника, а не тихий пропуск
- #1082 [OH] F8+F9 — цитаты на распиленный `lib.rs` в активных карточках обоих индексов; follow-up про mock-тестовый ряд спрятан внутри закрытого item
- #662, #763 — bench-scale-tool (вне кампании)

### recently completed
- #1077 F1 — пятый слой класса PAGE (`a828ebc`)
- #1062 — ревью волны через `@oh`, заведены #1077–#1082
- #1061 / #1054 — закрытие зонтичной триаж-задачи (`8785e10`)
- #1076 — гвардия против класса PAGE (`4218699`)
- #1073 — протухший `CARGO_MANIFEST_DIR` (`ae21113`, `23626b4`)
- #1074 / #1075 — macOS-блокер (`9d1bfc7`, `5eacf24`)
- #1070 / #1071 / #1072 — три слома волны красноты
- #1064 — независимый аудит `@fxx`

## Decisions

- **Ревью через `@oh` — с полной историей провала, аудит через `@fxx` — без контекста вообще.** Разные цели: аудитор должен смотреть свежим взглядом, ревьюер — знать, где предыдущая верификация оказалась тонкой. Отвергнут единый брифинг для обоих.
- **F9 (hugetlb-пул) закрыт как «только документация», код не тронут.** Success-путь неисполним ни на одной доступной машине (нет hugetlb-пула ни локально, ни в CI), а незамеренная правка горячего пути резервации нарушила бы правила проекта. Отвергнута правка «по очевидности».
- **Гвардия #1076 построена, а не заменена карточкой в индексе.** Класс сбежал трижды; отвергнут вариант «записать и надеяться». При этом честно записано, что она не видит переменных и межфайловых констант — то есть **не поймала бы** дефект #1074.
- **Вопрос расширения macOS-матрицы CI оставлен как рекомендация владельцу, а не выполнен.** Изменение матрицы CI — не территория подрядчика.
- **Правку на 4 строки (#1075) делал сам, не делегировал** — ниже порога, за которым делегирование окупается.

## Open questions

- **Расширять ли macOS-ряд CI.** Из шести сайтов, исправленных #1074, ровно **один** достижим хоть каким-то CI-рядом на хосте со страницей > 4 KiB; остальные пять сидят за фичами, которых macOS-ряды не включают. Значит зелёный `test macos (production)`, которым я закрыл #1065, подтверждает ~1/6 фикса. Предложен один дополнительный шаг стоимостью 2–4 мин. **Решение за владельцем.**
- **Задача #658 (публикация 0.2.0) заблокирована задачами `#842`/`#848`/`#849`, которых не существует** — `TaskGet` их не находит. Формально она заблокирована навсегда. Не трогал: владелец ранее сказал, что о публикации скажет сам.
- **Нужен ли `Reservation::try_decommit`** — семейство безопасных методов не имеет фаллибельного близнеца, на который ссылаются rustdoc свободной функции и README. Проектное решение, вынесено в #1079.

## Repo state

```
(git status --short — пусто, дерево чистое)
```

```
a828ebc fix: task #1077 — OPT-G reserved-capacity grow rounded to compile-time PAGE (5th escape of the page-constant class)
8785e10 docs(vmem): task #1061 — correct item 50's falsified headline (the rejection branch IS tested; the untestable part is narrower)
4218699 build: task #1076 — static page-constant guard for page_size()-validated vmem call sites (npm run check step 37)
23626b4 docs(arch): task #1073 — bump tests/*.rs count to 246 for the new stale-artifact tripwire test
ae21113 build: task #1073 — stale cross-checkout cargo-artifact guards for the root-crate gate rows
```

Незапушено: 1 коммит (`a828ebc`). `origin/main` = `8785e10`.
