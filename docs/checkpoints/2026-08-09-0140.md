# Checkpoint — 2026-08-09 01:40 [crate-by-crate-rust-intel-remediation]

## Session summary

This session is executing a `/babygoal`-driven crate-by-crate sweep of the
5+1 workspace sub-crates' `/rust-intel` audit findings from 2026-08-07,
following the user's explicit instruction: process ONE crate at a time, do
not move to the next until ALL of the current crate's tasks are closed;
implement via `/crush` delegation with commits between tasks; after each
crate finishes, `/checkpoint` + update CHANGELOG.md + commit markdown +
`@oh` review — repeat per crate. The full TaskList (#656-768) was built in
an earlier turn this session with a strict `blockedBy` dependency chain
enforcing crate order: sefer-region → tagged-index-stack → racy-ptr-cell →
a small cross-cutting workspace-`loom`-dependency task (#711) → aligned-vmem
→ numa-shim → size-classes, each crate's fixes gated behind the PREVIOUS
crate's `@oh`-review task, with 4 post-work tasks (checkpoint/changelog/
commit-md/`@oh`-review) per crate.

**sefer-region is fully closed** (13/13 tasks: #694-696 the crate's own
findings, #732-735 its post-work chain, then #769-770 — two NEW follow-up
tasks the round-2 `@oh` closing review itself surfaced: #769 found that
task #694's "order-agnostic" fix was only HALF true (the `drop_count==3`/
`len()==2` exact pair it kept was itself still the drain-order oracle
§D1a filed against — fixed with a genuinely order-free
`drop_count + len() == 5` invariant, personally verified via a real
counterfactual: temporarily moved `bomb_id` to the first and last slot and
confirmed the new assertion holds either way); #770 fixed 3 small
CHANGELOG/comment accuracy residuals the same review found. All 15
sefer-region-round-2 commits (`ea52f85` through `6cb3f6b`) are landed and
personally zero-trust reviewed.

**tagged-index-stack is 4/5 done, #705 IN PROGRESS with uncommitted
changes on disk right now** (interrupted by this `/checkpoint` invocation
mid-verification). Landed and committed: #698 (`1485bb6`, HIGH — fixed
`pop`'s Relaxed CAS-failure ordering, a real concurrency bug; added a loom
test calling the REAL `pop`/`push` end-to-end, personally verified via
counterfactual — reverting to Relaxed makes the new test fail with a real
duplicated-index corruption); #702 (`c552704`, HIGH — repaired the loom
ABA counterfactual, which was panicking on a harness accounting bug
(hardcoded `popped.push(0)` discarding the real popped index) rather than
genuine corruption; redesigned the scenario for real resurrection to be
reachable, and personally found + fixed TWO further bugs in the delegated
draft during review: a flawed assertion in my OWN added verification test
that assumed a fixed scheduling order, and a missing `is_empty` guard that
caused a real out-of-bounds panic under a valid loom interleaving);
#703 (`17185d6`, MEDIUM — promoted `push`'s only corruption guard from
`debug_assert!` to `assert!`; consulted `@oh` for the tradeoff per the
user's "ask @oh, don't ask me" instruction issued mid-session — `@oh`
found the violation is a REAL memory-unsafety path (aliased heap slots)
reachable from 100% safe code, the only real caller is teardown-only (not
hot-path), and the project already made this exact call elsewhere; also
tightened the F1 regression test to pin the panic message substring,
verified non-vacuous via a real counterfactual); #704 (`96e28a7`, INFO —
`raw_head`'s API posture decided as `#[doc(hidden)]`, confirmed zero
production callers workspace-wide, README policy note added).

**#705 (the last tagged-index-stack task, 5 batched INFO/low findings) is
in_progress with 5 uncommitted file changes on disk**: `Cargo.toml` (added
`proptest = "1"` dev-dependency), `README.md` (fixed the tag-budget
~1000x-inflated contrast: corrected "~43 s" to "~12 hours" for the 32-bit
tag comparison at the SAME 100k pushes/sec rate as the 48-bit figure),
`src/lib.rs` (same fix mirrored in the crate-doc paragraph; fixed `pack`'s
doc to say "truncated" not "collides" — the `& INDEX_MASK` mask actually
prevents any bitwise collision; moved `_CHECK_BITS`'s forcing reference
into `INDEX_MASK`'s own initializer so `unpack`/`empty_index`/`is_empty`
force the compile-time width guard too, not just `pack`; added a
"Stability" doc section to the `Links` trait declaring it intentionally
open), plus a NEW file `tests/proptest_pack_unpack.rs` (4 proptest
round-trip properties at widths 1/16/31/32, 64 cases each per this repo's
fast-proptest convention). All 4 new proptest cases pass, full non-loom
suite (13 tests) and loom suite (8 tests) both green, clippy clean in both
default and `--cfg loom` configs, fmt clean. **The one thing NOT yet
resolved**: `cargo doc -p tagged-index-stack --no-deps` emits ONE new
warning — `rustdoc::private_intra_doc_links` on the `_CHECK_BITS`
cross-reference I added to `INDEX_MASK`'s doc comment (`_CHECK_BITS` is a
private associated const, can't be linked to from a doc comment without
`--document-private-items`). This needs a one-line fix (change the
```
[`_CHECK_BITS`](Self::_CHECK_BITS)
```
markdown link to plain non-linked text `` `_CHECK_BITS` ``) before this
task can be committed. Was about to fix this when the user invoked
`/checkpoint`.

## Active goal

No formal `/goal` Stop hook is active. The crate-by-crate sequencing is
enforced structurally via the TaskList's `blockedBy` graph, not a Stop
hook — this is the standing directive from the `/babygoal` invocation
earlier this session and remains in force.

## TaskList

### in_progress
- #656 sefer-region — verify/prepare for crates.io republish (perpetually
  blocked on a maintainer decision, unrelated to the active crate sweep)
- #705 tagged-index-stack: 5 doc/coverage residuals — 4 of 5 sub-fixes
  done and verified, 1 known doc-warning fix remaining before commit

### pending (next up, in strict blockedBy order)
- #736-739 Post-work (tagged-index-stack): checkpoint → changelog →
  commit-md → `@oh` review — #739 unblocks racy-ptr-cell's group
- #700,706-710 racy-ptr-cell fixes (1 HIGH: README's loom-proof claim is
  silently false) — all blocked by #739
- #740-743 Post-work (racy-ptr-cell) — #743 unblocks #711
- #711 Workspace: hoist `loom = "0.7"` into `[workspace.dependencies]` —
  blocked by #743; unblocks aligned-vmem group
- #699,712-719 aligned-vmem fixes (1 HIGH: fault-injection tests run in
  zero CI configs) — all blocked by #711
- #744-747 Post-work (aligned-vmem) — #747 unblocks numa-shim group
- #697,720-727 numa-shim fixes (0 HIGH, but real bugs incl. mbind
  maxnode off-by-one) — all blocked by #747
- #748-751 Post-work (numa-shim) — #751 unblocks size-classes group (LAST)
- #701,728-731 size-classes fixes (0 HIGH, cleanest crate) — blocked by #751
- #752-755 Post-work (size-classes) — #755 is the final task of the
  entire crate-by-crate sweep
- #657-661,756-768 publish-readiness / bench-scale-tool / captrack tasks
  — independently gated behind their crate's final `@oh`-review, NOT part
  of the active sequence itself

### recently completed (this session)
- #704 tagged-index-stack: raw_head → `#[doc(hidden)]` — commit `96e28a7`
- #703 tagged-index-stack: push guard → `assert!` — commit `17185d6`
- #702 tagged-index-stack: loom ABA counterfactual repair — commit `c552704`
- #698 tagged-index-stack: pop's Relaxed CAS-failure fix (HIGH) — commit `1485bb6`
- #770, #769 sefer-region round-2 closing-review follow-ups
- #735, #734, #733, #732 sefer-region round-2 post-work chain
- #696, #695, #694 sefer-region round-2 own findings

## Decisions

- **User: "act independently, don't ask me — you can ask @oh"** (issued
  mid-session, after an `AskUserQuestion` for task #704's raw_head
  decision was interrupted/rejected). This SUPERSEDES the earlier pattern
  of using `AskUserQuestion` for maintainer-style tradeoff decisions —
  going forward, consult `@oh` (or decide independently when reasonably
  confident) instead of asking the user. Applied once already for #703
  (debug_assert vs assert! tradeoff) with a clear, well-justified `@oh`
  recommendation.
- **#703: promote to `assert!`** (not `Result`, not docs-only) — `@oh`'s
  reasoning: real memory-unsafety-shaped violation (aliased heap slots
  reachable from safe code), negligible perf cost (only caller is
  teardown, not hot-path), matches existing project convention for this
  exact debug_assert-vs-assert tradeoff class.
- **#704: `#[doc(hidden)]`** for `raw_head` (user's own choice, before the
  "ask @oh" instruction) over promoting to stable API.
- **#702's redesigned scenario**: B pops TWO indices, re-pushes only the
  first, modeling a live consumer holding a slot — chosen over the
  original single-pop-repush shape because the original is structurally
  incapable of producing real corruption in a 2-slot free list (next[0]
  round-trips to the same value after B's cycle).

## Open questions

- **The `_CHECK_BITS` private-intra-doc-link warning in #705's uncommitted
  work** — needs a one-line fix (unlink the doc reference) before #705 can
  be committed. Immediate next action when work resumes.
- Carried over from earlier sessions, still unresolved: the sefer-region
  0.1.1 version-bump decision (#656's gate), the unexplained `TypeId`
  mismatch from task #692's panic-payload downcast investigation
  (practically worked around), and `docs/reviews/2026-08-05-release-
  readiness-gap-audit.md`'s NO-GO verdict for a 0.3.0 release (summarized
  to the user, no task filed yet).

## Repo state

```
 M Cargo.lock
 M crates/tagged-index-stack/Cargo.toml
 M crates/tagged-index-stack/README.md
 M crates/tagged-index-stack/src/lib.rs
?? crates/tagged-index-stack/tests/proptest_pack_unpack.rs
```

```
96e28a7 docs(tagged-index-stack): decide raw_head's API posture before first publish -- #[doc(hidden)] (task #704)
17185d6 fix(tagged-index-stack): promote push's index-validity guard from debug_assert to assert (task #703)
c552704 test(tagged-index-stack): repair the loom ABA counterfactual harness accounting bug (task #702)
1485bb6 fix(tagged-index-stack): pop's CAS-failure ordering was Relaxed -- retry could read links through a stale head (task #698)
6cb3f6b docs(region): fix 3 small accuracy residuals from the round-2 closing review (task #770)
```
