# Checkpoint — 2026-06-26 08:30 [Phase 12.6 cross-thread reclaim DEFEATED + committed; doc-hygiene done]

## Session summary

`sefer-alloc` — a safe handle-store turned into a real, fast, safe general-purpose
allocator (mimalloc-class drop-in). This session **defeated the campaign's
keystone blocker**: the cross-thread-free *reclaim* crash that had kept Phase 12
on a sound-but-leaky *discard* workaround. The crash was a non-deterministic
corruption (panic `subtract with overflow` at varying lines / `STATUS_ACCESS_VIOLATION`)
that resisted FOUR layered "root causes" — each peeled off by zero-trust
verification: (1) intrusive-word §8 → (2) ring-ABA → (3) a real `SegmentHeader`
data race §11 (fixed, but not THE cause) → (4) the **TRUE root §13**. The true
root was found via **ThreadSanitizer on WSL/Linux** (which proved there is NO
data race in our code) + an in-process free-list audit on a **reliable Linux
repro** (`tests/race_norecycle.rs`): a segment's single bump cursor interleaves
size classes across pages, so `page_map.class_of()` is unreliable; the
cross-thread reclaim derived the class from `page_map` (wrong class → wrong
`block_size` → free-list `next` written mid-block → corruption), while the
own-thread free path uses the correct `Layout`-derived class. **Fix (commit
`255e18c`):** carry the class through the per-segment ring (`offset | class<<22`,
`pack_entry`/`unpack_entry`); `reclaim_offset` unpacks and uses it (never
`page_map`); + removed the redundant eager `drain_thread_free` (reclaim is now
lazy in `find_segment_with_free`, the ShardedRegion 7b discipline) — the eager
drain corrupted under the installed global allocator's real (libtest) workload
while the lazy path handles the identical workload correctly. Verified GREEN on
**both Windows and Linux**: `race_repro` x5, `race_norecycle`, isolated
ring/reclaim unit tests, loom protocol+ring models with counterfactuals, full
Windows suite (0 failed), clippy 0, feature matrix. Then did **doc-hygiene
(#39)**: updated MALLOC_BENCH.md / FINDINGS_PHASE12.md §8 (RESOLVED banner) /
CHANGELOG.md / race_repro.rs header to reflect reclaim-works (these 4 files are
**uncommitted** — awaiting the user's go to commit a small `docs:` commit).
Mid-session the user fixed WSL networking (a DNS sinkhole blackholed rust-lang to
127.x) and installed gdb/valgrind/heaptrack/flamegraph/cargo-fuzz/llvm-tools for
the future heavy gate (#32). Tools/method that cracked it: **TSan (proven tool)
beat the hand-probing**, exactly the user's "use proven tools, don't improvise"
principle. A `/babysit` cron is active (it tick-fires; each tick I report
"awaiting user direction" since the user is actively steering). Worktree
`../sefer-head` was created+removed during a regression bisect (HEAD passed →
proved the single-thread-churn failure was a regression, not pre-existing).

## Active goal

none (the earlier `/goal drive the campaign to victory` was cleared by the user with
`/goal clear`). `/babysit` cron still active (TaskList-driven).

## TaskList

### in_progress
- (none)

### pending — READY (no open blockers)
- #27 Phase 13.3: inlined + arithmetic own-thread free
- #35 M11 epoch-guard for M6 decommit-safety + M6 decommit + soak  (REFRAMED: Variant-2 ring means the freer no longer touches the block, so the old decommit hazard is gone; remaining is owner-internal "drain-before-decommit"; epoch likely unneeded — re-contemplate)

### pending — blocked (Phase 13 chain)
- #28 Phase 13.4 two-list free  (blockedBy #27)
- #29 Phase 13.5 refill tuning  (blockedBy #27)
- #30 Phase 13.6 heap==core pinning  (blockedBy #28, #29)
- #31 Phase 13.7 macro-bench + refresh MALLOC_BENCH  (blockedBy #30)
- #32 Heavy gate: fuzz + aarch64 + TSan  (blockedBy #31; WSL tools now installed)

### recently completed
- #33 cross-thread reclaim fix (the keystone) · #36 defeat ring-reclaim crash · #37 state-machine spec + loom · #38 WSL/TSan enabler (DoH/DNS) · #39 doc-hygiene · #26 13.1 O(1) lookup · #21–#25 Phase 12.1–12.5

## Decisions
- **TRUE root = page_map class unreliable for mixed-class pages** (shared bump cursor interleaves classes). Fix: carry class from the freer's Layout through the ring; do NOT use page_map for cross-thread reclaim class. (Rejected: generation-tag / epoch — the §8 note's guess; rejected: it's a data race — TSan disproved.)
- **Removed eager `drain_thread_free`** in favour of lazy `find_segment_with_free` reclaim. Eager was redundant + corrupted under libtest's real workload; lazy is the proven ShardedRegion discipline. (Honest caveat: the precise eager-vs-lazy timing mechanism was not 100% root-caused; the shipped lazy path is comprehensively verified on the identical workload.)
- **Use the proven tool (TSan on WSL) over hand-probing** — it cracked the heisenbug after manual probes/loom/analysis stalled. Matches the user's "proven tools, don't improvise" principle.
- **Committed the phase at the boundary** (`255e18c`) per CLAUDE.md sanction; doc-hygiene left uncommitted (not a phase boundary — awaiting explicit ask).

## Open questions
- **Next task: #35 (decommit/memory) or #27 (Phase 13 speed)?** — user asked, not yet answered.
- **Commit the doc-hygiene now** (small `docs:` commit) or fold into the next phase commit? — awaiting user.
- Plan has no "Phase 13.2" task between #26 (13.1) and #27 (13.3) — folded/skipped? Offered to create if needed.
- #35 re-contemplation: is crossbeam-epoch even needed for decommit now that the freer never derefs the block? (likely just drain-before-decommit + per-segment live_count).

## Repo state
```
 M CHANGELOG.md
 M docs/FINDINGS_PHASE12.md
 M docs/MALLOC_BENCH.md
 M tests/race_repro.rs
```
(uncommitted = the doc-hygiene #39; everything else committed in 255e18c)
```
255e18c feat(phase12.6): cross-thread-free reclaim — sound + reclaiming (no more discard-leak)
ea3a4ba test+docs: race_repro.rs (counterfactual gate) + confirmed root cause — intrusive TFS word at slot-reuse handoff (#33)
143b8be feat(phase13.1): O(1) size-class lookup — const SIZE2CLASS replaces the per-alloc linear scan
25b78cb docs(race): add §7 — root-cause-first; suspect the shared fallback heap
86e4860 docs: RACE_DRAIN_RECLAIM.md — the open cross-thread-free reclaim UAF + generation-tag guard design (task #33)
```
(NOTE: origin main was last at Phase 11 per earlier checkpoints; the whole Phase 12–13 arc, `8638a65..255e18c`, is LOCAL, NOT pushed.)
