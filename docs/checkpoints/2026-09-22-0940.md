# Checkpoint — 2026-09-22 09:40 [none]

## Session summary

This session is a direct continuation of two prior segments (see
`docs/checkpoints/2026-09-21-2102.md` and `docs/checkpoints/2026-09-22-0015.md`):
under the standing `/babygoal давай реализуем все задачи с помощью /wrush`
contract, I have been solo-implementing every task from four parallel
`/rush --role reviewer` reviews of `alloc_core/`, `registry/`, `global/`,
`concurrent/`, each in its own isolated `git worktree`
(`worktrees/<task-slug>`), with full personal zero-trust verification (the
complete 6-row clippy matrix + `cargo test --features "production internals
bench-internals batch-api"`, plus targeted extra feature combos when a task
touches conditionally-compiled guard logic) before every merge via
`git cherry-pick` onto `main`, then removing the worktree/branch.

**Completed and merged this segment** (since the `2026-09-22-0015.md`
checkpoint, whose only open item — confirming task #1995's backgrounded
verification — I resolved first): #1995 (bind-less cold-arm census, NULL/NO-GO,
merged `3b0ce380`), #2001 (a pre-existing `production`-row clippy break found
mid-#1995-verification: two targets missing `internals` in `required-features`,
merged `587446c5`), #1996 (doc-only NULL — the four `src/global/` `dbg_*`
hooks flagged as inconsistent are already correctly classified in
`tests/dbg_hook_safety_tripwire.rs`'s R30-2 allowlist; CLAUDE.md amended,
merged `9820c513`), #1998 (`AllocCore::pooled_count`/`pool_cap` narrowed
`usize`→`u32`, measured 864→856 bytes via `size_of::<AllocCore>()` A/B
git-stash comparison, merged `7bbdbd57`), #1999 (closed the iai stub gap:
`alloc_zeroed_magazine_{prefill,hit}_only_16b`'s `not(virgin-zero-skip)` arm
was `black_box(0u8)`; gave it a real body, re-measured #1982 against its
parent — 16-hit marginal cost identical 391 Ir before/after, confirming
#1982's own "NO SPEEDUP MEASURED OR CLAIMED" — merged `1f4ac4f3`), #2000
(4-item hot-path duplication cleanup from a #1994 follow-up — landed item 4,
`reclaim_offset` now delegates to `reclaim_offset_checked` with an
always-false predicate, merged `cd25b54b`; landed item 1, extracted shared
`clear_magazine_on_issue`/`bump_gen_on_issue` helpers used at all 6
magazine-issue call sites plus fixed a real double-`segment_base_of_ptr`
computation the review flagged, iai-confirmed byte-identical Ir on the
production path, merged `8078ee10`; items 2+3 split into new task #2002 as
higher-risk/needing-dedicated-care, per #2000's own explicit "land what's
safely completed" instruction).

**#2002 is IN PROGRESS right now, uncommitted, in worktree
`worktrees/unify-refill-dealloc-batch-duplication`** (branch of the same
name, based on `8078ee10`). Both of its two sub-items are functionally
COMPLETE in the worktree (not yet committed, not yet verified clean):

- **Item 2** (`refill_magazine_slow` vs `refill_magazine_slow_virgin`,
  `src/registry/heap_core/alloc/hot.rs`): extracted a shared
  `finish_magazine_refill(&mut self, c, n) -> *mut u8` tail (stamp-dedupe
  loop, `count` assignment, `mark_magazine` loop, hardened `bump_gen`) that
  both call after their own DIFFERENT substrate refill call
  (`refill_class_bump_checked` vs `refill_class_bump_virgin_checked` — these
  remain genuinely different, so full unification of the WHOLE function
  wasn't possible; only the shared tail was extracted). Straightforward,
  low-risk, mechanical.
- **Item 3** (`dealloc_batch_small` vs `dealloc_own_thread_with_base`,
  `src/registry/heap_core/free/dealloc_{batch,own_base}.rs`) — **discovered
  and fixed a REAL, reachable correctness bug while reading the two bodies
  side by side, not just deduplicating style**: `dealloc_batch_small`'s
  hand-copied F7 guard had NO equivalent to `dealloc_own_thread_with_base`'s
  F7 branch (A) (the `medium_promotion_reachable!`-gated legitimate-
  promoted-Large-block routing) — only branch (B)'s simpler `hardened`-only
  no-op. Under `medium-classes` promotion WITHOUT `hardened` (a real,
  reachable feature combo — `batch-api` requires `experimental`+`alloc-core`,
  `medium-classes` requires `alloc-core`, no mutual exclusion), a
  legitimately promoted-and-grown Large block freed via `dealloc_batch`
  would fall straight into the M2 oracles and read/write the Large block's
  own PAYLOAD bytes as fake Small-segment bitmap state — silent memory
  corruption, exactly the class of bug F7 exists to prevent. Fixed by
  extracting the FULL F7(branch A + branch B)/H1/M2-oracle-pair guard chain
  into one shared `small_free_guard(base, ptr, c, layout) -> SmallFreeGuard`
  (a 3-variant enum: `Accept { off }` / `RejectNoOp` / `RouteToLargeFree`),
  self-less (no `&self` needed), `pub(super)` in `dealloc_own_base.rs`,
  called identically from both functions — `dealloc_batch_small` now gets
  branch (A)'s correct routing too, closing the gap as a structural
  consequence of the unification, not a bolted-on separate fix. Full
  guard-by-guard rationale (F7 A/B case split, H1 interior-pointer blind
  spot, M2 oracle order, the RESIDUAL M2 LIMIT/task #164/X7 note) moved into
  `small_free_guard`'s own doc comment (the original ~300 lines of inline
  historical comments were condensed but every distinct technical fact
  preserved, not just deleted). One `#[cfg_attr(..., allow(dead_code))]`
  needed on the `RouteToLargeFree` variant (unconstructed under
  `--all-features`, where `numa-aware` defeats `large-reserved-capacity` —
  mirrors the exact same negated predicate `pack_entry`/`unpack_entry`
  already use in `remote_free_ring/mod.rs`).

**Verified so far for #2002 (both items combined)**: `cargo build`/`cargo
clippy` clean across `production`, `production hardened`,
`production hardened virgin-zero-skip`, `alloc-core alloc-xthread` (no
fastbin), `batch-api medium-classes fastbin alloc-global` (the exact combo
that exercises the fixed bug), `batch-api hardened medium-classes fastbin
alloc-global alloc-decommit` (all guards active), `batch-api production`,
and `--all-features` (after the `RouteToLargeFree` dead-code fix). **NOT yet
run**: the full 6-row clippy matrix + the standard 4-row test suite as ONE
clean pass — an earlier attempt (`biv78swcu`) was launched covering ONLY
item 2, then contaminated mid-run by continuing to edit item 3's files in
the SAME worktree while it was still compiling (a mistake — the log showed
a `no_stale_doc_references` failure that is almost certainly spurious
cross-contamination, not a real regression, but is NOT trustworthy evidence
either way and must be discarded, not diagnosed). A second, clean run
(`.verify_final.sh`, task id `bzuz3jt9w`) was launched AFTER all #2002 edits
settled, covering the full 6-row clippy matrix, the 4 standard test rows,
PLUS two targeted rows added specifically for item 3
(`hardened batch-api medium-classes internals` — exercises the existing
`r11_4_dealloc_batch_hardened_guards.rs` F7-branch-(B)/H1 tests; `batch-api
medium-classes fastbin alloc-global internals` — exercises branch (A), the
newly-fixed path, non-hardened). **This run's outcome is UNKNOWN — it was
still in progress (visibly past the halfway point, `regression_realloc_oob_
old_layout` test running) when a user interruption stopped the blocking
wait, immediately followed by this `/ccheckpoint` invocation.**

## Active goal

None (`/goal` was never invoked this segment).

## TaskList

### in_progress
- #2002 Unify refill_magazine_slow/_virgin and dealloc_batch_small's
  guard-chain duplication (items 2+3 from #2000) — BOTH items functionally
  complete in `worktrees/unify-refill-dealloc-batch-duplication`
  (uncommitted); a clean full-verification run (`bzuz3jt9w`) was in flight,
  outcome unconfirmed, when this checkpoint was requested

### pending
(none — #2002 is the only remaining task; the TaskList will be empty once
it lands, and the `/babysit` cron, job `872482b5` (every 15 min,
`7,22,37,52 * * * *`), will self-delete on its next tick per its own
stop-condition logic)

### recently completed (this session; see summary above for the full list)
- #1995, #2001, #1996, #1998, #1999, #2000 (items 1+4) — all merged to
  `main`, `HEAD` = `8078ee10` as of this checkpoint

## Decisions

- #2002's item 2: did NOT attempt to unify the two refill functions'
  ENTIRE bodies (their substrate calls are genuinely different functions —
  `refill_class_bump_checked` vs `refill_class_bump_virgin_checked`, the
  latter additionally gated on `virgin-zero-skip`) — only extracted the
  byte-identical SHARED TAIL (stamp-dedupe/mark_magazine/count/hardened-bump)
  both call after their own different substrate call. Judged the right
  scope: real, meaningful deduplication (~24 duplicated lines → one shared
  fn) without inventing a false abstraction over two genuinely different
  refill mechanisms.
- #2002's item 3: chose to extract the FULL guard chain (both F7 branches,
  not just the parts that were already duplicated) specifically BECAUSE
  doing so closes the discovered bug as a structural consequence — a
  narrower extraction that preserved `dealloc_batch_small`'s incomplete
  guard would have been faster but would have left the bug unfixed (or
  required a separate, less-well-motivated patch). This matches the
  original task's own prediction: "a guard fixed in one copy and not the
  other is a real correctness hazard" — validated by finding exactly that
  hazard while doing the unification the task called for.
- Condensed (not verbatim-preserved) ~300 lines of inline historical
  rationale (F7/H1/M2/RESIDUAL-M2-LIMIT comments) into `small_free_guard`'s
  doc comment when moving the guard chain — judged this preserves every
  distinct technical fact (case splits, cited task numbers, cited test
  names, the RESIDUAL M2 LIMIT's own sub-history) without the verbatim
  repetition, consistent with how #1993/#2000's own extractions handled
  comment consolidation earlier this session.
- Discarded the first (`biv78swcu`) verification run's log entirely as
  unreliable evidence rather than trying to diagnose its one failure — it
  ran against a moving/contaminated source tree (edited mid-run), so ANY
  result from it, pass or fail, would not be trustworthy. Relaunched a
  second clean run after all edits settled, per this project's zero-trust
  discipline ("do not trust a run against a tree you kept mutating").

## Open questions

- **Did the clean `.verify_final.sh` run (`bzuz3jt9w`) pass?** Unconfirmed
  at checkpoint time — this is the very next thing to check upon resuming:
  read `/tmp/item23_final_verify.log` (Windows path via the Bash tool:
  `D:/system_artefact/Temp/item23_final_verify.log`), confirm `CLIPPY_OK`
  and that the only test failure (if any) across all 6 test rows is the
  known sanctioned `tagged_index_stack_ab_runner_scratch_guard::
  build_check_success_leaves_no_scratch_root` LNK1104 flake (present in
  every worktree run this entire session, absent in the primary checkout).
  Do NOT assume it passed or failed — check the actual log.
- **Is a new regression test warranted for the discovered
  `dealloc_batch_small` F7-branch-(A) bug?** I had identified
  `tests/r11_4_dealloc_batch_hardened_guards.rs` as the natural home (it
  already tests F7 branch (B) and H1 through `dealloc_batch`, but not
  branch (A) — the newly-fixed non-hardened-promotion-Large-via-batch-free
  case) but had not yet written it when interrupted. CLAUDE.md's testing
  discipline ("Every phase is delivered with tests"; zero-trust
  "verify the tests are not vacuous — would they fail without the fix")
  argues for adding one before landing #2002, proving the fix with a
  counterfactual (should fail if `RouteToLargeFree`'s branch-A handling
  were reverted to `dealloc_batch_small`'s old branch-B-only no-op). Not
  yet decided whether to write it before or after confirming the pending
  verification run, but it should happen before #2002 is marked complete.
- Two long-standing unexplained detached worktrees (`worktrees/head-check`,
  `worktrees/sefer-baseline-ae87141e`, both at `ae87141e`) remain untouched,
  flagged in prior checkpoints, still not investigated or asked about.
- A third foreign worktree, `worktrees/src-review-xa-20260920` (detached at
  `e2ab999d`), belongs to a different, independently-observed session that
  committed directly to `main` earlier — left untouched per the standing
  don't-touch-other-agents'-work rule.

## Repo state (from the PRIMARY checkout, D:\dev\rust\sefer-alloc)

```
(clean — git status --short: no output)
```
```
8078ee10 fix(perf): extract the shared magazine-issue clear/bump-gen helpers
cd25b54b fix(perf): collapse reclaim_offset into reclaim_offset_checked with an always-false predicate
1f4ac4f3 bench: give alloc_zeroed_magazine_{prefill,hit}_only_16b a real not(virgin-zero-skip) body
7bbdbd57 perf(runtime): narrow AllocCore's pooled_count/pool_cap from usize to u32
9820c513 docs(config): record that the R30-2 tripwire allowlist supersedes rule 2's single exception
```

**Worktrees present:** `worktrees/head-check` (detached `ae87141e`,
unexplained, untouched), `worktrees/sefer-baseline-ae87141e` (detached
`ae87141e`, unexplained, untouched), `worktrees/src-review-xa-20260920`
(detached `e2ab999d`, foreign session's, untouched), and
**`worktrees/unify-refill-dealloc-batch-duplication`** (branch of the same
name, based on `8078ee10`, **contains #2002's fully-implemented-but-
uncommitted work** — items 2 and 3 both done in `src/registry/heap_core/
alloc/hot.rs` + `src/registry/heap_core/free/dealloc_batch.rs` +
`src/registry/heap_core/free/dealloc_own_base.rs`, plus two scratch
verification scripts, `.verify_item2.sh` (already deleted) and
`.verify_final.sh` (still present, not yet committed — delete before
committing, matches this session's established pattern of not committing
scratch `.verify*.sh` helper scripts)). This worktree is NOT yet ready to
remove — it holds real, unlanded, unverified work.

**Not pushed** to `origin` this segment.

**Active babysit cron:** job `872482b5`, every 15 minutes
(`7,22,37,52 * * * *`), session-only. Ticks so far have all reported "still
running #<id>" — no stall recovery has been needed.
