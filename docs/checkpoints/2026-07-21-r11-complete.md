# Checkpoint — 2026-07-21 [r11-complete]

## Session summary
This session picked up mid-CHANGELOG-backfill and Round 10 execution (see the prior checkpoint
`2026-07-21-r11-in-progress.md` for that earlier history), then executed the entire Round 11
follow-up queue (#244–#251, R11-1 through R11-8) end to end — implementing tasks via `/crush`
delegation with mandatory zero-trust review, commits, and tests between every task, per the
session's standing directive. All 8 tasks landed as 8 separate commits (`33581bd`..`229e25f`),
followed by a ninth commit documenting the whole round in `CHANGELOG.md` (`8e592f4`).

R11-1 fixed a real M2 double-issue defect in `HeapCore::alloc_batch` (a magazine-drain bit-clear
timing bug plus a copy-pasted predicate shortcut whose justifying invariant didn't hold in the
new context — the second half was found by the orchestrator's own independent re-analysis, not
stated in the source review). R11-2 fixed two pre-existing gaps in `HeapOverflow`'s cross-thread
drain (missing directory sync, missing pool/release finalization). R11-3 was a design-only stage
(realloc-aware Small→Large promotion for `medium-classes`, CONDITIONAL GO, no code shipped).
R11-4 batch-optimized `dealloc_batch`; zero-trust review caught a real gap before landing (missing
`hardened`-only F7/H1 guards that could corrupt a Large block's payload under a caller-contract
violation) and a layout-reconstruction bug, both fixed and proven red-before/green-after. R11-5
cached `numa::current_node()`, measured ~233×/~396× on this host. R11-6 implemented R10-6's
already-designed node-indexed NUMA directory, closing a 140× scan cliff down to 112× speedup at
S=1023; zero-trust review caught a genuinely vacuous headline test (passed even against a
deliberately broken bucket-scan order, because `alloc_small`'s `small_cur` fast path pre-empted
the code under test) and fixed it with a bypass test hook. R11-7 was a design-only stage for the
largest structural change in the queue (a new `SegmentKind` "page-run layer" for the 1.25–2 MiB
medium-classes-wide density gap), CONDITIONAL GO with an explicit warning that the true design
surface is 2-3× larger than initially framed. R11-8 was a design-only stage for a
correctness-critical virgin-zero-skip optimization; discovered mid-task that an earlier session
had already produced a complete design doc (R9-5) for the exact same question — rather than
duplicate or blindly trust it, independently re-verified R9-5's conclusions from the current tree
and reached the same CONDITIONAL GO verdict, documenting the reconciliation honestly.

Two `/crush` sessions hit transient timeouts mid-task (R11-5, R11-6) and were successfully resumed
into the same `--session` id per the established protocol. One `/crush` session (R11-6's own
follow-up fix for the vacuous-test finding) hit `/crush`'s weekly/monthly quota exhaustion
(resets 2026-07-23) partway through — per the user's explicit standing instruction ("when /crush
runs out of limits, switch to @sh agents"), the orchestrator switched to a local `@sh` Agent for
the rest of R11-6, and used `@sh` exclusively for R11-7 and R11-8 as well (crush remained
quota-exhausted for the rest of the session). Earlier in the session, `/crush` was also avoided
during a provider peak-hours window (zai, 08:00–12:00) per the same fallback rule — a one-shot
12:00 alarm (`CronCreate`) was set and fired correctly, after which normal `/crush` usage resumed
for R11-5 and the first attempt at R11-6, before the quota exhaustion forced the permanent
switch to `@sh` for the remainder.

Standing constraints honored throughout: never bump versions, never push (local `main` is now 9
commits ahead of the last known state — `1eacbd1` through `8e592f4` — still unpushed), zero-trust
review before every commit (full diff read personally, tests independently re-run, and in three
cases — R11-1, R11-4, R11-6 — a genuine RED-before/GREEN-after counterfactual reproduced
personally via temporary source reverts, not merely trusting the delegate's own claim), respond
in Russian, commit-between-phases pre-authorized per CLAUDE.md. The `/babysit` cron (`c0c6b019`)
was re-armed at the start of Round 11 and self-deleted (via its own tick logic) once the TaskList
emptied at the end of this session — confirmed via `CronList`/`CronDelete` at the final babysit
tick.

## Active goal
No `/goal` Stop hook is currently confirmed armed in this session's visible state. Earlier in the
session a Stop-hook-style directive was in force ("реализуй задачи с помощью /crush, между
задачами делай коммиты, покрой их тестами. Если /crush войдет в пиковые часы, то переключайся на
агентов @sh") — this has been fully satisfied (all 8 Round 11 tasks implemented, committed, and
tested; crush fallback to `@sh` applied both for peak-hours and for the later quota exhaustion).
Whether the underlying Stop-hook mechanism is still technically armed is unclear from this
session's own state — if the user re-invokes work expecting it, it should not block since its
condition is now met.

## TaskList
Empty. All Round 11 tasks (#244–#251) completed; the babysit tick's own stop condition fired and
deleted its cron job (`c0c6b019`) at the end of this session.

## Decisions
- Applied the same design-only two-stage gate (design doc first, mandatory review, prototype only
  on separate future authorization) established for R10-4 to R11-3, R11-7, and R11-8 — all three
  correctness/architecture-sensitive, open-ended optimization ideas in the Round 11 queue.
- For R11-4/R11-6/R11-1 (bounded, well-specified batch-api or already-designed work), used direct
  implementation with mandatory red-before/green-after tests instead of a separate design stage,
  matching the precedent R10-7 set earlier in the session for the same `batch-api` surface.
- When R11-6's own follow-up crush session hit `/crush`'s weekly/monthly quota wall mid-task
  (a hard, non-recoverable-by-retry limit, confirmed via the error message's exact reset
  timestamp), switched to a local `@sh` Agent immediately per the user's explicit standing
  instruction, rather than waiting or retrying — and used `@sh` for all subsequent tasks (R11-7,
  R11-8) since the quota window did not reopen before session end.
- Upon discovering R11-8's exact topic already had a complete, committed design doc from an
  earlier session (R9-5), chose to independently re-verify rather than either (a) silently
  duplicate the work without acknowledging the overlap, or (b) blindly trust R9-5's account
  without re-checking it against the current (since-moved) source tree — documented the
  reconciliation honestly in both the design doc and the CHANGELOG entry.
- Delegated the Round 11 CHANGELOG section to `/crush`... actually to `@sh` (crush was
  quota-exhausted), matching the established methodology from all five prior CHANGELOG
  sections in this session (pre-verified facts baked into the prompt from the orchestrator's own
  first-hand authorship/review of every commit, rather than trusting the sub-agent to re-derive
  numbers from scratch).

## Open questions
- None currently pending from the user. The standing convention (`npm run check` + push to
  `origin/main` only on explicit request) remains unexercised — 9 commits are sitting locally
  unpushed as of this checkpoint.
- Deferred, not currently blocking: R11-3's, R11-7's, and R11-8's stage-2 prototype sessions are
  all explicitly NOT authorized by this session's design-only verdicts — a future session would
  need explicit user sign-off to proceed past design review for any of them, per this project's
  established mandatory gate.

## Repo state
```
?? _gad_suite.log
?? docs/checkpoints/2026-07-21-r11-in-progress.md
?? docs/perf/_raw_baseline_off.log
?? docs/perf/_raw_baseline_off_reduced.log
?? docs/perf/_raw_criterion_medium.log
?? docs/perf/_raw_criterion_production.log
?? docs/perf/_raw_firstalloc_medium.log
?? docs/perf/_raw_firstalloc_production.log
?? docs/perf/_raw_iai_medium.log
?? docs/perf/_raw_iai_production.log
?? docs/perf/_raw_medium_on.log
?? docs/perf/_raw_medium_on_reduced.log
?? docs/perf/_raw_r10_6_dir_off_nonnuma.log
?? docs/perf/_raw_r10_6_dir_on_nonnuma.log
?? docs/perf/_raw_r10_6_numa_sweep.log
?? docs/perf/_raw_r10_7_d_vs_f.log
?? docs/perf/_raw_r10_7_tcache_arm.log
?? docs/perf/_raw_r10_7_tcache_isolated.log
?? docs/perf/_raw_r10_7_warm_arm.log
?? docs/perf/_raw_r11_6_after_numa.log
?? docs/perf/_raw_r11_6_baseline_numa.log
?? docs/perf/_raw_r9_9_followup.log
?? docs/perf/_raw_r9_9_followup_run2.log
?? docs/perf/_raw_r9_9_followup_run3.log
```
(All untracked files are generated measurement/CI scratch artifacts from this and prior sessions'
bench/test runs — not real pending changes. The repo tree is otherwise clean; `docs/checkpoints/
2026-07-21-r11-in-progress.md` is an earlier checkpoint from this same session, itself never
committed.)

```
8e592f4 docs(changelog): document Round 11 -- batch/NUMA fixes, two closed perf cliffs, three design-only stages
229e25f docs(perf): virgin-zero skip for Small alloc_zeroed -- independent re-verification, CONDITIONAL GO, design-only (R11-8)
c22807d docs(perf): page-run layer design for the 1.25-2 MiB density gap -- CONDITIONAL GO, design-only (R11-7)
89865ae perf(numa): node-indexed segment directory closes the 140x scan cliff (R11-6)
9b48844 perf(numa): cache current_node() to cut per-call syscall overhead (R11-5)
```
