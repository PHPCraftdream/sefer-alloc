# Checkpoint — 2026-07-12 10:30

## Session summary

This session finished the `/babygoal`-driven RAD/UBFIX implementation arc (19 tasks: RAD-1..5, RAD-4b, UBFIX-1..13), then moved into pushing and stabilizing CI, and ended with a comparative benchmark run.

**RAD/UBFIX arc (all landed, all zero-trust reviewed personally by the orchestrator before commit):** RAD-5 (`8604d4a`) — MagazineBitmap, O(1) magazine-residency probe replacing O(count) scans; GO verdict, +9 raw Ir on churn benches (well inside ±10 gate), independently re-measured via stash/pop baseline comparison, byte-identical to the implementing agent's own numbers. UBFIX-13 (`06c04ba` + `68f323b`, split across two commits because part of it — `alloc_core.rs`'s L-9b guards — was entangled with RAD-4b's concurrent edit to the same file). RAD-4b (`f74b4ed`) — `HeapOverflow`, a slot-resident second-chance MPSC overflow ring closing RAD-4's owner-starved residual to exactly zero (`exhausted_delta == 0`); RED→GREEN counterfactual personally reproduced (744/1000 lost blocks pre-fix, matching the historical measurement exactly). UBFIX-8 (`07dc2a0`) — M-4 finding resolved as UNREACHABLE via a personally-reverified call-graph trace (not just trusted from the agent), pinned with a `debug_assert!` using RAD-5's bitmap probe, no logic change.

**Push + CI stabilization (the bulk of the back half of this session):** Pushed 26 commits. Found and fixed a genuine regression pre-push: `tests/race_norecycle.rs` was livelocking (RAD-4's spin-retry paying its full 262,144-attempt budget on every cross-thread free into an owner-less segment's saturated ring) — fixed by gating the spin on a new `HeapCore::owner_slot_is_live` probe (`aa4ccf9`). Then hit CI job failures ("The runner has received a shutdown signal") on specific miri jobs. Initially misdiagnosed as a job-count/concurrency problem and consolidated the CI matrix (54 → 28 jobs across two commits, `a1c4886` + `d6ad7f2`, also fixing a genuinely dead `region_invariants` miri job that had been silently running zero tests via a bad filter string) — this consolidation is real, useful, and stays, but did NOT fix the actual failures. Root-caused for real by reading full job logs (at the user's explicit insistence — "не нужно по кругу гонять то, что падает, нужно улучшать понимание"): RAD-4b's `HeapOverflow` field is unconditionally materialized on every `HeapSlot` (`[AtomicUsize; 2048] + [AtomicU32; 2048]` = 24 KiB/slot × `MAX_HEAPS`(4096) = 96 MiB added to the Registry). Free on native (RAD-1's lazy-touch discipline — virtual, never resident), but miri has no lazy-paging concept — its Stacked/Tree-Borrows tracking eagerly materializes metadata proportional to the FULL allocation, driving miri's own process to ~11.8 GiB RSS (confirmed by directly checking `tasklist` memory during a local repro) and OOM-killing the CI runner 70-90s into whichever miri test happened to be running. Fixed (`ad78422`) by shrinking `HEAP_OVERFLOW_CAP` to 64 under `cfg(miri)` (native keeps 2048) — RSS dropped to ~700 MiB locally, all previously-failing tests now pass. Also shrank test-body N/write-window under `cfg(miri)` in two test files as a secondary optimization. **CI is now fully green** (28/28 jobs, confirmed via `gh run view`), Kani green too.

Ended with `npm run bench:table` (SeferAlloc vs mimalloc vs System, production build): SeferAlloc beats mimalloc on churn workloads (up to 10.46× faster at 1024B with reuse) but loses on cold-direct/no-reuse allocation (1.6-2.4× slower except at 1024B) — consistent with this session's whole optimization focus being the churn/magazine hot path (RAD-5, RAD-4b). Both crush System allocator almost everywhere.

No active `/babysit` or `/goal` — the babysit cron self-deleted once the TaskList emptied (all 19 tasks completed). No outstanding uncommitted work; the two untracked doc files (`docs/agent_reports/`, `docs/reviews/2026-07-10-ub-audit-final-synthesis.md`) are pre-existing background artifacts from earlier in the session, never requested to be committed.

## Active goal

None — no `/goal` Stop hook is currently armed.

## TaskList

Empty. All 19 RAD/UBFIX tasks (#54-#72) reached `completed` earlier this session; the babysit cron observed the empty list and self-deleted per its own protocol.

## Decisions

- **RAD-5 GO** over NO-GO: measured independently, both churn (+9 Ir, inside gate) and cold/recycle (in the 15-25 Ir/op honest budget) criteria met — see `docs/perf/IAI_BASELINE.md`'s "RAD-5 GO" section.
- **RAD-4b's `HeapOverflow` design** chosen over 3 rejected alternatives (blocking `dealloc`, provenance-exposed `SegmentHeader` field, retagging `next_abandoned`) — see `docs/perf/IAI_BASELINE.md`'s "RAD-4b GO" section for the full comparison.
- **UBFIX-8 resolved as "prove unreachable, pin with debug_assert"** rather than forcing a code change — the reachability argument was independently re-derived and verified, not just trusted.
- **CI consolidation (54→28 jobs) kept despite not being the root fix** — it's a real, independently-useful improvement (less fixed per-job overhead, one dead job fixed) and was explicitly requested by the user separately from the actual bug hunt.
- **Rejected the "too many CI jobs" theory** once full job logs showed the same 2-3 specific miri jobs failing consistently regardless of consolidation level, and once a historical pre-session CI run proved those exact tests passed cleanly before — redirected to a memory-based root cause instead of continuing to blindly retry.

## Open questions

None outstanding. The user's last request (comparative benchmark table) was delivered in full in the final message of this session.

## Repo state

```
?? docs/agent_reports/
?? docs/reviews/2026-07-10-ub-audit-final-synthesis.md
```

```
ad78422 fix(registry): shrink HEAP_OVERFLOW_CAP under miri (12 GiB -> ~700 MiB RSS)
d6ad7f2 ci: consolidate the test feature-matrix too (13 -> 4 jobs)
a1c4886 ci: consolidate matrix jobs to cut per-push job count (~53 -> 37)
aa4ccf9 fix(registry): gate RAD-4's overflow-retry spin on owner-slot liveness
07dc2a0 fix(alloc-core): pin M-4's unreachable magazine double-issue hazard
```

Branch `main` is up to date with `origin/main` (last push `ad78422`, confirmed CI green).
