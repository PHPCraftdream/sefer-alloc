# Checkpoint — 2026-08-09 03:30 [crate-by-crate-rust-intel-remediation]

## Session summary

This session is executing a `/babygoal`-driven crate-by-crate sweep of the
5+1 workspace sub-crates' `/rust-intel` audit findings from 2026-08-07,
following the user's explicit instruction: process ONE crate at a time, do
not move to the next until ALL of the current crate's tasks are closed;
implement via `/crush` delegation with commits between tasks; after each
crate finishes, `/checkpoint` + update CHANGELOG.md + commit markdown +
`@oh` review — repeat per crate. The full TaskList enforces a strict
`blockedBy` chain across crates: sefer-region → tagged-index-stack →
racy-ptr-cell → a small cross-cutting workspace-`loom`-dependency task
(#711) → aligned-vmem → numa-shim → size-classes.

**sefer-region is fully closed** (15 commits `ea52f85`..`6cb3f6b`).

**tagged-index-stack is now GENUINELY fully closed**, including a real
defect the round's own closing review caught. Landed: #698 (HIGH, `pop`'s
CAS-failure ordering Relaxed→Acquire), #702 (HIGH, loom ABA counterfactual
harness repair), #703 (`push` guard debug_assert→assert), #704 (`raw_head`
→ `#[doc(hidden)]`), #705 (5 doc/coverage residuals). The `@oh` closing
review (`docs/reviews/2026-08-09-tagged-index-stack-round-closing-review.md`)
then found **F1 (HIGH): #702's own audit finding was not actually
closed** — the untagged ABA counterfactual's oracle (`!popped.contains(&1)`)
was itself schedule-dependent and panicked on loom's very first, entirely
benign interleaving, so the genuine ABA-corrupting interleaving it claimed
to prove was never reached. Personally reproduced this in the review
(iteration 0 = spurious panic; iteration 15 = genuine corruption, only
found by substituting the companion test's own oracle). Fixed in task
#771 (commit `3c69a21`) by porting the companion test's scheduling-
independent `accounted`/`dedup` conservation invariant into the untagged
counterfactual — personally re-verified it still panics, but now on a
genuine duplicate rather than a benign schedule. Ten more LOW/INFO
findings (F2-F11: CHANGELOG wording errors, an undocumented 3rd
counterfactual, two missing CI steps — release-profile test run and a
bare-metal `x86_64-unknown-none` build — a doc overclaim about
`_CHECK_BITS`, a one-sided conservation oracle, and assorted hygiene
comments) were closed in task #772 (commit `7d8aab0`, SHA-citation
follow-up `c53b98c`). Both new CI steps were verified locally to actually
compile/pass before being added to `ci.yml`. Full verification after
every change: non-loom suite (18 tests, +1 new pack-truncation test),
release-profile suite, loom suite (8/8, all `#[should_panic]`
counterfactuals verified to fire for the RIGHT reason), clippy clean in
both default and `--cfg loom` configs, fmt clean, `cargo doc --no-deps`
clean, `ci.yml` YAML syntax validated, doc-consistency gate (13 tests)
green, `scripts/verify-commit-prefixes.mjs` PASS.

**racy-ptr-cell's fix-task group (#700, #706-710) is now UNBLOCKED** — the
tagged-index-stack post-work chain (#736-739) plus the two review-follow-up
tasks (#771-772) are all `completed`. This is the natural next crate to
start, per the standing one-crate-at-a-time instruction. Not yet started
this session.

## Active goal

No formal `/goal` Stop hook is active. Crate-by-crate sequencing is
enforced structurally via the TaskList's `blockedBy` graph.

## TaskList

### in_progress
- #656 sefer-region — verify/prepare for crates.io republish (perpetually
  blocked on a maintainer decision, unrelated to the active crate sweep)

### pending (next up, in strict blockedBy order)
- #700,706-710 racy-ptr-cell fixes (1 HIGH: README's loom-proof claim is
  silently false) — UNBLOCKED, ready to start
- #740-743 Post-work (racy-ptr-cell) — #743 unblocks #711
- #711 Workspace: hoist `loom = "0.7"` into `[workspace.dependencies]` —
  blocked by #743; unblocks aligned-vmem group
- #699,712-719 aligned-vmem fixes (1 HIGH) — blocked by #711
- #744-747 Post-work (aligned-vmem) — #747 unblocks numa-shim group
- #697,720-727 numa-shim fixes (0 HIGH) — blocked by #747
- #748-751 Post-work (numa-shim) — #751 unblocks size-classes group (LAST)
- #701,728-731 size-classes fixes (0 HIGH) — blocked by #751
- #752-755 Post-work (size-classes) — #755 is the final task of the
  entire crate-by-crate sweep
- #657-661,756-768 publish-readiness / bench-scale-tool / captrack tasks
  — independently gated behind each crate's final `@oh`-review

### recently completed (this session)
- #772 tagged-index-stack: F2-F11 bundle — commits `7d8aab0`, `c53b98c`
- #771 tagged-index-stack: F1 (HIGH) — commit `3c69a21`
- #739,738,737,736 tagged-index-stack post-work chain
- #705,704,703,702,698 tagged-index-stack own fix tasks
- #769,770 sefer-region round-2 closing-review follow-ups

## Decisions

- **User: "act independently, don't ask me — you can ask @oh"** — standing
  instruction for the rest of this session.
- **#771's fix**: ported the companion test's scheduling-independent
  conservation oracle verbatim rather than inventing a new pattern —
  matches the review's own suggested close exactly.
- **#772's F7**: strengthened BOTH the untagged and tagged conservation
  oracles from duplication-only (`dedup()` length) to `assert_eq!(sorted,
  vec![0, 1])` (catches loss too) — the review only named the tagged
  companion test explicitly, but the same class of weakness existed in
  #771's newly-ported oracle, so both were fixed for consistency.
- **#772's F5**: added a genuine new CI step (`x86_64-unknown-none`
  bare-metal build) rather than deferring it — verified feasible locally
  first (installed the target, confirmed clean compile) before committing
  to the CI change.
- **This crate's round-closing review found a real defect** (F1) despite
  the round's own #702 already claiming to fix the exact same audit
  finding — demonstrates why the "review after commit, fix before moving
  crates" discipline this session follows is load-bearing, not
  ceremonial.

## Open questions

None new this checkpoint. Carried over: the sefer-region 0.1.1
version-bump decision (#656's gate), the unexplained `TypeId` mismatch
from task #692 (practically worked around), and `docs/reviews/2026-08-05-
release-readiness-gap-audit.md`'s NO-GO verdict for a 0.3.0 release
(summarized to the user, no task filed yet).

## Repo state

```
(clean — nothing uncommitted)
```

```
c53b98c docs: fill task #772's commit SHA citation in CHANGELOG.md (was a TBD placeholder)
7d8aab0 docs,test,ci(tagged-index-stack): close 10 lower-severity residuals from the round-closing review (task #772, findings F2-F11)
3c69a21 test(tagged-index-stack): fix the untagged ABA counterfactual's schedule-dependent oracle (task #771, closes #702's audit finding for real)
7a993ce docs: commit the two tagged-index-stack round checkpoints (task #738)
28686dc docs: update CHANGELOG.md with the tagged-index-stack rust-intel round (tasks #698,702-705)
```
