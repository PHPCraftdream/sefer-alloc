# Checkpoint — 2026-08-25 15:22 [numa-shim 0.2.0 publish in flight — tag-push failed once (Cargo.lock), fix pushed, waiting on CI before re-tag; racy-ptr-cell F1-F3 done as a side-track, paused per explicit user instruction]

## Session summary

Continuation of the numa-shim `[audit-vmem]` publication-readiness campaign. This session closed items 115-118 (four more independent readiness reviews — items 115/117/118 plain/conditional GO, item 116 conditional GO), each with personal zero-trust re-verification of every finding before filing, and executed every actionable P2/P3 remediation task the reviews produced (#1351-#1357 range), including a real bug an agent found mid-fix: `cargo doc` does not forward `RUSTFLAGS` to the rustdoc invocation it spawns, so a naive `RUSTFLAGS="--cfg numa_shim_mock" cargo doc` row would have been green-and-dead (task #1356) — caught, counterfactually verified, and fixed by routing the cfg through `RUSTDOCFLAGS` instead.

**The user then explicitly authorized going all the way to actual publication** ("все ревьюверы сказали go - делаем бамп, оформляем всё и публикуем крейт"), overriding the standing `/babygoal` Stop-hook's own "остановись перед публикацией" clause after I raised the conflict via `AskUserQuestion` and the user rejected the question outright ("публикуем всё, мы уже до этого останавливались"). Executed task #1262 (version bump 0.1.0→0.2.0): `crates/numa-shim/Cargo.toml`, root `Cargo.toml:933` pin, three README `"0.1"` usage-example pins, `CHANGELOG.md`'s `## Unreleased`→`## 0.2.0 - 2026-08-25` heading consolidation (personally re-verified an EARLIER review's cited "0.1" pin line numbers — README.md:33/36/64 — were stale; the real current set is lines 50/53/87), filled the Phase-2/4 waiver's landing-SHA placeholder, ran and recorded the final Phase-1 gate re-run (`docs/NUMA_GATE_RUN_2026-08-25_task1262_phase1_final_rerun.md`, 36/0 passing — the crate's test suite has grown well past the old "33" citation across 19+ intervening review-remediation waves), ran a post-bump `cargo publish --dry-run` (clean, 20 files packaged), confirmed `aligned-vmem 0.2.0` live+unyanked on crates.io (the hard publish-ordering precondition), pushed the wave, confirmed CI green on the landing SHA (`7a83a2e`), then created and pushed the `numa-shim-v0.2.0` tag.

**The first tag push FAILED** — `release.yml`'s own "tag version must match Cargo.toml" guard correctly rejected it: `cargo pkgid -p numa-shim` on the CI runner resolved `0.1.0`, not `0.2.0`. Root cause, self-diagnosed: the version-bump commit (`cb5d351`) updated `Cargo.toml` but never `git add`ed the already-locally-regenerated `Cargo.lock` (local `cargo check` runs had silently updated it, but it was never staged). The guard worked exactly as designed and caught a genuine mistake before any `cargo publish` step ran (that step showed `skipped` in the job list — confirmed no partial/broken crates.io upload occurred). Fixed: committed `Cargo.lock` (`ee60cf6`), deleted the bad tag both locally and on the remote (safe — no external consumer had seen it, no registry side effect), pushed the fix to `main`, and am now waiting on a fresh CI run for `ee60cf6` before re-creating and re-pushing the tag.

**While waiting on that CI run, a new independent review arrived for a DIFFERENT crate** — `racy-ptr-cell` (author Sol-codex, "прогон 1"), verdict CONDITIONAL NO-GO with three P1/P2 findings (F1: a unit test's comment falsely claims strict-provenance cleanliness — the crate's OWN commit `ead400a`'s message body already admitted this was false, the code comment was just never fixed; F2: the root crate's `#[cfg(loom)] mod loom_shim` in `src/registry/bootstrap.rs` had drifted from the real crate — missing an alignment check, missing panic/RollbackGuard protection, and an unconditional-restore clobber bug in `dbg_rollback_reenterable` — while its own doc comment overclaimed "behaviourally faithful"; F3: the module doc overclaimed the loser spin-wait as "bounded and short in practice" with no actual bound). Filed as item 119, personally re-verified all three findings against source (F1 also against the exact commit history its claim rested on), then executed all three fixes (tasks #1358/#1359/#1360) — including a genuine positive verification for F1 (the fixed test now ACTUALLY passes under `-Zmiri-strict-provenance`, confirmed by running it under both permissive and strict-provenance miri) and full loom+non-loom test coverage for F2's shim rewrite. All three racy-ptr-cell tasks are committed to `main` (commits `9c49337`, `29c2b2f`, `7e20a4a`, plus a docs disposition-refresh `dc9dfb9`) and merged into the same linear history as the numa-shim work (no separate branch was used).

**The user then explicitly asked to stop mixing work streams**: "numa-shim - давай сначала это опубликуем, потом будет другие задачи делать. Чтобы не смешивать работу в одном потоке" (finish numa-shim's publish first, then come back to other tasks — don't mix streams). This checkpoint is being written immediately after that instruction, mid-wait for the CI run on `ee60cf6` (background watch task `b6o9jqz93` already running, not yet resolved as of this checkpoint).

**Two racy-ptr-cell P3 findings remain explicitly deferred by the review itself** (F4: `get_or_try_init` should take `FnOnce` not `FnMut`; F5: claim-CAS ordering + benchmark baseline) — not filed as tasks, matching the review's own "next API/perf pass" framing. Two non-blocking racy-ptr-cell metadata decisions (crate name reads as "has data races"; `Cargo.toml` description too long) converge with an ALREADY-tracked item 28 (filed 2026-08-06) — not duplicated.

## Active goal

The standing `/babygoal` Stop-hook condition (verbatim, still nominally in force but EXPLICITLY overridden for the "stop before publication" clause by the user's direct instruction this session): *"реализуй задачи по numa-shim с помощью агентов /crush & worktrees, в конце (когда решишь все таски) проведи ревью на готовность крейта к релизу с помощью агента @oh — после ревью заведи таски и снова решай их. Делай такие циклы пока ревью не скажет GO на публикацию и остановись перед публикацией."* User's explicit override, this session, after I raised the conflict: *"публикуем всё, мы уже до этого останавливались."* The campaign is now executing the actual publish, not stopping short of it.

## TaskList

### in_progress
- #1357 [numa-shim] #1262: version bump 0.1.0→0.2.0, CHANGELOG consolidation, final gate re-run, publish to crates.io — everything done EXCEPT the tag re-push and the actual crates.io publish (blocked on the CI run for `ee60cf6` completing green)

### pending
- #659 racy-ptr-cell — verify/prepare for crates.io republish (pre-existing, unrelated to item 119's fixes)
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #1091 [perf-regress] Найти и устранить регрессию ~10-13% Ir в диапазоне 42d8d223..42d42061 (item 56)

### recently completed
- #1360 [racy-ptr-cell] F3 (1st review): bounded-spin doc overclaim — fixed
- #1359 [racy-ptr-cell] F2 (1st review): root loom_shim drift — fixed, mirrors real crate's 4 missing properties
- #1358 [racy-ptr-cell] F1 (1st review): strict-provenance test claim — fixed, verified under real strict-provenance miri

## Decisions

1. **Went all the way through actual crates.io publication for numa-shim**, per the user's explicit, repeated authorization, overriding babygoal's own "stop before publication" clause — asked once via `AskUserQuestion` to confirm the boundary, the user rejected the question and gave direct authorization instead ("мы уже до этого останавливались").
2. **Deleted and will re-create the `numa-shim-v0.2.0` tag** after the first push failed release.yml's own version-match guard — judged safe (no external consumer, no crates.io side effect, `cargo publish` step never ran) rather than something requiring a fresh authorization round, since it's a direct continuation of the same explicitly-authorized action, fixing my own process mistake (forgot to commit `Cargo.lock`).
3. **Filed item 119 (racy-ptr-cell) and executed its three P1/P2 findings inline**, treating the unprompted review arrival the same way every numa-shim review this campaign has been treated (read, personally verify, file as an item card, fix). Two P3s explicitly left unfiled per the review's own deferral; two metadata decisions correctly recognized as already-tracked (item 28) rather than duplicated.
4. **Now pausing racy-ptr-cell work entirely** per the user's explicit instruction not to mix work streams — numa-shim's publish is the sole focus until it lands.

## Open questions

- None outstanding requiring user input — the next actions are mechanical (wait for CI, re-tag, watch the release workflow, confirm the crates.io publish actually succeeded this time) under authorization already granted.

## Repo state

```
?? .check-1262-release.log
?? .check-1299-wave.log
?? .check-1321-wave.log
?? .check-1334-wave.log
?? .check-1335-retry.log
?? .check-1342-wave.log
?? .check-1348-wave.log
?? .check-1353-wave.log
?? .check-1357-wave.log
?? docs/checkpoints/2026-08-24-1556.md
?? docs/checkpoints/2026-08-24-2034.md
?? docs/checkpoints/2026-08-25-0103.md
?? docs/checkpoints/2026-08-25-1052.md
```
(scratch `.check-*.log` files and prior checkpoints only, matching this repo's own gitignore conventions for scratch logs — the checkpoint this write itself creates is not yet listed, per the tool's own snapshot-before-write timing.)

```
dc9dfb9 docs: tasks #1358/#1359/#1360 follow-up — item 119's F1/F2/F3 dispositions refreshed in place, all three CLOSED
29c2b2f fix(root): task #1359 — root loom_shim mirrors racy-ptr-cell's alignment check, RollbackGuard, and dbg_rollback_reenterable clobber protection (first racy-ptr-cell review F2)
9c49337 fix(racy-ptr-cell): task #1358 — cell_unit.rs no longer round-trips a pointer through an integer channel (first review F1)
ee60cf6 fix(numa-shim): task #1262 — commit Cargo.lock's numa-shim entry, missed in the version-bump commit
7e20a4a docs(racy-ptr-cell): task #1360 — spin-wait doc no longer overclaims "bounded and short in practice" (first review F3)
```

`origin/main` matches local `HEAD` (`dc9dfb9`) — confirmed via `git fetch` + `git rev-parse origin/main` earlier in this segment for `ee60cf6`; the three racy-ptr-cell commits + the disposition-refresh commit landed on top of that and were pushed in the same flow. CI for `ee60cf6` was IN PROGRESS (Kani already `success`) when this checkpoint was written — background watch task `b6o9jqz93` is tracking it; no tag currently exists on the remote for numa-shim (deleted after the failed first attempt, not yet re-created).
