# Checkpoint — 2026-09-03 21:49

## Session summary
Continuation of the `tagged-index-stack` publish-readiness campaign (picked up from checkpoint `2026-09-03-1346.md`). Since then: closed out run-14's remaining group (GroupD, merged as `6085610`) and its T-push (confirmed CI+Kani green on `1ba35de`... actually on `fa43abc` first, then r1review's own fixes landed as `fa43abc`, both confirmed green). Then ran run-15's separately-named `R1_tagged_index_stack_review.md` fixes (`tis-r1review`, commit `fa43abc`, merged onto run-14's work — combined T-push confirmed green). Then ran run-16 (Sol-codex, 4 groups: GroupA/BD/C + a small out-of-scope `wrapping_add` drift-fix I made myself during T-push review) — landed as `8c8c019`/`ccb48aa`(actually squashed differently)/`0087bab`/`1ba35de`, confirmed CI+Kani green on `1ba35de`.

Then ran run-17 (Sol-codex), the biggest round this session: **two P1 blockers**. P1-1: `tis_p3_ab_runner.mjs`'s `--out-dir` resolved against repo root with no containment check and unconditionally `rmSync`'d it — `--out-dir .` deleted the entire repository (confirmed real, verified myself before delegating). P1-2: `push_index`'s clause 3 ("exclusive temporal ownership... until it returns") bound authority transfer to physical function return instead of the actual synchronization event (the successful CAS) — forbade a legitimate concurrent pattern (A publishes via CAS, B safely pops+repushes before A physically returns) that the algorithm itself handles fine. Fixed via a 3-wave, 6-group `/wrush` campaign:
- **Wave 1** (parallel): `tis-sol17-GroupOwnership` (solo, foundational — rewrote clause 3 around ownership EPOCHS with the CAS as the linearization point, not return; added a new positive loom regression `pop_repush_overlaps_unreturned_push_conserves`; fixed a false post-join "stale visibility" claim in the existing counterfactual; removed a now-redundant diagnostic flag — commit `6cd6c40`), `tis-sol17-GroupSecurity` (P1-1 fix: removed `--out-dir` entirely, added `validateScratchLeaf`+containment-checked `freshDir`, new 7-case negative-test suite with disposable skeleton-repo copies, counterfactually verified against the pre-fix runner — commit `96eb774`), `tis-sol17-GroupP31` (finished the `wrapping_add`→`+1` test-expression cleanup, dropped low-value proptest const asserts — commit `fd9df69`).
- **Wave 2** (parallel, after wave 1 landed): `tis-sol17-GroupR2` (P2-1: bounded runner windows/threads/samples + child timeout — commit `68bc130`), `tis-sol17-GroupSweep` (P2-3: threaded the epoch contract through every remaining call-site proof — benches/examples/tests/README/workspace-consumer `src/registry/heap_registry.rs`+`bootstrap.rs`/root `src/lib.rs`'s stale "TWO audited sites" claim; P3-2 CHANGELOG wording — commit `bccfef6`).

All 6 groups zero-trust reviewed personally (full diffs read, independent test/loom/rustdoc/build-check re-runs in isolated `CARGO_TARGET_DIR`s, several counterfactual verifications) and cp-merged sequentially onto `main`. **T-push then hit a genuine CI failure** (not caught by `npm run check`, which doesn't cover this job): the `tagged-index-stack package gates` CI job extracts the packaged `.crate` to a standalone temp dir and runs the test suite there — GroupSecurity's new negative-test file (`tis_p3_ab_runner_scratch_guard.rs`) derived a `real_repo` path via `crate_dir.ancestors().nth(2)`, correct only inside a workspace checkout; in the packaged-crate context this resolves to an unrelated directory, and copying `scripts/capture-measurement-identity.mjs` from there failed with ENOENT, failing all 7 tests. Diagnosed by reading the CI log in full (not just the summary), confirmed the mechanism by reproducing the exact CI scenario locally (`cargo package` → extract → `cargo test`/`cargo test --features test-internals`/`cargo bench --no-run`, all 3 CI steps), fixed by making the two workspace-only file copies best-effort (`copy_file_if_present` — those files are never needed by the FIXED runner's behavior under test, only by the not-CI-exercised pre-fix counterfactual) — commit `aaa55e0`. Verified the fix against a freshly re-packaged+re-extracted `.crate` before committing.

Also independently discovered (via zero-trust review, unprompted) a **real, pre-existing, out-of-scope finding**: the root crate's `rustdoc` CI job runs `cargo doc --no-deps --all-features` WITHOUT `RUSTDOCFLAGS="-D warnings"`, so it has been silently green despite ~66 warnings including genuinely broken intra-doc links (confirmed by reproducing locally with `-D warnings`: 12 files, real unresolved-link/private-item errors). This predates this session's work, is unrelated to any run-17 finding, and was correctly left untouched by the delegated groups (out of their scope). **Not yet logged into `docs/CORRECTNESS_OPEN_ITEMS.md`** — this is the single most important pending action, see Open questions below.

`npm run check` was re-run clean after the CI-fix commit and is running in the background as this checkpoint is written (not yet confirmed complete) — push has NOT happened yet for `aaa55e0`.

## Active goal
None — no `/goal` Stop hook is in force this session.

## TaskList
### in_progress
- #1882 tis-sol17-T-push: npm run check, push, confirm CI+Kani for all run-17 fixes (blocked internally on the just-completed `aaa55e0` CI-gate fix; `npm run check` re-running in background as of this checkpoint, not yet pushed)

### pending
- #661 tagged-index-stack — first publish to crates.io (blockedBy: none — technically unblocked, deliberately held pending explicit user go-ahead, irreversible: `cargo publish` has no delete, only yank). Now the SEVENTH consecutive independent review round (runs 9–17 plus the separate R1/R16-companion reviews) to find and close at least one real pre-publish issue — run-17 alone found two P1-severity blockers. Worth flagging strongly before any future publish authorization.

### recently completed
- #1877 tis-sol17-GroupOwnership (P1-2+P2-2, commit `6cd6c40`)
- #1878 tis-sol17-GroupSecurity (P1-1, commit `96eb774`)
- #1879 tis-sol17-GroupP31 (P3-1, commit `fd9df69`)
- #1880 tis-sol17-GroupR2 (P2-1, commit `68bc130`)
- #1881 tis-sol17-GroupSweep (P2-3, commit `bccfef6`)
- (all run-9 through run-16 tasks, plus tis-r1review, are also completed and deleted from the live TaskList per this session's repeated "удали выполненные таски" requests — durable record is `git log`, not the TaskList)

## Decisions
- Continued the established `/wrush` delegation pattern (manual `git worktree add` under `<repo>/worktrees/` + `rush run --role smart --timeout 60m`) for all of run-17's 5 delegated groups, unchanged from prior rounds.
- For run-17's two P1s, applied maximum rigor: independently verified BOTH claims against source before delegating (traced `push_index_impl`'s actual code to confirm no shared-memory op after the CAS; traced `scratchRoot`/`freshDir` to confirm `--out-dir .` genuinely resolves to repo root and gets `rmSync`'d).
- Drafted the new clause-3 text myself (epoch/linearization-point framing) as an anchor for `GroupOwnership`'s brief, rather than leaving the design fully open-ended — matches the rigor given to the original run-14 P1.
- Sequenced run-17 into 2 waves (not fully parallel) specifically to avoid file-overlap risk in `harness_bin.rs` (touched by both the ownership-contract fix and the P2-1 bounds fix) and to let `GroupSweep`'s call-site sweep use `GroupOwnership`'s actual final wording rather than a predicted draft.
- When CI failed on `aaa55e0`'s parent (`bccfef6`), did NOT treat it as blocking further zero-trust work — read the full CI log (not just the truncated summary), reproduced the exact failure locally before writing any fix, and independently reproduced the fix's correctness (re-packaged, re-extracted, re-ran all 3 CI steps) before committing — same rigor as the delegated-group reviews, applied to a self-authored fix.
- Deferred logging the newly-discovered root-crate `rustdoc -D warnings` CI-coverage gap into `docs/CORRECTNESS_OPEN_ITEMS.md` until after run-17's T-push fully lands (in-scope work takes priority; the gap is real but not urgent — it's been silently present for an unknown but likely long duration already).

## Open questions
- **Root-crate `rustdoc` CI-coverage gap still needs to be logged into `docs/CORRECTNESS_OPEN_ITEMS.md`** (see Session summary) — this is the single clearest next action once T-push confirms green. Not yet done as of this checkpoint.
- Does the user want `#661` (`cargo publish`, first release of `tagged-index-stack` to crates.io) executed once run-17's T-push is confirmed green, or held for a further separate explicit request? This is now the SEVENTH consecutive independent review round to find and close genuine pre-publish issues, and run-17 alone found two P1s (one an actual repo-destroying security bug in a shipped script) — the pattern is strong enough that it's worth surfacing explicitly before any publish authorization, not just noting in passing.

## Repo state
```
(main worktree clean — nothing to commit, working tree clean)
```
```
aaa55e0 fix(tis): fix tis_p3_ab_runner_scratch_guard.rs's real_repo path assumption -- broke the packaged-.crate CI gate
bccfef6 fix(tis): tis-sol17-GroupSweep — thread the epoch clause-3 contract through remaining call sites, workspace consumer, root lib.rs (P2-3, P3-2 CHANGELOG)
68bc130 fix(tis): tis-sol17-GroupR2 — bound A/B runner windows/threads/samples, add child timeout (P2-1, P3-2 runner comment)
96eb774 fix(tis)!: tis-sol17-GroupSecurity — remove --out-dir, an unbounded recursive-delete primitive (P1-1)
6cd6c40 fix(tis)!: tis-sol17-GroupOwnership — push_index clause 3 now transfers authority at the CAS, not physical return (P1-2, P2-2)
fd9df69 fix(tis): tis-sol17-GroupP31 — finish wrapping_add narrative cleanup, drop low-value proptest const asserts (P3-1)
08567e7 docs(review): audit tagged-index-stack Sol-codex run 17
1ba35de fix(tis): fix stale wrapping_add references in test comments left by GroupC's tag+1 switch
8c8c019 fix(tis): tis-sol16-GroupA — loom counterfactual structurally isolates the clause-3 violation (P2-1)
0087bab fix(tis): tis-sol16-GroupC — retry-overwrite causal chain, wrapping_add rationale, tautological proptest asserts (P3-1/P3-2/P3-3 + R16#2)
```

No stray worktrees — all run-17 worktrees already merged and removed (`git worktree list` shows only the main worktree for this repo). `aaa55e0` is local-only, NOT yet pushed — `npm run check` is running in the background as of this checkpoint; push + CI/Kani confirmation for `#1882` remain to be done in the next turn.
