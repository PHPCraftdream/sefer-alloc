# Checkpoint — 2026-07-31 01:00 [round-31-core-complete]

## Session summary

Round 31 (7 tasks: #471/R31-0, #466/R31-3, #465/R31-2, #464/R31-1, #476/R31-12
folded in as a ride-along with #464, #467/R31-4, #473/R31-9) is fully
implemented, zero-trust reviewed personally task-by-task, and committed —
matching the sequencing plan I laid out to the user after an explicit
self-review pass ("войди в созерцание и вылечи противоречие"). This picked
up directly from the prior window's pause (a `crush run` attempt for #471
was launched, then explicitly paused by the user mid-work and killed
cleanly with no side effects), resumed this window via `@sh` sub-agents per
the user's explicit `/babygoal` instruction.

**What Round 31 found/fixed, task by task** (all on `main`, `HEAD` at
`d93c091`):

- **#471/R31-0** (`dece4a7` + `c7b3eda`) — the round's P0. R30-3's NO-GO
  verdict for `virgin-zero-skip` was WRONG: its judge drove bare
  `AllocCore::new()` directly, bypassing the production magazine layer
  entirely. The real production chain (`HeapCore::alloc_zeroed` ->
  `refill_magazine_slow_virgin`) retains virginity across an ENTIRE
  freshly-carved magazine refill via `PerClass::virgin_mask` — a mechanism
  `tests/r13_3_magazine_virgin_hit_skips_zero.rs` already proved but no
  wall-clock judge had ever driven. New production-layer gate: 24/24
  activation cells PASS at 100%, `notouch` consumer shows a real -89% to
  -98.6% win across all 4 sizes (reproduced on an independent repeat run).
  Verdict: narrower, workload-shape-conditional GO-supporting result for
  touch-light consumers — NOT a blanket promotion. `docs/perf/OPEN_ITEMS.md`
  item 25 REOPENED (note: the review that flagged this cited "item 19" —
  wrong; the @sh agent caught this and correctly reopened item 25, the
  actual virgin-zero-skip item, independently confirmed by me).
  `Cargo.toml`'s `production` line untouched — promotion needs separate
  explicit sign-off, not sought here.
- **#466/R31-3** (`0985e22` + `d9d30cd`) — re-verified `large-cache-extended`'s
  six R14-5 hardening gates against current HEAD (all still hold), refreshed
  its turnover A/B on current code (t=127.776, 33.3%->100% hit rate,
  reproduces cleanly), and added two NEW regression gates the review
  demanded: N=1/2/4 narrow-working-set timing (no regression — extended
  cache measured FASTER at every N) and multi-heap RSS at 1/8/32 heaps
  (exact linear scaling, no blow-up). Ends with a GO-supporting promotion
  PROPOSAL (no numeric default change recommended) — pending explicit user
  sign-off, NOT enacted.
- **#465/R31-2** (`032048b` + `f93e663`) — swept the small-pool cap
  (4/8/16/32 segments) through the real global allocator on an 8-thread
  server-shaped workload, looking for the threshold where R30-7's
  `decommit_calls_total = 40 in both arms` null would break. Clean reject:
  mechanism delta stays EXACTLY zero through cap 32 (320 process launches,
  bit-identical `decommit_calls_total=40` in every one). MDE tightened to
  ~4-5% (vs R30-7's 18.8%), so this is a decisive null, not underpowered.
  Does not invalidate R27-4's original single-threaded finding — different
  workload shape — but confirms the small-pool win is workload-shape-
  dependent, not universal.
- **#464/R31-1 + #476/R31-12** (`fc11cf3` + `0da89c6`) — folded into one
  agent call since they share data/harness. R31-1: measured large-cache
  hit-rate PAST the 64 MiB boundary R30-6 never crossed — the tie BREAKS
  (87.5% vs 100.0% at both 128 MiB and 288 MiB bursts, exact across
  1/8/32 threads) — independently confirmed by me against the raw CSV.
  R31-12: confirmed (by reading `AllocCore::alloc_large`'s `div_ceil`
  SEGMENT-rounding directly) that R30-6's "48 MiB" workload actually rounds
  to a 64 MiB working set — exactly at the boundary where R30-6's original
  8/8-vs-8/8 parity was structurally guaranteed, not empirically discovered.
  `docs/perf/OPEN_ITEMS.md` item 27 narrowed to "parity at a 64 MiB rounded
  working set." Five R30-6 data-hygiene repairs also landed (wrong
  idle-delta column pair, one physically-impossible RSS row now excluded,
  SHA placeholder filled, MDE added, 100%-hit-workload limitation stated).
- **#467/R31-4** (`ca9aba9` + `74991c5`) — implemented the `ReservedSmallSegment`
  typed handle R30-10 had deferred (design doc §5.2-5.3), retrofitting
  `dbg_decomp_reserve_and_keep`/`dbg_decomp_release` (the one hook pair that
  mints-then-redeems a raw pointer) so double-release is now a COMPILE ERROR
  (E0382), not a `debug_assert!` that vanishes in `--release`. Also closed
  two P2 findings from the full Round 30 review: `has_bench_internals_cfg`'s
  `#[cfg_attr]` false-accept (fixed the parser to require the literal
  `#[cfg(` token), and `HeapCore::dbg_large_cache_hits`'s missing
  `bench-internals` gate (was shipping in every production build). I
  personally verified the constructor is genuinely non-`pub` (though noted
  the doc comment slightly overclaims its scoping tightness — `pub(super)`
  actually reaches all of `alloc_core`'s sibling modules, not just
  `alloc_core_small_pool.rs` — a precision nit, not a soundness gap, since
  external unforgeability still holds).
- **#473/R31-9** (`865534d`) — the round's last task. Replaced the flat,
  closed `Profile::{Rss,Balanced,Throughput}` enum (shipped only last round,
  unreleased) with a small builder over two independent axes
  (`SmallPoolPolicy` x `LargeCachePolicy`), fixing two confirmed defects:
  `Rss` never actually bounded RSS (it's a decay floor, not an admission
  cap — the name overclaimed), and `Throughput` silently narrowed the
  large-cache window on ground that R31-1's own crossing-regime finding
  (this same round) now proves was wrong. Doc comments carry the honest
  regime caveats throughout. `Profile::DEFAULT`/`new()` verified
  byte-identical to `SeferAlloc::new()`; low-level builders remain the
  escape hatch; `with_profile` stays `const fn`-usable.

**Two production-composition decisions now await the user's explicit
sign-off** (neither was enacted, per this project's standing discipline
that no default/composition change is auto-enacted from a measurement
task): (1) `virgin-zero-skip` — narrower, touch-light-conditional GO
evidence from #471; (2) `large-cache-extended` — GO-supporting evidence
with no proposed default-value change from #466. If either is accepted,
CLAUDE.md mandates the same-PR `npm run bench:table` + `npm run iai`
refresh — `production`'s composition hasn't changed since R13-9 (commit
`da77b38`, 18+ rounds), so this would be the first refresh in a long time.

**Zero-trust discipline applied throughout**: every task's diff and commit
message read personally; every headline number independently recomputed
from the actual committed CSV/raw log (not trusted from the agent's
narration) — including the #471 activation percentages, the #465 mechanism-
delta table, the #464/#476 crossing-regime hit-rate numbers, and the #476
SEGMENT-rounding arithmetic (read directly from `alloc_core_large.rs`'s
source). Caught one review citation error (item 19 vs item 25) via the
@sh agent's own independent source-check, confirmed correct by me. Caught
one doc-comment precision overclaim in #467 (`pub(super)` scope) — noted,
not blocking, since the substantive soundness property holds.

**Remaining Round 31 backlog explicitly deferred to a future round**
(tagged in task metadata `round: 32` during the "войди в созерцание"
self-review pass): #468/R31-5 (verify-gate-report.mjs tooling), #469/R31-6
(remaining Windows-crash-fix half, Profile doc half already absorbed into
#473), #470/R31-7 (macro-bench trigger unification + ETW question),
#472/R31-8 (one remaining CLAUDE.md rule), #474/R31-10 (trim/scavenge API
implementation — leads Round 32), #475/R31-11 (CI manifest clippy
centralization), #477/R31-13 (batch API consumer scouting).

## Active goal

The user's explicit `/babygoal` instruction this window (TaskList-driven,
not a Stop-hook condition): "реализуй задачи с помощью @sh, между тасками
делай коммиты. После завершения всей работы сделай /checkpoint, обнови
чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю
работу". Status: Round 31's core 6 tasks (+1 ride-along) implemented,
committed, personally verified; CHANGELOG already updated incrementally by
every task; markdown committed (including two review docs left over from
earlier in the session); this checkpoint is being written now; `@oh`
review is the one remaining step in this same turn.

## TaskList

### pending
- #449 crush-fallback state (persistent — do not complete): dormant.
  Explicitly cleared this session via `/crush-fallback clear`; `/crush` is
  back in normal use (though this round actually ran via `@sh` per the
  user's explicit choice this time, not `/crush`).
- #468, #469, #470, #472, #474, #475, #477 — Round 31's remaining backlog,
  explicitly deferred to Round 32 (see Session summary above for the
  reasoning: none blocks a product decision, several are process/polish).

### completed (Round 31, this session)
- #471/R31-0, #466/R31-3, #465/R31-2, #464/R31-1, #476/R31-12, #467/R31-4,
  #473/R31-9 — see Session summary above for per-task detail and commit
  SHAs.

### completed (Round 30, prior session window)
- #450–#463 (see `docs/checkpoints/2026-07-30-1730.md`).

### completed (Round 29, earlier session)
- #432–#448 (see `docs/checkpoints/2026-07-29-2340.md`).

## Decisions

- **Followed the user's explicit `@sh` strategy literally** (not `/crush`,
  which the user tried first this window, paused mid-work, then explicitly
  switched away from by re-issuing `/babygoal` with `@sh` named directly).
- **Did not blindly trust any agent's numeric claims** — independently
  recomputed the headline number for every task from the actual committed
  CSV/raw log before accepting it, catching zero errors this round (unlike
  Round 30, where 4 P1-level report defects were found and fixed) — a
  genuinely cleaner round on the reporting-accuracy axis, though still
  verified with the same rigor rather than assumed clean.
- **Resumed a user-paused `crush run` attempt's leftover files rather than
  discarding them** — the `@sh` agent for #471 inspected the paused
  attempt's uncommitted files with zero-trust scrutiny, found the design
  sound but `fmt`/`clippy` dirty, fixed the lint issues in place rather
  than starting over, and independently re-verified the numbers rather
  than trusting the paused attempt's own claims.
- **Kept both production-composition decisions (#471, #466) as proposals,
  not enactments** — consistent with this project's standing rule that
  default/composition changes need explicit user sign-off, never
  auto-enacted from a measurement task, even when the evidence trends GO.
- **Explicitly re-scoped the round mid-session** ("войди в созерцание и
  вылечи противоречие") — recognized that the originally-planned 14-task
  queue reproduced the exact "mostly meta-work, few product decisions"
  pattern the review criticized, cut it to the 6 tasks that actually feed
  a product decision, and deferred the rest with reasons recorded in task
  metadata rather than silently dropping them.

## Open questions

- **Awaiting explicit user sign-off on two production-composition
  decisions**: (1) promote `virgin-zero-skip` to `production` (narrower,
  touch-light-conditional GO evidence, #471); (2) promote
  `large-cache-extended` to `production` with a finite default budget
  (GO-supporting evidence, no default-value change proposed, #466). Both
  are fully proposed and evidenced but NOT decided.
- Whether the user wants Round 32 (the explicitly-deferred backlog, listed
  above) run under the same discipline as Round 31 — not yet asked.
- The `pub(super)` scoping precision nit in `ReservedSmallSegment`'s doc
  comment (#467) — cosmetic, not filed as a task, may be worth a one-line
  doc fix in a future pass if anyone reads that module closely.

## Repo state

```
?? .claude/
```

(clean otherwise — `HEAD` at `d93c091`, all Round 31 work + review docs
committed)

```
d93c091 docs: two independent readonly reviews that seeded Round 31 (@fm acceleration review, post-response P0 review)
865534d refactor(profile): split Profile into independent SmallPoolPolicy/LargeCachePolicy axes (R31-9, task #473)
74991c5 docs: fill in R31-4's landing-commit SHA placeholder (task #467)
ca9aba9 fix(alloc-core): ReservedSmallSegment typed handle; close P2-1/P2-2 (R31-4, task #467)
0da89c6 docs(perf): fill in R31-1's landing-commit SHA placeholder (task #464)
```
