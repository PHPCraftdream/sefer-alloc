# Checkpoint — 2026-07-22 [r12-complete]

## Session summary
This session resumed from checkpoint `2026-07-21-r12-planned.md` — Round 12 had just been
planned (13 tasks, #252–#264) from two external reviews of the Round 11 wave, no execution
had started. User invoked `/babygoal` with "реализуй задачи с помощью @sh, между задачами
делай коммиты, покрой их тестами" — full autonomous execution authorized. A babysit cron
heartbeat (every 15 min) was armed before starting.

All 13 planned tasks were executed sequentially via `@sh` sub-agents, each followed by
personal zero-trust review (read every diff line by line, re-run the tests myself — not
trust the delegate's own "tests passed" claim, reproduce red-before/green-after
counterfactuals for every safety-relevant change) before committing. One additional task
(#265, R12-14) was filed mid-session after I personally discovered two of the new R12-1/
R12-2 tests fail under the non-CI `--all-features` combo (confirmed pre-existing via git
stash, not a regression) — bringing the total to 14 tasks, all now completed and committed
as 14 individual commits (`79f4136`..`3dc7bd9`).

**What shipped, by priority:**
- **P0 correctness (unconditional, no feature gate):** R12-1 (directory-sidecar aliasing UB
  fix), R12-2 (NUMA node-id 8-63 dense bucket mapping).
- **P0-perf (new opt-in features):** R12-3 (`exact-span-large`), R12-4
  (`large-reserved-capacity`, follow-up to R12-3's OPT-G headroom loss).
- **P1:** R12-5 (bounded NUMA node-cache refresh), R12-6 (overflow-finalization beyond
  64-base cap), R12-7 (`class-aware-dirty`, wall-clock-gated then implemented with loom
  coverage), R12-8 (unify AllocBitmap+MagazineBitmap — **NO-GO**, cites the codebase's own
  prior 2026-07-10 G1 honest-reject, no code changed), R12-9 (`primordial-lazy-commit`/
  `small-segment-lazy-commit` split — **promoted to production** after user confirmation),
  R12-10 (`virgin-zero-skip` for Small `alloc_zeroed`).
- **P2:** R12-11 (`page-map-diag` — gates PageMap maintenance out of default carve path),
  R12-12 (batch API honestly marked experimental — **both** remediation options applied per
  user's explicit choice), R12-13 (page-run layer — **SUPERSEDED/NO-GO**, cites R12-3's own
  measured numbers), R12-14 (fixed the two `--all-features` test-density gaps).

**Two AskUserQuestion check-ins during execution** (both per the tasks' own instructions to
confirm with the user rather than decide unilaterally): (1) whether to promote
`primordial-lazy-commit` into `production` — user said yes; (2) how to mark the batch API
experimental (feature-gate-only vs. doc-only vs. both) — user said do both.

**Immediately after task #265 (R12-14) finished**, the user sent a new instruction mid-flow:
"потом сделай обновление чейнлжлога, чекпоинт, новый коммит всего, пуш, наладь ci" — update
CHANGELOG, checkpoint, commit everything, push, fix/set up CI. I decomposed this into 5
tracked tasks (#266–#270). #266 (CHANGELOG) is done — added a comprehensive "Round 12"
section to `CHANGELOG.md` under `## [Unreleased]`, replacing the narrower standalone
"batch-api" entry that had been there, covering all 14 tasks with the same per-task detail
style as the existing Round 11 entry. #267 (this checkpoint) is in progress now. #268
(commit CHANGELOG+checkpoint), #269 (push to origin/main — **not yet done, this session has
never pushed**), and #270 ("наладь CI" — audit `.github/workflows/` against all the new
Round 12 features/feature-renames, fix anything broken/stale) are still pending.

**Known pre-existing flaky test** (not a regression, encountered and confirmed flaky
multiple times this session via isolated reruns): `tests/dealloc_sublinear.rs::
own_thread_free_is_subquadratic`, a wall-clock timing threshold test — noisy host, not a
real O(1)→O(N) regression. Do not treat a red run of this specific test as a real problem
without isolated reruns first.

## Active goal
None currently armed as a `/goal` Stop hook. The `/babygoal` TaskList-driven execution model
is what's actually governing progress — see TaskList below. A babysit cron heartbeat
(15-minute interval) was armed at the start of Round 12 execution; it self-deletes once
pending+in_progress reaches 0, so it may already be gone or about to delete itself now that
only the 5 post-Round-12 admin tasks remain.

## TaskList
### in_progress
- #267 Взять чекпойнт сессии Round 12

### pending
- #268 Закоммитить CHANGELOG и чекпойнт
- #269 Push всех коммитов Round 12 в origin/main
- #270 Наладить CI под новые фичи Round 12

### recently completed
- #266 Обновить CHANGELOG.md за Round 12
- #265 R12-14 (P2): почини directory-тесты под --all-features
- #264 R12-13 (P2, после R12-3): page-run layer stage-2 — только если exact-span не закрыл нишу (SUPERSEDED/NO-GO)
- #263 R12-12 (P2): честно назвать batch API экспериментальным (semver-поверхность)
- #262 R12-11 (P2): убрать PageMap из production carve-пути (или за diag-фичу)
- #261 R12-10 (P1): virgin-zero skip для Small alloc_zeroed — stage-2 реализация
- #260 R12-9 (P1): primordial-only lazy commit — расщепить политику, включить в production
- #259 R12-8 (P1): объединить AllocBitmap+MagazineBitmap в 2-bit BlockStateMap (NO-GO)
- #258 R12-7 (P1): class-aware dirty routing — wall-clock gate, затем per-class bitmap
(#257, #256, #255, #254, #253, #252 also completed — the full R12-1..R12-6 P0/P0-perf/P1 set)

## Decisions
- Promoted `primordial-lazy-commit` into `production` (Cargo.toml) after R12-9's ~5.14×
  measured first-heap-commit win was independently reproduced — user explicitly confirmed via
  AskUserQuestion rather than this being decided unilaterally, per the task's own instruction
  that production feature-composition changes need explicit sign-off.
- Applied BOTH batch-API-experimental remediations (feature-gate dependency on `experimental`
  AND dropping `#[doc(hidden)]` in favor of a visible EXPERIMENTAL doc marker) rather than
  picking one — user's explicit choice via AskUserQuestion when the task flagged the two
  options as possibly equivalent.
- R12-8 (unify AllocBitmap+MagazineBitmap) and R12-13 (page-run layer) both reached honest
  NO-GO verdicts with zero code changed, each citing prior institutional record (2026-07-10
  G1 honest-reject; R12-3's own measured RSS numbers) rather than re-deriving from scratch —
  both treated and committed as complete, correct task outcomes, not failures.
- Filed and executed #265 (R12-14) as an additional task discovered mid-session (not part of
  the original 13-task plan) after personally finding two new tests fail under the
  non-CI-gated `--all-features` combo during R12-2/R12-3 verification — chose to fix
  immediately rather than defer, since it was cheap and directly related to work just done.
- For every task in this round that added a new opt-in Cargo feature (R12-3, R12-4, R12-7,
  R12-9's `small-segment-lazy-commit`, R12-10, R12-11's diagnostic split), deliberately did
  NOT add it to `production` in the same commit — promotion is treated as a separate decision
  each time, consistent with the project's stated methodology.

## Open questions
- None outstanding from Round 12 itself — both AskUserQuestion check-ins were resolved during
  execution (see Decisions above).
- The user's follow-on request ("обновление чейнлжлога, чекпоинт, новый коммит всего, пуш,
  наладь ci") is in progress via tasks #266–#270; "наладь CI" (#270) is intentionally vague
  in the user's own phrasing — the plan is to audit `.github/workflows/` against this round's
  feature changes and fix whatever is actually found broken/stale, not to assume a specific
  fix in advance.

## Repo state
```
 M CHANGELOG.md
?? docs/perf/_raw_baseline_off.log
?? docs/perf/_raw_baseline_off_reduced.log
?? docs/perf/_raw_criterion_medium.log
?? docs/perf/_raw_criterion_production.log
?? docs/perf/_raw_firstalloc_medium.log
?? docs/perf/_raw_firstalloc_production.log
?? docs/perf/_raw_iai_medium.log
?? docs/perf/_raw_iai_production.log
?? docs/perf/_raw_medium_on.log
?? docs/perf/_raw_medium_on_reduced.log
?? docs/perf/_raw_r10_6_dir_off_nonnuma.log
?? docs/perf/_raw_r10_6_dir_on_nonnuma.log
?? docs/perf/_raw_r10_6_numa_sweep.log
?? docs/perf/_raw_r10_7_d_vs_f.log
?? docs/perf/_raw_r10_7_tcache_arm.log
?? docs/perf/_raw_r10_7_tcache_isolated.log
?? docs/perf/_raw_r10_7_warm_arm.log
?? docs/perf/_raw_r11_6_after_numa.log
?? docs/perf/_raw_r11_6_baseline_numa.log
?? docs/perf/_raw_r9_9_followup.log
?? docs/perf/_raw_r9_9_followup_run2.log
?? docs/perf/_raw_r9_9_followup_run3.log
```
(All untracked files are generated measurement/CI scratch artifacts from this and prior
sessions' bench/test runs — not real pending changes, will not be committed.)

```
3dc7bd9 test(directory): make R12-1/R12-2 directory tests density-agnostic under --all-features (R12-14)
6d6e279 docs(perf): page-run layer superseded by exact-span Large -- NO-GO (R12-13)
a7db75a docs(batch-api): honestly mark the batch API as experimental/unstable (R12-12)
5199148 perf(alloc-core): gate PageMap maintenance behind a diagnostic-only feature (R12-11)
698cfca feat(alloc-core): virgin-carve zero-skip for Small alloc_zeroed, opt-in (R12-10)
```

Local `main` is 40 commits ahead of `origin/main` (`git rev-list --count origin/main..HEAD`)
— this Round 12 wave (14 commits) is only the most recent slice; the push has apparently been
lagging since at least Round 6/7 (the oldest unpushed commits are Round 6/7/8/9 CHANGELOG
entries). Pushing is task #269, still pending — worth flagging to the user before pushing,
since a 40-commit push is a larger action than "just this session's work."
