# Checkpoint — 2026-08-13 21:00 [aligned-vmem-round10-h2c-complete]

## Session summary

This session continued the `aligned-vmem` review campaign from checkpoint `2026-08-13-1730.md` (rounds 4-9 done, campaign methodology explicitly PIVOTED at round 9 away from scheduled full-crate reads). Round 9's closing review argued diminishing returns and recommended event-driven review instead. The user then asked to get an independent, self-contained review prompt to run with a fresh agent outside this session's accumulated context — a deliberate test of round 9's own "convergence is evidence about the reviewer, not the code" hypothesis. That prompt was handed to the user's own separately-run agent, which produced `docs/reviews/2026-08-13-aligned-vmem-independent-review.md` with a HIGH finding (H1) that 9 rounds of the established campaign methodology had never found.

**Round 10** (H1/M1/M2/L1): the user reported the independent review's findings and asked to fix them. Before delegating, I personally zero-trust-verified all four findings against the actual code (not just trusting the review's claims) — all confirmed real: **H1 (HIGH)** — Linux HugeTLB path could leak an entire pinned huge-page mapping when the system's configured default huge-page size isn't 2 MiB (`default_hugepagesz=1G` is a normal HPC/DB config); the crate's own `LINUX_HUGE_PAGE_SIZE = 2 MiB` constant was never actually requested from the kernel via `MAP_HUGE_2MB`, so `mmap(MAP_HUGETLB)` silently used the *system default*, and the crate's bookkeeping assumed 2 MiB regardless — this premise had been re-confirmed (arithmetic-only, never premise-only) by task #714 and multiple later reviews across 9 rounds. **M1 (MEDIUM)** — `Reservation::from_raw_parts` silently accepted two documented-forbidden `reservation_len` values its own comment falsely claimed were rejected. **M2 (MEDIUM)** — the hand-written `mmap` FFI declaration had a real 32-bit-Unix ABI shape mismatch risk. **L1 (LOW)** — the benchmark harness recorded OOM/no-op iterations as valid fast samples. Delegated to 4 parallel `/crush` sessions in isolated worktrees (tasks #909-912), personally verified each (including independently re-deriving the `MAP_HUGE_2MB = 21 << 26 = 0x54000000` constant from the Linux kernel's own header definitions rather than trusting the delegate's self-correction), merged with one real conflict (both H1 and M2 tasks picked open-item number 52 independently — resolved by renumbering H1 to 53, no content lost). Pushed as `00868d1`, CI confirmed green including `test macos (production)` and `test (aarch64-unknown-linux-gnu)`.

**Round-10 closing review** (`@oh`, commissioned specifically to answer the user's question "we said nothing was left, then this whole batch got found — what else are we obligated to fix?"): confirmed all four fixes correct (including proving M1's tightened check breaks no legitimate caller — traced `numa-shim`'s sole `from_raw_parts` call site to satisfy the new predicates by construction), then ran a full exhaustive sweep of every hard-coded constant/platform assumption in the crate specifically hunting H1's failure-mode shape ("has the premise ever been checked, or only the arithmetic on top of it?"). Found the campaign's signature "round N's fix creates round N+1's finding" pattern reproduced twice (H2C3, H2C8), plus a genuine **publish-blocking regression the round itself introduced**: **H2C1 (MEDIUM)** — M2's `compile_error!` fix broke Tier-1 `i686-unknown-linux-gnu` and Tier-2 `armv7-unknown-linux-gnueabihf` builds (both build 0.1.0 fine today) with no disclosed BREAKING note. Also found **H2C6 (LOW)** — the one remaining constant in the crate with H1's exact shape: `WIN_ALLOCATION_GRANULARITY` on the Windows fast path is never verified at its point of use (unlike the Unix side, which round 8's U1 already fixed the identical way). The closing review's central verdict (§5): round 9's PIVOT *recommendation* was right (H1 needed no infrastructure, just a differently-framed reader) but its *stated reason* was wrong ("nothing left" was actually "nothing left for THIS reader"); the actionable rule is "a premise re-confirmed N times by the same review lineage has been verified once, not N+1 times."

**H2C fix pass** (this session's final work): user asked to fix all 8 closing-review findings via `/crush` + worktrees. Decomposed into 5 tasks (#914-918), delegated in parallel, each personally zero-trust verified — including two real cross-worktree contamination false-test-failures caught and worked around via isolated `CARGO_TARGET_DIR` (shared target dir between concurrent `crush` builds produced stale binaries). Most rigorously reviewed: H2C6's Windows fast-path control-flow restructuring (traced the whole function for double-release/leak risk, confirmed `VirtualFree(addr, 0, MEM_RELEASE)` is the correct universal-release call regardless of committed state) and H2C1's fix (personally added the `i686-unknown-linux-gnu` rustup target and confirmed the build that would have failed pre-fix now succeeds). All 5 merged cleanly, CHANGELOG written, closing review doc committed, pushed as `d1de3bc`, CI confirmed fully green (including `test windows (production)` — critical given H2C6 changed Windows hot-path control flow — and all Linux/macOS jobs).

## Active goal

None — no `/goal` Stop hook is armed in this session.

## TaskList

### in_progress
- #657 numa-shim — verify/prepare for crates.io republish (blockedBy: #658)
- #658 aligned-vmem — publish 0.2.0 (local already bumped, crates.io still shows 0.1.0) (blockedBy: #842, #848, #849)
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io

### pending
- #662 design note for bench-scale-tool alongside criterion/iai
- #763 implement bench-scale-tool per approved design (blockedBy: #662)

### recently completed
- #909-913 aligned-vmem round-10 A-D + CI confirm: H1/M1/M2/L1 remediation from an independent review that found a HIGH the established campaign methodology never found
- #914-919 aligned-vmem round-10 closing-review fix pass (H2C1-H2C8) A-E + CI confirm: fixed a publish-blocking regression (H2C1) the round itself introduced, plus 7 more findings

## Decisions

- **Deliberately broke methodology-inheritance by handing an independent, context-free review prompt to a separately-run agent** rather than continuing the established campaign's own review template — this was the single highest-value action in the whole session, producing the campaign's first HIGH finding since round 3 and directly falsifying round 9's stated reasoning (though not its recommendation) for pivoting away from full reads.
- **H1's fix: explicit `MAP_HUGE_2MB` request over reading `/proc/meminfo`** — keeps the crate's zero-dependency, minimal-unsafe-surface design principle; accepted the resulting functional narrowing (can no longer get huge pages other than 2 MiB, ever, on any host) as the honest fail-closed trade-off, documented explicitly (H2C4) rather than left implicit.
- **H2C1's fix: per-target `OffT` type alias over a docs-only exemption** — keeps Tier-1 `i686-unknown-linux-gnu` building (real value) while still fixing the underlying ABI concern properly, rather than just disclosing a narrower supported-target list and living with the build break.
- **Resolved the item-52-numbering merge conflict (round 10) by renumbering, not by picking one side** — both H1's and M2's open-items entries were kept in full; this is the established convention from earlier rounds (never discard content on a real conflict).
- **Diagnosed two `CARGO_TARGET_DIR`-contamination false test failures during H2C verification by re-running with an isolated target dir** rather than trusting either the stale "FAILED" or blindly re-running in the shared dir hoping it'd resolve itself — this is a known hazard in this session's `/crush`-parallel-worktree pattern (also seen once in round 10's own task F).

## Open questions

- **Is `aligned-vmem` NOW actually ready for 0.2.0 (task #658)?** The closing review's own honest framing (§5.2): not "no known HIGH/CRITICAL findings" (that's the same unfalsifiable statement that let H1 hide for 9 rounds) but "one exhaustive premise sweep found 15 constants/assumptions, of which 1 (`WIN_ALLOCATION_GRANULARITY`) had H1's exact shape — now fixed (H2C6) — and the rest are either exercised by real CI or fail closed." Task #658 remains blocked pending #842/#848/#849 per its own TaskList record, and publish is explicitly gated on maintainer go-ahead — not requested this session.
- **The `mock` feature-unification question** (flagged by both the independent review's null-results section and `Cargo.toml`'s own comment as "worth settling before first publish," cost strictly increasing with time, becomes a breaking change the moment 0.2.0 ships) — raised by the round-10 closing review's Tier-0 list but not acted on this session; needs an explicit decision (convert to `--cfg`, or explicitly accept the hazard for 0.2.0 and record why).
- **Build the ~50-line citation-resolver script?** — proposed by round 9's closing review as a precondition for the pivot, not built; round 10 then independently reproduced the exact defect class it would catch (H2C8, a citation invalidated by a same-round parallel merge, itself found only by the round-10 closing review). Now cited twice as evidence for building it (round 9 §5.4 point 5), still not started.
- **Linux `bench-internals` real-backend CI row** — unchanged priority from rounds 8/9/10, still the highest-leverage coverage item (4+ beneficiaries: item 48's S4 remainder, item 50's U10, U1's fast-path observability, and now a second live host for H2C's `page_size() > PAGE`-gated test arms). Not started.
- **Should round 11 happen, and in what shape?** Round 10's own closing review explicitly recommended AGAINST another scheduled full-crate read (the pivot's evidence is unrefuted) and FOR either another independent/fresh-context review aimed specifically at premises, or the infrastructure items above. Not decided this session.

## Repo state

```
(clean — no untracked or modified files)
```

```
d1de3bc fix(vmem), docs: round-10 closing review fix pass — H2C1-H2C8, write CHANGELOG entry, commit closing review doc
d41baf9 Merge vmem-h2c-c (task #916): fix from_raw_parts's overclaiming comment, add three cheap missing predicates (len, base alignment, reservation_len sufficiency) with negative tests (H2C3)
e95b9de fix(vmem): add missing from_raw_parts contract checks for len and base (task #916, finding H2C3)
b06f1d1 Merge vmem-h2c-b (task #915): comprehensive fix for open-items 52/53 — move to Recently Resolved, fix indentation, fix stale citation, rewrite H2C1's description to match the type-alias fix, add H2C4+H2C5 notes (H2C1-docs+H2C2+H2C4+H2C5+H2C8)
aec8274 Merge vmem-h2c-d (task #917): add missing alignment check to Windows fast-path, mirroring round-8's U1 decision on Unix (H2C6)
```

Local `main` (`d1de3bc`) **matches `origin/main` exactly** — pushed and CI-confirmed fully green (including `test windows (production)`, `test macos (production)`, `test (x86_64-unknown-linux-gnu)`, `test (aarch64-unknown-linux-gnu)`) as of this snapshot.
