# Checkpoint — 2026-07-22 [r13-planned]

## Session summary
Round 12 (14 tasks, commits `79f4136`..`3dc7bd9` + CHANGELOG/checkpoint commit
`7d43125`) is complete, committed, and pushed to `origin/main`. This session
picked up from the `2026-07-22-r12-complete.md` checkpoint and moved into
Round 13 planning: two independent read-only reviews of the Round 12 wave were
run and personally cross-checked against the actual source before any task was
filed.

**Review #1** (external, prioritization-focused) found: a P0 lost-signal risk
in `class-aware-dirty`'s OOM transition (producer sets only the coarse dirty
bit on sidecar-OOM; once another producer later materialises the sidecar, the
consumer scans the per-class slice exclusively — the OOM-window entry stays
invisible until a periodic full scan/OOM-rescue); a P1 NUMA bucket-lifetime
bug (`node_ids` is append-only, so the 8-slot cap is "8 nodes ever seen", not
"8 concurrent", plus a stale doc comment claiming NUMA directory lookup is
disabled when R11-6 already implemented it); a P1 `virgin-zero-skip` regression
risk (bypasses the magazine for ALL Small `alloc_zeroed`, so warm-reuse pays
both the lost tcache-hit AND the memset, no wall-clock gate was run before
merge); a P1 premature "page-run superseded" verdict (exact-span-large and
medium-classes-wide are both still opt-in, so production gets no RSS benefit
from either yet — correct status is deferred, not superseded); and a P2 CI gap
(only `production` and `--all-features` are tested, masking feature
interactions like numa-aware vs. Windows reserved-capacity).

**Review #2** (independent third pass, via `@fm` agent, explicitly scoped to
find NEW issues beyond review #1's list) confirmed all five of review #1's
findings with a one-line agreement each, explicitly checked and found NO
issues in R12-4/5/6/9/10, and surfaced four new findings, all personally
re-verified against the source by the orchestrator (not taken on the
sub-agent's word): **N1** — the `virgin-zero-skip` branch never reaches
`refill_magazine_slow`, so it never calls `drain_heap_overflow()`; a heap that
allocates ONLY through `alloc_zeroed` (the feature's own target calloc-heavy
profile) never drains its cross-thread-free overflow ring — a real resource-
retention defect, not just a perf regression, confirmed by reading
`heap_core_alloc.rs`'s three call sites. **N2** — `SegmentDirectory::clear_bit`
calls the REGISTERING `node_bucket_mut` instead of the read-only
`node_bucket`, so a no-op clear for a never-before-seen node can burn one of
the 8 NUMA registration slots; confirmed by reading `segment_directory.rs` and
tracing `sync_directory_for_segment_classes`'s call pattern. **N3** — the new
`tests/loom_class_aware_dirty.rs` (added in R12-7) is absent from the
`loom-xthread` CI job's explicit file list and no loom job enables
`class-aware-dirty` at all — confirmed by reading `ci.yml:515-534`. Also
surfaced: the README `bench:table` numbers are stale ("post-Round9",
2026-07-20) relative to the current `production` composition (PageMap removed
in R12-11, primordial-lazy-commit added in R12-9) — confirmed against
`README.md`'s own date-stamps and `Cargo.toml:369`'s current feature list —
and a caution that the 8-slot Large cache's size-factor matching may distort
the R13-6 A/B gate under `exact-span-large`'s finer-grained span sizes.

**All findings were folded into 10 tracked tasks** (#271–#280, "R13-1".."R13-
10") rather than left as prose — no new tasks were created for review #2's
findings; they were merged into the existing task descriptions with file:line
citations, and two tasks had their priority raised (#273/R13-3 to reflect the
N1 resource-retention severity; #275/R13-5 from P2 to P1 to reflect that N3's
CI gap blocks the class-aware-dirty promotion decision, #279). A durable
mirror of both review summaries and the prioritized plan was written to
`docs/reviews/2026-07-22-r13-plan.md`.

**No Round 13 code work has started yet** — this checkpoint captures the
planning state only. The user then asked to fold this checkpoint and the
review-plan doc into the ALREADY-PUSHED checkpoint commit `7d43125` via
`git commit --amend` + `git push --force-with-lease`, rather than a new
commit — an explicit, confirmed choice (the user was asked to choose between a
safer new-commit approach and a real amend+force-push, and chose the latter
knowing it rewrites already-shared history).

## Active goal
None armed. No `/babygoal` heartbeat is currently running for Round 13 — task
execution has not started.

## TaskList
### pending
- #271 R13-1 (P0): починить OOM-переход class-aware-dirty — coarse-only latch или DUAL/READY, loom
- #272 R13-2 (P1): переиспользование NUMA-bucket слотов + тест миграции через 9+ узлов + стейл-комментарий
- #273 R13-3 (P1, повышен: дефект ресурсов): virgin-zero-skip через magazine + drain-прелюдии + wall-clock gate
- #274 R13-4 (P1): перестатусить page-run с superseded на deferred/no-demonstrated-victim
- #275 R13-5 (P1, повышен с P2): feature-изолированный CI + loom_class_aware_dirty в CI + loom-инвентаризация
- #276 R13-6: production gate пары exact-span-large + large-reserved-capacity (A/B по платформам)
- #277 R13-7: расширить Large cache за пределы 8 слотов (lazy sidecar 32-64 или size-bucketed)
- #278 R13-8: judge на 256-2048 живых объектов 260 KiB-2 MiB  (blockedBy: #277)
- #279 R13-9: решение о promotion class-aware-dirty в production  (blockedBy: #271, #275)
- #280 R13-10 (процесс): wave-отчётность — production A/B/B/A отчёт, машинно-читаемые результаты, гигиена комментариев

## Decisions
- Merged both review passes into the existing 10-task Round 13 plan instead of
  filing duplicate tasks — every new finding from review #2 was traced to a
  specific file:line and appended to the task whose scope it belongs to.
- Raised #273 (virgin-zero-skip) and #275 (CI/loom gaps) in priority based on
  review #2's findings (resource retention defect; CI blocks a promotion
  decision), left the rest of the ordering unchanged.
- User explicitly chose real `--amend` + `--force-with-lease` over a new
  commit for folding this checkpoint + the review-plan doc into `7d43125`,
  after being shown the force-push consequence explicitly.

## Open questions
None outstanding — both reviews' findings are fully triaged into tasks; no
Round 13 task has been started, so no promotion or design decision is pending
yet.

## Repo state
```
?? docs/perf/_raw_baseline_off.log
?? docs/perf/_raw_baseline_off_reduced.log
?? docs/perf/_raw_criterion_medium.log
?? docs/perf/_raw_criterion_production.log
?? docs/perf/_raw_firstalloc_medium.log
?? docs/perf/_raw_firstalloc_production.log
?? docs/perf/_raw_iai_medium.log
?? docs/perf/_raw_iai_production.log
?? docs/perf/_raw_medium_on.log
?? docs/perf/_raw_medium_on_reduced.log
?? docs/perf/_raw_r10_6_dir_off_nonnuma.log
?? docs/perf/_raw_r10_6_dir_on_nonnuma.log
?? docs/perf/_raw_r10_6_numa_sweep.log
?? docs/perf/_raw_r10_7_d_vs_f.log
?? docs/perf/_raw_r10_7_tcache_arm.log
?? docs/perf/_raw_r10_7_tcache_isolated.log
?? docs/perf/_raw_r10_7_warm_arm.log
?? docs/perf/_raw_r11_6_after_numa.log
?? docs/perf/_raw_r11_6_baseline_numa.log
?? docs/perf/_raw_r9_9_followup.log
?? docs/perf/_raw_r9_9_followup_run2.log
?? docs/perf/_raw_r9_9_followup_run3.log
```
(All untracked files are generated measurement/CI scratch artifacts from prior
sessions' bench/test runs — not real pending changes, will not be committed.)

```
7d43125 docs(changelog): document Round 12 -- correctness fixes, exact-span Large, class-aware dirty routing, virgin-zero skip
3dc7bd9 test(directory): make R12-1/R12-2 directory tests density-agnostic under --all-features (R12-14)
6d6e279 docs(perf): page-run layer superseded by exact-span Large -- NO-GO (R12-13)
a7db75a docs(batch-api): honestly mark the batch API as experimental/unstable (R12-12)
5199148 perf(alloc-core): gate PageMap maintenance behind a diagnostic-only feature (R12-11)
```

Local `main` is fully in sync with `origin/main` before this amend (0 commits
ahead/behind, confirmed via `git rev-list --count origin/main..HEAD`) — this
checkpoint and `docs/reviews/2026-07-22-r13-plan.md` are being folded into the
tip commit `7d43125` via `--amend`, per the user's explicit choice, requiring
a `--force-with-lease` push to `origin/main` next.
