# Checkpoint — 2026-07-20 08:01 [R8 queue: 7/10 done, #221 blocked on crush peak-hours]

## Session summary

`sefer-alloc` (D:\dev\rust\sefer-alloc), 100%-Rust allocator. This session executed
the R8 perf-review task queue (#214–#223, 10 tasks derived from an external
perf/correctness review of commit range `ffd3215..f0dd9a9`), via sequential
`/crush` delegation with personal zero-trust review before every commit — the
same discipline established in earlier sessions of this project.

**7 of 10 tasks are done and committed on `main`, already pushed to
`origin/main`** (`ffd3215..38f4108`, 24 commits): #217 (docs fix, `c815927`),
#214 (incremental directory sync, `af7b039`), #215 (authoritative directory
miss + self-heal, `09237e0`), #216 (directory→production, `ec7ac34`, plus 2
opportunistically-fixed clippy regressions found via `--all-features`), #218
(Unix/miri frontier=SEGMENT, `fa2c064`), #219 (payload_start/decommit_start
split, `e9db718`), plus hygiene fixes along the way (`180bb8a` stale test
count, `f97cf1f` pre-existing vmem fmt drift, `38f4108` a `numa-aware`
test-gate gap in #215's own test file — caught by `npm run check`'s
`--all-features` matrix entry right before the push).

After the push, the user explicitly asked to continue with the remaining 4
tasks (#220–#223) via a fresh `/goal` directive. Before that, a **contemplation
round** (`/fm` "войди в созерцание") reprioritized and re-scoped all four:
**#222** (medium-classes judge-sweep) was promoted to P2 and run FIRST since
it's pure measurement — completed and committed (`9afba66`,
`docs/perf/R8_9_MEDIUM_CLASSES_VERDICT.md`): confirms the review's "16× fewer
segments" claim precisely at n=64, finds a NEW second cliff at the relocated
`SMALL_MAX` = 1 MiB (1.5/2 MiB still pay full dedicated-span cost under
`medium-classes`), verdict GO on the existing 6 classes, conditional-GO split
on further extension (strong case for the 1 MiB–2 MiB gap, weak case for
finer sub-1 MiB granularity).

**#221** (zero-skip for `alloc_zeroed`) was reprioritized to P2 and picked up
next, re-scoped during investigation down from a general "zero-state
tracking" ask to a narrow, safety-reasoned **Large-path-only** skip: a fresh
OS reservation via `alloc_large_slow` is unconditionally zero on every
platform (same reasoning #218 already established), so `AllocCore::
alloc_large` was changed to return `(*mut u8, bool)` (freshness), threaded
through `AllocCore::alloc_zeroed` AND `HeapCore::alloc_zeroed` (the REAL
production entry point behind `SeferAlloc`/`GlobalAlloc` — discovered
mid-investigation that `AllocCore::alloc_zeroed` alone is NOT what
`SeferAlloc` calls; `HeapCore::alloc_zeroed` needed its own Large-specific
dispatch replicating `HeapCore::alloc`'s cross-thread-drain prelude). The
small-class path (magazine/refill-batch) was explicitly scoped OUT as too
entangled/risky for this task. A crush run was launched with an extremely
detailed prompt (mandating a byte-level, memcmp-based test proving the
`large_cache`-HIT path still ALWAYS fully zeroes — the critical regression
guard against the exact information-disclosure failure mode this task risks).

**That run FAILED — not a code/design problem, a provider outage class of
failure**: `crush`'s `zai` provider refused with "peak hours (08:00–12:00),
refusing until 12:00" (current time at failure: 08:00). Per this project's
own `/crush` skill rule, peak-hours refusals are NEVER bypassed
unsolicited (`--allow-peak-hours` requires explicit human authorization each
time) and are NOT a quota-exhaustion case (so no local-agent fallback either)
— the correct response is to report and wait for user direction, which is
where this checkpoint is being written from.

**The working tree currently holds a PARTIAL, UNREVIEWED, UNCOMMITTED diff**
from that failed run (`docs/ARCHITECTURE.md`, `src/alloc_core/alloc_core.rs`,
`src/alloc_core/alloc_core_large.rs`, `src/registry/heap_core_alloc.rs`, plus
a new untracked `tests/alloc_zeroed_fresh_large_skip.rs`). I personally
confirmed it at least COMPILES clean under `--features "alloc-global
alloc-decommit"` (no errors, only pre-existing dead-code warnings), but have
NOT yet done a real zero-trust review (full diff read, test run, the critical
cache-hit-still-zeroes assertion verification) — the run errored out before
crush's own verification phase, so this diff's correctness is UNKNOWN, not
yet trusted either way.

#220 (batch/scoped API, P3-design) and #223 (Windows lazy-commit→production,
P3-hold) remain untouched, per their own design-first/hold gates from the
contemplation round.

## Active goal

A session-scoped `/goal` Stop hook is armed: **"реализуй оставшиеся задачи с
помощью /crush, между задачами делай коммиты, покрой их тестами"** (implement
the remaining tasks via /crush, commit between tasks, cover with tests) — set
by the user after the push, superseding an earlier "stop and ask" decision
for #220–#223. This hook is why the session did not stop after #222; it is
currently blocking on #221's crush-provider outage, not on lack of
direction.

## TaskList

### in_progress
- #221 R8-8 P2: консервативный zero-skip для alloc_zeroed — Large-path-only, `/crush` run failed on provider peak-hours refusal (zai, until 12:00), partial uncommitted diff in working tree, not yet reviewed

### pending
- #220 R8-7 P3-design: batch/scoped API — типизированная область (batch_scope), требует реального потребителя до фиксации сигнатур
- #223 R8-10 P3-hold: Windows lazy-commit → production — hold подтверждён; предусловие: удешевить segment-lifecycle (50–75×)

### recently completed
- #222 R8-9 (`9afba66`) — medium-classes judge-sweep verdict doc
- #219 R8-6 (`e9db718`) — payload_start/decommit_start split
- #218 R8-5 (`fa2c064`) — Unix/miri frontier=SEGMENT
- #216 R8-3 (`ec7ac34`) — alloc-segment-directory → production
- #215 R8-2 (`09237e0`) — authoritative directory-miss + self-heal
- #214 R8-1 (`af7b039`) — incremental directory sync
- #217 R8-4 (`c815927`) — R7 perf-doc staleness fix

## Decisions

- Reprioritized #220–#223 via a contemplation round (`/fm`): #222→P2-first
  (pure measurement, cheapest, could redirect the others' priorities), #221→P2
  (minimal form nearly free on top of #218/#219), #220 stays P3-design
  (needs a real consumer before signatures are fixed), #223 stays P3-hold
  (needs segment-lifecycle cost reduction first) — recorded in each task's
  updated `subject`/`description`.
- #221 deliberately scoped to Large-path only, NOT the small-class path — the
  small path's refill-batch/magazine entanglement was judged too risky for
  the task's actual payoff (the review's own "gains measured in multiples"
  framing points at large calloc-heavy workloads anyway).
- Peak-hours provider refusal is NOT treated as a retry-and-continue
  situation — per the `/crush` skill's explicit rule, waiting for user
  direction (possible explicit `--allow-peak-hours` authorization, or simply
  waiting until 12:00) rather than silently working around it.
- Raw bench log files from #222's measurement run were deliberately NOT
  committed (no repo precedent for checking in raw bench dumps under
  `docs/perf/`) — only the polished verdict doc was committed.

## Open questions

- **How to proceed on #221 right now**, given the crush provider is refusing
  until 12:00: (a) wait it out, (b) the user explicitly authorizes
  `--allow-peak-hours` for this one run, (c) I personally complete the
  partial diff by hand (Edit tools, no further crush delegation) given the
  design is already fully worked out, or (d) move on to #220's design phase
  (or leave everything paused) while waiting. Not yet answered.
- **Whether the partial uncommitted diff in the working tree is worth
  keeping** — it compiles, but has not been reviewed or tested; needs a
  decision (finish it, discard and restart fresh once unblocked, or review
  as-is) before any further action touches those 4 files.

## Repo state

```
 M docs/ARCHITECTURE.md
 M src/alloc_core/alloc_core.rs
 M src/alloc_core/alloc_core_large.rs
 M src/registry/heap_core_alloc.rs
?? _gad_suite.log
?? ci_watch.log
?? ci_watch2.log
?? ci_watch_ffd3215.log
?? docs/perf/_raw_baseline_off.log
?? docs/perf/_raw_baseline_off_reduced.log
?? docs/perf/_raw_medium_on.log
?? docs/perf/_raw_medium_on_reduced.log
?? tests/alloc_zeroed_fresh_large_skip.rs
```

```
9afba66 docs(perf): medium-classes 256 KiB-1 MiB GO/NO-GO verdict (R8-9)
38f4108 fix(test): exclude numa-aware from directory_authoritative_miss.rs (R8-2 follow-up)
f97cf1f chore(fmt): fix pre-existing rustfmt drift in crates/vmem/src/lib.rs
f919d5b docs(checkpoints): land session checkpoint file
e9db718 perf(segment-layout): split payload_start from decommit_start (R8-6)
```
