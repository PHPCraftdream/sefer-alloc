# Checkpoint — 2026-07-16 22:00 [round7-mid-B2 + crate-tasks-filed]

## Session summary

`sefer-alloc` (D:\dev\rust\sefer-alloc), a Rust allocator. This session:
(1) finished Round6 + a cross-version benchmark report (pushed through `461fe8f`);
(2) improved LOCAL build config (`incremental=false` in `D:\dev\rust\.cargo\config.toml`
+ `SCCACHE_CACHE_SIZE=30G`); (3) is DEEP into **Round7** — a 16-task plan (#149–164)
implemented via **o4h (Opus 4.7 high) agents** under an explicit operating mode;
(4) ran a side crate-extraction research (#165–169, 5 reports in
`docs/crate_extraction/`); (5) just analyzed those 5 reports and **filed 10 new
crate-extraction tasks (#170–179)**.

**Operating mode (user's explicit, IN FORCE):** implement via o4h agents, one task
at a time down the blockedBy chain; **the AGENTS run their own gates**
(fmt/clippy/tests/loom/miri) — the orchestrator does NOT re-run gates after an agent
(only reads the diff and commits); **orchestrator commits between tasks** (agents
leave work uncommitted); **ONE final full run** by the orchestrator at the very end.
Machine: ONE cargo/bench process at a time (16-thread host). `/goal` Stop hook ACTIVE.
Babysit cron ARMED (`# babysit tick`, every ~20m).

**Round7 progress — Workstream A (segment directory) COMPLETE + GO:** A0–A6 committed
(`f7d3a1c`→`0eb4794`) behind opt-in feature `alloc-segment-directory` (materialization
threshold 32). Measured 60–98× (S=256) / 166–254× (S=1023) refill-miss improvement.

**Workstream B (incremental Windows commit) IN PROGRESS:** B0 (`e5310a0`, vmem
`reserve_aligned_lazy`+`commit_range`) and B1 (`0c981d7`, `committed_payload_end`
frontier + `LAZY_FIRST_CHUNK=256 KiB` + temporary "commit whole remaining payload"
safety net) committed, behind opt-in Windows-only `alloc-lazy-commit`
(not(numa-aware) gate, eager fallback on Unix/miri). **#159 (B2) IS IN FLIGHT via o4h
agent `ab81fa720e1175abb`** — replacing B1's safety net with fallible incremental
grow-on-carve. Its UNCOMMITTED work is in the tree (3 tracked M files:
alloc_core_small.rs, alloc_core_small_diag.rs, os.rs + new test
`tests/lazy_commit_b2_grow.rs`) — NOT yet reviewed/committed. As of this checkpoint the
agent is STILL RUNNING (last file touch 21:39, 6 rustc/cargo active — deep in its gate
suite). Awaiting completion notification → then zero-trust diff review + commit → #160.

**New this turn — crate-extraction tasks #170–179 filed** from the 5 reports
(`docs/crate_extraction/{01_data_structures,02_concurrency,03_os_platform,04_test_infra,
SUMMARY}.md`). #170 is the R7-FINAL run (blocks all crate work — machine is one-process
and vmem is being reshaped by B right now). #171–179 follow SUMMARY's priority order.
All crate work: `cargo publish` needs separate explicit user approval; vmem 0.1→0.2
bump is sanctioned inside #172 by the plan.

**Deferred/skipped by report verdicts:** carved-mem, intrusive-once-stack,
iai-judge, criterion-arms (later/on-demand); gen-slot (retired tier), tcache-magazine,
bucket-(c) internals (skip). In-place hygiene (single-source sanitizer matrices, bench
MANIFEST lines, dead-hook detect) partially folded into #171/#176. All of this is now
(a) recorded in `docs/crate_extraction/DEFERRED_AND_SKIPPED.md` and (b) captured as a
research/decision task **#180 (CRATE-P10)** — read-only, produces
`P10_DEFERRED_VERDICT.md`, blockedBy #170.

**Two known pre-existing flaky tests** for the final run: `race_repro`
(STATUS_STACK_BUFFER_OVERRUN) and `backshift_no_latency_spike_at_threshold_boundary`
(wall-clock timing).

## Active goal

`/goal` Stop hook ACTIVE: "реализуй план, используй агентов @o4h, между тасками делай
коммиты, после агентов не делай прогоны (поручай это им). В самом конце сделаешь
прогон." Re-arm line:
```
/goal реализуй план, используй агентов @"o4h (agent)", между тасками делай коммиты, после агентов не делай прогоны (поручай это им). В самом конце сделаешь прогон.
```

## TaskList

### in_progress
- #159 R7-B2: fallible bump growth (commit-before-publish on all carve paths) — o4h agent ab81fa720e1175abb in flight; uncommitted work in tree, NOT yet reviewed/committed

### pending
- #160 R7-B3: decommit/recommit integration for lazy-commit mode (blockedBy #159)
- #161 R7-B4: correctness gates for incremental commit (+ fault-injection hook) (blockedBy #160)
- #162 R7-B5: incremental-commit GO/NO-GO + chunk-size sweep (blockedBy #161)
- #163 R7-C1: repeat TCACHE_CAP sweep (16/32/64) on post-RAD-5 cost model (no blocker)
- #164 R7-C2: pool-cap configuration sweep → presets (no blocker)
- #170 R7-FINAL: single full verification run (blockedBy #162, #163, #164)
- #171 CRATE-P1: publish-prep malloc-bench-rs + pin hook (blockedBy #170)
- #172 CRATE-P2: aligned-vmem 0.2 + numa-shim publish-prep (blockedBy #170)
- #173 CRATE-P3: new crate racy-ptr-cell (blockedBy #170)
- #174 CRATE-P4: new crate ring-mpsc + DirtyRouter (blockedBy #170)
- #175 CRATE-P5: new crate size-classes (blockedBy #170)
- #176 CRATE-P6: new crate globalalloc-model (blockedBy #170)
- #177 CRATE-P7: new crate tagged-index-stack (blockedBy #170)
- #178 CRATE-P8: new crate proc-memstat (blockedBy #170)
- #179 CRATE-P9: paired-ab family (blockedBy #170, #178)
- #180 CRATE-P10: research deferred/skipped candidates + in-place hygiene — file-or-drop verdict (blockedBy #170)

### recently completed
- #149–158 R7 spec + Workstream A (A0–A6) + B0 + B1
- #165–169 crate-extraction research (4 reports + SUMMARY)

## Decisions

- Round7 via o4h agents; agents self-run gates; orchestrator reviews diff + commits
  between tasks + does ONE final run (user's explicit mode).
- Crate work gated behind the R7-FINAL run (#170): machine is one-process at a time and
  vmem's lazy-commit API is actively reshaped by B3–B5, so #172 (vmem 0.2) especially
  cannot start until B settles.
- Crate priority order = SUMMARY's ranked list (P1 malloc-bench publish first — cheapest
  win; P2 vmem 0.2 — one coherent release absorbing 4 OS-lane items incl. R7-B4 fault
  injection; P3/P4 the ≥3×-reused protocols racy-ptr-cell + ring-mpsc; then
  size-classes, globalalloc-model, tagged-index-stack, proc-memstat, paired-ab).
- Deferred carved-mem/intrusive-once-stack/iai-judge/criterion-arms; skipped
  gen-slot/tcache/bucket-(c) — per report verdicts, not filed.
- `cargo publish` for any crate = separate explicit user approval; vmem 0.1→0.2 bump
  sanctioned within #172 by the plan.

## Open questions

- **#159 (B2) disposition:** agent still running at checkpoint time; on completion do
  the zero-trust diff review + commit, then start #160. If the agent died, re-message
  `ab81fa720e1175abb` or re-delegate B2 fresh.
- In-place hygiene + deferred/skipped candidates are now folded into research task #180
  (CRATE-P10) → `P10_DEFERRED_VERDICT.md`; the concrete file-or-drop sub-tasks get filed
  after that research runs (post-#170), with the user.
- Whether to commit the `docs/crate_extraction/` reports (5 files, untracked) and the
  accumulated `docs/checkpoints/` — user's call.
- Round7 not pushed since `461fe8f`; all R7 commits (`c0c011f`..`0c981d7`) local-only.
- Final full run (#170) owed at the very end per the goal; two known flakes to expect.

## Repo state

```
 M src/alloc_core/alloc_core_small.rs
 M src/alloc_core/alloc_core_small_diag.rs
 M src/alloc_core/os.rs
?? docs/crate_extraction/            (5 research reports — untracked)
?? tests/lazy_commit_b2_grow.rs      (B2 in-flight test)
(the 3 tracked M files + the new test = #159 B2's IN-FLIGHT, UNCOMMITTED, unreviewed
work; plus pre-existing untracked docs/checkpoints, docs/agent_reviews_*, ci_watch*.log)
```

```
0c981d7 feat(r7-b1): committed_payload_end frontier + lazy fresh-segment reserve
e5310a0 feat(r7-b0): vmem incremental-commit primitives (alloc-lazy-commit)
0eb4794 docs(r7-a6): segment-directory GO/NO-GO verdict — GO
6eb425a test(r7-a5): close the directory correctness-gate matrix
7cc3ccf perf(r7-a4): remote dirty routing — drain only dirty segments
```

`main` ahead of `origin/main` by all Round7 commits (`c0c011f`..`0c981d7`); last push
`461fe8f`. Babysit cron armed.
