# Checkpoint — 2026-08-20 11:52

## Session summary

Preparing the Rust crate `aligned-vmem` (v0.2.0, never published — crates.io still serves 0.1.0 under a different API) for its first real crates.io release. This session ran under a `/babygoal` loop: solve tasks with `@sl` sub-agents in isolated git worktrees, commit between tasks, then close with an independent `@oh` review that files new tasks.

**Wave SOL2 is done and landed** — the fourth independent audit's findings. Seven commits: #1209 (audit committed + card), #1211 (docs half `05557a6`, src half folded into `1522d25`), #1210 (`1f930e2`), #1212+#1213 (`1522d25`), #1214 (`dd1061e`), #1207 (`7db9f08`, NULL), #1208 (`e19976b`). Then the owner asked for the correctness index to be split into a folder (#1217, `4f4d9f4`), then for the `[T]` tier to be split again (#1221, `1525ccd`), then for that split to be **redone by subject instead of by line count** (#1222, `1b72e73`).

**Every agent deliverable this session had at least one real defect that its own report did not mention**, found by zero-trust review before commit: agent B broke a test name across a line inside a code span (would render with a space on crates.io); the #1217 agent LOST card item 93 entirely while its own census reported success (it compared output against its own stale input — the worktree predated `105cf53`); agent C's new paragraph failed `vmem-linux-android-pairing-guard.mjs`, a guard it never ran; agent A left an intra-doc link pointing at the item the same sentence said it deleted; the #1214 agent REPLACED a `Full history:` pointer instead of appending (caught only by `grep -c`: 35 → 34); the #1221 agent left a dangling pointer in `docs/perf/OPEN_ITEMS.md`; the #1222 agent left the old range-file names in `ARCHIVE.md`'s sibling list.

**Two of my own claims were refuted this session.** #1207 turned out to be NULL: I wrote "no guard anywhere looks at the word Unreleased" as verified fact, from a grep covering only `scripts/*.mjs` and `ci.yml` — the guard lives in `.github/workflows/release.yml:301-309` and its own comment names `## 0.2.0 - Unreleased` verbatim. And #1208 found item 90's Status line had been carrying "None of #1172/#1180/#1173/#1174 has landed yet" for a full wave after all four landed.

**In flight:** the third attempt at `npm run check` for `1b72e73` (background id `b16aj3a8i`). The first attempt failed with `0xc0000142 STATUS_DLL_INIT_FAILED` on eight unrelated link.exe invocations (environment, not code — sccache reports `Cache errors 0`). The second was cut mid-run at `alloc_core_differential`. **Diagnosed:** that test takes **130.43 s** in isolation and passes, printing nothing the whole time — the gate's silence there is what gets it killed, the fourth such kill this session. This is a real pre-push-gate fragility and must become a task regardless of how the current run ends.

`1525ccd` was pushed (landing SHA read from remote); its `Kani verification` is `success` and the main `CI` was still `in_progress` at last check — **not yet confirmed green**. `1b72e73` is unpushed, 1 commit ahead. A `# babysit tick` cron fires every 15 min.

## Active goal

> реализуй с помощью агентов @sl & worktrees, в конце (когда решишь все таски) проведи ревью с помощью агента @oh - после ревью заведи таски и остановись, между тасками делай коммиты.

## TaskList

### in_progress
- #1216 [SOL2] ЗАКРЫТИЕ: гейт → пуш → CI на landing SHA → ПЯТЫЙ независимый аудит `@oh` → завести задачи и ОСТАНОВИТЬСЯ

### pending
- #1190 [РЕШЕНИЕ ВЛАДЕЛЬЦА] HugeTLB-карты с гранулярностью ≠ 2 МиБ — единственный неотвеченный владелец обоих NO-GO
- #1218 `verify-commit-prefixes` слеп к не-ASCII путям: git-кавычки ломают `SEGMENT_AWARE_RE`
- #1219 Восстановить покрытие `DecommitOutcome::Refused` — нужен seam в `src/`
- #1220 У racy-ptr-cell / size-classes / tagged-index-stack / numa-shim НЕТ `CHANGELOG.md`
- #658 публикация 0.2.0 (blockedBy: #1190, #1207 ✓, #1216)
- #657 numa-shim republish (blockedBy: #658)
- #659, #660, #661 первые публикации; #662, #763 bench-scale-tool; #1091 perf-регрессия

### recently completed
- #1222 SPLIT3: пересборка [T]-яруса по смыслу + карта «item N → файл»
- #1221 SPLIT2: распил `TRACKED.md` по диапазонам номеров
- #1208 Status items 90/91/93 — последней в волне, намеренно
- #1207 NULL: гвардия существовала, ошибка была в моём grep
- #1214 C2+C3+P1/P3 — все шесть находок оказались уже отслеживаемыми
- #1217 SPLIT: распил индекса в папку; потерян и восстановлен item 93
- #1210 H1: UB в арифметике `base.add(64 MiB)`; тест удалён, не перегейчен
- #1212, #1213, #1211 — контракт `Advised`, двойное чтение page-size, doc drift
- #1215 CI зелёный на `dc2ecdd`

## Decisions

- **#1222: ось нарезки — смысл, но с обязательной картой «item N → файл».** Отвергнут вариант «просто переименовать файлы»: все 42 цитаты индекса имеют форму `item N`, и без карты тематическое имя перестаёт находиться в один шаг. Карта проверена машинно: 70 записей, 70 карточек, ноль расхождений в назначении файла.
- **#1222: файл-остаток вместо натяжки.** Items 45 и 49 оставлены в `TRACKED_misc.md`, а не слиты в выдуманную «гигиену кода» — на изобретённой под давлением таксономии сломался первый распил #1116.
- **#1210: тест УДАЛЁН, а не загейчен третий раз.** Цена записана прямо: покрытие `Refused` теперь нулевое на всех платформах, хуже прежнего. Обоснование — тест, которому для прохождения нужен UB, покрытием не является. Пять `tests/`-путей перебраны и отвергнуты с причинами; детерминизм требует seam в `src/` → #1219.
- **#1207: NULL, посылка признана моей ошибкой.** Отвергнут вариант «добавить вторую гвардию»: существующая покрывает случай, контрфакт на извлечённом теле это показал. Заголовок версии датируется перед `cargo publish`, не сейчас — держится гвардией, а не намерением.
- **Worktree не удаляю.** 33 штуки на диске; удаление — ровно сценарий #1073, где закэшированный бинарник с запечённым `CARGO_MANIFEST_DIR` начинает падать с `NotFound`.

## Open questions

- **#1190 — единственный неотвеченный владелец обоих вердиктов NO-GO.** Поддерживаем ли приём HugeTLB-карт с гранулярностью ≠ 2 МиБ (1 ГиБ)? Умолчание записано как НЕТ. Разведка: «ДА» касается ИСКЛЮЧИТЕЛЬНО приёма чужих карт (`os/unix.rs:1046` жёстко ставит `MAP_HUGE_2MB`); «НЕТ» сегодня не запрет, а умолчание — 1-ГиБ карта проходит 2-МиБ ассерт, и `decommit()` тихо съедает `EINVAL`. Рекомендация: НЕТ + три записи в контракт, кода не требуется.
- **Чем именно срезает гейт на `alloc_core_differential`** — тишина 130 с выглядит достаточной причиной, но прямого доказательства (какой именно watchdog) у меня нет. Первый сегодняшний срыв был другим механизмом (`STATUS_DLL_INIT_FAILED` при линковке). Оба средовые.

## Repo state

```
?? .check-1222.log
?? docs/checkpoints/2026-08-19-2140.md
?? docs/checkpoints/2026-08-19-2216.md
?? docs/checkpoints/2026-08-19-2300.md
?? docs/checkpoints/2026-08-20-0846.md
```

```
1b72e73 docs: task #1222 — re-split the [T] tier by SUBJECT, and pay the navigation cost the new axis creates instead of noting it
1525ccd docs: task #1221 — split the [T] tier by item-number range, and this time the census was run against committed history
e19976b docs: task #1208 (SOL2) — items 90/91/93 refreshed last, on purpose, because one of them had been carrying a false line for a full wave
7db9f08 docs: task #1207 (SOL2) — NULL: the guard I said did not exist has existed all along, and my own grep is what said otherwise
dd1061e docs: task #1214 (SOL2) — the fourth audit's six remaining findings were all already-tracked questions, and one of them is now NULL for the fourth time
```

`origin/main` = `1525ccd`; local `main` 1 commit ahead (`1b72e73`, unpushed, gate in flight).
