# Checkpoint — 2026-09-09 19:43

## Session summary

Continuation of the multi-crate crates.io publish-prep campaign. This
stretch covers: closing out proc-memstat's review rounds 3-5, actually
publishing proc-memstat 0.1.0 to crates.io, and running proc-probe through
its own round-1 review-fix cycle (four sequential `/wrush` tasks). This
segment picks up mid-checkpoint-history — the prior checkpoint
(`2026-09-09-0858.md`) covered proc-memstat rounds 2-4; this one covers
round 3 continuation through the actual publish and all of proc-probe.

**proc-memstat round 3** (`12cb915`, `71c8d43`, `f5cebc6`) — P2-1 (PowerPC
compile_error on the native lifecycle test target — SYS_EXIT became
`Option<c_long>`, unverified arch skips at runtime instead of failing
compile), P3-1 (legacy-kernel test capability policy), P3-2 (CSV RFC 4180
quoting), P4 batch (non-monotonic optional-field error contract via a new
`KibFieldLookup` three-state enum, prctl FFI width, RSS-scope docs,
grammar/label precision). All verified personally with real counterfactuals
(cross-compile to powerpc-unknown-linux-gnu, revert-and-confirm-failure
patches).

**A genuine flake found and fixed mid-session, not deferred**: `npm run
check` before the round-3 push failed
`budget_infeasible_deposit_after_base_eight_full_never_materialises_extension`
(`tests/large_cache_extended_budget_before_materialization.rs`) with `alloc
of 2143289344 bytes failed unexpectedly` — the exact ~2 GiB top rung of a
9-rung ladder already flagged as a LATENT risk in
`docs/correctness-open-items/TRACKED_test_flakiness.md` item 146 (this file
was explicitly named as one of four not-yet-patched siblings). Applied the
same `dbg_segments_reserve_failed_total()`-delta discrimination already
used in three sibling files (distinguishing a legitimate OS commit-limit
refusal from a real allocator bug), updated item 146's card. Commit
`11ec50d`, pushed alongside the round-3 work.

**Round 4** (`5f0a70f`) — P3-1 (the acquisition seam took `&str` not
`&Path`, so a non-UTF-8 TMPDIR path panicked test setup on Unix before
ever reaching the fallback logic under test — fixed with a `#[cfg(unix)]`
regression fixture using a raw invalid byte), P4-1 (the legacy-kernel SKIP
notice was invisible to a normal test run — libtest hides a passing test's
captured stdout, and the parent only surfaced captured child text on
failure — fixed with a result-file marker, mirroring
`tests/thread_leader_exit.rs`'s pattern), P4-2 (three stale comments:
`KibFieldLookup`'s dead-code justification wrongly called `status_convert`
a `#[path]` includer when it's an ordinary `mod` in production; a stale
"directory" reference in a test's module doc; CHANGELOG's "one ASCII
whitespace" vs the actual one-or-more grammar).

**Round 5 was discovered PARTIALLY DONE by someone/something else, not by
me** — a real, disclosed process incident. While the session was
mid-workflow, a new commit (`0a221ac`, review report only) and *uncommitted*
changes to `tests/non_utf8_name.rs`/`tests/status_read.rs`/`CHANGELOG.md`
appeared in the working tree without any action on my part. The diff was a
clean, targeted fix for round 5's own two P4 findings (SKIP notices moved
to direct `stderr` writes bypassing both libtest capture layers; the
non-UTF-8-path fixture's error classification narrowed to two specific
known filename-encoding errno values instead of any `fs::write` failure).
Rather than silently merge or silently discard this — either would violate
the "investigate unexpected state before acting" rule — **I stopped, asked
the user directly** ("what should happen to these uncommitted round-5
changes before publish?"). The user replied "мы закомитили — можешь
проверять" (already committed on their end, elsewhere — go ahead and
verify), which resolved the ambiguity. Zero-trust reviewed the resulting
commit (`67898f8`) exactly as if it were a sub-agent's output: full diff
read, WSL re-verification, one real defect found and fixed (a rustfmt line
that had drifted, `d882fbf`) before it could reach CI.

**Then, explicit user request**: "давай публиковать краайт - proc-memstat,
публикуй с помощью ci" (let's publish proc-memstat, publish via CI). This
was the real, irreversible step:
1. Stamped the CHANGELOG's `## 0.1.0 - Unreleased` header to a real date
   (`c583a49`) — `release.yml`'s own guard rejects "Unreleased" sections
   and requires an exact-once dated match; verified by re-deriving and
   running the guard's own grep/regex logic locally before trusting it.
2. Ran `npm run check` (full local gate) — green.
3. Pushed to `origin/main` (`11ec50d..c583a49`), watched CI (`main CI`
   workflow) go fully green on that exact landing SHA (46 jobs, only
   weekly/non-blocking jobs skipped).
4. Tagged `proc-memstat-v0.1.0` on the CI-green SHA, pushed the tag —
   `release.yml`'s tag-push trigger fired, its own CI-status guard
   re-confirmed green, the CHANGELOG guard passed, `cargo publish -p
   proc-memstat` ran for real (not dry-run) and succeeded: "Uploaded... /
   Published proc-memstat v0.1.0 at registry `crates-io`". Independently
   confirmed via a direct `crates.io` API query (with a proper User-Agent
   header — the API 403s without one) showing `default_version: "0.1.0"`.
   **proc-memstat is now live on crates.io.** Task #1911 marked completed.

**proc-probe round 1** — a NEW crate, not previously touched this session.
Sol-codex review round 1 (report `docs/reviews/2026-09-09-174540-proc-probe-review-sol-codex-run-1.md`,
commit `aab0174`) came back NO-GO: P2=3, P3=4, P4=3. Investigated the crate
(114-line `lib.rs`, 119-line `tests/protocol.rs`) before decomposing, since
this domain was genuinely new this session. Decomposed into four
sequential `/wrush` tasks matching the review's own recommended fix order,
babysit already armed, each task's diff personally reviewed line-by-line
and independently re-verified (fmt/clippy/test/rustdoc in both feature
configs, plus real counterfactuals) before merging and committing:

1. **`83b5de4`** (P2-1+P3-1) — the crate declared `#![no_std]` and a
   `std = []` feature that gated NOTHING (the `proc-memstat` dependency was
   unconditional, and itself an std-only crate), so `--no-default-features`
   never actually dropped the std requirement — a bare-metal
   `thumbv7em-none-eabi` build genuinely could not compile despite the
   crate's own manifest description promising a "no_std-friendly core".
   Fixed by making `proc-memstat` `optional = true` and wiring
   `std = ["dep:proc-memstat"]`. **Personally re-ran the exact
   counterfactual myself** (not just trusting the sub-agent's report):
   `git stash` the fix, confirmed the cross-build fails with the exact
   cited E0425/E0463 errors, `git stash pop`, confirmed it passes again.
   Also gated the now-orphaned std-only tests/rustdoc links to match.
2. **`a02ecbd`** (P2-2+P3-2) — every existing test built its own expected
   line from the library's own format string/constant, so emptying every
   `emit*` body or renaming `RESULT_PREFIX` to `"RESULTX"` would have
   passed every test. Fixed with a real re-exec'd-child test that captures
   ACTUAL stdout bytes against independently hardcoded expectations.
   Separately, the test's "semantically identical" claim about matching
   the real runner's ECMAScript regex was false (Rust `char::is_whitespace`
   and ECMAScript `\s`/`trim` disagree at the U+FEFF/U+0085 boundary) —
   fixed by running the ACTUAL regex through real Node.js (v24.12.0) against
   a shared corpus, verified live myself via `--nocapture`, matching the
   sub-agent's exact reported verdict string. **Personally ran both named
   counterfactuals** (empty emit bodies; RESULT_PREFIX rename) and confirmed
   the new tests genuinely fail, then confirmed clean restore.
3. **`0590ce9`** (P2-3+P3-3) — no CHANGELOG.md existed, which
   `release.yml`'s pre-publish guard hard-requires; added one describing the
   crate's actual post-fix state. Also added a `proc-probe-gates` CI job
   (single-OS, unlike proc-memstat's 3-OS matrix, since proc-probe holds no
   unsafe/FFI of its own) running fmt/clippy/test/rustdoc in both feature
   configs plus the bare-metal cross-build. **Personally replayed the
   release.yml CHANGELOG guard's shell logic** against the new file and
   confirmed YAML validity of the edited `ci.yml` (48 jobs) via WSL Python.
4. **`21898f1`** (P3-4+P4 batch, last task) — added a fallible
   `try_snapshot`/`SnapshotError` re-export (previously only the
   best-effort, all-zero-on-failure `snapshot()` was reachable through
   proc-probe alone — a real correctness risk for paired-A/B judges);
   documented the `emit*` family as explicitly UNCHECKED (chose this over
   validating input, given the crate's stated zero-cost/no-alloc design —
   an explicit policy judgment call, recorded and justified); added
   `# Panics` doc sections (deliberately did NOT build the review's fuller
   suggested sink/writer API — explicit scope discipline); fixed three
   doc/comment inaccuracies (README's dangling `t0`, an overclaimed test-
   coverage statement, `emit_ns`'s wrong "avoids a cast" framing).

All ten review findings across the four tasks are closed. proc-probe is
NOT yet published — task #1912 stays `pending`, same two-part gate as
proc-memstat had: a fresh review round to actually lift the NO-GO verdict
(code fixes alone aren't a new review), plus the user's own explicit
publish go-ahead.

## Active goal

None armed. No `/goal` is in force this session.

## TaskList

### pending
- #1912 proc-probe — prepare for crates.io publish (0.3.0 companion). All
  round-1 review findings fixed (commits `83b5de4`/`a02ecbd`/`0590ce9`/
  `21898f1`), but publish itself needs (a) a fresh review round to lift the
  round-1 NO-GO verdict, and (b) the user's explicit publish authorization
  — neither exists yet. Babysit ticks correctly report this as blocked,
  not stalled.

### recently completed (this session segment)
- #1911 proc-memstat — publish prep — **COMPLETED, and actually published
  to crates.io** (v0.1.0, confirmed live via direct API query)
- #1952-1955 proc-memstat rounds 3-4 review-fix tasks
- #1956-1959 proc-probe round-1 review-fix tasks (four sequential `/wrush`
  tasks, each independently verified)

## Decisions

- When uncommitted, unexplained changes appeared in the working tree mid-
  session (round-5 proc-memstat fixes from an unidentified concurrent
  source), stopped and asked the user rather than merging or discarding —
  matching this repo's established "investigate before acting on
  unexpected state" convention and the session's own earlier precedent
  (item #1916, "concurrent Sol-codex process" from an earlier segment).
- Treated the user's confirmation ("мы закомитили — можешь проверять") as
  authorization to zero-trust REVIEW that already-committed work exactly
  like a sub-agent's output, not as license to skip verification — found
  and fixed one real rustfmt drift before it could reach CI.
- For proc-probe's P4-1 (input validation policy), chose "document as
  unchecked" over "validate at the emit boundary", given the crate's
  stated zero-cost/no-alloc design goal and the fact the wire format is
  already test-pinned at every layer — an explicit, recorded judgment
  call, not a default.
  For P4-2 (panic/sink API), deliberately built only the panic-docs half
  and explicitly declined the fuller sink/writer API redesign — no
  measured need, and it would double the public surface right before a
  first publish.
- Did not push any of proc-probe's four fix commits — no explicit push
  request was given for that work (only proc-memstat's publish was
  explicitly requested and pushed).
- Every rush-produced diff this segment was independently re-verified
  end-to-end (fmt/clippy/test/rustdoc in every relevant feature
  configuration, cross-compiles where claimed, and — critically — the
  actual named counterfactuals re-run personally, not accepted from the
  sub-agent's own report) before merge and commit.

## Open questions

- Does the user want to authorize a fresh Sol-codex review round for
  proc-probe now that all round-1 findings are fixed? (Needed to actually
  lift the NO-GO verdict before any publish step — same gate proc-memstat
  went through across five rounds.)
- Does the user want `#1912`'s actual publish (tag + `release.yml` run) to
  proceed once/if a clean review lands, following the same pattern just
  used for proc-memstat (stamp CHANGELOG date, `npm run check`, push, wait
  for CI green on the landing SHA, tag, push tag, confirm via crates.io
  API)?
- No unresolved technical open items from this segment — everything
  discovered (the item-146 flake, the rustfmt drift, the round-5 mid-
  session mystery commit) was resolved to a clean, verified state before
  moving on.

## Repo state

```
(clean at time of writing)
```
```
21898f1 fix(proc-probe): fallible re-export, unchecked-contract docs, panic docs, doc precision
0590ce9 feat(proc-probe): add CHANGELOG.md and CI gates for the first publish
a02ecbd fix(proc-probe): test real emitted stdout bytes, verify parser model against real ECMAScript regex
83b5de4 fix(proc-probe): make the no_std promise actually true, gate tests/docs to match
aab0174 docs(reviews): review proc-probe, Sol-codex round 1
c583a49 docs(proc-memstat): stamp the 0.1.0 CHANGELOG section with the real release date
d882fbf style(proc-memstat): rustfmt the round-5 P4 fix
67898f8 test(proc-memstat): fix P4 skip reporting and fixture setup
```
Local `main` and `origin/main` are in sync through `c583a49` (the
proc-memstat publish commit, pushed + tagged `proc-memstat-v0.1.0`, tag
pushed). The four proc-probe commits (`83b5de4` through `21898f1`) are
**local-only, not pushed** — no push was requested for that work. Babysit
cron remains armed (session-only), correctly reporting `#1912` as blocked
on a human decision (review round + publish authorization), not stalled.
