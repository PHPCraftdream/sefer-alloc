# Checkpoint — 2026-08-25 23:10 [racy-ptr-cell publish-readiness campaign reached unconditional GO — 40 unpushed commits, publication itself still awaiting explicit owner go-ahead]

## Session summary

This session picked up from a prior numa-shim 0.2.0 publish (already completed and pushed earlier this session — confirmed live on crates.io, `max_version: "0.2.0"`, `origin/main` = `ee60cf6`) and then, per the user's explicit instruction ("numa-shim — давай сначала это опубликуем, потом будет другие задачи делать"), pivoted fully to `racy-ptr-cell`'s own publish-readiness campaign once numa-shim was confirmed done.

The user ran `/babygoal` with the standing instruction: run cycles of `@oh`-invoked review → file tasks → fix → repeat, until the review says GO, then stop before actual publication. Early in the racy-ptr-cell thread the user gave two scope-narrowing corrections that shaped the whole rest of the session: (1) delete any TaskList "wrapper" tasks for the review action itself — only file tasks for concrete target-repo edits; (2) never file a task for the actual publish step — "не заводи задачи на публикацию, я скажу когда публиковать" (I'll say when to publish).

What followed was **eight consecutive `@oh`-invoked review cycles** (plus two earlier, unprompted arrivals from a different reviewer, "Sol-codex", making 12 independent reviews total across the campaign — tracked as items 119–128 in `docs/correctness-open-items/TRACKED_publish_readiness.md`). Each cycle: launch `oh` agent with full context of all prior reviews and their closing commits (instructed each time to re-derive, not trust, the immediately-preceding fix — with an explicit "regression-hunting lens" from cycle 5 onward, after cycle 5 itself caught cycle 4's own fix introducing a fresh regression); personally re-verify every P1/P2 finding against source before filing a TaskList task; fix; verify (rustdoc/fmt/clippy/tests/loom/miri/bare-metal cross-builds/`cargo package` as relevant); commit; file/update the item card in `TRACKED_publish_readiness.md` plus the lookup row in `docs/CORRECTNESS_OPEN_ITEMS.md`; commit that too.

**A real, repeated failure pattern emerged and was eventually broken structurally.** The crate's `#[global_allocator]`/panic-site documentation paragraph was revised four cycles running (items 122→123→124→125→126), and cycles 5, 6, and 7 (items 124, 125, 126 in order — actually the exact numbering is: item 123's fix → item 124 found a regression in it → item 124's fix → item 125 found a regression in it → item 125's fix → item 126 found a regression in it) each found that the **immediately preceding** cycle's own one-clause prose patch had introduced a fresh, measurable defect (mostly around which "panic site" a bare numeral referred to, and whether a documented mitigation actually closed the hazard it claimed to — several of these were *measured*, not argued: real allocator probes built outside the repo, exit codes, timeouts proving an actual hang). Item 126's fix broke the chain by replacing the counting prose with a 4-row table containing no cross-referencing numerals at all — item 127 then found and closed the one leftover numeral in an adjacent sentence the table's insertion hadn't touched (the first finding in five cycles that was a genuine leftover, not a fresh regression). **Item 128 (the 8th `@oh` cycle, 12th review overall) returned the campaign's first unconditional GO**: 0 P1, 0 P2, only 4 P3s (doc-reference hygiene — a stale "below"/"above" direction word, a transitional sentence invalidated by the table's own insertion point, README/rustdoc block-order divergence, internal task-tracker IDs leaking into public rustdoc) — none touching behavior, caller obligations, or measured claims. The reviewing agent's own words: "Recommendation: publish 0.1.0 as it stands... none of them is worth delaying the publish for." None of the four P3s were filed as tasks (consistent with the session's P2-only filing convention and the review's own non-blocking framing) — they remain available for a future 0.1.1 pass.

**This satisfies babygoal's own terminal condition** ("Делай такие циклы пока ревью не скажет GO на публикацию") — the review-fix loop is done. Per the SAME babygoal instruction's other clause ("и остановись перед публикацией" — and stop before publication) and the user's separate explicit "I'll say when to publish" instruction, **no publish action has been taken or queued**. `/checkpoint` was invoked immediately after reporting the GO milestone to the user; nothing has happened since.

Two git-history hygiene corrections were needed mid-campcampaign, both handled by rewriting only commit MESSAGES (never content) via `git filter-branch --msg-filter` on the unpushed local range, verified via an empty `git diff` against the pre-rewrite backup ref each time: (a) two `docs(...)`-prefixed commits that touched `Cargo.toml`'s non-comment description/keywords fields were retitled `fix(...)` (matching this repo's own `verify-commit-prefixes` R30-12 lint, which is part of `npm run check`); (b) one `docs(...)`-prefixed commit that edited an actual `compile_error!` macro string (not a comment) got the same treatment. `npm run check` (the full local gate: fmt, clippy across 6 rows, tests, iai, the commit-prefix lint) was run and confirmed ALL GREEN after essentially every remediation wave.

## Active goal

The standing `/babygoal` instruction from this session (not a `/goal` Stop-hook — babygoal doesn't use that mechanism):

> давай переключимся на racy-ptr-cell - веди циклы ревью @oh, заведение тасок, решение тасок, пока ревью не скажет go. Готовим крейт к публикации, ищем поломки памяти, ub, пахнущий код, возможности ускорения, ошибки, неточности и тд. Ревью пишем в файл с именем claude и временем создания. Между задачами делаем коммиты. Ревью файлы тоже включаем в коммит сразу после ревью

**This condition is now satisfied** — item 128's review (`docs/reviews/claude-2026-08-25-2247.md`) returned an unconditional GO. The babysit cron that was armed for this loop (`ba6c3e80`) was already deleted earlier in the session when its own TaskList went empty (a distinct, shorter-lived babysit instance tied to the numa-shim/racy-ptr-cell-cycle-1 phase, not re-armed for the later cycles — the later review cycles 2-8 were driven by direct conversational continuation, not by babysit ticks).

## TaskList

### in_progress
(none)

### pending
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #1091 [perf-regress] Найти и устранить регрессию ~10-13% Ir в диапазоне 42d8d223..42d42061 (item 56)

### recently completed
- #1383 [racy-ptr-cell] "третий panic site" противоречит собственной таблице (review-9 F1, P2, последний числительный-хвост)
- #1382 [racy-ptr-cell] Структурная таблица panic-sites вместо счётной прозы (review-8 F1, P2, 4-я регрессия подряд)
- #1381 [racy-ptr-cell] Нет compile_error! guard'а для no-CAS bare-metal таргетов (review-7 F2, P2)
- #1380 [racy-ptr-cell] Ложный parenthetical: "три panic sites" не все static-строки (review-7 F1, P2)
- #1379 [racy-ptr-cell] Нет rustdoc-гейта в CI (review-6 F2, P2)
- #1378 [racy-ptr-cell] set_hook не спасает от formatted panic-сообщений (review-6 F1, P2)
- #1377 [racy-ptr-cell] --cfg loom ломает headline static-пример (review-5 F2, P2)
- #1376 [racy-ptr-cell] panic=abort не останавливает аллокацию в panic-runtime (review-5 F1, P2)
- #1375 [racy-ptr-cell] Добавить miri-строку в ci.yml (review-4 F3, P2)
- #1374 [racy-ptr-cell] Ложный latency-claim: fork()/signal handler не упомянуты (review-4 F1, P2)

(also completed earlier this session, not listed above for space: #1365-#1373 — numa-shim publish-adjacent + racy-ptr-cell review-2/review-3 wave fixes, #1358-#1360 — racy-ptr-cell's very first review's F1/F2/F3.)

Deleted this session (wrapper/publish tasks the user explicitly told me not to file): #659 ("racy-ptr-cell — first publish to crates.io" umbrella), and an early 4-task review-cycle scaffold (#1361-#1364) the user told me to delete in favor of filing only concrete fix tasks.

## Decisions

1. **File TaskList tasks only for P1/P2 findings, never for the review action itself or for the eventual publish step** — established early via explicit user correction, held for the entire 8-cycle campaign.
2. **When ≥3 P3 findings in the same reviewed area are explicitly recommended by the review to be folded into one commit (and are cheap/non-blocking), bundle them into the same task as the P2 finding** rather than filing 4 separate tasks — done for items 126 and 127's remediation waves (tasks #1382, #1383), matching each review's own explicit "fold these in" recommendation.
3. **Rewrite commit MESSAGE PREFIXES (never content) via non-interactive `git filter-branch --msg-filter` when the local `verify-commit-prefixes` lint flags a `docs(...)`-labeled commit that touched non-comment code** — done twice, both times verified safe via empty `git diff` against the pre-rewrite state and confirmed the range was fully unpushed first.
4. **Do not file the 4 P3s from item 128 (the GO review) as tasks** — the review's own recommendation is "publish as-is," and filing more work after a GO verdict would run against both the P2-only convention and the spirit of the babygoal loop's own terminal condition.
5. **Stop the review/fix loop now that GO was reached; do not proceed to actual publication** — babygoal's own "stop before publication" clause plus the user's separate standing "I'll say when to publish" instruction.

## Open questions

- None outstanding requiring user input on the racy-ptr-cell work itself — the review loop is complete. The only remaining question is entirely the user's to answer in their own time: **when to authorize the actual `cargo publish` / version-bump / tag-push sequence for racy-ptr-cell**, and separately, **whether/when to decide item 28's one-way-door metadata question** (crate name `racy-ptr-cell` reading as "has data races"; `Cargo.toml`'s `description` field, now 495 characters, up from 383 when first flagged) before that publish happens — both are pre-existing, already-tracked items, not new.
- Not yet asked, worth surfacing next session if not addressed here: **the CHANGELOG's `## 0.1.0 - Unreleased` header must be date-stamped before any tag push** — `release.yml`'s guard fails closed on this and was independently re-confirmed by five consecutive review cycles (items 123-127) to actually block a real tag push, not just a theoretical gap.

## Repo state

```
?? .check-1262-release.log
?? .check-1299-wave.log
?? .check-1321-wave.log
?? .check-1334-wave.log
?? .check-1335-retry.log
?? .check-1342-wave.log
?? .check-1348-wave.log
?? .check-1353-wave.log
?? .check-1357-wave.log
?? docs/checkpoints/2026-08-24-1556.md
?? docs/checkpoints/2026-08-24-2034.md
?? docs/checkpoints/2026-08-25-0103.md
?? docs/checkpoints/2026-08-25-1052.md
?? docs/checkpoints/2026-08-25-1522.md
```
(scratch `.check-*.log` files and prior checkpoints only — this checkpoint's own file is not yet listed, per the tool's snapshot-before-write timing.)

```
4a7189a docs: file item 128 -- racy-ptr-cell review cycle 8, UNCONDITIONAL GO, babygoal cycle terminal condition met
dbdbc36 docs(review): racy-ptr-cell publication readiness review cycle 8, eighth oh audit -- unconditional GO
c2dfaec docs: file item 127 -- racy-ptr-cell review cycle 7 (seventh oh audit), the P2 plus three co-filed P3s CLOSED; regression chain broken
de7da71 docs(racy-ptr-cell): task #1383 -- remove the last stale numeral, fix table row, reorder rustdoc sections, msp430 error class (review-9 F1/F2/F3/F4)
4c41fe9 docs(review): racy-ptr-cell publication readiness review cycle 7, seventh oh audit
```

`origin/main` is still at `ee60cf6` (the numa-shim Cargo.lock fix, confirmed live/published earlier this session) — **40 commits ahead, entirely unpushed**, covering the full racy-ptr-cell campaign (items 119-128, tasks #1358-#1360 and #1365-#1383) plus two commit-message-only history rewrites (`filter-branch`, content-verified identical via empty diffs against backup refs both times). `npm run check` (full local gate) was launched again after the item-128 commit to re-confirm green post-campaign; its background output had not yet appeared when this checkpoint was written — re-run it on resume if not already confirmed. Nothing has been pushed to `origin` for the racy-ptr-cell work; no publish action has been taken.
