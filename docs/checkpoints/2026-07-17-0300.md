# Checkpoint — 2026-07-17 03:00 [round7-done+benched+pushed]

## Session summary

`sefer-alloc` (D:\dev\rust\sefer-alloc), a Rust allocator. This long session:
(1) implemented the full **Round7** plan (#149–164) via **o4h agents** (one task
at a time, commit between tasks, agents self-gate, orchestrator reviews + commits);
(2) ran the ONE final verification run (#170) personally; (3) **pushed everything
to origin/main** — HEAD and origin are in sync at **`32765e2`**; (4) removed the
babysit cron; (5) ran comparative + cross-version benchmarks and wrote them into
docs + README. **All work is committed and pushed. Nothing is in flight.**

**Round7 verdicts:** A (per-class segment directory) = **GO** (opt-in
`alloc-segment-directory`; measured ~166–180× refill-miss at high S, parity at
S≤3). B (incremental Windows commit, opt-in `alloc-lazy-commit`) = mechanism
correct + fully tested, **headline NO-GO** (primordial is always eager and
dominates first-heap commit) → deferred as **#181 (R7-B6, lazy primordial)**.
C1 (TCACHE_CAP) = NO-GO, stays 16. C2 (pool-cap) = default stays 4/16 MiB, three
documented presets.

**The #170 final run caught & fixed THREE problems** (all committed): (a) `a834fca`
lazy-commit tests didn't gate off the `numa-aware` eager fallback → failed under
`--all-features`; (b) `42f8343` (R7-A7) a REAL correctness bug — B3's
decommit-on-pool-admission forgot `clear_segment_directory`, desyncing the
directory at the top medium class under `--all-features`; (c) `49046ef` the
`miri.mjs` runner passed space-separated `--features` under `shell:true`, so
Windows re-split them → 3-feature entries hard-errored and 2-feature entries
silently ran **0 tests** (VACUOUS green). Fixed by comma-joining. Final gate state:
`npm run check` ALL GREEN; loom PASS; miri 14/14 NON-vacuous PASS; no UB.

**Benchmarks (this session's tail work, all committed+pushed):**
- `docs/perf/R7_BENCH_RESULTS.md` (`5511af0`) — directory sweep ON/OFF (the A win)
  + canonical 3-arm (SeferAlloc vs mimalloc vs System).
- `docs/perf/R7_CROSS_VERSION_BENCH.md` + README subsection (`32765e2`) — fresh
  same-harness **0.2.1 vs 0.3.0** run (via the `bench/0.2.1` branch, tip `5edb3d9`)
  with mimalloc/System reference and vs-0.2.1 / vs-mimalloc ratio columns, all 7
  bench:table groups. Headline: churn +1.0–2.3× over 0.2.1, **segment_decommit_cycle
  ~318× faster**, working_set up to 4×; vs mimalloc churn up to 10.7× (loses cold
  small-size). Root causes documented: the **~318×** = Mechanism-2 small-segment
  hysteresis pool + OPT-E large cache (retain emptied segments; reuse = pool-pop,
  not an OS reserve+commit); the **~128 MiB→~6 MiB first-alloc commit-charge cut**
  = the chunked Registry (R6-OPT-P0-2, `e4b3e1d`+`8dc6fe8`), NOT R7-B lazy-commit.

**State:** babysit cron REMOVED. `/goal` condition (implement plan + final run)
SATISFIED. Bench logs in /tmp (xver_current.log, xver_021.log, bench_dir_on/off.log,
r7_miri_full.log) — not in repo. Working tree clean (only ci_watch*.log untracked).
On branch `main`, synced with origin.

## Active goal

`/goal` Stop hook may still be armed: "реализуй план, используй агентов @o4h,
между тасками делай коммиты, после агентов не делай прогоны (поручай это им). В
самом конце сделаешь прогон." — SATISFIED (Round7 done, final run green, pushed).
User clears it with `/goal clear`. Benchmark + docs work was a separate request,
also done.

## TaskList

### in_progress
- (none — everything requested is done)

### pending — crate-extraction phase, NOT started (awaiting explicit user go)
- #171 CRATE-P1 malloc-bench-rs publish-prep
- #172 CRATE-P2 aligned-vmem 0.2 + numa-shim publish-prep
- #173 CRATE-P3 racy-ptr-cell
- #174 CRATE-P4 ring-mpsc + DirtyRouter
- #175 CRATE-P5 size-classes
- #176 CRATE-P6 globalalloc-model
- #177 CRATE-P7 tagged-index-stack
- #178 CRATE-P8 proc-memstat
- #179 CRATE-P9 paired-ab (blockedBy #178)
- #180 CRATE-P10 research deferred/skipped + in-place hygiene
- #181 R7-B6 lazy-commit the primordial (deferred; salvages B's headline; bootstrap-critical, higher risk — do NOT start without explicit go)

### recently completed
- #184 X-VER-DOCS (cross-version results → README + docs)
- #183 X-VER-BENCH (0.3.0 vs 0.2.1 vs mimalloc/System, all bench:table groups)
- #182 R7-A7 directory medium-class desync fix
- #170 R7-FINAL (npm run check ALL GREEN + loom + miri 14/14 non-vacuous)
- #149–169 all Round7 A/B/C + crate research

## Decisions

- Push everything; pause the crate phase; remove babysit — user's explicit choices
  across the session. Now fully synced at `32765e2`.
- Cross-version bench driven INLINE (not via agents): the global rule forbids
  spawning sub-agents without an explicit request, and /crush quota is exhausted;
  Round7's o4h-agent authorization was scoped to Round7, not this bench request.
- Docs-only pushes skip a redundant `npm run check`: verified the origin..HEAD
  delta was 100% markdown and the code is byte-identical to the last ALL-GREEN
  run at `49046ef`.
- New `R7_CROSS_VERSION_BENCH.md` created rather than clobbering the R6 one
  (keeps R6 history intact).

## Open questions

- **Start the crate-extraction phase (#171+)?** Paused, awaiting explicit user go.
  It involves crates.io publish decisions + a vmem 0.1→0.2 bump (both gated on
  explicit approval in the task descriptions).
- **#181 (lazy primordial)** — the only path to salvage B's headline first-heap
  win; higher-risk bootstrap work, filed but not to be started without a go.
- **Two stale base-revision stamps** in `R7_TCACHE_SWEEP.md` (C1) and
  `R7_POOL_CAP_PRESETS.md` (C2): both say base `e53f1f7`, which is 123 commits
  back / pre-Round7 (the writing agents mis-stamped). Conclusions valid; the
  stamp is wrong. Offered to fix; user hasn't said yes.
- Whether to re-run `bench:table` on a quiet machine for publication-grade numbers
  (current numbers carry ±15–20 % host noise).

## Repo state

```
(working tree clean; only ci_watch*.log untracked)
```

```
32765e2 docs(r7): cross-version bench 0.2.1 -> 0.3.0 (+ README subsection)
5511af0 docs(r7): benchmark results (directory win + 3-arm comparison) + checkpoint
49046ef fix(miri-runner): comma-join features so multi-feature entries aren't vacuous
42f8343 fix(r7-a7): clear directory bits on the lazy-commit pool-admission path
a834fca test(r7-b): gate lazy-commit tests off the numa-aware eager fallback
```

`main` synced with `origin/main` at `32765e2` (nothing unpushed). Babysit cron
removed. Crate phase not started.
