# Checkpoint — 2026-09-08 09:30

## Session summary

This session continued the `sefer-alloc` publish-prep campaign for
`crates/globalalloc-model`, working through an ongoing multi-round independent
review cycle with a second reviewer identity ("Sol-codex" — a separate process
that commits its own review reports directly into this repo). Rounds 5-follow-up,
6, 7 and 8 were all handled here. Two of those rounds (6 and 8) appeared while I
was mid-work and were only noticed afterwards — round 6 in particular reviewed my
own three commits and raised a P2 I had merged, so it is worth checking
`docs/reviews/` for new files at the start of any continuation rather than
assuming the last-seen round is the latest.

Round-5-follow-up work (commits `367c975`, `f6fde25`, `cd6acbe`) fixed the probe's
realloc byte accounting, split one mislabeled memory scenario into three, pinned
the enum/boxed shrink trajectory step-for-step including `complicate`, and dropped
over-broad repeat/permutation and unwritten-memory promises from the docs. The
substantive finding there: separating the regimes REVERSED a published claim —
at a plain successful draw the boxed form uses LESS memory (−221,196 B default,
−390,009 B single); "the enum uses less memory" holds only in shrink regimes.

Round 6/7 (the same six findings, re-confirmed by round 7 against unchanged code)
were closed by commits `27502c5`, `7b5fb08`, `a05a848`: the P2 stopper (the probe's
self-check passed `isize::MAX` to `realloc` on an align-8 layout, violating the
rounded-size precondition — replaced by a controlled-refusal backend on a small
valid request); S3 rewritten to drive proptest's real `TestRunner::run_one`; and
the inverted memory-direction claims plus the allocs/op numerator confusion fixed.
Round 8 then reviewed exactly those three commits and returned **GO** (P0-P3 = 0,
two non-blocking P4s), lifting the previous NO-GO.

Everything is pushed: `origin/main` = `81625b1`, local is level with it. CI on that
landing SHA: `globalalloc-model` and Kani green; the main `CI` workflow was still
in progress at checkpoint time (11m16s) and has NOT been confirmed green — a
background `gh run watch` was armed for it.

Currently in flight: task #1932 (round-8's two P4s) is running in a `/rush` agent
in worktree `worktrees/gam-r8-p4`, branch `gam-r8-p4`. It had not modified any
files as of the last tick.

Also this session, `/task` was used to plan the flake-fixing effort: five distinct
flaky tests were enumerated from `docs/correctness-open-items/TRACKED_test_flakiness.md`
(the authoritative index) plus one I found empirically that has no card yet, and
tasks #1933-#1937 were registered for them. Those are PLANNED ONLY, not started.

## Active goal

None (`/goal` not set).

## TaskList

### in_progress
- #1932 globalalloc-model round-8 P4-1/P4-2: pin S3's runner config against env-driven early stop, and assert TOTAL_BYTES increments in the self-check — running in `/rush` session `gam-r8-p4`

### pending
- #1911 proc-memstat — prepare for crates.io publish (0.3.0 companion)
- #1912 proc-probe — prepare for crates.io publish (blockedBy #1911)
- #1925 root crate: fix r14_7_max_segments_ceiling flake (item 143) — mechanism established (ERROR_COMMITMENT_LIMIT), fix designed, not implemented
- #1933 flake item 14: xthread_large_free_tiny_size_huge_align_is_reclaimed — fails only when its own file runs in parallel (the ONLY one reproducible on demand; start here)
- #1934 flake item 12: xthread_large_double_free_no_double_reclaim reclaim-count race (50 expected, 42 once)
- #1935 flake item 96: wasted_dirty_drains ratio threshold (26.7% vs 20%) is statistically fragile
- #1936 UNTRACKED flake: best_fit_picks_tightest_slot_across_base_extension_boundary — 380 MiB alloc fails with 33.9 GiB free; mechanism NOT established, needs its own card
- #1937 flake closure gate (blockedBy #1925, #1933, #1934, #1935, #1936)

### recently completed
- #1931, #1930, #1929 — round 6/7's six findings
- #1928, #1927, #1926 — round-5-followup findings
- #1924, #1923 — round 5

## Decisions

- Kept the `SizeStrategy` enum rather than reverting it, but rewrote the
  justification to state honestly that it does NOT win on every axis: it wins
  allocation COUNT at every config and memory in the shrink regimes at the
  default config, and LOSES memory at a plain successful draw and at the single
  config in every scenario. The reviewer explicitly warned against reverting on
  finding-count alone; the decision rests on the corrected, correctly-labeled A/B.
- Recorded commit `f66b9f0`'s R30-12 prefix mis-slot in the lint's grandfather
  list plus item 78 sub-card 15 rather than rewriting pushed history, per the
  rule's own non-retroactive posture.
- Did NOT commit `docs/artifacts/2026-09-07-2124-unattributed-strategy-rs-worktree-edit.diff`
  (an unattributed edit found in the working tree, source never identified). It
  stays untracked on disk; commit `8874ce5` records its sha256 and why.
- Skipped the full `npm run check` before the last push and reproduced its steps
  by hand instead, stating so explicitly — it fail-fasts on the r14_7 flake before
  reaching half its steps. All six clippy rows, ten guard scripts, fmt and three
  of four test rows were run individually and were green.
- Treated `/task`'s "fix all flakes" as an EXTENSION of the existing plan, not a
  replacement (#1925 was already a flake task; #1932 was mid-flight).

## Open questions

- The main `CI` workflow on landing SHA `81625b1` was still running at checkpoint
  time. It must be confirmed green — do not assume it.
- Two root-crate tests fail on this machine for environment reasons and are NOT
  caused by any commit here: `r14_7_max_segments_ceiling` (item 143, mechanism
  confirmed) and `best_fit_picks_tightest_slot_across_base_extension_boundary`
  (mechanism NOT established — 380 MiB failing with 33.9 GiB free does not fit
  the commit-limit explanation; do not conflate the two).
- Whether `scripts/check-all.mjs` should run its cheap guard scripts BEFORE the
  long environment-sensitive test rows — the current order let a real
  commit-prefix defect reach origin/main because the flake aborted the gate first.
  Raised inside task #1937 as a judgement call, not yet decided.
- Whether a Sol-codex round 9 should be expected after #1932 lands.

## Repo state

```
?? docs/artifacts/
```
```
81625b1 docs(reviews): globalalloc-model Sol-codex round 8
a05a848 docs(globalalloc-model): fix the inverted memory directions and name the allocs/op numerator
7b5fb08 bench(globalalloc-model): drive S3 with proptest's real shrink protocol, time S1's current(), and stop calling a runtime lockfile build identity
27502c5 fix(globalalloc-model): stop the probe self-check from violating GlobalAlloc::realloc's size precondition
edec7fa docs(reviews): globalalloc-model Sol-codex round 7
6cb3f2a docs(reviews): globalalloc-model Sol-codex round 6
```
Local `main` is level with `origin/main` (0 ahead, 0 behind).
