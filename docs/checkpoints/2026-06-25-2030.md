# Checkpoint — 2026-06-25 20:30 [Phase 12 sound+committed; Phase 12.6 shard-race fix (Variant 2) building]

## Session summary

`sefer-alloc` — a safe handle-store turned into a real, fast, safe general-purpose
allocator (mimalloc-class drop-in). This long arc drove **Phases 12 (production
MT trust) and 13 (speed)** of `docs/MALLOC_PLAN_PHASE12-13.md` via **crush
sub-agents** (one at a time, serialized — two concurrent crush runs clobber the
shared cargo build, learned the hard way), with **zero-trust review before every
commit** (re-run tests/loom/clippy MYSELF; claim is not a receipt).

**Committed to main:** Phases 12.1–12.5 (`abe5610` is the keystone:
**heap-as-shard isolation → a SOUND multithreaded `#[global_allocator]`** — the
installed allocator no longer aborts/segfaults under libtest; verified MT gate
green x3). Phase 13.1 (`143b8be`: O(1) `SIZE2CLASS` lookup replacing the per-alloc
linear scan, non-vacuous test caught a real off-by-one).

**The open race (the whole current focus).** Phase 12.5 ships a SOUND *workaround*
not a fix: `drain_thread_free` **DISCARDS** cross-thread-freed blocks (bounded RSS
leak) instead of reclaiming them to the `BinTable`, because the naive reclaim
**SEGFAULTS** (`STATUS_ACCESS_VIOLATION`). The user pushed hard: "if we have shards,
where's the race? — fixing bugs and moving on is wrong; fix the shards." This was
RIGHT and exposed that I'd jumped to a fix (generation-tag) without root-causing.
A dedicated **instrumented root-cause crush run** then PROVED the cause
(`ea3a4ba`, `tests/race_repro.rs` is the committed counterfactual gate +
`docs/RACE_DRAIN_RECLAIM.md` §8): **suspect #2 — the intrusive TFS word at the
slot-reuse handoff.** A block's first word is contended between "in-flight TFS
entry" (per a cross-thread freer) and "reused by the new slot owner", because the
TFS head is a stable per-slot address; trace caught `next=0x6c64657463657078`
(ASCII `xpectdl` user data read as a free-list pointer). Suspects #1 (fallback)
and #3 (routing) REFUTED. Counterfactual: reclaim → 3/3 segfault, discard → 3/3 OK.

**The fix being built right now** (crush session `phase12-6-fix`, task #33): an
`/oxx` contemplation (grounded in `segment_header.rs` — BinTable already stores
OFFSETS not pointers) chose **Variant 2 — a per-segment, self-hosted, bounded
MPSC ring of u32 offsets in the segment metadata next to BinTable.** Cross-thread
freer pushes the block's *offset* into the segment's ring (never touches the
block word); the owner drains the ring → BinTable (single-writer). The contended
word vanishes → reclaim is single-writer → race eliminated by construction
(restores the original `ShardedRegion` 7b discipline: queues carry offsets, never
poison the object). M5-clean, overflow→bounded discard. Bonus: also dissolves the
future M11 decommit race. Brief at `.crush/stdin/phase12-6-fix.prompt`. Crush is
LIVE (already editing node.rs + segment_header.rs). Gate: `race_repro` must pass
under reclaim x5 + a new loom MPSC-ring counterfactual + all configs + clippy.

Mid-session interventions of note: a 35-min "hang" turned out to be crush stuck on
a compile typo then a deadlocked stress test; I killed two concurrently-running
crush sessions that were clobbering the build and serialized them; reverted a
stray `Cargo.toml race-diag` and a stray drain-restore that leaked across the kill.

## Active goal
`/goal drive the campaign to victory, friend` — IN FORCE (Stop hook fires each turn-end).
Condition: the whole campaign delivered/committed/gated. NOT met (Phase 12.6
fix building; Phase 13.2–13.7 + heavy gate untouched).

## TaskList
### in_progress
- #33 Phase 12.6: eliminate cross-thread-free reclaim race — non-intrusive per-segment remote-free ring (Variant 2)  (owner crush, session phase12-6-fix)
### pending
- #27 Phase 13.3: inlined arithmetic own-thread free
- #28 Phase 13.4: two-list free (local_free + thread_free)
- #29 Phase 13.5: refill batch tuning
- #30 Phase 13.6: heap==core pinning
- #31 Phase 13.7: macro-bench (mstress/larson) + refresh MALLOC_BENCH
- #32 Heavy gate: fuzz + aarch64 CI + ThreadSanitizer
- #35 M11 epoch-guard for M6 decommit-safety + M6 decommit + soak  (blockedBy #33)
### recently completed
- #21 12.1 segment-centric free state · #22 12.2 HeapRegistry · #23 12.3 raw-TLS+fallback · #24 12.4 adoption (loom) · #25 12.5 heap-as-shard sound MT · #26 13.1 O(1) lookup
### deleted
- 1 (#34, re-created as #35 to fix a circular blocker)

## Decisions
- **Fix the shard race with Variant 2 (non-intrusive per-segment MPSC ring), NOT
  Variant 1 (generation-tag).** Variant 2 eliminates the contended word (root
  cause) and restores the ShardedRegion discipline; Variant 1 only masks it and
  hits a "where to store the 32-bit generation" wall (4 spare pointer bits).
- **Discard-leak stays shipped until the fix lands.** It is SOUND (no UAF), just
  leaks cross-thread frees. Do NOT re-attempt the naive drain restore (segfaults,
  proven — FINDINGS §8).
- **Serialize crush runs.** Two concurrent runs share the cargo target and clobber
  each other (build lock, half-written files). One at a time.
- **Root-cause before fix.** The user's correction; jumping to a fix without a
  proven interleaving wasted a cycle. `race_repro.rs` is now the deterministic gate.
- **Phase 13 paused at #26.** The user prioritized "fix the shards" over advancing
  Phase 13; #27–#31 wait until the race is eliminated.

## Open questions
- Will Variant 2 pass `race_repro` under reclaim? (the whole hypothesis — pending
  crush + my zero-trust). If a residual race remains, the trace will say where.
- RING_CAP sizing + overflow-leak bound — to be confirmed by the build.
- After #33: resume Phase 13 (#27→#31) then #35 (M11/decommit) then heavy gate #32.

## Repo state
```
 M src/alloc_core/node.rs              (crush #33 in-flight — ring atomics)
 M src/alloc_core/segment_header.rs    (crush #33 in-flight — RemoteFreeRing in metadata)
```
(uncommitted = crush's live #33 work; tree was clean at HEAD before it started)
```
ea3a4ba test+docs: race_repro.rs (counterfactual gate) + confirmed root cause — intrusive TFS word at slot-reuse handoff (#33)
143b8be feat(phase13.1): O(1) size-class lookup — const SIZE2CLASS replaces the per-alloc linear scan
25b78cb docs(race): add §7 — root-cause-first; suspect the shared fallback heap
86e4860 docs: RACE_DRAIN_RECLAIM.md — the open cross-thread-free reclaim UAF
4f40067 docs(findings): #8 drain-reclaim naive restore SEGFAULTS — hypothesis falsified
```
Remote `origin` main: last pushed was `996723b` (Phase 11) per earlier checkpoints;
the whole Phase 12–13 arc (`8638a65..ea3a4ba`) is LOCAL, NOT pushed.
