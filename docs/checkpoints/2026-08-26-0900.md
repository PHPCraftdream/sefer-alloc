# Checkpoint — 2026-08-26 09:00 [racy-ptr-cell publish-readiness campaign — two consecutive unconditional GOs, 76 unpushed commits, publication itself still awaiting explicit owner go-ahead]

## Session summary

This session continues the long-running `[audit-vmem]`/publication-readiness campaign for `racy-ptr-cell` in `D:\dev\rust\sefer-alloc`. It picks up directly from a prior `/checkpoint` (`2026-08-25-2310.md`), which had recorded the campaign's first unconditional GO (item 128, `@oh`-driven cycle) and 40 unpushed commits.

The user then ran a fresh `/babygoal` that switched the reviewing agent identity from `@oh` to `@fh` (Fable) while keeping every other convention (read-only review, file-task-then-fix cycle, review files named `claude-<timestamp>.md`, commit between tasks). That `@fh` cycle (item 130) returned an immediate unconditional GO — a second confirmation the crate was clean under a different reviewer model. Babysit cron `add4d85e` (armed for this cycle) was deleted right after the GO to avoid a risk identified mid-session: with no racy-ptr-cell work queued, a future babysit tick could otherwise pick up unrelated dormant tasks (#660/#661, "first publish to crates.io" for OTHER crates) under its "lowest-id pending" rule — exactly what the user's standing "не заводи задачи на публикацию" instruction forbids touching autonomously.

From there, a new reviewer identity — "Sol-codex", arriving independently outside the babygoal loop, not invoked by this session — began a run of **seven consecutive review cycles** (runs 3 through 7, i.e. this session's items 129, 131, 132, 133, 134), each triggered by the user pasting a short verdict summary and a report path, with the user's own instruction shifting mid-campaign from the established "P2-only filing convention" to an explicit **"давай изучим и исправим всё"** (fix EVERYTHING) directive that held for the rest of the session. Every review was personally re-verified against source (grep/read, never trusted from the verdict summary alone) before filing TaskList tasks; every fix was gate-verified (fmt, clippy `--all-targets -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc`, `cargo test`, and where relevant loom/miri/bare-metal cross-builds/`cargo package`) before its own commit.

**Run 3 (item 129, 3 P2s, tasks #1384-1386):** fork-safety mitigation gap, `compile_error!` wording overclaim, panic-table contract/measurement conflation.

**Run 4 (item 131, ONE P1 + eight P3 groups, tasks #1387-#1395):** the P1 was this session's OWN defect — the just-landed fork-contract rewrite (task #1384) presented a process-wide barrier as sufficient for allocator/cell use in a forked child, when POSIX actually restricts the child to async-signal-safe calls only. Rewritten so the POSIX rule IS the rule. The eight P3s included adding `#[repr(transparent)]`-adjacent layout groundwork (deferred to run 5), `FnMut`→`FnOnce`+`#[must_use]`, `Option<bool>`→`RollbackProbe` enum, a rewritten benchmark (still imperfect — see below), new loom/native concurrency oracles, cfg-isolating the crate into `lib.rs`(facade)+`imp.rs`(impl), and a new `racy-ptr-cell-gates` CI job running `cargo publish --dry-run`.

**Run 5 (item 132, ONE P2 + six P3s, tasks #1396-#1402):** the P2 was the missing `#[repr(transparent)]` layout guarantee — added, plus a `size_of`/`align_of` oracle test and a mirrored attribute on the root crate's `#[cfg(loom)]` shim. Two of the six P3s were THIS SESSION'S OWN bugs caught by a 5th-generation review: the benchmark's `baseline/barriers_only` row still let worker threads touch the cell (task #1391's bug), and a loom test's own name overclaimed a temporal property `_init_started` (a dead variable) never actually asserted (task #1392's bug). Both re-verified non-vacuous via fault injection after fixing.

**Run 6 (item 133, four P3s, tasks #1403-#1406) — first unconditional GO in this Sol-codex sub-series:** two were second-generation refinements of run-5's own fixes — the benchmark baseline STILL wasn't isolating cell-protocol cost cleanly (excluded mutex+Arc scaffolding too), and a native unwind/liveness test's own comment ("deterministic ordering, not a timing guess") was itself an overclaim once re-derived carefully (a real scheduling race between the loser's signal and its actual CAS attempt is not closed). The test was honestly renamed rather than instrumented with a new production hook (explicitly declined as disproportionate for a P3 on a near-publication minimal-API crate). Also fixed: a stale CHANGELOG claim and a drifting test-count in a CI comment.

**Run 7 (item 134, ONE P3, task #1407) — SECOND consecutive unconditional GO:** independently re-derived (not trusted) all four of run 6's fixes as correct, then found one more benchmark-wording issue — the doc claimed the contention/baseline subtraction "isolates" the cell protocol's intrinsic cost, when it is actually a round MAKESPAN difference (overlapping-thread critical-path duration), which does not algebraically decompose the way sequential measurements would. Fixed as a wording-only change (no new API/hooks — review explicitly said none needed).

Two `npm run check` background runs were launched this session; the first (`bsriolrf0`, after run-6's wave) came back "completed" per a task notification but its output file was later found EMPTY when re-checked (likely a session-boundary artifact) — per this session's own zero-trust discipline, it was NOT trusted and was re-run from scratch (`bjfmg4a2b`), which this time genuinely completed with `[check-all] ALL GREEN`, confirmed by reading its actual tail output, not just the notification. Six pre-existing session checkpoints and a batch of scratch `.check-*.log` files were explicitly committed at the user's request mid-session (checkpoints only; the `.check-*.log` scratch files remain untracked, per this repo's own raw-log-is-scratch-by-default convention).

## Active goal

No `/goal` Stop hook is active. The prior `/babygoal` (switched to `@fh`) reached its own terminal condition at item 130's GO and was not re-armed — all subsequent review cycles (runs 3-7 / items 129, 131-134) arrived from the independent "Sol-codex" identity outside any babygoal loop, driven by the user pasting each new report path directly. No babysit cron is currently armed (the one from the `@fh` cycle, `add4d85e`, was deleted after item 130's GO).

## TaskList

### in_progress
(none)

### pending
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #1091 [perf-regress] Найти и устранить регрессию ~10-13% Ir в диапазоне 42d8d223..42d42061 (item 56)

### recently completed
- #1407 [racy-ptr-cell] Benchmark выдаёт разность makespan за точную аддитивную изоляцию protocol cost (review-15 F1)
- #1406 [racy-ptr-cell] Устаревшие количество/характер описания native-тестов в module doc и ci.yml (review-14 F4)
- #1405 [racy-ptr-cell] CHANGELOG всё ещё безусловно обещает "restores observed state" для dbg_rollback_reenterable (review-14 F3)
- #1404 [racy-ptr-cell] native unwind-тест не гарантирует, что loser реально вошёл в spin (review-14 F2)
- #1403 [racy-ptr-cell] Benchmark baseline исключает лишний scaffolding, не только cell call (review-14 F1)
- #1402 [racy-ptr-cell] Нет package/publish-dry-run гейта в обычном CI (review-13 F7)
- #1401 [racy-ptr-cell] CHANGELOG отстал от исправленного API и документации (review-13 F6)
- #1400 [racy-ptr-cell] Не закрыт liveness-пробел: unwind + уже ожидающий loser (review-13 F5)
- #1399 [racy-ptr-cell] loom-тест не доказывает то, что заявлено в имени (review-13 F4)
- #1398 [racy-ptr-cell] baseline/barriers_only на самом деле не barriers_only (review-13 F3)

(also completed this session, not listed above for space: #1384-1397, ~21 more tasks across items 129/131/132 — see `docs/correctness-open-items/TRACKED_publish_readiness.md` items 129/131-134 for the full per-task breakdown.)

No tasks deleted or filed-then-declined this session (the P2-only filing convention was explicitly superseded by the user's "fix everything" instruction partway through, so no findings were left unfiled after that point).

## Decisions

1. **Honored the user's mid-campaign shift from "file P2s only" to "fix everything"** starting with run 4 — every P3 from runs 4 through 7 was filed as a task and closed, not deferred.
2. **Declined to add a new test-only production verification hook** to close run-6's F2 (proving a loser thread genuinely entered the CAS/spin path) — chose an honest test rename instead, judging new semver-surface on a near-publication minimal-API crate disproportionate for a P3, consistent with this campaign's repeated stance against gratuitous `dbg_*`-style hooks.
3. **Reframed the contention benchmark's baseline subtraction as a "differential estimate," not an exact isolation** (run 7) — recognized that a round-makespan difference across overlapping concurrent threads does not algebraically decompose like sequential measurements would, and declined any code/API restructuring to force it to (wording fix only).
4. **Did not trust a "completed" background-task notification at face value** — when `bsriolrf0`'s output file was found empty on inspection, re-ran the gate from scratch (`bjfmg4a2b`) rather than reporting the wave as confirmed green.
5. **Committed the six pre-existing session checkpoints on explicit user request**, but left `.check-*.log` scratch files untracked (matching this repo's own scratch-by-default convention for raw logs) since the user's request named only checkpoints.

## Open questions

- None outstanding on the racy-ptr-cell review work itself — both run 6 and run 7 returned unconditional GO with every finding closed.
- Entirely the user's own, unchanged from the prior checkpoint: **when to authorize the actual `cargo publish`/version-bump/tag-push sequence**, and separately **item 28's crate-name/description one-way-door metadata decision** (now stale across many more review cycles) before that publish happens.
- Not yet re-flagged this session but still true per the prior checkpoint: the CHANGELOG's `## 0.1.0 - Unreleased` header must be date-stamped before any tag push — `release.yml`'s guard fails closed on this, independently reconfirmed by numerous review cycles across the whole campaign.

## Repo state

```
?? .check-1262-release.log
?? .check-1299-wave.log
?? .check-1321-wave.log
?? .check-1334-wave.log
?? .check-1335-retry.log
?? .check-1342-wave.log
?? .check-1348-wave.log
?? .check-1353-wave.log
?? .check-1357-wave.log
?? .check-1386-wave.log
?? .check-1393-wave.log
?? .check-1401-wave.log
?? .check-1406-retry.log
?? .check-1406-wave.log
```
(scratch `.check-*.log` files only — the six session checkpoints that were untracked at the start of this session have since been committed per explicit user request; this checkpoint's own file is not yet listed, per the tool's snapshot-before-write timing.)

```
8c13ae0 docs: file item 134 -- racy-ptr-cell Sol-codex run 7, unconditional GO, all four run-6 fixes independently re-derived
82ea415 docs(racy-ptr-cell): task #1407 -- benchmark claimed exact additive isolation a round-makespan subtraction cannot give (review-15 F1)
4920dc8 docs(review): audit racy-ptr-cell publication readiness run 7
c1d812d docs: file item 133 -- racy-ptr-cell Sol-codex run 6, unconditional GO, all four P3 groups closed
6d8032c docs: task #1406 -- drop stale test-count/description drift for cell_unit.rs (review-14 F4)
3b0f1f2 docs(racy-ptr-cell): task #1405 -- CHANGELOG still unconditionally promised restoring observed state (review-14 F3)
43ab70c test(racy-ptr-cell): task #1404 -- rename the unwind test to the property it actually proves (review-14 F2)
06e3886 fix(racy-ptr-cell): task #1403 -- baseline/barriers_only excluded useful scaffolding too, not just the cell call (review-14 F1)
```

`origin/main` is 76 commits behind local `HEAD` — entirely unpushed. `npm run check` (full local gate) confirmed ALL GREEN after the run-6 wave (re-run to completion as `bjfmg4a2b` after an earlier attempt's notification could not be trusted — its output file was empty on inspection). The run-7 wave's single fix (task #1407) was verified individually (fmt/clippy/rustdoc/test/full benchmark run) but the full-suite `npm run check` has not been re-run since that commit landed — worth re-running on resume if not already done. Nothing has been pushed to `origin`; no publish action has been taken or queued.
