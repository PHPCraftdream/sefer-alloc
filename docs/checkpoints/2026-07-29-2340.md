# Checkpoint — 2026-07-29 23:40 [round-29-complete]

## Session summary

Round 29 (16 originally-queued tasks, #432–#447, plus one systemic follow-up
discovered mid-round, #448) is now fully implemented, zero-trust-reviewed, and
committed — all 17 tasks show `completed` in the TaskList. The round worked
down two independent readonly reviews' backlogs
(`docs/reviews/2026-07-29-r28-readonly-review.md` and, found mid-session,
`docs/reviews/2026-07-29-oh-acceleration-code-project-review.md`) plus one
tripwire-discovered follow-up. All work was delegated via `/crush` where
usable, and via `Agent(subagent_type: "sh")` once `/crush` hit a genuine hard
weekly/monthly quota limit partway through (confirmed exact-signature Trigger
2 per the `/crush-fallback` skill; task #449, the persistent fallback marker,
is `STATUS: active, AGENT: sh, TRIGGER: hard-limit`, resets ~2026-07-30
17:01). Every delegated task's diff was independently zero-trust reviewed
before committing — reading every line, re-running tests/fmt/clippy myself,
and independently re-verifying every cited number against raw logs — which
caught and fixed real bugs in at least 8 of the 17 tasks (see Decisions
below), several of which the delegated agent's own narrower verification had
missed.

Headline results this round: R29-5 found medium→Large promotion is genuinely
rare (33/60,722 allocations under a doubling-growth workload) — NO VICTIM for
the Linux-mremap design. R29-13 measured the large-cache's 256 MiB default
headroom for the first time ever: idle reclaims 0 KiB always (event-driven
decay confirmed empirically), forced convergence retains ~238–241 MiB/heap
(only 12.4–12.5% reclaimed) — 30x the small pool's own proven ~8 MiB/heap
floor, closing a real measurement asymmetry. R29-16 finally ran
`virgin-zero-skip`'s own design docs' Stage-0/Stage-3 promotion gate (never
run since the feature shipped): a real 21.4x Ir win at 64 KiB, but wall-clock
inconclusive for a specific, source-verified reason (eager small-segment page
commit under `production` masks the software saving behind an OS
first-touch cost paid either way) — not a clean promotion GO. R29-9 (systemic)
added a structural tripwire enumerating every safe `pub fn dbg_*` raw-pointer
hook against a hand-reviewed allowlist; it caught a 4th live instance of the
R25-1 soundness-bug class the same day it was added (R29-17,
`dbg_directory_bit_for_ptr`). R29-6 split `OPEN_ITEMS.md`'s closure narratives
into a new `docs/perf/OPEN_ITEMS_ARCHIVE.md` (1,581→426 lines, zero lines lost
per an independent line-multiset diff) and added a CLAUDE.md rule requiring
an immutable source identity for perf-gate reports measuring an uncommitted
tree.

`CHANGELOG.md`'s `## [Unreleased]` section now has a full Round 29 entry
(written and personally verified against the actual commits, not copied from
any agent's self-report). The standing goal's remaining steps — commit all
markdown files and launch an `@oh`-equivalent review agent (`subagent_type:
"oh"`) to review the entirety of Round 29's work — have NOT yet been done;
they are the immediate next actions after this checkpoint.

## Active goal

```
/goal реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу
```

Status against this goal: tasks implemented via `/crush`/`sh` with commits
between each — DONE (17/17). `/checkpoint` — DONE (this file, a fresh one
superseding `2026-07-29-2124.md`). Update CHANGELOG — DONE (Round 29 entry
added, verified fmt-clean and doc-tripwire-clean). Commit all markdown files —
NOT YET DONE (CHANGELOG.md plus the two readonly-review docs are currently
uncommitted; see Repo state below). Run `@oh` review — NOT YET DONE. The goal
condition is not yet satisfied.

## TaskList

### pending
- #449 crush-fallback state (persistent — do not complete) — `STATUS: active, AGENT: sh, TRIGGER: hard-limit, PROVIDER: zai (crush), UNTIL: none (informational: resets ~2026-07-30 17:01:11), CRON_JOB_ID: none`. Not a work item — a sentinel, intentionally never completed.

### recently completed (this round, chronological)
- #432 R29-1 (P0): classify or reopen R28-2's unexplained anomalous test failure — fixed (windowed→cumulative invariant)
- #433 R29-2 (P0): README (8, 32 MiB) throughput recipe + overclaim fix
- #438 R29-7 (P0): re-gate + unsafe-ify tls_heap dbg hooks
- #439 R29-8 (P0): re-gate + unsafe-ify AllocCore::dbg_force_decommit_retain_for
- #440 R29-9 (P0, systemic): dbg-hook safety tripwire
- #448 R29-17 (P0): re-gate + unsafe-ify HeapCore::dbg_directory_bit_for_ptr (found by #440's tripwire)
- #434 R29-3 (P1): decommit→reserve segment lifecycle decomposition
- #441 R29-10 (P1): alloc-hit clear_magazine cost isolation
- #442 R29-11 (P1): IAI_BASELINE.md honest-rejects → OPEN_ITEMS.md + tripwire
- #443 R29-12 (P1): feature-promotion status survey
- #435 R29-4 (P1): per-heap segment-state accounting (R27-3 residual)
- #436 R29-5 (P2): promotion frequency/byte-distribution measurement (NO VICTIM)
- #444 R29-13 (P2): large-cache retention gate (~238–241 MiB/heap floor)
- #445 R29-14 (P2): `cargo check "production numa-aware"` per-PR CI step
- #446 R29-15 (P2): 3 doc corrections (tcache.rs, numa-aware trap, README enum)
- #447 R29-16 (P2): virgin-zero-skip calloc iai gate (21.4x Ir, wall-clock inconclusive)
- #437 R29-6 (P3): OPEN_ITEMS.md archive split + immutable-provenance rule

No `pending`/`in_progress` work tasks remain — only the `#449` sentinel.
The babysit cron (job id not re-verified this checkpoint, ~15 min interval)
should self-delete once it next ticks and sees the TaskList's only entry is
the never-completed sentinel — worth confirming in the next session if it
hasn't already fired.

## Decisions

- **Two separate commits per task when a zero-trust review finds a bug in
  the delegated diff PLUS an unrelated bug in earlier work**: task-scoped
  commit for the task itself, standalone commit for the incidental fix (done
  for #436's trim_split_whitespace fix, #444's R29-5 gating fix). Keeps each
  commit's message accurate to what it actually contains.
- **`OPEN_ITEMS.md` split: archive narratives, keep current-state cards in
  place** (not e.g. per-item files or a database) — matches the file's own
  existing tier structure, needs zero renumbering, and the back-link
  convention (`Full history: OPEN_ITEMS_ARCHIVE.md § <anchor>`) is
  grep/click-navigable both directions.
- **Ir-isolation results reported honestly even when wall-clock doesn't
  confirm them** (R29-16): the task's brief was explicit that this closes a
  measurement gap, not that it proves a promotion case — the split verdict
  (real Ir win, inconclusive wall-clock, with a specific mechanism identified)
  was preserved rather than rounded to either "win" or "no effect."
- **Fixed a stale doc enumeration the delegated task's own report claimed was
  handled but wasn't** (R29-15's fix 3): re-verified against a fresh `grep`
  of every `bench-internals`-gated site in `src/` rather than trusting the
  agent's "already accurate" claim about a table row it had actually not
  touched (a different, adjacent row was fixed instead).
- **CHANGELOG Round 29 entry written directly from my own review notes**, not
  copied from any delegated agent's self-report — cross-checked against the
  actual `git log` for this round before writing.

## Open questions

- None outstanding from the user. The only remaining ambiguity is procedural:
  whether the `@oh` review agent invocation should cover just this round's
  diffs (commits `9ef2f85`..`7c2c62d`, i.e. everything since the last
  checkpoint) or the full session — leaning toward "this round's full diff
  set," to be decided when launching it next.

## Repo state

```
 M CHANGELOG.md
?? .claude/
?? docs/checkpoints/2026-07-29-1323.md
?? docs/checkpoints/2026-07-29-2124.md
?? docs/checkpoints/2026-07-29-2340.md
?? docs/reviews/2026-07-29-oh-acceleration-code-project-review.md
?? docs/reviews/2026-07-29-r28-readonly-review.md
```

```
7c2c62d perf(docs): calloc-shaped isolation gate closes virgin-zero-skip's missing Stage-0/Stage-3 measurement (R29-16, task #447)
56484f3 docs: three doc corrections from the oh-acceleration readonly review (R29-15, task #446)
7568030 ci: check "production numa-aware" per-PR, not just weekly (R29-14, task #445)
8e6f9d1 fix(alloc-core): gate R29-5's promotion counters on their real reachability, not just bench-internals (task #444 gate)
894e9e3 perf(docs): measure the large-cache headroom's idle-RSS floor for the first time (R29-13, task #444)
34f3702 docs(perf): archive OPEN_ITEMS.md's closure narratives; add immutable-provenance rule (R29-6, task #437)
866c5f4 fix(tests): remove redundant trim_start before split_whitespace (clippy, task #436 gate)
79aad56 perf(docs): measure medium->Large promotion frequency and copied-byte distribution (R29-5, task #436)
0f6ce0d docs: survey non-production feature promotion status; index the two dangling decisions (R29-12, task #443)
2fd0b39 docs(perf): migrate IAI_BASELINE.md's 7 unindexed honest-rejects into OPEN_ITEMS.md; add a structural tripwire (R29-11, task #442)
```

(10 most recent shown; the full round spans back to `9ef2f85`, 17 commits
total across the round.)
