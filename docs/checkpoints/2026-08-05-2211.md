# Checkpoint — 2026-08-05 22:11 [wave-4-closing]

## Session summary

This session ran the `/babygoal` ritual across four successive remediation waves on `sefer-alloc`, each triggered by an `@oh`-style readonly review of the prior wave's own work: implement → checkpoint → review → repeat. Waves 1-3 (tasks #547-578) are fully complete and were already checkpointed earlier this session (see `docs/checkpoints/2026-08-05-2013.md` for the wave-3 recap). This checkpoint covers **wave 4**: after wave 3 closed (commit `85dacfc`), an independent `@oh` review of wave 3's own work (`docs/reviews/2026-08-05-wave3-h1h8-remediation-readonly-review.md`) found 10 findings (F1-F10: 1×P1, 3×P2, 3×P4, 3×P3) — filed as tasks I1-I10 (#579-588) and **all 10 are now closed**, across 8 commits (`3d57a26` through `782b92e`).

The one P1 finding (F1) was a genuine, independently-reproduced CI-red regression: `cargo test --features "production medium-classes internals"` failed to compile (`size_of::<HeapCore>() = 8408 > 8192` budget) — a gap in H1's own earlier fix (task #571), which had excluded 4 named experimental features from the stack-pressure assert but never checked whether OTHER real shipping compositions (like `medium-classes`) also exceeded the original 8192 B budget. Fixed structurally rather than patching the exclusion list again: measured the TRUE global maximum across every feature composition (`--all-features` = 8840 B, provably the ceiling since it's the union of every feature), raised the budget to 9216 B, and removed the fragile per-feature exclusion mechanism entirely — both the compile-time assert and its mirrored runtime test are now unconditional. The other 9 findings were structural/doc-precision issues: a mis-nested CHANGELOG heading that had silently orphaned ~40 bullets from their section (F3, same defect class H3 fixed one level up in the same wave), 39 test files missing an `internals` cfg gate that would hard-fail instead of skip under a plain `production` build (F4, closed together with a permanent 2nd check added to the exhaustive verify script), a still-ungated `SeferAlloc::dbg_trim_current_thread` hook (F5, verified closed via an external downstream probe crate), 8 of 13 stale post-rebase SHA citations H4 had missed (F2, verified via tree-identity + reachability), an item-number collision in the correctness index (F6), a stale "9 files"/"5 clippy rows" pair of miscounts (F8/F9), and the wave-3 manifest's own persistent under-count, now finally closed at its true 14-commit span with a new wave-4 manifest started (F7).

Every fix was independently re-verified before committing: `cargo check`/`cargo test`/`cargo clippy`/`cargo fmt --check` re-run personally (not trusted from any sub-agent), an external probe crate built outside the repo to confirm F5's fix, the reviewer's own reachability-sweep methodology re-run for F2, and a full `cargo test --no-run --tests --features production` (no internals) re-run to confirm F4's 39-file fix left zero compile errors. A `npm run check` full-pipeline re-run was in progress (to confirm the whole gate is green again after all 8 wave-4 commits) when the user interrupted it to run this checkpoint instead — **that verification has NOT yet completed or been reconfirmed since `782b92e` landed**, though every individual fix was separately verified with its own targeted checks before each commit.

Two loose ends remain before wave 4 can be considered fully closed per this session's own established ritual: (1) re-run `npm run check` to completion and confirm the only remaining red step is still the same pre-existing, already-documented `43115cf`/`5c1142f` commit-prefix debt (not a new regression); (2) complete the closing ritual — update `R34_REMEDIATION_4_MANIFEST.md` with this wave's own final commits (per the "wave's last commit lists itself" convention just established in wave 3's manifest), write a CHANGELOG.md entry for wave 4, commit this checkpoint + any other markdown, and launch a FOURTH `@oh` readonly review of wave 4's own work — continuing the same review-and-remediate chain, per the user's standing instruction governing this whole session. There is also a stray untracked temp file (`src/global/sefer_alloc.rs.tmp.23920.75bad19dff17`, likely an editor lock/backup artifact) noticed but not yet investigated or removed.

## Active goal

None — governed by the `/babygoal` ritual's standing Stop-hook condition (implement via `@sh`/`/crush`, commit between tasks, checkpoint+changelog+commit-markdown+`@oh`-review when all work is done), not a `/goal` condition. The user separately asked "потом сделай пуш" (then push) earlier in the session — still pending, to be done after the wave-4 closing ritual and review.

## TaskList

All tasks #547 through #588 are `completed`. No `in_progress` or blocked `pending` tasks remain besides the persistent sentinel:

### pending
- #553 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), switch back with `/crush-fallback clear`

### recently completed (wave 4, this session)
- #579 I1 [P1]: F1 — HeapCore stack-pressure budget didn't cover production medium-classes (CI-red)
- #580 I2 [P2]: F2 — fixed the remaining 8 of 13 orphaned SHA citations H4 missed
- #581 I3 [P2]: F3 — moved the BREAKING CHANGE heading out of the Round-34 bullet list
- #582 I4 [P2]: F4 — fixed 39 test files missing internals gate; tightened the verify script
- #583 I5 [P3]: F5 — gated SeferAlloc::dbg_trim_current_thread behind internals
- #584 I6 [P3]: F6 — renumbered colliding CORRECTNESS_OPEN_ITEMS.md items 17/18
- #585 I7 [P3]: F7 — finalized wave 3's manifest (14/14 commits), started wave 4's
- #586 I8 [P4]: F8 — corrected CHANGELOG's "9 files" miscount to 6
- #587 I9 [P4]: F9 — fixed check-all.mjs's stale "5 clippy rows" count
- #588 I10 [P4]: F10 — tightened the exhaustive verify script's gate-detection

## Decisions

- F1: raise the stack-pressure budget to cover the TRUE global maximum (`--all-features` = 8840 B) and remove the fragile per-feature exclusion list entirely, rather than adding `medium-classes` to the exclusion list (which would have repeated the same fragile-enumeration mistake a third time).
- F2: label the 5 newly-fixed SHA citations "SHA cascaded by the rebase (message unchanged)" rather than "reworded" — precise wording distinguishing them from G1's 3 actually-reworded commits, since a rebase changes every descendant's hash regardless of whether that commit's own content was touched.
- F7: wave 3's manifest is genuinely closed at 14/14 commits (`85dacfc`); every commit since then belongs to a NEW wave-4 manifest (`R34_REMEDIATION_4_MANIFEST.md`, created this task, explicitly marked NOT YET FINAL) — established the convention "the wave's last commit updates its own manifest, listing itself" for future waves to avoid repeating this file's own two-round extension history.
- Scope discipline: F2/F4/F7 all found MORE work than their own source review estimated (F2: 8 not "a few" remaining citations; F4: 39 not a handful of files; F7: the review's own cited 14-commit span, not my initial 11-row guess) — in each case, re-derived the true scope from first principles (git log, static scans, reachability sweeps) rather than trusting the review's or my own prior estimate at face value.

## Open questions

None outstanding from the user's side. Two self-identified loose ends (not user-facing questions) are named in the session summary above: the interrupted `npm run check` re-verification, and the not-yet-investigated stray `.tmp` file.

## Repo state

```
?? .claude/
?? docs/reviews/2026-08-03-round33-readonly-review.md
?? docs/reviews/2026-08-04-r32-r33-global-bench-readonly-review.md
?? docs/reviews/2026-08-04-release-stabilization-audit.md
?? docs/reviews/2026-08-05-fallback-release-readonly-review.md
?? docs/reviews/2026-08-05-r34-review-remediation-readonly-review.md
?? docs/reviews/2026-08-05-round34-readonly-review.md
?? docs/reviews/2026-08-05-sol-release-readonly-review.md
?? docs/reviews/2026-08-05-sol-remediation-readonly-review.md
?? docs/reviews/2026-08-05-wave3-h1h8-remediation-readonly-review.md
?? src/global/sefer_alloc.rs.tmp.23920.75bad19dff17
```
```
782b92e docs: close F7 — finalize wave 3's manifest, start wave 4's
650b818 docs(config): close F9 — scripts/check-all.mjs's stale "5 clippy rows" count
04c2f74 docs: close F2 — fix the remaining 8 of 13 orphaned SHA citations H4 missed
addc63d docs: close F6 — renumber colliding CORRECTNESS_OPEN_ITEMS.md items 17/18
7a9b7c7 fix(perf): close F5 — gate SeferAlloc::dbg_trim_current_thread behind internals
ba18071 docs: close F3+F8 — move BREAKING CHANGE heading out of the Round-34 bullet list
b1a9b7b test: close F4+F10 — 39 test files missing internals gate; tighten the verify script
3d57a26 fix(perf): close F1 — HeapCore stack-pressure budget didn't cover production medium-classes
```
