# Checkpoint — 2026-08-06 00:15 [release-readiness-sprint]

## Session summary

This session's governing instruction is the `/babygoal` ritual, executed repeatedly across an extended review-and-remediate chain on `sefer-alloc`: implement findings via `@sh`, commit between tasks, then checkpoint/CHANGELOG/commit-markdown/`@oh`-review to close each wave. Earlier waves (1-4, tasks up through #589) closed 4 rounds of readonly-review findings and ended with a push: `origin/main` moved from `42d8d22` to `42d4206` (153 local commits landed at once). That push triggered the FIRST full remote CI run over the whole backlog, and it came back genuinely red — not hypothetical, independently confirmed via `gh run view`.

Three independent readonly reviews then assessed actual release readiness for 0.3.0: `docs/reviews/2026-08-05-hs-new-waves-release-readonly-review.md` (verified wave 4's diff, found F1-F6), `docs/reviews/2026-08-05-release-readiness-gap-audit.md` (standalone release-engineering audit, findings R0-R17 — manifest self-SHA, CHANGELOG state, publish DAG, package dry-run, CHANGELOG-guard bypass, red commit-prefix gate, stale docs, etc.), and `docs/reviews/2026-08-05-fh-release-readiness-verification-review.md` (independent verification agent, `@fh`, that ACTUALLY RAN commands including a live `cargo package` attempt — something the two prior readonly audits couldn't do — and found 7 new issues N1-N7 plus corrected 2 misdiagnoses in the earlier reviews).

Findings were filed as tasks K1-K18/L1-L8/M1-M2. Before executing, I invoked `/map` to build `docs/plans/2026-08-05-release-execution-map.md` — a verified (not just transcribed) execution plan that independently re-checked every load-bearing claim against the current tree. That verification pass caught two significant errors in the source reviews' own diagnoses: (1) K6 (commit-prefix gate) had FLIPPED from red to green because the push moved the offending commits behind `@{u}` — recommendation reversed from "consider rebasing" to "do NOT rebase, it's now published history"; (2) K10 (concurrency flakes items 12/14) — both reviews recommended "add serialization," but direct file inspection showed BOTH affected test files already fully serialize every test via a `SerialGuard`/`AtomicBool` spin-lock — the real open question is an unexplained 42-of-50 reclaim shortfall under already-complete serialization, rescoped from a mechanical fix to a genuine investigation.

The user then explicitly deferred the crates.io publish-DAG work (K3/K4/K9/L2/L3/L5 — three unpublished path-dependency crates, a version mismatch on `aligned-vmem`, drift on `numa-shim`) to a dedicated pass immediately before tagging, rather than this sprint — path dependencies stay local-folder references for now. I updated all six tasks' descriptions to reflect this and stopped blocking on them.

Executed via `@sh` sequentially (one agent writing to git at a time, per this session's established git-safety discipline), each result independently zero-trust re-verified before moving on — including, notably, independently re-running `cargo clippy --all-targets --all-features -- -D warnings` myself on WSL (not trusting the sub-agent's own claim) to confirm the Linux-only bench fix (L1), plus a counterfactual (reverting the fix reproduces the exact original 4 CI errors). Closed this sprint: L8+L1+M1 (two genuine CI-red jobs fixed and verified on real Linux; audited all 5 Linux-only-blind-spot files), K5+L4+K8 (release.yml's CHANGELOG-guard bypass, wrong-crate-changelog defect, and missing CI-success gate — one connected pass through the same file, `actionlint`-clean), K2 (two overstated wave-4 CHANGELOG claims corrected, append-only correction added, `docs/CORRECTNESS_OPEN_ITEMS.md` items 16/19 marked resolved), K12/K13/K14 (CONTRIBUTING.md/SECURITY.md/README.md/CLAUDE.md stale-doc fixes, each independently spot-checked), K6 (documented the premise flip), L6 (`.claude/` gitignored — it was blocking `cargo package --allow-dirty`). A CHANGELOG entry summarizing the whole sprint was added and committed (`e74a7b1`).

A full `npm run check` was launched in the background (task `bclrbbu1c`) to confirm the whole local gate is green after this sprint's commits — **not yet confirmed complete at the time of this checkpoint**, interrupted by the user's `/checkpoint` invocation.

Remaining before this sprint can be considered fully closed per the babygoal ritual: (1) confirm `npm run check` is green; (2) commit the two remaining untracked markdown artifacts (`docs/plans/2026-08-05-release-execution-map.md`, `docs/reviews/2026-08-05-fh-release-readiness-verification-review.md`) — the user's standing instruction this session has been to commit review/plan markdown explicitly, unlike earlier waves' "reviews stay uncommitted" default; (3) launch a closing `@oh` readonly review of this sprint's own work, per the ritual; (4) after that review's findings are triaged, the user's separately-standing "push" authorization from earlier in the session should be re-confirmed before any further push (the last push already happened; a NEW push needs a fresh explicit go-ahead per this project's standing rule of never pushing without being asked, though the user's `/babygoal` phrasing this turn didn't explicitly re-authorize a push — only checkpoint/changelog/commit-md/review).

## Active goal

None — governed by the `/babygoal` ritual's standing completion criteria (implement via `@sh`, commit between tasks, checkpoint+changelog+commit-markdown+`@oh`-review when the sprint's work is done), not a `/goal` Stop-hook condition.

## TaskList

### pending
- #553 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), switch back with `/crush-fallback clear`
- #598 K3 [DEFERRED to pre-release pass]: R3 — publish DAG (3 crates missing release targets)
- #599 K4 [DEFERRED to pre-release pass]: R4 — cargo package --list / publish --dry-run + trim package content
- #602 K7 [P0]: R0/N1/N7 — release-candidate SHA freeze (blockedBy: #598, #599, #615 — naturally deferred alongside the DAG work)
- #604 K9 [DEFERRED to pre-release pass]: R8 — full test/doc/package matrix for all workspace members
- #605 K10 [P1, DIAGNOSIS CORRECTED]: R9 — explain the 42/50 reclaim shortfall; serialization is already complete (post-release hardening, not a blocker)
- #606 K11 [P1]: R10 — coverage or accepted-risk record for 5 tier-1 unsafe seams (post-release)
- #610 K15 [P2, trigger-only]: R14 — structural panic/unwind guard completeness (post-release)
- #611 K16 [P2]: R15 — two Kani arithmetic proofs (post-release)
- #612 K17 [P2]: R16 — strengthen the MSRV gate beyond cargo check (post-release)
- #613 K18 [P2]: R17 — migrate to crates.io trusted publishing (post-release)
- #615 L2 [DEFERRED to pre-release pass]: N2 — cargo package fails now (aligned-vmem ^0.2 unresolvable)
- #616 L3 [DEFERRED, NEEDS USER DECISION]: N3 — published numa-shim/aligned-vmem drift
- #618 L5 [DEFERRED to pre-release pass]: N5 — package tarball leaks internal review docs and local paths
- #623 M2 [P3]: 9 remaining item-number collisions in CORRECTNESS_OPEN_ITEMS.md (post-release)

### recently completed (this sprint)
- #596 K1, #597 K2, #600 K5, #601 K6, #603 K8, #607 K12, #608 K13, #609 K14, #614 L1, #617 L4, #619 L6, #620 L7, #621 L8, #622 M1

## Decisions

- Deferred the entire crates.io publish-DAG resolution (K3/K4/K9/L2/L3/L5) to a dedicated pass immediately before tagging 0.3.0, per explicit user instruction ("по крейтам — временно ссылаемся на свои папки. Потом перед релизом будем публиковать") — path dependencies stay local-folder references for now.
- K6 (commit-prefix gate): reversed the source reviews' "consider a reword rebase" recommendation to "do NOT rebase" — the offending commits are now published on `origin/main`, so rewriting them means rewriting public history for a cosmetic prefix issue, not unpushed local commits.
- K10 (concurrency flakes): rescoped from "add serialization" (both reviews' recommendation) to "investigate why a FULLY serialized test observed 42/50 reclaims" — direct file inspection refuted the "missing lock" hypothesis. Not a release blocker (not reproducible on demand); deferred to post-release.
- Variant A (GitHub Actions API poll) chosen over Variant B (`workflow_run` retrigger) for K8's CI-success gate — smaller diff, preserves both existing trigger types (tag push + manual dispatch) without needing an artifact/dispatch handoff mechanism.
- Left GitHub repo-settings-level changes (protected environments, required reviewers) explicitly out of scope for K8 — that's an operator action on live repo configuration, not a code change to delegate.

## Open questions

None newly raised this session beyond what's already tracked in task descriptions (K3/K4/K9/L2/L3/L5's deferred-pass scope, L3's version-bump decision still needed at that time, K11's blocking-vs-accepted-risk choice for unsafe-seam coverage). The map's own "Не мои решения" section (in `docs/plans/2026-08-05-release-execution-map.md`) lists 5 items for the user's eventual attention; decision #1 (three unpublished crates) was answered this session (temporarily path-only). Decisions #2 (version bumps), #3 (K6 rebase — now effectively answered by the push itself), #4 (README's "11 crates on crates.io" claim), #5 (K11 blocking-vs-accepted-risk) remain open for whenever the pre-release pass begins.

## Repo state

```
?? docs/plans/
?? docs/reviews/2026-08-05-fh-release-readiness-verification-review.md
```
```
e74a7b1 docs: CHANGELOG entry for the release-readiness sprint (tasks #596-621)
b8d6235 docs: close K6 — commit-prefix debt now published history, do not rebase
5db488c docs(contributing): fix stale paths, test/fuzz names, and mandatory commands
6391497 docs(security): fix stale feature vocabulary and dead email promise
c9c7341 docs(claude): fix stale 'all five clippy rows' → six in npm run check
```
