# Checkpoint — 2026-09-20 07:12 [none]

## Session summary

This session continued from an earlier segment (finished proc-probe's
round-3 fixes and first crates.io publish, then started a subsystem-by-
subsystem correctness review of the sefer-alloc root allocator). Partway
through that review cycle the user reported the repo's local checkout had
been accidentally deleted — confirmed `.git` and all files except `.rush/`
were gone. Re-cloned from `github.com/PHPCraftdream/sefer-alloc`
(origin/main only, HEAD `58e7a82c`). This meant ~4 local-only commits from
the review-fix cycle (G1/G3 fixes, checkpoint) plus an in-progress G8
worktree diff were lost — confirmed unrecoverable (no local `.git` reflog
survived). The user then reported "мы многое восстановили" (we recovered
a lot) — inspection showed the user (or another session) had independently
redone the G1 (use-after-free) and G3 (batch reclaim leak) fixes, even
improving G3 by catching an additional non-fastbin Large-overflow bug,
verified by an independent agent (ha/Aristotle) with real negative
controls, and pushed to origin as commits `46b8459b`..`ae87141e` (CI
green on both `CI` and `Kani verification` jobs). I deleted the entire
stale review-planning TaskList (#1965-#1975, which referenced the lost
G2/G4-G10 findings) per explicit "удали все задачи" instruction.

The user then asked for a completely different, large mechanical task via
`/wrush`: reorganize `src/` so no file exceeds 1000 lines (oversized files
become directories) and no directory has more than 7 direct entries
(overflowing directories get grouped into semantic subfolders). I surveyed
`src/` and found violations in `alloc_core/` (37 entries, 7 oversized
files), `registry/` (14 entries, 6 oversized files), `global/` (5 entries
but `sefer_alloc.rs` at 1126 lines), and `concurrent/` (9 entries, no
oversized files). Filed 4 sequential tasks (#1976-#1979: alloc_core,
registry, global, concurrent) after checking real cross-module coupling
via grep — `alloc_core`⇄`registry` are tightly coupled (116+ direct
submodule-path imports) so those two stay sequential, but `concurrent/`
has ZERO inbound references from the other three subsystems, so it was
launched in TRUE parallel with `alloc_core`.

Both `#1979` (concurrent) and `#1976` (alloc_core) are now DONE, verified
zero-trust, and merged into `main` (commits `72189f0d` for concurrent,
`d7fa9c16`+`00b3e10a` for alloc_core — the second alloc_core commit was
needed because I caught the rush agent's own leftover scratch directories
(`.reorg_tmp/`, `.tmp_extract/`) about to be committed as real source).
The alloc_core phase needed 4 rush relaunches into the same session
(60m→150m→120m→final) due to repeated timeouts (one hit rush's internal
45-minute single-tool-call watchdog because the agent ran long cargo
commands as blocking calls instead of background+poll) — after each
timeout I inspected the worktree's real progress via `git status`/`wc -l`
before deciding to relaunch, and personally fixed several real bugs the
agent left behind before merging: two `mod` declarations pointing at the
wrong nesting depth after a file was moved into a subdirectory
(E0583/E0432/E0433 cascade, root-caused via careful `super::`/`super::
super::` path-depth tracing), two clippy-flagged unused imports, a source-
text drift-guard test (`tests/no_panic_doc_accuracy.rs`) that broke because
a panic message's home file moved during a later manual split, and a file
(`mem.rs`) that was exactly 1000 lines pre-format but grew to 1013 after
`cargo fmt` — split further into `mem/mod.rs` + `mem/realloc_fastpath.rs`,
which then pushed the parent directory to 8 entries, requiring nesting
`mem_realloc_fastpath.rs` inside a new `mem/` subfolder alongside `mem.rs`
to get back to 7. Both full feature configs (production;
non-fastbin+decommit+segment-directory+internals) are fully green in the
primary checkout (257/257 "ok" test-binary results each) except the one
already-confirmed-pre-existing `tagged_index_stack_ab_runner_scratch_guard`
Windows LNK1104 flake (path-length-sensitive, does not even reproduce in
the primary checkout, only in deeper worktree paths).

`#1977` (registry, 14 entries, 6 oversized files: `heap_core_xthread.rs`
1751, `bootstrap.rs` 1590, `heap_core_free.rs` 1445, `heap_core_diag.rs`
1439, `heap_registry.rs` 1192, `heap_core_alloc.rs` 1162) is now IN
PROGRESS — launched in worktree `worktrees/src-reorg-registry`, rush
session `src-reorg-registry`, 150m timeout, prompt explicitly encodes every
lesson learned from the alloc_core phase (background+poll for long
cargo commands, re-check line counts after `cargo fmt`, delete scratch
files before finishing, `mod.rs` must be reexports-only per this repo's
own CLAUDE.md convention, chase `tests/no_panic_doc_accuracy.rs` and
`dbg_hook_safety_tripwire.rs` too, watch for `pub(super)` visibility gaps
when splitting one `impl` block across files). Confirmed alive (12
processes) as of this checkpoint. `#1978` (global/sefer_alloc.rs split)
remains blocked behind `#1977`, sequentially, due to real
`registry`↔`global` coupling. A `/goal` Stop-hook ("решить все задачи" —
solve all tasks) is active and has been driving continuous progress
through periodic check-ins during the long rush waits.

Two unexplained detached worktrees exist (`worktrees/head-check`,
`worktrees/sefer-baseline-ae87141e`, both at `ae87141e`) — not created by
me this session (my own throwaway baseline-check worktrees were all under
`/tmp` and were removed immediately after use); left untouched pending
investigation, likely leftover from the pre-session repo-recovery work.

## Active goal

решить все задачи (a `/goal` Stop-hook condition — keep working until the
TaskList is fully resolved; do not stop or ask before then)

## TaskList

### in_progress
- #1977 Reorg src/registry (≤1000 lines/file, ≤7 entries/dir)  (blockedBy: none — ready, running)

### pending
- #1978 Split src/global/sefer_alloc.rs (1126 lines) into a mod dir  (blockedBy: #1977)

### recently completed
- #1976 Reorg src/alloc_core (≤1000 lines/file, ≤7 entries/dir)
- #1979 Regroup src/concurrent (9 entries, no oversized files)

## Decisions

- Deprioritized then fully deleted the 5-task subsystem review backlog
  (#1965-#1969) rather than trying to reconcile it against the
  independently-recovered G1/G3 state, per explicit user instruction
  ("удали все задачи") — its ground-truth review report's remaining
  findings (G2, G4-G10) are gone with the deleted local commits and would
  need a fresh review pass if ever revisited.
- Sequenced `alloc_core`→`registry`→`global` strictly (each blocks the
  next) because grep-confirmed real cross-module coupling (116+ direct
  `crate::alloc_core::<submodule>::` imports from `registry/`, real but
  lighter `registry::heap_core`/`alloc_core::{os,remote_free_ring}`
  coupling from `global/`) makes parallel reorgs likely to collide at
  merge time on the same lines — chose safety over speed here, unlike
  `concurrent/`, which had zero inbound coupling and ran in true parallel.
- Chose `git cherry-pick` over `cp`/`Edit`-based manual merging for both
  completed reorg phases (concurrent, alloc_core) despite `/wrush`'s
  stated preference for cp/Edit — justified because each worktree branched
  cleanly from `main`'s current tip with zero intervening divergence,
  making a native rename-aware cherry-pick strictly safer than manual
  reconstruction for a 200+ file diff full of git-tracked renames;
  hand-resolved the one real conflict (`docs/ARCHITECTURE.md`, two
  different reorg phases both editing the same inventory-table rows).
- After alloc_core's third rush timeout revealed a broken build (E0583 +
  wrong `super::` nesting depth), fixed the wiring myself directly instead
  of relaunching rush a 4th time for that specific narrow bug — relaunched
  rush only for the larger remaining content-split work, keeping the
  narrow/mechanical/well-understood fixes in my own hands per the
  session's standing "surgical fix" discipline.

## Open questions

- None pending user input. (Non-blocking future consideration, not yet
  worth raising: whether to investigate/clean up the two unexplained
  detached worktrees noted above once the reorg chain is done.)

## Repo state

```
(clean — git status --short: no output)
```
```
00b3e10a chore(alloc_core): drop leftover extraction scratch directory
d7fa9c16 refactor(alloc_core): split oversized files, group into ≤7-entry dirs
72189f0d refactor(concurrent): group into epoch/lock_free/sharded (≤7 entries/dir)
ae87141e Merge remote-free lifetime and batch reclamation
e573b5e1 test(global): split gated G1 runtime regression from structural guards
```

Worktrees: `worktrees/src-reorg-registry` (branch `src-reorg-registry`,
active, rush session running); `worktrees/head-check` and
`worktrees/sefer-baseline-ae87141e` (both detached at `ae87141e`,
unexplained, not touched). `main`/`origin/main` — local is 2 commits ahead
of the last known-pushed state from before this segment; not pushed this
session (no push requested for this workstream).
