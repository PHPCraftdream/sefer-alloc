# Checkpoint — 2026-09-22 00:15 [none]

## Session summary

This session is a direct continuation of the prior segment (recorded in
`docs/checkpoints/2026-09-21-2102.md`): after a large `src/` reorg, four
parallel `/rush --role reviewer` agents produced perf/quality reviews of
`alloc_core/`, `registry/`, `global/`, and `concurrent/`; I verified every
finding against real source and filed 19 tasks (#1980-#1998), each with
full verified detail. The user then invoked `/babygoal давай реализуем все
задачи с помощью /wrush`, which armed a `/babysit` cron (job `872482b5`,
every 15m) and began working the task queue. Wave-1's 5 parallel rush
agents hit this account's request-rate ceiling (2 died on rate-limit
errors, 3 more hit their own 120-minute timeout) — after that, EVERY
remaining task in this entire session (from #1983 onward) was implemented
directly by me, solo, in an isolated `git worktree` per task, with the
same rigor throughout: read the real source first, make the minimal
correct change, run `rustfmt --check` on touched files, run the full
6-row clippy matrix (`default` / `experimental` / `--all-features` /
`hardened medium-classes internals` / `production` / `production
internals`, matching `npm run check`'s CI-identical rows) plus `cargo
test --features "production internals bench-internals batch-api"`,
zero-trust-review my own diff, commit in the worktree, `git cherry-pick`
onto `main`, then remove the worktree and branch. Every single task this
session hit exactly ONE test failure across every verification run: the
already-known, already-documented, path-length-sensitive
`tagged_index_stack_ab_runner_scratch_guard::build_check_success_leaves_no_scratch_root`
LNK1104 flake (confirmed present in every worktree, absent in the primary
checkout) — never a new regression, always the same pre-existing one.

Tasks #1980 through #1994 are ALL completed and merged to `main` (commit
`20e64ca4` is HEAD as of this checkpoint). Notable non-mechanical
decisions along the way: #1989 deliberately used `AtomicBool` instead of
the task's own suggested `AtomicUsize` (a post-push increment can
underflow under concurrent drain/push interleaving); #1982 was landed as
`fix(perf)` with NO speedup claimed after discovering `benches/
perf_gate_iai.rs`'s `alloc_zeroed_*` benches are a `black_box` stub under
plain `production` (filed as new task #1999, still pending); #1993
extracted a shared `drain_segment_ring` helper collapsing 3 near-identical
ring-drain copies (`RingDrainOutcome` enum, careful per-feature-combination
`#[allow(unused_variables)]` tuning across ~6 narrow cfg combos, all
verified individually); #1994 was explicitly a LARGE 10-item duplication
cleanup — I completed the 5 smaller/safer sub-items (counters.rs walk
dedup building on #1986, `drain_heap_overflow`'s cfg-arm dedup,
`find_segment.rs`'s numa/non-numa self-heal dedup via a new `finalize_hit`
helper, `carve_batch`'s double-computation hoist, and dropping an
unhonourable `#[inline]` hint) and explicitly split the 4 LARGER,
higher-risk hot-path items (magazine-issue 6-copy consolidation,
`refill_magazine_slow` vs its virgin sibling, `dealloc_batch_small` vs
`dealloc_own_thread_with_base`'s guard-chain duplication, `reclaim_offset`
vs `reclaim_offset_checked`) into a new follow-up task, #2000, rather than
either rushing them unsafely or silently dropping them.

**#1995 is IN PROGRESS right now, mid-verification, at the moment this
checkpoint was requested.** #1995 is explicitly framed by its own task
description as "Priority: low; measure first ... let it decide whether
the flag is worth it" — a census, not a code change. I built a small
permanent probe, `examples/task1995_bind_less_cold_arm_census.rs`
(registered in `Cargo.toml` with `required-features = ["production",
"internals"]`), that exhausts the registry cheaply (calls
`HeapRegistry::claim()` 4095 times directly from ONE thread — this raw op
never touches the calling thread's own TLS, so no real multi-thread
reproduction is needed) and then measures a SEPARATE fresh thread's
`alloc`/`dealloc` cost while bind-less, using the EXISTING
`dbg_fallback_lock_acquisitions()` counter as a path-activation oracle.
Ran the probe 3 times: `fallback_lock_delta` was EXACTLY `2000` (== N) in
all three runs, proving (not assuming) that every exhausted-thread `alloc`
call independently redoes the full claim attempt (no negative cache exists
today) — the `dealloc` side does NOT touch the fallback lock at all
(confirmed: it routes via the already-documented `ForeignNoBind`
bind-less-dealloc fast path, `current_for_dealloc`, R6-OPT-P0-1). Absolute
added cost measured at roughly 40-70 ns/call over the warm-path baseline
(9-15 ns/call warm vs 55-85 ns/call exhausted; ratios 5.7x-6.2x across 3
runs, all noise-y `std::time::Instant` timings, not criterion-controlled —
explicitly flagged as such in the report). Wrote up the full report,
`docs/perf/TASK1995_BIND_LESS_COLD_ARM_CENSUS.md`, with a raw log
(`docs/perf/_raw_task1995_bind_less_cold_arm_census.log`) and a summary
CSV (`docs/perf/TASK1995_BIND_LESS_COLD_ARM_CENSUS_summary.csv`), verdict
**NULL / NO-GO** on building the negative-cache flag now (case (a)'s
`> MAX_HEAPS = 4096` simultaneously-live-threads trigger is itself a
pathological deployment shape; case (b), TLS-teardown-during-allocation,
is argued structurally as narrower still, not measured — forcing real
thread-destructor ordering isn't portably reproducible from a probe).

**Exact point of interruption:** all of the above (probe, report, raw log,
summary CSV, Cargo.toml registration) exists ONLY in the worktree
`worktrees/fix-negative-cache-census` (branch `fix-negative-cache-census`,
based on `20e64ca4`) — UNCOMMITTED there, and NOT YET merged to `main`. I
had just launched the mandatory full 6-row clippy matrix + test-suite
verification in the background (`run_in_background: true`, task id
`bkfaeu0rv`, log path `/tmp/negcache_census_verify.log` inside that
worktree) and was blocking on its result via `TaskOutput` when the user
interrupted that specific tool call to invoke `/ccheckpoint` instead. **I
do not yet know whether that verification run passed.** Before resuming
#1995, the very next step must be to check `/tmp/negcache_census_verify.log`
inside `worktrees/fix-negative-cache-census` for `CLIPPY_OK` and the test
result line (expect only the known LNK1104 flake to fail, as in every
other task this session) — do NOT assume it passed, and do NOT re-run the
whole matrix blind without first checking whether the backgrounded run
already finished.

## Active goal

None (`/goal` was never invoked this segment; the `/babygoal` skill's own
contract — the `/babysit` cron + this TaskList — is the only automation in
force).

## TaskList

### in_progress
- #1995 Add negative caching for the bind-less cold arm (registry-exhausted / teardown threads repeat full claim work per alloc) — census complete (NULL/NO-GO verdict drafted), full clippy+test verification launched but NOT YET CONFIRMED, nothing committed yet (all in worktree `worktrees/fix-negative-cache-census`)

### pending
- #1996 Harmonize the dbg_* hook gating in global/ (four stragglers vs CLAUDE.md's benchmark-hook rule 2)
- #1998 Narrow pooled_count/pool_cap from usize to u32 (8 B × 4096 slots) — explicitly conditional on a real footprint measurement per its own description
- #1999 Close the iai coverage gap: alloc_zeroed's non-virgin path is benched as a black_box stub (self-filed follow-up from #1982)
- #2000 Collapse the four large hot-path duplication clusters #1994 deferred (magazine-issue 6-copy consolidation, refill_magazine_slow vs its virgin sibling, dealloc_batch_small vs dealloc_own_thread_with_base, reclaim_offset vs reclaim_offset_checked) — self-filed follow-up from #1994, each independently landable, each needs iai-neutral confirmation given hot-path/safety-sensitive scope

### recently completed (this session, all merged to main)
- #1980, #1981, #1982, #1983, #1984, #1985, #1986, #1987, #1988, #1989,
  #1990, #1991, #1992, #1993, #1994, #1997 — see prior checkpoint
  (`2026-09-21-2102.md`) and this session's commit log for #1980-#1990;
  #1991-#1994 are new to this segment (commits `02259598`, `6e472c54`,
  `814ad194`, `20e64ca4`)

## Decisions

- Continued the established pattern from the prior segment: every task
  implemented solo (no more parallel rush agents after wave-1's rate-limit
  wall), in its own worktree, with personal zero-trust verification (full
  6-row clippy matrix + test suite) before every merge — no exceptions
  this segment.
- #1994 (a 10-item duplication-cleanup task): deliberately split into
  "5 done now" + "4 deferred to new task #2000" rather than either
  rushing the 4 large hot-path items unsafely or silently dropping them —
  the 4 deferred items touch real hot-path code (`hot.rs`,
  `alloc_core_small_reclaim.rs`) and safety-sensitive dealloc guard chains,
  warranting dedicated iai-neutral verification of their own.
- #1995: chosen to measure cheaply by exhausting the registry via 4095
  direct `HeapRegistry::claim()` calls from ONE thread (bypasses that
  thread's own TLS entirely) instead of a real many-thread reproduction
  (which would need 4097 real, simultaneously-alive OS threads) — this
  is what made the census fast (~0.1s) instead of a heavy/exhaustive test.
- #1995's verdict is NULL/NO-GO based on the measured evidence (mechanism
  confirmed via `fallback_lock_delta == N` exactly; absolute cost tens of
  ns/call) combined with the structural rarity of the trigger condition
  (>4096 concurrently-alive threads) — explicitly NOT dropped silently:
  the report states what would change the verdict (a real workload shown
  to spend measurable time in either cold arm).

## Open questions

- Did the backgrounded 6-row clippy + test verification for #1995
  (`bkfaeu0rv`, log `/tmp/negcache_census_verify.log` inside
  `worktrees/fix-negative-cache-census`) pass? **Unconfirmed at checkpoint
  time** — this is the immediate next thing to check upon resuming, before
  any further action on #1995.
- Two long-standing unexplained detached worktrees
  (`worktrees/head-check`, `worktrees/sefer-baseline-ae87141e`, both at
  `ae87141e`) remain untouched, flagged in prior checkpoints, still not
  investigated or asked about.
- A third foreign worktree, `worktrees/src-review-xa-20260920` (detached
  at `e2ab999d`), belongs to a different, independently-observed rush
  session that committed directly to `main` earlier in this overall
  session — left untouched per the standing don't-touch-other-agents'-work
  rule.

## Repo state (from the PRIMARY checkout, D:\dev\rust\sefer-alloc)

```
(clean — git status --short: no output)
```
```
20e64ca4 fix(perf): collapse five verified registry/alloc_core duplication clusters
814ad194 fix(perf): factor the triplicated ring-drain body into one helper
6e472c54 docs(config): fix seven false/stale doc claims flagged by the review wave
02259598 fix(perf): remove three flagged dead-code items
90872190 perf(opt-in): fuse stats()' two registry slot-array walks into one pass
```

**Worktrees present:** `worktrees/head-check` (detached `ae87141e`,
unexplained, untouched), `worktrees/sefer-baseline-ae87141e` (detached
`ae87141e`, unexplained, untouched), `worktrees/src-review-xa-20260920`
(detached `e2ab999d`, foreign session's, untouched), and
**`worktrees/fix-negative-cache-census`** (branch `fix-negative-cache-census`,
based on `20e64ca4`, **contains #1995's uncommitted work** — the probe,
its `Cargo.toml` entry, the report, raw log, and summary CSV — plus an
in-flight/unconfirmed background verification run). This last one is
NOT yet ready to remove — it holds real unlanded work.

**Not pushed** to `origin` this segment (the earlier CI-green push,
`2fe99299`, was a separate, already-completed prior action from before
this session's review/task-filing/implementation work began).

**Active babysit cron:** job `872482b5`, every 15 minutes
(`7,22,37,52 * * * *`), session-only. Ticks so far have all reported
"still running #<id>" — no stall recovery has been needed.
