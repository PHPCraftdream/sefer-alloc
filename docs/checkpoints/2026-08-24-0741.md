# Checkpoint — 2026-08-24 07:41 [numa-shim 0.2.0-prep — three architecture decisions implemented, pushed, CI in flight, twelfth @oh review not yet launched]

## Session summary

Direct continuation of the numa-shim publication-readiness campaign (see `docs/checkpoints/2026-08-23-2037.md` for the prior checkpoint, taken mid-session). Since that checkpoint: the CI run triggered by the first push (landing SHA `66f5ec2`) came back genuinely RED — not a Bash-tool watch-timeout artifact this time, confirmed via `gh run view --json jobs`. Root cause found by direct log inspection: `tests/dbg_hook_safety_tripwire.rs` (a workspace-wide static scanner enforcing CLAUDE.md's R25-1/R30-2 rule that every crate-public `dbg_*` hook must be `bench-internals`-gated or individually allowlisted) had never classified `crates/numa-shim/src/lib.rs::dbg_node_resolution_for_cpu` (added by task #1266 earlier this session) — a gap BOTH prior independent `@oh` reviews missed (they called it "a convention point, not a defect" without realizing an automated tripwire enforces it repo-wide). Fixed by adding the hook to the `PURE_OBSERVERS` allowlist (task #1287, commit `99ecef4`), verified locally, pushed, confirmed CI green on that landing SHA (run `32659791510`, all numa-shim jobs + MSRV green).

A user-facing `/ox` review (a different model/command than `@oh`, invoked directly by the user, NOT delegated by me) then produced a sharp, well-verified critique of the two consultation-derived decisions from the prior checkpoint. I independently verified every one of its concrete file:line citations before accepting anything. Its most important catches: (1) the `#[cfg(not(numa_shim_mock))] fn main() {}` bench stub `@fh` had proposed for the mock-cfg migration is itself a "vacuous-green" hazard (a forgotten cfg would make the bench silently report success while measuring nothing) — needed a fail-loud guard instead; (2) `@fh`'s "zero code change" framing for the doc-hidden-modules decision was already inaccurate, since the tripwire scanner itself proved that `pub`-ness (not just `#[doc(hidden)]`) is what actually creates exposure; (3) `docs/NUMA_RELEASE_GATE.md`'s own text has an explicit "skip only for patch releases that don't touch these files" clause that a 0.2.0 release touching `crates/numa-shim/**` does NOT qualify for, so shipping with F8 Phases 2/4 outstanding is a knowing policy override, not a neutral default.

I asked the user three follow-up decisions via `AskUserQuestion` (re-explaining the doc-hidden question in simpler terms after the user said "не понял" the first time): (a) mock-feature mechanism — user re-confirmed `@fh`'s option (a), cfg conversion; (b) doc-hidden modules — user picked (d) (document-only, "just predupredit v dokah") OVER the `/ox`-recommended (e) (real privacy via a second cfg) — a live disagreement the user resolved in favor of the simpler option; (c) F8 Phase 2/4 — user chose "publish with a documented gap" OVER `/ox`'s recommendation to block, an explicit, informed risk acceptance.

Filed and executed three tasks implementing these decisions, each via the established `crush run` + git-worktree pipeline, each zero-trust reviewed personally before merging:
- **#1288** (by far the largest — 24 files, +488/−380): converted `numa-shim`'s `mock = []` Cargo feature to a build-time `--cfg numa_shim_mock` flag, spanning numa-shim itself, the root crate's 8 mock-consuming test files + `numa-aware-mock` feature, and 15+ CI rows with `expectTest`-style sentinel greps against the exact vacuous-green hazard `/ox` flagged. Also closed correctness item 42 (moved from `ACTIVE.md` to `ARCHIVE.md`/`RESOLVED.md`). One real merge conflict in `lib.rs` (both #1288 and #1289 touched the same doc-comment/cfg-attribute line) resolved by hand, re-verified both cfg arms green after resolution.
- **#1289**: documented both `#[doc(hidden)]` modules (`cpumap`, `linux::dbg_node_resolution_for_cpu`) as explicitly SemVer-exempt (the `serde::__private` convention) — option (d), zero code/visibility change.
- **#1290**: recorded an explicit, dated F8 Phase 2/4 risk-acceptance waiver (`docs/NUMA_GATE_2026-08-23_0.2.0_phase24_waiver.md`), carefully scoped to NOT waive the still-owed Phase-1 final re-run or Phase 3's in-guest remainder.

**A second real CI-breaking bug was caught by ME, not by any sub-agent, running `npm run check` in the WORKTREE first (failed with a Windows path-length artifact, correctly diagnosed as unrelated to the code change by comparing against the main worktree's shorter path) and then in MAIN after merging** (this second run failed for real): `tests/no_stale_doc_references.rs::correctness_index_recently_resolved_pointers_carry_verdicts` — a test enforcing 1:1 correspondence between `RESOLVED.md`'s "Recently resolved" pointer entries and `ARCHIVE.md`'s actual closure entries. Task #1288's own agent had added item 42's archive closure correctly but wrote a NEW, non-matching pointer summary in `RESOLVED.md` instead of reproducing the archive entry's ORIGINAL first-paragraph headline verbatim (the test requires exact whitespace-normalized headline correspondence, not just same item number), used the wrong archive-filename string in the pointer (`docs/correctness-open-items/ARCHIVE.md` instead of the historical literal `CORRECTNESS_OPEN_ITEMS_ARCHIVE.md` string every other pointer uses and the test's own detection filter greps for), and placed the pointer at the WRONG POSITION in the file (archive-entry order must match pointer order; item 42 is the archive's LAST entry but the bad pointer sat first). Fixed by reading the actual test source code line-by-line to understand its exact matching algorithm (not guessing), rewriting the pointer to be a verbatim copy of the archive's original first paragraph, and moving it to the end of the pointer list. Verified via the specific failing test, then a full `npm run check` (ALL GREEN).

Pushed this second batch (landing SHA `8f9bdf9`) after asking the user for fresh, explicit re-authorization (per the system rule that one push approval doesn't generalize to later pushes) — the user re-confirmed. **CI run `32693675209` is IN PROGRESS as of this checkpoint** — not yet confirmed green.

**The Stop hook fired twice this session with substantive, correct feedback**, both times validating that my instinct to stop was premature: once correctly noting CONDITIONAL GO ≠ GO, once correctly noting 3-of-4 babygoal sub-steps were done but no fresh `@oh` review had run yet. Both times I engaged with the feedback rather than overriding it — asked the user for the blocking decisions, then continued.

## Active goal

The standing `/babygoal` Stop-hook condition (verbatim, re-confirmed active by the hook's own feedback messages this session): *"реализуй задачи по numa-shim с помощью агентов /crush & worktrees, в конце (когда решишь все таски) проведи ревью на готовность крейта к релизу с помощью агента @oh — после ревью заведи таски и снова решай их. Делай такие циклы пока ревью не скажет GO на публикацию и остановись перед публикацией."* Still NOT satisfied: no unconditional GO has been returned by any `@oh` review yet (the last one, item 102/eleventh review, returned CONDITIONAL GO with three conditions — one now closed, C1/push-CI-green in progress, and the architecture decisions it didn't even know about are now implemented). A twelfth `@oh` review is the clear next step once CI confirms green on `8f9bdf9`.

## TaskList

### in_progress
(none — #1288/#1289/#1290 all completed and merged since the prior checkpoint)

### pending
- #657 numa-shim — verify/prepare for crates.io republish (blockedBy: #1262)
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #662 Root sefer-alloc: design note for applying bench-scale-tool alongside criterion/iai
- #763 Root sefer-alloc: implement bench-scale-tool per the approved design (blockedBy: #662)
- #1091 [perf-regress] Найти и устранить регрессию ~10-13% Ir в диапазоне 42d8d223..42d42061 (item 56)
- **#1262 [numa-shim] F1 (P0): version bump to 0.2.0 — content fully specified in its own task description (crate manifest, root pin, README's 3 "0.1" pins, dated CHANGELOG consolidation, then F8 Phase-1 gate re-run LAST per E1's ordering rule, then tag+publish) — NOT YET STARTED. This is now the last non-owner-gated numa-shim task; everything else in the campaign is either done or needs a twelfth review's verdict first.**

### recently completed (this checkpoint's session slice, most recent first)
- #1288 mock Cargo feature → build-time `--cfg numa_shim_mock` (+ RESOLVED.md pointer fix as a same-day follow-up commit, not its own task)
- #1290 F8 Phase 2/4 explicit risk-acceptance record
- #1289 doc-hidden modules → semver-exempt documentation
- #1287 CI red fix: `dbg_node_resolution_for_cpu` tripwire classification

## Decisions

1. **Mock feature → build-time `--cfg numa_shim_mock`, un-staged, at 0.2.0.** Confirmed twice (once via `@fh`, once re-confirmed after the `/ox` review). Implemented in #1288 with a fail-loud (runtime panic, not `compile_error!`, not a silent stub) bench guard per `/ox`'s specific catch.
2. **Doc-hidden modules (`cpumap`, `linux::dbg_node_resolution_for_cpu`) → option (d), document-only semver-exemption — the user's explicit choice OVER both `@fh`'s original (d)-vs-(c) framing AND `/ox`'s counter-recommendation of (e) (real privacy via a second cfg).** This is a live three-way disagreement among reviewers that the human resolved in favor of the simplest option. Not revisited since.
3. **F8 Phases 2/4 → publish with a documented, dated risk-acceptance waiver — the user's explicit choice OVER `/ox`'s recommendation to block the release.** Recorded in a new standalone file, carefully scoped to not waive Phase 1's final re-run or Phase 3's remainder.
4. **Push authorization is NOT durable across turns within this same campaign** — re-asked and re-confirmed before the second push of this checkpoint's slice, even though the user had already authorized a push once earlier in the same session for the same crate. Treated the system rule ("one approval does not generalize") as binding even under Stop-hook pressure to keep moving.
5. **A stale-path-length `npm run check` failure in a worktree was correctly diagnosed as an environment artifact (not a real regression) by comparing against a `cargo fmt --all` run in the shorter main-worktree path before merging** — avoided wasting a debugging cycle chasing a non-bug, but did NOT skip the mandatory full-gate run after merging into main (where it's not a worktree-length problem), which is exactly what caught the second, REAL RESOLVED.md/ARCHIVE.md pointer bug.

## Open questions

1. **Is CI run `32693675209` (on landing SHA `8f9bdf9`) actually green?** Unconfirmed as of this checkpoint — it was `in_progress` at write time. Must be checked via `gh run view --json status,conclusion` (not a bare `gh run watch` retrust) before treating C1 as closed for a second time.
2. **When CI is confirmed green, launch the twelfth independent `@oh` review** (matching this session's own established pattern — read-only, no sub-agents, timestamped report, commit report + new correctness-index item in the SAME commit before acting on any finding). This is the direct next action per the still-unsatisfied babygoal condition.
3. **Task #1262 (the actual 0.2.0 version bump) has not been started.** Its own task description already fully specifies the site list and ordering (Phase-1 gate rerun LAST). Whether to execute it before or after the twelfth review is itself a question the review's own verdict should settle — a NO-GO would likely need to be resolved first; a GO might make #1262 the very next and possibly final step before the actual `cargo publish`.
4. Same standing question from the prior checkpoint, still unresolved and not revisited this session: the unexplained `.claude/worktrees/agent-*` directories and any other artifacts from parallel `crush` usage outside this visible session (the `/ox`-reviewer-invoking user turn this session at least partially explains the mystery — the user is directly running review tooling in parallel with this session, as suspected).

## Repo state

```
?? docs/checkpoints/2026-08-23-2037.md
```
```
8f9bdf9 fix: task #1288 — repair the missing item-42 RESOLVED.md pointer
04b115c merge: task #1288 — mock Cargo feature to build-time --cfg numa_shim_mock (48ab4e5)
48ab4e5 feat(numa-shim)!: task #1288 — convert mock Cargo feature to build-time --cfg numa_shim_mock
c00fd99 merge: task #1290 — F8 Phase 2/4 explicit risk-acceptance record (099a4cf)
099a4cf docs(numa-shim): task #1290 — record explicit owner risk-acceptance for F8 Phase 2/4
99ef477 merge: task #1289 — document cpumap/linux modules as semver-exempt (b6db2ac)
```

`origin/main` = `8f9bdf91eadc344a218a81b4e2e8cfd8d04239b3` (pushed this session, matches local `HEAD`). CI run `32693675209` in progress on that SHA — outcome unconfirmed (see Open questions #1).
