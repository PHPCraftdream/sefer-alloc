# Checkpoint — 2026-08-27 12:30 [size-classes: 5 review rounds closed, push in flight]

## Session summary

This session continued the `size-classes` crate's pre-publication hardening campaign, picking
up after a `/resume`d checkpoint. Five more independent review rounds landed and were fully
processed with the same zero-trust discipline as every prior round (personally re-verify every
finding against source/empirically before acting — never trust a review's prose):

1. **Sol-codex run 9** — GO, one trivial CI-comment-tense fix.
2. **MS prepublish review, round 1** (`docs/reviews/2026-08-27-081200-...-MS.md`) — NO-GO with
   3 claimed blockers. Processed: P1-1 (`class_for`'s debug-assert-only align validation) was a
   genuine design question, delegated to `@fh` (same pattern as an earlier session's Sol-run3
   delegation) — `@fh` recommended adding a checked `try_class_for(size, align) ->
   Result<Option<usize>, InvalidAlign>` twin (mirrors `Layout::from_size_align` vs
   `_unchecked`), implemented and committed. P1-2 (claimed the crate's own doc is wrong about
   const-eval overflow following the `overflow-checks` profile) was **empirically DISPROVEN** —
   built a minimal repro (`const RESULT: usize = bad(usize::MAX)` with unchecked `x+1`),
   confirmed on both rustc 1.97 and the crate's pinned MSRV 1.88 that `--release`
   (`overflow-checks=false`, the default) lets a const item silently wrap with NO compile
   error — the crate's doc was already correct. P2-1 (tighten `build_size2class` to reject
   non-multiple-of-`min_block` table entries) was REJECTED — would have broken an existing
   load-bearing regression test that deliberately uses non-multiple entries. Two other findings
   confirmed and fixed (a real second bug in the `reference_table` test oracle; a severed
   perf-claim citation restored).
3. **`@oxx` (Opus max effort) independent prepublish review** — the reviewer that found the
   actually-important gap the whole campaign had missed: **`origin/main` (SHA `74b49c8`) is
   CI-red**, and the session's dozens of local commits had never been pushed/CI-verified. All
   of `@oxx`'s ~12 code/doc findings (P0–P4) were processed and fixed. Then **4 parallel `@sl`
   agents** (explicitly requested by the user: "используй для этого агентов @sl") closed the
   previously-deferred P3 items: bench coverage (wall-clock jump-vs-walk comparison — jump wins
   by ~12% even at this crate's small N=49 table, confirmed by direct measurement, not just
   iteration count; multi-jump/`None`-after-search/density rows, each with a path-activation
   oracle test), the root consumer's LUT-duplication "fix" (personally independently
   re-verified the agent's own claim that the linked binary is byte-size-IDENTICAL before/after
   via a `git stash` before/after build of `examples/global_allocator` — 217088 bytes both
   times), a ~10x speedup of an exhaustive 4.9M-call sweep test (fault-injected 3 different bugs
   myself to confirm the narrowed test still catches them), and a read-only MSRV-floor research
   task (production floor ~1.81, test+bench floor ~1.87, no code changed).
4. **MS prepublish review, round 2** (`docs/reviews/20260827-111115-...-round-2-MS.md`) — NO-GO
   with 3 claimed items. P1-1 was the **exact same const-eval claim from round 1, re-rejected**
   with the same repro (the reviewer explicitly said it read no prior review history). P1-2 was
   a genuine, real gap: `class_for`'s own doc said an invalid-align violation "is otherwise
   silent... can only produce a wrong CLASS CHOICE" — false for `(size,align)=(0,0)`, which
   panics via an unconditional bounds-check (a fact I had already discovered and documented in
   `try_class_for`'s own test comment earlier this session, but never backported to
   `class_for`'s own doc paragraph) — fixed. Three P3 items also fixed: a duplicated README
   paragraph (my own bug from an earlier "Memory cost" section edit) + a broken
   rustdoc-style-link-in-Markdown; review-attribution citations removed from **published**
   rustdoc specifically (3 sites) while deliberately leaving test/bench `//` comments alone
   (purging those would reverse this whole campaign's entire documentation convention — out of
   scope for this fix); a benchmark perf-claim citation that pointed at an unreachable git
   commit SHA, softened to point at the crate's own reproducible bench file instead. One item
   (P2-2, an external tarball/path consumer smoke fixture) explicitly deferred as bigger scope,
   recorded in task #660, not built.

After all 5 rounds, the user asked me to investigate what remains open in task #660. Finding:
**the crate's code/docs are review-exhausted** (5 independent reviewers, all zero-trust
verified) — the only real remaining gate is operational: `origin/main` is still CI-red on the
same SHA, and 63+ local commits had never been pushed. Along the way I also found and fixed a
housekeeping gap: the MS-round-2 review report file itself had never been committed (unlike
round 1) — committed it as a catch-up.

The user then said **"давай сделаем коммит всего и пуш, и проконтролируем ci"** (let's commit
everything and push, and monitor CI) — explicit authorization for both actions per the
project's standing rule that pushing/committing require explicit requests. I:
- Committed the 6 untracked `rush`-wave review reports (never auto-committed since `rush`'s own
  tooling doesn't write to git) — commit `9202d66`.
- Committed the 4 untracked session checkpoint files, matching this repo's own established
  precedent of periodically committing `docs/checkpoints/` — commit `10856a8`.
- Found 13 `.check-*.log` scratch files (8.5 MiB, zero prior git history — raw `npm run check`
  stdout captures) that had accumulated untracked. Exercised judgment: did NOT blindly commit
  these (no established pattern, matches this repo's own "raw logs are scratch by default"
  philosophy elsewhere) — deleted them and added a `.gitignore` pattern (`/.check-*.log`) so
  they don't recur — commit `6697810`.
- Confirmed working tree fully clean (`git status --short` → empty) before proceeding to the
  mandatory pre-push gate.

**Currently in flight:** `npm run check` (the mandatory pre-push gate per this repo's own
CLAUDE.md rule — fmt, 6 clippy rows, 8 test rows, `iai`, plus this repo's various other
`verify-*`/`stale-artifact-diagnosis`/`vmem-*` steps, 52 steps total) is running in the
background (Bash tool background ID `bkxymnf5q`, output redirected to
`.check-prepush.log` — which is itself gitignored by the pattern just added). As of this
checkpoint it has reached the final `iai` step (perf-gate benchmark instruction counts for the
allocator core) and has not yet finished or reported a final PASS/FAIL. **I have not yet
pushed.** Once `npm run check` completes green, the plan is: `git push`, then read the landing
SHA via `git fetch && git rev-parse origin/main`, open the GitHub Actions run for that exact
SHA, and confirm it goes green (per this repo's own explicit CLAUDE.md rule about confirming
CI on the landing SHA, not assuming it from local success) — this is what the user asked me to
"проконтролируем" (monitor).

No `/goal`/`/babysit` timer is active this session.

## Active goal

None.

## TaskList

### in_progress
(none)

### pending
- #660 size-classes — first publish to crates.io (blocked on: push completing + green CI on
  the landing SHA — this checkpoint's own in-flight work; the code/docs are otherwise
  review-exhausted, 5 independent rounds this session alone)
- #661 tagged-index-stack — first publish to crates.io (untouched this session)
- #1091 [perf-regress] Найти и устранить регрессию ~10-13% Ir в диапазоне 42d8d223..42d42061
  (item 56) (untouched this session, long-dormant)

### recently completed
- #1521–#1527 MS prepublish review round 2 (7 findings: 1 rejected/re-disproven, 6
  fixed/addressed/deferred-with-record)
- #1517–#1520 four parallel `@sl`-agent tasks closing previously-deferred P3 items (bench
  coverage, LUT-dedup with independently-verified binary size, sweep-test narrowing, MSRV
  research)
- #1505–#1516 `@oxx` independent prepublish review (12 findings processed)
- #1500–#1504 MS prepublish review round 1 (5 findings processed: 1 design decision via `@fh`,
  1 rejected/disproven, 1 rejected, 2 confirmed+fixed)
- #1499 Sol-codex run 9 (1 trivial fix)
(38 tasks total from this session's review rounds, #1492–#1527, all completed — see individual
task descriptions/commit messages for the full per-finding trail; not re-listed exhaustively
here.)

## Decisions

1. **Delegated `class_for`'s release-mode invalid-align contract to `@fh`** (MS round-1 P1-1):
   chose to add a checked `try_class_for` twin over promoting the existing `debug_assert!` to a
   release-active `assert!` (would cost the hot path) or folding invalid input into `None`
   (would silently misroute a bad `align` into the caller's large path). Mirrors
   `core::alloc::Layout::from_size_align` / `_unchecked`.
2. **Rejected `build_size2class`'s "reject non-multiple entries" suggestion** (MS round-1 P2-1):
   verified it would break an existing load-bearing regression test
   (`build_size2class_bucket_need_overflow_flips_the_release_answer_for_a_hand_built_table`)
   that deliberately constructs a hand-built table with non-multiple entries to exercise a
   different, real overflow-clamp bug fix.
3. **Scoped the "review-archaeology" cleanup to published rustdoc only** (MS round-2 P3-2): did
   NOT purge task-ID/review-round citations from test/bench `//` comments (not rendered on
   docs.rs, unlike `///` doc comments) — a wholesale purge would reverse this entire session's
   (and prior sessions') established documentation convention; treated as out of scope for a
   single review-response fix.
4. **Did not commit the 13 `.check-*.log` scratch files** despite the user's "commit
   everything": zero prior git history, 8.5 MiB of raw `npm run check` stdout, matches this
   repo's own established "raw logs are scratch by default" philosophy (see `docs/perf/_raw_*.log`
   convention in CLAUDE.md) — deleted them and gitignored the pattern instead of committing.
5. **Committed the rush-wave review reports and session checkpoints** as legitimate content
   (established precedent for both categories exists in this repo's history), distinguishing
   them from the scratch logs above.

## Open questions

None outstanding from the user's side. The one operational question — "is the push actually
going to go green" — is being actively resolved by the in-flight `npm run check` run; no
decision is pending on the user.

## Repo state

```
?? docs/checkpoints/2026-08-27-1230.md
```
(this checkpoint file itself; nothing else untracked — the `.check-*.log` scratch files were
deleted and are now gitignored, per Decision 4 above.)

```
6697810 chore: gitignore .check-*.log scratch files (task #660 push prep)
10856a8 docs: commit session checkpoints (2026-08-26-0900 through 2026-08-26-2201)
9202d66 docs(review): commit size-classes rush wave 1+2 review reports
b0c286c docs(review): commit size-classes prepublish audit round 2 (MS) -- NO-GO, all findings processed and resolved
7f876dd docs(size-classes): task #1526 -- soften the mask-vs-division perf claim to reproducible-in-crate evidence (MS round-2 P3-3)
```

`origin/main` sync status: local `HEAD` (`6697810`) is far ahead of `origin/main` (still
`74b49c8`, confirmed CI-red on that SHA as of this session) — exact commit count not
re-measured at checkpoint time, but was 63 before this checkpoint's own 4 commits, so ~67.
**No push has occurred yet** — `npm run check` must go green first (in flight, background ID
`bkxymnf5q`), then `git push`, then confirm CI green on the actual landing SHA read via
`git fetch && git rev-parse origin/main` (not assumed from local success).
