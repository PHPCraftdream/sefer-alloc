# Checkpoint — 2026-07-25 [r18-planned]

## Session summary
This is the same long-running session that completed Rounds 13-17 (each a
review→task-queue→execution cycle). Round 17 (tasks #318-#328, R17-1..
R17-10 plus a CHANGELOG wrap-up) finished, was personally zero-trust
verified task-by-task, committed (12 commits, `70a8f2f`..`a99314b`), and
pushed to `origin/main` on explicit user request ("если все задачи
закрыты — сделай пуш и проконтролируй/наладь CI"). The push's CI run had
one failing job (`test (gated bodies + all-features)`) — diagnosed as a
known, pre-existing, self-describing "coarse wall-clock, noisy" flake in
`tests/regression_segment_table_tombstone_rebuild.rs` (already hardened
once before against OS noise, commit `68f5da7`; confirmed unrelated to
Round 17 since R17-6 only touched comments in that area). Re-ran just that
job via `gh run rerun --failed`; it passed on retry, CI is now fully green.
Separately, the scheduled nightly `perf-gate` had been red for two
consecutive days (07-24, 07-25) with enormous instruction-count deltas
(+51% to +215%); investigated and found this was caused by a stale cached
baseline (from before R14-7's `MAX_SEGMENTS` raise) that R17-3's own fix
(already in this push) resolves — manually dispatched `perf-gate` via
`workflow_dispatch` against post-push `main`, confirmed it passes and the
baseline cache was refreshed.

After the push, the user asked for an `@oh` (Opus) agent to independently
review whether Rounds 13-17 actually sped up the code, what else could be
sped up, and what needs improving — readonly, no builds. It wrote
`docs/reviews/2026-07-25-oh-review-r13-r17.md`; I read and spot-verified
several of its citations before committing it. **A SECOND review file
(`docs/reviews/2026-07-25-r17-readonly-review.md`) then appeared in the
working tree without any dispatch visible in this session's tool-call
history** — the same unexplained-provenance anomaly that occurred once
before with commit `5709c24` in Round 17. Its content was excellent and
personally verified (three specific claims checked line-by-line against
source, all confirmed accurate) — most notably, a much stronger,
falsifiable explanation for the twice-observed `STATUS_STACK_BUFFER_
OVERRUN` in `race_repro.rs`: the test's OWN 20-second watchdog calls
`std::process::abort()` on timeout, and on Windows/MSVC that resolves to
`__fastfail`, whose exception code IS literally `STATUS_STACK_BUFFER_
OVERRUN` (0xC0000409) — meaning both "unexplained" crashes were most
likely the test's own watchdog firing under heavy load, not allocator
corruption. The user then asked to also run a `/crush` agent for the same
kind of review; it was explicitly instructed to form independent
conclusions FIRST and only cross-check the other two reports afterward.
Its report (`docs/reviews/2026-07-25-crush-review-r13-r17.md`) found all
three "special-attention" items independently and went further on each:
formally proved the `kind_at` narrowing is sound (OPT-G only grows,
shrink takes the move-leg — checked and confirmed against
`alloc_core.rs:1701-1703` and `heap_core_free.rs:929-933` personally),
found the `#[cfg]` gating that check is WIDER than the actual promotion
predicate (compiles even where promotion itself is compiled off — checked
and confirmed against `heap_core_free.rs:774-780` vs `:245` personally),
found a proof-gap in R17-4's "zero hot-path cost" claim (iai only ever
ran under plain `production`, where the branch doesn't compile — not
under `production,medium-classes` where it's live), and found that
R17-7's own raw logs show standard deviation LARGER than the measured
mean delta (the environment cannot resolve an effect that size, a
sharper diagnosis than "not significant").

All three reviews were synthesized into `docs/reviews/2026-07-25-r18-
plan.md`, and 8 Round 18 tasks were filed (#329-#336, P1 through P3),
then further sharpened in a follow-up pass once the `/crush` review's
stronger evidence came in (updated task descriptions for #329, #330,
#331, #333 to carry the formal soundness proof, the cfg-alignment
requirement, the iai proof-gap, the refutation-test idea, and the
SD-vs-delta caveat). Nothing in Round 18 has been executed yet — this
checkpoint is written at the planning boundary, before any task work
begins.

## Active goal
The most recent standing instruction (not a formal `/goal` Stop-hook
condition currently armed, as far as I'm aware — no `/goal` command was
issued this segment): dispatch via `/crush` for task work, per the
user's repeated instruction earlier this session ("запусти /crush агента
для такого же ревью", plus the earlier `/goal`-style "продолжай решать
задачи с помощью агентов /crush" from before Round 17's wrap-up). Task
descriptions for #329-#336 all name `/crush` as their strategy
accordingly. If a fresh session resumes this, the user should confirm
whether `/crush` is still the intended delegation tool or whether to
switch (this session has switched tooling several times on explicit
instruction: `Agent(sh)` → `/crush` → `Agent(sh)` → `Agent(sx)` →
`/crush`).

## TaskList
### pending
- #329 R18-1 (P1): race_repro.rs watchdog abort() likely explains
  STATUS_STACK_BUFFER_OVERRUN
- #330 R18-3 (P1): narrow R17-4's kind_at(base) check to the
  promotion-reachable domain + align its #[cfg] with the actual
  promotion predicate + add first iai gate under production,medium-classes
- #331 R18-2 (P1): re-run R10-2 realloc kill-gate after R17-4's leak fix
  (blockedBy: #330)
- #332 R18-7 (P1): inventory PERF_PLAN_beat_mimalloc_small_medium.md's
  dormant task chain vs README's live 2.4-2.7x cold-carve gap
- #333 R18-4/R18-5 (P3): fix contradictory pad-target wording + sharpen
  class-aware-dirty recoverability wording
- #334 R18-6 (P2, design): evaluate a lightweight automated guard
  against stale derived-number literals in comments
- #335 R18-9 (P3, design): unified adaptive Large policy design doc
- #336 R18-8 (P3, process): tracking mechanism for dangling "open
  items" across docs/perf/*.md gate reports

No tasks are in_progress or completed within Round 18 yet — none of
#329-#336 has been started.

## Decisions
- Manually dispatched `perf-gate` via `workflow_dispatch` (rather than
  waiting for the next nightly cron) once I suspected R17-3's own fix
  (already pushed) would resolve the two-day-red instruction-count
  regression — confirmed correct, baseline refreshed, gate green.
- Accepted the second review file's (`r17-readonly-review.md`) content
  despite its unexplained provenance — chose to verify-then-trust rather
  than discard an unexplained artifact, consistent with how the earlier
  `5709c24` anomaly was handled in Round 17. Flagged, not silently
  accepted or silently rejected.
- Split R18-3's kind_at fix into TWO required changes (runtime size gate
  AND `#[cfg]` alignment) rather than accepting the first, narrower
  framing from the readonly-review alone, after `/crush`'s review proved
  the `#[cfg]` itself is over-broad — a stronger, independently-derived
  finding that changed the task's scope.
- Ordered R18-3 (kind_at narrowing) as a hard prerequisite (`blockedBy`)
  for R18-2 (R10-2 gate re-run) rather than running the stale-gate re-run
  first, so the eventual medium-classes verdict isn't contaminated by an
  avoidable extra per-free cost the narrowing removes.

## Open questions
- **Provenance of `docs/reviews/2026-07-25-r17-readonly-review.md`
  remains unexplained** — this is now the SECOND time in this session an
  unattributed file/commit has appeared without a visible dispatch (the
  first was commit `5709c24` in Round 17). Worth asking the user directly
  whether they ran a parallel process I'm not aware of, since a third
  occurrence would warrant real investigation into a possible session/
  tooling leak rather than continued honest-but-passive flagging.
- Whether `/crush` remains the user's intended delegation tool for Round
  18's actual task execution (not yet re-confirmed since the two review
  dispatches).
- The exact mechanism/exception code of `std::process::abort()` on this
  specific toolchain (`STATUS_STACK_BUFFER_OVERRUN` via `__fastfail`) is
  INFERRED from documented Rust/Windows platform behavior, not confirmed
  by a reproduction in this repo — task #329 includes a cheap refutation
  test (check for the watchdog's eprintln diagnostic in any preserved
  crash log) but whether those logs still exist is unknown.

## Repo state
```
?? docs/checkpoints/2026-07-24-r17-in-progress.md
?? docs/checkpoints/r17-complete.md
```
(both pre-existing checkpoint artifacts from before this segment, left
untracked per the checkpoint skill's own convention — not part of this
session's active work)

```
2214d88 docs(reviews): fold crush's third review into the Round 18 plan, sharpen tasks #329-#331/#333
de5de49 docs(reviews): third independent (crush) review of Rounds 13-17, sharpens all three special-attention findings
c51e339 docs(reviews): synthesize two Round 13-17 reviews into Round 18 plan (tasks #329-#336)
8ef52ff docs(reviews): independent @oh review of Rounds 13-17
a99314b docs: Round 17 CHANGELOG entry (#318-#327)
```

Local `main` is 4 commits ahead of `origin/main` (`a99314b`..`2214d88`) —
the four review/plan-synthesis commits made after Round 17's own push.
NOT yet pushed — no push has been requested for this segment of work.
