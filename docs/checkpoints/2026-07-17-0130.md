# Checkpoint — 2026-07-17 01:30 [round7-done-pushed-benching]

## Session summary

`sefer-alloc` (D:\dev\rust\sefer-alloc), a Rust allocator. This long session:
(1) finished Round6 + cross-version bench report (pushed earlier `461fe8f`);
(2) implemented the full **Round7** plan (#149–164) via **o4h agents**, one task
at a time, commit between tasks, agents self-gate, orchestrator reviews diffs +
commits; (3) ran the ONE final verification run (#170) personally; (4) **pushed
all 20 Round7 commits to origin/main** (`461fe8f..49046ef`); (5) removed the
babysit cron; (6) is now — at the user's request — **running comparative
benchmarks and about to write them into docs.**

**Round7 verdicts:** A (per-class segment directory) = **GO** (opt-in
`alloc-segment-directory`). B (incremental Windows commit, opt-in
`alloc-lazy-commit`) = mechanism correct + fully tested, but **headline NO-GO**:
first-heap commit can't move because the primordial segment is always eager
(deliberate B1 choice) and dominates the 4 MiB first-heap charge — filed as
deferred follow-up **#181 (R7-B6, lazy primordial)**. C1 (TCACHE_CAP) = NO-GO,
stays 16. C2 (pool-cap) = default stays 4/16 MiB, three documented presets.

**The final run (#170) was the most valuable phase — it caught THREE classes of
problem, all fixed & committed:** (a) `a834fca` lazy-commit tests didn't gate off
the `numa-aware` eager fallback → failed under `--all-features`; (b) `42f8343`
(R7-A7, #182) a REAL correctness bug — B3's decommit-on-pool-admission under
`alloc-lazy-commit` forgot to `clear_segment_directory`, desyncing the
incremental directory from a rebuild at the top medium class (found only under
`--all-features` = directory+medium+lazy-commit combo); (c) `49046ef` the
`scripts/miri.mjs` runner passed space-separated `--features` under `shell:true`,
so Windows re-split them → 3+-feature entries hard-errored and 2-feature entries
silently ran **0 tests** (`decommit_miri_cycle`, `regression_ring_drain_guard_miri`
were VACUOUS green under strict-provenance miri). Fixed by comma-joining features.
Final gate state: `npm run check` ALL GREEN; loom PASS; miri 14/14 non-vacuous
PASS (the two previously-vacuous entries now run 1+ real tests each); no UB.

**CURRENT WORK — benchmarks (user: "давай сделаем прогоны того что выиграли и
общие", then "давай запишем это в доки"):**
- **"What we won" — segment directory sweep, DONE** (`benches/segment_directory_sweep.rs`,
  ON vs OFF). Refill-miss judge, holes=0%, class=48 (SMALL_MAX): S=1 25→21ns,
  S=64 228→53ns (~4.3×), S=256 5038→128ns (~39×), S=1023 67515→400ns (~166×).
  class=25: S=256 3555→123ns (~29×), S=1023 67515→376ns (~180×). Parity at S≤3
  (no regression). Logs: /tmp/bench_dir_off.log, /tmp/bench_dir_on.log.
- **"General" — `npm run bench:table` (SeferAlloc vs mimalloc vs System),
  IN FLIGHT** at checkpoint time (criterion arms still measuring). Log:
  /tmp/bench_table.log (background task; will notify on completion).
- **PENDING (the actual ask): write both result sets into a docs file** — plan a
  new `docs/perf/R7_BENCH_RESULTS.md` (match R6_CROSS_VERSION_BENCH.md style)
  once bench:table finishes, containing the directory ON/OFF win table + the
  canonical 3-arm comparison + honest host-noise caveats.

**State:** babysit cron REMOVED (user paused the crate phase). `/goal` condition
("реализуй план… в самом конце сделаешь прогон") is SATISFIED — plan done, final
run green. The crate-extraction phase (#171–180) + #181 (B6) are filed, pending,
NOT started (awaiting explicit user go). Nothing about crates started.

## Active goal

`/goal` Stop hook may still be armed with: "реализуй план, используй агентов @o4h,
между тасками делай коммиты, после агентов не делай прогоны (поручай это им). В
самом конце сделаешь прогон." — this condition is now SATISFIED (Round7 done +
final run green + pushed). To clear the hook the user types `/goal clear`. The
current benchmark work is a separate user request, not part of that goal.

## TaskList

### in_progress
- (none — #170 final run completed; current benchmark work is not tracked as a task)

### pending — crate-extraction phase, NOT started (awaiting explicit user go)
- #171 CRATE-P1 malloc-bench-rs publish-prep
- #172 CRATE-P2 aligned-vmem 0.2 + numa-shim publish-prep
- #173 CRATE-P3 racy-ptr-cell
- #174 CRATE-P4 ring-mpsc + DirtyRouter
- #175 CRATE-P5 size-classes
- #176 CRATE-P6 globalalloc-model
- #177 CRATE-P7 tagged-index-stack
- #178 CRATE-P8 proc-memstat
- #179 CRATE-P9 paired-ab (blockedBy #178)
- #180 CRATE-P10 research deferred/skipped + in-place hygiene
- #181 R7-B6 lazy-commit the primordial (deferred; salvages B's headline; higher risk, bootstrap-critical — do NOT start without explicit go)

### recently completed
- #170 R7-FINAL (npm run check ALL GREEN + loom + miri 14/14 non-vacuous)
- #182 R7-A7 directory medium-class desync fix (`42f8343`)
- #149–164 all Round7 A/B/C + #165–169 crate research

## Decisions

- Push Round7 now, pause the crate phase, remove babysit — user's explicit choice
  ("Пауза — только пуш"). 20 commits pushed `461fe8f..49046ef`.
- The two hardening-runner + numa-aware failures found by #170 were fixed by the
  orchestrator inline (test/harness-only, self-validated by the re-run), while the
  real correctness bug (A7) was delegated to an o4h agent per the operating mode.
- miri.mjs comma-join fix retained (a genuine vacuity bug in the gate, not R7-new
  but exposed/expanded by A5's multi-feature entries).
- Benchmarks run by the orchestrator directly (measurement + interpretation), not
  delegated — one process at a time on the 16-thread host.

## Open questions

- **Write-to-docs is pending bench:table completion** — the doc should contain
  BOTH the directory ON/OFF win and the 3-arm general table; bench:table was still
  measuring at checkpoint time. Next step: read /tmp/bench_table.log's final
  canonical table, then write docs/perf/R7_BENCH_RESULTS.md.
- Whether to start the crate phase (#171+) — awaiting explicit user go (paused).
- Whether to also refresh README's benchmark section with the directory win —
  user's call (not yet asked).

## Repo state

```
(tracked tree clean; only ci_watch*.log untracked. bench logs live in /tmp, not repo)
```

```
49046ef fix(miri-runner): comma-join features so multi-feature entries aren't vacuous
42f8343 fix(r7-a7): clear directory bits on the lazy-commit pool-admission path
a834fca test(r7-b): gate lazy-commit tests off the numa-aware eager fallback
ad443d9 docs(r7-c2): pool-cap sweep → documented presets, default stays 4/16 MiB
cf22c96 docs(r7-c1): TCACHE_CAP re-sweep verdict — NO-GO, cap stays 16
```

`origin/main` is now up to date with local `main` (pushed through `49046ef`).
Babysit cron removed. Crate phase not started.
