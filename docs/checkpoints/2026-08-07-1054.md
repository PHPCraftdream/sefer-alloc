# Checkpoint — 2026-08-07 10:54 [sefer-region-round]

## Session summary

This session applied the user's own crates.io tools (`bench-scale-tool`, `captrack`) to
`crates/region/` (package `sefer-region`), then investigated further, then ran a full
correctness/safety audit, and is now working through the resulting fix list via `/crush`
under a `/babygoal`-driven, babysit-monitored round.

Sequence: (1) wired `bench-scale-tool` benches + a `captrack` capacity-telemetry probe into
`crates/region/` (commit `b5f7e7b`); (2) user asked to actually extract profit from the
tools — found and fixed a real bench-id substring-collision bug (`sync_region/insert`
containing `region/insert`), documented real perf/capacity numbers in the README (`f7749a2`);
(3) user asked to regression-test that bug and investigate wrapper overhead — added
`tests/bench_ids_isolatable.rs` and a `raw/*` SlotMap A/B comparison bench, found **zero**
measurable `Region<T>` wrapper overhead (`2fcf120`); (4) user asked to launch three parallel
`@fh` research agents (performance / logic-correctness / safety) over the whole crate —
produced `docs/reviews/2026-08-07-sefer-region-{performance,logic,safety}-review.md`
(untracked); (5) user asked a follow-up `@oh` agent to arbitrate the reports' one apparent
contradiction, confirm the most severe finding, and file tasks — produced
`docs/reviews/2026-08-07-sefer-region-work-plan.md` (untracked) and filed tasks **#664-673**;
(6) mid-flow, user asked for a **local skill** `.claude/skills/research/SKILL.md` (gitignored,
purely local) codifying the parallel-research-agents pattern for reuse — created, with an
explicit rule that sub-agents reply ONLY `done: <path>`, never a summary in the reply channel;
(7) user then invoked `/babygoal` with the literal instruction: implement the sefer-region
tasks via `/crush`, commit between tasks, and after all work — `/checkpoint`, update
CHANGELOG, commit all markdown, run a final `@oh` review.

**Headline finding (task #664, CLOSED):** `crates/region/src/region.rs`'s "Generation
saturation" doc comment falsely claimed slotmap "retires" a saturated slot rather than
wrapping into alias. slotmap 1.1.1 has no retirement code at all — genuinely re-reproduced at
full scale (2^31-1 cycles, ~12.4s release-mode churn on one slot) a real ABA alias: a stale
handle became bit-identical to a fresh one and `remove(stale)` stole the fresh handle's live
value. Memory safety unaffected (logic/aliasing defect, not UB). Fixed docs-only across the
whole workspace (region.rs, lib.rs, README.md, fuzz/, docs/PLAN.md, docs/INVARIANTS.md).

**Two real bugs the delegated `/crush` fixes themselves introduced were caught during
zero-trust review, not shipped:** (a) task #664's own delegated fix invented a wrong
mechanism explanation ("`remove_from_slot` calls `wrapping_add(1)` twice") — the real
mechanism is insert setting the low bit (`version | 1`) and remove separately incrementing
(`wrapping_add(1)`) in two different functions; independently re-read slotmap's actual source
and corrected it before committing. (b) that same delegated fix copy-pasted the specific
"2^31" slotmap figure onto an UNRELATED mechanism — `docs/ALLOC_PLAN.md`/`docs/INVARIANTS.md`'s
"M8 — generational coherence" entry, which describes the ALLOCATOR'S OWN segment-substrate
generation scheme (a real, shipped Phase-8 mechanism, NOT `sefer_region`/`slotmap` — confirmed
`sefer_region::Region` has zero internal runtime consumers in this workspace). Reverted that
specific figure to a hedged, honest statement rather than asserting an unverified number.

**Working strategy for the round:** sequential `/crush` delegations, one task at a time, each
followed by a PERSONAL zero-trust review (full diff read, independent re-run of
tests/clippy/fmt/doc/no_std-build, sometimes an injected counterfactual) before committing —
matching this project's own established culture. Every task so far has surfaced at least one
small thing the delegated fix got wrong that was caught and corrected before landing (a
misleading `mem::forget` in a test, a stale "None forever" doc line task #664 missed due to
backtick-formatting defeating a grep, the two issues above). One transient false alarm during
task #668's review (a test appeared to still fail after reverting a counterfactual injection —
traced to stale test-binary caching from a rapid edit/revert/rerun cycle, resolved via
`cargo clean -p sefer-region` + fresh rebuild, not a real bug) is recorded in that commit's
message so a future reader doesn't re-diagnose it.

**Landing order followed** (per `docs/reviews/2026-08-07-sefer-region-work-plan.md` §4):
#664 → #665 → #667 → #666 → #668 → #669 → [now: #672] → #670 → #671 (then #673 deliberately
deferred, per the work-plan's own recommendation — "no defect found, blocks nothing").

**Currently in flight:** task #672 (docs.rs-facing polish — dangling `docs/BENCHMARKS.md`
relative link, "Phase 3b" parent-workspace jargon leaking into published rustdoc,
`get_cloned`'s overclaiming opening line, and a staleness note for `contains()` that must be
worded CONSISTENTLY with task #664's already-landed 2^31-cycle qualifier, not as a fresh
unconditional "never" claim) — `/crush` session `sefer-region-task672` was launched and is
running in the background at the moment this checkpoint was written; its completion has NOT
yet been reviewed or committed.

**Not yet started:** #670 (de-vacuum the I3 test, add `Handle` layout/auto-trait static
asserts), #671 (measure tombstone-iteration cost, document capacity-never-shrinks), then the
four post-work tasks #674-677 (checkpoint [this one] → CHANGELOG → commit-all-markdown →
final `@oh` round review), all chained via `blockedBy`. #673 stays pending/deferred
indefinitely per the work-plan's own recommendation, not blocking anything.

**Babysit is armed**: cron job `8c5473ca`, fires at `7,22,37,52 * * * *` (every 15 min,
off-minute), session-only (not durable — dies if this session ends), auto-expires in 7 days
from when it was created earlier this session. It resumes task #672 (or whichever task is
`in_progress`) if no fresh progress signal is seen between ticks, per the standard babysit-tick
protocol already running successfully across this whole round so far.

## Active goal

A session-scoped Stop-hook goal is active (set by the `/babygoal` invocation), verbatim:
"реализуй задачи sefer-region с помощью /crush, между тасками делай коммиты. После
завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти
ревью агента @oh - пусть проверит всю работу. следуй плану" — the hook blocks session
stop until this condition holds and auto-clears once met; it is NOT yet met (post-work
tasks #674-677 are still pending).

## TaskList

### in_progress
- #656 sefer-region — verify/prepare for crates.io republish (the original parent
  investigation task; largely superseded by #664-673's more specific findings, left open
  pending the user's own publish/version-bump decision)
- #672 sefer-region: docs.rs-facing polish — dangling BENCHMARKS.md link, parent-workspace
  jargon, contains() staleness note (blockedBy: #664 [resolved])

### pending
- #657 numa-shim — verify/prepare for crates.io republish
- #658 aligned-vmem — publish 0.2.0 (local already bumped, crates.io still shows 0.1.0)
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #662 Root sefer-alloc — add bench-scale-tool alongside existing criterion/iai benches
- #663 4 dev-only sub-crates — bench-scale-tool + captrack (malloc-bench-rs,
  globalalloc-model, proc-memstat, proc-probe)
- #670 sefer-region: de-vacuum the I3 test and pin Handle's layout/auto-trait claims with
  static assertions
- #671 sefer-region: measure iteration over tombstones and document that capacity never
  shrinks
- #673 sefer-region: [UNVERIFIED — no defect found] add one contended SyncRegion measurement
  as a future decision gate (deliberately deferred, blocks nothing)
- #674 Post-work: /checkpoint the session after all sefer-region fixes land (blockedBy:
  #670, #671, #672)
- #675 Post-work: update CHANGELOG.md with the sefer-region round (blockedBy: #674)
- #676 Post-work: commit all markdown docs from this round (blockedBy: #675)
- #677 Post-work: run @oh final review of all sefer-region round work (blockedBy: #676)

### recently completed
- #669 sefer-region: correct the panic contracts on reserve/with_capacity/insert (commit
  `ecc5138`)
- #668 sefer-region: close the I5 drop-once and clear() zero-coverage gaps (commit `cec0333`)
- #666 sefer-region: document clear()'s partial-clear under a panicking T::Drop + pin it
  with a test (commit `0931d35`)
- #667 sefer-region: add the missing no_std CI build (commit `6ace228`)
- #665 sefer-region: fix the bench harness fidelity defect (commit `67062b3`)
- #664 sefer-region: rewrite the false "Generation saturation" doc (commit `395258e`)

## Decisions

- Chose **sequential** `/crush` delegation (one task in flight at a time) over parallel,
  because most of #664-673 have real ordering dependencies (#668 needs #666's doc landed
  first for its test fixture to match; #669/#672 need #664's corrected I2/I3 wording to stay
  consistent) and the user explicitly asked for a commit between each task.
- Chose to revert, not keep, the delegated #664 fix's specific "2^31" figure on
  `docs/ALLOC_PLAN.md`/`docs/INVARIANTS.md`'s M8 entry (the allocator's own segment-substrate
  generation scheme) rather than trust the copy-paste — no evidence the real mechanism (a
  custom, non-slotmap generation/tag scheme, e.g. `tagged-index-stack`'s own differently-
  scaled 48-bit tag budget) shares slotmap's exact wrap point.
- Chose docs-only fixes throughout #664-669/672 (no API changes, no `try_clear`, no
  `is_poisoned`, no checked-arithmetic change to `reserve`/`with_capacity`) — every task's own
  brief explicitly deferred behavior-level decisions to the maintainer; this round stays
  within that gate.
- Chose to defer #673 (contended `SyncRegion` measurement) rather than implement it now —
  the work-plan's own explicit recommendation ("no defect claimed or found... blocks
  nothing"), and it does not block the post-work chain (#674's `blockedBy` excludes it).

## Open questions

- **Version bump for 0.1.1** (task #656/#664's own "user decision gate"): whether the
  false-claim fixes and other corrections warrant a patch republish, and whether the
  already-published 0.1.0's false safety claim needs any advisory/yank consideration.
  Explicitly NOT decided by this session — maintainer's call.
- **Whether to publish the six crates.io sub-crates** (#657-661 and the root #656) — separate,
  pre-existing open question from before this session, untouched by the current round.
- **Task #656 itself** — should it be marked completed/superseded now that #664-673 cover its
  findings in more detail, or left open pending the version-bump decision? Not resolved this
  session; left as-is (`in_progress`).

## Repo state

```
?? docs/reviews/2026-08-07-sefer-region-logic-review.md
?? docs/reviews/2026-08-07-sefer-region-performance-review.md
?? docs/reviews/2026-08-07-sefer-region-safety-review.md
?? docs/reviews/2026-08-07-sefer-region-work-plan.md
```

```
ecc5138 fix(docs): correct the panic contracts on reserve/with_capacity/insert (task #669)
cec0333 test(region): close the I5 drop-once and clear() zero-coverage gaps (task #668)
0931d35 fix(docs): document clear()'s partial-clear under a panicking T::Drop + pin it with a test (task #666)
6ace228 build(ci): add the missing no_std bare-metal build for sefer-region (task #667)
67062b3 bench(region): fix the bench harness fidelity defect before the README perf table ships (task #665)
```
