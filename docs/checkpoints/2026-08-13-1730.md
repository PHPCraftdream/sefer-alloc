# Checkpoint — 2026-08-13 17:30 [aligned-vmem-round6-complete]

## Session summary

This session resumed from checkpoint `2026-08-13-0130.md` (rounds 1-4 of the `aligned-vmem` review campaign, merged to local `main`). The user then ran a full round 5 and round 6 of the same "review → decompose → parallel delegation in isolated worktrees → personal zero-trust merge → closing review → personal fix pass" campaign pattern that has now repeated six times on this crate.

**Round 5** (`docs/reviews/2026-08-13-aligned-vmem-round5-review.md`, findings Q1-Q9) was delegated via `/crush` in 5 worktrees, closed by a closing review (`...round5-closing-review.md`, QC1-QC9), personally fixed (commit `e60e46a`). After merging, the user asked to commit the checkpoint via `git commit --amend`, push, and confirm CI. **The push (30 commits, the first real push of this whole campaign since round 3) immediately turned CI red**: `test macos (production)` failed on `decommit_recommit_roundtrip` — a genuine, previously-undiscovered-by-this-crate correctness gap (macOS's `MADV_DONTNEED` is advisory-only for anonymous memory, unlike Linux, so `decommit()`+`recommit()` doesn't actually zero-fill or return RSS there). Diagnosed by comparing against the same CI run's Linux/Windows passes, fixed by scoping the test off macOS and documenting the gap (commit `9c777bc`, filed as `docs/CORRECTNESS_OPEN_ITEMS.md` item 48), pushed, confirmed CI green.

**Round 6** (`docs/reviews/2026-08-13-aligned-vmem-round6-review.md`, findings S1-S12) reviewed the post-`9c777bc` tree and found real residue in that CI-fix: 4 more public doc sites still stating the false guarantee (S1), an unestablished root-cause attribution — the CI evidence couldn't actually distinguish "MADV_DONTNEED is advisory" from "the madvise syscall itself failed" (S2), and — most pointedly — the claim "previously-undiscovered" was false at the repository level: the same hazard had been documented in `ci.yml`, a production code comment, and two `virgin-zero-skip` perf design docs since Round 9, long before this campaign (S3). This round was delegated differently from all prior rounds: **7 parallel `Agent(subagent_type: "sh")` instances instead of `/crush`**, each in its own isolated git worktree (#880-886, letters A-G). All 7 personally zero-trust reviewed and merged (2 hand-resolved merge conflicts on the same `docs/CORRECTNESS_OPEN_ITEMS.md` item 48, since 2-3 parallel worktrees had all added content to it; 3 personal fix commits for a test race condition, a README wording mismatch, and a "discovered" phrasing inconsistency). The round-6 CLOSING review (`docs/reviews/2026-08-13-aligned-vmem-round6-closing-review.md`, findings SC1-SC10) then found the pattern held a sixth time — two MEDIUM findings were direct products of worktree isolation (SC2: two agents wrote inconsistent Darwin-family scoping across 6 sites, no merge conflict since the edits didn't textually overlap; SC3: no CHANGELOG entry for round 6's own 17 commits, since the agent writing the *previous* entry's closing sentence couldn't see the 7th task's existence). All 10 SC findings were personally fixed in the closing pass (commit `1dbd6b4`), including unifying Darwin-family scoping across 7 sites, writing the round-6 CHANGELOG section, fixing a root-crate cross-file doc-drift (the same "crate-scoped review misses cross-crate contract changes" lesson from round 3's task #864), and 3 INFO-level hygiene fixes.

Full verification matrix (test ×2 feature sets, clippy ×3 rows, fmt, drift-guard, root-crate `cargo check`) is green as of the last commit. **Local `main` is now 18 commits ahead of `origin/main`** (which is still at `9c777bc`, confirmed green in CI from earlier this session) — none of round 6's work has been pushed yet. This checkpoint was invoked mid-flow, immediately after the round-6 closing-review fix commit (`1dbd6b4`) landed, before any push/CI-confirmation step was requested.

## Active goal

None — no `/goal` Stop hook is armed in this session.

## TaskList

### in_progress
- #657 numa-shim — verify/prepare for crates.io republish (blockedBy: #658)
- #658 aligned-vmem — publish 0.2.0 (local already bumped, crates.io still shows 0.1.0)
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io

### pending
- #662 design note for bench-scale-tool alongside criterion/iai
- #763 implement bench-scale-tool per approved design (blockedBy: #662)

### recently completed
- #880-886 aligned-vmem round-6 A-G: S1-S12 remediation (all 7 completed and merged, delegated via Agent/sh instead of /crush)
- #875-879 aligned-vmem round-5 A-E: Q1-Q9 remediation (all 5 completed and merged, from before this session's active window per the resumed checkpoint)

## Decisions

- **Delegated round 6 via `Agent(subagent_type: "sh")` in 7 isolated worktrees instead of `/crush`**, per explicit user instruction for this round specifically — first use of this delegation path in the campaign. Worked well but demonstrated a new residue class (SC2, SC3): agents in fully isolated worktrees cannot see each other's *content* even when their diffs don't textually conflict, so a merge can be clean while the merged text is internally inconsistent — something only a closing review, not per-task zero-trust review, can catch.
- **Diagnosed and fixed the macOS decommit CI failure by scoping the test rather than attempting a real fix** — the real fix (re-`mmap`(MAP_FIXED) over the range) needs its own safety analysis and review round; rushing it under a "make CI green" task would repeat the campaign's own "the fix introduces the next round's finding" pattern at higher stakes. Filed as item 48, explicitly left open.
- **For SC2's Darwin-family scoping inconsistency, chose to unify wording (docs) rather than change `madv_free_advice()`'s cfg (behavior)** — widening `MADV_FREE_REUSABLE` to tvOS/watchOS would be an unverifiable behavior change on hardware this session cannot test; narrowing the docs to state the truth (tvOS/watchOS fall back to `MADV_DONTNEED` for the lazy path too) is the zero-risk fix.
- **Bumped `docs/CORRECTNESS_OPEN_ITEMS.md` item 1's recurrence card to 6** (the "missing CHANGELOG entry" pattern) rather than treating round 6's SC3 catch as just another instance — explicitly noted the 2-round "caught within round" streak (rounds 4, 5) broke at round 6, strengthening the case for the still-undecided standing CLAUDE.md rule proposal.
- **Did not push** — round 6 was never explicitly requested to be pushed; the standing question (push + confirm CI) remains open, same as every prior round's checkpoint in this campaign.

## Open questions

- **Push local `main` (`1dbd6b4`, 18 commits ahead of `origin/main`'s `9c777bc`) and confirm CI green on the landing SHA?** This time there is a specific reason to expect a possible new signal: round 6 added two headline test artifacts (`macos_decommit_madvise_syscall_actually_succeeds`, `apple_silicon_page_size_is_16_kib`) whose entire evidentiary purpose is to run on real macOS CI — item 48's H1-vs-H2 question and item 43's macOS `_SC_PAGESIZE` verification both stay open until that push happens.
- Should the standing CLAUDE.md rule for "every round needs a CHANGELOG entry written within the round" finally be adopted, given it's now failed to self-enforce in 4 of 6 rounds (1, 2, 3, 6) despite the closing-review safety net?
- Is `aligned-vmem` ready for its 0.2.0 `cargo publish` (task #658)? Six rounds deep, no soundness hole found in any of them. The macOS decommit gap (item 48) does not block publishing per the round-6 review's own assessment, but the README's new "Platform caveats" section (now Darwin-scoped correctly) is publish-relevant and should be considered part of what ships. Publish itself remains explicitly gated on maintainer go-ahead, not requested this session.

## Repo state

```
?? docs/checkpoints/2026-08-13-0130.md
```

```
1dbd6b4 fix(vmem), docs: round-6 closing review — fix SC1-SC10, write CHANGELOG entries, commit both round-6 review docs
c1211a4 Merge vmem-r6g (task #886): fix guard header rationale + arrow-only-token strip (verified independently), write CHANGELOG entry for commit 9c777bc (S10+S11)
8718f8f Merge vmem-r6f (task #885): widen macOS caveat to Darwin family, fix reservation_len() uniqueness claim, record MADV_FREE_REUSE alternative (S7+S8+S9)
ec7bbe4 fix(vmem): zero-trust review of task #885 — align remaining 'discovered 2026-08-13' phrasing with task #883/S3's more accurate framing
97c1e8b Merge vmem-r6e (task #884): Apple-Silicon 16 KiB page-size assertion, correct item 43's macOS verification claim (S6)
```

Local `main` (`1dbd6b4`) is **18 commits ahead** of `origin/main` (`9c777bc`) — none of round 6's work has been pushed yet, as of this snapshot.
