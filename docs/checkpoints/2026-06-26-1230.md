# Checkpoint — 2026-06-26 12:30 [Phase 13.3 done + committed; campaign-critical ~90x perf regression discovered (#41)]

## Session summary

`sefer-alloc` — safe-by-construction allocator (mimalloc-class drop-in). This
session committed the post-12.6 **doc-hygiene** (`7447994`) and **Phase 13.3 —
inlined, arithmetic-only own-thread free** (`ea4dd89`), both verified by hand
under zero-trust. #27 (13.3) was executed by a `/crush` agent (session
`phase13_3`) which crashed TWICE mid-run (transient, exit 1) — first during
exploration (no edits), second AFTER completing the edits + a new test (crash on
the bench step). I took over verification myself: read the full diff
(own-thread `dealloc` now reads ONE `kind_at` byte instead of a full-struct
`read_at`+magic; class derived from `Layout` not `page_map` per §13; `#[inline]`
on hot pop/push/Node/classify/dealloc_small + SeferMalloc GlobalAlloc entries),
confirmed the new `tests/phase13_3_dealloc_layout_class.rs` is **non-vacuous via
a real counterfactual** (I temporarily switched dealloc to `page_map` derivation
→ both tests went RED → reverted → GREEN), ran the full suite green on
`alloc-core` / `alloc-global` / `alloc-global+alloc-xthread`, race x5 no-flake,
clippy 0 new. Committed at the phase boundary, marked #27 done. **The big
finding:** running the bench myself (the step crush died on) exposed
SeferMalloc/16B churn = **~1.9 ms** vs mimalloc ~11 us and System ~134 us — i.e.
**~180x slower than mimalloc**, against `MALLOC_BENCH.md`'s Phase-11 claim of
~21.5 us (competitive). I **isolated** it: stashed #27, benched HEAD `7447994`
alone → same ~1.9 ms → so #27 is NOT the cause; the ~90x regression is
**pre-existing, introduced somewhere in Phase 12** (the registry inversion). The
campaign's headline thesis "competitive with mimalloc" is therefore currently
**false**. Logged it as **#41** (with profiling hypotheses + bisect plan) and
also logged **#40** (a separate latent §13: `dealloc_small_by_segment` still
derives class from `page_map` on the TFS drain paths — `heap.rs:371` +
`heap_core.rs:206`). Asked the user for priority; **user chose #41** (the 90x
regression) over continuing Phase 13. A `/babysit` cron (15m) is active. crush
watchdog stood down (session finished). Next concrete step: start #41 — profile
under WSL (flamegraph/heaptrack installed in #38) + bisect `8638a65..7447994`.

## Active goal

none (`/goal` was cleared earlier). `/babysit` 15m cron active (TaskList-driven).
Standing `/babygoal` grant: "implement the plan, use only /crush agents" —
authorizes /crush delegation; research+bisect I do myself first.

## TaskList

### in_progress
- (none)

### pending — READY (no open blockers)
- #41 REGRESSION ~90x: SeferMalloc/16B churn = 1.9 ms vs Phase-11 ~21.5 us (Phase-12 registry path) — **USER-CHOSEN NEXT**; strategy: profile+bisect myself → actualize → /crush the fix
- #40 Latent §13: dealloc_small_by_segment derives class from page_map on drain paths (heap.rs:371, heap_core.rs:206)
- #35 M11 epoch-guard for M6 decommit + soak (REFRAMED — Variant-2 ring means freer never touches block; epoch likely unneeded; re-contemplate)

### pending — blocked (Phase 13 chain)
- #28 Phase 13.4 two-list free  (blockedBy #27 — now RESOLVED; #28 is effectively READY but deprioritized behind #41)
- #29 Phase 13.5 refill tuning  (blockedBy #27 — RESOLVED; effectively READY, deprioritized)
- #30 Phase 13.6 heap==core pinning  (blockedBy #28, #29)
- #31 Phase 13.7 macro-bench + refresh MALLOC_BENCH  (blockedBy #30)
- #32 Heavy gate: fuzz + aarch64 + TSan  (blockedBy #31)

### recently completed
- #27 Phase 13.3 inlined arithmetic own-thread free (`ea4dd89`) · #39 doc-hygiene (`7447994`) · #33 cross-thread reclaim fix · #36 ring-reclaim crash · #37 state-machine spec+loom · #38 WSL/TSan enabler · #26 13.1 O(1) lookup · #21–#25 Phase 12.1–12.5

## Decisions

- **User chose #41 (90x regression) as the next priority** over finishing Phase 13 (13.4–13.7) or #40 — micro-optimizing under a dominant ~90x overhead is pointless, and the "competitive with mimalloc" thesis is currently false. Rejected: continue Phase 13 chain; do #40 first.
- **Committed #27 at the phase boundary despite its speed benefit being unmeasurable** — the change is correct, non-vacuously tested, and non-regressive (baseline measured the same ~1.9 ms); its inlining benefit is just masked by #41's overhead. Per CLAUDE.md phase-boundary commits are sanctioned.
- **#27's `magic`-read removal on own-thread `dealloc` is sound** — `contains_base` is the load-bearing foreign guard; `magic` is only zeroed on the Large-free path (kind stays Large → idempotent re-entry), never on a registered Small segment. Kept magic only on the defensive cross-thread `dealloc_routing` path.
- **Class from `Layout`, never `page_map`, on the own-thread path** (§13 consistency) — page_map records only the first class per page; own-thread free has the Layout.
- **crush proved unreliable for delicate work again** (2 mid-run crashes); I verified + the bench-discovery happened only because I ran the bench myself, not trusting the envelope.

## Open questions

- **#41 root cause unknown** — hypotheses to test by profiling/bisect: (a) bench hits the fallback `Mutex` path on every alloc (TLS not binding in bench ctx?); (b) `stamp_segment_owner` CAS on every alloc; (c) `drain`/`find_segment_with_free` scanning per slow-path under the OPS=1024 alloc-then-dealloc pattern; (d) regression may even be on the single-thread `Heap` path — compare `benches/heap*.rs`.
- **Is `MALLOC_BENCH.md` now actively wrong?** Yes — its 21.5 us numbers are stale by ~90x; must be refreshed honestly after #41 (don't ship the old numbers as current).
- **#40**: is the intrusive-TFS drain path even live after 12.6 (all cross-thread frees go via the ring)? If dead → delete; if live → §13 corruption risk. Needs the same "who pushes to the intrusive TFS" investigation.

## Repo state
```
(clean — nothing uncommitted)
```
```
ea4dd89 feat(phase13.3): inlined, arithmetic-only own-thread free
7447994 docs: post-12.6 hygiene — remove stale discard-leak caveats, add CHANGELOG entry, checkpoint
255e18c feat(phase12.6): cross-thread-free reclaim — sound + reclaiming (no more discard-leak)
ea3a4ba test+docs: race_repro.rs (counterfactual gate) + confirmed root cause — intrusive TFS word at slot-reuse handoff (#33)
143b8be feat(phase13.1): O(1) size-class lookup — const SIZE2CLASS replaces the per-alloc linear scan
```
(NOTE: origin/main was last at Phase 11; the whole Phase 12–13 arc `8638a65..ea4dd89` is LOCAL, NOT pushed.)
```
