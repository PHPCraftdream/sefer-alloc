# Checkpoint — 2026-07-13 15:30

## Session summary

This session continued directly from an auto-compact of a prior session (whose own checkpoint, `2026-07-13-0030.md`, covers the round2+round3 audit-remediation batches, both closed and pushed to `origin/main` at `063ab08`). After `/resume 2026-07-13-0030.md` restored context, this session did four things, none of which touched git state (no commits, no pushes):

1. **Comparative benchmarks vs mimalloc/System** (`npm run bench:table`): confirmed no regression — churn path ~20-24ns/op matching the 0.2.1-era baseline; cold-direct path 30-42ns/op. The user flagged the cold-direct numbers as looking regressed vs a remembered "~20ns in 0.2.1." Investigation (git-tag diffing `docs/ALLOC_BENCH.md` at `sefer-alloc-v0.2.1`, cross-checking `docs/perf/IAI_BASELINE.md`) established: the "~20ns" memory is real but belongs to the **churn** bench (`bench_churn_alloc`, unregressed then-and-now), not cold-direct (`bench_direct_alloc`), which HAS grown from ~24-30ns (0.2.1) to ~31-42ns (now) — but a fresh `npm run iai` run proved this growth is NOT from this session's round2/round3 work (Ir/op flat vs the last pre-session pin from 2026-07-12) — it's an older, already-documented architectural trade-off from the fastbin/X-arc/PERF-PASS era, not a new regression.

2. **Round4 review synthesis** (`/fm`): read three fresh audit reports at `docs/agent_reviews_round4/{memory_safety_review,performance_review,code_quality_review}.md` (all dated 2026-07-13, read-only static analysis, no code/tests run by the reviewers) and delivered a structured summary distinguishing genuinely-new findings from round3 re-litigation. Key new finding: **R4-MS-4 (CRITICAL)** — public registry control-plane atomics (`HeapSlot::state`/`generation`, `Registry.free_slots`, `tagged_ptr::dbg_pack`, all `pub` via the doc-hidden `pub mod` pattern) let safe downstream code manually force a `LIVE→FREE→reclaim` cycle and hijack a live `HeapCore` between two threads — a real capability-boundary hole distinct from the already-settled M2 (`dealloc`/`realloc` stay safe) debate, which round4 re-raises (R4-MS-1/2) without new evidence and which this session did NOT reopen.

3. **Task creation** ("заведи таски, друг"): created tasks #93–#102, one per round4 finding/cluster, each with self-contained descriptions (file/line refs, mechanism, required fix, mandatory RED→GREEN regression test) sequenced via a `blockedBy` chain matching priority order: #93 (R4-1, control-plane) → #101 (R4-9, raw test-surface, same architecture) → #94 (R4-2, two point bugs: `refill_class` release-count bug + `magic_at(0)` foreign-base read-before-membership-check) → #95 (R4-3, memory envelope: teardown trim + config-conflict detection) → #96 (R4-4, doc fixes) → #97 (R4-5, abandon/adopt substrate fate + feature-matrix trim) → #98 (R4-6, AllocBitmap/MagazineBitmap dedup + invalid `Reservation::is_empty` test) → #100 (R4-8, tombstone-rebuild p99 spike + O(1) aligned lookup) → #99 (R4-7, RAD perf campaign: O(S) segment scan, remote-ring retry storm, MagazineBitmap per-hit RMW) → #102 (R4-10, monolith file splits, lowest priority, last).

4. **Implementation plan** (`/fm` "войди в созерцание..."): wrote `docs/reviews/2026-07-13-round4-remediation-plan.md` — a "contemplation" pass that collapses the 10 tasks into three underlying themes (A: "doc-hidden ≠ private" — one architectural fix for #93+#101, chosen as `pub(crate)` + `unsafe fn`+`# Safety` over a separate test-support crate; B: "finish the whole-slot-reuse model" — #95→#96→#97, recommending outright DELETION of the dead abandon/adopt substrate rather than fixing it, citing the `LargeCacheMode` precedent; C: RAD perf campaign — #100→#99, with a specific safe-ordering recommendation R2→R3→R1 by ascending risk) plus concrete per-task schemas/algorithms and three explicit escalation points requiring user sign-off before implementation (abandon/adopt removal; whether to enable cheap hardened-guards in production if iai shows ≤~2 Ir/op; test-surface narrowing deadlocks).

No `/babygoal`, no `/crush` agents, no `/babysit` cron were invoked this session — all work (bench runs, git-tag archaeology, review synthesis, task creation, plan writing) was done directly by the orchestrator. No commits were made; `docs/agent_reviews_round4/`, this checkpoint, and the new plan file are all untracked. The user has not yet said whether/how to begin executing tasks #93+.

## Active goal

None — no `/goal` Stop hook is armed this session.

## TaskList

### pending
- #93 R4-1: закрыть публичный control plane registry (R4-MS-4, CRITICAL) — not blocked, ready to start
- #101 R4-9: изолировать/пометить unsafe публичную raw test-surface  (blockedBy: #93)
- #94 R4-2: точечные баги — refill_class release-count + magic_at(0) foreign-base барьер  (blockedBy: #101)
- #95 R4-3: memory envelope — trim неактивных heap-слотов + честная модель config (N1+N2)  (blockedBy: #94)
- #96 R4-4: док-фиксы — устаревшие описания архитектуры, противоречащие коду  (blockedBy: #95)
- #97 R4-5: решить судьбу abandon/adopt substrate + сократить feature-матрицу  (blockedBy: #96)
- #98 R4-6: механическая уборка — дедуп bitmap-кода + невалидный Reservation-тест  (blockedBy: #97)
- #100 R4-8: tombstone-rebuild tail-latency spike (N3) + O(1) aligned lookup (R8)  (blockedBy: #98)
- #99 R4-7 (RAD-кампания): перф-ядро — O(S)-скан, remote-ring retry storm, MagazineBitmap RMW-per-hit  (blockedBy: #100)
- #102 R4-10: разбить монолитные файлы по подсистемам (quality #9, низкий приоритет)  (blockedBy: #99)

### recently completed
(none this session — all round2/round3 tasks from the prior session were already completed/cleared before this session started; see `2026-07-13-0030.md` for that history)

## Decisions

- **Cold-direct wall-clock growth since 0.2.1 (~24-30ns→~31-42ns) is a pre-existing architectural trade-off, not a regression from this session's work** — confirmed via `npm run iai` (Ir/op flat vs the 2026-07-12 pre-session pin in `IAI_BASELINE.md`).
- **Round4's abandon/adopt substrate: recommend deletion (option A), not repair** — internally inconsistent (`try_adopt` ignores `register_segment_internal` failure, `reset_stamp_cache` not called, `next_abandoned` shared with deferred-large), production path (whole-slot reuse) never reaches it, and the `LargeCacheMode::{Background,Both}` removal precedent from round3 established "delete unreachable/inconsistent code, git preserves history" as this project's pattern. **Not yet executed — flagged as an escalation point pending user confirmation in task #97.**
- **Theme A (control-plane + raw test-surface) fix: `pub(crate)` + `unsafe fn`/`# Safety`, not a separate test-support crate** — cheaper (no need to restructure 149 test files' access patterns) for the same soundness guarantee.
- **N2 (config-stickiness on recycled slots): detect-and-signal, not full reconfigure-with-trim** — a full reconfigure needs old-owner quiescence that doesn't cleanly exist; the realistic single-global-allocator case doesn't need it, so a `config_conflicts` counter/debug-assert is proposed instead.
- **M2 (`dealloc`/`realloc` stay safe `pub fn`) is NOT being re-litigated a fourth time** — round4's R4-MS-1/2 restate round3's already-settled argument without new exploit evidence; only the non-relitigation kernels inside them (the `magic_at(0)` foreign-base read-before-membership-check, and "measure whether cheap hardened-guards are affordable in production") were carried into tasks #94 and #99.

## Open questions

- **Execution kickoff:** the user has not yet said how to begin task #93 onward — same `/babygoal`-style sequential `/crush` protocol as round2/round3, or something else. Last orchestrator message ended by asking this but got no reply before `/checkpoint` was invoked.
- **Escalation point 1 (task #97):** confirm abandon/adopt substrate deletion before it's implemented.
- **Escalation point 2 (task #99, RAD-R3):** if cheap `hardened`-feature guards (interior-pointer / Large-as-Small checks) measure at ≤~2 Ir/op in the iai campaign, should they move into `production` by default? Not yet measured, not yet decided.
- **Escalation point 3 (tasks #93/#101):** if some integration test turns out to structurally require a `pub` (not `pub(crate)`) field with no clean `unsafe fn`/accessor substitute, escalate rather than silently widening the surface back out.

## Repo state

```
?? docs/agent_reviews_round4/
?? docs/checkpoints/2026-07-13-0030.md
?? docs/reviews/2026-07-13-round4-remediation-plan.md
```

```
063ab08 docs(alloc-core): R3-F review follow-up — fix stale large_cache_mode field doc
3947b9b docs: R3-E — anchor the unsafe-seam inventory grep, drop the wrong hardcoded count
eb0dbd3 fix(alloc-core)!: R3-C — remove Default for AllocCore
eccf2a2 perf(registry): R3-A — stats() honest cost contract, gate walk on alloc-stats
121a445 feat(alloc-core)!: R3-B — remove LargeCacheMode::{Background,Both}
```

Branch `main` is up to date with `origin/main` (working tree otherwise clean — the three untracked files above are the only difference; nothing staged, nothing modified-tracked). No commits made this session.
