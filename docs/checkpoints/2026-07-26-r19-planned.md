# Checkpoint — 2026-07-26 [r19-planned]

## Session summary
Same long-running session that completed Rounds 13-18. Round 18 (tasks
#329-#336) finished and was pushed... actually NOT pushed yet — Round 17 was
pushed on explicit request, but Round 18's 9 commits (`dc95d1a..3b8fdc0`)
were only committed locally, never pushed (no push request was made for
Round 18). After Round 18 closed, the user asked for a fresh `/crush`
review of the new wave; mid-dispatch the user also asked for a parallel
`@oh` review of the same scope, so both were launched concurrently
(explicitly instructed to form independent conclusions first, cross-check
each other and prior reviews only afterward). Separately, and BEFORE either
of those two dispatched reviews finished, a third review file
(`docs/reviews/2026-07-26-r18-readonly-review.md`) appeared in the working
tree with no dispatch visible in this session's tool history — the THIRD
occurrence of this exact unexplained-provenance anomaly this session (after
commit `5709c24` in Round 17 and `docs/reviews/2026-07-25-r17-readonly-
review.md`). Its content was read immediately because the user pasted its
summary directly and asked to study it.

That mystery review's most important claim: R18-3's `hardened`-bypass
(landed this same Round 18, in commit `912740f`) does not actually
implement the defensive "no-op" contract it claims for a specific
`hardened + medium-classes(-with-promotion-reachable)` combination — a
fabricated small `Layout` on ANY Large pointer gets routed through branch
(A) to `self.core.dealloc(ptr, layout)`, which (personally verified by
reading `src/alloc_core/alloc_core.rs`'s Large-dealloc branch, ~line 1268+)
frees/caches the segment purely by `kind`, NEVER checking the caller's
`layout` against the segment's actually-stored size — so the "detected
no-op" `hardened` promises (task #25) is NOT delivered in this combination;
the segment is actually freed. The existing regression test
(`tests/regression_hardened_large_kind_own_free.rs`) cannot catch this
because its oracle only checks for small-magazine aliasing (structurally
impossible regardless, since Large segments are never reused for Small
requests) and an immediate post-free memory read (which typically still
shows stale pre-free data — a classic non-conclusive UAF-read pattern).

**Critically, both of the two independently-dispatched reviews I launched
(`@oh` and `/crush`) explicitly said this exact code was "sound and
correctly verified"** — both checked only whether the `kind_at` check
*fires* correctly under `hardened` (yes), not what the *action taken* when
it fires (`self.core.dealloc`) actually *does* to the segment. I personally
re-derived the whole mechanism from source before accepting the mystery
review's claim (did not take it on faith), and confirmed it is real. This
is now documented as the central finding driving Round 19's #1 priority
task.

Given the severity, I asked the user via `AskUserQuestion` whether to open
Round 19 and fix now, defer, or just note it — user chose "open Round 19,
fix now." I then read the mystery review, the `@oh` review
(`docs/reviews/2026-07-26-oh-review-r18.md`), and the `/crush` review
(`docs/reviews/2026-07-26-crush-review-r18.md`) in full, cross-verified
several additional findings personally (the `OPEN_ITEMS.md` stale NUMA
item, the `R18_7_MIMALLOC_GAP_STATUS.md` §7 residual stale text that both
`@oh` and `/crush` independently caught, the wrong line-range citation in
`912740f`'s commit message), wrote a synthesis plan
(`docs/reviews/2026-07-26-r19-plan.md`), and filed 9 Round 19 tasks
(#337-#345, P0 through P3). **The plan doc write succeeded, but the user
then explicitly rejected the `git add` tool call for it** (a `git status`
right after shows it staged anyway — unclear why, possibly a race between
the rejection and a partially-completed prior call; this is flagged
honestly as an open question below, not silently resolved). The user then
immediately invoked `/checkpoint`, interrupting before any further action
(no task has been marked `in_progress`, none dispatched to `/crush` yet).

## Active goal
No formal `/goal` Stop-hook condition is known to be currently armed in
this segment (none was (re-)issued since the Round 18 wrap-up). The
standing behavioral pattern this session has followed throughout — dispatch
task work via `/crush`, commit between tasks, personally zero-trust-verify
every diff — remains the operative default per repeated explicit
instructions earlier in the session, but is not enforced by an active Stop
hook right now as far as this checkpoint can confirm.

## TaskList
### pending
- #337 R19-1 (P0): fix hardened Large-layout no-op contract violation under
  promotion-reachable combos — **the critical finding, not yet started**
- #338 R19-2 (P1): fix OPEN_ITEMS.md's stale NUMA item #12
- #339 R19-3 (P1): complete R18-7's incomplete self-correction (§7 residual)
- #340 R19-4 (P2): fix wrong line-range citation in 912740f's commit message
- #341 R19-5 (P2): write Round 18 CHANGELOG entry
- #342 R19-6 (P2): methodological fixes to R14_4 gate report
- #343 R19-7 (P2): make watchdog thread panic fail the test in race_repro.rs
- #344 R19-8 (P2): canonicalize the promotion cfg predicate
- #345 R19-9 (P3): track dirty_by_class.rs's stale derived-literal doc-debt

No task has been marked `in_progress` yet — the interrupt landed before any
dispatch began.

## Decisions
- Chose to personally re-derive the hardened/no-op finding from source
  (reading `alloc_core.rs`'s actual Large-dealloc code) rather than accept
  the mystery review's claim on its word — confirmed it independently
  before treating it as real, consistent with this session's standing
  zero-trust discipline.
- Explicitly surfaced to the user, via `AskUserQuestion`, that this exact
  code had already been reviewed as "sound" by two agents I dispatched
  myself (`@oh`, `/crush`) — chose transparency about the disagreement
  between reviews rather than quietly picking a side without disclosing
  the conflict.
- Did not silently fold the mystery-review's severity into a lower-priority
  bucket alongside the other (real but smaller) findings — gave it its own
  P0 task (#337) with the most detailed description of the nine, including
  an explicit "construct a real red/green counterfactual" requirement,
  because a `hardened`-feature defense silently not doing what it claims is
  qualitatively more serious than a doc/process finding.
- Filed all 9 Round 19 tasks in one pass (mirroring the Round 18 planning
  pattern) rather than only filing #337 and deferring the rest, since the
  other 8 findings were independently corroborated by at least one of the
  three reviews (several by two) and are cheap, well-scoped fixes.

## Open questions
- **Unresolved, newly surfaced this segment:** why did `git status` show
  `docs/reviews/2026-07-26-r19-plan.md` as staged (`A`) immediately after
  the user rejected the `git add` tool call for that exact file? Not
  investigated further before the `/checkpoint` interrupt. Possible
  explanations: a race between the rejection and a partially-completed
  prior invocation, or a misread of the rejection's scope. Needs a plain
  `git status`/`git diff --cached` check at the start of the next turn
  before doing anything else with git — do not assume either that the file
  is safely staged or that it needs re-adding without checking first.
- **Still unexplained (third occurrence now):** the provenance mechanism
  behind `docs/reviews/2026-07-26-r18-readonly-review.md`'s appearance
  (and the two earlier instances: commit `5709c24` in Round 17,
  `docs/reviews/2026-07-25-r17-readonly-review.md`). This has now happened
  three times across two rounds. Genuinely worth asking the user directly
  next time it recurs, rather than continuing to just flag it — the
  pattern is frequent enough now that "unexplained" is starting to look
  like it might have an explanation the user has simply not yet been asked
  for directly.
- Whether the user wants Round 19 executed via `/crush` (the pattern used
  throughout Round 18) or a different tool — not re-confirmed since Round
  18 closed; task descriptions all default to "/crush" per the
  last-known-active instruction, but this should be treated as inherited
  default, not a freshly re-confirmed choice.

## Repo state
```
A  docs/reviews/2026-07-26-r19-plan.md
?? docs/reviews/2026-07-26-crush-review-r18.md
?? docs/reviews/2026-07-26-oh-review-r18.md
?? docs/reviews/2026-07-26-r18-readonly-review.md
```

```
3b8fdc0 docs(process): add docs/perf/OPEN_ITEMS.md tracking convention (R18-8, task #336)
60633e3 docs(perf): unified adaptive Large policy design doc (R18-9, task #335)
ed75b06 docs: design note for stale derived-literal comment guard (R18-6, task #334)
1d2c9cd docs: fix contradictory pad-target wording + sharpen class-aware-dirty framing (R18-4/R18-5, task #333)
4ba35dc docs(perf): correct R18-7's CI-gate claim -- perf-gate.yml already exists (task #332 follow-up)
```

Local `main` is 9 commits ahead of `origin/main` (Round 18's full range,
`dc95d1a..3b8fdc0`) — NOT pushed (no push request made for Round 18). Four
files are new/staged in the working tree (the three Round-18 reviews plus
the Round-19 plan doc) — none committed yet; the `git add` for the plan doc
was explicitly rejected by the user immediately before this checkpoint, so
these should NOT be committed without re-confirming intent first.
