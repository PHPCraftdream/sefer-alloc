# Checkpoint — 2026-07-20 [Round 9 complete, pushed, CI green]

## Session summary

`sefer-alloc` (D:\dev\rust\sefer-alloc), 100%-Rust allocator. This window
picked up mid-session after a `/resume` + `/compact` cycle: two carry-over
items were finished first — the tombstone-rebuild flaky-test hardening
(commit `68f5da7`) and the R9-1 P0 miri fresh-zero fix (commit `860d897`,
task #221's own follow-up, found by an external review). For R9-1's miri
counterfactual, the miri run initially took 2+ hours (root cause diagnosed:
`interleaved_alloc_zeroed_always_zero` did 80 iterations of full-buffer
touch on a 2 MiB buffer, byte-by-byte under miri's interpreter) — shrunk the
miri-only workload (`LARGE` 2 MiB→1 MiB+4KiB, `ITERS` 80→4 under `cfg(miri)`
only) without weakening native-path coverage, cutting the run to ~17 min.
Pushed both commits (`38f4108..860d897`), confirmed CI genuinely green (not
just exit code — read full step-by-step output).

Then the user authorized a `/babygoal`-style sequential `/crush` pipeline for
the 9 filed Round-9 follow-up tasks (#224-#232, `blockedBy`-chained in
priority order), with an explicit new constraint mid-run: **no push between
tasks, minimal verification only, one full check + push at the very end**
(this was honored — see Repo state below, only pushed once at the finish).
A `/babysit` cron (15 min) was armed for the run and has now **self-deleted**
(TaskList went empty) — this checkpoint additionally confirmed it's gone via
`CronList`/`CronDelete`, both done before writing this file.

All 9 tasks are done, personally zero-trust-reviewed (full diff read, tests
independently re-run, counterfactuals verified for every code-changing task —
not just trusted the sub-agent's claim), and pushed. Two commits landed for
work each `/crush` session produced across R9-3 through R9-9 (each on top of
the last): R9-1 `860d897`, R9-2 `3021b16` (fresh cross-version bench, GO on
Round 8's shape unchanged), R9-3 `c8f5f32` (medium-classes production-gate,
GO), R9-4 `f469343` (medium-classes-wide prototype 1.25/1.5/1.75 MiB — real
density measured at 2x/1x/1x, not the review's guessed 3x/2x/2x, an honest
downgrade the sub-agent found itself), R9-5 `7bdbc0f` (virgin zero-skip for
Small, **design-only** — found the production entry point doesn't reach the
tested layer, so a substrate prototype would be dead code; declined to
rush), R9-6 `fd28ff8` (class-aware dirty routing waste measured, up to 95%
wasted drains at N=8 producer classes, CONDITIONAL-GO), R9-7 `021c098` (low-
RSS pool policy, **design-only** — found the review's own suggested shape
"keep free-list metadata, decommit payload" is UNSOUND: the free-list `next`
link lives in the block body, decommit destroys it; corrected to a
carve-target-reuse shape, still declined to prototype given the
correctness-review budget), R9-8 `5a4ba62` (per-class directory miss-streak
256→64 + OOM-rescue scan, real code change, verified via counterfactual —
temporarily reverted to a shared-counter simulation and confirmed the new
test goes red exactly as its own comment predicts), and R9-9 `5e467ec`
(batch-API realistic-size follow-up — **downgraded** R8-7's GO/GO/NO-GO
verdict to CONDITIONAL-NO-GO once measured at realistic batch=8-64 against
the REAL SeferAlloc scalar path, which turned out to be 2-30x faster than
the AllocCore batch primitive at small N).

**One important self-caught regression**: while verifying R9-9's own "clean
under the full feature matrix" claim, ran the complete suite under
`--all-features` and found R9-4 (`medium-classes-wide`) had silently broken
3 pre-existing tests that hardcoded plain-`medium-classes` topology
assumptions (55 classes / 1 MiB) without accounting for `medium-classes-wide`
being enabled simultaneously (exactly what `--all-features` does). This was
MY OWN oversight during R9-4's review — I'd tested each feature combo in
isolation but never ran the full suite with both features on at once. Fixed
directly (not delegated) in a separate commit `78fd98d` before R9-9's own
commit, with fresh `--all-features` full-suite verification (180 test
binaries green) before either landed.

Also mid-review (R9-8), I made a real operational mistake: while running a
manual counterfactual (temporarily patching `alloc_core_small.rs` to
simulate the old shared miss-streak counter), I ran `git checkout --` on the
file to "revert my patch" and it discarded the ENTIRE R9-8 diff for that
file (not committed yet, so unrecoverable via git). Recovered by
reconstructing the diff as a patch file from the `git diff` output already
visible earlier in the conversation and `git apply`-ing it — verified
byte-identical (`git diff --stat` matched, all tests re-passed). Lesson
applied going forward: for in-place counterfactual patches on uncommitted
work, prefer `sed`/`Edit`-based targeted reverts over `git checkout --`,
which nukes the whole file.

Final gate: full `npm run check` (fmt + clippy x3 + test x4 including
`--all-features` + iai) ran clean, then pushed 9 commits
(`860d897..5e467ec`) to `origin/main` in one push. Watched both the `CI` and
`Kani verification` GitHub Actions runs to completion via `gh run watch
--exit-status` (a genuine blocking wait, not polling) — both `success`, 0
non-passing jobs. Wrote the user a Round-9 summary table in the previous
turn. Cron cleaned up. This checkpoint is being written at the user's
explicit `/checkpoint` request with no further work queued.

## Active goal

None. No `/goal` Stop hook is armed. The `/babysit` cron (`b9262829`,
15-minute interval) that supervised the R9 task queue has been explicitly
`CronDelete`d in this turn (TaskList is empty, so it would have self-deleted
on its next tick anyway — deleted proactively instead of waiting).

## TaskList

Empty. `TaskList` returns "No tasks found" — all 9 Round-9 tasks (#224-#232)
plus the 2 carry-over items were completed and are no longer tracked (this
tool does not retain a "recently completed" list once the queue drains, so
no completed-tasks section is populated below; see git log instead for the
authoritative record of what shipped).

## Decisions

- Split "harden the flaky tombstone-rebuild test" and "fix R9-1's miri bug"
  into two separate commits (different concerns) rather than one — done
  before this window's visible start, honored on resume.
- R9-4/R9-5/R9-7: when a sub-agent's own investigation surfaces that the
  external review's premise or suggested shape is wrong (R9-4's 3x/2x/2x
  guess, R9-7's "keep free-list metadata" idea), report the corrected
  finding honestly rather than forcing the original suggestion to "work" —
  applied consistently by design (the task prompts explicitly permitted and
  asked for this).
- R9-5/R9-7: both chose **design-only** over a rushed prototype when the
  correctness-review surface (decommit/recommit correctness, virgin-memory
  invariants) exceeded a single session's "minimal and safe" bar — accepted
  as a fully successful outcome for a "P2-design" task, not a shortfall.
- Chose to fix the R9-4/`--all-features` regression myself directly (Edit
  tool, 3 targeted `cfg!` branches) rather than delegating a 10th `/crush`
  session for a 3-test one-line-pattern fix — proportionate to the fix's
  size.
- Recovered the accidentally-`git checkout`-clobbered R9-8 diff by
  reconstructing a patch from the `git diff` text already in the
  conversation transcript and `git apply`-ing it, rather than re-delegating
  R9-8 from scratch — verified byte-identical before trusting it.

## Open questions

- None outstanding from this window. The 9 Round-9 report docs
  (`docs/perf/R9_*.md`) each end with their own "next step" / "staged
  implementation" recommendations (e.g. R9-5's 4-stage plan, R9-7's 4-stage
  plan, R9-6's "run a wall-clock bench to upgrade CONDITIONAL-GO to GO") —
  these are natural candidates for a future Round 10 if the user wants to
  continue, but nothing was left ambiguous or blocking within Round 9 itself.
- The untracked `_raw_*.log` scratch files (see Repo state) are, per this
  session's established convention, intentionally NOT committed (raw
  benchmark/measurement output, reproducible via the commands documented in
  each report) — flagging only so a future session doesn't mistake them for
  forgotten work.

## Repo state

```
?? _gad_suite.log
?? ci_watch.log
?? ci_watch2.log
?? ci_watch_ffd3215.log
?? docs/perf/_raw_baseline_off.log
?? docs/perf/_raw_baseline_off_reduced.log
?? docs/perf/_raw_criterion_medium.log
?? docs/perf/_raw_criterion_production.log
?? docs/perf/_raw_firstalloc_medium.log
?? docs/perf/_raw_firstalloc_production.log
?? docs/perf/_raw_iai_medium.log
?? docs/perf/_raw_iai_production.log
?? docs/perf/_raw_medium_on.log
?? docs/perf/_raw_medium_on_reduced.log
?? docs/perf/_raw_r9_9_followup.log
?? docs/perf/_raw_r9_9_followup_run2.log
?? docs/perf/_raw_r9_9_followup_run3.log
```

Local `main` == `origin/main` (`5e467ec`, fully pushed and synced). No
outstanding modified/staged files — every substantive change this window
produced is already committed.

```
5e467ec perf(bench): batch-alloc ceiling follow-up - small batches + real SeferAlloc arm (R9-9)
78fd98d fix(tests): account for medium-classes-wide under --all-features (R9-4 follow-up)
5a4ba62 fix(directory): per-class miss-streak + OOM-rescue scan for drift recovery (R9-8)
021c098 docs(design): low-RSS pool policy for lazy-commit tradeoff - design-only (R9-7)
fd28ff8 perf(diag): measure class-aware dirty routing waste ratio - CONDITIONAL-GO (R9-6)
```

CI status (verified via `gh run watch --exit-status` + `gh run view
--json conclusion`, not just narration): both the `CI` workflow and the
`Kani verification` workflow for the final push (`5e467ec`) completed with
`conclusion: success`, 0 non-passing jobs.
