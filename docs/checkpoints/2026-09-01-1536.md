# Checkpoint — 2026-09-01 15:36 [Sol-codex run-4 remediation, W2-Core just merged, npm check in flight]

## Session summary

This session picked up the `tagged-index-stack` publish-readiness campaign after an external static review, "Sol-codex run 4" (`docs/reviews/2026-09-01-124216-tagged-index-stack-review-Sol-codex-run-4.md`, audited HEAD `90a01ed`), delivered a **NO-GO** verdict — its headline finding (P1-1) was that six rounds of this crate's own internal `@oh` review loop (rounds 10-15) had each found a new safe-code double-issue hazard shape and closed it with documentation, when the underlying defect was never a documentation gap: `StackStorage`'s methods are `pub` and safe, so `StackOps`'s trust in a coherent head↔links binding was never type-enforced. The user explicitly asked me to implement the review's fixes via `/wrush` (a stricter `/rush` variant: worktrees MUST live in-repo under `<repo-root>/worktrees/`, not as sibling directories, and merges are done by hand — `cp` the reviewed files into the primary checkout, never `git merge` — then the worktree and branch are removed). I decomposed the review into three waves (W1 doc/harness fixes, W2 the structural double-issue closure, W3 perf-measurement + doc-diet, both gated behind W2) plus a mandatory post-closure re-audit (`#1813`) before the crate's original publish task (`#661`) can proceed — `#661` is now explicitly blocked on `#1813`.

The central engineering decision this session made was task `#1807` (`W2-Design`): I discovered mid-investigation that `sefer-alloc`'s own `Registry` (`src/registry/heap_registry.rs`) is a REAL, load-bearing external implementor of `StackStorage` (not a hypothetical), which ruled out simply removing the public trait. I wrote an ADR (`docs/adr/2026-09-01-tagged-index-stack-storage-binding-closure.md`) splitting the six demonstrating hazard tests into "Group A" (one test, `ArrayIndexStack`-specific head leakage — closeable structurally with zero cost) and "Group B" (five tests, third-party-implementor value-level obligations — closeable only via `unsafe trait` or removing the external seam entirely). I got explicit user sign-off on Group A's closure, then — per the user's explicit instruction — consulted an independent second opinion from `@fh` (Fable 5) on Group B specifically, framed with "we strive for perfection and are willing to break backward compatibility for it." `@fh` recommended whole-trait `unsafe trait StackStorage` with the three methods staying safe `fn` (a deliberate deviation from the literal `GlobalAlloc` shape, which also marks methods unsafe) — reasoning: the trait is morally in `GlobalAlloc`/`Allocator` territory since unsafe allocator code depends on its exclusive-issuance contract, and whole-trait-unsafe-with-safe-methods puts the acknowledgment at the implementor site (where every Group B hazard actually originates) with zero new unsafe blocks anywhere. The user approved this too. I recorded the full decision + `@fh`'s complete reasoning in the ADR (not just a citation) since it's a consequential, hard-to-reverse architectural call.

**Work completed and pushed (wave 1, landing SHA `0a37f33`, CI+Kani confirmed green per-job):** `W1-DocSync` (#1804, README synced to canonical rule-3/guard/loom claims — found and fixed a FOURTH stale-phrase site round-15 missed by grepping only two old phrases, a direct lesson-learned application), `W1-Harness` (#1806, bench published-window rendezvous fix + example input validation, with a genuine counterfactual proof via an injected-then-reverted artificial stall), `W1-MSRV` (#1805, measured real library-floor MSRV 1.79, replacing an untested 1.81 claim, with real `+1.79`/`+1.78` toolchain boundary evidence). **A real CI-only flake surfaced on wave 1's landing SHA** (`threaded_conservation`'s activation oracle failed with 0 CAS retries across 1.6M iterations — the test spawned 8 threads via `thread::scope` with NO start-synchronization, so on a CI runner with few cores the threads could run almost sequentially with no real contention) — caught via the standing per-job CI-verification discipline (aggregate `gh run watch` reported success; per-job `gh api .../jobs` showed 1 failure), root-caused, and fixed on the spot with a `std::sync::Barrier` rendezvous (same fix shape as the W1-Harness bench fix), verified 5x locally, pushed as its own commit `7c7ffe5`, and re-confirmed green per-job on the new landing SHA.

**Work completed, merged to local main, verification in flight (wave 2, NOT yet pushed):** `W2-Core` (#1808) — the largest diff of the campaign, landed in two commits after the sub-agent's `rush run` process hit its 60-minute timeout mid-turn TWICE (both times recovered cleanly by resuming the same `--session` id with a larger timeout against real uncommitted progress already sitting in the worktree, per the rush skill's documented recovery path for a transient timeout). Local commits `a205d75` (Group A: `ArrayIndexStack` sealed off the public trait via a new `pub(crate) SealedStorage` accessor + `push_index_impl`/`pop_index_impl` free functions the algorithm moved into verbatim; the old runtime demo `array_index_stack_head_still_double_issue` replaced by a real compile-fail fixture, E0277+E0599, counterfactual-verified) and `0486e90` (Group B: `#![forbid(unsafe_code)]` → `#![deny(unsafe_code)]`, `StackStorage` → `pub unsafe trait` with a normative 5-clause `# Safety` section, `Registry`'s impl → `unsafe impl` with a clause-by-clause `// SAFETY:` comment I independently verified against the actual current code, all five Group-B hazard test implementors → `unsafe impl` naming which clause each deliberately violates). I did an unusually thorough zero-trust pass on this one given its size and risk: read both full diffs line-by-line, confirmed the CAS-retry algorithm move was byte-for-byte verbatim, personally wrote TWO of my own independent double-issue repro attempts (a generic-fn-bounded-on-StackStorage extraction and a direct `.head()` call) and confirmed both fail to compile exactly as claimed, verified every SAFETY-comment factual claim against the real source (`static REGISTRY`, `MAX_HEAPS=4096`, `push_free_slot`'s `debug_assert`), ran a REAL packaged-tarball extract-and-test round-trip (not just `cargo package --list`) and confirmed the compile-fail driver takes its packaged-skip path correctly, ran the 16 self-verifying doc-consistency tests (`no_stale_doc_references.rs`, including `readme_unsafe_inventory_counts_match_reality`), a full `cargo build --workspace`, the root suite under both default and `production internals` features, and a `no_std x86_64-unknown-none` build. I also caught and personally fixed (in the same merge pass) two stale `#![forbid(unsafe_code)]` claims the sub-agent had explicitly flagged as out-of-scope in its own final summary (README.md's front-page headline and its 128-bit-CAS-rejection paragraph) rather than leave a known-false public claim on `main` even briefly. **Currently running in the background**: full `npm run check` on the merged-but-unpushed W2-Core state (task `b7v9s4daj`) — once green, this needs `git push`, then `git fetch && git rev-parse origin/main` for the real landing SHA, then CI+Kani watched and confirmed green PER-JOB (not just the aggregate watch result — this exact gap bit wave 1 this session).

**Independent, parallel workstream (user-approved mid-session):** task `#1091` (a pre-existing, unrelated perf-regression investigation — item 56 in `docs/perf/OPEN_ITEMS.md`, a ~10-13% Ir regression across 153 commits in a historical range) was launched via `/wrush` in its own worktree at the user's explicit request ("можем что-то ещё параллельно делать?"), also hit one 60-minute-timeout-and-resume cycle, and has now reported a completed investigation: two contributing commits (`5df56d3`, a one-time +759 Ir per `HeapCore` creation from an R32-5 `#[repr(C)]` layout change; `5289c66`, +35-47 Ir fixed plus ~3 Ir/call from an R32-10 cache-size/counter change) found via dual bisect with counterfactual reversion, verdict "(b) accepted cost + (c) measurement artifact — no production fix owed," with a gate report, summary CSV, and derive script already committed to its own branch (`perf-regress-item56`, commits `79ccf4f`/`c46527a`/`f2dc82f`, NOT yet reviewed by me or merged). **This has NOT been zero-trust reviewed yet** — it carries an explicit tool warning ("reduction-loss: final_text is 2463 chars, 29% of 8591 combined sub-agent chars... the parent likely summarised away detail") that I have not yet followed up on, and is the next thing to check once wave 2's push is confirmed green.

## Active goal

No `/goal` Stop hook armed this session. Progress is driven entirely by the TaskList + a `/babysit` cron (job id `04ca34e5`, every 15 minutes, re-armed earlier this session after finding the prior session's cron had expired/been cleared). The standing directive behind the whole campaign (from an much earlier session, still in force): loop `@oh`-style review → fix → merge → push → verify CI+Kani per-job, repeat until a review reports zero P2 findings — this Sol-codex run-4 remediation is a continuation of that same standing loop, now specifically executing via `/wrush` per this session's explicit instruction.

## TaskList

### in_progress
- #1091 [perf-regress] item 56 — investigation complete per sub-agent report, NOT yet zero-trust reviewed by me; committed on branch `perf-regress-item56`, not merged
- #1808 tis-sol4-W2-Core — merged to local main (`a205d75`, `0486e90`), zero-trust reviewed and verified by me; `npm run check` running in background (task `b7v9s4daj`), not yet pushed
- #1812 tis-sol4-T-push-W1 — wave 1 pushed and CI+Kani-confirmed green per-job on landing SHA `7c7ffe5` (after the flake fix); task itself not yet marked completed
- #1814 tis-sol4-T-push-W2 — in progress: `npm run check` running, then push + CI/Kani verification remain

### pending
- #661 tagged-index-stack — first publish to crates.io (blocked by #1813)
- #1809 tis-sol4-W2-Oracles (blocked by #1808 — ready to start once #1808/#1814 fully close)
- #1810 tis-sol4-W3-Perf (blocked by #1808)
- #1811 tis-sol4-W3-DocDiet (blocked by #1808, #1809)
- #1813 tis-sol4-ReAudit — full external re-audit, blocked by #1808, #1809, #1804 (#1804 already done)
- #1815 tis-sol4-T-push-W3 (blocked by #1810, #1811)

### recently completed
- #1804 tis-sol4-W1-DocSync
- #1805 tis-sol4-W1-MSRV
- #1806 tis-sol4-W1-Harness
- #1807 tis-sol4-W2-Design (ADR decided, owner-approved, `@fh` second opinion recorded)
- #1799-1803 round-15/16 of the OLDER internal `@oh` review loop (superseded by Sol-codex run-4's deeper findings; #1803 closed as superseded, not completed)

## Decisions

- **Group A closure mechanism**: `pub(crate)`-sealed `SealedStorage` accessor + free-standing `push_index_impl`/`pop_index_impl` functions, rather than an access-token type — zero new public API, zero `unsafe`, keeps the CAS-retry algorithm written exactly once. Owner-approved.
- **Group B closure mechanism**: whole-trait `unsafe trait StackStorage`, methods stay safe `fn` (NOT the literal `GlobalAlloc` shape of unsafe-trait-plus-unsafe-methods) — `@fh`'s recommendation, adopted verbatim after independent second-opinion consultation explicitly requested by the user. Requires `forbid(unsafe_code)` → `deny(unsafe_code)` crate-wide (the only way to admit any `unsafe` at all, since `forbid` cannot be locally relaxed — re-confirmed against round-13's own historical compiler-error evidence).
- **Merge mechanism for this whole campaign**: per the user's mid-session `/wrush` correction, worktrees live at `<repo-root>/worktrees/<slug>` (added to `.gitignore` in commit `110cc7a`), and merges are done by hand (`cp` reviewed files into the primary checkout, commit, then `git worktree remove` + `git branch -D`) — NOT `git merge --no-ff`, which was this campaign's convention in all EARLIER rounds (10-15) before this session.
- **P4-2 (public blanket `StackOps`) acknowledged, not acted on** — `Registry` genuinely needs it; the ADR's own recommendation, not revisited.
- **Any stale doc claim discovered during a merge's zero-trust review gets fixed in the same pass**, even if the originating sub-agent explicitly flagged it as "out of scope, deferred to a later task" — applied twice this session (round-15's 4th stale README phrase; W2-Core's two stale `forbid` claims in README.md).

## Open questions

- Is `#1091`'s (perf-regress item 56) investigation actually sound, or did the "reduction-loss" summarization warning hide something material? Needs a proper zero-trust pass — read the actual committed report/CSV/raw logs on branch `perf-regress-item56`, not just the compressed final_text, before merging or accepting its "no fix owed" verdict.
- Wave 2's `npm run check` (task `b7v9s4daj`) result is not yet known as of this checkpoint — if it's clean, push immediately and confirm CI+Kani per-job before touching anything else.
- No user-facing open questions currently blocking progress — all architectural decisions for W2 are made and owner-approved; remaining work is mechanical (verify, push, then W2-Oracles/#1809, W3, and the mandatory re-audit #1813).

## Repo state

```
(clean — working tree has no uncommitted changes as of this checkpoint)
```
```
0486e90 fix(tis): StackStorage becomes an unsafe trait; crate moves forbid -> deny(unsafe_code) (Sol-codex run-4 P1-1 Group B + P4-2 acknowledgment)
a205d75 fix(tis): seal ArrayIndexStack off the public StackStorage trait (Sol-codex run-4 P1-1 Group A)
7c7ffe5 fix(tis): add start-rendezvous barrier to threaded_conservation test (CI flake)
0a37f33 fix(tis): declared MSRV corrected to measured library floor 1.79 (Sol-codex run 4, P2-3)
17cbde5 bench(tis): Sol-codex run 4 P3-3/P3-4 — published-window contention timing + probe input validation
```
(local `main` is 2 commits ahead of the last pushed/CI-confirmed SHA `7c7ffe5` — `a205d75` and `0486e90`, i.e. all of W2-Core, are committed locally but NOT yet pushed; `npm run check` on this state is running in the background as of this checkpoint)
