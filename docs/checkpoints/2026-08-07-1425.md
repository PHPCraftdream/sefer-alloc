# Checkpoint — 2026-08-07 14:25 [rust-intel-multi-crate-audit-round]

## Session summary

This session continued a long-running `sefer-region` correctness/safety/performance round
(fully closed earlier this session — see `docs/checkpoints/2026-08-07-1130.md` for that
round's own detailed narrative), then expanded scope twice more at the user's explicit
request. Sequence, in order: (1) the `sefer-region` `/babygoal` round completed in full —
9 fixes (#664-672) + post-work chain (#674-677: checkpoint/CHANGELOG/commit-markdown/`@oh`
review) all landed; the closing `@oh` review found 8 more issues (findings A-H), all fixed
in 3 more commits (#678-684); (2) user asked `/research @fh sefer-region` for a genuine
follow-up sweep (not re-litigating settled ground) — 3 parallel `@fh` agents (performance/
logic/safety) found real NEW content: `SyncRegion`'s one-shot API is measured
ANTI-scalable under read contention (4x aggregate throughput LOSS at 8 threads; guard-
batching gives ~30x mitigation), `get_cloned` runs `T::clone` under the read lock (writer
stall ≈ full clone duration), a genuine reentrancy self-deadlock (two shapes, both
empirically reproduced), and a factually-wrong poisoning-policy doc sentence (read-guard
panics do NOT poison `RwLock`, contra the current doc) — filed as tasks #685-689, none
yet implemented (all still `pending`); (3) user asked `/rust-intel /research` to sweep the
REMAINING FIVE workspace sub-crates (`numa-shim`, `aligned-vmem`, `racy-ptr-cell`,
`size-classes`, `tagged-index-stack`) with the `rust-intel` skill's fan-out audit workflow
(`audit-project.workflow.js`, one agent per skill theme-module + a synthesis agent, 14
agents per crate) — ALL SIX crates now audited (including `sefer-region` itself, run
earlier: `wf_6a104c8f-b46`, 0 crit/high, 7 medium, 5 info, tasks #690-696). The five new
runs: `numa-shim` (`wf_42d49739-b45`, 0/0/12/10), `aligned-vmem` (`wf_785991fc-92f`,
0/**1 HIGH**/11/9), `racy-ptr-cell` (`wf_12cd2580-8bc`, 0/**1 HIGH**/5/7), `size-classes`
(`wf_5ce47cc9-736`, 0/0/4/8), `tagged-index-stack` (`wf_f39c9b3c-d84`, 0/**2 HIGH**/2/7).
Every audit's full report was written to `docs/reviews/2026-08-07-<crate>-rust-intel-audit.md`
(all currently untracked/uncommitted) and one TaskList entry per crate was filed
(#697-701) summarizing the HIGH + most consequential MEDIUM findings inline in the task
description, citing the full report file for the rest. **The three HIGH findings are the
headline result of this whole session's audit phase**: `tagged-index-stack`'s `pop()` has
a real Relaxed-CAS-failure-ordering bug that can theoretically lose/duplicate free-list
indices (double-allocated slot downstream) — AND its own loom ABA counterfactual test that
was supposed to prove the tag matters has a harness accounting bug and doesn't actually
prove what it claims; `aligned-vmem`'s fault-injection tests run in literally zero CI
configurations despite a CI comment claiming otherwise; `racy-ptr-cell`'s published README
loom-proof claim is silently false for the Release-publish ordering rule (the real-type
test synchronizes via `join()` before reading the marker, hiding exactly the bug the proof
claims to catch). None of tasks #685-701 (17 tasks, all `pending`) have been started yet —
this is purely a research/triage phase; NO code fixes for any of these findings have been
applied. Throughout, the `/babysit` cron (job id unknown from this context but confirmed
still firing every tick) has been ticking on task #656 (`sefer-region` — verify/prepare for
crates.io republish), which is genuinely gated on a maintainer publish/version-bump
decision and cannot be progressed by an agent — every tick this session has correctly
reported "blocked #656" or "still running #656" and taken no action, which is the correct
behavior, not a stall.

## Active goal

No `/goal` Stop hook is active this session (the earlier `/babygoal`-installed goal for
the `sefer-region` round was fully satisfied and cleared once #664-684 all landed).
`/babysit` is still armed and ticking on the TaskList generically (not tied to a specific
goal condition) — see the babysit-tick pattern in the Session summary above.

## TaskList

### in_progress
- #656 sefer-region — verify/prepare for crates.io republish (genuinely gated on a
  maintainer decision — see Open questions; not actionable by an agent)

### pending
- #657 numa-shim — verify/prepare for crates.io republish
- #658 aligned-vmem — publish 0.2.0 (local already bumped, crates.io still shows 0.1.0)
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #662 Root sefer-alloc — add bench-scale-tool alongside existing criterion/iai benches
- #663 4 dev-only sub-crates — bench-scale-tool + captrack tooling
- #673 sefer-region: [UNVERIFIED — no defect found] add one contended SyncRegion
  measurement as a future decision gate (deliberately deferred, blocks nothing)
- #685 sefer-region: doc contended-perf guidance (one-shot reads anti-scale; +decide how
  to close #673)
- #686 sefer-region: doc get_cloned's clone-under-lock cost
- #687 sefer-region: doc the reentrancy self-deadlock (2 shapes) + promote remove's
  drop-outside-lock to stated contract + note the RwLock-guard-type semver coupling
- #688 sefer-region: fix the poisoning-policy doc (read-guard panics do NOT poison —
  independently confirmed 3x)
- #689 sefer-region: Region↔SyncRegion doc-symmetry pass + 2 minor nits
- #690 sefer-region: reserve()/with_capacity() debug-vs-release panic divergence —
  needs a maintainer decision (code fix vs doc-only, the one behavior-changing option
  this whole round has surfaced)
- #691 sefer-region: add #[repr(transparent)] to Handle<T> before first publish
- #692 sefer-region: tighten reserve-overflow catch_unwind test
- #693 sefer-region: two testing gaps (untested concurrency claim + no release-profile
  CI run for a documented "profile-independent" panic contract)
- #694 sefer-region: clear_partial_under_panic pins slotmap's unspecified drain order
- #695 sefer-region: qualify README's "DefaultKeys never escape" claim
- #696 sefer-region: minor test hygiene (2 small items, low priority)
- #697 numa-shim: address rust-intel audit findings (12 medium, 10 info — see its own
  audit file; most consequential: mbind maxnode off-by-one, cpumap-read truncation on
  large-NUMA hosts, None-case doc/code mismatch, Windows commit-charge doubling, zero
  behavioral oracle on the mbind path)
- #698 tagged-index-stack: fix pop's Relaxed CAS-failure ordering (real bug) + repair the
  loom ABA counterfactual's accounting bug — **both HIGH findings for this crate**
- #699 aligned-vmem: fault-injection CI gap (HIGH) + the already-crashed-a-consumer
  offset-contract bug + 9 other medium findings
- #700 racy-ptr-cell: README's loom-proof claim is silently false (HIGH) + an
  unwind-livelock bug + a release-mode sentinel-collision gap
- #701 size-classes: silently-masked geometric overflow + Params non_exhaustive decision
  + 2 smaller findings

### recently completed
- #684 sefer-region: Fix G — unexplained numeric disagreement in sync/churn spread
- #683 sefer-region: Fix F — CHANGELOG double-counts a test
- #682 sefer-region: Fix E — CHANGELOG #665 bullet overstates what shipped
- #681 sefer-region: Fix C — region_invariants.rs absolute "None forever" claim
- #680 sefer-region: Fix B — PLAN.md orphaned text fragment + indentation
- #679 sefer-region: Fix A — INVARIANTS.md fabricated remove_from_slot mechanism
- #678 sefer-region: Fix D — #670's I3 capacity assertion didn't discriminate slot reuse
- #677 Post-work: `@oh` final review of all sefer-region round work
- #676 Post-work: commit all markdown docs from that round
- #675 Post-work: update CHANGELOG.md with the sefer-region round

## Decisions

- Chose to treat #656 (task's own description explicitly says "the user makes the actual
  publish/republish decision... do not mark this task completed on your own") as
  perpetually non-actionable by an agent — every babysit tick reports "blocked" rather
  than attempting to force progress, and this is the CORRECT behavior per the task's own
  gate, not a bug in the tick loop.
- Chose, for the 5 new `rust-intel` audits, to file ONE consolidated task per crate
  (rather than one task per individual finding, which would have produced 50+ tasks
  across ~50 findings) — each task's description enumerates the HIGH + most
  consequential MEDIUM findings inline with concrete fix guidance, and points to the
  full audit file for the remaining MEDIUM/INFO items. This is a deliberate scale
  tradeoff versus the finer-grained one-task-per-finding pattern used earlier this
  session for the sefer-region closing-review findings (#678-684) and follow-up-review
  findings (#685-696) — justified by the sheer volume (5 crates × ~10-20 findings each)
  making 1:1 task filing impractical to review/triage at this scale.
- Chose NOT to start implementing any of the 17 pending audit-derived tasks (#685-701)
  yet — this session's audit phase was explicitly research/triage only; no user
  instruction to begin fixing landed before this checkpoint was requested.

## Open questions

- **Version bump for sefer-region 0.1.1** (task #656's own gate, restated from the prior
  checkpoint): whether the round's false-claim fixes (and now the follow-up findings in
  #685-696) warrant a patch republish. Not decided this session — maintainer's call.
- **#690's code-fix-vs-doc-only choice**: reserve()/with_capacity()'s debug-vs-release
  panic contract divergence is the one finding across the ENTIRE audit sweep this session
  that proposes an actual behavior change (a `checked_mul`/`checked_add` guard) rather
  than a pure doc/test fix — every other task this round stayed within a docs/tests-only
  gate by design. Needs explicit maintainer sign-off before implementation.
- **#687's RwLock-guard-type semver question** (the §B1b sub-finding folded into #687):
  whether to just document that `SyncRegion::read()`/`write()` returning std's concrete
  `RwLockReadGuard`/`WriteGuard` types is a deliberate stable commitment, or to wrap them
  in opaque newtype guards before first publish (an actual API change, bigger scope).
- **Publish sequencing for the 5 remaining crates.io crates** (#657-661): each now has a
  fresh rust-intel audit informing what should land before that specific crate's first/
  next publish (e.g. `size-classes`'s Params #[non_exhaustive] decision, `tagged-index-
  stack`'s two HIGH bugs, `aligned-vmem`'s CI gap) — no publish order or version-bump
  decision has been made this session; all still gated on the maintainer.
- **Root `sefer-alloc` bench-scale-tool integration (#662) and the 4 dev-only sub-crates'
  tooling wiring (#663)** — untouched this session, pre-existing open items, no new
  information.

## Repo state

```
?? docs/reviews/2026-08-07-aligned-vmem-rust-intel-audit.md
?? docs/reviews/2026-08-07-numa-shim-rust-intel-audit.md
?? docs/reviews/2026-08-07-racy-ptr-cell-rust-intel-audit.md
?? docs/reviews/2026-08-07-sefer-region-followup-logic-review.md
?? docs/reviews/2026-08-07-sefer-region-followup-performance-review.md
?? docs/reviews/2026-08-07-sefer-region-followup-safety-review.md
?? docs/reviews/2026-08-07-sefer-region-round-closing-review.md
?? docs/reviews/2026-08-07-sefer-region-rust-intel-audit.md
?? docs/reviews/2026-08-07-size-classes-rust-intel-audit.md
?? docs/reviews/2026-08-07-tagged-index-stack-rust-intel-audit.md
```

(Note: `docs/checkpoints/2026-08-07-1425.md`, this file, will also appear untracked once
written — checkpoints are never auto-committed per this skill's own convention.)

```
aa24f84 docs: correct three CHANGELOG/README accuracy residuals (tasks #682-684)
9fcbbf1 docs: fix three surviving stale-claim residuals from task #664's sweep (tasks #679-681)
39704e1 test(region): fix I3 test to actually discriminate slot reuse (task #678)
a980f58 docs: commit all sefer-region round markdown artifacts (task #676)
8dfd041 docs: add the sefer-region correctness/safety/performance sweep to CHANGELOG.md (task #675)
```

Working tree is otherwise clean — no uncommitted source changes, only the 9 untracked
review/report markdown files listed above (plus this checkpoint). Nothing is mid-edit.
