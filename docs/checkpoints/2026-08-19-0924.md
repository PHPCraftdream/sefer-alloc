# Checkpoint — 2026-08-19 09:24

## Session summary

The user is preparing the `aligned-vmem` crate (in `crates/aligned-vmem`, version 0.2.0,
unpublished) for its first real release to crates.io. This session continued a long
adversarial-review campaign driven by a `/babygoal` directive: solve all `[audit-vmem]`
tasks via `/crush` sub-agents in isolated git worktrees, commit between tasks, then have
an independent `@oh` agent review the wave and file new prefixed tasks from its findings.
That loop ran to completion — waves OH6 through OH11 all closed in this session (tasks
#1120 through #1138), each with its own worktree fan-out, its own zero-trust merge, its
own full `npm run check`, and its own closing review.

**The campaign is now finished and everything is pushed.** 59 commits landed as
`a1e1e89..1ed79e9`; the landing SHA was read from the remote (`git fetch && git rev-parse
origin/main`), not from local HEAD, per CLAUDE.md. `origin/main..HEAD` is 0. The final
gate before the push was fully green: 47/47 steps, `[check-all] ALL GREEN — safe to push`.

Three notable things happened around infrastructure. First, the gate was killed three
times mid-run at the heaviest steps (twice on `cargo test --all-features` with a cold
cache at step 11/47, once on `iai` at 46/47); I diagnosed this by running the suspect
step alone (it passed, exit 0), which showed the failure was duration-related, not
content-related — a warm cache moved the death point from step 11 to step 46. Second,
the user asked to disable perf runs on GitHub; the cause turned out to be that the fix
already existed locally (commit `3e4190e`, task #1090, commenting out the nightly cron)
but was among the then-unpushed commits, so GitHub kept using the old file. I disabled
it server-side with `gh workflow disable perf-gate` rather than pushing 59 unreviewed
commits — it now reads `disabled_manually`. **Re-enabling later requires BOTH
uncommenting the triggers in `perf-gate.yml` AND `gh workflow enable perf-gate`.**
Third, mid-session the `crush` provider `zai` entered its peak-hours window (08:00–12:00
local, refusing until 12:00); per the `/crush` skill I did not add `--allow-peak-hours`
or switch provider unilaterally, reported the refusal, and the user directed me to
continue with `@sh` agents instead — which worked.

After the push, an `@ox` publication-readiness audit ran against `1ed79e96`. Its verdict
is **NO-GO**, and it is materially deeper than the summary the user pasted from a
parallel review. Both audits agree the `page_size()` fail-open-to-4-KiB defect is real
and that the crate's own `page_size()` rustdoc gives false comfort (its "all-or-nothing"
sentence is about the ADDRESS, while `madvise` rounds the LENGTH up). They **disagree on
severity** — HIGH publication-blocker vs. MEDIUM latent — and I deliberately did not pick
one silently; that disagreement is now task #1145, a Phase-0 owner decision that
determines the scope of #1139.

The deep audit's most valuable findings were ones eleven waves never looked at, because
they live in the *published artifact* rather than the repo's own paperwork: the crates.io
front page (README.md) still documents a 0.1-era API and never mentions `LazyReservation`
at all (`grep -c` → 0); docs.rs will render four dead links, and CI's doc gate is
structurally blind to them because it builds `--all-features` — the one feature set in
which those links resolve — while `package.metadata.docs.rs` deliberately excludes
`bench-internals`. That last one is the **fourth** instance of a meta-pattern CLAUDE.md
already has three rules about (R26-4 wrong config, R30-8 wrong code path, R31-0 wrong
layer); the new axis is "wrong feature set".

The audit also measured something worth acting on: since commit `2cedbbb` (the `&mut
self` fix, wave OH5) there have been 21 commits and **the shipped crate's executable code
has not changed by a single non-comment line** — I verified this independently. The last
four waves found nothing in shipping code; three consecutive waves argued about the same
miscounted number. The audit's recommendation is "publish after the named fixes and stop
reviewing"; whether to run a twelfth wave is folded into #1145 as an owner decision.

The final act of the session was restructuring the seven crate tasks (#1139–#1145) into
an ordered work plan at the user's request. In doing so I found a real duplication that
would have sent two agents into the same file: item 72's falsification appeared in both
#1141 (as "L2") and #1143 (as finding 1 of 9). I split them **by artifact, not by
wording** — #1141 now owns the code and `ci.yml` changes, #1143 owns all index-card
corrections — and set real `blockedBy` edges rather than describing the ordering in prose.

Currently in flight: nothing is executing. CI on the landing SHA was still `in_progress`
for the main workflow at checkpoint time (Kani already `success`), so **CI green on
`1ed79e96` is NOT yet confirmed** and should be checked first thing next session. A
`/babysit` cron fires every 15 minutes and will keep waking on the pending tasks; the
user was told it can be removed with `CronDelete` but I have not touched it.

## Active goal

None in force. The `/babygoal` directive (`[audit-vmem]` — solve all tasks via `/crush` &
worktrees, commit between tasks, then review with `@oh` and file new prefixed tasks) ran
to completion this session. The user's last explicit instruction was to file tasks from
the review and **stop**, which I did.

## TaskList

### in_progress
- #657 numa-shim — verify/prepare for crates.io republish
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io

### pending — the crate work plan (this is the ordered list)

**Phase 0 — decision (blocks Phase 1's scope)**
- #1145 `[ФАЗА 0 — РЕШЕНИЕ]` Two audits disagree on H1 severity (HIGH blocker vs MEDIUM);
  the choice determines #1139's scope. Also folds in the "stop reviewing?" decision.

**Phase 1 — publication blockers**
- #1139 `[ФАЗА 1 — БЛОКЕР]` `page_size()` fail-open to 4 KiB: safe decommit can discard
  live data past the requested end  (blockedBy: #1145)
- #1140 `[ФАЗА 1 — БЛОКЕР]` HugeTLB contract stale since Linux 5.18 — `MADV_DONTNEED`
  works, the crate unconditionally skips it and promises `EINVAL`
- #1142 `[ФАЗА 1 — БЛОКЕР]` Published artifact: crates.io page documents the 0.1 API,
  docs.rs has four dead links, and the gate is blind by construction

**Phase 2 — fix before publishing**
- #1141 `[ФАЗА 2]` `VmemError` taxonomy incomplete (three sources claimed, a fourth
  exists) + add a real-backend release CI row

**Phase 3 — hygiene, non-blocking**
- #1143 `[ФАЗА 3]` Indexes are currently untrue: nine discrepancies, incl. item 72
  falsified by the very task that cited it
- #1144 `[ФАЗА 3]` Prefix lint fires nowhere in this repo; false MSRV claim;
  `fault-injection` carries the hazard `mock` was moved away from

**Phase 4 — release**
- #658 `[ФАЗА 4 — РЕЛИЗ]` Publish aligned-vmem 0.2.0
  (blockedBy: #1139, #1140, #1142)

**Out of campaign scope**
- #1091 `[perf-regress]` Find and fix the ~10-13% Ir regression in `42d8d223..42d42061`
  (perf item 56). NOTE: the Callgrind artifacts of the last green run expire ~2026-09-04.
- #662 Root sefer-alloc: design note for bench-scale-tool alongside criterion/iai
- #763 Root sefer-alloc: implement bench-scale-tool per the approved design

### recently completed (last 10)
- #1138 `[OH11]` A figure wrong three times running (52 → 48 → 46)
- #1137 `[OH11]` A completeness-claiming sweep missed the same class it fixed
- #1136 `[OH10]` "Zero flagged lines" rests on an unnamed carve-out
- #1135 `[OH10]` The census answered "is it in any gate", not "is it in CI"
- #1134 `[OH10]` The measurement behind "document, don't fix" was taken blind
- #1133 `[OH9]`  My rewrap relocated the orphan instead of removing it
- #1132 `[OH9]`  `URL_TOKEN` bought a silent miss for a branch with nothing to catch
- #1131 `[OH9]`  A guard hardened across eight waves that no gate ever ran
- #1130 `[OH8]`  All five task citations in the guard named the wrong task
- #1129 `[OH8]`  One `android` mention would have re-proposed a twice-closed mistake

## Decisions

- **Disabled perf-gate server-side (`gh workflow disable`) rather than pushing to fix
  it.** The fix existed locally but was among 59 unpushed commits; pushing them all would
  have shipped an unreviewed campaign to satisfy a one-line request. Rejected: push now;
  add `--allow-peak-hours`-style workarounds.
- **Did not pick a severity for H1 when two audits disagreed.** Filed the disagreement
  as an owner decision (#1145) with both audits' reachability arguments recorded,
  because the choice changes #1139's scope from "full fail-closed" to "one rustdoc
  paragraph". Rejected: silently adopting the HIGH rating from the pasted summary.
- **Split the task duplication by artifact, not by wording.** Item 72 appeared in both
  #1141 and #1143; #1141 now owns code + `ci.yml`, #1143 owns index cards. Rejected:
  merging both into one task (would have mixed engineering and doc work in one worktree).
- **Kept #1142 (README + docs.rs) as a Phase-1 blocker despite being "just docs".** What
  is wrong is the artifact that actually ships. Rejected: demoting it to hygiene.
- **Moved the `## 0.2.0 - Unreleased` date-stamp out of a finding and into #658's release
  checklist.** It is a release precondition already gated by `release.yml`, not a defect
  needing its own task — but the gate does NOT cover a hand-run `cargo publish`.
- **Did not add `--allow-peak-hours` or switch provider when `crush` refused.** Reported
  the refusal and waited; the user redirected to `@sh`. Rejected: routing around a
  provider's own refusal unilaterally.

## Open questions

- **#1145 is unanswered and blocks #1139**: is the `page_size()` fail-open a HIGH
  publication blocker (audit A) or a MEDIUM latent defect (audit B)? Three options are
  written out in the task: (a) full fail-closed, (b) rustdoc paragraph + oracle,
  (c) BSD-branch-only fail-closed.
- **Should there be a twelfth review wave?** The deep audit says no, on measured grounds
  (21 commits, zero non-comment shipped-code lines changed). Folded into #1145.
- **Should the `/babysit` cron be removed?** It fires every 15 minutes and will keep
  waking on the pending tasks now that the user has said stop. I have not touched it —
  it is the user's tool.
- **Should both open-items indexes be re-split?** Both are past the ~1000-line R34-24
  threshold again (2409 and 2422 lines) after the earlier split. Noted inside #1143 as a
  sub-decision rather than assumed.

## Repo state

```
?? docs/checkpoints/2026-08-18-2103.md
?? docs/reviews/2026-08-18-aligned-vmem-publication-readiness-audit.md
?? docs/reviews/2026-08-19-aligned-vmem-publication-readiness-audit.md
```

Three untracked files, none of them mine to commit unasked: two audit reports (written by
the review agents) and a prior checkpoint. `git diff --stat` is empty; the working tree
is otherwise clean and identical to `origin/main`.

```
1ed79e9 docs: task #1138 — a figure wrong three times running, each correction landing in a commit about wrong figures
ce6ed88 build: task #1137 — a sweep that claimed completeness missed the same class it fixed, and the eighth script was never a gap
9b3d24b docs: task #1136 — the index records what two of my commit bodies got wrong, and the brief that ordered the record was wrong twice itself
0464369 build: task #1135 — the census answered "is it in any gate", but the wave's own standard was "is it in CI"; four of seven guards now have rows
89d28c5 build: task #1134 — the measurement behind "document, don't fix" was taken with an instrument that had already erased what it measured
```

**Sync state:** `origin/main` = `1ed79e9`, local HEAD = `1ed79e9`, ahead = 0. Everything
is pushed.

**CI on the landing SHA `1ed79e96` — NOT yet confirmed green.** At checkpoint time:
Kani verification = `success`, main CI workflow = `in_progress`. This is the first thing
to check next session; CLAUDE.md requires confirming CI on the SHA read from the remote,
and that confirmation is still outstanding.

**GitHub workflow state:** `perf-gate` is `disabled_manually` (this session, at the
user's request). `CI`, `Kani verification` and `Release` remain `active`.
