# Checkpoint — 2026-09-03 13:46

## Session summary
Continuation of the `tagged-index-stack` publish-readiness campaign (this session picked up from checkpoint `2026-09-03-0802.md`, itself mid-way through run-8). Since then, ran runs 9 through 14 of the Sol-codex external review loop end-to-end, each following the same pattern: an external process periodically drops a new dated review doc under `docs/reviews/` and auto-commits it directly to `main` in parallel with my own work (confirmed repeatedly this session, not a bug); on each new review, I read it, decomposed findings into grouped tasks (blocking P1/P2 first, non-blocking P3 last, `blockedBy` reflecting real file-overlap dependencies), executed via sub-agents in isolated git worktrees, personally zero-trust-reviewed every diff (read line-by-line, independently re-ran tests/loom/rustdoc in an isolated `CARGO_TARGET_DIR`, ran real counterfactuals where possible), cp-merged (never `git merge`) with a mandatory staleness check before each merge, ran `npm run check`, pushed, and confirmed CI+Kani green on the actual landing SHA (not local HEAD) before moving on.

**Run 9** (P1 flagship): fixed a tag-wrap ABA hole (`TagExhausted` seal at `TAG_MAX`) plus 3 P2/P3 cleanups. **Run 10** (P2 flagship): fixed the codegen A/B wrapper's stale safe-fn-pointer signature after `push` became `unsafe fn -> Result`. **Run 11**: 4 independent P3 cleanups (stale "seven" fixture count, dead CI path citation, codegen wrapper warning-hardening, a stale wrap-named test rename) — first round with a clean **GO** verdict. **Run 12**: 3 more P3s (`with_tag_for_test` silently truncating out-of-range tags instead of panicking, README's unsafe-region-vs-site terminology, `DURABILITY.md`'s stale wrap/pop-counted framing) — also GO. **Run 13**: first round executed via **`/wrush`** instead of the `Agent`-tool-with-`isolation:"worktree"` pattern, per the user's explicit standing instruction ("далее для работы используем /wrush") issued mid-session — 3 more independent P3s (terminology sweep continuation, `OPEN_ITEMS.md` item 61 correcting a false "measurement-ready" claim, another stale test-name reference) — GO again. Between run-13 and run-14, also handled a **standalone, non-review request**: the user asked to move CI off the Node.js 20 action runtime (`actions/setup-node@v4`→`v5`, `actions/upload-artifact@v4`→`v6` in both `ci.yml` and `perf-gate.yml`) — verified via GitHub's API which minimum major versions actually declare `runs.using: node24` before bumping (not guessed), confirmed the "Node.js 20 is deprecated" warning is gone from the resulting CI run's full log.

**Run 14** was the second real blocker since run-9: **P1** — `StackOps::push_index`'s two-clause `# Safety` contract (link domain, liveness) only checks point-in-time reachability at call entry, so two threads concurrently pushing the *same* never-live index both satisfy the literal contract text, and the loser's CAS-retry can write a self-loop (`next[X] = X`) after observing the winner's just-published head — a real, non-hypothetical soundness-contract gap (not an algorithm bug: "given a strengthened exclusive-ownership precondition, the algorithm itself is correct" per the review). Fixed by adding a **third clause** (exclusive temporal ownership, composing with clause 2) to the ONE normative location and propagating just the clause-*count* label (not full text) to ~10 cross-reference sites across `imp.rs`, both READMEs, `CHANGELOG.md`, `lib.rs`, and script/compile-fail SAFETY-comment labels — plus a new loom counterfactual (`counterfactual_same_index_concurrent_push_self_loops`) that races two real `push(0)` calls and proves the self-loop panic is reachable. The sub-agent's first draft of that test had a real bug (a fallback panic that pre-empted loom's exploration before a self-loop-observing schedule fired, since `pop`'s initial head load is a plain `Acquire` that can legitimately observe stale pre-self-loop state) — it diagnosed this itself with temporary step-logged probes, fixed it, and reported the whole debugging story instead of hiding it. I independently re-ran the full loom suite (14/14 including the new should-panic test) both in the sub-agent's worktree and again after merging onto `main`, and verified the panic's expected substring against the real `pop_link_out_of_range` message myself. Two P2s (self-verifying unsafe-inventory grep actually returning 12 not the promised 8; A/B harness's warm-up window opening *before* the warm-up it claimed to run, fixed by mirroring the already-correct benchmark file's future-anchor + entry-lateness-guard protocol) were fixed in parallel via `/wrush`, both merged. P1 (GroupA) is merged as of this checkpoint; **P3 GroupD (OPEN_ITEMS item 63 stale line refs + `StackHead` layout claim needing `#[repr(transparent)]`) is still running**, deliberately forked from *post-P1* `main` since it also touches `imp.rs`.

**Also in flight, launched in parallel with run-14's P1**: a separately-named, non-Sol-codex review, `docs/reviews/R1_tagged_index_stack_review.md` (also auto-committed by the same external-process pattern). Per the user's explicit instruction ("если ревью адекватное" — only act if the review is adequate), I assessed it myself before delegating: independently verified its two most substantive claims against the real source (a misleading `wrapping_add`-doesn't-need-checked-add comment — confirmed correct on the math, though the review's own file:line citation for it was wrong, pointing at the call site instead of the comment's real location ~1150 lines earlier; a redundant bounds mask in `pack_truncating` — confirmed correct by reading all 4 call sites and proving each already guarantees in-range input). Judged the review adequate and scoped a single combined task to its 7 findings that have a real (non-"none") "Fix:" entry in its own summary table, explicitly skipping the 5 it itself marks as no-fix-needed. That task (`tis-r1review`) is **still running** as of this checkpoint, in its own worktree, entirely independent of the run-14 P3 GroupD also in flight.

## Active goal
None — no `/goal` Stop hook is in force this session.

## TaskList
### in_progress
- #1870 tis-sol14-GroupD: P3-1 (OPEN_ITEMS item 63 stale `tis_p3_ab_runner.mjs`/CHANGELOG line refs) + P3-2 (`StackHead` layout claim → `#[repr(transparent)]`) — running via `/wrush`, forked from post-P1 main
- #1872 tis-r1review: apply R1 review's 7 fix-required findings (misleading comment, dead mask, `Backoff::spin` split, 2 SAFETY-comment extensions, pop-CAS cross-reference, proptest const-assert) — running via `/wrush`

### pending
- #661 tagged-index-stack — first publish to crates.io (blockedBy: none — technically unblocked, deliberately held pending explicit user go-ahead, irreversible: `cargo publish` has no delete, only yank)
- #1871 tis-sol14-T-push: merge GroupD once it lands, `npm run check`, push, confirm CI+Kani (blockedBy: #1867 [done], #1868 [done], #1869 [done], #1870 [in progress])

### recently completed
- #1867 tis-sol14-GroupA: P1 — push_index's contract now requires exclusive temporal ownership (`f65ee31`)
- #1868 tis-sol14-GroupB: P2-1 — self-verifying unsafe-inventory grep now actually returns 8 (`ccb48aa`)
- #1869 tis-sol14-GroupC: P2-2 — A/B harness warm-up window now actually runs (`5f34b9c`)
- (all run-9 through run-13 tasks are also completed and already deleted from the live TaskList this session, per an explicit "удали выполненные таски" request — durable record is `git log`, not the TaskList)

## Decisions
- Switched sub-agent delegation mechanism mid-session from the `Agent` tool's `isolation:"worktree"` param to `/wrush` (manually-created `git worktree add` + `rush run`), per the user's explicit standing instruction. `/wrush`'s own convention (worktrees live under `<repo-root>/worktrees/`, `.gitignore`d) matches the mitigation I'd already independently adopted earlier in the session after discovering `isolation:"worktree"` snapshots `main` from a fixed early point, not live-current-`main`, across multiple launches in the same session.
- For run-14's genuinely blocking P1: verified with maximum rigor given the severity — independently re-ran the full loom suite twice (in the sub-agent's worktree, then again on the merged `main` tree) rather than trusting the sub-agent's own "14/14 passed" claim, and independently confirmed the new should-panic test's expected panic substring against the actual source of that panic message.
- Assessed the separately-named `R1_tagged_index_stack_review.md` for adequacy myself before delegating any fix work, per the user's explicit "if adequate" gate — independently verified two of its claims against the source (one review citation turned out to be wrong even though the underlying technical claim was right) before judging the review trustworthy enough to act on.
- Scoped the R1 review's fix task to exactly the 7 findings with a real "Fix:" entry in the review's own summary table, skipping the 5 it explicitly marks "none required" — avoids scope creep into polish the review itself doesn't ask for.
- `cargo publish` (`#661`) still deliberately NOT executed — same standing irreversibility reasoning as every prior checkpoint this session. Now the SIXTH consecutive independent review round (run-9 through run-14, plus the separate R1 review) to find and close at least one real pre-publish issue in this crate.

## Open questions
- Does the user want `#661` (`cargo publish`, first release of `tagged-index-stack` to crates.io) executed once the current in-flight work (GroupD + R1-review fixes) lands and is confirmed green, or held for a further separate explicit request? This is now the sixth consecutive independent review round to find and close a genuine pre-publish issue — still seems worth flagging as a pattern before actually publishing, once/if authorized.

## Repo state
```
(main worktree clean — nothing to commit, working tree clean)
```
```
f65ee31 fix(tis)!: tis-sol14-GroupA — push_index's contract now requires exclusive temporal ownership (P1)
3df5a2b review(R1): tagged-index-stack round-1 read-only review
5f34b9c fix(tis): tis-sol14-GroupC — A/B harness's warm-up window now actually runs before the counted window
ccb48aa fix(tis): tis-sol14-GroupB — self-verifying unsafe-inventory grep now actually returns 8
9b64267 docs(review): audit tagged-index-stack Sol-codex run 14
6f9b04f build(ci): move setup-node/upload-artifact off the deprecated Node.js 20 action runtime
f83c90e fix(tis): tis-sol13-GroupB — OPEN_ITEMS.md item 61 no longer claims a ready measurement variant
15778e9 fix(tis): tis-sol13-GroupC — trim run-12's triple-repeated with_tag_for_test explanation
```

Two worktrees currently live and in-use (NOT stray — active background work, do not remove): `worktrees/tis-sol14-groupd` and `worktrees/tis-r1review`. All other run-9 through run-14 worktrees have already been merged and cleaned up (`git worktree remove --force` + `git branch -D`) as each group landed. Landing SHA `6f9b04f` (the Node-24 CI change, last SHA actually pushed+confirmed green so far) was confirmed green on both CI and Kani; `f65ee31` and everything after it is local-only, not yet pushed — the run-14 T-push (`#1871`) is still blocked on GroupD.
