# Checkpoint — 2026-09-21 21:02 [none]

## Session summary

This session continued directly from an earlier segment (recovering the
repo after an accidental deletion, then a large `/wrush`-driven reorg of
`src/` so every file is ≤1000 lines and every directory ≤7 entries: split
`alloc_core/`, `registry/`, `global/sefer_alloc.rs`, and `concurrent/`,
each merged via cherry-pick with zero-trust verification). After the reorg
landed and CI went green (fixing two real `--all-features`/feature-combo
clippy regressions the reorg had introduced), the user asked for a parallel
static review of `src/` via `/rush --role reviewer`. I launched 4 parallel
reviewer agents (alloc_core, registry, global, concurrent), each in its own
worktree, each producing `docs/reviews/2026-09-20-<area>-perf-quality-review.md`
and committing it. I personally spot-verified findings against the real
source before merging all four review commits.

The user then asked to "проверь отчеты, заведи таски по корректным
находкам" (check the reports, file tasks for the valid findings). I read
all four reports in full, cross-checked every citation against the actual
code (not just trusted the prose), and filed 19 tasks (#1980-#1998), each
carrying the full verified finding, exact file:line citations, fix
direction, and measurement/framing requirements (R30-12 commit-prefix
taxonomy) directly in the task description — written so a fresh
session/agent could pick any one up without re-reading the reviews.

The user then invoked `/babygoal давай реализуем все задачи с помощью
/wrush` (implement all tasks via /wrush). Domain was already covered by
this session, so I skipped investigation and went straight to strategy:
batches-with-worktree-isolation, grouped into non-overlapping-file agent
batches. I armed `/babysit` (cron job `872482b5`, every 15m,
`7,22,37,52 * * * *`) BEFORE starting work, then launched 5 parallel rush
agents (wave 1: `fix-concurrent-tier` for #1980/#1988/#1989,
`fix-owner-stamp` for #1981, `fix-alloc-zeroed-classify` for #1982,
`fix-realloc-assert` for #1984, `fix-large-cache-bitmask` for #1985), each
in its own worktree per `/wrush`'s worktree-isolation contract.

**5 parallel rush agents exceeded the account's request-rate ceiling**: 2
died immediately on "Rate limit reached for requests" (fix-concurrent-tier,
fix-alloc-zeroed-classify — both wrote nothing, 0 commits); the other 3
survived long enough to do real, substantial work but then hit their own
120-minute `--timeout` (exit 124) before committing. Rather than relaunch
more parallel agents into the same rate-limit wall, I switched strategy:
personally zero-trust-reviewed each of the 3 timed-out agents' uncommitted
worktree diffs (including running each one's NEW tests counterfactually —
temporarily reverting the fix to confirm the test actually goes red), fixed
one real gap each agent's own scope-lock had prevented it from fixing
(a stale `255`→`256` test-file-count doc drift for fix-owner-stamp; a
tripwire-doc wording correction after fix-large-cache-bitmask made an
earlier claim inaccurate — caught this myself, not flagged by any agent),
ran the full `npm run check`-equivalent matrix (all 6 clippy rows +
production+internals+bench-internals+batch-api test suite) myself on each,
committed, cherry-picked onto `main`, and removed the worktree. This became
the pattern for the rest of the session: for every remaining task I either
verified-and-merged an agent's leftover work, or (for the 2 agents that
wrote nothing, plus subsequent tasks) implemented the fix myself directly
in a fresh worktree, followed by the same personal-verification-then-merge
discipline — no more parallel rush agents were launched after wave 1's
rate-limit lesson.

Two implementation choices where I deliberately deviated from the task
description's own suggested design, because the suggested design had a
real bug: (1) #1989's task description sketched a `pending: AtomicUsize`
counter for the drain-fast-path fix; I used an `AtomicBool` instead because
a count incremented AFTER the push can underflow (a drain can take an item
whose increment hasn't landed, then subtract it) — wrote out the full
stranding-impossibility argument in the code comment. (2) #1982 (alloc_zeroed
double-classify): implemented via a clean `alloc`/`alloc_with_class` split
(verbatim body extraction, `#[cfg]`-gated classification parameter,
`#[inline(always)]` preserved), then discovered — not flagged by the
original review — that `benches/perf_gate_iai.rs` gates the REAL
`alloc_zeroed_*` benches on `virgin-zero-skip` and compiles a `black_box(0u8)`
stub otherwise, so the exact path this fix touches has ZERO iai coverage
under plain `production`. Landed the fix as `fix(perf)` (no speedup
claimed, per CLAUDE.md's R30-12 taxonomy) and filed a new task, #1999, to
close that measurement gap.

11 of 20 tasks are done and merged onto `main` as of this checkpoint (none
pushed to `origin` this segment — the earlier CI-green push was a separate,
already-completed prior action). 9 remain: #1983, #1986, #1991-#1996,
#1998, #1999 (all `pending`). A `npm run check`-equivalent 6-row clippy +
full test verification was run personally for every merged task (not
trusted from any agent's self-report), plus counterfactual test verification
for every new falsification test added (#1981's `owner_stamp_round_trip.rs`,
#1984's re-pinned `no_panic_doc_accuracy.rs`).

Two unexplained worktrees continue to exist untouched (`worktrees/head-check`,
`worktrees/sefer-baseline-ae87141e`, both detached at `ae87141e`) — flagged
in a prior checkpoint, still not investigated. A third foreign worktree,
`worktrees/src-review-xa-20260920` (detached, not mine), belongs to a
DIFFERENT, still-apparently-live rush session (`--session
deployment-folder-observed`, confirmed via a live process at one point this
segment) that independently committed `a0d654f1 docs(review): add
independent static src review xa` directly onto `main` between two of my
own commits — not created or reviewed by me; left untouched per the
standing "don't touch other agents' work" rule.

## Active goal

None (`/goal` was never invoked this segment; `/babygoal`'s own contract —
babysit cron + TaskList — is the only automation in force, not a Stop-hook
condition).

## TaskList

### in_progress
(none)

### pending
- #1983 Add a load-filter before drain_dirty_segments' per-word atomic swap (blockedBy: none)
- #1986 Fuse stats()'s two registry slot-array walks into one pass (blockedBy: none)
- #1991 Remove dead code flagged by the reviews: tls_heap::current()/bind_slow(), pop_free's block_size, pinning's R: Send (blockedBy: none)
- #1992 Doc-accuracy pass over the false/stale claims flagged by all four reviews (blockedBy: #1980, #1981, #1984, #1991 — all now resolved, so effectively ready)
- #1993 Factor the triplicated ring-drain body (demonstrated R10-3 drift) into one helper (blockedBy: none)
- #1994 Collapse the registry/alloc_core duplication clusters flagged as drift surfaces (blockedBy: #1982, #1983, #1986, #1993 — #1982 resolved, #1983/#1986/#1993 still open)
- #1995 Add negative caching for the bind-less cold arm (blockedBy: none)
- #1996 Harmonize the dbg_* hook gating in global/ (blockedBy: none)
- #1998 Narrow pooled_count/pool_cap from usize to u32 (blockedBy: none — explicitly conditional on a real footprint measurement, per its own description)
- #1999 Close the iai coverage gap: alloc_zeroed's non-virgin path is benched as a black_box stub (blockedBy: none)

### recently completed (this segment, all merged to main)
- #1980 Fix LockFreeRegion::with_pages silent truncation
- #1981 Fix pack_owner's unmasked owner_id (fallback-heap stamp cache bug)
- #1982 Stop alloc_zeroed classifying twice
- #1984 Demote realloc_inplace_fast_path_known_base's release assert!
- #1985 Drive large-cache scans from the occupancy bitmask
- #1987 Resolve the #[inline(always)] asymmetry across GlobalAlloc methods
- #1988 Delete the no-op epoch::pin() from EpochRegion::insert
- #1989 Give drain_remote_free a real lock-free fast path
- #1990 Widen trim_current_thread's feature gate
- #1997 Align the two dealloc arms' SAFETY wording
(plus #1976/#1977/#1978/#1979 — the src/ reorg tasks — and the 4 review-authoring
tasks, all completed in the prior segment of this same session)

## Decisions

- Abandoned 5-way parallel rush agents after wave 1 hit the account's
  request-rate ceiling (2 agents died on "Rate limit reached", 3 more hit
  their own 120m timeout before committing) — switched to a
  verify-then-merge-agent-leftovers-or-implement-myself pattern for the
  rest of the session, never re-attempting >1 concurrent rush agent.
- Chose `AtomicBool` over the review-sketched `AtomicUsize` pending-counter
  for #1989's drain fast path: a post-push increment can underflow under
  concurrent drain/push interleaving; a boolean has no such arithmetic and
  the drain already takes the whole queue in one pass.
- Landed #1982 (alloc_zeroed classify-once) as `fix(perf)` with explicitly
  NO speedup claimed, after discovering the iai suite's `alloc_zeroed_*`
  benches are a `black_box` stub under plain `production` (no
  `virgin-zero-skip`) — filed #1999 to close that gap rather than either
  skipping measurement silently or inventing a number.
- Personally caught and fixed two problems no agent (nor the original
  reviews) flagged: a stale `255`→`256` test-file-count doc-drift-guard
  failure surfaced only by adding a new test file (#1981's merge), and a
  tripwire-doc sentence in `sefer_alloc/mod.rs` that #1985's bitmask
  conversion made factually wrong (fixed as a same-session follow-up
  commit, `d30470b6`).
- Left the two long-standing unexplained detached worktrees and the
  foreign `src-review-xa` worktree/commit untouched — not this session's
  work, no evidence of harm, matches the standing
  don't-touch-other-agents'-work rule.

## Open questions

- None pending user input for the in-flight task work. Non-blocking:
  whether to ever investigate the two long-standing unexplained detached
  worktrees (`head-check`, `sefer-baseline-ae87141e`) remains open from a
  prior checkpoint, still not raised as an active question.

## Repo state

```
(clean — git status --short: no output, as of this checkpoint, before this file is added)
```
```
68fe92ef fix(perf): let alloc_zeroed reuse its own classification instead of re-deriving it in alloc
105d89e2 perf(opt-in): give drain_remote_free the lock-free fast path its comment claimed
a0d654f1 docs(review): add independent static src review xa   <- NOT mine, foreign session
d30470b6 docs(global): correct the tripwire soundness argument after #1985
338cbe37 perf(runtime): drive the large-cache best-fit and FIFO-oldest scans from the occupancy bitmask
```
(Note: `ec449cd6`-equivalent commit for #1987/#1990, "fix(perf): widen
trim_current_thread's gate; unify GlobalAlloc inline hints", landed on
`main` immediately before this checkpoint was written — `git log -1`
at write time is that commit, one ahead of the `68fe92ef` shown above.)

Worktrees present: `worktrees/head-check` (detached `ae87141e`, unexplained,
untouched), `worktrees/sefer-baseline-ae87141e` (detached `ae87141e`,
unexplained, untouched), `worktrees/src-review-xa-20260920` (detached,
foreign session's, untouched). No worktree of mine is currently open — each
task's worktree was removed immediately after its merge. `main`/`origin/main`:
not pushed this segment (the earlier CI-green push, `2fe99299`, was a
separate already-completed prior action in this same session, before the
review/task-filing/implementation work described above began).

Active babysit cron: job `872482b5`, recurring every 15 minutes
(`7,22,37,52 * * * *`), session-only (dies with this session, 7-day
auto-expire regardless). Ticks so far have all reported "still running
#<id>" — no stall recovery has been needed.
