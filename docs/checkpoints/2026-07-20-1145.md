# Checkpoint — 2026-07-20 11:45 [R8 done+pushed-pending; R9-1 miri fix in flight; two bg tasks running]

## Session summary

`sefer-alloc` (D:\dev\rust\sefer-alloc), 100%-Rust allocator. This session
executed the full R8 perf-review task queue (#214–#223, 10 tasks from an
external review of `ffd3215..f0dd9a9`) via sequential sub-agent delegation
(`/crush`, then a local `@sh` fallback after the `zai` provider's peak-hours
refusal) with personal zero-trust review before every commit — read every
diff, independently re-ran tests/clippy/fmt, and used counterfactual proofs
(temporarily reverting a fix to confirm its test goes red) for every
non-trivial change.

**All 10 R8 tasks are done and committed on local `main`** (`ffd3215..de4c4ae`):
#217 `c815927`, #214 `af7b039`, #215 `09237e0`, #216 `ec7ac34`, #218 `fa2c064`,
#219 `e9db718`, #222 `9afba66`, #221 `93dba14`, #223 `852828e`, #220 `de4c4ae`,
plus hygiene fixes (`180bb8a`, `f97cf1f`, `38f4108`, `ac110b6`). **Local `main`
is 5 commits ahead of `origin/main` — NOT YET PUSHED** (the user asked for
push + "наладь CI" but we got sidetracked into fixing a flaky test and then
into a fresh external review; push has not happened yet this window).

**A `/goal` Stop hook fight**: the user had set "реализуй оставшиеся задачи с
помощью /crush..." earlier; #220/#221/#223 were actually done via `@sh` (with
the user's own explicit real-time authorization each time — "переключись на
агентов @sh", "решай агентами @sh оставшиеся задачи", "поставь будильник на
12 часов и забудь про /crush"). The Stop hook kept re-firing every turn
because it checks the literal transcript for `/crush` usage and doesn't know
about mid-session user overrides. After ~8 identical re-fires (I could not
clear `/goal` myself — no tool for it — cron `e31e8c4c` for the 12:00
reminder WAS deletable and I deleted it per the user's "снимаем все кроны и
цели" request), the user ran `/goal clear` themselves, ending the loop.

**Then: `наладь CI` investigation.** Before pushing, `npm run check` kept
failing intermittently on `tests/regression_segment_table_tombstone_rebuild.rs`'s
`backshift_no_latency_spike_at_threshold_boundary` — a coarse wall-clock test
(single dealloc's `Instant::now()` max/median ratio) that is genuinely flaky
under this dev machine's current load (proven flaky even on a clean baseline
tree earlier in the session, and again just now: 1/6 to 6/6 pass rate across
different isolated runs, wildly different ratios 42×–607× with no code
change). Diagnosed as OS-scheduling noise (single dealloc occasionally stalls
multi-ms due to preemption/paging/AV), NOT a real regression — the test's own
doc comment already defers to `npm run iai` as "the deterministic signal".
**Fixed** by adding a bounded retry (3 attempts) around ONLY the timing
assertion (part b) — the correctness assertions (part a, membership) remain
unconditional every attempt, never retried-away. This is an UNCOMMITTED,
separate fix from the R9-1 work below (different concern, should be its own
commit).

**Then: a fresh external review (`/fh`) of HEAD `de4c4ae` + the last 15
commits arrived, mid-session, via the user pasting review text.** Its most
serious finding — verified TRUE by me, independently, before touching
anything — is a **P0 correctness bug in task #221's own zero-skip work**:

- `crates/vmem/src/lib.rs:1320`'s miri aperture (`reserve_aligned_raw` under
  `#[cfg(miri)]`) falls back to bare `std::alloc::alloc`, which does **NOT**
  zero — confirmed by reading `leak_zeroed_pages`'s own doc comment in the
  same file, which explicitly zeroes under miri for exactly this reason.
- But `alloc_large_slow` (commit `93dba14`, task #221) returned
  `is_fresh = true` **unconditionally**, including under miri — so a fresh
  Large `alloc_zeroed` under miri could skip the explicit `Node::zero` and
  return **uninitialized memory**, violating the `alloc_zeroed` contract.
- The review also correctly noted the original test
  (`tests/alloc_zeroed_fresh_large_skip.rs`) could not actually prove the
  OPTIMIZATION fired — byte-content checks alone can't distinguish "skipped
  because OS-zeroed" from "zeroed redundantly".

**Fixed, in the current uncommitted working tree** (this is what "R9-1" /
task #232 tracks): `alloc_large_slow` now returns `cfg!(not(miri))` instead
of unconditional `true`; doc comments on `alloc_large`/`alloc_large_slow`
corrected (removed the false "miri's zeroing guarantee" claim). Added a new
process-wide diagnostic counter `LARGE_ZERO_PASS_CALLS` (pattern-matched on
the existing `DECOMMIT_CALLS` counter) bumped at BOTH consumers of the
freshness signal (`AllocCore::alloc_zeroed` and `HeapCore::alloc_zeroed`),
exposed via `dbg_large_zero_pass_count()`. Rewrote
`tests/alloc_zeroed_fresh_large_skip.rs` to assert exact deltas: fresh→0
under `not(miri)`, fresh→1 under `miri`, cache-hit→exactly 1 always; added a
file-wide `TEST_LOCK` mutex (the counter is process-wide, tests in the same
binary would otherwise pollute each other). **Verified personally**: full
fmt/clippy (production + `--all-features`) clean; the rewritten test passes;
counterfactual #1 (temporarily made the AllocCore-face zero pass
unconditional) confirmed the new test goes red exactly as expected, then
reverted and re-confirmed green. **Counterfactual #2 (the actual miri run,
to independently confirm the `cfg!(miri)` branch behaves correctly under
real miri) is STILL RUNNING IN THE BACKGROUND** (task id `bgkg1rj7c`) — not
yet returned as of this checkpoint.

Also fixed, as directly-tied doc-drift the same review flagged: `Cargo.toml`
(`alloc-segment-directory`'s comment still said "NOT part of production",
stale since #216/R8-3); `docs/ARCHITECTURE.md` §6 (M6 decommit section still
described the pre-#223 decommit-on-pool-admission lifecycle, and had a
now-false "every alloc_zeroed zeroes explicitly" claim in the macOS
footnote); `tests/segment_directory_a5.rs` (a test doc comment described
pooled segments as "decommitted" — false since #223).

**A full `cargo test --release --features production` run is ALSO still
running in the background** (task id `bjtnjyw5f`) to give a final green
signal across BOTH uncommitted diffs (the tombstone-rebuild retry hardening
AND the R9-1 miri fix + doc fixes) before committing either. Neither has
been committed yet — the user's last message ("сделай коммит") arrived while
both background verifications were still in flight, then this `/checkpoint`
interrupted before I could act on it.

**Nine new Round-9 follow-up tasks were filed** (#224–#231, all `pending`,
none started) directly from the rest of the external review's findings
(medium-classes production-gating, the 1–2 MiB cliff, virgin zero-skip for
Small, class-aware dirty routing, lazy-commit RSS-vs-latency policy tradeoff,
directory self-heal drift cost, batch-bench follow-up, a fresh cross-version
benchmark to replace the stale R6 report). None of these are started; they
are queued for whenever `/crush` (or another sub-agent) is next dispatched.

## Active goal

None — the user ran `/goal clear` earlier this window (confirmed via the
`Goal cleared: ...` system output). No Stop hook is currently armed.

## TaskList

### in_progress
- #232 R9-1 P0: miri fresh-zero fix in `alloc_zeroed` (#221 follow-up) — code done and personally verified (fmt/clippy/counterfactual #1), miri counterfactual #2 still running in background (`bgkg1rj7c`), NOT YET COMMITTED

### pending
- #224 R9-2 P1-measure: fresh cross-version benchmark of current HEAD
- #225 R9-3 P1-measure: production gates for medium-classes before promotion
- #226 R9-4 P2: prototype 1.25/1.5/1.75 MiB classes (close the 1–2 MiB cliff)
- #227 R9-5 P2-design+measure: virgin zero-skip for Small + judge alloc_zeroed 4 KiB–1 MiB
- #228 R9-6 P2-measure: class-aware dirty routing judge for mixed remote fan-in
- #229 R9-7 P2-design: lazy-commit policy — latency-first pool vs low-rss preset
- #230 R9-8 P2: cheapen directory drift recovery — per-class miss-streak + rescue scan before OOM
- #231 R9-9 P3: batch-bench follow-up — sizes 8/16/32/64 + real SeferAlloc comparison

### recently completed (R8 queue, this session)
- #220 R8-7 (`de4c4ae`) — batch API ceiling measurement (GO 16B/64B, NO-GO 256B)
- #223 R8-10 (`852828e`) — removed decommit-on-pool-admission (Windows lazy-commit)
- #221 R8-8 (`93dba14`) — Large-path zero-skip for alloc_zeroed (⚠ had the miri bug R9-1 now fixes)
- #222 R8-9 (`9afba66`) — medium-classes judge-sweep verdict

## Decisions

- Split the two currently-uncommitted diffs into TWO separate commits (not
  one): (1) the tombstone-rebuild flaky-test hardening, unrelated in concern
  to (2) the R9-1 miri fix + its directly-tied doc-drift fixes. Not yet
  executed — was about to do this when `/checkpoint` interrupted.
- The tombstone-rebuild retry fix keeps correctness assertions (part a)
  unconditional on every attempt — only the noisy wall-clock ratio check
  (part b) gets retried, so the test's actual regression-detection power
  (catching a real per-delete O(HASH_CAPACITY) algorithmic regression, which
  would reproduce deterministically every attempt) is preserved.
- R9-1's freshness signal fix uses `cfg!(not(miri))` rather than restructuring
  the bool into an enum (the review's alternative suggestion) — minimal,
  surgical fix scoped to the actual bug, not a larger API redesign.
- Chose to independently verify the review's P0 claim by reading the actual
  vmem miri source before trusting it (confirmed true), rather than
  blindly implementing the review's suggested fix.

## Open questions

- **Miri counterfactual (`bgkg1rj7c`) and the full test suite
  (`bjtnjyw5f`) have not returned yet** — must be checked before either
  commit lands; if miri surfaces anything unexpected, the R9-1 fix needs
  another look before committing.
- **Push is still pending** — the user's "сделай пуш и наладь ci" request
  from earlier this window has not been completed; CI has not been checked
  at all yet (no push has happened to trigger it). Once both current
  diffs are committed and `npm run check` is clean, push + watch the
  GitHub Actions run is the next step.
- **Whether to start any R9-2..R9-9 task now or wait for explicit
  direction** — all nine are queued but unscoped-for-execution; given the
  session's `/crush`-vs-`@sh` back-and-forth, worth asking the user which
  channel to use before dispatching the next one.

## Repo state

```
 M Cargo.toml
 M docs/ARCHITECTURE.md
 M src/alloc_core/alloc_core.rs
 M src/alloc_core/alloc_core_core_diag.rs
 M src/alloc_core/alloc_core_large.rs
 M src/alloc_core/mod.rs
 M src/registry/heap_core_alloc.rs
 M tests/alloc_zeroed_fresh_large_skip.rs
 M tests/regression_segment_table_tombstone_rebuild.rs
 M tests/segment_directory_a5.rs
?? _gad_suite.log
?? ci_watch.log
?? ci_watch2.log
?? ci_watch_ffd3215.log
?? docs/checkpoints/2026-07-20-0801.md
?? docs/checkpoints/2026-07-20-0810.md
?? docs/perf/_raw_baseline_off.log
?? docs/perf/_raw_baseline_off_reduced.log
?? docs/perf/_raw_medium_on.log
?? docs/perf/_raw_medium_on_reduced.log
```

```
de4c4ae perf(bench): measure batch-alloc ceiling before designing a public API (R8-7)
852828e perf(alloc-lazy-commit): stop decommitting pooled small segments on admission (R8-10 Phase 1)
ac110b6 fix(test): correct misaligned_offset_guard's release-build truncation math
93dba14 perf(alloc-large): skip explicit zero for fresh OS reservations in alloc_zeroed (R8-8)
9afba66 docs(perf): medium-classes 256 KiB-1 MiB GO/NO-GO verdict (R8-9)
```

Local `main` is 5 commits ahead of `origin/main` (none of these 5 have been
pushed yet).
