# Checkpoint — 2026-07-14 14:00

## Session summary

This session covered two major waves of work on `sefer-alloc`, a Windows-dev-host Rust allocator project with an extremely rigorous zero-trust review discipline (CLAUDE.md).

**Wave 1 (round5 remediation, completed earlier this session):** 10 tasks (R5-F1..F4 fixes, R5-R1..R5 research/tooling benchmarks) plus two emergent findings: R5-R2 found a real, paired-A/B-statistically-significant (~14-29%) wall-clock regression in non-write churn between commit `e6b9b3a` and HEAD; R5-R2b then re-measured the SAME window via `npm run iai` (deterministic instruction count) and found the OPPOSITE — Ir dropped 20.6%. Per the user's explicit directive ("измерять iai, не wall-clock, машина шумная") and the project's own "IAI is the deterministic judge" rule, this was closed as an honest-reject of the "algorithmic regression" hypothesis — the wall-clock delta is almost certainly a platform artifact (IAI runs a Linux/WSL binary under Callgrind; the wall-clock numbers came from a native Windows build — two different compiled artifacts, not the same binary measured two ways). This mismatch is flagged as UNRESOLVED/unexplained, not swept under the rug. R5-R3 then fixed two real measurement confounds in `benches/global_alloc.rs` (cross-group TLS heap-state leakage + fixed SeferAlloc→mimalloc→System arm order), which made the canonical README/ALLOC_BENCH.md tables stale — refreshed them with explicit noise-framing (absolute numbers moved 20-40% across EVERY column including mimalloc/System, confirming host drift not code regression). All of round5 finished with `npm run check` fully green (fixed a genuine ~40-50% flaky test in the process — two new ShardedRegion regression tests needed a bounded-retry pattern instead of a single post-join assertion, due to apparent TLS-destructor-timing jitter under load on this Windows host).

**Wave 2 (round6, in progress now):** the user asked me to study a fresh, much deeper "radical optimization review" (`docs/agent_reviews_us/radical_optimization_review.md`) plus re-read the round5 `memory_safety_review.md` and `code_quality_review.md` (which I had NOT yet acted on). I personally spot-verified the two highest-stakes optimization claims against source (both confirmed: Windows commits the ENTIRE Registry+HeapOverflow inline array on first alloc, ~125 MiB, invisible to the existing RSS-only probe; and the remote-free retry storm does up to 8,193 failed attempts before trying the already-paid-for HeapOverflow secondary queue). The user then made three explicit decisions via AskUserQuestion: (1) make `dealloc`/`realloc` `unsafe fn` (not the "add checks everywhere" compromise) — with the added directive that every new unsafe boundary needs provocative stress-test coverage (Miri/Loom/TSan/Kani already exist; ASan + cargo-fuzz were missing, now added); (2) delete the rejected `alloc-runfreelist` feature entirely (same treatment as abandon/adopt in round4); (3) build the FULL Stage-A-judges + P0-source-changes optimization backlog now, not just a subset. I decomposed all of this into 21 tasks (#119-#139: 7 safety, 4 quality, 6 Stage-A measurement harnesses, 4 P0 source changes with `blockedBy` deps on their measurement harness), armed a 15-minute babysit cron (`5c4a6b68`), and started sequential `/crush` execution (zero-trust reviewed and committed after each).

**Completed so far this session (round6):** #120 (ASan+fuzz infra, commit `ac17366`), #119 (dealloc/realloc → unsafe fn, the largest task — 70 files, commit `33d9a2b`, CHANGELOG entry I wrote myself since the agent skipped it, three scratch artifacts the agent left in-tree removed before commit), #121 (flush_class + BinTable class_idx release-bounds → unsafe fn, commit `7b7530f`, agent found and fixed a call site R6-MS-1/2 had missed in `benches/`). **Currently in flight:** #122 (R6-MS-4, gating `dbg_push_to_ring` behind unsafe — a `#[doc(hidden)]` safe hook that lets fully-safe code construct a stale-note→double-issue chain), running via `/crush` session `r6-ms4`, actively editing (multiedit/edit/bash cycles observed across several babysit ticks), not yet complete as of this checkpoint.

**Crush peak-hours note:** the `zai` provider enforces a peak-hours window (08:00-12:00) during which `crush run` immediately fails with "provider zai is in peak hours, refusing until 12:00" — per the user's standing instruction this session, the fallback on that specific failure is `@sh` (Agent tool, Sonnet-top-high), NOT the crush skill's default `@ao46l`/`@ash` fallback. This was invoked twice during round5 (R5-R2, R5-R3, R5-R4, R5-R5 all needed it) before the window passed; round6 so far has NOT hit it (crush has been running normally).

## Active goal

None (`/goal` Stop hook not armed). Progress is tracked entirely via the TaskList + the babysit cron per the `/babygoal` contract the user invoked for round6 ("Давай реализуем таски, используй агентов /crush, после выполнения всех таско сделай ревью с помощью агента @fl. Между тасками делай коммиты. Желательно иметь регрессионные тесты").

## TaskList

### in_progress
- #122 R6-MS-4: gate ring test hooks (dbg_push_to_ring/dbg_drain_all_rings) behind unsafe/hardening (no blockedBy)

### pending
- #123 R6-MS-5: make magic_at an atomic load (close atomic-store/plain-read data race)
- #124 R6-CQ-2: make dbg_stamp_kind_byte/dbg_stamp_segment_id unsafe fn (safe metadata-corruption hooks)
- #125 R6-CQ-3: fix config-conflict debug_assert! slot leak on unwind + honest instance model
- #126 R6-CQ-4: remove rejected alloc-runfreelist feature + code + tests; slim pinning off legacy bundle
- #127 R6-CQ-5: remove future-only dead scaffolding (unused constructors, unreachable variants)
- #128 R6-CQ-6: doc sync — purge stale abandon/adopt architecture text
- #129 R6-CQ-7: continue thematic file split of the 3 remaining monoliths
- #130 R6-OPT-A1: add Windows PrivateUsage/PagefileUsage to first-alloc probe
- #131 R6-OPT-A2: persistent-thread fan-in harness (setup outside timer, barriers around dealloc burst)
- #132 R6-OPT-A3: medium-size sweep harness around the SMALL_MAX cliff
- #133 R6-OPT-A4: multi-segment S=1..1024 directory harness (with holes, 49 classes, remote dirty density)
- #134 R6-OPT-A5: dealloc-only unbound-thread benchmark (process-per-sample, real global allocator)
- #135 R6-OPT-A6: three separate #[global_allocator] executables + process-level A/B/B/A runner
- #136 R6-OPT-P0-4: overflow-first remote free + uncounted probes (blockedBy: #131)
- #137 R6-OPT-P0-1: dealloc-without-heap-bind route for unbound/TORN threads (blockedBy: #134; #119 already resolved)
- #138 R6-OPT-P0-2: chunked Registry + lazy HeapOverflow sidecar (blockedBy: #130)
- #139 R6-OPT-P0-3: medium allocator for 256 KiB–2 MiB (blockedBy: #132)

### recently completed
- #121 R6-MS-3: flush_class + BinTable release-bounds → unsafe fn (commit 7b7530f)
- #119 R6-MS-1/2: AllocCore/HeapCore dealloc+realloc → unsafe fn (commit 33d9a2b)
- #120 R6-INFRA-1: ASan + cargo-fuzz wiring (commit ac17366)
- (round5, all 10 tasks + 2 emergent, commits `34c1cc9`..`958eaa4`, see prior checkpoint `2026-07-14-0100.md` for full round5 detail)

## Decisions

- **dealloc/realloc → `unsafe fn`, not defensive-checks-everywhere** — user's explicit choice among 3 options presented; the 5th time this exact M2 question was raised across rounds 2-5, this time with concrete safe-Rust counterexamples (block resurrection via realloc, overlapping copy_nonoverlapping UB) that finally tipped the decision.
- **Every new unsafe boundary needs provocative stress tests: Miri/Loom/TSan/Kani (existing) + ASan/fuzz (added this session)** — user's explicit follow-up directive, drove task #120 as a prerequisite blocking #119.
- **Delete `alloc-runfreelist` entirely (task #126), same treatment as abandon/adopt in round4** — user's explicit choice over "leave in matrix with a removal-date TODO".
- **Build the FULL Stage-A-judges + P0-source optimization backlog now (21 tasks), not a reduced subset** — user's explicit choice over "Stage A + only the best-evidenced P0-4" or "safety/quality only for now".
- **IAI is the decisive tie-breaker over single-run wall-clock, but the underlying Windows-vs-Linux/WSL platform mismatch remains an open, unexplained question** (R5-R2b) — not re-litigated this session, carried forward as background context for round6's P0-2 (which needs the Windows commit-charge probe, R6-OPT-A1, precisely because RSS-only measurement already proved blind once).
- **CHANGELOG breaking-change entries are the orchestrator's responsibility to verify/write, not assumed from the agent** — R6-MS-1/2's implementing agent skipped the CHANGELOG entry despite it being explicitly requested; caught during zero-trust review, written by the orchestrator directly before commit.

## Open questions

- None from the user directly. The one standing unresolved technical question (not requiring user input, just future investigation) is R5-R2b's platform-mismatch hypothesis (Linux/WSL IAI vs native Windows wall-clock) — explicitly out of scope for round6, noted for whoever picks up Windows-native profiling someday.
- Whether `dbg_drain_all_rings` (the consumer side of the ring seam #122 is currently fixing the producer side of) also needs to become `unsafe fn`, or whether making only `dbg_push_to_ring` unsafe closes the soundness gap — left to #122's implementing agent's judgment per the task prompt; not yet resolved as of this checkpoint (task still in flight).

## Repo state

```
 M benches/global_alloc.rs
 M benches/heap_xthread.rs
 M src/alloc_core/alloc_core_small_reclaim.rs
 M src/registry/heap_core_diag.rs
 M tests/decommit_stale_ring.rs
 M tests/phase13_drain_reclaim_layout_class.rs
 M tests/reclaim_offset_unit.rs
 M tests/regression_batch_flush.rs
 M tests/regression_c3_unbounded_recycle.rs
 M tests/regression_gen_table_lifecycle_seams.rs
 M tests/regression_reclaim_offset_garbled_class.rs
 M tests/regression_refill_window_double_issue.rs
 M tests/regression_ring_drain_guard_miri.rs
 M tests/regression_run_stack_drain.rs
 M tests/regression_run_stack_flush.rs
 M tests/regression_xthread_double_free_residual.rs
 M tests/small_segment_pool.rs
?? docs/agent_reviews_round5/
?? docs/agent_reviews_us/
?? docs/checkpoints/2026-07-14-0100.md
?? tests/regression_push_to_ring_unsafe_boundary.rs
```

(The above working-tree diff is task #122's IN-FLIGHT, uncommitted work — the crush agent is actively editing; do not treat it as stable until the next commit lands. `docs/agent_reviews_round5/`, `docs/agent_reviews_us/`, and the prior checkpoint remain untracked by design — never auto-added to git per this project's convention.)

```
7b7530f fix(alloc-core)!: R6-MS-3 -- flush_class unsafe fn + release-mode class_idx bounds
33d9a2b fix(alloc-core,registry)!: R6-MS-1/2 -- dealloc/realloc narrowed to unsafe fn
ac17366 test(infra): R6-INFRA-1 -- wire ASan + cargo-fuzz for the unsafe boundary
958eaa4 docs: refresh benchmark tables to 2026-07-14 post-round5 (R5-R3 methodology fix)
e5afe60 fix(test): sharded_remote regression tests -- retry instead of single check
```

Babysit cron `5c4a6b68` armed (every 15 min, off-minutes 7/22/37/52), session-only (not durable — dies if this Claude session ends; re-arm with `/babysit` if resuming in a fresh session and work remains). `main` is far ahead of `origin/main` (all of round5 + round6-so-far, unpushed — no push requested this session, consistent with CLAUDE.md's standing rule).
