# Checkpoint — 2026-08-13 01:30 [aligned-vmem-round4-plus-misc]

## Session summary

This session continued the multi-round `aligned-vmem` review-and-fix campaign and handled several unrelated small requests before and after it. Chronologically:

1. **Resumed from a prior checkpoint** (`2026-08-12-1752.md`) covering rounds 1-3 of the `aligned-vmem` campaign, all merged to local `main` but not yet pushed.
2. **Pushed the 14 round-3 commits** to `origin/main` on explicit request, confirmed CI green on the landing SHA (`3f915d4`), with the one pre-existing/expected exception (`sefer-region` "already published" guard).
3. **User asked to fix the `sefer-region` guard's own CI unfriendliness** (it correctly detects `sefer-region` 0.2.0 is already on crates.io, but hard-fails every ordinary post-release push). Asked the user to choose between bumping the version vs. fixing the CI job's logic — user chose "fix the CI job." Downgraded the guard from `::error::` + `exit 1` to a `::warning::` annotation (`.github/workflows/ci.yml`), reasoning that `cargo publish` itself is the real backstop against re-publishing a taken version. Committed (`8804fc9`, after user asked to also fold in the still-uncommitted checkpoint file from the prior session) and pushed; confirmed CI green on that landing SHA too (all 37 jobs green, only the guard now shows a warning instead of red).
4. **sccache side-quest**: user asked whether sccache is in use (yes, machine-level config at `D:\dev\rust\.cargo\config.toml`, not repo-level — nothing to change); then asked about cache hit rate (found 0%, and discovered 3 duplicate `sccache.exe` server processes fragmenting stats — user declined to consolidate them); then asked to set the cache size to 500GB (discovered the registry env var `SCCACHE_CACHE_SIZE` was *already* set to `500G` persistently, but the running server(s) predated that and were still using the old `30G` limit — killed all `sccache.exe` processes and started a fresh one, confirmed `Max cache size: 500 GiB`).
5. **User asked which crate was last reviewed** (`aligned-vmem`, 3 rounds done) **then asked for "another round like that"** — round 4. Launched a read-only `@oh` review agent scoped explicitly to avoid re-litigating rounds 1-3 findings and to specifically check whether round 3's own fixes (the Windows single-call fast-path change, task #848) had left new gaps. It found **R1-R13** (13 findings), the standout being **R1 (HIGH)**: `--all-features` in CI actually runs the `mock` feature (a non-additive backend-replacing feature), so both platform-coverage CI rows added in rounds 2/3 were silently testing a recording stub, not the real OS backend — proven by an executed counterfactual on this Windows host.
6. **Decomposed R1-R13 into 8 tasks (A-H, #867-874)** via `/babygoal`, armed `/babysit` (cron `9e11dc0f`, 15 min), created 8 isolated git worktrees, launched 8 parallel `/crush --role smart` sessions with self-contained Russian-language prompts including explicit fabrication warnings (referencing the round-2 fabrication incident, commit `7663811`).
7. **Personally zero-trust reviewed and merged all 8 tasks** into `main` (`--no-ff` merges), each independently re-verified (test/clippy/fmt/doc, and for task A specifically re-running the new CI feature set locally to confirm `fault_injection.rs` actually goes from 0→5 tests). One real bug found and fixed personally during review: the new `scripts/vmem-doc-drift-guard.mjs` (task E's own deliverable) had a substring false-negative — `/conditional/` matched inside `"unconditionally"`, meaning the guard could never catch the exact historical drift phrasing it existed to catch. Fixed with a `\bconditional\b` word-boundary anchor, verified via injected counterfactual.
8. **Deleted all 8 worktrees/branches, disarmed the babysit cron** (since other unrelated pending tasks in the TaskList would otherwise have caused it to wander into unrequested work).
9. **Launched the final `@oh` closing review** (per the user's original instruction to run one at the end). It found the round-4 remediation itself had introduced new residue — most notably **CR1 (MEDIUM-HIGH)**: task A's CI fix had *deleted* the pre-existing `--all-features` step on both platform jobs instead of supplementing it (despite R1's own instructions saying "replace, OR supplement... keep the --all-features row too"), leaving dangling comments describing a step that no longer existed, and un-covering round-3's own F1 fix (`tests/mock.rs`'s page-size assertion) on the exact macOS/16-KiB-page runner F1 was written for. Also found **CR2 (MEDIUM, left open)**: my own word-boundary fix to the drift guard was correct as far as it went, but the guard's qualifier is an OR (`align|conditional|Windows`) and every real historical drift sentence contains "align" or "Windows" too — so the guard passes clean on both real historical drift sentences reinjected as counterfactuals. Plus CR3-CR10 (mostly small residue: reverted docs, mis-citations, one phantom `VirtualFree` in the new design note, a wrong task-number comment, and the round's own CHANGELOG entry being missing at review time).
10. **Personally fixed CR1, CR3, CR4, CR5, CR6, CR7, CR8** directly on `main` (small, well-understood mechanical fixes — restored the two deleted CI steps, corrected reverted/half-applied doc text in 3 files, fixed 2 mis-citations, fixed 1 wrong comment). **Left CR2 and CR9 explicitly open** (both need more than a one-clause fix; documented as open in both the CHANGELOG entry and the closing review itself, not silently dropped). Wrote the round-4 CHANGELOG entry (folding in an honest account of the CR-series corrections) and the `docs/CORRECTNESS_OPEN_ITEMS.md` `[A]` card update, committed both round-4 review documents (previously untracked) — all as one commit, `7c6e4be`. Full verification matrix (test ×2 feature sets, clippy ×2 rows, fmt, doc ×2 crates, YAML parse + step enumeration, drift-guard run) all green after the fix.

**Not yet done**: none of this session's work (rounds 3's push aside) has been pushed again — `origin/main` is still at whatever SHA it was after step 3 above (`8804fc9`), and the entire round-4 campaign (16 merge/fix commits + 1 closing-review-fix commit = local `main` @ `7c6e4be`) is **local only, never run in real CI**. This is explicitly flagged as the standing open item in both the CHANGELOG entry and the closing review's own verdict.

## Active goal

None — no `/goal` Stop hook is armed in this session.

## TaskList

### in_progress
- #657 numa-shim — verify/prepare for crates.io republish (blockedBy: #658; untouched this session, pre-existing thread)
- #658 aligned-vmem — publish 0.2.0 (local already bumped, crates.io still shows 0.1.0) (untouched this session as a task, but round-4's R5 finding — the ReservationParts Clone footgun — was explicitly framed as and is now closed as a publish gate for this)
- #659 racy-ptr-cell — first publish to crates.io (untouched)
- #660 size-classes — first publish to crates.io (untouched)
- #661 tagged-index-stack — first publish to crates.io (untouched)

### pending
- #662 Root sefer-alloc: design note for applying bench-scale-tool alongside criterion/iai (untouched)
- #763 Root sefer-alloc: implement bench-scale-tool per the approved design (blockedBy: #662; untouched)

### recently completed (this session, most recent first)
- (closing-review fix pass, no task ID — direct personal fix, not tracked as a numbered task) CR1/CR3/CR4/CR5/CR6/CR7/CR8 fixes + round-4 CHANGELOG entry + committed both round-4 review docs (commit `7c6e4be`)
- #867-874 aligned-vmem round-4 A-H: R1-R13 remediation (all 8 completed and merged)
- #866 Verify CI green on landing SHA 8804fc9 (ci.yml guard fix)
- #865 Verify CI green on landing SHA 3f915d4 after round-3 push
- (all of rounds 1-3, tasks #842-864, were already completed before this session's active window)

## Decisions

- **Downgraded `sefer-region`'s "already published" CI guard from a hard error to a `::warning::`** rather than bumping the version, per the user's explicit choice between the two options offered — the real backstop (crates.io itself rejecting a re-publish) still exists independently.
- **Left the 3 duplicate `sccache.exe` processes un-consolidated** per the user's explicit "no" — only the cache-size limit was worth fixing (the registry env var was already correct at 500G; the running servers just hadn't picked it up, fixed by a clean restart).
- **For round 4, restored the deleted `--all-features` CI steps rather than leaving CR1's "supplement not replace" violation as a mere note** — this was a real, executable coverage regression (round-3's own F1 fix losing its only discriminating CI runner), not a cosmetic finding, so it was fixed immediately rather than just flagged for a hypothetical round 5.
- **Left CR2 (the drift guard's OR-qualifier weakness) and CR9 (a pre-existing, not-this-round `mock`-deferral text with a false premise) explicitly open** rather than rushing a fix — both would need a more substantial rewrite (a per-sentence positional guard rewrite; a maintainer decision on crates.io's actual published feature set) than the mechanical one-clause fixes applied to the other CR findings, and doing either hastily risked introducing a *third*-order version of the same "the fix creates the next finding" pattern this whole round-4/closing-review pair was about.
- **Wrote round 4's own CHANGELOG entry in the same pass that closed the round**, rather than deferring it — this is the fourth instance of the exact "missing CHANGELOG entry" pattern the campaign has now hit three times uncaught (rounds 1→2→3), and this time it was caught and closed within the round itself via the closing review (CR10), which is itself now recorded as a partial win for the pattern in the `docs/CORRECTNESS_OPEN_ITEMS.md` card.

## Open questions

- **Push local `main` (`7c6e4be`, 17 commits ahead of `origin/main`'s `8804fc9`) to origin, and confirm CI green on the landing SHA?** This is the explicit standing item both the CHANGELOG entry and the closing review's own verdict call out — none of round 4's work (or the closing-review fix pass) has run in real CI yet, only locally on this one Windows host.
- **Should CR2 (the drift-guard's real weakness) get a proper per-sentence rewrite in a future round?** Left open on purpose; not urgent (the guard is at worst a false sense of security, not a regression by itself), but flagged in both the CHANGELOG and `docs/perf`/review docs so a future round doesn't have to rediscover it.
- **Should CR9 (the `mock`-as-Cargo-feature deferral text's false "not yet published" premise) be resolved by re-stating the deferral honestly, or by actually taking the `--cfg`-flag conversion in the 0.2.0 window?** A maintainer decision, not something to guess at.
- **Is `aligned-vmem` now actually ready for its 0.2.0 `cargo publish`?** The round-4 closing review's own explicit verdict: source-level yes (4 rounds, no soundness hole found); the one round-4-identified hard publish gate (R5, the `ReservationParts: Clone` footgun) is closed; CR9 is a separate, older, still-open publish-relevant item. Publish itself remains explicitly gated on an explicit maintainer go-ahead per this project's standing convention — not requested this session.

## Repo state

```
(clean — git status --short produced no output)
```
```
7c6e4be fix(vmem), docs: round-4 closing review — fix CR1/CR3/CR4/CR5/CR6/CR7/CR8, commit both round-4 review docs
a59569d Merge vmem-r4c (task #869): remove Clone from ReservationParts (R5)
36058de fix(vmem): task #869 remove Clone from ReservationParts before the 0.2.0 publish (R5)
53b2cc0 Merge vmem-r4e (task #871): fix 5th over-reserve doc drift + stale huge-page justification + add drift guard (R6/R7)
1519e0c docs: task #871 fix the 5th over-reserve doc drift + stale huge-page justification + add a drift guard (R6/R7)
```

Local `main` (`7c6e4be`) is **17 commits ahead** of `origin/main` (last confirmed at `8804fc9`) — none of round 4's work has been pushed yet.
