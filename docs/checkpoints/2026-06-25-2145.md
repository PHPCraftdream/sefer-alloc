# Checkpoint — 2026-06-25 21:45 [Phase 12.6 race fix: Variant 2 ring built, ABA-vs-drain-bug to disentangle]

## Session summary

`sefer-alloc` — safe handle-store → real fast safe general-purpose allocator
(mimalloc-class). This arc drives Phases 12 (production MT trust) + 13 (speed) of
`docs/MALLOC_PLAN_PHASE12-13.md` via **crush sub-agents (serialized — two
concurrent runs clobber the shared cargo build)**, with **zero-trust review before
every commit** (re-run tests/loom/clippy MYSELF; claim ≠ receipt).

**Committed to main:** Phases 12.1–12.5 (`abe5610` keystone: heap-as-shard →
SOUND multithreaded `#[global_allocator]`, MT gate green ×3) + 13.1 (`143b8be`:
O(1) SIZE2CLASS lookup, non-vacuous test caught a real off-by-one).

**The whole current focus = #33/#36: eliminate the cross-thread-free reclaim
race.** Phase 12.5 SHIPS a sound *workaround* (`drain_thread_free` DISCARDS
cross-thread-freed blocks = bounded RSS leak) because the naive reclaim
SEGFAULTS. The user insisted "fix the shards, don't paper over bugs." A dedicated
instrumented root-cause run PROVED the cause (`ea3a4ba`, `tests/race_repro.rs` =
committed deterministic gate, `RACE_DRAIN_RECLAIM.md` §8): **the intrusive TFS
word at the slot-reuse handoff** (suspect #2 confirmed; fallback #1 and routing #3
refuted). Counterfactual: reclaim → 3/3 STATUS_ACCESS_VIOLATION, discard → 3/3 OK.

**Variant 2 built (uncommitted, in working tree):** `/oxx` contemplation chose a
**non-intrusive per-segment bounded MPSC ring of u32 offsets** (`RemoteFreeRing`,
self-hosted in segment metadata after BinTable, RING_CAP=256, Vyukov-style
CAS-reserve) — cross-thread freer pushes the block's OFFSET (never touches the
block word), owner drains ring→BinTable. Restores the ShardedRegion 7b discipline
(queues carry offsets, never poison the object). Files: NEW
`src/alloc_core/remote_free_ring.rs`; modified `node.rs` (+atomic_u32_at/write_u32),
`segment_header.rs` (Layout::remote_ring_off + SegmentMeta::remote_ring),
`bootstrap.rs`/`alloc_core.rs` (init + reserve), `mod.rs`, `registry/heap_core.rs`
(cross-thread routing pushes offset). The original §8 intrusive-word race is
eliminated (proven by isolation).

**The new wall (task #36, the live cliffhanger):** under MT stress the ring
reclaim crashes — `pop_free` panics `attempt to subtract with overflow` at
`alloc_core.rs:516` (a free-list node's `next` points OUTSIDE its segment). crush
debugged it to "the same offset (e.g. 16384) reclaimed 4 times" and proposed it is
**ABA-by-address from slot-recycle** (whole-heap-reuse aliases a block address
across owners; the offset can't tell "old dead X" from "new live X at same
address"; ShardedRegion had no recycle so no ABA), and asked sanction for a
**generation-tag hybrid** (ring slot → u64 = offset|generation, gen from
owner_state, bumped on recycle; drain accepts only if gen matches).

**My standing hypothesis (NOT yet proven):** the ABA diagnosis is conflated with
an UNFIXED drain bug — `remote_free_ring.rs:286` is still `while h < t` (cursors
wrap → should be `while h != t`). I could not construct the ABA from correct app
behaviour (the ring consumes X's offset BEFORE the owner can reuse X, since X
reaches the BinTable only via drain). So "4 reclaims" may be the drain
re-processing slots, OR normal address-reuse-over-time, NOT a fundamental ABA.

**Decided next step (the /oxx verdict — "truth before beauty"):** DISENTANGLE
first, don't add machinery to an undiagnosed bug. Direct crush to: (1) fix
`while h != t`; (2) build an ISOLATED `RemoteFreeRing` unit test + **loom** (N
producers + 1 consumer over a plain buffer, no allocator) asserting every offset
reclaimed exactly once — the ring must be correct ALONE; (3) add a bounded
fail-fast **watchdog** to MT tests so a deadlock fails fast instead of hanging
(the recurring "long tests" pain — a hung race_repro blocked crush ~14–35 min
twice); (4) re-enable reclaim and report whether double-reclaim SURVIVES isolation
(→ ABA real → generation-tag is the correct standard fix) or VANISHES (→ it was
the drain bug → clean ring, no generation needed). I had just marked #36
in_progress + owner crush and was about to launch this disentangle run when
/checkpoint fired — **the crush run is NOT yet launched.**

Recurring theme: every hard Phase-12 bug (#7 abandon/reuse landmine, §8 race, now
ABA) traces to **whole-heap-reuse on slot recycle** — the one deviation from
ShardedRegion (which released the token but never aliased shard contents).

## Active goal
`/goal доведи кампанию до победы, друг` — IN FORCE (Stop hook fires each turn-end).
NOT met: #33/#36 race fix unfinished; Phase 13 #27–#31 + heavy gate #32 untouched.

## TaskList
### in_progress
- #36 Defeat ring-reclaim crash: subtract-overflow in pop_free (next outside segment) — disentangle ABA vs drain bug  (owner crush; run NOT yet launched)
- #33 Phase 12.6: eliminate cross-thread-free reclaim race — non-intrusive per-segment ring (Variant 2)  (blockedBy: #36)
### pending
- #35 M11 epoch-guard for M6 decommit-safety + M6 decommit + soak  (blockedBy: #33)
- #27 Phase 13.3 inlined arithmetic own-thread free
- #28 Phase 13.4 two-list free
- #29 Phase 13.5 refill batch tuning
- #30 Phase 13.6 heap==core pinning
- #31 Phase 13.7 macro-bench + refresh MALLOC_BENCH
- #32 Heavy gate: fuzz + aarch64 + TSan
### recently completed
- #21 12.1 · #22 12.2 · #23 12.3 · #24 12.4 · #25 12.5 (sound MT) · #26 13.1 (O(1) lookup)
### deleted
- 1 (#34, re-created as #35)

## Decisions
- **Truth before beauty (the /oxx verdict).** Do NOT sanction the generation-tag
  hybrid yet — disentangle the ABA from the unfixed `while h<t` drain bug first
  (isolated loom-clean ring test). The perfect solution is revealed by isolation,
  not chosen blind. generation-tag is the *correct standard* ABA fix IF ABA is
  real (not "masking" as I first framed) — but only if isolation proves ABA.
- **Variant 2 (non-intrusive ring) over Variant 1 (generation-tag) as the base.**
  Ring removes the contended word (root of §8); generation may sit ON TOP only if
  ABA is confirmed.
- **Re-materialize-on-claim REJECTED** as a standalone fix — late cross-thread
  frees still arrive post-recycle, so it trades ABA for a leak/adoption problem.
- **Serialize crush runs; kill hung tests by PID.** Two concurrent runs clobber
  the cargo target; a hung race_repro blocks crush for tens of minutes.
- **Discard-leak stays shipped (sound) until the fix lands.** Never re-attempt the
  naive intrusive-drain restore (segfaults, proven — FINDINGS §8).

## Open questions
- Does double-reclaim SURVIVE an isolated loom-clean ring? (→ ABA real →
  generation-tag) or VANISH? (→ drain bug → clean ring). THE pivotal unknown.
- If ABA is real, is whole-heap-reuse-on-recycle the deeper thing to revisit
  (the recurring theme), vs a local generation-tag?
- After #36/#33: #35 (M11 decommit, shares the generation mechanism), then Phase
  13 #27–#31, then heavy gate #32.

## Repo state
```
 M src/alloc_core/alloc_core.rs      (Variant 2 ring wiring; reclaim currently DISABLED=discard)
 M src/alloc_core/bootstrap.rs       (ring init)
 M src/alloc_core/mod.rs             (mod remote_free_ring)
 M src/alloc_core/node.rs            (+atomic_u32_at, +write_u32)
 M src/alloc_core/segment_header.rs  (Layout::remote_ring_off, SegmentMeta::remote_ring)
 M src/registry/heap_core.rs         (cross-thread routing pushes OFFSET to ring)
?? src/alloc_core/remote_free_ring.rs (NEW — RemoteFreeRing MPSC ring; drain still `while h<t`, UNFIXED)
?? docs/checkpoints/2026-06-25-2030.md
```
(uncommitted = Variant 2 WIP; compiles all configs; tests green EXCEPT the
reclaim path which is currently discard-stubbed)
```
ea3a4ba test+docs: race_repro.rs (gate) + confirmed root cause — intrusive TFS word at slot-reuse handoff
143b8be feat(phase13.1): O(1) size-class lookup — const SIZE2CLASS
25b78cb docs(race): §7 root-cause-first
86e4860 docs: RACE_DRAIN_RECLAIM.md
4f40067 docs(findings): #8 naive restore SEGFAULTS
```
Whole Phase 12–13 arc is LOCAL, NOT pushed (origin main last at `996723b`/Phase 11).
