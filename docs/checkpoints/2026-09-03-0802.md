# Checkpoint — 2026-09-03 08:02

## Session summary
Continuation of the `tagged-index-stack` publish-readiness campaign. Previous checkpoint (`2026-09-02-1737.md`) left run-7's wave 3-of-4 groups landed, with GroupC blocked on a genuine rush weekly/monthly quota exhaustion (reset stated as 2026-09-03 17:01:11) and the user asked to choose: wait / do it myself / switch agent.

Sequence this session: (1) user chose to switch agents — "продолжи работу с помощью агентов @sx" — I finished run-7's GroupC directly via a fresh `@sh` agent (not rush) working in the already-existing `worktrees/tis-sol7-groupc` worktree that held real uncommitted progress; zero-trust reviewed and merged as `f35e4fb`; ran `npm run check`, pushed, confirmed CI+Kani green (landing SHA `dcb742c`, which itself included an externally-authored "Sol-codex run-8" review doc that had landed directly on `main` in parallel — unrelated, no conflict). (2) User asked to read that run-8 review and file grouped tasks — filed `#1839`-`#1842` (GroupA-D) + later `#1843` (T-push), with `blockedBy` chains reflecting real file-overlap dependencies (GroupB/D depend on GroupA's flagship landing first). (3) `/babygoal ... @sx & worktrees` — executed the whole run-8 wave:
- **GroupA (#1839, P1-1 flagship, BLOCKING)**: the review found a genuine soundness gap — a fully contract-compliant push/pop sequence could wrap the tag counter back to a stale observer's exact snapshot, letting a stale CAS double-issue an index. Consulted `@fh` for a second opinion (same pattern as run-6/run-7's flagship findings); `@fh` recommended Option 2 — a strictly monotonic tag with a stateless seal at `TaggedIndex::TAG_MAX` (`push_index`/`ArrayIndexStack::push` now return `Result<(), TagExhausted>` instead of `()`) — over Option 1 (hazard/epoch reclamation, rejected as wrong fit for a `no_std`/allocation-free/const-generic crate) and Option 3 (probabilistic downgrade, rejected as an undischargeable caller obligation). Implemented via `@sx` in an isolated worktree; landed as `eba76e4` after thorough zero-trust review including a personal counterfactual (temporarily neutered the seal check, confirmed `tag_seal.rs` fails without the fix) and independently re-running the full `loom_aba.rs` suite (13/13 green, including a `#[should_panic]` counterfactual that reproduces a genuine duplicate-index `[0, 1, 1]` corruption when the seal is bypassed — proof the fix is load-bearing).
- **GroupC (#1841, P2-2, independent)**: A/B wall-clock harness template hadn't been migrated to the unsafe `push` API from an earlier round; fixed + added a regular-CI static build-check gate so this class of break can't hide behind the workflow_dispatch-only measurement job again. Landed as `fdc1650`.
- **GroupB (#1840, P2-1+P3-3, blockedBy GroupA)**: closed 3 implicit-unsafe-call sites hidden by `unsafe fn`'s edition-2021 ambient permission (`#![deny(unsafe_op_in_unsafe_fn)]` added) + fixed 3 stale/overclaim doc items. **Discovered mid-merge**: this agent's worktree (via `isolation: "worktree"`) had snapshotted `main` from BEFORE GroupA and GroupC landed (not live-current-main at launch time — confirmed this is apparently how the harness's worktree isolation behaves this session), requiring manual `git apply --3way` + hand conflict resolution per overlapping file. **Also discovered during this merge's own verification** (not GroupB's fault): the root README's tier-2 unsafe-site count had drifted stale (81→84) since GroupC's merge added 3 new sites in `harness_bin.rs` — a gap in my OWN zero-trust review of GroupC's merge, caught by actually running `no_stale_doc_references` this time and fixed in the same commit (`f3a2ff6`). Lesson applied going forward: for GroupD, I manually created the worktree from live current `main` myself (`git worktree add`) instead of relying on `isolation: "worktree"`, avoiding the same staleness class entirely.
- **GroupD (#1842, P3-1+P3-2+P3-4, blockedBy GroupA+GroupB, do LAST)**: reframed "compiler-checked" → "compiler-enforced" language; fixed a stale "clause 1 covers shape 4 only in spirit" hedge that had crept back after an earlier compression pass (P3-2); further compressed `StackStorage`'s trait doc 466→333 lines with a full relocated-fact ledger in a new ADR addendum — personally verified the ADR's central claim (that trimmed worked examples already lived in `tests/custom_storage_impl.rs`'s pinning-test doc comments) by reading those comments directly at the pre-change commit. Landed as `ccd160e`.
- **T-push (#1843)**: ran `npm run check` — first attempt failed with all 16 `no_stale_doc_references` tests hitting `NotFound` `io::Error`s, self-diagnosed by the check script itself as a stale-cross-checkout-cargo-artifact (task #1073's known class — shared `CARGO_TARGET_DIR` replaying a binary compiled against a now-deleted worktree's baked-in `CARGO_MANIFEST_DIR` path); fixed via `cargo clean -p sefer-alloc` + re-run, which then passed clean. Pushed (`ccd160e`), but full CI then genuinely failed on a REAL bug: `cargo clippy -p tagged-index-stack --all-targets -- -D warnings` (a dedicated CI row with NO features enabled — never checked by me or by any sub-agent during the whole round, which only ever verified with `--features test-internals`) — `tests/tag_seal.rs` imported `ArrayIndexStack`/`TagExhausted` unconditionally but only used them inside `#[cfg(any(feature = "test-internals", loom))]`-gated functions. Fixed by gating the import too; verified all three of the crate's dedicated CI clippy rows (no-features, `test-internals`, `loom`) directly this time; committed (`ec966d0`) and pushed; confirmed CI+Kani both green on that landing SHA.

Once the whole run-8 wave was confirmed green, the babysit cron (`bbf7d077`, armed earlier this session) kept firing identically ~16 times in a row reporting "blocked — #661 (crates.io publish) irreversible, held for explicit user go-ahead" — the only remaining TaskList item is `#661`, deliberately never auto-started per this session's standing irreversibility policy. Judged continuing to fire the cron for up to 7 days with zero new information each time as wasteful, so I stopped it myself (`CronDelete`) and reported that decision to the user in one line, rather than letting it loop silently.

## Active goal
None — no `/goal` Stop hook is in force this session.

## TaskList
### pending
- #661 tagged-index-stack — first publish to crates.io (blockedBy: none — technically unblocked, held pending explicit user request, irreversible)

### recently completed
- #1843 tis-sol8-T-push: merge all run-8 groups, npm run check, push, confirm CI+Kani (`ccd160e` then hotfix `ec966d0`)
- #1842 tis-sol8-GroupD: P3-1/P3-2/P3-4 doc precision + StackStorage compression (`ccd160e`)
- #1841 tis-sol8-GroupC: P2-2 A/B harness migration + static CI gate (`fdc1650`)
- #1840 tis-sol8-GroupB: P2-1 implicit-unsafe closure + P3-3 stale-doc fixes (`f3a2ff6`)
- #1839 tis-sol8-GroupA: P1-1 flagship — tag-wrap seal fix (`eba76e4`)
- (all run-6/run-7 tasks from earlier in this session are also completed — durable record is `git log`, not the TaskList)

No tasks currently pending/in_progress/blocked besides #661 — babysit cron intentionally stopped (see Decisions).

## Decisions
- Run-8's flagship P1-1 (full tag wrap breaks exclusive issuance): consulted `@fh` again (third consecutive round using this pattern); implemented `@fh`'s exact recommended design (strictly monotonic tag + stateless seal, `Result<(), TagExhausted>`) faithfully, including exact doc/API wording.
- Discovered `isolation: "worktree"` (Agent tool) snapshots `main` from a fixed point early in the session, not live-current-`main` at launch time — for GroupD (heavy overlap with already-merged GroupA/GroupB), switched to manually creating the worktree myself (`git worktree add ... main`) to get a guaranteed-fresh base and avoid a third round of manual conflict reconciliation.
- Found and fixed, in the same commit as the group that surfaced it (not deferred), two real gaps my own earlier zero-trust reviews had missed: (a) README's tier-2 unsafe-count drift after GroupC's merge, (b) a crate-specific no-features CI clippy row nobody had verified all round. Both are now cited explicitly in their fix commits' messages as coverage gaps, not silently patched.
- Stopped the babysit cron (`bbf7d077`) myself after ~16 consecutive identical "blocked on #661" ticks — judged continuing to fire it for up to 7 days with no possible new information as wasteful; reported the action to the user in one line rather than letting it loop silently. `#661` itself remains untouched, still pending explicit authorization.
- `cargo publish` (`#661`) still deliberately NOT executed — same standing irreversibility reasoning, unchanged across every checkpoint this session.

## Open questions
- Does the user want `#661` (`cargo publish`, first release of `tagged-index-stack` to crates.io) executed now, or held for a separate explicit request? This is now the fourth consecutive independent review round (run-5 through run-8) to find and close a genuine pre-publish blocker in this crate — worth flagging as a pattern, not just a checklist item, before actually publishing.

## Repo state
```
(main worktree clean — nothing to commit, working tree clean)
```
```
ec966d0 fix(tis): gate tag_seal.rs's ArrayIndexStack/TagExhausted imports behind test-internals/loom
ccd160e fix(tis): P3-1 "compiler-checked" reframed + P3-2 clause-1/shape-4 contradiction fixed + P3-4 StackStorage doc split
f3a2ff6 fix(tis): P2-1 close 3 implicit unsafe calls + deny(unsafe_op_in_unsafe_fn); P3-3 fix 3 stale/overclaim doc items
eba76e4 fix(tis)!: tis-sol8-GroupA — P1-1 full-wrap exclusive-issuance hole closed
fdc1650 fix(tis): P2-2 — A/B wall-clock harness template migrates to unsafe push + regular-CI static build gate
dcb742c docs(review): audit tagged-index-stack run 8
f35e4fb docs(tis): P3-1 — compress StackStorage rustdoc + CHANGELOG out of review-archaeology
c123bdb checkpoint: 2026-09-02-1737
```

All run-6/7/8 worktrees have been cleaned up (`git worktree remove --force` + `git branch -D`) after each merge — no stray worktrees remain. Landing SHA `ec966d0` confirmed green on both CI and Kani verification on GitHub Actions.
