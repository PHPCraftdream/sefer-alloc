# Checkpoint — 2026-07-29 21:24

## Session summary

This session executed Round 29 of sefer-alloc's ongoing perf/correctness cycle, working through a 16-task queue (#432–447, plus one discovered follow-up #448) generated from two independent readonly reviews. The operating pattern throughout: write a detailed, source-grounded `/crush` prompt per task, launch `crush run` in the background, and on completion perform a genuine zero-trust review (read every diff line, independently re-verify cited numbers against raw logs, re-run the pre-push gate myself) before committing — never trusting a delegated agent's "tests passed"/"verified" claim at face value.

**Completed and committed this session (13 tasks, all on `main`, HEAD `0f6ce0d`):**
- **P0 soundness fixes** (the two most consequential): `dbg_restore_local_for_test`/`dbg_mark_local_torn_for_test` in `tls_heap.rs` (R29-7, `3deb244`) and `AllocCore::dbg_force_decommit_retain_for` (R29-8, `1bfa1d2`) — both were safe `pub fn`s that touched allocator/TLS state through a raw pointer with no validation, the exact R25-1 bug class. R29-8's zero-trust review caught the delegated work missing a downstream compile break (two `#[cfg]` blocks in `r14_4_promotion_free_correctness.rs` that consumed variables now gated behind `bench-internals` without the same gate) — fixed by switching to the existing safe `tls_heap::current_for_alloc()` accessor instead of the newly-unsafe hook, which also *removed* the need for `bench-internals` in that file entirely.
- **R29-9** (`025b302`): a systemic tripwire test (`tests/dbg_hook_safety_tripwire.rs`) enumerating every safe `pub fn dbg_*` with a raw-pointer-shaped signature — it immediately found a REAL fourth instance of the same bug class (`HeapCore::dbg_directory_bit_for_ptr`, missing a containment guard before a header read), filed as task #448 and fixed same-session (R29-17, `bd1bd3e`).
- **R29-1** (`9ef2f85`): reproduced and root-caused R28-2's "anomalous test failure" (previously mis-attributed to build contention) as a real test-logic bug — a windowed-delta assertion that's unsound because the underlying counters are process-wide cumulative, not per-test. Fixed with a global (non-windowed) invariant.
- **R29-2** (`4dee452`): documented the `(8, 32 MiB)` small-pool throughput opt-in in README, fixed a misleading "throughput-first defaults" claim.
- **R29-3** (`db35617`): measured the decommit→reserve segment-lifecycle cost split via wall-clock (iai/Ir was proven blind to the kernel-time-dominated cost mid-task, a genuine methodological pivot); trigger 2 does not fire, closed OPEN_ITEMS item 15 to `[L]`.
- **R29-4** (`e4576d9`): built a per-segment-state reconciliation hook that fully attributes R27-3's previously-unexplained ~4 MiB retention residual to the `small_active` state (magazine-residency mechanism), upgrading it from "inferred by RSS subtraction" to "source-verified."
- **R29-10** (`66a54e4`): isolated the alloc-hit `clear_magazine` block's Ir cost (12.19 Ir/hit, 54.5% of a magazine hit) — closed permanently, correctness-required and not tunable.
- **R29-11** (`2fd0b39`): migrated all 7 still-unindexed `IAI_BASELINE.md` "honest-reject" sections into `OPEN_ITEMS.md`, plus a structural tripwire test preventing recurrence. My own zero-trust review caught a `cargo fmt` failure the delegated work's own verification missed.
- **R29-12** (`0f6ce0d`): surveyed every non-`production` Cargo feature's promotion status; found `virgin-zero-skip` genuinely NEVER-DECIDED (its own Stage-3 promotion gate was never run) and `small-segment-lazy-commit`/`alloc-lazy-commit` as a reasoned-but-unindexed CONDITIONAL-keep-opt-in; indexed both.

**Currently in flight: R29-5 (task #436), NOT yet committed.** A `/crush` run hit a genuine hard weekly/monthly API quota limit mid-task (confirmed error text: "Weekly/Monthly Limit Exhausted... resets 2026-07-30 17:01:11") — not a peak-hours refusal. Per the pre-armed `/crush-fallback sh` marker (task #449), switched to the `sh` Agent subagent type to continue. The `sh` agent inherited solid partial work `/crush` had left (Stage-1 diagnostic counters for medium→Large promotion frequency/copied-bytes: `PROMOTION_COUNT`/`PROMOTION_BYTES_SUM`/`_MIN`/`_MAX`/`_HIST`, all `bench-internals`-gated, mirroring the existing `OPT_H_ATTEMPTS` pattern) that did NOT compile (a private-module cross-reference error), fixed the compile break using an established `pub(crate) use` re-export precedent already in the same file, then completed the actual measurement: a realistic `Vec`-growth-shaped probe found promotions are RARE (33/60,722 = 0.054% of allocation activity) with every single promotion copying exactly 131,072 bytes (a structural artifact of pure-doubling growth). Verdict: **NO VICTIM** — `OPEN_ITEMS.md` item 6 (the Linux `mremap`-for-promotion design) stays deferred, no design/prototype task opened, per the task's own fixed-in-advance anti-p-hacking verdict rule.

**My own zero-trust review of R29-5's returned work is IN PROGRESS, not finished.** I have NOT yet reviewed the actual diffs for correctness (only confirmed compilation). I DID catch one important thing while spot-checking clippy output the `sh` agent flagged as a tangential note: `cargo clippy --all-targets --features "hardened medium-classes" -- -D warnings` — a REAL CI-matrix row (`.github/workflows/ci.yml` line 86-87) — currently FAILS due to a `clippy::trim_split_whitespace` lint in `tests/no_stale_doc_references.rs:538`, inside the `honest_reject_sections_are_indexed` test I committed earlier this session as part of R29-11 (`2fd0b39`). This is a real, confirmed gap in MY OWN prior work that the delegated R29-11 crush run's own verification never caught (it likely never ran that specific clippy row). I fixed it directly (removed a redundant `.trim_start()` call before `.split_whitespace()`, per clippy's own suggestion) and re-verified: the `honest_reject_sections_are_indexed` test still passes, and the previously-failing `hardened medium-classes` clippy row now passes clean. This fix is currently UNSTAGED/UNCOMMITTED, staged to be folded into R29-11's follow-up or committed as its own small fix — not yet decided which.

No `/loop` or `/babysit` cron is currently active except the standing babysit tick (armed earlier this session, still ticking every ~15 min, most recent tick reported "still running #436"). The `/crush-fallback` marker task (#449) currently reads `STATUS: active, AGENT: sh, TRIGGER: hard-limit` — `/crush` remains unusable until the quota resets (~2026-07-30 17:01) or the operator runs `/crush-fallback clear`.

## Active goal

A `/goal` Stop hook is in force, set earlier this session, verbatim: "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу" (implement tasks via /crush, commit between tasks; after all work is done, checkpoint, update changelog, commit all markdown, and run an @oh review agent to check everything). This is mid-satisfaction: most of the "implement tasks + commit between tasks" work is done, but NOT all — 4 tasks remain pending/in-progress (#436 in flight, #437/#444/#445/#446/#447 pending), and the wrap-up steps (checkpoint-per-se already happening now, changelog update, commit-all-markdown, @oh review) have not started.

## TaskList

### in_progress
- #436 R29-5 (P2): Stage-1 measurement of medium→Large promotion frequency and copied-byte distribution — measurement DONE (NO VICTIM verdict), my own zero-trust diff review not yet finished, not committed
- #449 crush-fallback state (persistent — do not complete) — currently STATUS: active, AGENT: sh, TRIGGER: hard-limit (not a real task, the fallback sentinel)

### pending
- #437 R29-6 (P3): archive resolved OPEN_ITEMS narratives and establish immutable measurement provenance
- #444 R29-13 (P2): large-cache retention gate mirroring R27-3's methodology (256 MiB/heap idle floor, never measured)
- #445 R29-14 (P2): add `cargo check --features "production numa-aware"` as a per-PR CI step
- #446 R29-15 (P2): doc corrections — tcache.rs stale claim, numa-aware×small-segment-lazy-commit interaction, README bench-internals hook enumeration drift (blockedBy: none currently listed, though originally noted as depending on #438/#439 which are both done)
- #447 R29-16 (P2): calloc-shaped iai arm for virgin-zero-skip

### recently completed
- #443 R29-12 (P1): feature-promotion status table
- #442 R29-11 (P1): migrate IAI_BASELINE.md's honest-rejects
- #441 R29-10 (P1): alloc-hit clear_magazine isolation
- #435 R29-4 (P1): segment-state reconciliation
- #434 R29-3 (P1): decommit→reserve decomposition
- #440 R29-9 (P0, systemic): safe-dbg-pointer-hook tripwire
- #448 R29-17 (P0): dbg_directory_bit_for_ptr guard (found BY #440)
- #439 R29-8 (P0): AllocCore::dbg_force_decommit_retain_for fix
- #438 R29-7 (P0): tls_heap soundness fix
- #433 R29-2 (P0): README pool recipe doc
- #432 R29-1 (P0): R28-2 anomaly classification

## Decisions

- **Peak-hours vs. hard-limit distinction, applied correctly this session:** when `/crush` first appeared to fail mid-session, I initially (incorrectly, per the user's correction) treated an "unconfirmed" ambiguous state as groundsfor fallback; the user corrected this ("Есть два случая смены агента - часы пик и лимиты" — there are exactly two cases). When a LATER, genuine hard-limit error occurred (confirmed exact error text), I switched to `sh` immediately and silently per the pre-armed marker, without asking — matching the now-tightened `/crush-fallback` skill's explicit "switch immediately and silently... arming this command already was their instruction" rule.
- **Continue partial `/crush` work rather than discard-and-restart:** when the hard-limit-interrupted `/crush` run left uncommitted, non-compiling partial work (the R29-5 promotion counters), I read it, judged it sound in design (mirrors an established pattern), and had the `sh` agent fix the compile break and continue rather than throwing it away — a judgment call favoring salvaging good work over a clean restart.
- **Fixed a real CI-matrix clippy failure found in my OWN prior committed work (R29-11)**, discovered only because the R29-5 `sh` agent happened to mention it as a tangential aside while explaining why it scoped its own clippy check narrowly. Chose to verify and fix it myself immediately rather than deferring — it's a genuine, confirmed CI break (`hardened medium-classes` clippy row) that would have surfaced on the next CI run regardless.

## Open questions

- Whether to fold the `trim_split_whitespace` clippy fix into a `git commit --amend` of R29-11's already-pushed(not yet pushed, but already-committed) commit `2fd0b39`, or commit it as its own small standalone fix alongside R29-5's commit — not yet decided. Leaning toward standalone (amending a commit already several commits back, with other work layered on top, is riskier than a new small commit).
- My own zero-trust review of R29-5's actual diff content (the counter implementation, the example probe, the report's numbers against raw logs) has NOT been performed yet — only compilation/lint were checked so far. This must happen before committing #436.
- Whether `/crush` will become available again before this session's remaining 4 tasks (#437/#444/#445/#446/#447) are done, or whether the whole rest of Round 29 needs to go through `sh` — no decision needed yet, just continue routing through `sh` per the standing fallback until `/crush-fallback clear` or a natural quota reset.

## Repo state

```
 M Cargo.toml
 M docs/perf/OPEN_ITEMS.md
 M src/alloc_core/alloc_core.rs
 M src/alloc_core/alloc_core_core_diag.rs
 M src/alloc_core/mod.rs
 M src/registry/heap_core_free.rs
 M tests/no_stale_doc_references.rs
?? .claude/
?? docs/checkpoints/2026-07-29-1323.md
?? docs/perf/R29_5_PROMOTION_FREQUENCY_GATE.md
?? docs/perf/R29_5_PROMOTION_FREQUENCY_GATE_summary.csv
?? docs/reviews/2026-07-29-oh-acceleration-code-project-review.md
?? docs/reviews/2026-07-29-r28-readonly-review.md
?? examples/r29_5_promotion_frequency_gate.rs
```

```
0f6ce0d docs: survey non-production feature promotion status; index the two dangling decisions (R29-12, task #443)
2fd0b39 docs(perf): migrate IAI_BASELINE.md's 7 unindexed honest-rejects into OPEN_ITEMS.md; add a structural tripwire (R29-11, task #442)
66a54e4 perf(docs): isolate the alloc-hit clear_magazine block's cost at 12.19 Ir/hit; close permanently (R29-10, task #441)
e4576d9 perf(docs): reconcile R27-3's ~4 MiB committed-non-pooled residual to the small_active state (R29-4, task #435)
db35617 perf(docs): measure the decommit->reserve segment-lifecycle split; trigger 2 does NOT fire, close item 15 (R29-3, task #434)
```
