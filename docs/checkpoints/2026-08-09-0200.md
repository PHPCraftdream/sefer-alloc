# Checkpoint — 2026-08-09 02:00 [crate-by-crate-rust-intel-remediation]

## Session summary

This session is executing a `/babygoal`-driven crate-by-crate sweep of the
5+1 workspace sub-crates' `/rust-intel` audit findings from 2026-08-07,
following the user's explicit instruction: process ONE crate at a time, do
not move to the next until ALL of the current crate's tasks are closed;
implement via `/crush` delegation with commits between tasks; after each
crate finishes, `/checkpoint` + update CHANGELOG.md + commit markdown +
`@oh` review — repeat per crate. The full TaskList (#656-768) enforces a
strict `blockedBy` chain across crates: sefer-region → tagged-index-stack →
racy-ptr-cell → a small cross-cutting workspace-`loom`-dependency task
(#711) → aligned-vmem → numa-shim → size-classes, each gated behind the
previous crate's `@oh`-review task, with 4 post-work tasks (checkpoint/
changelog/commit-md/`@oh`-review) per crate.

**sefer-region is fully closed** (15 commits `ea52f85`..`6cb3f6b`,
including two follow-up tasks #769/#770 that its own round-2 closing
review surfaced and which were then fixed in the same round).

**tagged-index-stack's own 5 fix tasks are now ALL closed and committed**
(5 commits `1485bb6`..`d156e5c`): #698 (HIGH — `pop`'s CAS-failure ordering
Relaxed→Acquire, a real concurrency bug, verified via counterfactual);
#702 (HIGH — repaired the loom ABA counterfactual harness, which was
panicking on an accounting bug rather than genuine corruption; found and
fixed two further self-authored bugs during review); #703 (MEDIUM —
promoted `push`'s guard `debug_assert!`→`assert!` per `@oh`'s
recommendation); #704 (INFO — `raw_head` → `#[doc(hidden)]`); #705 (5
batched doc/coverage residuals — tag-budget doc fix, `pack`'s truncation-
not-collision doc fix, `_CHECK_BITS` now forced from BOTH `pack` and
`INDEX_MASK`'s own initializer closing a real gap where out-of-range
widths could reach `unpack`/`empty_index`/`is_empty` without ever calling
`pack`, a `Links` trait Stability doc section, and 4 new proptest
round-trip tests at widths 1/16/31/32). #705's one blocker at the last
checkpoint — a `rustdoc::private_intra_doc_links` warning on a doc-link to
the private `_CHECK_BITS` const — was fixed this session (unlinked to
plain text at both occurrences, lines 193 and 209 of `src/lib.rs`) and
verified via `cargo doc -p tagged-index-stack --no-deps` (zero warnings).
Full verification before commit: 17 non-loom tests green, 8 loom tests
green under `--cfg loom --release`, clippy clean in both default and
`--cfg loom` configs, fmt clean, `scripts/verify-commit-prefixes.mjs`
PASS (the new commit uses `fix(tagged-index-stack)`, correctly reflecting
that it includes a real code change — the `_CHECK_BITS` guard-widening —
not just docs).

**Now starting tagged-index-stack's post-work chain** (#736-739): this
checkpoint is #736. Next: #737 (CHANGELOG.md), #738 (commit markdown),
#739 (`@oh` final review of the whole tagged-index-stack round) — #739's
completion unblocks racy-ptr-cell's fix-task group (#700, #706-710).

## Active goal

No formal `/goal` Stop hook is active. Crate-by-crate sequencing is
enforced structurally via the TaskList's `blockedBy` graph — the standing
directive from the `/babygoal` invocation this session, still in force.

## TaskList

### in_progress
- #656 sefer-region — verify/prepare for crates.io republish (perpetually
  blocked on a maintainer decision, unrelated to the active crate sweep)
- #736 Post-work (tagged-index-stack): /checkpoint — THIS FILE

### pending (next up, in strict blockedBy order)
- #737-739 Post-work (tagged-index-stack): changelog → commit-md →
  `@oh` review — #739 unblocks racy-ptr-cell's group
- #700,706-710 racy-ptr-cell fixes (1 HIGH: README's loom-proof claim is
  silently false) — all blocked by #739
- #740-743 Post-work (racy-ptr-cell) — #743 unblocks #711
- #711 Workspace: hoist `loom = "0.7"` into `[workspace.dependencies]` —
  blocked by #743; unblocks aligned-vmem group
- #699,712-719 aligned-vmem fixes (1 HIGH: fault-injection tests run in
  zero CI configs) — all blocked by #711
- #744-747 Post-work (aligned-vmem) — #747 unblocks numa-shim group
- #697,720-727 numa-shim fixes (0 HIGH, but real bugs incl. mbind
  maxnode off-by-one) — all blocked by #747
- #748-751 Post-work (numa-shim) — #751 unblocks size-classes group (LAST)
- #701,728-731 size-classes fixes (0 HIGH, cleanest crate) — blocked by #751
- #752-755 Post-work (size-classes) — #755 is the final task of the
  entire crate-by-crate sweep
- #657-661,756-768 publish-readiness / bench-scale-tool / captrack tasks
  — independently gated behind their crate's final `@oh`-review, NOT part
  of the active sequence itself

### recently completed (this session)
- #705 tagged-index-stack: 5 doc/coverage residuals — commit `d156e5c`
- #704 tagged-index-stack: raw_head → `#[doc(hidden)]` — commit `96e28a7`
- #703 tagged-index-stack: push guard → `assert!` — commit `17185d6`
- #702 tagged-index-stack: loom ABA counterfactual repair — commit `c552704`
- #698 tagged-index-stack: pop's Relaxed CAS-failure fix (HIGH) — commit `1485bb6`
- #770, #769 sefer-region round-2 closing-review follow-ups

## Decisions

- **User: "act independently, don't ask me — you can ask @oh"** (issued
  mid-session). Supersedes the earlier pattern of using `AskUserQuestion`
  for maintainer-style tradeoff decisions.
- **#703: promote to `assert!`** — `@oh`'s reasoning: real memory-
  unsafety-shaped violation, negligible perf cost (teardown-only caller),
  matches existing project convention.
- **#704: `#[doc(hidden)]`** for `raw_head` (user's own choice).
- **#705's commit prefix: `fix(...)` not `docs(...)`** — chosen because
  the `_CHECK_BITS`-forcing change in `INDEX_MASK`'s initializer is a real
  code change (widens a compile-time guard's enforcement points), not
  merely a doc correction; verified clean against
  `scripts/verify-commit-prefixes.mjs`.

## Open questions

None new this checkpoint. Carried over from earlier sessions, still
unresolved: the sefer-region 0.1.1 version-bump decision (#656's gate),
the unexplained `TypeId` mismatch from task #692's panic-payload downcast
investigation (practically worked around), and `docs/reviews/2026-08-05-
release-readiness-gap-audit.md`'s NO-GO verdict for a 0.3.0 release
(summarized to the user, no task filed yet).

## Repo state

```
?? docs/checkpoints/2026-08-09-0140.md
?? docs/checkpoints/2026-08-09-0200.md
```

```
d156e5c fix(tagged-index-stack): close 5 doc/coverage residuals before first publish (task #705)
96e28a7 docs(tagged-index-stack): decide raw_head's API posture before first publish -- #[doc(hidden)] (task #704)
17185d6 fix(tagged-index-stack): promote push's index-validity guard from debug_assert to assert (task #703)
c552704 test(tagged-index-stack): repair the loom ABA counterfactual harness accounting bug (task #702)
1485bb6 fix(tagged-index-stack): pop's CAS-failure ordering was Relaxed -- retry could read links through a stale head (task #698)
```
