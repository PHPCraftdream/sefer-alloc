# Checkpoint — 2026-08-24 20:34 [fifteenth review wave complete, pushed, CI pending confirmation]

## Session summary

Continuation of the numa-shim `[audit-vmem]` publication-readiness campaign under the standing `/babygoal` condition: implement tasks via `/crush` + `git worktree`, run an independent readiness review when a wave lands, file tasks from its findings, repeat until GO, stop before `cargo publish`. This session picked up mid-campaign (checkpoint `2026-08-24-1556.md`) with CI on commit `8394108` (task #1307's fix for two bugs in task #1306's `bind_range` redesign) unconfirmed. Confirmed it green (a `gh run watch` false-negative earlier in the session was double-checked directly via `gh run view --json jobs`, matching this project's established discipline against watch-timeout artifacts).

The user then re-invoked `/babygoal` with the same standing instruction (re-arming babysit at 15-minute intervals, job id `48f51693`) and pasted a **fifteenth independent review** (Sol-codex, `docs/reviews/2026-08-24-170047-numa-shim-publication-audit-Sol-codex.md`, reviewed revision `8394108`, verdict NO-GO with twelve findings: F1 the only P1, F2 a release-metadata blocker, F3–F12 P2/P3). Notably this review found **no confirmed UB/UAF/double-free** in the post-#1306 architecture — its NO-GO rested on a semantic defect (F1: `current_node()` silently collapsing an undetermined Linux topology into `Some(0)`, now dangerous because task #1306 made `reserve_preferred_on_node` actually install the policy instead of silently no-op'ing), release metadata (F2), and missing verification evidence (F3). I personally re-verified 6 of the 12 findings against source before filing (item 106, task #1308) and committed the report + card together per the established item-89/90 convention.

**All twelve findings from this review are now closed except F2** (the owner explicitly said to ignore the version bump — "игнорируем бап версии... я сам скажу когда это сделать" — and deleted the pre-existing tracking task #1262; F2 is recorded in item 106's card but deliberately not actioned). The remediation ran as six sequential `/crush` + worktree tasks, each reviewed zero-trust (full diff read, independent re-run of fmt/clippy/tests, not trusting the agent's own report) before merging:
- **#1310** (F5+F10): replaced the per-node raw-text cpumap cache with an allocation-free reverse index (`crate::cpumap::ReverseIndex`, `[u8; 8192]`), correcting a mathematically-wrong buffer-size justification and an O(nodes×bytes) per-lookup cost.
- **#1308** (F1, the one P1): owner chose fail-closed remapping via `AskUserQuestion` (over two other options: deprecate+`try_current_node()`, or switch to `getcpu(2)`). One-line behavioral fix (`cpu_to_numa_node`'s `.unwrap_or(0)` → `.unwrap_or(NO_NODE)`) plus a rename (`NodeResolution::FellBackToZero` → `TopologyUnavailable`) and full doc-comment honesty pass. Root crate's `src/alloc_core/numa.rs` needed **zero changes** — its existing `NO_NODE`-as-no-preference composed-fallback pattern from task #1306 already did the right thing automatically once the shim was fixed.
- **#1309** (F4): `NodeId::new(u32) -> Self` (unchecked) → `NodeId::new(u32) -> Option<Self>`, rejecting only the exact `NO_NODE` sentinel (not a general node-existence validator — platform validity stays at `reserve_preferred_on_node`). Every call site across the workspace updated.
- **#1311** (F3+F6+F8, the largest task): real release oracle replacing a false "OOM proves no leak" claim in smoke tests (Windows `VirtualQuery` / Linux `/proc/self/maps`, 4 probes); self-skip discipline replacing `current_node().unwrap_or(0)`; platform-conditional node-count assertion; a scripted two-stage mock (`mock::set_policy_failure`, new `MockCall::InstallPolicy`/`PolicyFailureRelease`) making "reservation succeeded, policy failed, released exactly once" observable for the first time; and a brand-new Linux-only `tests/policy_oracle_linux.rs` querying the kernel's own `get_mempolicy(2)`/`MPOL_F_ADDR` record to prove `reserve_preferred_on_node`'s `Ok` isn't just trusting its own answer — cross-compile-checked only (dev host is Windows), first real execution pending a Linux CI run, with a negative control proving the oracle isn't vacuous.
- **#1312** (F7+F12, doc-only): reconciled a self-contradiction ("successfully bound" next to "does not guarantee physical placement"); corrected the README's claim that binding existing pages is an OS impossibility (it isn't — `MPOL_MF_MOVE` exists; not implementing it is a crate-scope choice); corrected a false claim that a direct `aligned-vmem` dependency is needed to name `Reservation` (it's re-exported).
- **#1313** (F9+F11, the final task): owner decision via `AskUserQuestion` — numa-shim officially supports 64-bit Windows only (documentation-only, no code gate). F9's safe half (strict-provenance `.addr()`/`.with_addr()` replacing an integer pointer round-trip, mirroring sibling `aligned-vmem`'s own fix) landed; the risky half (a one-call `MEM_RESERVE|MEM_COMMIT` fast path touching a function with three prior documented correctness bugs) was deliberately deferred as an unmeasured perf candidate, `docs/perf/OPEN_ITEMS.md` item 60, rather than rushed.

**Two genuine zero-trust catches this session, both fixed before merge/push:**
1. During #1313's review, discovered its own CHANGELOG entry had landed in the ROOT `CHANGELOG.md`, breaking this entire wave's established convention (every prior task used only `crates/numa-shim/CHANGELOG.md`) — reverted and relocated before merging.
2. **A build-cache-contamination false positive**, diagnosed mid-session: running `cargo clippy --features "numa-aware internals"` in the main checkout immediately after running commands in a sibling git worktree with a differing, not-yet-merged `numa-shim` source produced a spurious extra compile error that could only be true for the WORKTREE's code. A forced `cargo clean -p sefer-alloc -p numa-shim` made it disappear, confirming it was cache cross-contamination, not a real bug — the REMAINING 6 errors after that clean rebuild were genuinely pre-existing, unrelated `main` rot (dead code / unused imports in `alloc_core_small_reclaim.rs`/`magazine_bitmap.rs` under that specific untested feature combo), filed as item 107 in `docs/correctness-open-items/TRACKED_ci_gate_coverage.md` with a process-note caveat for future worktree-heavy verification in this repo.

**A third, genuine gate failure was caught by the first FULL `npm run check` run since the wave started** (task #1315): `dbg_current_node_for_cpu`, a new doc-hidden test forwarder task #1308 added, was never added to `tests/dbg_hook_safety_tripwire.rs`'s `PURE_OBSERVERS` allowlist — the exact same defect class as an earlier-session task #1287 for its sibling function. None of tasks #1308–#1313's own per-task verification ran the FULL root-crate check (only scoped `cargo test -p numa-shim` / `cargo test --test numa_seam`), so this slipped through six zero-trust reviews until the end-of-wave full gate run. Fixed with a one-line allowlist addition; full `npm run check` re-run ALL GREEN.

**Pushed**: 22 commits (`8394108..9137c51`) to `origin/main`, landing SHA `9137c514775ca539a99e454945cf6a6103cc7ecb`, confirmed matching between local `HEAD` and `origin/main`. CI run `32763139107` was `queued` at last check; a background `gh run watch --exit-status` (task id `beiz2ysmw`) is running, not yet resolved as of this checkpoint.

## Active goal

The standing `/babygoal` Stop-hook condition (verbatim, re-armed this session): *"реализуй задачи по numa-shim с помощью агентов /crush & worktrees, в конце (когда решишь все таски) проведи ревью на готовность крейта к релизу с помощью агента @oh — после ревью заведи таски и снова решай их. Делай такие циклы пока ревью не скажет GO на публикацию и остановись перед публикацией."* Still NOT satisfied — no unconditional GO has been returned by any review yet (fifteenth review returned NO-GO, all its findings now remediated except owner-deferred F2, but NOT yet re-reviewed). babysit is armed (job `48f51693`, every 15 min, session-only, auto-expires in 7 days from arming).

## TaskList

### in_progress
(none)

### pending
- #657 numa-shim — verify/prepare for crates.io republish (no live blockedBy — #1262 was deleted this session; #657's own description still names #658, aligned-vmem's own publish gap, as the real blocker, unrelated to this campaign)
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #662 Root sefer-alloc: design note for applying bench-scale-tool alongside criterion/iai
- #763 Root sefer-alloc: implement bench-scale-tool per the approved design (blockedBy: #662)
- #1091 [perf-regress] Найти и устранить регрессию ~10-13% Ir в диапазоне 42d8d223..42d42061 (item 56)
- #1299 [numa-shim] T8+T9+T12+T14: CI-hardening follow-up set — explicitly non-blocking, deferred to AFTER the release per two independent reviews' own recommendation. Do not start before publish.

None of these are part of the active numa-shim readiness cycle; #1299 is deliberately deferred and the others are pre-existing, unrelated backlog. The next real task is not yet filed — it depends on the sixteenth review's verdict (see Open questions).

### recently completed (this session, most recent first)
- #1315 Fix dbg_hook_safety_tripwire red: dbg_current_node_for_cpu missing from allowlist
- #1313 [numa-shim] F9+F11 (P3): Windows one-call fast path + strict-provenance; определить политику поддержки 32-bit Windows
- #1312 [numa-shim] F7+F12 (P2/P3 docs): soft MPOL_PREFERRED называется «successfully bound»; README противоречит реэкспорту Reservation
- #1311 [numa-shim] F3+F6+F8: нет независимого оракула установленной NUMA policy, mock не моделирует двухстадийность, smoke tests переоценивают свои оракулы
- #1309 [numa-shim] F4 (P2): NodeId::new не обеспечивает собственный документированный инвариант != NO_NODE
- #1308 [numa-shim] F1 (P1): current_node() Linux fallback превращает неизвестный узел в Some(0) — owner decision на fail-closed семантику
- #1310 [numa-shim] F5+F10 (P2/P3): cpumap bound обоснован неверной величиной + hot lookup O(nodes × 1 KiB) — заменить на reverse index cpu→node
- #1307 Fix CI red on 9c345de: two real bugs from task #1306's redesign
- #1262 [numa-shim] version bump to 0.2.0 — **DELETED** this session, per explicit owner instruction, not completed

(1 deleted this session besides #1262: a throwaway placeholder task #1314, created and immediately deleted for an unrelated TaskList-refresh workaround — not meaningful.)

## Decisions

1. **F1 fix: fail-closed remapping** — chosen via `AskUserQuestion` over "deprecate + `try_current_node()`" and "switch to `getcpu(2)`". `current_node()` on Linux now returns `None` for every undetermined case (not just `Unavailable`), not just `Some(0)`; `NodeResolution::FellBackToZero` renamed to `TopologyUnavailable`.
2. **F11 policy: 64-bit Windows only** — chosen via `AskUserQuestion` over "add i686 to CI + cfg-gate". Documentation-only; the crate's test FFI was already 64-bit-only in practice.
3. **F9's risky half (one-call Windows fast path) deliberately NOT implemented** — filed as an unmeasured perf candidate (`docs/perf/OPEN_ITEMS.md` item 60) rather than rushed, given the target function's three prior documented correctness bugs (tasks #724, #778/F2, #1304) and this repo's own evidence-before-perf-claim discipline.
4. **F2 (version bump) deliberately excluded from this wave's task filing** — explicit owner instruction mid-session ("игнорируем бап версии... я сам скажу когда это сделать"); pre-existing task #1262 deleted rather than left dangling.
5. **CHANGELOG entries go in `crates/numa-shim/CHANGELOG.md` exclusively, never the root `CHANGELOG.md`, for this wave's per-task entries** — established convention from tasks #1306–#1312, one deviation caught and fixed in #1313's own zero-trust review before merge.

## Open questions

1. **Is CI run `32763139107` (landing SHA `9137c51`) actually green?** Unresolved as of this checkpoint — was `queued` at last check; a background watch (`beiz2ysmw`) is running. Given this session's own established discipline (a `gh run watch` result was double-checked and found to be a false negative earlier this session), confirm via `gh run view --json jobs` directly once the watch resolves, not the bare watch exit code alone.
2. **When CI is confirmed green: launch the SIXTEENTH independent review.** This is the direct, explicit next step per the still-unsatisfied `/babygoal` condition — all fifteenth-review findings are remediated (except owner-deferred F2), so the wave is ready for a fresh readiness pass to see whether it returns GO.
3. **Task #1262 (0.2.0 version bump) has no active task** — deliberately, per the owner's explicit instruction this session. Do not re-file it without the owner's own signal to proceed, even if a future review re-flags version/CHANGELOG staleness as a blocker (that would just re-confirm F2, already known and already deliberately deferred).
4. Standing, unresolved from prior checkpoints: the unexplained `.claude/worktrees/agent-*` directories (30+, from parallel `crush`/agent usage across this session and prior ones) — not investigated, likely benign artifacts of the worktree-per-task pattern, not cleaned up.

## Repo state

```
(clean except this checkpoint's own new file — no other output from git status --short)
?? docs/checkpoints/2026-08-24-2034.md
```
```
9137c51 fix(ci): task #1315 — add dbg_current_node_for_cpu to the dbg-hook safety allowlist
ed582fe docs: task #1313 follow-up — item 106's F9/F11 disposition refreshed in place, wave complete
ddd734e merge: task #1313 — F9+F11: strict-provenance base derivation + 64-bit-only Windows policy (fifteenth review)
de0fc11 fix(numa-shim): task #1313 follow-up — move CHANGELOG entry to the crate-local file, matching this wave's convention
7905a0c fix(numa-shim): task #1313 — F9+F11: strict-provenance base derivation + 64-bit-only Windows policy (fifteenth review)
```

`origin/main` = `9137c514775ca539a99e454945cf6a6103cc7ecb` (pushed this session, matches local `HEAD` — confirmed via `git fetch` + `git rev-parse origin/main HEAD`). CI run `32763139107` in progress/queued on that SHA — outcome unconfirmed (see Open questions #1).
