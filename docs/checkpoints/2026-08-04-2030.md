# Checkpoint — 2026-08-04 20:30 [round-34-in-progress-16-of-27-tasks-done]

## Session summary

This session continues Round 34 of the `sefer-alloc` project — the babygoal ritual re-armed with "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh". The round's 26 work tasks (#520-545) plus a closing task (#546) were decomposed and refined earlier this session (including a `@fm` review of the task queue that caught and fixed several real defects in the task descriptions before execution began), based on two independent readonly reviews: `docs/reviews/2026-08-04-release-stabilization-audit.md` (UB/soundness/panic-safety/coverage audit) and `docs/reviews/2026-08-04-r32-r33-global-bench-readonly-review.md` (Round 32/33 benchmark methodology review).

**Execution model**: sequential `/crush` delegation (external CLI sub-agent, model glm-5.2), one task at a time, with rigorous personal zero-trust re-verification after every single task — every commit's diff read in full, every claimed test/clippy/fmt result independently re-run, several claimed measurements independently re-derived from raw data, and in multiple cases a real counterfactual check performed myself (temporarily reverting a fix, confirming the associated test fails, then restoring it) before accepting a task as done. This is not a formality in this session — it has caught and fixed several real defects along the way (see Decisions/Errors below).

**Progress: 15 of 27 tasks fully completed and verified (#520-534)**; task #535 (R34-16) is currently in-flight via crush, with partial uncommitted work already visible in the working tree (`docs/ARCHITECTURE.md`, `docs/CORRECTNESS_OPEN_ITEMS.md`, `src/global/sefer_alloc.rs` modified; new file `tests/no_panic_doc_accuracy.rs`) — this looks like it chose option (b) from the task brief (doc-only fix + a doc-accuracy pin test), consistent with the task's own lean recommendation, but has not yet reported completion or been reviewed by me. Tasks #536-545 remain pending; #546 (closing sequence: `/checkpoint`, CHANGELOG, commit-md, `@oh` review) is blocked on all of them.

**Two crush sessions were manually restarted this window** (R34-13 and R34-15) at the user's explicit terse instruction "перезапусти агента. просто сделай это" — both times I killed the live session cleanly (`crush sessions kill`), inspected the working tree for partial work before restarting, found substantial legitimate partial progress in both cases, and relaunched into the *same* session with a resume prompt describing the existing state rather than discarding it. Both resumed runs completed correctly and passed my zero-trust review.

**Most significant finding of the round so far**: task #533 (R34-14) uncovered and fixed a REAL permanent-leak bug (not merely a doc/proof gap) — F12's targeted-write optimization on the large-cache hit path had stopped resetting `deferred_next` to `ABANDONED_TAIL`, so a segment that had gone through the deferred-large-free path and was later reused via cache-hit would carry a stale link value; a subsequent cross-thread free would then have its `push_large_deferred_free` CAS silently fail (mistaken for a double-free), permanently leaking the segment. I independently verified this myself: read the exact original `SegmentHeader::large` constructor values, confirmed the fix's reset values match byte-for-byte, and personally ran the counterfactual (disabled the fix, confirmed the new test fails with an explicit "R34-14 leak bug" panic message, restored the fix, confirmed it passes again).

**Second significant finding**: task #529 (R34-10)'s sparse-decay gate found the R32-8 decay-throttle's "bounded to one segment" retention claim does NOT hold — I independently re-ran the gate myself and reproduced the exact reported numbers (peak 4-segment gap, persisting 27/40 consecutive intervals). Task #530 (R34-11) then added a bounded catch-up loop that measurably closes most of that gap (final gap 3→1 segments) while preserving R32-8's original throughput benefit — I independently reproduced this fix's core behavior too (interval-30 catch-up firing 3 steps in one read).

**Task #531 (R34-12)** re-measured the RemoteFreeRing shadow-head claim with a genuinely clean A/B (same features, same harness, same drain mechanism, separate git worktree for the "before" tree) and CONFIRMED the original -30…-36% claim at -32.07% in the favorable regime — this one turned out to validate a prior claim rather than refute it.

**A recurring minor defect class**: several gate-report derive scripts, when re-run by me for reproducibility verification, produced a byte-identical result except for one metadata column (`base_commit`/`doc_commit`) — because the script's `git rev-parse HEAD` fallback, invoked at derive time *inside* the report's own landing commit, captures the PARENT SHA (the commit doesn't exist yet at derive time). This is the exact class already filed as `docs/perf/OPEN_ITEMS.md` item #41 (from R34-2 this session). It recurred in R34-10 and R34-11's own CSVs; both were caught and fixed with a small follow-up commit each, following the established pattern.

**Also caught and fixed this session**: `scripts/miri.mjs` and `scripts/tsan.mjs` (local convenience scripts, NOT the CI workflow definitions, which were already correct) had silently stopped validating several tests since R34-3 introduced the `internals` Cargo feature — the local matrices' feature strings were never updated to include it, so several `--test` binaries compiled with the whole module `#[cfg]`d out and reported "0 tests, PASS". Both fixed with dedicated commits. Also: R34-2's crush run committed three readonly-review report files out-of-scope (violating this project's established convention that review reports stay uncommitted); caught and fixed with `git rm --cached` + a follow-up commit.

**User also asked**, earlier in this session (now already satisfied): to bump `crossbeam-epoch` to close `RUSTSEC-2026-0204` (done, task #523) and to commit all outstanding markdown at one point (done — only the checkpoint file, review reports stay excluded per convention).

## Active goal

The re-armed `/babygoal` Stop-hook condition: "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу". Not yet satisfied — 12 work tasks plus the closing task remain.

## TaskList

### in_progress
- #535 R34-16: reconcile the "NEVER panics" claim with five release-surviving panic sites (F-5) — crush run in flight, partial uncommitted work visible, not yet reviewed by me

### pending
- #536 R34-17: add the two missing RAII unwind guards (F-7 drain head, F-8 fallback init)
- #537 R34-18: pin size_of::<HeapCore>() against silent stack-pressure growth (F-6)
- #538 R34-19: add a loom model exercising the F10 fast path over a RECYCLED slot (G2)
- #539 R34-20: wire ASan into CI (G6)
- #540 R34-21: fix the src/lib.rs unsafe-seam inventory and the stale `production` feature lists (F-9, F-10)
- #541 R34-22: sweep the remaining stale current-state docs (blockedBy: #540 — others already resolved)
- #542 R34-23: build a real realloc / Vec growth gate suite
- #543 R34-24: define the docs/perf artifact policy + a per-round manifest
- #544 R34-25 [research]: Small magazine provenance design
- #545 R34-26 [research]: page-run layer design gate for 256 KiB–2 MiB
- #546 R34-27: closing steps (blockedBy: #535-545, all remaining)

### recently completed
- #534 R34-15: process::abort() widened to graceful Option on the free path (real fix, found a 3rd call site beyond the 2 the task brief named)
- #533 R34-14: real permanent-leak bug found and fixed (deferred_next carry-forward) + exhaustive field-classification compile-time pin + re-derived safety argument
- #532 R34-13: OWN_CACHE_SIZE kept at 16 (evidence-justified — real Tier-2 victim confirmed at cache=4 in R32-10's own data)
- #531 R34-12: clean A/B re-gate CONFIRMED the original RemoteFreeRing shadow-head claim (-32.07%)
- #530 R34-11: bounded catch-up loop for sparse-decay, benefit preserved, gap substantially reduced
- #529 R34-10: sparse-decay gate found the R32-8 retention bound does NOT hold (real, independently reproduced finding)
- #528 R34-9: six global_alloc.rs bench label/unit/doc corrections
- #527 R34-8: control-arm drift guard added to bench-table.mjs
- #526 R34-7: causal subprocess comparative harness MVP (SeferAlloc/mimalloc/System, real #[global_allocator] per binary)

## Decisions

- Chose to restart two crush sessions on the user's direct instruction by killing cleanly and resuming with existing partial work described in the relaunch prompt, rather than discarding — both times the partial work was legitimate and worth keeping.
- Chose to independently re-run/re-derive claimed numbers myself wherever feasible (gate scripts, counterfactual test disable/re-enable) rather than trusting the sub-agent's report — this is the discipline that caught the base_commit off-by-one class and confirmed the R34-14 leak bug is real, not overstated.
- Did not second-guess R34-12's crush run choosing to confirm (not refute) the original RemoteFreeRing claim — accepted the result since my own re-derivation of the same raw JSON matched exactly.
- Left the review-file-exclusion convention (readonly review reports stay uncommitted) intact throughout, re-applying it once when a crush run violated it.

## Open questions

None new. The standing question from earlier in this session (does the user want the two most-recent-round-prior `@oh` review's findings acted on) was implicitly superseded — this whole Round 34 IS the action taken on those findings.

## Repo state

```
 M docs/ARCHITECTURE.md
 M docs/CORRECTNESS_OPEN_ITEMS.md
 M src/global/sefer_alloc.rs
?? .claude/
?? docs/reviews/2026-08-03-round33-readonly-review.md
?? docs/reviews/2026-08-04-r32-r33-global-bench-readonly-review.md
?? docs/reviews/2026-08-04-release-stabilization-audit.md
?? tests/no_panic_doc_accuracy.rs
```
(The three modified/new files above are #535's in-flight, not-yet-committed crush work — do not treat as stray changes.)

```
49929d0 fix(perf): widen free-path chunk-materialisation OOM from abort to graceful return (R34-15, task #534)
7ef5a46 fix(perf): reset owner/deferred fields on large-cache hit; pin field classification; re-derive targeted-write safety arg (R34-14, task #533)
1c686f8 docs: keep OWN_CACHE_SIZE=16; fix stale "(4)"/"& 3" doc comments to parametric OWN_CACHE_SIZE (R34-13, task #532)
9e70266 bench: R34-12 clean A/B re-gate of RemoteFreeRing shadow-head (task #531)
43115cf fix(perf): correct R34-11 CSV's base_commit off-by-one (parent -> landing SHA)
```
