# Checkpoint — 2026-07-29 13:23

## Session summary

This session executed Round 27 (tasks #419–429, 11 tasks) and Round 28 (tasks #430–431, 2 tasks) of sefer-alloc's ongoing perf/correctness cycle, performed the end-of-round wrap-up ritual (CHANGELOG backfill for Rounds 26/27/28, checkpoint, commit, push, CI verification — all completed and green), then launched two independent readonly reviews to plan Round 29.

**Round 27/28 recap** (all committed and pushed, `origin/main` at `b7ff9fe`): corrected a malformed one-knob pool-cap default proposal into the real paired `(4,16MiB)→(8,32MiB)` decision; quantified the retention cost (~+8 MiB/heap, victim-activation-proven) and reconfirmed the ~22% latency win at the real config; wrote (and rejected building) an adaptive pool-budget design; removed dead NO-GO code and a soundness-adjacent bitmap-clear hook; isolated `flush_class`'s standalone Ir cost (449 Ir); strengthened the `r14_4` leak-bound assertion from "no double-release" to "no leak," catching and fixing a real CI-breaking gap (`hardened medium-classes` lacks `alloc-decommit`) during my own zero-trust review before commit.

**Round 29 planning, two passes:**
1. The user pasted a review already written by a separate `/oh` invocation (`docs/reviews/2026-07-29-r28-readonly-review.md`) — I independently verified its three checkable claims against source (all confirmed) and filed 6 tasks (#432–437, P0–P3): classify/reopen R28-2's unexplained flaky-test anomaly, document the `(8,32MiB)` pool recipe in README, Stage-1 decompose the reservation-only-overflow-tier's unmeasured trigger, per-heap segment-state accounting, medium→Large promotion frequency measurement, and OPEN_ITEMS archival/provenance cleanup.
2. The user then explicitly asked me to launch a fresh `@oh` Agent (subagent_type `oh`, Opus top/1M/high) to do an INDEPENDENT second readonly review answering the same three questions (accelerate / code / project), deliberately scoped to avoid duplicating the first review. It wrote `docs/reviews/2026-07-29-oh-acceleration-code-project-review.md` and found — critically — **two real P0 soundness holes**: `tls_heap::dbg_restore_local_for_test` (a safe `pub fn` that installs a caller-supplied raw pointer as the current thread's `*mut HeapCore`, reachable from 100%-safe code under plain `production`, can alias two threads onto one `HeapCore`) and `AllocCore::dbg_force_decommit_retain_for` (a safe `pub fn` on the fully-public re-exported `AllocCore` type that decommits payload pages with no `live_count` check — can UAF live blocks from safe code). **I personally re-verified both against source line-by-line before trusting them** (read the actual function bodies, confirmed the `#[cfg]` gates, confirmed module/type reachability via `src/lib.rs`) — both are real, exactly the R25-1 class the project already has a CLAUDE.md rule about, and survived every prior audit (R24-6, my own R27-7) because those audits only searched for hooks already marked `unsafe fn`.

Filed 10 more tasks (#438–447) from this second review, all P0–P2, including the two soundness fixes (#438, #439), a systemic tripwire to catch this bug class going forward (#440, blocked on #438/#439), a Stage-1 measurement of the alloc-hit path's never-measured segment-metadata RMW cost (#441, mirrors R28-1's methodology), migrating `IAI_BASELINE.md`'s 8 untracked honest-rejects into `OPEN_ITEMS.md` (#442), a feature-promotion status table for 3 features with dangling promotion decisions (#443), a large-cache retention gate (#444, the 256 MiB/heap idle-floor analogue of R27-3's small-pool work, never measured), a `numa-aware` per-PR CI compile step (#445), three bundled doc corrections (#446, blocked on #438/#439), and a `calloc`-shaped iai arm for the already-built `virgin-zero-skip` feature (#447).

I had already started investigating the fix for #438 (grepped all 3 call sites across `tests/dealloc_only_no_bind_torn.rs` and `tests/r14_4_promotion_free_correctness.rs`, read `dealloc_only_no_bind_torn.rs` in full to judge whether its whole-file gate can be widened without losing CI coverage) when the user interrupted to ask me to formalize findings into the TaskList first — that investigation's conclusions are captured in #438's task description but the fix itself was not yet applied.

No `/loop` or `/babysit` timers are currently active (the babysit cron from earlier in the session self-deleted when the TaskList emptied after Round 28; the one-shot 12:00 `/crush`-peak-hours alarm already fired and is gone). `/crush` peak hours ended at 12:00 and the provider should be available again, but no `/crush` session has been (re)launched yet this pass — Round 27/28 finished via a mix of `/crush` and the `sh` Agent subagent type after a peak-hours refusal mid-session.

## Active goal

None currently set (no `/goal` Stop hook condition is in force at checkpoint time, per the most recent turns in this conversation).

## TaskList

### pending (16, Round 29 queue — none started yet)
- #432 R29-1 (P0): classify or reopen R28-2's unexplained anomalous test failure
- #433 R29-2 (P0): document the (8, 32 MiB) small-pool throughput recipe in README and fix the contradictory "throughput-first defaults" claim
- #434 R29-3 (P1): Stage-1 decomposition of the decommit→reserve segment lifecycle (resolves R27-11's unmeasured trigger 2)
- #435 R29-4 (P1): per-heap segment-state accounting to explain R27-3's ~4 MiB committed-non-pooled residual
- #436 R29-5 (P2): Stage-1 measurement of medium→Large promotion frequency and copied-byte distribution (gate for the Linux mremap design)
- #437 R29-6 (P3): archive resolved OPEN_ITEMS narratives and establish immutable measurement provenance
- #438 R29-7 (P0): re-gate + unsafe-ify tls_heap::dbg_mark_local_torn_for_test / dbg_restore_local_for_test — investigation already started, see Decisions below
- #439 R29-8 (P0): re-gate + unsafe-ify AllocCore::dbg_force_decommit_retain_for
- #440 R29-9 (P0, systemic): add a tripwire enumerating every safe pub fn dbg_* taking/returning a raw pointer, not unsafe fn, not bench-internals-gated  (blockedBy: #438, #439)
- #441 R29-10 (P1): Stage-1 isolation of the alloc-hit segment_base_of_ptr + clear_magazine cost
- #442 R29-11 (P1): migrate IAI_BASELINE.md's eight honest-reject sections into OPEN_ITEMS.md's [L] tier
- #443 R29-12 (P1): feature-promotion status table; resolve or explicitly re-defer virgin-zero-skip / small-segment-lazy-commit / alloc-lazy-commit
- #444 R29-13 (P2): large-cache retention gate mirroring R27-3's methodology (256 MiB/heap idle floor, never measured)
- #445 R29-14 (P2): add cargo check --features "production numa-aware" as a per-PR CI step
- #446 R29-15 (P2): doc corrections — tcache.rs stale claim, numa-aware×small-segment-lazy-commit interaction, README hook enumeration drift (blockedBy: #438, #439)
- #447 R29-16 (P2): calloc-shaped iai arm for virgin-zero-skip

### recently completed
- #431 R28-2 (P2): strengthen canary_survives_promotion_and_free_leaves_no_leak's leak-bound assertion
- #430 R28-1 (P2): measure/isolate flush_class's own Ir cost in the magazine-overflow free path
- #429 through #419 (R27-1..R27-11) — full Round 27 queue, all completed

## Decisions

- **Formalize review findings into TaskList before fixing code.** The user explicitly interrupted my in-progress direct-fix attempt on #438 to ask for tasks to be filed/updated first — chose to fully capture the investigation already done (exact call sites, which test files can have their whole gate widened vs. need R28-2's narrower per-statement `#[cfg]` pattern, which CI rows are at risk) into #438's task description rather than let that analysis be lost, then paused the fix itself.
- **File the two P0 soundness findings as separate tasks (#438, #439) rather than one combined task**, matching the project's "one task per executable unit" convention — the two hooks live in different files, have different caller sets, and different CI-row exposure, even though they're the same bug class.
- **Independently re-verify both P0 soundness claims against source before trusting the sub-agent's report** — read the actual function bodies, gate attributes, and reachability chain (`src/lib.rs` → `global`/`tls_heap` module, `AllocCore` crate-root re-export) myself rather than filing tasks off the agent's prose alone.
- **Scoped the second (`oh`) review explicitly to avoid duplicating the first (`r28`) review** — instructed it to read the first review, agree-and-not-re-derive where it concurs, and focus effort on genuinely new angles; this worked (the agent's own "what I checked vs. prior review" table shows real non-overlap: alloc-hit-path cost, large-cache retention, the two soundness holes, `IAI_BASELINE.md` indexing gap, feature-promotion tracking gap — none of which the first review touched).

## Open questions

- Whether to resume `/crush` for Round 29 execution now that peak hours have ended, or continue with the `sh` Agent subagent type that worked for the tail of Round 27/28 — not yet decided this pass.
- Order of Round 29 execution: P0 items (#432, #433, #438, #439) are the natural first batch, but no explicit sequencing/parallelization decision has been made yet.

## Repo state

```
?? .claude/
?? docs/reviews/2026-07-29-oh-acceleration-code-project-review.md
?? docs/reviews/2026-07-29-r28-readonly-review.md
```

```
b7ff9fe docs: CHANGELOG Round 26/27/28 entries, session checkpoints, R25/R26 readonly reviews
5d81f64 test(r14-4): strengthen the leak-bound assertion to prove no leak, not just no double-release (R28-2, task #431)
eb0b6b8 perf(registry): isolate flush_class's standalone Ir cost in the magazine-overflow free path (R28-1, task #430)
66282c3 docs(perf): evaluate R27-11's reservation-only overflow tier trigger — not opened (task #429)
07d03d8 fix(registry): remove dbg_overflow_bitmap_clear_pass and its bench arm (R27-10, task #428)
```
