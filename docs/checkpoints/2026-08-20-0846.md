# Checkpoint — 2026-08-20 08:46

## Session summary

The work is preparing the Rust crate `aligned-vmem` (v0.2.0, never published — crates.io still serves 0.1.0 under a different API) for its first real crates.io release. This session ran under a `/babygoal` loop: solve tasks with sub-agents in git worktrees, commit between tasks, then review the wave with an independent agent and file new tasks from the findings.

Two full waves completed and landed. **Wave OX6** (#1192–#1196, five commits) closed the third audit's review findings; **wave OX7** (#1201–#1206, five commits) closed the findings of the `@ox` review of OX6. Both were pushed and CI-confirmed on landing SHAs read from the remote (`3d6c98f`, then `dc2ecdd`). One red-`main` incident occurred and was fixed: `dff7b1d` left two CI jobs failing on `refused_variant_is_produced_by_a_genuine_os_refusal`; task #1199 re-gated the test and `3d6c98f` went green 38-executed/3-skipped.

The dominant finding of the session is that my own corrections kept reproducing the class they fixed. #1197 gated a test on ONE cfg instead of stating the condition, and `main` went red on the other two the next CI run. #1194 fixed a self-falsifying count and recorded a self-falsifying count in its own commit body. #1201 found that a "three failing jobs" list — which reached a permanent index card — came from my own broken API filter (`conclusion not in (None,...)` counts still-RUNNING jobs as failures, because their `conclusion` is the empty string); I had noticed the filter bug on the NEXT run, reported it honestly, and never went back to re-derive the list it had already produced.

A **fourth independent audit** (Сол-кодекс, `docs/reviews/2026-08-20-073908-...`, revision `dc2ecdd`, verdict NO-GO) arrived near the end. Its H1 corrects me personally: `src/os/windows.rs:366` does `base.add(start)` and the test passes `start = 64 MiB` against a 2 MiB reservation — `pointer::add` requires the result to stay in bounds of the same object, so the **arithmetic itself is UB**, not just the access. I had handled that test three times (#1197, #1199, #1202) and in #1202 wrote "No out-of-bounds MEMORY ACCESS occurs" into its SAFETY comment — true about the access, and exactly why it read as safe. I verified H1, M1 (`README.md:56` still documents `try_decommit -> Result<(), VmemError>` on the crates.io front page), M2's semver premise (`decommit_outcome.rs:25` IS `#[non_exhaustive]`, so a `Simulated` variant can be added post-publication) and C3 (item 59b already exists — no duplicate) myself before filing.

That audit's findings became wave SOL2: #1209 (done — report committed with its card, item 93), #1210 (H1), #1211 (M1 doc drift), #1212 (M2 `Advised` semantics), #1213 (L1+L2+L3), #1214 (indexes), plus #1207 (the live `## 0.2.0 - Unreleased` header, which ships in the tarball — verified `grep -rn "Unreleased" scripts/ ci.yml` returns ZERO, so no gate catches it), #1208 (Status-line refresh, deliberately moved to LAST so it isn't invalidated by its own wave — the #1193 class) and #1216 (gate → push → CI → fifth audit by `@oh`, which is step 5 of the audit's own release gate and had no owner).

**In flight at checkpoint time:** an `@sl` agent in an isolated git worktree working task **#1217** — splitting `docs/CORRECTNESS_OPEN_ITEMS.md` (2566 lines) into a folder, per the owner's direct request. That request reverses recorded decision item 86, sits against precedent #1116 (the FIRST split of this index truncated 9 pointers mid-heading and lost the verdict on 19 of 32), and must update `tests/no_stale_doc_references.rs`, which parses the file at five sites plus the new item-59a sum assertion from #1206. The brief demands a card census before/after and a mandatory counterfactual so the parser cannot go green-and-dead.

Local `main` is **1 commit ahead** of `origin/main` (`105cf53`, task #1209) and unpushed — the gate has not been run for it yet. A `# babysit tick` cron (`dcc21d11`, every 15 min) is active.

## Active goal

> реализуй с помощью агентов @sl & worktrees, в конце (когда решишь все таски) проведи ревью с помощью агента @oh - после ревью заведи таски и остановись, между тасками делай коммиты.

## TaskList

### in_progress
- #1215 [ЗАКРЫТИЕ] Подтвердить зелёный CI на landing SHA dc2ecdd (волна OX7) — 40/41 done, 0 failures at last check
- #1217 [SPLIT] Распилить `docs/CORRECTNESS_OPEN_ITEMS.md` в папку — `@sl` agent running in a worktree

### pending
- #1190 [РЕШЕНИЕ ВЛАДЕЛЬЦА] HugeTLB-карты с гранулярностью ≠ 2 МиБ — умолчание НЕТ, ответа нет
- #1207 `## 0.2.0 - Unreleased` в тарболе + гвардия  (blockedBy: #1211)
- #1208 ЗАКРЫТИЕ ВОЛНЫ: Status items 90/91/93  (blockedBy: #1210, #1211, #1212, #1213, #1214)
- #1210 H1: UB в `base.add(64 MiB)` + детерминированный seam  (blockedBy: #1209 ✓)
- #1211 M1: README/rustdoc/CHANGELOG под текущий API  (blockedBy: #1209 ✓)
- #1212 M2: семантика `Advised` под mock/miri
- #1213 L1+L2+L3
- #1214 C2+C3+P1/P3 в индексы  (blockedBy: #1211)
- #1216 ЗАКРЫТИЕ: гейт → пуш → CI → пятый аудит `@oh`  (blockedBy: #1208)
- #658 публикация 0.2.0  (blockedBy: #1190, #1207, #1216)
- #657 numa-shim republish  (blockedBy: #658)
- #659, #660, #661 первые публикации racy-ptr-cell / size-classes / tagged-index-stack
- #662, #763 bench-scale-tool (design + implementation)
- #1091 [perf-regress] регрессия ~10-13% Ir в 42d8d223..42d42061

### recently completed
- #1209 SOL2: закоммитить четвёртый аудит + карточка item 93
- #1206 OX7: пин суммы item 59a, форма карточки item 92, поправка к формулировке ревью
- #1205 OX7: четыре заявления сильнее сделанного (включая свежую сироту)
- #1204 OX7: самоопровергающийся счёт в теле коммита #1194
- #1203 OX7: «one fewer test in the binary» опровергается собственным логом
- #1202 OX7: `windows` — та же ставка; SAFETY-посылка опровергнута CI-логом
- #1201 OX7: джоб aarch64 никогда не падал — сломанный фильтр
- #1200 ревью волны OX6 агентом `@ox`
- #1199 КРАСНЫЙ MAIN: тот же тест на miri/aarch64/Linux
- #1198 CI зелёный на `3d6c98f`

## Decisions

- **`Refused` тест сужен до Windows, а не починен** (#1199) — детерминированный механизм требовал Linux-раннера, которого нет; цена (нет покрытия вне Windows) записана карточкой item 92 в том же коммите. Четвёртый аудит затем отверг и мой кандидатный механизм из item 92 как гоночный — план отозван, теперь #1210.
- **#1208 перенесена в конец волны** — правка Status-строк сейчас была бы обесценена пятью коммитами той же волны (класс #1193). Отвергнут вариант «сделать сразу как самостоятельную задачу».
- **#1212: вариант (a) доком, (b) отложить** — так как `DecommitOutcome` уже `#[non_exhaustive]`, добавление `Simulated` не semver-ломающее и не обязано приземлиться до публикации; обязательна только документная половина.
- **Границы файлов между #1211/#1207/#1214** — #1211 владеет содержимым раздела 0.2.0 в CHANGELOG, #1207 только заголовком версии и гвардией, #1214 берёт формулировку HugeTLB готовой. Порядок #1211 → #1207 → #1214. Отвергнут параллельный запуск: три задачи правят один файл.
- **Пятый аудит получил владельца (#1216)** — пункт 5 собственного release-гейта отчёта («новый независимый аудит после исправлений») его не имел; без него волна закрылась бы по самооценке.

## Open questions

- **#1190 — единственный неотвеченный владелец обоих вердиктов NO-GO.** Поддерживаем ли приём HugeTLB-карт с гранулярностью ≠ 2 МиБ (например 1 ГиБ)? Умолчание записано как НЕТ. Разведка установила: «ДА» касается ИСКЛЮЧИТЕЛЬНО приёма чужих карт (`os/unix.rs:1046` жёстко ставит `MAP_HUGE_2MB`, свой путь не может выдать иное); «НЕТ» сегодня не запрет, а умолчание — 1-ГиБ карта проходит 2-МиБ ассерт, и `decommit()` тихо съедает `EINVAL`. Рекомендация: «НЕТ» + три дешёвые записи в контракт, кода не требуется.
- **Схема нарезки индекса (#1217)** — решает агент, но выбор («остаётся ли `CORRECTNESS_OPEN_ITEMS.md` оглавлением») влияет на 161 ссылку против 679 упоминаний; я склоняюсь к сохранению оглавления, но это должно быть записанным решением.

## Repo state

```
?? docs/checkpoints/2026-08-19-2140.md
?? docs/checkpoints/2026-08-19-2216.md
?? docs/checkpoints/2026-08-19-2300.md
```

```
105cf53 docs: task #1209 (SOL2) — commit the fourth independent audit and file its card in the SAME change, which is what items 89 and 90 exist to punish
dc2ecdd test(vmem): task #1206 (OX7) — pin the total item 59a states about itself, fix item 92's card shape, and correct the review's "four loose copies" framing
22a91cc docs(vmem): task #1205 (OX7) — four claims from this wave that are stronger than what was done, including a fresh orphan in the commit that claimed to remove orphans
15ee543 docs(vmem): task #1204 (OX7) — the commit that fixed a self-falsifying count recorded a self-falsifying count in its own body
c402871 docs(vmem): task #1202 (OX7) — the SAFETY comment asserted a property neither OS has, and `windows` is the same bet, not determinism
```

`origin/main` = `dc2ecdd`; local `main` is 1 commit ahead (`105cf53`, unpushed, gate not yet run for it).
