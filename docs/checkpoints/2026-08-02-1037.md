# Checkpoint — 2026-08-02 10:37 [round-32-backlog-in-progress]

## Session summary

This session continues Round 31/32 work on sefer-alloc under a standing `/babygoal` ritual: implement backlog tasks via `@sh` sub-agents, commit between tasks, then (once the backlog is empty) `/checkpoint`, update CHANGELOG, commit all markdown, and run an `@oh` review of all the work. A `babysit` cron (job id confirmed armed earlier this session, fires every `7,22,37,52 * * * *`, session-only) is monitoring the TaskList and resuming stalled work automatically.

The backlog has two origins: (1) 8 tasks (#486–493) filed after personally reading and independently verifying every claim in `docs/reviews/2026-07-31-r31-r32-readonly-review.md` (a P0 soundness bug in `ReservedSmallSegment`, an invalid `large-cache-extended` narrow-timing gate, and the `virgin-zero-skip`/`large-cache-extended` promotion decisions); (2) 12 tasks (#494–505) filed after an `@ox` research agent produced `docs/perf/SPEEDUP_OPPORTUNITY_SURVEY_2026-07-31.md` (13 findings + 1 sub-finding, F1–F13), each independently re-verified by reading source before a task was created.

**#486–494 are all completed and personally zero-trust-verified** — for each, I independently re-read the diff, re-ran the relevant test suite (`cargo test --features production`, consistently 0 failures across ~230+ test blocks each time), and for measurement-heavy tasks (#488, #490, #492/cost-gate) re-ran the checked derive script against the committed raw JSON and confirmed byte-identical reproduction. Along the way I personally found and fixed **four instances of the same recurring bug** (a report's `landing_commit` CSV field filled with a short 7-char SHA instead of the required 40-hex form) in R31-8, R31-10's cost gate, and R32-0 — each fixed as its own small follow-up commit, independent of the sub-agent's own work. Task #493 (tooling hygiene on `verify-gate-report.mjs`/`capture-measurement-identity.mjs`) was found already substantially done but *uncommitted* in the working tree when I picked it up — I verified it (re-ran the verifier, which surfaced the fourth landing_commit bug, in R32-0's CSV), fixed a `.gitignore` gap the new patch-file feature needed, and committed it myself.

**#495 is currently `in_progress`**, worked by a live background `@sh` agent (id `a58f98b03c45262de`, launched this turn, not yet returned) — removing (if genuinely dead code) an asymmetric `stamp_segment_owner()` call in `alloc_small_zeroed_via_magazine`'s magazine-hit arm that plain `alloc`'s equivalent arm explicitly does not pay, and appending a dated correction to `docs/perf/R31_0_VIRGIN_ZERO_SKIP_PRODUCTION_LAYER_GATE.md` noting this as an unstated confound in that report's ON/OFF comparison. Uncommitted working-tree changes already visible (`benches/perf_gate_iai.rs`, `src/registry/heap_core_alloc.rs`) confirm the agent is actively mid-task, not stalled.

**#496–505 remain untouched (`pending`)** — F4 (PerClass repr(C) fix), F1b (bitmap merge), F12 (large-cache header field-write), F9 (Instant::now decay guard), F3 (the missing ≥64-segment macro-bench harness — flagged as the single highest strategic-priority item in the whole survey), F2 (OWN_CACHE_SIZE tuning + Tier-1 oracle), F10 (RemoteFreeRing shadow-head + new producer/consumer harness), F8 (large-cache scan layout), F11 (Windows segment-reservation decomposition gate), and #505 (indexing the survey's dead/negative findings F5/F13 into OPEN_ITEMS.md).

Key standing facts from this round's own findings, worth carrying forward: `production`'s feature composition has NOT changed this round (verified against `Cargo.toml` multiple times) — every runtime win landed so far (#494's realloc fix, and whatever #495 lands) is either in the small always-on hot path or gated behind an already-default feature, never a new opt-in promotion. `virgin-zero-skip` (task #490) is now cost-and-benefit-complete evidence-wise (GO on cost, ~8.2% worst-case relative cost vs 89–98.6% benefit) but is NOT promoted to `production` — that decision is explicitly reserved for the user's own sign-off, same posture task #491 took for `large-cache-extended`'s new `LargeCachePolicy::DiverseTurnover` variant.

This turn was interrupted mid-babysit-tick by the user to run `/checkpoint` — the tick itself had not yet produced its one-line report when interrupted.

## Active goal

No `/goal` Stop hook is separately armed. The operative standing instruction (delivered via `/babygoal`, tracked as a session-scoped Stop-hook condition, not `/goal`) is:

> реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу

(Implement the tasks via `@sh`, commit between tasks. After all work is done: `/checkpoint` [in progress now], update the CHANGELOG, commit all markdown, and launch an `@oh` review agent to check all the work.) This condition is NOT yet fully satisfied — the backlog (#495–505) is not empty, and the CHANGELOG-update / final-markdown-commit / `@oh` review steps have not started.

## TaskList

### in_progress
- #495 Remove alloc_zeroed magazine-hit's stamp_segment_owner call (unjustified vs plain alloc's P4 no-stamp arm) + correct R31-0's ON/OFF asymmetry (F7) — live `@sh` agent `a58f98b03c45262de` working, uncommitted changes to `benches/perf_gate_iai.rs` and `src/registry/heap_core_alloc.rs` already present

### pending
- #496 Add #[repr(C)] + reorder PerClass fields to restore the documented one-cache-line magazine optimization (F4)
- #497 Merge the alloc/magazine bitmaps into one 2-bit-per-granule word (F1b) — Ir-visible, no macro-bench dependency
- #498 Large-cache hit: targeted field write instead of full SegmentHeader rewrite (F12)
- #499 Fix maybe_decay_large_cache's Instant::now() cliff + document its cost on LowHeadroom/Trimmed64MiB profiles (F9)
- #500 Build the missing ≥64-live-segment macro-benchmark harness (F3, strategic priority #1)
- #501 OWN_CACHE_SIZE: raise from 4 to 16/32 + build the Tier-1 hit/miss counter needed to judge it (F2)
- #502 RemoteFreeRing: add a shadow/cached head to remove the cross-core-coherent head read on every push (F10) — needs a new producer/consumer harness
- #503 Large-cache scans: occupancy bitmask + usable_size/seq sidecars instead of walking the 56 B/slot AoS (F8)
- #504 Windows: measure segment reservation cost (2x VA over-reservation, no aligned-reservation fast path) — first Windows-native decomposition gate (F11)
- #505 Index the speedup survey into OPEN_ITEMS.md: narrow item 19's revisit trigger (F5), record F13's negative results, cross-reference F1–F13

### recently completed
- #494 realloc move leg: use dealloc_own_thread_with_base instead of re-deriving base + re-running contains_base (F6) — commit `5d72bc6`, -120 Ir on `realloc_grow`, 4 churn benches byte-exact
- #493 Tooling hygiene: signal-bearing gate verifier + atomic provenance — commit `f3020fd`
- #492 trim_current_thread: passive no-bind TLS lookup + cost/next-burst gate — commit `e6bbc6a` (+ 2 landing_commit follow-ups)
- #491 large-cache-extended: LargeCachePolicy::DiverseTurnover named opt-in policy — commit `45f3d83`
- #490 virgin-zero-skip: LOST-front cost gate, GO on cost — commit `c72a27b` (+ landing_commit follow-up `0b6d5bd`)
- #489 Reopen wrongly-closed large-cache-extended scan-cost item; reconcile ledgers — commit `4ccd18b`
- #488 Re-run R31-3 narrow gate in matched state; NO-GO verdict — commit `4f89723` (+ landing_commit follow-up `db2ec89`)
- #487 Fix R31-3 narrow-gate workload defects — commit `ac7845d`
- #486 Fix P0: bind ReservedSmallSegment to its owning AllocCore — commit `d29a82e`

## Decisions

- Chose to launch each backlog task as a single sequential `@sh` sub-agent (not parallel, not `crush`) per the standing `/babygoal` instruction's explicit "@sh" naming this turn.
- Chose to personally re-verify every sub-agent's work (diff read, test re-run, and for measurement tasks a derive-script re-run against committed raw data) before marking a task complete or moving to the next — this caught 4 real bugs (short-SHA landing_commit) and one already-uncommitted-but-correct body of work (#493) that would otherwise have been silently lost or duplicated.
- For task #491 (large-cache-extended policy), declined to build a process-wide shared budget mechanism (would need its own contention-cost gate with no standing evidence to justify it) in favor of a doc-only linear-worst-case disclosure plus a named `LargeCachePolicy::DiverseTurnover` opt-in variant — an explicit, justified scope-narrowing, not a shortcut.
- For task #494, accepted the realloc/try_promote_to_large fix only after independently re-tracing the full unregister-call-site enumeration in source (not just trusting the sub-agent's citation) — confirmed `dec_live_and_maybe_decommit`'s `live == 0` gate and every Large-segment `unregister` call site's structural disjointness from the Small/medium segment table myself.

## Open questions

- None requiring user input right now — the backlog itself has no open design questions left unresolved by prior task descriptions. The two production-promotion decisions this round's evidence now supports (`virgin-zero-skip`, `large-cache-extended`'s `DiverseTurnover`) are deliberately NOT being acted on without explicit user sign-off, per this project's own standing policy (`docs/perf/OPEN_ITEMS.md`) — not blocking current work, just noted as a future decision point for the user.

## Repo state

```
 M benches/perf_gate_iai.rs
 M src/registry/heap_core_alloc.rs
?? .claude/
?? docs/perf/SPEEDUP_OPPORTUNITY_SURVEY_2026-07-31.md
```

```
5d72bc6 perf(runtime): realloc move leg + try_promote_to_large skip redundant base recompute (R32-3, task #494)
f3020fd build(ci): make verify-gate-report.mjs signal-bearing; fix capture-measurement-identity.mjs's two identity forms (R32-2, task #493)
0b6d5bd docs(perf): fix R32-0's landing_commit -- was a 7-char short SHA, not 40-hex (task #492 follow-up)
58d59d9 docs(perf): fix R31-10 cost-gate's landing_commit -- was a 7-char short SHA, not 40-hex (task #492 follow-up)
ceaca66 docs(perf): fill R32-1's landing-commit SHA placeholder (task #492 follow-up)
```
