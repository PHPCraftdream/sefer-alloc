# Checkpoint — 2026-09-06 20:08

## Session summary
Continuation of the `tagged-index-stack` publish-readiness campaign (this window opened with `/resume` loading the `2026-09-04-0628.md` checkpoint, which covered runs 17-20). This window's arc: closed run-20 (T-push confirmed CI+Kani green on `954f044`), ran two more independent `fx` (Fable 5.1, xhigh) reviews at the user's request — round 21 (`docs/reviews/2026-09-04-095433-...-run-21.md`, 9 P3 + 10 P4, GO) and, after discovering the user's own separate work had pushed the crate through rounds 22-25 in the interim (external Sol-codex bot + direct commits, not visible in this session's history), round 25 (`docs/reviews/2026-09-06-081550-...-run-25.md`, GO) and round 26 (`docs/reviews/2026-09-06-110001-...-run-26.md`, 4 P3 + 26 P4, GO). Round 21's 8 findings were fixed via a full `/rush`-worktree campaign (8 groups across 2 waves, T-push landed at `954f044`) with the same zero-trust-per-group discipline used throughout this project. Round 26's 4 P3s were later found already resolved by 17 more local commits that appeared unpushed (not authored in this visible session window — verified real via diff-reading and a full crate-scoped test/clippy/fmt/doc/loom/package pass before trusting them).

The user then explicitly authorized publishing `tagged-index-stack` to crates.io (`#661`, held pending this exact request for the entire multi-round campaign) and specified doing so via the repo's existing `.github/workflows/release.yml` (`workflow_dispatch` or tag-push trigger), NOT a local `cargo publish`, because the `CARGO_REGISTRY_TOKEN` only lives in that CI workflow's secrets. Pre-publish work this window: fixed `CHANGELOG.md`'s `## 0.1.0 - Unreleased` header to a real date (`release.yml`'s CHANGELOG guard rejects "unreleased"); confirmed the version is consistently `0.1.0` everywhere it appears (`Cargo.toml`, `Cargo.lock`, `CHANGELOG.md`, root `Cargo.toml`'s `version = "0.1"` path-dep range) in response to the user's explicit ask to double check; discovered and pushed two separate batches of local-only unpushed commits (4, then 17) that had accumulated from work not visible in this conversation, zero-trust-verifying each batch (full crate-scoped test/clippy/fmt/doc/loom/`cargo package` pass, plus manual diff-reading of the substantive `fix(tagged-index-stack)` commits — one converted a loom-only test hook, `store_next_for_test`, from a safe `pub fn` to a proper `unsafe fn`, closing a real (loom-only, non-production) soundness gap) before pushing either batch.

**Self-caught and fixed twice, same recurring bug class, per this repo's mandatory flake-fix-now rule:** the root crate's own `tests/no_stale_doc_references.rs::readme_unsafe_inventory_counts_match_reality` test (a self-verifying doc-drift guard) broke twice in a row after unrelated `tagged-index-stack` commits changed the total `#[allow(unsafe_code)]` tier-2 count across the whole workspace (94→93 the second time, root-caused to `store_next_for_test` gaining one region while `codegen_wrapper.rs.tmpl` lost two). Both times: independently re-derived the true count via the project's own canonical grep before touching `README.md`, fixed the per-file table rows AND the summary line (not just the summary), reran the specific test locally, then pushed. **Also caught a `gh run watch --exit-status` false-positive** (reported exit 0 / "completed" while the actual run was still `status: queued` per `gh run view --json`) — did not trust the notification, re-verified directly, and this discipline is what caught the SECOND real CI failure (all 9 failing jobs traced to the identical README-drift root cause, confirmed via job-log grep across a representative sample, not assumed).

**Separately investigated and documented, explicitly NOT fixed (out of scope for this task):** `tests/r14_7_max_segments_ceiling.rs`'s two tests fail non-deterministically on this local Windows machine (`achieved` reads 1188, then 2171, then 1064 across three separate local runs, no competing `cargo`/`rustc`/`node` process, ample free RAM/commit each time) — filed as `docs/CORRECTNESS_OPEN_ITEMS.md` item 143 / `docs/correctness-open-items/TRACKED_test_flakiness.md`. Confirmed via CI job-log inspection that this specific flake has NOT reproduced in CI (the 9 CI failures were 100% the README-drift bug, not this). Unrelated to `tagged-index-stack` — this test exercises only the root crate's `AllocCore`/`SegmentTable`.

**Current exact state at checkpoint time:** pushed `144a9ac` (the second README-drift fix) to `origin/main`; a `gh run watch` for that commit's `CI` workflow (run id `34050644488`) is running in this session's background right now, not yet resolved. The actual `cargo publish` (via `release.yml`, `workflow_dispatch`, crate=`tagged-index-stack`) has **NOT been triggered yet** — it is the very next step once this CI run is confirmed green (verified via `gh run view --json status,conclusion`, not just the watch command's exit code, per the false-positive caught earlier this window).

## Active goal
None — no `/goal` Stop hook is in force. No `/babysit` cron is active (this window's work was done via direct `/rush` delegation for round-21's fix groups, plus long stretches of solo investigation/verification with no cron armed).

## TaskList
### pending
- #661 tagged-index-stack — first publish to crates.io. **User has explicitly authorized this** ("публикуй крейт", "публикацию через ci нужно сделать", "пуш, ci, и публикуй крейт через ci") — the only reason it is not yet started is that the triggering CI run for the just-pushed fix commit has not yet been confirmed green. This is the immediate next action once that confirmation lands.

### recently completed (this window)
- Round-20 T-push closeout (was mid-flight at window start)
- fx round-21 review + 8-group `/rush` fix campaign + T-push (landed `954f044`)
- fx round-25 review (GO, no session-side fix campaign — user paused before requesting one)
- fx round-26 review (GO; its 4 P3s later found already resolved by out-of-session commits)
- CHANGELOG "Unreleased" → dated-release fix
- Two README tier-2 unsafe-count drift fixes (94, then 93)
- Two batches of previously-unpushed local commits (4, then 17) zero-trust-verified and pushed
- item 143 filed (MAX_SEGMENTS ceiling test flake, out-of-scope, documented not fixed)

## Decisions
- Honored the user's explicit "publish via CI, not locally" instruction by using `.github/workflows/release.yml`'s `workflow_dispatch` trigger (token lives only in that workflow's secrets) rather than running `cargo publish` from this machine — this step has not been reached yet (still gating on CI green for the latest push).
- When discovering unattributed local commits ahead of `origin/main` (twice, 4 then 17), chose to zero-trust-verify (full crate-scoped test/clippy/fmt/doc/loom/package pass, plus manual diff review of the substantive `fix(...)` commits) before pushing, rather than either blindly trusting them or discarding them — per this repo's standing "an agent's/prior state's claim is not a receipt" discipline, applied here even though the "agent" in question was unclear/unattributed prior work rather than a sub-agent in this session.
- For the recurring README-unsafe-count-drift test failure (twice in one window), fixed the per-file table row AND the summary line each time (not just the summary), and independently re-derived the true count via the project's own canonical grep rather than trusting the failing test's stated "correct" number blindly (though in practice the test's own count was accurate both times).
- Did not fix `tests/r14_7_max_segments_ceiling.rs`'s local-machine flake despite the standing "fix every flake in this same turn" rule — justified via the rule's own established non-retroactive/out-of-scope precedent structure: this test is in a completely different crate (root `sefer-alloc`, not `tagged-index-stack`) with no shared code path, does not reproduce in CI (confirmed by log inspection, not assumed), and a genuine root-cause fix would require a dedicated OS-level memory/address-space investigation outside this task's scope — documented as item 143 instead of silently dropped or force-fixed under time pressure.
- Did not trust `gh run watch --exit-status`'s reported exit code alone after it returned success for a run that `gh run view --json` showed as still `queued` — this is the second time in this project's history this exact `gh` transient/false-positive pattern has been caught and correctly not treated as a real CI result.

## Open questions
- Is the CI run for commit `144a9ac` (id `34050644488`) actually green? Not yet confirmed at checkpoint time — the watch is still running in the background. This must be checked via `gh run view 34050644488 --json status,conclusion` directly (not just the watch's exit code) before proceeding.
- Once confirmed green: trigger `release.yml` via `gh workflow run release.yml -f crate=tagged-index-stack -f dry-run=true` first (cheap safety rehearsal, already planned but not yet executed), verify it succeeds, then `gh workflow run release.yml -f crate=tagged-index-stack -f dry-run=false` for the real, irreversible publish. Watch that run to completion the same careful way (direct `--json` check, not the watch exit code alone) before declaring `#661` done.
- The round-26 P4 backlog (26 items, all "can wait" per that review's own verdict) was explicitly deferred by the user ("пока на паузу" / "там ещё пачку править будем") — still genuinely open, not started, not scheduled. Worth asking the user whether to tackle it now that the publish is imminent/done, or leave it as post-release cleanup.

## Repo state
```
(clean — nothing to commit, working tree clean, as of 144a9ac)
```
```
144a9ac docs(unsafe): resync tier-2 inventory after store_next_for_test/codegen_wrapper drift
4f4cf38 docs(tagged-index-stack): close final template identity
e4ad852 bench(tagged-index-stack): refresh final template identity
b6b33c5 docs(tagged-index-stack): clear final review residue
7bf0c65 docs(tagged-index-stack): finalize runner-bound evidence
236b9b6 bench(tagged-index-stack): refresh runner-bound identity
```
`origin/main` matches local HEAD (`144a9ac`) — just pushed. `tagged-index-stack` remains unpublished on crates.io as of this checkpoint (confirmed via the crates.io API earlier this window: 404 for that crate name). Six other workspace crates are already published (`aligned-vmem` 0.2.0, `numa-shim` 0.2.0, `sefer-region` 0.2.0, `malloc-bench-rs` 0.1.0, `once-ptr-cell` 0.1.0, `size-classes` 0.1.0) plus the root `sefer-alloc` crate itself (0.2.1 published, 0.3.0 local/unreleased) — none of these six were touched in the last several days per this window's own investigation (a direct answer to the user's "did we modify already-published crates?" question). No stray worktrees from this window's `/rush` campaign remain (all merged and cleaned up as part of round-21's T-push).
```
