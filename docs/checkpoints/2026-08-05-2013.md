# Checkpoint — 2026-08-05 20:13 [wave-3-closing]

## Session summary

This session executed the `/babygoal` ritual across three successive remediation waves on `sefer-alloc`, each triggered by an `@oh`-style readonly review of the prior wave's own work: implement → checkpoint → review → repeat. Wave 1 (tasks #547-554) closed 8 findings (F1-F8) from a closing review of Round 34. Wave 2 (tasks #555-570) closed 16 more findings (Sol-F1-F7 + G1-G7 + a bonus) from two parallel independent reviews of wave 1, including a genuine user decision on version/CHANGELOG policy (0.3.0 not yet released, consolidate `[Unreleased]` into it) and a real scripted `git rebase -i` (G1/task #555) rewording 3 commit messages, verified byte-identical content before/after.

Wave 3 (tasks #571-578, just completed this session) closed 8 findings (H1-H8) from a third review that critically found 2 of wave 2's own 3 "P1 release blockers" were only PARTIALLY closed: Sol-F1's `internals`-gating fix covered only 3 of 9 files with `AllocCore::dbg_*` methods (H2 closed the remaining 6, gating 31 more methods + 26 transitive delegation call sites), and Sol-F2's CHANGELOG "consolidation" had deleted a header without moving content, leaving Round 34-12's content orphaned with no enclosing version header (H3 fixed this structurally). H1 fixed 2 pre-existing compile failures; H4 fixed 10 stale SHA citations left by G1's rebase; H5 cross-filed 2 documented residuals into the correctness index; H7 added a proper `### BREAKING CHANGE` CHANGELOG entry; H8 decided (recorded, not executed) to leave a `fix(perf)` prefix as-is rather than rebase; H6 redefined `R34_MANIFEST.md`'s scope (frozen at Round 34 proper) into 3 new per-wave manifest files, closing a recurring 38→43→(would-have-been-74) under-count pattern permanently.

After H1-H8 landed, I ran the FULL `npm run check` pipeline (not just each task's own scoped verification) and it caught 3 more real bugs my own narrower per-task checks had missed: (1) H2's `internals` gating broke 6 `examples/` + 1 `benches/` target whose independent `required-features` were never updated (a category invisible to a library-only `cargo check`), plus one target missing a PRE-EXISTING `large-cache-extended` requirement its own doc comment already documented but never encoded — fixed and folded into H2's own commit via amend (before anything stacked on it). (2) H1's runtime stack-pressure test was vacuous under `--all-features` (asserted unconditionally, contradicting its own doc comment's claim of "non-vacuous coverage under ALL configurations") — fixed by mirroring the compile-time guard's exact `#[cfg]` exclusion, plus a signed-arithmetic fix for a related overflow. (3) An unrelated, genuinely pre-existing flaky test (`segment_table_contains_base_tier1_counters.rs`, last touched in Round 34, confirmed via git blame — not caused by this session) was serialized with the project's established `TEST_LOCK` pattern.

The only remaining red step in `npm run check` is the local `verify-commit-prefixes` check on `43115cf`/`5c1142f` — confirmed as pre-existing, already-documented debt (item 21, filed by a prior wave, deliberately NOT rebased due to disproportionate risk) that predates this whole session, confirmed not a new regression (`git merge-base --is-ancestor` — not an ancestor of `origin/main`; nothing has been pushed all session), and confirmed CI's own `commit-prefix-lint` job never runs on a direct push to `main` (PR-scoped only, by design, per that job's own comment in `ci.yml`). Every other pipeline step — fmt, all 5 clippy rows, all 4 test-feature combos, both compile-fail oracles, `verify-perf-gate-stubs`, `verify-gate-report`, and `npm run iai` — is green.

Just wrote the wave-3 CHANGELOG entry (commit `b57f988`). Remaining ritual steps per `/babygoal`: this checkpoint (in progress), commit all markdown (review reports stay uncommitted per established convention — only `docs/checkpoints/*.md` and this checkpoint itself need `git add`), then launch an `@oh` review agent to check all of wave 3's work. The user separately asked "потом сделай пуш" (then push) — to be done AFTER the `@oh` review, addressing any findings first, per the same review-before-push discipline this whole session has followed.

## Active goal

None — governed by the `/babygoal` ritual's standing Stop-hook condition (implement via `@sh`/`/crush`, commit between tasks, checkpoint+changelog+commit-markdown+`@oh`-review when all work is done), not a `/goal` condition.

## TaskList

### pending
- #553 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), switch back with `/crush-fallback clear` (sentinel; do not complete)

### recently completed (all wave 3, this session)
- #571 H1 [P1]: fix two pre-existing compile failures breaking `npm run check`
- #572 H2 [P1]: Sol-F1 only partially closed — gate remaining `AllocCore::dbg_*` methods
- #573 H3 [P1]: Sol-F2's CHANGELOG "consolidation" left content with no enclosing header
- #574 H4 [P2]: fix 10 pre-rebase SHA citations after G1's rebase
- #575 H5 [P2]: cross-file Sol-F5/Sol-F6's residuals into the correctness index
- #576 H6 [P3]: redefine `R34_MANIFEST.md`'s scope instead of a 3rd extension
- #577 H7 [P3]: add a BREAKING CHANGE CHANGELOG entry for the internals narrowing
- #578 H8 [P4]: decide (record, don't rebase) `dbb4016`'s prefix

(All 3 waves' tasks — #547 through #578 — are now `completed`. No `in_progress` or blocked `pending` tasks remain besides the persistent sentinel #553.)

## Decisions

- H6: redefine `R34_MANIFEST.md`'s scope (frozen at Round 34 proper) into per-wave manifest files, rather than a 3rd perpetual extension — closes a recurring staleness pattern structurally.
- H8: leave `dbb4016`'s `fix(perf)` prefix as-is (accepted, lint-passing precedent now repeated by H2's own `25d6ac4`) rather than rebase — matches CLAUDE.md's own non-retroactive posture for the R30-12 taxonomy rule.
- The 2 real compile/test bugs `npm run check`'s full-matrix run surfaced after H1/H2 landed were fixed as NEW follow-up commits (or folded via amend when still the tip, before anything stacked on it) rather than silently left broken — matches the session's explicit priority instruction to fix all red things first.
- The `43115cf`/`5c1142f` commit-prefix-lint failure is accepted as pre-existing, already-documented debt (not re-litigated) — confirmed via `git merge-base --is-ancestor` that it predates this session and confirmed CI's own job never runs on a direct push to `main`, so it is not a real push blocker.

## Open questions

None outstanding — the one genuine user-decision point this whole multi-wave effort needed (Sol-F2's version/CHANGELOG policy) was already resolved in wave 2 via `AskUserQuestion`.

## Repo state

```
?? .claude/
?? docs/checkpoints/2026-08-05-1730.md
?? docs/reviews/2026-08-03-round33-readonly-review.md
?? docs/reviews/2026-08-04-r32-r33-global-bench-readonly-review.md
?? docs/reviews/2026-08-04-release-stabilization-audit.md
?? docs/reviews/2026-08-05-fallback-release-readonly-review.md
?? docs/reviews/2026-08-05-r34-review-remediation-readonly-review.md
?? docs/reviews/2026-08-05-round34-readonly-review.md
?? docs/reviews/2026-08-05-sol-release-readonly-review.md
?? docs/reviews/2026-08-05-sol-remediation-readonly-review.md
```
```
b57f988 docs: CHANGELOG entry for wave 3 release-readiness remediation (tasks #571-578)
28663e4 docs: extend R34_REMEDIATION_3_MANIFEST.md with H6's own commit + 3 fallout fixes
2f16ba6 fix: serialize a pre-existing flaky test on process-wide tier1 counters
0d23e7f fix: correct H1's stack-pressure runtime test — was vacuous under --all-features
e886ea4 fix(perf): close H2's remaining fallout — 6 examples + 1 bench need internals
db63aed docs: close H6 — redefine R34_MANIFEST.md's scope instead of a 3rd extension
800ee86 docs: close H8 — record the decision to leave dbb4016's fix(perf) prefix as-is
5c17cc3 docs: close H7 — add a BREAKING CHANGE entry for the internals surface narrowing
```
