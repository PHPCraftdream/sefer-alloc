# Checkpoint — 2026-08-25 10:52 [numa-shim campaign: 21st/22nd review waves closed, CI pending on push 774716e]

## Session summary

Continuation of the numa-shim `[audit-vmem]` publication-readiness campaign under the standing `/babygoal` Stop-hook condition (verbatim, still in force): *"реализуй задачи по numa-shim с помощью агентов /crush & worktrees, в конце (когда решишь все таски) проведи ревью на готовность крейта к релизу с помощью агента @oh — после ревью заведи таски и снова решай их. Делай такие циклы пока ревью не скажет GO на публикацию и остановись перед публикацией."* This session picked up mid-campaign after the #1336-#1342 wave (nineteenth review, item 111; twentieth review, item 112) had already been pushed and CI-confirmed green.

**Twenty-first review launched, item 114 filed and fully remediated.** Launched the twenty-first independent review (`@oh`, task #1344) on the post-#1336-#1342 revision (`5ff29cb`). Verdict NO-GO: no P1, two P2s (both incomplete halves of the very P2s the prior wave landed — a "negative control" test in `policy_oracle_linux.rs` still hard-panicked in seccomp containers because task #1336 fixed only the sibling preflight call site, not this one; `tests/readme_examples.rs`, created by the prior wave, reintroduced the exact arch-blind panic-on-`UnsupportedArchitecture` defect an earlier task had just removed from `smoke.rs`), plus four P3s (one — a self-contradictory race-window comment — a genuinely NEW defect introduced by the prior wave's own docs-honesty fix). Filed as item 114.

**Twenty-second review arrived unprompted, item 113 filed.** While the twenty-first review was still running, a second independent review (Sol-codex, self-titled "прогон 18") appeared directly in the working tree — same arrival pattern as items 110/112 earlier in the campaign, except this time it also showed up ALREADY COMMITTED to `main` (commit `19ba84d`, same git author identity as this session's own user — investigated, confirmed benign, most likely a parallel Claude Code session/automated pipeline the user runs; not disruptive, no conflicts). The user explicitly asked me to study it and file tasks. It found two P2s: sparc/sparc64's `O_CLOEXEC` constant was left disabled (`0`) even though the correct UAPI value (`0x400000`) was already documented in the very comment right above it; and the same README/`NodeId::new` panic-on-`UnsupportedArchitecture` defect the twenty-first review also independently found. Filed as item 113 — the two reviews' overlapping-but-not-identical finding sets are documented as cross-references in both cards (item 113's F1/F2 converge with item 114's P3-1/P2-2; item 114 additionally found P2-1, P3-2, P3-3, P3-4 that item 113 did not).

**Crush hit a provider peak-hours refusal; `/crush-fallback` engaged.** The first fallback-eligible task (#1345, the sparc O_CLOEXEC fix) failed with `crush`'s provider (`zai`) refusing during its 08:00-12:00 peak-hours window, `RESUME AT: 12:00`. Invoked `/crush-fallback` — asked the user which agent to use (initially mislabeled `sl` as "Sonnet high" in an `AskUserQuestion`, the user caught this: `sl` is actually Sonnet **low** effort; user confirmed to proceed with `sl` for ALL fallback tasks anyway, not just small ones). Armed the fallback per the skill's protocol: a persistent `crush-fallback state` marker task (#1349, `STATUS: active`, `AGENT: sl`, `UNTIL: 2026-08-25T12:00:00+02:00`) plus a one-shot durable cron (`e76c4888`) that will auto-revert the marker to `armed` at 12:00 and instruct routing back through `/crush` — the cron has NOT fired yet as of this checkpoint.

**All four remediation tasks for items 113+114 executed via the `sl` fallback agent, each personally zero-trust reviewed exactly as if `/crush` had produced them:**
- **#1345** (sparc/sparc64 `O_CLOEXEC` → `0x400000`, closing item 113's F1 + item 114's P3-1 with one fix): this session's host turned out to have the `sparc64-unknown-linux-gnu` rustc target actually installed (unlike every earlier sparc-touching task in the campaign), so this is the FIRST sparc-touching task with a genuine cross-compile check, not just structural reading.
- **#1346** (README/`NodeId::new` examples made genuinely panic-free via the crate's own already-established `.ok().or_else(...)` idiom, closing item 113's F2 + item 114's P2-2/P3-3/P3-4 together: also fixed a stale non-compiling snippet still published in `CHANGELOG.md`, and added a missing CI sentinel for `tests/readme_examples.rs`).
- **#1347** (the negative control's `get_mempolicy_addr` call site in `policy_oracle_linux.rs` now classifies EPERM/ENOSYS the same way the preflight does — closing item 114's P2-1, the review's own top-priority finding).
- **#1348** (fixed the self-contradictory race-window comment in `smoke.rs` — both twins now say the same, mutually-consistent, honest thing about what actually protects the test).

Every task followed the established disciplined loop even under fallback: detailed prompt file written to the worktree, `Agent({subagent_type: "sl", ...})` dispatch instead of `crush run`, personal zero-trust diff read (not trusting the agent's own summary), independent re-run of the exact verification commands, merge with `--no-ff` and a detailed commit message, item-card Status update refreshed IN PLACE, separate `docs:` follow-up commit.

**Pushed and CI launched, not yet confirmed.** After all four tasks landed, ran the full `npm run check` pre-push gate (background, ALL GREEN), asked for and received explicit push authorization ("Да, пуш"), pushed the resulting 15-commit wave (`5ff29cb..774716e`). Both CI workflows (Kani verification, main CI) are running on the landing SHA `774716e` as of this checkpoint — NEITHER has been confirmed green yet; both `gh run watch` background tasks were still in progress when `/checkpoint` was invoked.

**Standing discipline maintained throughout:** never pushed without explicit fresh authorization (asked twice this session, for the #1336-#1342 wave earlier and again for this #1345-#1348 wave — did not assume either carried over); never bumped versions; the 0.2.0 bump (task #1262-class) remains deliberately un-actioned per standing owner instruction. `babysit` remained armed throughout (job id not re-confirmed this segment but was active in the prior session and presumably still is), firing its usual tick prompts, all answered with brief one-line status per its protocol.

## Active goal

The standing `/babygoal` Stop-hook condition (verbatim): *"реализуй задачи по numa-shim с помощью агентов /crush & worktrees, в конце (когда решишь все таски) проведи ревью на готовность крейта к релизу с помощью агента @oh — после ревью заведи таски и снова решай их. Делай такие циклы пока ревью не скажет GO на публикацию и остановись перед публикацией."* Still NOT satisfied — no review has yet returned an unconditional GO (reviews seventeen through twenty-two have all returned NO-GO; the wave closing items 113+114 is pushed but CI is not yet confirmed green, and the twenty-third review has not yet been launched).

## TaskList

### in_progress
- #1350 [numa-shim] Confirm npm run check green after #1345-#1348 wave, request push authorization, launch 23rd review — gate confirmed green, authorization received, PUSHED (`774716e`); CI confirmation and the 23rd-review launch are the two steps still outstanding

### pending
- #1349 crush-fallback state (persistent marker — do not complete): `sl` — ACTIVE (peak-hours trigger), auto-reverts to `armed` when cron `e76c4888` fires at 12:00
- #657 numa-shim — verify/prepare for crates.io republish
- #659 racy-ptr-cell — first publish to crates.io
- #660 size-classes — first publish to crates.io
- #661 tagged-index-stack — first publish to crates.io
- #662 Root sefer-alloc: design note for applying bench-scale-tool alongside criterion/iai
- #763 Root sefer-alloc: implement bench-scale-tool per the approved design (blockedBy: #662)
- #1091 [perf-regress] Найти и устранить регрессию ~10-13% Ir в диапазоне 42d8d223..42d42061 (item 56)
- #1299 [numa-shim] T8+T9+T12+T14: CI-hardening follow-up set — explicitly deferred to AFTER the release

None of the pending items besides #1349/#1350 are part of the active numa-shim readiness cycle; #1299 is deliberately deferred and the rest are pre-existing unrelated backlog.

### recently completed (this session, most recent first)
- #1348 [numa-shim] P3-2 (21st review): race-window comment self-contradiction in smoke.rs
- #1347 [numa-shim] P2-1 (21st review): negative control in policy_oracle_linux.rs still panics in containers
- #1346 [numa-shim] F2 (22nd review, converges 21st P2-2/P3-3/P3-4): README/NodeId examples panic-free + CHANGELOG snippet + CI sentinel
- #1345 [numa-shim] F1 (22nd review, converges 21st P3-1): sparc/sparc64 O_CLOEXEC → 0x400000
- #1344 [numa-shim] Twenty-first independent readiness review (@oh) — filed item 114
- #1343 [numa-shim] Confirm npm run check green after #1336-#1342 wave, push, launch 21st review
- #1342 through #1336 — full remediation of the nineteenth/twentieth review waves (items 111/112), all closed and pushed earlier this session

## Decisions

1. **Adopted `/crush-fallback` with the `sl` (Sonnet, LOW effort) agent for all four remediation tasks in this wave**, per the user's explicit confirmation after I mislabeled `sl` as "high effort" in an `AskUserQuestion` — the user caught the error and chose to proceed with `sl` anyway for every fallback task, not just the small ones. Every fallback-produced diff was still personally zero-trust reviewed to the same standard as a `crush`-produced one.
2. **Filed items 113 and 114 as separate cards rather than merging them**, since they are two genuinely independent reviews (different authors/methodologies) that happen to converge on some findings and diverge on others — cross-referenced explicitly in both cards' text rather than picking one as canonical.
3. **Fixed sparc/sparc64's `O_CLOEXEC` by APPLYING the correct known value (`0x400000`) rather than leaving the hardening disabled** — reversing an earlier task's (#1339) "disable rather than risk a wrong bit" choice, which was sound only while the correct value was unverified; both this and the earlier decision are recorded in the CHANGELOG as sequential, not contradictory.
4. **Made the README's first example and `NodeId::new`'s "Ergonomic path" snippet genuinely panic-free** (via `.ok().or_else(...)`) rather than merely accepting the documented-panic-as-known-property posture an earlier task had left in place — matching the crate's own already-established best-effort idiom used two sections away, rather than inventing a new pattern.
5. **Investigated but did not disrupt an apparently-concurrent external process** that committed the twenty-second review directly to `main` under the same git identity mid-session — confirmed no conflicts, proceeded with the campaign's normal filing/remediation flow on top of it.

## Open questions

1. **Are both CI runs for landing SHA `774716e` confirmed green?** NOT YET — both `gh run watch` background tasks (Kani verification `32828561028`, main CI `32828560947`) were still in progress when this checkpoint was written. This is the immediate next thing to confirm (via direct `gh run view --json jobs`, not just the watch exit code) before proceeding to the next review.
2. **Once CI is confirmed green: launch the TWENTY-THIRD independent readiness review** (already the next step of task #1350) — the direct, still-unsatisfied requirement of the standing `/babygoal` condition. Both item 113 and item 114 are now fully remediated.
3. **Crush-fallback remains ACTIVE** — do NOT manually re-attempt `crush run` before the armed cron (`e76c4888`, fires at 12:00 local) reverts the marker task (#1349) to `armed`. If the twenty-third review surfaces new remediation tasks before 12:00, they should also route through `sl` per the user's standing confirmation, unless the user says otherwise.
4. **Task #1262-class (0.2.0 version bump) remains deliberately un-actioned** — must NOT be re-filed or acted on without a fresh, explicit owner signal, even if the twenty-third review re-flags version/CHANGELOG staleness.
5. Standing, unresolved from prior checkpoints: `.claude/worktrees/agent-*` directories (unrelated to this campaign, other agent sessions) remain untouched; this campaign's own `../sefer-alloc-numa-NNNN` worktrees have all been `git worktree remove --force`'d immediately after each merge this session (no accumulation this segment, unlike earlier in the campaign).

## Repo state

```
?? .check-1321-wave.log
?? .check-1334-wave.log
?? .check-1335-retry.log
?? .check-1342-wave.log
?? .check-1348-wave.log
?? docs/checkpoints/2026-08-24-1556.md
?? docs/checkpoints/2026-08-24-2034.md
?? docs/checkpoints/2026-08-25-0103.md
```
(the checkpoint file this write itself creates is not yet listed above, per the tool's own snapshot-before-write timing; scratch `.check-*.log` files and prior checkpoints are the only untracked content, matching the repo's own `.gitignore` conventions for scratch logs.)

```
774716e docs: task #1348 follow-up — item 114 fully closed, entire 19th-22nd review wave remediated
96da02b merge: task #1348 — fix self-contradictory race-window comment in smoke.rs (twenty-first review P3-2)
44264ac fix(numa-shim): task #1348 — fix self-contradictory race-window comment in smoke.rs, introduced by task #1340 (twenty-first review P3-2)
5bb2d9b docs: task #1347 follow-up — item 114's P2-1 disposition refreshed in place
5f22166 merge: task #1347 — negative control's get_mempolicy_addr now classifies EPERM/ENOSYS as environment skips (twenty-first review P2-1)
```

`origin/main` = `774716e85f4a3bb735610fbd9242878df04c608d` (confirmed via `git fetch` + `git rev-parse origin/main` right after the push in this session) — local `HEAD` matches `origin/main` exactly, nothing unpushed. CI on this exact SHA is IN PROGRESS, not yet confirmed green (see Open Question 1).
