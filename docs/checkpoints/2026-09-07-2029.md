# Checkpoint — 2026-09-07 20:29

## Session summary

This session continued the `sefer-alloc` `globalalloc-model` publish-prep campaign, resuming an ongoing multi-round independent-review-fix cycle with a second reviewer identity ("Sol-codex", the same convention used elsewhere in this project for a parallel AI tool). At session start, `globalalloc-model` had just closed Sol-codex review round 3 (commit `2821b64`, fixing a fill-pattern shift collision plus two doc/CI accuracy items, three perf follow-ups explicitly deferred pending measurement) and I had just finished writing and committing a session checkpoint (`6c6b0d2`).

The user then surfaced Sol-codex review round 4 (`docs/reviews/2026-09-07-145650-globalalloc-model-review-sol-codex-run-4.md`, commit `0a12b94`) via `/babygoal`, asking me to study it, create tasks, and solve with `/rush`. I read the review: verdict P0-P2=0, P3=1, P4=5. The one P3 was a genuinely new collision class in round 3's own hash-mixed `pattern_byte` formula — the u32-to-u8 truncation was not injective by fill-identifier even at a FIXED offset (e.g. `pattern_byte(5,0) == pattern_byte(30,0)`), so a wrong-source realloc copying a short prefix from an unrelated, never-reused, unshifted live block could pass undetected. Two of the five P4s were new doc/CI-accuracy findings (a "period 256" claim that was never true for the actual truncate-after-mixing implementation, repeated in 4 places; a docs.rs CI job that built `--all-features` instead of the crate's actual narrower `[package.metadata.docs.rs]` feature list after round 3 added a third feature). The other three P4s were the SAME three perf follow-ups round 3 had already deferred (per-byte fill/verify cost, O(M²) overlap scan, boxed size-strategy allocations) — the review explicitly re-flagged them as still-pre-existing, not new regressions.

Followed the full `/babygoal` contract for this round: investigated (read the review in full — domain otherwise already well understood from this session's prior rounds), chose "single context" execution strategy (the work is tightly coupled around one function and its cross-file documentation, same shape as round 3's fix), created one task (#1919), armed `/babysit` (`CronCreate` job `a5f6946a`, confirmed via `CronList`), then delegated to a single `/rush` agent in an isolated worktree (`worktrees/globalalloc-model-run4-fix`). The agent's fix (per the review's own recommendation): make `pattern_byte`'s offset 0 return the raw fill identifier directly (mutually unique across all 255 valid ids, never confused with zero) instead of hash-mixing it, keeping the hash for offsets ≥ 1 — since `drive` always clamps sizes to ≥ 1, a realloc's preserved prefix always includes offset 0, so this one change closes the ENTIRE short-prefix collision class, not just the review's two illustrative examples. Consolidated the "period 256" description to one canonical, detailed doc comment (on `pattern_byte` itself) with three short summaries elsewhere referencing it. Added a "Build EXACT docs.rs feature set" CI step alongside the existing all-features step (matching this project's own pre-existing CLAUDE.md rule for exactly this defect class, the `aligned-vmem` precedent). I then did full zero-trust review: read every diff hunk, independently re-derived the reported `pattern_byte` arithmetic in a standalone Rust program (all values matched), reran the complete verification battery (fmt/test across 5 feature combos/clippy/both doc builds/actionlint) in both the worktree and the primary checkout, and personally reproduced the counterfactual — temporarily removing the offset-0 special case made the new regression test fail to panic (bug reproduces), confirming the fix is load-bearing. Committed as `b3a28e4`, worktree and branch removed, babysit cron self-deleted by me (remaining pending tasks #1911/#1912 are an unrelated older queue, not part of this round, so I did not let the tick auto-pick them).

The user then asked why the three P4 performance items keep getting deferred, and after my explanation, asked me to actually measure them and look for a simple fix. This was genuinely investigative (not a pre-specifiable delegation), so I did it myself directly rather than delegating: built `examples/perf_probe_p4_measurements.rs`, a throwaway-turned-committed probe measuring all three. P4-3 (per-byte cost) and P4-4 (overlap scan): both measured NO-GO — absolute costs stay negligible (single-digit ns/byte; a few ms even at the crate's own most adversarial reachable peak-live-count of 2048) within the crate's actual designed usage envelope, so no structural fix was pursued for either. P4-5 (boxed size-strategy): measured GO — an isolated baseline comparison found ~154 avoidable allocations and roughly a 2x generation-time overhead per 200-op stream draw, entirely attributable to `size_strategy`'s `.boxed()` calls (confirmed `TupleUnion`, the weighted-arm case, is ALREADY non-boxing by proptest's own design — the boxing was purely to unify it with a bare `RangeInclusive<usize>` into one return type). Implemented a hand-rolled `SizeStrategy`/`SizeValueTree` enum that delegates to proptest's own already-correct implementations (no new shrink logic, so no risk to the zero-weight-arm-excluded-during-shrinking invariant), reducing measured cost to ~1.585 allocs/op (from ~2.336 with boxing). Added a permanent regression test (`tests/size_strategy_avoids_boxed_value_trees.rs`) using a counting `#[global_allocator]`, calibrated against both states. That test crashed under local Miri on Windows with a Stacked Borrows violation traced to std's own Windows allocator backend (`std::sys::alloc::windows`'s Header-lookback pointer arithmetic) — unrelated to my code (the test body itself passed before the unrelated crash during the `test` harness's own teardown) and specific to the Windows target this crate's Miri CI (which runs on `ubuntu-latest`) never exercises; gated the file `#![cfg(not(miri))]` (an existing convention in this crate) and added it to both of `ci.yml`'s miri multi-target run lines to satisfy that job's own drift-check script, then verified locally it compiles to zero tests and does not crash under a full 7-target Miri run matching CI's exact command. Also cleaned up two clippy lints (`large_enum_variant`, silenced deliberately with a documented reason since boxing would undo the whole fix; `type_complexity`, fixed with type aliases). Personally reproduced the counterfactual twice (revert to `.boxed()` → new test fails at exactly the calibrated 2.3357 allocs/op). Committed as `f66b9f0`.

No `/loop`/`/babysit`/other timer is currently active (the round-4 babysit cron was self-deleted once task #1919 completed; no cron was armed for the P4-measurement work since the user did not invoke `/babygoal` for it).

## Active goal

None currently active (no `/goal` Stop hook is in force).

## TaskList

### pending
- #1911 proc-memstat — prepare for crates.io publish (0.3.0 companion)
- #1912 proc-probe — prepare for crates.io publish (0.3.0 companion) (blockedBy: #1911 — real path-dependency on proc-memstat's actual publish)

### recently completed
- #1922 globalalloc-model P4-5: measure BoxedStrategy/heap-alloc overhead in strategy.rs — GO, implemented and landed
- #1921 globalalloc-model P4-4: measure overlap-scan O(M²) worst case — NO-GO, measured negligible
- #1920 globalalloc-model P4-3: measure per-byte fill/verify (pattern_byte) cost — NO-GO, measured negligible
- #1919 globalalloc-model: fix Sol-codex review run-4 findings (P3-1 fill-id collision + P4-1/2 doc/CI)
- #1918 globalalloc-model: fix Sol-codex review run-3 findings (P3-1 pattern collision + P4-1/2/3)
- #1917 globalalloc-model: fix P3-1/P3-2 + safe P4s from Sol-codex review run 2

## Decisions

- Chose "single context" (one `/rush` delegation) over further task decomposition for round 4's fix, since the work (P3-1 + two doc/CI P4s) is tightly coupled around one function's cross-file documentation — matches round 3's precedent, and a single leaf task under `/babygoal` is legitimate when the work is genuinely one coherent chunk.
- Chose to measure the 3 deferred P4 perf items personally (not delegate to `/rush`) since "measure and decide" is open-ended investigation, not a pre-specifiable fix — matches this repo's own investigation-pattern guidance (gather evidence before editing).
- Chose NOT to restructure P4-3 (byte-oracle cost) or P4-4 (overlap scan) despite real, measurable per-call/per-comparison overhead, because the ABSOLUTE cost stays negligible within the crate's own actual designed usage envelope (bounded stream lengths/peak-live-counts) — a real measured NO-GO, not a deferral.
- Chose a hand-rolled enum `Strategy`/`ValueTree` over `BoxedStrategy` for P4-5 specifically because `TupleUnion` (the only non-trivial arm) is already non-boxing by proptest's own design, so a pure-delegation enum (no new shrink logic) carries zero correctness risk while removing 100% of the identified avoidable allocations — confirmed by measurement, not assumed.
- Chose to gate the new allocation-counting regression test `#![cfg(not(miri))]` rather than either (a) dropping the permanent test entirely or (b) trying to work around the Miri-Windows crash directly, since the crash is inside std's own platform-specific allocator backend (unrelated to this crate's code), the crate's actual Miri CI runs on Linux (which has no such backend), and Miri's own allocator instrumentation makes host-allocation counting meaningless under Miri regardless of platform.

## Open questions

- Should `globalalloc-model`'s local commits (11 ahead of `origin/main`, ending at `f66b9f0`) be pushed now? Not yet requested by the user.
- Should a follow-up Sol-codex review (run 5?) be expected/invited, or is the crate now considered publish-ready pending the user's own judgment? No review run 5 has appeared as of this checkpoint.
- Tasks #1911 (`proc-memstat`) and #1912 (`proc-probe`, blocked by #1911) are still pending from before the `globalalloc-model` review-cycle detour — the user has not yet indicated whether to resume that queue next or continue iterating on `globalalloc-model`.
- The `docs/perf/*.md` formal gate-report convention (raw logs, summary CSVs, immutable source identity) was deliberately NOT applied to the P4-3/4/5 measurement work, since that convention is scoped in CLAUDE.md to the ROOT crate's `production`-feature promotion gates, not this separately-published test-harness crate — worth confirming this scoping judgment holds if a future round wants a more formal record of the P4-3/P4-4 NO-GO numbers.

## Repo state

```
(clean — no output from `git status --short`)
```
```
f66b9f0 perf(globalalloc-model): measure the 3 deferred P4 perf follow-ups, land the one that pays off
b3a28e4 fix(globalalloc-model): close P3-1 fill-id collision and P4-1/2 from Sol-codex review run 4
0a12b94 docs(reviews): globalalloc-model Sol-codex round 4
2821b64 fix(globalalloc-model): close P3-1 pattern-collision and P4-1/2/3 from Sol-codex review run 3
810287f docs(reviews): globalalloc-model Sol-codex round 3
```
Local `main` is 11 commits ahead of `origin/main`, 0 behind. Not pushed.
