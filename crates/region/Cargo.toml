[package]
name = "sefer-region"
version = "0.2.0"
edition = "2021"
rust-version = "1.88"
license = "MIT OR Apache-2.0"
description = "Typed, generational handle-addressed store over slotmap — zero own unsafe, no C/C++, no_std + alloc capable."
readme = "README.md"
repository = "https://github.com/PHPCraftdream/sefer-alloc"
homepage = "https://github.com/PHPCraftdream/sefer-alloc/tree/main/crates/region"
documentation = "https://docs.rs/sefer-region"
keywords = ["slotmap", "arena", "handle", "generational", "no-std"]
categories = ["data-structures", "memory-management", "no-std"]

# docs.rs: render with all features so SyncRegion appears with the std feature badge
[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[features]
default = ["std"]
std = ["slotmap/std"]

[dependencies]
slotmap = { version = "1", default-features = false }

[lints.rust]
unsafe_code = "forbid"
# sefer-region does not use the loom or kani build-time cfgs (those are workspace-
# only, for the main crate's concurrency harnesses and proof harnesses), so we
# inherit the workspace's unexpected_cfgs policy with an empty check-cfg.
# The docsrs cfg is well-known (recognized by docs.rs builds) and does not need
# to be listed here.
unexpected_cfgs = { level = "warn", check-cfg = [] }

[dev-dependencies]
# Fixed-iteration bench harness (dev-only — never reaches published
# consumers). See benches/region_bench.rs.
bench-scale-tool = "0.1"
# Capacity telemetry (dev-only). `slotmap::SlotMap` is not one of captrack's
# natively-wrapped collection types, so tests/captrack_probe.rs drives its
# public registry API (record_creation/record_sample) directly rather than
# via the CapInspect trait/macros. `telemetry` is required for the registry
# module to exist at all.
captrack = { version = "=0.1.1", features = ["telemetry"] }
# Note: captrack is intentionally heavy (proc macro, ctor background thread,
# supply-chain side effects) but accepted as a known cost for one ignored
# diagnostic probe. This workspace's .cargo/config.toml sets CAPTRACK_AUTODUMP=0
# to mitigate autodump side effects — that config does NOT travel with the
# published crate tarball, so external consumers who build tests will see the
# ctor run at process startup. Version is exact-pinned to avoid silent
# side-effect profile drift on future captrack releases.

[[bench]]
name = "region_bench"
harness = false
required-features = ["std"]

[[bench]]
name = "region_new_contention_gate"
harness = false
required-features = ["std"]

[[bench]]
name = "r828_dense_iteration_probe"
harness = false
required-features = ["std"]

[[bench]]
name = "r828_batch_guard_probe"
harness = false
required-features = ["std"]

[[bench]]
name = "r828_drop_outside_lock_probe"
harness = false
required-features = ["std"]

[[example]]
name = "contended_reads"
required-features = ["std"]
